def solve(x, y):
	res = 0
	r = 1
	while r * (r + 2) <= x:
		res += max(0, min(y, x // r - 1) - r)
		r += 1
	return res
t = int(input())
for _ in range(t):
	(x, y) = map(int, input().split())
	print(solve(x, y))
