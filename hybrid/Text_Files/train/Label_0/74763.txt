import sys
input = sys.stdin.readline
(n, k) = map(int, input().split())
a = [int(i) for i in input().split()]
acc = [a.pop()]
while a:
	acc.append(a.pop() + acc[-1])
s1 = acc.pop()
acc.sort()
ans = sum(acc[n - k:]) + s1
print(ans)
