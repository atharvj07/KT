from fractions import Fraction

def pairs(n):
	k = n * (n - 1)
	m = Fraction(1, 2)
	return k * m
import math
(a, b) = map(int, input().split())
kmax = pairs(a + 1 - b)
ji = math.floor(a / b)
yu = a - ji * b
kmin = pairs(ji) * (b - yu) + pairs(ji + 1) * yu
print(int(kmin), int(kmax))
