import math
t = int(input())

def getSum(p):
	sum = 0
	for digit in str(p):
		sum += int(digit)
	return sum

def sum_gcd(e, f):
	return math.gcd(e, f)
for _ in range(t):
	n = int(input())
	chk = sum_gcd(n, getSum(n))
	while chk == 1:
		n += 1
		chk = sum_gcd(n, getSum(n))
	print(n)
