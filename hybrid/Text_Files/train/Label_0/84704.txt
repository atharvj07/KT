import sys

def check(a, b, x, y):
	if y == -1 or b == -1:
		return True
	if b < x:
		return True
	if a > y:
		return True
	if a < x and b > y:
		return True
	return False
(n, q) = list(map(int, input().split(' ')))
array = list(map(int, input().split(' ')))
first = [n + 1 for i in range(q + 1)]
last = [-1 for i in range(q + 1)]
first_after = [n + 1 for i in range(q + 1)]
b = True
for i in range(n):
	j = array[i]
	first[j] = min(i, first[j])
	if j > 1:
		if first[j - 1] < last[j] and last[j - 1] > last[j]:
			b = False
	last[j] = max(i, last[j])
for i in range(1, q):
	b &= check(first[i], last[i], first[i + 1], last[i + 1])
if not b or first[0] == first[q]:
	print('NO')
else:
	print('YES')
	if last[q] == -1:
		array[last[0]] = q
	x = iter(array)
	for i in range(1, n):
		if array[i] == 0:
			array[i] = array[i - 1]
		j = n - 1 - i
		if array[j] == 0:
			array[j] = array[j + 1]
	print(' '.join(map(str, array)))
