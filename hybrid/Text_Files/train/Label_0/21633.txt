#include <bits/stdc++.h>
using namespace std;
int ar[110];
int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) cin >> ar[i];
  sort(ar, ar + n);
  cout << ar[n - 1];
  for (int i = 1; i < n - 1; i++) {
    cout << " ";
    cout << ar[i];
  }
  cout << " " << ar[0] << endl;
}
