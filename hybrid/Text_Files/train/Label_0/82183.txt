from sys import stdin, stdout

def pcol2(s, i):
	write = stdout.write
	for j in range(len(s)):
		write(s[j][i] * 2)
	write('\n')
next(stdin)
s = stdin.read().split()
for i in range(len(s[0])):
	pcol2(s, i)
	pcol2(s, i)
