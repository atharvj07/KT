T = int(input())
for t in range(1, T + 1):
	N = int(input())
	A = [int(s) for s in input().split()]
	sumA = sum(A)
	maxA = max(A)
	if sumA % 2 == 1 or 2 * maxA > sumA:
		print('T')
	else:
		print('HL')
