count = 1
min_d = 100000000000000000
n = int(input())
data = list(map(int, input().split()))
data.sort()
prev = data[0]
for el in data:
	if prev == el:
		continue
	if el - prev < min_d:
		min_d = el - prev
		count = 1
	elif el - prev == min_d:
		count += 1
	prev = el
print(min_d, count)
