#include <bits/stdc++.h>
using namespace std;
int n, i, x, y, r, OK;
int main() {
  cin >> n;
  cin >> x;
  cin >> y;
  r = y - x;
  OK = 0;
  for (i = 3; i <= n; i++) {
    x = y;
    cin >> y;
    if (y - x != r) OK = 1;
  }
  if (OK == 0)
    cout << y + r;
  else
    cout << y;
  return 0;
}
