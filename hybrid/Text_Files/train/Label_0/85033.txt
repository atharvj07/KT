import sys
for test in range(int(input())):
	n = int(input())
	s = input()
	zac = s[0]
	p = zac
	c = []
	d = []
	np = 0
	for i in range(1, len(s)):
		if s[i] == p:
			np += 1
		else:
			if p != zac:
				c.append(np)
			p = s[i]
			np = 0
	if p != zac:
		c.append(np)
	print(sum(c))
