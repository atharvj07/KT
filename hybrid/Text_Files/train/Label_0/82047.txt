for _ in range(int(input())):
	n = int(input())
	st = input()
	left = -1
	right = -1
	for i in range(n):
		if st[i] == '>':
			if right == -1:
				right = i
		else:
			left = i + 1
	print(max(min(right, n - left), 0))
