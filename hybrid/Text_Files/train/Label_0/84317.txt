from collections import defaultdict
for _ in range(int(input())):
	s = input()
	n = len(s)
	if s.count(s[0]) == n:
		print(-1)
	elif s != s[::-1]:
		print(s)
	else:
		d = defaultdict(lambda : 0)
		for c in s:
			d[c] += 1
		for c in d:
			for _ in range(d[c]):
				print(c, end='')
		print()
