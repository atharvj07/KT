from sys import stdout, stdin
from collections import defaultdict, deque
import math
t = int(stdin.readline())
for _ in range(t):
	(n, k) = map(int, stdin.readline().split())
	flag = False
	for i in range(n):
		(x, y) = map(int, stdin.readline().split())
		(z, zz) = map(int, stdin.readline().split())
		if flag == False:
			if y == z:
				flag = True
	if k % 2 == 0:
		if flag:
			print('YES')
		else:
			print('NO')
	else:
		print('NO')
