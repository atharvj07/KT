#include <bits/stdc++.h>

using namespace std;

typedef long long LL;
#define repp(i,a,b) for(int i = (int)(a) ; i < (int)(b) ; ++i)
#define repm(i,a,b) for(int i = (int)(a) ; i > (int)(b) ; --i)

int main(){
	int N; cin >> N;
	cout << N*(N-1)/2-N/2 << endl;
	int z = (N%2==1?N:N+1);
	repp(i,1,N+1) repp(j,i+1,N+1) if(i+j != z) cout << i << ' ' << j << endl;
	return 0;
}
