/**
 * Created by Anna on 08.02.2015.
 */


import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.StringTokenizer;

public class TaskBTest {

    StringTokenizer st;
    BufferedReader in;
    PrintWriter out;

    public static void main(String[] args) throws NumberFormatException,
            IOException {
        TaskBTest solver = new TaskBTest();
        solver.open();
        solver.solve();
        solver.close();
    }

    public void open() throws IOException {
        in = new BufferedReader(new InputStreamReader(System.in));
        out = new PrintWriter(System.out);
        //in = new BufferedReader(new FileReader("input.txt"));
        //out = new PrintWriter(new FileWriter("output.txt"));
    }

    public String nextToken() throws IOException {
        while (st == null || !st.hasMoreTokens()) {
            String line = in.readLine();
            if (line == null)
                return null;
            st = new StringTokenizer(line);
        }
        return st.nextToken();
    }

    public int nextInt() throws NumberFormatException, IOException {
        return Integer.parseInt(nextToken());
    }

    public long nextLong() throws NumberFormatException, IOException {
        return Long.parseLong(nextToken());
    }

    public double nextDouble() throws NumberFormatException, IOException {
        return Double.parseDouble(nextToken());
    }

    boolean hasMoreTokens() throws IOException {
        while (st == null || !st.hasMoreTokens()) {
            String line = in.readLine();
            if (line == null)
                return false;
            st = new StringTokenizer(line);
        }
        return true;
    }

    public void solve() throws NumberFormatException, IOException {
        int n = nextInt();
        int[] p = new int[n];
        Arrays.fill(p, 0);
        int indLeft = 0;
        int indRight = n - 1;
        long m = nextLong();
        for (int i = 1; i <= n; i++) {
            long half = 1L << (n - i - 1);
            if (m <= half)
                p[indLeft++] = i;
            else {
                p[indRight--] = i;
                m -= half;
            }
        }
        printP(p);

//        cntPerm = 1;
//        for (int i = 0; i < n; i++) {
//            p[i] = i + 1;
//        }
//
//        maxSum = countSum(p);
//
//        while (true) {
//            printP(p);
//            if (change(p)) cntPerm++;
//            else break;
//        }
//
//        out.println(cntPerm + "   good: " + cntGoodPerm + "   maxSum: " + maxSum);
    }

//    int maxSum;
//    long cntPerm;
//    long cntGoodPerm = 0;
//
//    boolean change(int[] p) {
//        for (int i = p.length - 1; i >= 1; i--) {
//            if (p[i] < p[i - 1]) continue;
//            int ind = i;
//            for (int j = p.length - 1; j > i; j--) {
//                if (p[j] > p[i - 1]) {
//                    ind = j;
//                    break;
//                }
//            }
//            int tmp = p[i - 1];
//            p[i - 1] = p[ind];
//            p[ind] = tmp;
//            ArrayList<Integer> buf = new ArrayList<Integer>();
//            for (int j = i; j < p.length; j++) {
//                buf.add(p[j]);
//            }
//            Collections.sort(buf);
//            for (int j = 0; j < buf.size(); j++) {
//                p[i + j] = buf.get(j);
//            }
//            return true;
//        }
//
//        return false;
//    }

    //    int countSum(int[] p) {
//        int result = 0;
//        for (int i = 0; i < p.length; i++) {
//            for (int j = i; j < p.length; j++) {
//                int min = 1000000;
//                for (int k = i; k <= j; k++) {
//                    min = Math.min(p[k], min);
//                }
//                result += min;
//            }
//        }
//        return result;
//    }
//
//
    void printP(int[] p) {
//        if (countSum(p) != maxSum) return;
//        cntGoodPerm++;
        for (int i = 0; i < p.length; i++) {
            out.print(p[i] + " ");
        }
        out.println();
//        out.println("    << " + cntPerm);
    }

    public void close() {
        out.flush();
        out.close();
    }
}
