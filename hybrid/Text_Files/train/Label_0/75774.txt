(n, m) = map(int, input().split())
maxi = max(n, m)
mini = n + m - maxi
count = 0
while not (mini == 0 or mini == maxi == 1):
	maxi -= 2
	mini -= 1
	if maxi < mini:
		(maxi, mini) = (mini, maxi)
	count += 1
print(count)
