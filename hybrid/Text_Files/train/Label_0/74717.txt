orig = input()
s = input()
d = {}
z = True
same = set()
for i in range(len(orig)):
	if orig[i] != s[i]:
		if orig[i] in same or s[i] in same:
			z = False
			break
		elif d.get(orig[i]) is None and d.get(s[i]) is None:
			d[orig[i]] = s[i]
			d[s[i]] = orig[i]
		elif d.get(orig[i]) == s[i] and d.get(s[i]) == orig[i]:
			pass
		else:
			z = False
			break
	elif not d.get(orig[i]) is None or not d.get(s[i]) is None:
		z = False
		break
	elif orig[i] in same:
		pass
	else:
		same.add(orig[i])
if not z:
	print(-1)
else:
	keys = list(d.keys())
	print(len(keys) // 2)
	b = set()
	for i in keys:
		if i not in b and d[i] not in b:
			b.add(i)
			b.add(d[i])
			print(i, d[i])
