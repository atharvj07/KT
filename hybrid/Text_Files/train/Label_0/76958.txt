from collections import defaultdict
t = int(input())
for _ in range(t):
	n = int(input())
	a = []
	for i in range(n):
		(x, y) = map(int, input().split())
		a.append([y - x, x, y])
	a.sort(reverse=True)
	ds = defaultdict(list)
	de = defaultdict(list)
	truth = [False] * n
	(dist, x, y) = a.pop(0)
	q = [[x, y]]
	for i in a:
		ds[i[1]].append(i[2])
		de[i[2]].append(i[1])
	for i in range(n):
		(x, y) = q.pop(0)
		if ds[x] == [] and de[y] == []:
			print(x, y, x)
		elif ds[x] == []:
			print(x, y, x)
			q.append([de[y][0], y])
			m = de[y].pop(0)
			ds[m].pop(0)
		elif de[y] == []:
			print(x, y, y)
			q.append([x, ds[x][0]])
			m = ds[x].pop(0)
			de[m].pop(0)
		else:
			q.append([x, ds[x][0]])
			q.append([de[y][0], y])
			print(x, y, (ds[x][0] + de[y][0]) // 2)
			m1 = ds[x].pop(0)
			de[m1].pop(0)
			m2 = de[y].pop(0)
			ds[m2].pop(0)
