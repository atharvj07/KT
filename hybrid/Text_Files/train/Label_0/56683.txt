
import java.util.Scanner;
import java.lang.StringBuilder;
import java.math.BigInteger;
public class Test5 {
    static BigInteger zero = big(0);
    static BigInteger one = big(1);
    static BigInteger two = big(2);
    
    public static BigInteger big(Integer x)  {
        return new BigInteger(x.toString());
    }
    public static void main(String Args[])   {
        Scanner sc = new Scanner(System.in);
        int r = sc.nextInt();
        int s = sc.nextInt();
        String[] s0 = new String[r];
        Boolean x1 = true;
        sc.nextLine();
        for (int i = 0; i < r; i++)  {
            s0[i] = sc.nextLine();
            for (int j = 0; j < s; j++)   {
                if (s0[i].charAt(j) == 'W' && i >0 && s0[i - 1].charAt(j) == 'S') x1 = false;
                if (s0[i].charAt(j) == 'W' && j >0 && s0[i].charAt(j - 1) == 'S') x1 = false;
                if (s0[i].charAt(j) == 'S' && i >0 && s0[i - 1].charAt(j) == 'W') x1 = false;
                if (s0[i].charAt(j) == 'S' && j >0 && s0[i].charAt(j - 1) == 'W') x1 = false;
            }     
        }    
        if (!x1) System.out.println("No");
        else  {
            System.out.println("Yes");
            StringBuilder x = new StringBuilder();
            for (int i = 0; i <r; i++)  {
                for (int j = 0; j <s; j++)  {
                if (s0[i].charAt(j) == '.') x.append('D');
                else x.append(s0[i].charAt(j));
                } 
                x.append("\n");
            }    
            System.out.println(x);
        }    
        
    }    
}
