from fractions import gcd
s = input()
(t, w, b) = map(int, s.split())
lcm = w * b // gcd(w, b)
if w > b:
	c = w
	w = b
	b = c
cnt = t // lcm
if cnt > 0:
	good = max(0, cnt * w - 1) + min(w, t % lcm + 1)
else:
	good = min(w - 1, t)
print(str(good // gcd(good, t)) + '/' + str(t // gcd(good, t)))
