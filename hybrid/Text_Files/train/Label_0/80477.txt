(r, c) = [int(s) for s in input().split(' ')]
rows = []
r_cnt = 0
c_cnt = 0
for i in range(r):
	row = input()
	if row == '.' * c:
		r_cnt += 1
	rows.append(row)
for j in range(c):
	col = [rows[k][j] for k in range(r)]
	if col == ['.'] * r:
		c_cnt += 1
print(r_cnt * c + c_cnt * (r - r_cnt))
