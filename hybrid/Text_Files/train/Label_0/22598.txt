#include <bits/stdc++.h>
int e, ans;
int a[11];
int sqr(int x) { return x * x; }
int main() {
  for (int i = 0; i < 6; i++) scanf("%d", &a[i]);
  e = a[0] + a[1] + a[2];
  ans = sqr(e) - sqr(a[0]) - sqr(a[2]) - sqr(a[4]);
  printf("%d\n", ans);
  return 0;
}
