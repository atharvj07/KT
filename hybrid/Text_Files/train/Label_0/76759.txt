n = int(input())
graph = list(map(int, input().split()))
for i in range(n):
	graph[i] -= 1
for i in range(n):
	cur = i
	visited = [0] * n
	visited[cur] = 1
	while visited[cur] < 2:
		cur = graph[cur]
		visited[cur] += 1
	print(cur + 1, end=' ')
