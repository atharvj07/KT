#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  long q;
  long long l, r, s = 0, x = 0, y = 0;
  cin >> q;
  while (q--) {
    cin >> l >> r;
    r++;
    if (!(r & 1)) r *= -1;
    r /= 2;
    if (!(l & 1)) l *= -1;
    l /= 2;
    cout << r - l << "\n";
  }
}
