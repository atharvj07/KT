t = int(input())
while t:
	s = input()
	s = sorted(list(s))
	flag = 0
	for i in range(len(s) - 1):
		if s.count(s[i]) > 1:
			flag = 1
			break
		if chr(ord(s[i]) + 1) not in s:
			flag = 1
			break
	if flag == 0 or len(s) == 1:
		print('Yes')
	else:
		print('No')
	t = t - 1
