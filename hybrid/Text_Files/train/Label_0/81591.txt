import random

def gcd(a, b):
	if a == 0:
		return b
	return gcd(b % a, a)

def lcm(a, b):
	return a * b / gcd(a, b)
for _ in range(int(input())):
	n = int(input())
	k = n // 4
	if n % 4:
		k += 1
	print('9' * (n - k) + '8' * k)
