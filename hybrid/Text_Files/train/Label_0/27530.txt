#include <bits/stdc++.h>
using namespace std;
int main() {
  double c[5005];
  int n, m;
  double a, b;
  cin >> n >> m;
  for (int i = 0; i < n; i++) {
    cin >> a >> b;
    c[i] = a / b;
  }
  sort(c, c + n);
  double cost = c[0] * m;
  printf("%.8lf\n", cost);
  return 0;
}
