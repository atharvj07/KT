import math
(n, k) = map(int, input().split())
dp = [1, n * (n - 1) // 2, n * (n - 1) * (n - 2) * 2 // 6, n * (n - 1) * (n - 2) * (n - 3) * 9 // 24]
ans = 0
for i in range(0, k):
	ans += dp[i]
print(ans)
