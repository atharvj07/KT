t = int(input())
for i in range(t):
	(n, x) = map(int, input().split())
	a = sorted(list(map(int, input().split())), reverse=True)
	total = 0
	output = 0
	for i in range(n):
		total += a[i]
		if total / (i + 1) >= x:
			output += 1
		else:
			break
	print(output)
