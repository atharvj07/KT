from sys import stdin, stdout
q = int(input())
for i in range(q):
	(x, y, n) = list(map(int, stdin.readline().split()))
	rem = n % x
	if rem > y:
		n -= rem - y
	elif rem < y:
		n -= rem
		n -= x - y
	print(n)
