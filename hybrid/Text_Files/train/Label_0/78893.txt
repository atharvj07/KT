for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	d = {}
	for i in range(n):
		x = a[i] - i
		if x in d:
			d[x] += 1
		else:
			d[x] = 1
	count = 0
	for i in d:
		count += d[i] * (d[i] - 1) // 2
	print(count)
