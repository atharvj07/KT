import math as mt
for x in range(int(input())):
	n = int(input())
	l = list(map(int, input().split()))
	ans = 0
	tmp = -1
	flag = 1
	for i in range(n):
		if l[i] != i + 1:
			ans += 1
			if tmp != -1 and i > tmp + 1:
				flag = 0
			tmp = i
	if ans == 0:
		print(0)
	else:
		print(2 - flag)
