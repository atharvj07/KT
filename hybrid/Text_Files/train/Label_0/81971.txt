n = int(input())
for i in range(n):
	num = str(input())
	zerocount = 0
	evencount = 0
	summ = 0
	for j in num:
		summ = summ + int(j)
		if int(j) % 2 == 0:
			evencount += 1
		if int(j) == 0:
			zerocount += 1
	if evencount >= 2 and zerocount >= 1 and (summ % 3 == 0):
		print('red')
	else:
		print('cyan')
