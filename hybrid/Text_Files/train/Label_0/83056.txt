def getInts():
	return [int(s) for s in input().split()]

def getInt():
	return int(input())

def getStrs():
	return [s for s in input().split()]

def getStr():
	return input()

def listStr():
	return list(input())
import collections as col
import sys
sys.setrecursionlimit(10 ** 5 + 5)
from types import GeneratorType

def solve():

	def bootstrap(f, stack=[]):

		def wrappedfunc(*args, **kwargs):
			if stack:
				return f(*args, **kwargs)
			else:
				to = f(*args, **kwargs)
				while True:
					if type(to) is GeneratorType:
						stack.append(to)
						to = next(to)
					else:
						stack.pop()
						if not stack:
							break
						to = stack[-1].send(to)
				return to
		return wrappedfunc

	@bootstrap
	def numberOfNodes(s, e):
		count1[s] = 1
		for u in graph[s]:
			if u == e:
				continue
			yield numberOfNodes(u, s)
			count1[s] += count1[u]
		yield
	N = getInt()
	count1 = [0] * (N + 1)
	graph = col.defaultdict(set)
	for n in range(N - 1):
		(x, y) = getInts()
		graph[y].add(x)
		graph[x].add(y)
	max_subtree = 0
	max_neighbour = 0
	root = 1
	numberOfNodes(1, 0)
	for neighbour in graph[root]:
		c = count1[neighbour]
		if c > max_subtree:
			max_subtree = count1[neighbour]
			max_neighbour = neighbour
	while max_subtree > N / 2:
		prev_root = root
		root = max_neighbour
		max_subtree = N - max_subtree
		max_neighbour = prev_root
		for neighbour in graph[root]:
			if neighbour != prev_root:
				c = count1[neighbour]
				if c > max_subtree:
					max_subtree = count1[neighbour]
					max_neighbour = neighbour
	if max_subtree < N / 2:
		for x in graph[root]:
			print(root, x)
			print(root, x)
			return
	for nn in graph[max_neighbour]:
		if nn != root:
			print(max_neighbour, nn)
			print(root, nn)
			return
T = getInt()
for t in range(T):
	solve()
