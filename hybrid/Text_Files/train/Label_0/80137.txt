f = lambda : input()
(m, t, r) = list(map(int, f().split()))
w = list(map(int, f().split()))
candles_at_second = [0] * 301
light_at = []

def light(time):
	global candles_at_second, num_candles, t, light_at
	light_at.append(time)
	for i in range(time + 1, time + t + 1):
		if 0 < i:
			if i > 300:
				break
			candles_at_second[i] += 1
possible = True
for i in w:
	for j in range(i - r + candles_at_second[i], i):
		if j in light_at:
			possible = False
			break
		light(j)
if possible:
	possible2 = True
	for i in w:
		if candles_at_second[i] != r:
			possible2 = False
			break
	if possible2:
		print(len(light_at))
	else:
		print(-1)
else:
	print(-1)
