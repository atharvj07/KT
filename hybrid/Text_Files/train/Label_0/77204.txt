import math

def main():
	(n, m) = map(int, input().split())
	inc = 0
	x = m
	for a in range(1, x + 1):
		for b in range(1, x + 1):
			if (a ** 2 + b ** 2) % m == 0:
				inc += ((n - a) // m + 1) * ((n - b) // m + 1)
	print(inc)
main()
