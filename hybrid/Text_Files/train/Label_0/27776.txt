#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, c = 0, i;
  cin >> n;
  if (n == 2) {
    cout << 1;
    exit(0);
  }
  for (i = 1; n > 0; i++) {
    if (i % 2 == 0)
      n -= 2;
    else
      n -= 1;
    if (n >= 0) c++;
  }
  cout << c;
  return 0;
}
