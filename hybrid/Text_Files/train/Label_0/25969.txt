#include <iostream>
#include <cstring>
using namespace std;

int main(void){
  char s[210], p[110];
  cin >> s >> p;
  strcpy(s+strlen(s), s); // sの直後にsをつなげる
  cout << (strstr(s, p)?"Yes":"No") << endl;
    
  return 0;
}

