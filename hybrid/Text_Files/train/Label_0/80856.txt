import math
for _ in range(int(input())):
	n = int(input())
	step = math.ceil(math.log(n, 3))
	ans = 3 ** step
	i = 1
	new_sum = 0
	while i <= step:
		new_sum += 3 ** (step - i)
		if new_sum >= n:
			ans = new_sum
			new_sum -= 3 ** (step - i)
		i += 1
	print(ans)
