#include <bits/stdc++.h>
int main() {
  int n, m;
  std::cin >> n >> m;
  double result = 0;
  for (double i = 1; i <= n; ++i)
    result += i * pow(i / n, m) - i * (pow((i - 1) / n, m));
  std::cout << std::setprecision(10);
  std::cout << std::fixed;
  std::cout << result;
}
