def solve(arr):
	ans = 0
	sorted_arr = list(sorted(arr))
	for i in range(0, len(sorted_arr), 2):
		ans += sorted_arr[i + 1] - sorted_arr[i]
	return ans

def main():
	n = int(input())
	arr = list(map(int, input().split()))
	print(solve(arr))
main()
