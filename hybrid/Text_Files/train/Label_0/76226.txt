n = int(input())
a = [int(_) for _ in input().split()]
v = [a[0]]
max_v = 0
for i in range(1, n):
	max_v = max(max_v, a[i])
	if v and v[-1] == a[i]:
		v.pop()
		if v and v[-1] < a[i]:
			print('NO')
			exit()
	else:
		v.append(a[i])
print('NO' if len(v) > 1 or (v and v[0] < max_v) else 'YES')
