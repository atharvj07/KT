def main():
	(l, c) = ([], 0)
	for _ in range(int(input())):
		(a, b) = map(int, input().split())
		l.append(a * 2)
		l.append(b * 2 + 1)
	l.sort()
	for a in l:
		if a & 1:
			c -= 1
		else:
			c += 1
			if c > 2:
				print('NO')
				break
	else:
		print('YES')
main()
