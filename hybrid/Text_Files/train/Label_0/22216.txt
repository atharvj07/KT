#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  int a[n][2];
  int i, j, x = 0;
  for (i = 0; i < n; i++) {
    for (j = 0; j < 2; j++) {
      cin >> a[i][j];
    }
    if (a[i][0] + 1 < a[i][1]) {
      x++;
    }
  }
  cout << x;
  return 0;
}
