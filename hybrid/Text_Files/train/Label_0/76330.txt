(n, k) = [int(y) for y in input().split()]
pn = [int(y) for y in input().split()]
p = [0 for y in range(100)]
kmpl = 0
for i in pn:
	p[i - 1] += 1
maxk = 0
import math
for i in p:
	if i > 0:
		kmpl += 1
	maxk = max(maxk, math.ceil(i / k))
print(maxk * kmpl * k - n)
