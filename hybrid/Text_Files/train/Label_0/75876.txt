from operator import itemgetter

def main():
	n = int(input())
	a = [(int(ai), int(bi)) for _ in range(n) for (ai, bi) in (input().split(),)]
	a.sort(key=itemgetter(0))
	p = a[0][1]
	for (_, bi) in a:
		if bi < p:
			print('Happy Alex')
			return
		p = bi
	print('Poor Alex')
main()
