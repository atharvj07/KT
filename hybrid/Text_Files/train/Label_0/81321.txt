(n, k) = map(int, input().strip().split())
ids = list(map(int, input().strip().split()))
if k == 1:
	print(ids[0])
else:
	count = 1
	for step in range(1, 100000):
		if count + step > k:
			break
		count += step
	print(ids[k - count])
