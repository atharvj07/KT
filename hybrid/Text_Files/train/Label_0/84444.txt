n = int(input())
problem = sum([int(x) for x in input().split()])
for _ in range(int(input())):
	(l, r) = [int(x) for x in input().split()]
	if problem <= r:
		if problem <= l:
			print(l)
		else:
			print(problem)
		break
else:
	print(-1)
