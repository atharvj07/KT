#include <iostream>

using namespace std;
int b[110];
int main(){
	int i,n;
	cin >> n;
	for(i=0;i<n - 1;i++){
		cin >> b[i];
	}
	int ans = b[n - 2];
	for(i=0;i<n - 1;i++){
		if(i==0) ans += b[i];
		else ans += min(b[i - 1],b[i]);
	}
	cout << ans << endl;
}