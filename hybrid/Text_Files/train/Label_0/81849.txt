for i in range(int(input())):
	x = int(input())
	arr = list(map(int, input().split()))
	brr = []
	for i in range(x):
		if arr[i] > i + 1:
			m = arr[i] - (i + 1)
			brr.append(m)
	if brr:
		print(max(brr))
	if not brr:
		print(0)
