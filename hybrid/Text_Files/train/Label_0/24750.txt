#include <bits/stdc++.h>
using namespace std;
int main(void){
    double n;
    std::deque<double> deq;
    while (cin>>n) {
        deq.emplace_back(n);
    }
    sort(deq.begin(),deq.end());
    cout << deq.back()-deq.front() << endl;
}

