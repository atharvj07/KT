k = int(input())
q = input()
used = set([])
strings = []
for i in range(len(q)):
	if len(strings) < k and q[i] not in used:
		strings += [q[i]]
		used.add(q[i])
	else:
		strings[-1] += q[i]
if len(strings) == k:
	print('YES')
	for s in strings:
		print(s)
else:
	print('NO')
