#include <bits/stdc++.h>

using namespace std;

int main() {
	string s, k = "keyence";
	cin >> s;
	for (int i = 0; i < 8; i++) {
		if (s.substr(0, i) == k.substr(0, i) && s.substr(s.length() + i - 7) == k.substr(i)) {
			cout << "YES" << endl;
			return 0;
		}
	}
	cout << "NO" << endl;
	return 0;
}
