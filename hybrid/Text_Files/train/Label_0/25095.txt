#include <bits/stdc++.h>
int main() {
  int l;
  scanf("%d", &l);
  char str[10000];
  scanf("%s", &str);
  int i;
  int num = 0;
  for (i = 0; i < l; i++) {
    if (str[i] == '1')
      num++;
    else
      num *= 10;
  }
  printf("%d\n", num);
}
