from math import floor
for _ in range(int(input())):
	(d, k) = map(int, input().split())
	c2 = d ** 2 / (2 * k ** 2)
	c = int(pow(c2, 0.5))
	t2 = (c ** 2 + (c + 1) ** 2) * k ** 2
	if t2 <= d ** 2:
		print('Ashish')
	else:
		print('Utkarsh')
