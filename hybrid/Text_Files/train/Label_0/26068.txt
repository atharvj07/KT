#include <iostream>
#include <map>
using namespace std;
map<int,bool> m;
int n, t,i,ans;
int main()
{
	cin>>n;
	for (i=0;i<n;i++)
	{
		cin>>t;
		if(m[t])m[t]=0,ans--;
		else m[t]=1,ans++;
	}
	cout << ans<<"\n";
}