def f():
	(n, x, y) = [int(s) for s in input().split()]
	bar = x + y
	if bar >= n + 1:
		bad = n
	else:
		bad = bar - 1
	if bar <= n:
		good = 1
	elif bar == 2 * n:
		good = n
	else:
		good = bar + 1 - n
	print('{} {}'.format(good, bad))
T = int(input())
for t in range(T):
	f()
