#include <bits/stdc++.h>
using namespace std;
int main() {
  int i, n, k = 0, j, t = 0;
  cin >> n;
  for (i = 1; k <= n; i++) {
    k += i;
    if (k <= n) t++;
  }
  cout << t << endl;
  for (i = 1; i < t; i++) cout << i << ' ';
  cout << n - (i * (i - 1) / 2);
  return 0;
}
