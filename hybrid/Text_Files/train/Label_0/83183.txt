import math
for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	mn = min(a)
	if a.count(mn) == len(a):
		print(-1)
	else:
		k = 0
		for i in a:
			k = math.gcd(k, i - mn)
		print(k)
