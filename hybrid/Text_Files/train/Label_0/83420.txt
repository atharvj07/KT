t = int(input())
for z in range(t):
	(n, m) = map(int, input().split())
	if n == 1:
		print(0)
		continue
	x = m // (n // 2)
	xt = m % (n // 2)
	if n % 2 == 0:
		ans = x * (n // 2) * 2
		if n > 2:
			ans += xt * 2
		else:
			ans += xt - x
	else:
		ans = x * (n // 2) * 2
		ans += 2 * xt
	print(ans)
