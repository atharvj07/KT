import math
test = int(input())
for i in range(test):
	num = int(input())
	div = float(num / 3)
	a = math.ceil(div)
	b = math.floor(div)
	if a + 2 * b != num:
		a = math.floor(div)
		b = math.ceil(div)
	print(a, b)
