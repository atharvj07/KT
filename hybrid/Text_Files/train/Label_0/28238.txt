#include <stdio.h>
int a[100];
int main()
{
	int n,m,sum,i,j,k;
	while(~scanf("%d",&n))
	{
		for(i=1;i<=n;i++) scanf("%d",&a[i]);
		sum=0;
		for(i=1;i<=n;i++)
		for(j=i+1;j<=n;j++) sum+=(a[i]*a[j]);
		printf("%d\n",sum);
	}
	return 0;
}