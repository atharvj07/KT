(l, r, a) = input().split()
l = int(l)
r = int(r)
a = int(a)
counter = 0
required = 0
if l > r:
	required = l - r
	counter += r
	if a > required:
		a = a - required
		counter = 2 * l
		counter += int(a / 2) * 2
	else:
		counter += a
		counter *= 2
else:
	required = r - l
	counter += l
	if a > required:
		a = a - required
		counter = 2 * r
		counter += int(a / 2) * 2
	else:
		counter += a
		counter *= 2
print(counter)
