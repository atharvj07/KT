import sys
input = sys.stdin.readline
from collections import defaultdict

def main():
	n = int(input())
	b = list(map(int, input().split()))
	m = 0
	g = defaultdict(int)
	for i in range(n):
		g[b[i] - i] += b[i]
		m = max(m, g[b[i] - i])
	print(m)
main()
