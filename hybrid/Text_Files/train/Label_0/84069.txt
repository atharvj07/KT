def gcd(a, b):
	if a == 0:
		return b
	elif b == 0:
		return a
	else:
		return gcd(b, a % b)
t = int(input())
for i in range(t):
	n = int(input())
	if n % 2 == 0:
		print(gcd(n, n // 2))
	else:
		n1 = n - 1
		print(gcd(n1, n1 // 2))
