#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  string a, b;
  cin >> a >> b;
  int idx = 0;
  for (int i = 0; i < b.length(); i++) {
    if (b[i] == a[idx]) {
      idx++;
    }
  }
  cout << idx + 1;
}
