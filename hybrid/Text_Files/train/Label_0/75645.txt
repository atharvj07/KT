(n, d) = map(int, input().split())
l = [input() for _ in range(n + 1)]
pw = l[-1]
red = len([x for x in l if len(x) < len(pw)])
same = len([x for x in l if len(x) <= len(pw)]) - 2
print(red + red // d * 5 + 1, same + same // d * 5 + 1)
