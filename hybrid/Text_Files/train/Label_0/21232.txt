#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
	string S;
	cin >> S;
	int result = S.size();

	for(int i = 1;i < S.size();i++)
	{
		if(S[i] != S[i - 1])
		{
			result = min(result , max(i,(int)S.size() - i));
		}
	}

	cout << result << endl;
	return 0;
}
