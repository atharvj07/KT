#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long int x1, x2, y1, y2, ans;
    cin >> x1 >> x2 >> y1 >> y2;
    ans = abs((x1 - y1) * (x2 - y2)) + 1;
    cout << ans << endl;
  }
}
