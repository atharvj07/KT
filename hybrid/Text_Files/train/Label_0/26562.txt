#include <bits/stdc++.h>
using namespace std;
const long long N = 1e5 + 5, MOD = 1e9 + 7;
long long n;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  cin >> n;
  if (n == 1) {
    cout << -1 << endl;
    return 0;
  }
  cout << n << " " << n + 1 << " " << n * (n + 1) << endl;
  return 0;
}
