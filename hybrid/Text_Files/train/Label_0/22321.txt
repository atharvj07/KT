#include <bits/stdc++.h>
using namespace std;
int num[200010];
int main() {
  int n, tmp;
  set<int> s[4];
  set<int>::iterator it;
  scanf("%d", &n);
  for (int i = 0; i < n; i++) {
    scanf("%d", &num[i]);
  }
  for (int i = 0; i < n; i++) {
    scanf("%d", &tmp);
    s[tmp].insert(num[i]);
  }
  for (int i = 0; i < n; i++) {
    scanf("%d", &tmp);
    s[tmp].insert(num[i]);
  }
  int m;
  scanf("%d", &m);
  for (int i = 0; i < m; i++) {
    scanf("%d", &tmp);
    if (!s[tmp].empty()) {
      it = s[tmp].begin();
      int ans = *it;
      printf("%d ", ans);
      s[1].erase(ans);
      s[2].erase(ans);
      s[3].erase(ans);
    } else {
      printf("-1 ");
    }
  }
}
