#include <bits/stdc++.h>
using namespace std;
int a[500];
int b[500];
set<int> st;
int main() {
  ios_base::sync_with_stdio(0);
  long long n, k, m;
  cin >> m >> n;
  m--;
  vector<pair<int, int> > v;
  for (int i = 0; i < n; i++) {
    int l, k;
    cin >> k >> l;
    k--;
    l--;
    v.push_back(make_pair(k, l));
  }
  for (int i = 1; i <= 100; i++) {
    int f = 0;
    for (int j = 0; j < n; j++) {
      if (v[j].first / i != v[j].second) {
        a[i] = 1;
      }
    }
  }
  for (int i = 1; i <= 100; i++)
    if (a[i] == 0) {
      st.insert(m / i);
    }
  if (st.size() > 1)
    cout << -1;
  else
    cout << (*st.begin()) + 1;
}
