#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x;
  cin >> n >> x;
  int count = 0;
  for (int i = 0; i < n; i++) {
    int temp;
    cin >> temp;
    count += temp;
  }
  if (x == count + n - 1) {
    cout << "YES" << endl;
  } else {
    cout << "NO" << endl;
  }
}
