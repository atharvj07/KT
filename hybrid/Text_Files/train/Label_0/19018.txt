#include <bits/stdc++.h>
int main() {
  long long a, b, c = 2, d, e, f, g, h, i, j, n;
  scanf("%lld", &n);
  a = sqrt(n);
  for (i = 0; i <= a; i++) {
    b = sqrt(n);
    for (j = c; j <= (c + b); j++) {
      d = 0;
      if (n % j == 0) {
        n = n / j;
        if (n == 1) {
          printf("%lld", j);
        } else {
          printf("%lld", j);
        }
        c = j;
        d++;
        break;
      }
    }
    if (d == 0) {
      printf("%lld ", n);
      n = 1;
    }
    if (n == 1) {
      break;
    }
  }
}
