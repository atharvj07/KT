n = int(input())
a = list(map(int, input().split()))
b = {}
c = 0
max_now = 0
for i in range(n):
	t = a[i] - i - 1
	if t not in b:
		b[t] = a[i]
	else:
		b[t] += a[i]
	if b[t] > max_now:
		max_now = b[t]
print(max_now)
