__author__ = 'Alexander'
import sys
(l1, r1, l2, r2, k) = map(int, sys.stdin.readline().split())
a = max(l1, l2)
b = min(r1, r2)
length = max(0, b - a + 1)
if length != 0 and k >= a and (k <= b):
	length -= 1
sys.stdout.write(str(length))
