import sys
import math
from collections import defaultdict, Counter
t = int(input())
for i in range(t):
	n = int(input())
	a = list(map(int, input().split()))
	ans = 0
	for j in range(n - 1, 0, -1):
		if a[j - 1] > a[j]:
			ans += a[j - 1] - a[j]
	print(ans)
