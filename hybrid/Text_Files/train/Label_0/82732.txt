from math import floor
(a, b) = map(int, input().split(' '))
n = b
p = 10 ** 9 + 7
n = n % (p - 1)
result = 1
power = 2
while n:
	if n % 2:
		result = result * power % p
	power = power * power % p
	n = n // 2
result = (result + p - 1) % p

def powermod(base, exponent, modulus):
	if base < 1 or exponent < 0 or modulus < 1:
		return -1
	result = 1
	while exponent > 0:
		if exponent % 2 == 1:
			result = result * base % modulus
		base = base * base % modulus
		exponent = exponent // 2
	return result
print(powermod(result, a, 10 ** 9 + 7))
