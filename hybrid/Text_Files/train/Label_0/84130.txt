n = int(input())
ai = list(map(int, input().split()))
ai.sort()
ans = sum(ai)
ans3 = ans
for j in range(1, n):
	for i in range(1, 11):
		ans2 = ans3
		if ai[j] % i == 0:
			ans2 -= ai[j] + ai[0]
			ans2 += ai[0] * i + ai[j] // i
		ans = min(ans, ans2)
print(ans)
