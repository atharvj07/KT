for _ in range(int(input())):
	(n, m, k) = map(int, input().split())
	least = min(n, m)
	great = max(n, m)
	so_far = great - 1
	so_far += (least - 1) * great
	if so_far == k:
		print('YES')
	else:
		print('NO')
