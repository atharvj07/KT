def solve(num):
	if len(num) < 2:
		return num
	sz = len(num)
	ans = 9 * (sz - 1)
	temp = num[0] * sz
	if temp <= num:
		return ans + int(num[0])
	else:
		return ans + int(num[0]) - 1
for _ in range(int(input())):
	num = input()
	print(solve(num))
