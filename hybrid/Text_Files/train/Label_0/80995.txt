t1 = input().strip()
t2 = input().strip()
h1 = int(t1[:2])
m1 = int(t1[3:])
h2 = int(t2[:2])
m2 = int(t2[3:])
t = ((h1 + h2) * 60 + (m1 + m2)) // 2
h = str(t // 60)
m = str(t % 60)
if len(h) == 1:
	h = '0' + h
if len(m) == 1:
	m = '0' + m
print(h + ':' + m)
