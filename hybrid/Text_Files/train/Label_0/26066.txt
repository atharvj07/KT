#include <bits/stdc++.h>
using namespace std;

int n, a;
set<int> s;
int main(){
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> a;
        if(s.find(a) == s.end()) s.insert(a);
        else s.erase(a);
    }
    cout << s.size();
}
