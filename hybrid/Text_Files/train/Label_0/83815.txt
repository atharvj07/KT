R = lambda : map(int, input().split())
(n, m) = R()
if m >= n:
	print(n)
else:
	(l, r) = (0, n)
	while l < r:
		mid = (l + r) // 2
		if (2 * m + 2 + mid) * (mid + 1) // 2 >= n + mid * m:
			r = mid
		else:
			l = mid + 1
	print(m + r + 1)
