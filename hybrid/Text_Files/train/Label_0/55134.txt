
import java.util.*;




public class NewClass {
    public static void main(String args[]){
        Scanner sc= new Scanner(System.in);
       long l1=sc.nextLong();long l2=sc.nextLong();
       
       String s1=l1+"";
       String s2=l2+"";
       String temp1="",t2="";
        for (int i = 0; i < s1.length(); i++) {
            if (s1.charAt(i)!='0') {
                temp1=temp1+s1.charAt(i);
            }
        }
        for (int i = 0; i < s2.length(); i++) {
            if (s2.charAt(i)!='0') {
                t2=t2+s2.charAt(i);
            }
        }
        String s3=(l1+l2)+"";
        String t3="";
        for (int i = 0; i < s3.length(); i++) {
            if (s3.charAt(i)!='0') {
                t3=t3+s3.charAt(i);
            }
        }
        
        long n1=Long.parseLong(temp1);
        long n2=Long.parseLong(t2);
         long r=Long.parseLong(t3);
         if (n1+n2==r) {
             System.out.println("YES");
        }else{
             System.out.println("NO");
         }
        }
                
    
    }