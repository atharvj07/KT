#include <bits/stdc++.h>
using namespace std;

int main(){
  long long N, Z, W;
  cin >>N >>Z >>W;
  vector<long long> A(N);
  for (int i=0; i<N; i++){
    cin >> A[i];
  }
  
  if (N==1){
    cout << abs(A[0]-W) << endl;
  }
  else{
    cout << max(abs(A[N-2]-A[N-1]), abs(A[N-1]-W)) << endl;
  }
}