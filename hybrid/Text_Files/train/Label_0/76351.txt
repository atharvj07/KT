import sys

def solve(a, b):
	if b != 1:
		print('YES')
		if b != 2:
			print(a, a * (b - 1), a * b)
		else:
			print(a, 3 * a, 4 * a)
	else:
		print('NO')
T = int(input())
for t in range(T):
	(a, b) = map(int, input().split())
	solve(a, b)
