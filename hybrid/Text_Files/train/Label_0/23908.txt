#include <iostream>
#include <vector>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int>a(n);
    for(int i=0;i<n;i++)cin >> a[i];
    vector<int>b(n);
    b = a;
    int cnt = 0;
    for(int i=1;i<n;i++){
        if(i%2==1){
            if(b[i-1]<b[i]){
                continue;
            }else{
                cnt++;
                if(i!=n-1){
                   if(b[i-1]<b[i+1]&&b[i+1]>b[i]){
                       swap(b[i],b[i+1]);
                   }else{
                       swap(b[i],b[i-1]);
                   }
                }else{
                    swap(b[i],b[i-1]);
                }
            }
        }else{
            if(b[i-1]<b[i]){
                cnt++;
                if(i!=n-1){
                   if(b[i-1]>b[i+1]&&b[i+1]<b[i]){
                       swap(b[i],b[i+1]);
                   }else{
                       swap(b[i],b[i-1]);
                   }
                }else{
                    swap(b[i],b[i-1]);
                }
            }else{
                continue;
            }
        }
    }
    // for(auto x:b){
    //     cout << x << " ";
    // }
    // cout << endl;
    b = a;
    int cnt2 =  0;
    for(int i=1;i<n;i++){
        if(i%2==0){
            if(b[i-1]<b[i]){
                continue;
            }else{
                cnt2++;
                if(i!=n-1){
                   if(b[i-1]<b[i+1]&&b[i+1]>b[i]){
                       swap(b[i],b[i+1]);
                   }else{
                       swap(b[i],b[i-1]);
                   }
                }else{
                    swap(b[i],b[i-1]);
                }
            }
        }else{
            if(b[i-1]<b[i]){
                cnt2++;
                if(i!=n-1){
                   if(b[i-1]>b[i+1]&&b[i+1]<b[i]){
                       swap(b[i],b[i+1]);
                   }else{
                       swap(b[i],b[i-1]);
                   }
                }else{
                    swap(b[i],b[i-1]);
                }
            }else{
                continue;
            }
        }
    }
    // for(auto x:b){
    //     cout << x << " ";
    // }
    // cout << endl;
    cout << min(cnt,cnt2) << endl;
    return 0;
}
