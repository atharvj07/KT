#include <iostream>
using namespace std;

const int MAX = 10001;

char p[MAX] = {0};

void prime() { // f©¬©¾¯»f·éB1ÈçfB0Èç¬
  for(int i = 2 ; i < MAX ; i++ ) p[i] = 1;

  for(int i = 2 ; i * i < MAX ; i++ ) {
    if( p[i] == 1 ) {
      for(int j = i + i ; j < MAX ; j += i ) {
	p[j] = 0;
      }
    }
  }
}

int main(void) {
	int n;
	prime();
	while( cin >> n ) {
	  int cnt = 0;
	  for(int a = 1 ; a <= n ; a++) {
	    int b = n + 1 - a;
	    if( p[a] && p[b] == 1 ) cnt++; //p[a], p[b]ÇÁ¿àfÈçCOUNT
	  }
	  cout << cnt << endl;
	}
	return 0;
}