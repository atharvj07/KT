def check():
	(a, b, c, r) = map(int, input().split())
	L = c - r
	R = c + r
	start = max(L, min(a, b))
	end = min(R, max(a, b))
	res = abs(a - b) - max(0, end - start)
	return res
for i in range(int(input())):
	print(check())
