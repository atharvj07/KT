#include<vector>
#include<iostream>
#include<algorithm>
#include<string>
using namespace std; 
#define L 14
#define M 1456527
int v[1000];
vector<int> w(1244); int c; int ans[1000];
int main()
{
     int n,q; cin >> n;
     for (int i = 0; i < n; i++)
         cin >> v[i];
     cin >> q;
     for (int i = 0; i < q; i++)
     {
         int b, e; cin >> b >> e;
         reverse(v + b, v + e);
     }
    for (int i = 0; i < n; i++)cout << (i ? " " : "") << v[i];
    cout << endl;
}
