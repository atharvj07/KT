#include <bits/stdc++.h>
using namespace std;
int main() {
  long long i, j, k, n, m, p;
  scanf("%lld", &n);
  while (n--) {
    scanf("%lld %lld %lld", &i, &j, &k);
    cout << (i + j + k) / 2 << endl;
  }
  return 0;
}
