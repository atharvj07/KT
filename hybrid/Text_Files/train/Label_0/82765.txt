from math import log as lg
(n, b, p) = map(int, input().split(' '))
bottle = 0
k = pow(2, int(lg(n, 2)))
towel = n * p
while k >= 2:
	bottle += k * b + int(k / 2)
	n = int(k / 2) + n - k
	if n > 0:
		k = pow(2, int(lg(n, 2)))
print(bottle, towel)
