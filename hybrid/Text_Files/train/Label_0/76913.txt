t = int(input())
for i in range(t):
	n = int(input())
	a = list(map(int, input().split()))
	b = [0 for i in range(101)]
	b[0] = 1
	ans = 0
	for j in a:
		if b[abs(j)] < 2:
			b[abs(j)] += 1
			ans += 1
	print(ans)
