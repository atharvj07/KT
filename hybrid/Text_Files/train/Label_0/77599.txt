(n, m) = map(int, input().split())
x = [int(w) for w in input().split()]
if n > m:
	print(0)
else:
	ans = 1
	for i in range(n):
		for j in range(i + 1, n):
			ans *= abs(x[i] - x[j])
			ans %= m
	print(ans)
