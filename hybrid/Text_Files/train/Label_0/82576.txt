n = int(input())
A = list(map(int, input().split()))
res = 0
if 0 in A:
	for a in A:
		if a != 0:
			res += abs(a) - 1
		else:
			res += 1
	print(res)
else:
	count = 0
	for a in A:
		res += abs(a) - 1
		if a < 0:
			count += 1
	if count % 2 == 0:
		print(res)
	else:
		print(res + 2)
