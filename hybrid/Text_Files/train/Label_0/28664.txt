#include<cstdio>
#include<iostream>
#include<algorithm>
#include<cstring>
using namespace std;
const int N=100005;
char ss[N];
int n;
int X[N],Y[N];
int num=0;
int main()
{
	scanf("%s",ss+1);
	n=strlen(ss+1);
	if (ss[1]=='0'||ss[n]=='1')	{printf("-1\n");return 0;}
	for (int u=1;u<n;u++)
	{
		if (ss[u]!=ss[n-u])
		{
			printf("-1\n");
			return 0;
		}
	}
	printf("1 2\n");
	int tp=1,sum=2;
	for (int u=2;u<n;u++)
	{
		sum++;
		printf("%d %d\n",sum,tp);
		if (ss[u]=='1') tp=sum;
	}
	return 0;
}