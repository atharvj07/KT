import math
(n, s) = list(map(int, input().split()))
v = list(map(int, input().split()))
sum_v = sum(v)
if sum_v < s:
	print(-1)
else:
	v.sort()
	minimum = v[0]
	remaining = s - (sum_v - n * minimum)
	if remaining <= 0:
		print(minimum)
	else:
		print(minimum - int(math.ceil(remaining / n)))
