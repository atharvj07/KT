n = int(input())
res = []
for d in range(1, int(n ** (1 / 2)) + 1):
	if n % d == 0:
		if d * d == n:
			res.append(n * (d - 1) // 2 + d)
		else:
			res.append(n * (n // d - 1) // 2 + n // d)
			res.append(n * (d - 1) // 2 + d)
res.sort()
for x in res:
	print(x, end=' ')
