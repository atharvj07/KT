def findit(n, st):
	for i in range(n):
		for j in range(1, n // 4 + 1):
			if i + 4 * j < n and st[i] == '*' and (st[i + j] == '*') and (st[i + 2 * j] == '*') and (st[i + 3 * j] == '*') and (st[i + 4 * j] == '*'):
				print('yes')
				return
	print('no')
n = int(input())
st = input()
findit(n, st)
