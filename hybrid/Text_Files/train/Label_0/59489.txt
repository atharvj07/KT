import static java.lang.Integer.parseInt;
import static java.lang.Long.parseLong;
import static java.lang.Math.abs;
import static java.lang.Math.max;
import static java.lang.System.arraycopy;
import static java.lang.System.exit;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.util.StringTokenizer;

public class CodeForces4 {

	static final int MOD = 998244353;

	static int add(int a, int b) {
		int res = a + b;
		return res >= MOD ? res - MOD : res;
	}

	static int sub(int a, int b) {
		int res = a - b;
		return res < 0 ? res + MOD : res;
	}

	static int mul(int a, int b) {
		int res = (int) ((long) a * b % MOD);
		return res < 0 ? res + MOD : res;
	}

	static int pow(int a, int e) {
		if (e == 0) {
			return 1;
		}
		int r = a;
		for (int i = 30 - Integer.numberOfLeadingZeros(e); i >= 0; i--) {
			r = mul(r, r);
			if ((e & (1 << i)) != 0) {
				r = mul(r, a);
			}
		}
		return r;
	}

	static int inv(int a) {
		return pow(a, MOD - 2);
	}

	static final int HALF = (MOD + 1) / 2;

	static int dbl(int a) {
		return add(a, a);
	}

	static int half(int a) {
		return mul(a, HALF);
	}

    static int gcd(int a,int b){
         if(b==0) {
             return a;
         }
         return gcd(b,a%b);
    }

	static int scanInt() throws IOException {
		return parseInt(scanString());
	}

	static long scanLong() throws IOException {
		return parseLong(scanString());
	}

	static String scanString() throws IOException {
		while (tok == null || !tok.hasMoreTokens()) {
			tok = new StringTokenizer(in.readLine());
		}
		return tok.nextToken();
	}

	
	static BufferedReader in;
	static PrintWriter out;
	static StringTokenizer tok;
	public static void main(String[] args) {
		try {
			in = new BufferedReader(new InputStreamReader(System.in));
			out = new PrintWriter(System.out);
			solve();
			in.close();
			out.close();
		} catch (Throwable e) {
			e.printStackTrace();
			exit(1);
		}
	}

    	static void solve() throws Exception {

		int n = scanInt();

		int arr[]= new int[2*n];

		for(int i=0;i<2*n;i++){
			arr[i]=scanInt();
		}

		int arr1[]=new int[arr.length];
			for(int i=0;i<arr.length;i++){
				arr1[i]=arr[i];
				
			}


		int steps1=0;
		int steps2=0;


		if(n%2==0){
			// System.out.println("n is even");
			
			boolean issort1=false;
			boolean issort2=false;

			int op1=1;
			int op2=1;
			
			for(int i=0;i<4;i++){

				boolean sort=isSorted(arr, n);
				if(sort){
					issort1=true;
					break;
				}

				if(op1==1){
				operation1(arr,n);
				steps1++;
				op1=0;
				}
				else{
					// System.out.println("op2");
					operation2(arr, n);
				steps1++;
					op1=1;
				}
				

			}

			for(int i=0;i<4;i++){

				boolean sort=isSorted(arr1, n);
				if(sort){
					issort2=true;
					break;
				}

				if(op2==0){
				operation1(arr1,n);
				steps2++;
				op2=1;
				}
				else{
					// System.out.println("op2");
					operation2(arr1, n);
				steps2++;
				op2=0;
				}
				

			}
			
			if(issort1==true && issort2==true ){
				System.out.println(Math.min(steps1,steps2));
			}
			else{
				System.out.println("-1");
			}
		}
		else{
			// System.out.println("n is odd");
			boolean issort1=false;
			boolean issort2=false;

			int op1=1;
			int op2=1;
			
			for(int i=0;i<2*n;i++){

				boolean sort=isSorted(arr, n);
				if(sort){
					issort1=true;
					break;
				}

				if(op1==1){
				operation1(arr,n);
				steps1++;
				op1=0;
				}
				else{
					// System.out.println("op2");
					operation2(arr, n);
				steps1++;
					op1=1;
				}
				

			}
			
			for(int i=0;i<2*n;i++){

				boolean sort=isSorted(arr1, n);
				if(sort){
					issort2=true;
					break;
				}

				if(op2==0){
				operation1(arr1,n);
				steps2++;
				op2=1;
				}
				else{
					// System.out.println("op2");
					operation2(arr1, n);
				steps2++;
					op2=0;
				}
				

			}
			
			if(issort1 && issort2 ){
				System.out.println(Math.min(steps1,steps2));
			}
			else{
				System.out.println("-1");
			}
		}

		



		// char s[] = scanString().toCharArray();
		// out.println(n);
		// out.print(s);
        // int ans=gcd(n,m);
		// System.out.println(ans);

	}

	static void operation1(int arr[],int n){

		for(int i=1;i<2*n;i+=2){
			int temp=arr[i];
			arr[i]=arr[i-1];
			arr[i-1]=temp;
		}
		// System.out.println("operation1");
	}

	static void operation2(int arr[],int n){

		for(int i=0;i<n;i++){
			int temp=arr[i];
			arr[i]=arr[i+n];
			arr[i+n]=temp;
		}

		// System.out.println("operation2");
		
	}

	static boolean isSorted(int arr[],int n){

		boolean issort=true;

		for(int i=1;i<2*n;i++){
			if(arr[i]<arr[i-1]){
				issort=false;
				break;
			}
		}

		// System.out.println("sortfuntion");

		return issort;

	}
}