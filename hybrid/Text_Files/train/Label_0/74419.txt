for a0 in range(int(input())):
	(a, b) = [int(x) for x in input().split()]
	v = abs(a - b)
	if v == 0:
		print(0, 0)
	else:
		if a > b:
			m = b
		else:
			m = a
		v1 = m % v
		if v1 > v - v1:
			v1 = v - v1
		print(v, v1)
