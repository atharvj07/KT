for _ in range(int(input())):
	a = sorted(list(map(int, input().split())))
	r = a[1] - a[0]
	r = int(r ** 0.5)
	k = r * (r + 1) // 2
	while not (a[0] - a[1] + k >= 0 and (a[0] - a[1] + k) % 2 == 0):
		r += 1
		k = r * (r + 1) // 2
	print(r)
