T = int(input())
for i in range(T):
	n = int(input())
	s = input()
	n = n // 2
	flag = 1
	for a in range(n):
		if s[a] == s[2 * n - 1 - a]:
			continue
		if abs(ord(s[a]) - ord(s[2 * n - 1 - a])) == 2:
			continue
		else:
			flag = 0
			break
	if flag == 0:
		print('NO')
	else:
		print('YES')
