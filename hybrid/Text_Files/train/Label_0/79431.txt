a = int(input())
for i in range(a):
	(x, y, z) = map(int, input().split())
	if z % 2 == 0:
		if x == y:
			print(0)
		else:
			tot = x * (z // 2) - y * (z // 2)
			print(tot)
	elif x == y:
		print(x)
	else:
		tot = x * (z - z // 2) - y * (z // 2)
		print(tot)
