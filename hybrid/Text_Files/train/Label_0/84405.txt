from collections import Counter
t = int(input(' '))
for i in range(0, t):
	st = input(' ')
	vals = Counter(st)
	rd = 0
	gd = 0
	sol = 0
	for i in vals:
		if vals[i] >= 2:
			rd = rd + 1
		elif vals[i] == 1:
			gd = gd + 1
	sol = rd + gd // 2
	print(sol)
