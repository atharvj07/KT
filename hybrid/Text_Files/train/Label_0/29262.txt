#include<bits/stdc++.h>
using namespace std;

int main(){
	int n;
	string s, t;
	cin >> n >> s >> t;
	int count = 2 * n;
	for(int i = 0; i < n; i++){
		if(s.substr(i) == t.substr(0, n - i)){
			count = n + i;
			break;
		}
	}
	cout << count << endl;
}
