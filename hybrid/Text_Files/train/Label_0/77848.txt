import itertools

def testcases(a, n):
	if a[0] + a[1] <= a[n - 1]:
		return (1, 2, n)
	else:
		return -1
for _ in range(int(input())):
	n = int(input())
	a = [int(x) for x in input().split()]
	ans = testcases(a, n)
	if ans == -1:
		print(-1)
	else:
		for i in ans:
			print(i, end=' ')
		print()
