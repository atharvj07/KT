t = int(input())
for _ in range(t):
	n = int(input())
	a = list(map(int, input().split(' ')))
	limit = n * (n - 1) / 2 - 1
	d = 0
	for i in range(n - 1):
		if a[i] > a[i + 1]:
			d += 1
		else:
			status = 'YES'
	if d == n - 1:
		status = 'NO'
	print(status)
