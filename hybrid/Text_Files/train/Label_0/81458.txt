from sys import stdin, gettrace
if not gettrace():

	def input():
		return next(stdin)[:-1]

def main():

	def solve():
		(x1, y1, x2, y2) = map(int, input().split())
		print((x2 - x1) * (y2 - y1) + 1)
	q = int(input())
	for _ in range(q):
		solve()
main()
