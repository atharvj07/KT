for _ in range(int(input())):
	n = int(input())
	t = 0
	lsx = []
	ls = list(map(int, input().split()))
	for i in range(len(ls)):
		if ls[i] not in lsx:
			lsx.append(ls[i])
			t += 1
		elif -ls[i] not in lsx:
			lsx.append(-ls[i])
			t += 1
	print(t)
