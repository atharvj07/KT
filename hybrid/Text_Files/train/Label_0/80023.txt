for _ in range(int(input())):
	(x, y) = map(int, input().split())
	(*li,) = map(int, input().split())
	li.sort(reverse=True)
	(cnt, ans) = (0, 0)
	for i in range(x):
		min1 = li[i]
		cnt += 1
		if min1 * cnt >= y:
			ans += 1
			cnt = 0
	print(ans)
