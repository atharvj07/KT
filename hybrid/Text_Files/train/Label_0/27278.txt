#include <bits/stdc++.h>
using namespace std;
const long N = 300000, mx = 1000000000;
long i, m, n, k, j, w, c[N], xx, yy, w1, w2, l, a[N], b[N], s;
int main() {
  cin >> n >> m;
  for (i = 1; i <= n; i++) cin >> a[i];
  l = m;
  s = 1;
  while (l > 0) {
    if (s < l)
      l -= s;
    else
      break;
    s++;
  }
  cout << a[l];
}
