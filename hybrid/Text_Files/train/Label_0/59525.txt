import java.util.Scanner;


public class C2 {
	public void solve_correct(){
		Scanner sc = new Scanner(System.in);
		int n = sc.nextInt();
		int[] a = new int[n];
		for (int i = 0; i < n; i++) {
			a[i] = sc.nextInt();
		}
		double[][] p = new double[n][];
		for (int i = 0; i < n; i++) {
			p[i] = new double[a[i] + 1];
			p[i][a[i]] = 1;
		}

		double ans = 0;
		for (int i = 0; i < n; i++) {
			ans += p[i][0];
		}
		int q = sc.nextInt();
		for (int i = 0; i < q; i++) {
			int u = sc.nextInt() - 1, v = sc.nextInt() - 1, K = sc.nextInt();
			ans -= p[u][0];
//			for (int take = 0; take < k; take++) {
//				double frac = (double) 1 / a[u];
//				for (int j = 0; j < p[u].length - 1; j++) {
//					p[u][j] = frac * (p[u][j] * (a[u] - j) + p[u][j + 1] * (j + 1));
//				}
//				int j = p[u].length - 1;
//				p[u][j] = (double) (a[u] - j) * frac * p[u][j];
//				a[u]--;
//			}
			
			for(int k = 0; k < K; k++){
				for(int j = 1; j < p[u].length; j++){
					double au = (double)a[u];
					double pn = p[u][j]* j/au;
					double pn2 = p[u][j] * (1-j/au);
					p[u][j-1] += pn;
					p[u][j] = pn2;
				}		
				
				//a[v]++;
				a[u]--;
			}
			
			ans += p[u][0];
			a[v] += K;
			System.out.println(ans);
		}
	}
	
	public void solve(){
		Scanner sc = new Scanner(System.in);
		int N = sc.nextInt();
//		int[] a = new int[N];
//		double ans = 0;
//		double[][] p = new double[N][];
//		for(int i = 0; i < N; i++){
//			a[i] = sc.nextInt();
//			p[i] = new double[a[i]+1];
//			p[i][a[i]]=1;
//			if(a[i] == 0) ans+=p[i][a[i]];
//		}
		
		int[] a = new int[N];
		for (int i = 0; i < N; i++) {
			a[i] = sc.nextInt();
		}
		double[][] p = new double[N][];
		for (int i = 0; i < N; i++) {
			p[i] = new double[a[i] + 1];
			p[i][a[i]] = 1;
		}

		double ans = 0;
		for (int i = 0; i < N; i++) {
			ans += p[i][0];
		}
		
		int Q = sc.nextInt();
		
		for(int i = 0; i < Q; i++){
			int u = sc.nextInt()-1;
			int v = sc.nextInt()-1;
			int K = sc.nextInt();
			ans -= p[u][0];
			for(int k = 0; k < K; k++){
				for(int j = 1; j < p[u].length; j++){
					double au = (double)a[u];
					double pn = p[u][j]* j/au;
					double pn2 = p[u][j] * (1-j/au);
					p[u][j-1] += pn;
					p[u][j] = pn2;
				}						
				//a[v]++;
				a[u]--;
			}
			a[v]+=K;
			ans += p[u][0];
			System.out.println(ans);
		}
	}
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		new C2().solve();
	}
}
