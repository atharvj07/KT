import heapq as hq
n = int(input())
a = list(map(int, input().split()))
ans = 0
q = []
for i in range(n):
	if q:
		x = hq.heappop(q)
		if x < a[i]:
			ans += a[i] - x
			hq.heappush(q, a[i])
		else:
			hq.heappush(q, x)
	hq.heappush(q, a[i])
print(ans)
