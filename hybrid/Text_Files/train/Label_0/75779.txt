for _ in range(int(input())):
	n = int(input())
	a = [int(x) for x in input().split(' ')]
	a.sort()
	b = [a[0]]
	c = []
	last = -1
	for i in range(1, len(a)):
		if a[i] == b[-1]:
			c.append(a[i])
		else:
			b.append(a[i])
	b.extend(c)
	print(*b, sep=' ')
