#include <bits/stdc++.h>
using namespace std;
int main() {
  long long a, b;
  cin >> a >> b;
  long long ans = 0;
  for (int i = 1; i <= a; i++) {
    ans += ((i * b + 1) % 1000000007) * (((b * (b - 1)) / 2) % 1000000007);
    ans %= 1000000007;
  }
  cout << ans << endl;
  return 0;
}
