#include <bits/stdc++.h>
using namespace std;
unsigned long long sft = 1, n, a[1000010], cnt;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cin >> n;
  a[0] = 1;
  a[1] = 1;
  for (int i = 2; true; i++) {
    a[i] = a[i - 1] + a[i - 2];
    if (a[i] > n) break;
    cnt++;
  }
  cout << cnt;
  return 0;
}
