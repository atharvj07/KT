(n, m) = map(int, input().strip().split(' '))
low = 1
high = n
ans = 1000000000000000000000000000
if n > m:
	while low <= high:
		mid = (high + low) // 2
		t = mid * (mid + 1) // 2 + m
		if t >= n:
			ans = min(ans, mid)
			high = mid - 1
		else:
			low = mid + 1
	print(m + ans)
else:
	print(n)
