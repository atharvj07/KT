from sys import stdin, stdout
import math, queue, heapq
fastinput = stdin.readline
fastout = stdout.write
t = int(fastinput())
while t:
	t -= 1
	(v, c, n, m) = map(int, fastinput().split())
	if v - c > 0:
		d = v - c
		if c - m + v >= n and c >= m:
			print('Yes')
		else:
			print('No')
	elif v - c < 0:
		if v >= m and c + (v - m) >= n:
			print('Yes')
		else:
			print('No')
	elif m <= c and c - m + v >= n:
		print('Yes')
	else:
		print('No')
