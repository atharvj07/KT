#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, ans = 1, tem, k;
  cin >> n >> m;
  tem = m;
  for (int i = 1; i <= n; i++) {
    cin >> k;
    if (k <= tem)
      tem -= k;
    else {
      ans++;
      tem = m - k;
    }
  }
  cout << ans << endl;
  return 0;
}
