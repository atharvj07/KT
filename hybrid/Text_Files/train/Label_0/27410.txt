#include <bits/stdc++.h>
using namespace std;
int ara[105];
bool func(int a, int b) { return a > b; }
int main() {
  int n, x;
  scanf("%d", &n);
  for (int i = 0; i < n; i++) {
    scanf("%d", &x);
    ara[x]++;
  }
  sort(ara, ara + 105, func);
  printf("%d\n", ara[0]);
}
