#include<bits/stdc++.h>
using namespace std;
int main()
{
  long long n,i,x,y;
  string s;
  s="";
  cin>>n;
  while(n>0)
  {
     x=(n-1)/26;
     y=n-(26*x+1);
     s+=('a'+y);
     n=x;
  }  
  reverse(s.begin(),s.end());
  cout<<s<<endl;

}