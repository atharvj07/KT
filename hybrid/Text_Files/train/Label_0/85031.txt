import sys
input = sys.stdin.readline
for _ in range(int(input())):
	n = int(input())
	s = input().strip()
	c = [0, 0]
	for i in range(1, n):
		if s[i] == s[i - 1]:
			if s[i] == '1':
				c[1] += 1
			else:
				c[0] += 1
	if s[-1] == s[0]:
		c[int(s[0])] += 1
	print(sum(c) // 2)
