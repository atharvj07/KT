//package codeforces.r494;

import java.util.Scanner;

public class C {
    static Scanner scanner;
    public static void main(String[] args) {
        scanner = new Scanner(System.in);
        int totalDays = scanner.nextInt();
        int k = scanner.nextInt();
        int[] temperatures = new int[totalDays];
        for (int i = 0; i < totalDays; i++) {
            temperatures[i]= scanner.nextInt();
        }
        double maxIntensity = 0;
        for (int i = 0; i < totalDays; i++) {
            if (i+k>totalDays) {
                break;
            }
            double sum = 0;
            for (int j = i; j <i+k ; j++) {
                sum += temperatures[j];
            }
            maxIntensity = Math.max(sum/k, maxIntensity);
            for (int j = i+k, l=1; j <totalDays; j++,l++) {
                sum += temperatures[j];
                int days = k + l;
                maxIntensity = Math.max(sum/ days, maxIntensity);
            }
        }
        System.out.println(maxIntensity);
    }
}
