def main():
	n = int(input())
	arr = list(map(int, input().split()))
	res = 0
	cnt = 0
	for i in range(1, len(arr)):
		if arr[i] <= 2 * arr[i - 1]:
			cnt += 1
			if i == len(arr) - 1:
				res = max(cnt, res)
				break
		else:
			res = max(cnt, res)
			cnt = 0
	print(res + 1)
main()
