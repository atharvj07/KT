(n, k) = [int(x) for x in input().split()]
candies = [int(x) for x in input().split()]
has = 0
count = 0
while count < n:
	has += candies[count]
	if has < 8:
		k -= has
		has = 0
	elif has >= 8:
		k -= 8
		has -= 8
	count += 1
	if k <= 0:
		print(count)
		exit()
print(-1)
