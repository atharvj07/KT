#include <bits/stdc++.h>
using namespace std;
int main() {
  long long int n, m, i;
  cin >> n >> m;
  float a, b, min = 101, x, y;
  for (i = 0; i < n; i++) {
    cin >> a >> b;
    x = a / b;
    if (x < min) {
      min = x;
    }
  }
  y = min * m;
  cout << fixed << setprecision(8) << y;
}
