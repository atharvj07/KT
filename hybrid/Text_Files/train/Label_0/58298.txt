import java.util.*;
import java.util.regex.*;
import static java.lang.Math.*;
import static java.lang.System.out;

// AOJ4
public class Main {
	final Scanner sc=new Scanner(System.in);
	final int[] vx={0,1,0,-1}, vy={-1,0,1,0};
	final int[] vvx={0,1,0,-1,-1,1,1,-1}, vvy={-1,0,1,0,-1,-1,1,1};
	final int INF=1<<24;
	static class Point{
		int x, y;
		Point(int x, int y){	this.x=x;	this.y=y;}
		Point(){	this.x=0; this.y=0;}
		@Override public String toString(){	return "("+this.x+","+this.y+")";}
		static boolean ok(int x,int y,int X,int Y,int min){	return (min<=x&&x<X)&&(min<=y&&y<Y);}
		static boolean ok(int x,int y,int X,int Y){	return ok(x,y,X,Y,0);}
	}
	public static void main(String[] args) {
		//long s=System.currentTimeMillis();
		new Main().C();
		//out.println((System.currentTimeMillis()-s)+"msec");
	}
	
	void C(){
		int n=sc.nextInt();
		String[] s=new String[n];
		sc.nextLine();
		for(int i=0; i<n; i++)	s[i]=sc.nextLine().replaceAll("^\\s*", "");
		int tc=0,tc2=0;
		String sin="";
		boolean flag=false;
		for(String str:s){
			if(str.length()<3)	continue;
			String f=str.substring(0,3);
			if(flag){
				if(f.equals("try"))	tc2++;
				else if(f.equals("cat")){
					if(tc2>0)	tc2--;
					else{
						//String str2=str.replaceAll(" ", "");
						//out.println(str);
						Matcher m = Pattern.compile("catch\\s*\\(\\s*"+sin+"\\s*,\\s*\"(.+)\"\\s*\\s*\\)\\s*").matcher(str);
						if(m.matches()){
							out.println(m.group(1));
							System.exit(0);
						}
					}
				}
			}else{
				if(f.equals("thr")){
					flag=true;
					String str2=str.replaceAll(" ", "");
					Matcher m = Pattern.compile("throw\\((.+)\\)").matcher(str2);
					if(m.matches())	sin=m.group(1);
					//out.println(sin);
				}
				else if(f.equals("try"))	tc++;
				else if(f.equals("cat"))	tc--;
			}
		}
		out.println("Unhandled Exception");
	}
	
	void B(){
		int n=sc.nextInt(),m=sc.nextInt();
		// debug
		int[] mm=new int[m+1];
		if(m%2==0){
			int c=m/2;
			boolean small=true;
			for(int i=0,j=0; i<n; i++){
				int idx=c+(small?-j:j);
				j+=(small?1:0);
				small=!small;
				out.println(idx);
				// debug
				//mm[idx]++;
				if(small && j==c)	j=0;
			}
		}else{
			int c=(int)ceil(m/2.0);
			boolean small=false;
			for(int i=0,j=0; i<n; i++){
				int idx=c+(small?-j:j);
				j+=(small?0:1);
				small=!small;
				out.println(idx);
				// debug
				//mm[idx]++;
				if(small && j==c){
					j=0;
					small=!small;
				}
			}
		}
		
		// debug
		//out.println("--------");
		//for(int i=1; i<=m; i++)	out.print(mm[i]+" \n");
	}
	
	void A(){
		int a=sc.nextInt(),b=sc.nextInt(),c=sc.nextInt();
		double d=(((double)a*c)-((double)b*c))/(double)b;
		out.println((int)ceil(d));
	}
}