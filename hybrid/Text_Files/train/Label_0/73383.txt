import math
import re
(n, k) = map(int, input().split())
a = list(map(int, input().split()))
s = sum(a[:k])
res = s / (n - k + 1)
for i in range(n - k):
	s -= a[i]
	s += a[i + k]
	res += s / (n - k + 1)
print(res)
