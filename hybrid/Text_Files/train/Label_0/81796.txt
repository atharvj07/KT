import sys
t = int(input())
for _ in range(t):
	(n, m, x, y) = map(int, input().split())
	y = min(x * 2, y)
	res = 0
	for i in range(n):
		a = input().split('*')
		res += sum((y * (len(s) // 2) + x * (len(s) % 2) for s in a))
	print(res)
