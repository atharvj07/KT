import sys
input = sys.stdin.readline
from collections import *
n = int(input())
(e, o) = (1, 0)
MOD = 998244353
for i in range(n):
	if i % 2 == 0:
		dp = e
		o += dp
		o %= MOD
	else:
		dp = o
		e += dp
		e %= MOD
print(dp * pow(pow(2, n, MOD), MOD - 2, MOD) % MOD)
