import math
for _ in range(int(input())):
	(n, k) = map(int, input().split())
	ans = 0
	curr = 1
	while curr < n and curr <= k:
		curr *= 2
		ans += 1
	n -= curr
	if n > 0:
		ans += (n - 1) // k + 1
	print(ans)
