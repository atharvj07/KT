#include <bits/stdc++.h>
using namespace std;
long long int k, v, n, m, ans, sum, sum1, l, r, i, j, mx, mn, c, z, x, y,
    a[111111], d[111111], t[111111];
string s, sos, ss;
char ch;
bool used[111111];
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cin >> n;
  cout << (n - 1) / 2;
  return 0;
}
