#include <bits/stdc++.h>
using namespace std;
char s[1005];
int main() {
  scanf("%s", s + 1);
  int n = strlen(s + 1);
  printf("%s", s + 1);
  for (int i = n; i >= 1; i--) cout << s[i];
  return 0;
}
