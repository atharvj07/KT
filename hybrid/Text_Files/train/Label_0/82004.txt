import sys

def debug(x, table):
	for (name, val) in table.items():
		if x is val:
			print('DEBUG:{} -> {}'.format(name, val), file=sys.stderr)
			return None

def solve():
	a = input()
	b = input()
	if a == b:
		ans = -1
	else:
		ans = max(len(a), len(b))
	print(ans)
solve()
