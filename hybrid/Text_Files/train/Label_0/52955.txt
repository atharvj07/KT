import sun.reflect.generics.tree.Tree;

import java.io.*;
import java.util.*;


public class A {

    long MODULO = 1L * 1000 * 1000 * 1000 * 1000 * 1000 ;
    long b = 31;
    String fileName = "";
    class PointCompX implements Comparable<PointCompX>{
        long x, y;
        int ind;
        PointCompX(long x, long y, int ind){
            this.x = x;
            this.y = y;
            this.ind = ind;
        }

        @Override
        public int compareTo(PointCompX o) {
            return Long.compare(this.x, o.x);
        }
    }
    class PointCompY implements Comparable<PointCompY>{
        long x, y;
        int ind;
        PointCompY(long x, long y, int ind){
            this.x = x;
            this.y = y;
            this.ind = ind;
        }

        @Override
        public int compareTo(PointCompY o) {
            return Long.compare(this.y, o.y);
        }
    }

    //////////////////////    SOLUTION   SOLUTION  SOLUTION    //////////////////////////////
    int INF = Integer.MAX_VALUE;
    void solve() throws IOException {
        int n = readInt();
        int k = readInt();
        PointCompX[] x = new PointCompX[n];
        PointCompY[] y = new PointCompY[n];

        for(int i=0; i<n; ++i){
            long x1 = readInt()*2;
            long y1 = readInt()*2;
            long x2 = readInt()*2;
            long y2 = readInt()*2;

            x[i] = new PointCompX((x1+x2)/2, (y1+y2)/2, i);
            y[i] = new PointCompY((x1+x2)/2, (y1+y2)/2, i);
        }
        Arrays.sort(x);
        Arrays.sort(y);
        int[] used = new int[n];
        Arrays.fill(used, -1);
        long ans = Long.MAX_VALUE;
        for (int i=0; i<=k; ++i){
            for(int j=0; j<=k; ++j){
                for(int h=0; h<=k; ++h){
                    for(int m=0; m<=k; ++m){
                        if(h+i+m+j==k){
                            int curColor = i*1000+j*100+h*10+m;
                            int lx = 0;
                            int ly = 0;
                            int rx = n-1;
                            int ry = n-1;
                            for(int b=0; b<i; ++b){
                                used[x[lx].ind] = curColor;
                                lx++;
                            }
                            for(int b=0; b<j; ++b){
                                if(used[x[rx].ind] == curColor){
                                    b--;
                                    rx--;
                                    continue;
                                }
                                used[x[rx].ind] = curColor;
                                rx--;
                            }
                            for(int b=0; b<h; ++b){
                                if(used[y[ry].ind] == curColor){
                                    b--;
                                    ry--;
                                    continue;
                                }
                                used[y[ry].ind] = curColor;
                                ry--;
                            }
                            for(int b=0; b<m; ++b){
                                if(used[y[ly].ind] == curColor){
                                    b--;
                                    ly++;
                                    continue;
                                }
                                used[y[ly].ind] = curColor;
                                ly++;
                            }
                            while(lx<rx&&used[x[lx].ind]==curColor) lx++;
                            while(lx<rx&&used[x[rx].ind]==curColor) rx--;
                            while(ly<ry&&used[y[ly].ind]==curColor) ly++;
                            while(ly<ry&&used[y[ry].ind]==curColor) ry--;
                            //out.println(ans);
                            long width = x[rx].x-x[lx].x;
                            width += width%2;
                            width = Math.max(width, 2);
                            long height = y[ry].y-y[ly].y;
                            height += height%2;
                            height = Math.max(height, 2);
                            ans = Math.min(ans, height*width/4);
                        }
                    }
                }
            }
        }
        out.println(ans);
    }

    class Pair implements Comparable<Pair>{
        int a, b;
        Pair(int a, int b){
            this.a = a;
            this.b = b;
        }

        @Override
        public int compareTo(Pair o) {

            return -Long.compare(Math.max(this.a - this.b, 0), Math.max(o.a - o.b, 0));
        }
    }
    ///////////////////////////////////////////////////////////////////////////////////////////

    class SparseTable{
        int[][] rmq;
        int[] logTable;
        int n;
        SparseTable(int[] a){
            n = a.length;
            logTable = new int[n+1];
            for(int i = 2; i <= n; ++i){
                logTable[i] = logTable[i >> 1] + 1;
            }
            rmq = new int[logTable[n] + 1][n];
            for(int i=0; i<n; ++i){
                rmq[0][i] = a[i];
            }

            for(int k=1; (1 << k) < n; ++k){
                for(int i=0; i + (1 << k) <= n; ++i){
                    int max1 = rmq[k - 1][i];
                    int max2 = rmq[k-1][i + (1 << (k-1))];
                    rmq[k][i] = Math.max(max1, max2);
                }
            }
        }

        int max(int l, int r){
            int k = logTable[r - l];
            int max1 = rmq[k][l];
            int max2 = rmq[k][r - (1 << k) + 1];
            return Math.max(max1, max2);
        }
    }
    long checkBit(long mask, int bit){
        return (mask >> bit) & 1;
    }
    class Dsu{
        int[] parent;
        int countSets;
        Dsu(int n){
            countSets = n;
            parent = new int[n];
            for(int i=0; i<n; ++i){
                parent[i] = i;
            }
        }
        int findSet(int a){
            if(parent[a] == a) return a;
            parent[a] = findSet(parent[a]);
            return parent[a];
        }
        void unionSets(int a, int b){
            a = findSet(a);
            b = findSet(b);
            if(a!=b){
                countSets--;
                parent[a] = b;
            }
        }
    }
    static int checkBit(int mask, int bit) {
        return (mask >> bit) & 1;
    }
    boolean isLower(char c){
        return c >= 'a' && c <= 'z';
    }

    ////////////////////////////////////////////////////////////

    class SegmentTree{
        int[] t;
        int n;
        SegmentTree(int n){
            t = new int[4*n];
            build(new int[n+1], 1, 1, n);
        }
        void build (int a[], int v, int tl, int tr) {
            if (tl == tr)
                t[v] = a[tl];
            else {
                int tm = (tl + tr) / 2;
                build (a, v*2, tl, tm);
                build (a, v*2+1, tm+1, tr);
            }
        }

        void update (int v, int tl, int tr, int l, int r, int add) {
            if (l > r)
                return;
            if (l == tl && tr == r)
                t[v] += add;
            else {
                int tm = (tl + tr) / 2;
                update (v*2, tl, tm, l, Math.min(r,tm), add);
                update (v*2+1, tm+1, tr, Math.max(l,tm+1), r, add);
            }
        }

        int get (int v, int tl, int tr, int pos) {
            if (tl == tr)
                return t[v];
            int tm = (tl + tr) / 2;
            if (pos <= tm)
                return t[v] + get (v*2, tl, tm, pos);
            else
                return t[v] + get (v*2+1, tm+1, tr, pos);
        }
    }
    class Fenwik {
        long[] t;
        int length;

        Fenwik(int[] a) {
            length = a.length + 100;
            t = new long[length];

            for (int i = 0; i < a.length; ++i) {
                inc(i, a[i]);
            }
        }

        void inc(int ind, int delta) {
            for (; ind < length; ind = ind | (ind + 1)) {
                t[ind] += delta;
            }
        }

        long getSum(int r) {
            long sum = 0;
            for (; r >= 0; r = (r & (r + 1)) - 1) {
                sum += t[r];
            }
            return sum;
        }
    }
    int gcd(int a, int b){
        return b == 0 ? a : gcd(b, a%b);
    }

    long binPow(long a, long b, long m) {
        if (b == 0) {
            return 1;
        }
        if (b % 2 == 1) {
            return ((a % m) * (binPow(a, b - 1, m) % m)) % m;
        } else {
            long c = binPow(a, b / 2, m);
            return (c * c) % m;
        }

    }
    int minInt(int... values) {
        int min = Integer.MAX_VALUE;
        for (int value : values) min = Math.min(min, value);
        return min;
    }

    int maxInt(int... values) {
        int max = Integer.MIN_VALUE;
        for (int value : values) max = Math.max(max, value);
        return max;
    }
    public static void main(String[] args) throws NumberFormatException, IOException {
        // TODO Auto-generated method stub
        new A().run();
    }

    void run() throws NumberFormatException, IOException {
        solve();
        out.close();
    };

    BufferedReader in;
    PrintWriter out;

    StringTokenizer tok;
    String delim = " ";
    Random rnd = new Random();

    A() throws FileNotFoundException {
        try {
            in = new BufferedReader(new FileReader("input.txt"));
            out = new PrintWriter("output.txt");
        } catch (Exception e) {
            if (fileName.isEmpty()) {
                in = new BufferedReader(new InputStreamReader(System.in));
                out = new PrintWriter(System.out);
            } else {
                in = new BufferedReader(new FileReader(fileName + ".in"));
                out = new PrintWriter(fileName + ".out");
            }

        }
        tok = new StringTokenizer("");
    }

    String readLine() throws IOException {
        return in.readLine();
    }

    String readString() throws IOException {
        while (!tok.hasMoreTokens()) {
            String nextLine = readLine();
            if (null == nextLine) {
                return null;
            }

            tok = new StringTokenizer(nextLine);
        }
        return tok.nextToken();
    }

    int readInt() throws NumberFormatException, IOException {
        return Integer.parseInt(readString());
    }
    byte readByte() throws NumberFormatException, IOException {
        return Byte.parseByte(readString());
    }
    int[] readIntArray (int n) throws NumberFormatException, IOException {
        int[] a = new int[n];
        for(int i=0; i<n; ++i){
            a[i] = readInt();
        }
        return a;
    }

    Integer[] readIntegerArray (int n) throws NumberFormatException, IOException {
        Integer[] a = new Integer[n];
        for(int i=0; i<n; ++i){
            a[i] = readInt();
        }
        return a;
    }

    long readLong() throws NumberFormatException, IOException {
        return Long.parseLong(readString());
    }

    double readDouble() throws NumberFormatException, IOException {
        return Double.parseDouble(readString());
    }
}


