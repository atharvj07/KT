
import java.awt.AWTException;
import java.awt.Robot;
import java.awt.event.InputEvent;
import java.awt.event.KeyEvent;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.io.OutputStream;
import java.io.PrintWriter;
import java.math.BigInteger;
import java.net.InetAddress;
import java.net.UnknownHostException;
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.Comparator;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Iterator;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;
import java.util.PriorityQueue;
import java.util.Queue;
import java.util.Set;
import java.util.StringTokenizer;
import java.util.TreeMap;
import java.util.TreeSet;
import java.util.Vector;
import java.util.logging.Level;
import java.util.logging.Logger;

public class Main {

    public static int n, m, k;
    public static int[][] a;
    public static int[] c;
    public static boolean[] bad;
    public static ArrayList<Integer>[] ll;
    public static ArrayList<Integer>[] llbad;
    public static boolean[] was;
    public static int[] badname;
    public static void dfs(int x, int bb) {
        was[x] = true;
        badname[x] = bb;
        llbad[bb].add(x);
        for(int y : ll[x]) {
            if(!was[y]) {
                bad[y] = true;
                was[y] = true;
                dfs(y, bb);
            }
        }
    }
    public static void main(String[] args) throws InterruptedException, AWTException, UnknownHostException {
        InputStream inputStream = System.in;
        OutputStream outputStream = System.out;
        PrintWriter out = new PrintWriter(outputStream);
        InputReader in = new InputReader(inputStream);

        n = in.nextInt(); m = in.nextInt(); k = in.nextInt();
        a = new int[n][n];
        c = new int[k];
        was = new boolean[n];
        bad = new boolean[n];
        ll = new ArrayList[n];
        badname = new int[n];
        llbad = new ArrayList[n];
        for(int i = 0; i < n; i++) {
            ll[i] = new ArrayList<Integer>();
            llbad[i] = new ArrayList<Integer>();
        }
        boolean[] ab = new boolean[n];
        for(int i = 0; i < k; i++) {
            c[i] = in.nextInt() - 1;
            ab[c[i]] = true;
            bad[c[i]] = true;
        }
        for(int i = 0; i < m; i++) {
            int x = in.nextInt() - 1, y = in.nextInt() - 1;
            a[x][y] = 1;
            a[y][x] = 1;
            ll[x].add(y);
            ll[y].add(x);
        }
        int cntg = 0;
        for(int i = 0; i < n; i++) badname[i] = -1;
        for(int i = 0; i < k; i++) {
            dfs(c[i], c[i]);
        }
        // neuekantebads
        int t = 0;
        int cc = 0;
        for(int i = 0; i < n; i++) {
            for(int j = 0; j < n; j++) {
                if(!bad[i] && !bad[j] && a[i][j] == 0 && i != j ) {
                    cc++;
                    a[i][j] = 1;
                    a[j][i] = 1;
                }else if(i != j && badname[i] == badname[j] && a[i][j] == 0) {
                    t++;
                    a[i][j] = 1;
                    a[j][i] = 1;
                    //out.println(i + " " + j);
                }
            }
        }
        
        int max = 0, maxi = -1;
        for(int i = 0; i < k; i++) {
            if(max < llbad[c[i]].size()) {
                max = llbad[c[i]].size();
            }
        }
        //if(cntg == 0) cntg = 1;
        int cnt = 0;
        for(int i = 0; i < n; i++) {
            if(!bad[i]) cnt++;
        }
        out.println(cc + t + (cnt) * max);
        out.close();
    }

    static class InputReader {

        BufferedReader reader;
        StringTokenizer tokenizer;

        public InputReader(InputStream inputStream) {
            reader = new BufferedReader(new InputStreamReader(inputStream), 32624);
            tokenizer = null;
        }

        public String next() {
            while (tokenizer == null || !tokenizer.hasMoreTokens()) {
                try {
                    tokenizer = new StringTokenizer(reader.readLine());
                } catch (IOException ex) {
                    Logger.getLogger(Main.class.getName()).log(Level.SEVERE, null, ex);
                }
            }
            return tokenizer.nextToken();
        }

        public int nextInt() {
            return Integer.parseInt(next());
        }

        private long nextLong() {
            return Long.parseLong(next());
        }

        private double nextDouble() {
            return Double.parseDouble(next());
        }

        public long[] nextArray(int n) {
            long[] a = new long[n];
            for (int i = 0; i < n; i++) {
                a[i] = nextLong();
            }
            return a;
        }

        public double[] nextArrayd(int n) {
            double[] a = new double[n];
            for (int i = 0; i < n; i++) {
                a[i] = nextDouble();
            }
            return a;
        }

        private BigInteger nextBigInteger() {
            return new BigInteger(next());
        }
    }

}
