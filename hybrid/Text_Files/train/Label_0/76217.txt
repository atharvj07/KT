import math
n = int(input())
for _ in range(n):
	(a, b) = input().split()
	if int(b) != pow(10, len(b)) - 1:
		b = min(int(b), pow(10, len(b) - 1) - 1)
	if b == 0:
		print('0')
	else:
		temp = int(a) * len(str(b))
		print(temp)
