def rec(str):
	n = len(str)
	if n % 2 == 1:
		return str
	n //= 2
	a = rec(str[:n])
	b = rec(str[n:])
	return a + b if a < b else b + a
(a, b) = (input(), input())
print('YES' if rec(a) == rec(b) else 'NO')
