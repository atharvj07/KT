n = int(input())
head = list(range(n + 1))
group = [[i] for i in range(n + 1)]
for _ in range(n - 1):
	(x, y) = map(int, input().split())
	(x, y) = (head[x], head[y])
	if len(group[x]) < len(group[y]):
		(x, y) = (y, x)
	group[x] += group[y]
	for z in group[y]:
		head[z] = x
print(*group[head[1]])
