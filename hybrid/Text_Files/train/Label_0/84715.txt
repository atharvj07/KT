(n, t) = map(int, input().split())
skips = [int(x) for x in input().split()]
position = 1
while position <= n:
	if position == t:
		print('YES')
		exit(0)
	if position > t:
		break
	if position == n:
		break
	step = skips[position - 1]
	position += step
print('NO')
