def solve():
	(n, l, r, k) = [int(x) for x in input().split()]
	a = [int(x) for x in input().split()]
	a.sort()
	count = 0
	for i in a:
		if i < l:
			continue
		elif i > r:
			break
		elif i > k:
			break
		else:
			k -= i
			count += 1
	print(count)
for i in range(int(input())):
	solve()
