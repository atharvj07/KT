n = str(input())
maxium = -1
lastInArr = int(n[-1])
for (x, y) in enumerate(n):
	if int(y) % 2 == 0:
		maxium = x
		if int(y) < lastInArr:
			break
if maxium == -1:
	print(-1)
else:
	print(n[:maxium] + n[-1] + n[maxium + 1:-1] + n[maxium])
