t = int(input())
for T in range(t):
	(n, k) = [int(x) for x in input().split()]
	s = input()
	ans = 0
	for i in range(k):
		alpha = [0] * 26
		for j in range(i, n, k):
			alpha[ord(s[j]) - 97] += 1
			alpha[ord(s[k - j - 1]) - 97] += 1
		ans += sum(alpha) - max(alpha)
	print(ans // 2)
