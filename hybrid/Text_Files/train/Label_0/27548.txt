#include "bits/stdc++.h"
using namespace std;
typedef long long ll;

int main() {
	int A, B, C, K;
	cin >> A >> B >> C >> K;
	int m = max(A, max(B, C));
	cout << A + B + C + m * ((1 << K) - 1) << endl;
}