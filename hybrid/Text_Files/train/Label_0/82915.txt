test = int(input())
for _ in range(test):
	(n, m, k) = map(int, input().split())
	pp = n // k
	if m <= pp:
		print(m)
	else:
		left = m - pp
		if left % (k - 1):
			maxi = left // (k - 1) + 1
		else:
			maxi = left // (k - 1)
		print(pp - maxi)
