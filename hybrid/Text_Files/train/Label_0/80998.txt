from datetime import timedelta
a = input()
b = input()
times = [a + ':00', b + ':00']
ans = str(timedelta(minutes=sum(map(lambda f: int(f[0]) * 60 + int(f[1]), map(lambda f: f.split(':'), times))) / len(times)))
if len(ans) % 2 == 1:
	print('0' + ans[:-3])
else:
	print(ans[:-3])
