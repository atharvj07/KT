def volume_setter():
	remote = [1, 2, 5]
	(vol, dvol) = map(int, input().split())
	count = 0
	fvol = abs(vol - dvol)
	count = 0
	if fvol >= 5:
		count = fvol // 5
		fvol = fvol % 5
	if fvol >= 2:
		count += fvol // 2
		fvol = fvol % 2
	count += fvol
	print(count)
for i in range(int(input())):
	volume_setter()
