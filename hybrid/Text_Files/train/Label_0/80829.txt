for _ in range(int(input())):
	n = int(input())
	arr = tuple(map(int, input().strip().split()))
	dic = {}
	for i in arr:
		if i not in dic:
			dic[i] = 1
		else:
			dic[i] += 1
	print(max(dic.values()))
