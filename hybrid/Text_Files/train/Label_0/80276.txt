t = int(input())
for i in range(0, t):
	(x, y, n) = [int(x) for x in input().split()]
	if n % x == y:
		print(n)
	elif n % x < y:
		q = n % x
		print(n + abs(y - q) - x)
	else:
		q = n % x
		print(n - abs(y - q))
