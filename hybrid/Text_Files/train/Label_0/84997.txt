import math
t = int(input())
for w in range(t):
	(a, b, n) = (int(i) for i in input().split())
	(a, b) = (min(a, b), max(a, b))
	c = 0
	while a <= n and b <= n:
		if c % 2 == 0:
			a += b
			c += 1
		else:
			b += a
			c += 1
	print(c)
