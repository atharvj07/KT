from collections import defaultdict
for _ in range(int(input())):
	n = int(input())
	s = list(map(int, input().split()))
	s.sort()
	d = defaultdict(int)
	for i in s:
		d[i] += 1
	j = 1
	while j < 2048:
		d[2 * j] += d[j] // 2
		j *= 2
	if d[2048] != 0:
		print('YES')
	else:
		print('NO')
