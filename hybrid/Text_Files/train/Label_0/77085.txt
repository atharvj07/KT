(n, k) = map(int, input().split())
arr = list(map(int, input().split()))
d = [0 for i in range(k)]
for i in arr:
	d[i % k] += 1
ans = 0
for i in range(k // 2 + 1):
	if i == 0 or i == k - i:
		ans += d[i] // 2 * 2
	else:
		ans += min(d[i], d[k - i]) * 2
print(ans)
