from math import gcd, ceil, floor
for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	flag = 0
	for i in a:
		if ceil(i ** (1 / 2)) != floor(i ** (1 / 2)):
			flag = 1
			break
	if flag:
		print('YES')
	else:
		print('NO')
