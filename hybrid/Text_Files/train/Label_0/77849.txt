import math as mat
for j in range(int(input())):
	(k, p) = map(int, input().split())
	litt = list(map(int, input().split()))
	sm = sum(litt)
	mm = mat.ceil(sm / p)
	tt = 0
	for g in range(k):
		tt += mat.ceil(litt[g] / p)
	print(mm, tt)
