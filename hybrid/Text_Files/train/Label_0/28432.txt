#include <bits/stdc++.h>
using namespace std;
int n, k, i, g, p, a[1000000], j, x, ans;
string s;
int main() {
  cin >> n;
  while (n > 0) {
    if (n % 8 == 1) ans++;
    n /= 8;
  }
  cout << ans;
}
