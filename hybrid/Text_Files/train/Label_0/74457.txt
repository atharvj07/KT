from collections import defaultdict
for _ in range(int(input())):
	s = input()
	stack = []
	for i in range(len(s)):
		if len(stack) > 0 and s[i] == 'B':
			stack.pop()
		else:
			stack.append(s[i])
	print(len(stack))
