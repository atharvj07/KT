#include <bits/stdc++.h>
using namespace std;
int main() {
  long long h = 2, n, l, j = 2;
  cin >> n;
  cout << 2 << endl;
  for (long long i = 2; i <= n; i++) {
    l = (i * (i + 1) * (i + 1)) - i + 1;
    printf("%I64d\n", l);
  }
  return 0;
}
