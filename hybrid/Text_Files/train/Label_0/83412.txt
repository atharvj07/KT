for _ in range(int(input())):
	a = int(input())
	b = bin(a)[2:]
	if '0' not in b:
		res = 1
		for b in range(2, int(a ** 0.5) + 2):
			if a % b == 0:
				res = max(res, b)
				res = max(res, a // b)
		print(res)
		continue
	i = len(b)
	print((1 << i) - 1)
