#include <bits/stdc++.h>
using namespace std;
const int N = 222222;
char s[] = "AHIMOTUVWXY";
bool used[1010];
string t;
bool ok() {
  for (int i = 0; i < t.length(); i++) {
    if (t[i] != t[t.length() - 1 - i]) {
      return false;
    } else if (!used[t[i]]) {
      return false;
    }
  }
  return true;
}
int main() {
  for (int i = 0; s[i]; i++) used[s[i]] = 1;
  cin >> t;
  if (ok())
    puts("YES");
  else
    puts("NO");
}
