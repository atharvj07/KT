#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  ;
  long long int r, b, g, c = 0, v, z;
  cin >> r >> g >> b;
  if ((r + b + g) < 3) {
    cout << "0";
  } else {
    if (r == 0 || b == 0 || g == 0) {
      c = (r / 3) + (b / 3) + (g / 3);
      cout << c;
    } else if (((r % 3) == 0 || (b % 3) == 0) &&
               ((r % 3) == 0 || (g % 3) == 0) &&
               ((b % 3) == 0 || (g % 3) == 0)) {
      c = (r / 3) + (b / 3) + (g / 3);
      cout << c;
    } else {
      if (r % 3 == 0)
        z = r;
      else if (b % 3 == 0)
        z = b;
      else
        z = g;
      if (r < b && r < g)
        if (r != z)
          v = r % 3;
        else if (b > g)
          v = b % 3;
        else
          v = g % 3;
      else if (b < r && b < g)
        if (b % 3 != 0)
          v = b % 3;
        else if (r < g)
          v = r % 3;
        else
          v = g % 3;
      else if (g % 3 != 0)
        v = g % 3;
      else if (b < r)
        v = b % 3;
      else
        v = r % 3;
      r -= v;
      b -= v;
      g -= v;
      c = (r / 3) + (b / 3) + (g / 3);
      cout << c + v;
    }
  }
  return 0;
}
