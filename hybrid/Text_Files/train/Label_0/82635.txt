for _ in range(int(input())):
	(a, b, c) = map(int, input().split())
	maxi = abs(a - b) * 2
	if maxi >= a and maxi >= b and (maxi >= c) and (abs(a - b) > 1):
		if c > abs(a - b):
			print(abs(c - abs(a - b)))
		else:
			print(c + abs(a - b))
	else:
		print(-1)
