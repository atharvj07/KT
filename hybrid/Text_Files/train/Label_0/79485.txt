def main():
	inte = int(input(''))
	n = inte
	maxi = 0
	while n > 0:
		lista = input('').split()
		a = int(lista[0])
		b = int(lista[1])
		if a + b > maxi:
			maxi = a + b
		n -= 1
	print(maxi)
main()
