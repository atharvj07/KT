def solve(a, b):
	result = float('inf')
	result = min(result, max(2 * a, b))
	result = min(result, max(2 * b, a))
	result = min(result, a + b)
	return pow(result, 2)
for i in range(int(input())):
	(a, b) = map(int, input().split())
	print(solve(a, b))
