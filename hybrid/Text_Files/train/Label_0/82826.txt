def max_sum(x):
	ans = now = 0
	for v in a:
		now += v - x
		now = max(now, 0)
		ans = max(ans, now)
	return ans

def min_sum(x):
	ans = now = 0
	for v in a:
		now += v - x
		now = min(now, 0)
		ans = min(ans, now)
	return ans
n = int(input())
a = [int(x) for x in input().split()]
l = min(a)
r = max(a)
for _ in range(50):
	x = (r + l) / 2
	A = abs(max_sum(x))
	B = abs(min_sum(x))
	if A < B:
		r = x
	else:
		l = x
print(max(A, B))
