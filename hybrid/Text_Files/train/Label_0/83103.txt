def fun(h):
	s = ''
	i = 9
	if h <= 9:
		return h
	if h > 45:
		return -1
	while h > 0:
		h = h - i
		if h >= 0:
			s += str(i)
		elif h < 0:
			if 1 <= h + i <= 9:
				s += str(h + i)
		i -= 1
	return s[::-1]
for _ in range(int(input())):
	print(fun(int(input())))
