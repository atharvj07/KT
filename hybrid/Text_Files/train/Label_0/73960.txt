def sol(n, m):
	if n == 1 or m == 1:
		return 0
	elif n == 2 and m == 2:
		return 0
	else:
		return 1
t = int(input())
for x in range(t):
	(n, m) = map(int, input().split())
	flag = sol(n, m)
	if flag == 0:
		print('Yes')
	else:
		print('No')
