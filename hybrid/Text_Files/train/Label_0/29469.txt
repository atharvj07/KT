#include <bits/stdc++.h>
using namespace std;
char s[65007];
int main() {
  int n;
  scanf("%d%s", &n, s);
  long long cnt = 0;
  for (int i = n - 1; i >= 0; --i) {
    if ((s[i] - '0') % 2 == 0) cnt += i + 1;
  }
  printf("%I64d\n", cnt);
}
