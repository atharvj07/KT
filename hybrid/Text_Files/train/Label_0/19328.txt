#include <bits/stdc++.h>
#define r(i,n) for(int i=0;i<n;i++)
using namespace std;
int main(){
  int n,sum=0;
  string s;
  cin>>s;
  n=s.size();
  r(i,s.size())if(s[i]=='p')sum++;
  cout<<n/2-sum<<endl;
}