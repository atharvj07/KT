from sys import stdin, stdout
from collections import defaultdict, Counter, deque
for _ in range(int(stdin.readline())):
	(r, b, d) = map(int, stdin.readline().split())
	a = (min(r, b) - 1) * max(r, b) + (max(r, b) - 1)
	if a == d:
		print('YES')
	else:
		print('NO')
