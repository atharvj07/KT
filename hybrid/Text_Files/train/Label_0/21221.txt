#include <bits/stdc++.h>
using namespace std;
int a, b, c;
int main() {
  cin >> a >> b >> c;
  int ans = 0;
  for (int i = 0; i <= min(a, min(b, min(c, 2))); i++) {
    ans = max(ans, (a - i) / 3 + (b - i) / 3 + (c - i) / 3 + i);
  }
  cout << ans << endl;
  return 0;
}
