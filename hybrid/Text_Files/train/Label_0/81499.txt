import math
(l, r, k) = map(int, input().split())
a = math.ceil(math.log(l, k))
b = math.floor(math.log(r, k))
c = k ** a
if c > r:
	print(-1)
else:
	z = []
	temp = c
	while temp <= r:
		if temp >= l:
			z.append(temp)
			temp *= k
		else:
			print(-1)
			break
	print(*z)
