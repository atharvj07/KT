import os
import sys
import math
import heapq
from decimal import *
from io import BytesIO, IOBase
from collections import defaultdict, deque

def r():
	return int(input())

def rm():
	return map(int, input().split())

def rl():
	return list(map(int, input().split()))
ints = [6, 10, 14, 15, 18, 21, 27, 33]
n = 8
for _ in range(r()):
	num = r()
	a = -1
	res = False
	for i in range(n - 2):
		for j in range(i + 1, n - 1):
			for k in range(j + 1, n):
				if num - ints[i] - ints[j] - ints[k] <= 0:
					continue
				temp = num - ints[i] - ints[j] - ints[k]
				if temp != ints[i] and temp != ints[j] and (temp != ints[k]):
					a = [ints[i], ints[j], ints[k], temp]
					res = True
					break
			if res:
				break
		if res:
			break
	if a == -1:
		print('NO')
	else:
		print('Yes')
		print(a[0], a[1], a[2], a[3])
