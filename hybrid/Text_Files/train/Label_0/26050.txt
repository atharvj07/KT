#include <bits/stdc++.h>
using namespace std;
int n, w = 1, j;
int main() {
  int a, b, c, d, e, f;
  cin >> a >> b >> c >> d >> e >> f;
  if (a == d || b == e || f == c)
    cout << "YES", exit(0);
  else
    cout << "NO";
  return 0;
}
