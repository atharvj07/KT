import re
S = input()
S = S.replace(';', ',').split(',')
a = []
b = []
for s in S:
	if re.fullmatch('0|([1-9][0-9]*)', s):
		a.append(s)
	else:
		b.append(s)
print('"{}"'.format(','.join(a)) if len(a) > 0 else '-')
print('"{}"'.format(','.join(b)) if len(b) > 0 else '-')
