def read():
	return list(map(int, input().split()))
(a, b, c, d) = read()

def gcd(x, y, big=False):
	if not big:
		sum = x + y
		x = max(x, y)
		y = sum - x
	if y == 0:
		return x
	else:
		return gcd(y, x % y, True)
e = gcd(c, d)
c = c // e
d = d // e
print(str(min(a // c, b // d)))
