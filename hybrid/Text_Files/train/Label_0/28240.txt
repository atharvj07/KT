#include<bits/stdc++.h>
using namespace std;
int main () {
  int n,sum=0,a,ret=0;
  scanf ("%d",&n);
  for (int i = 0; i < n; i++) {
  	scanf ("%d",&a);
    ret += sum*a;
    sum += a;
  }
  printf ("%d\n",ret);
}