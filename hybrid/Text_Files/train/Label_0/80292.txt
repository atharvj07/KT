import math
(n, t) = map(int, input().split())
raw = list(map(int, input().split()))
q = list(map(int, input().split()))
d = dict()
a = []
for i in range(len(raw)):
	if raw[i] not in d:
		d[raw[i]] = i
for el in q:
	print(d[el] + 1, end=' ')
	for key in d:
		if d[key] < d[el]:
			d[key] += 1
	d[el] = 0
print('')
