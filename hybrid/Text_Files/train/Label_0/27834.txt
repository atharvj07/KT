#include <bits/stdc++.h>
using namespace std;
long long F(long long x) {
  if (x % 2 == 0)
    return x / 2;
  else
    return x / 2 - x;
}
int main() {
  int Q;
  cin >> Q;
  while (Q--) {
    long long x, y;
    cin >> x >> y;
    cout << F(y) - F(x - 1) << endl;
  }
}
