from sys import stdin
from math import *
for __ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	val = a[0]
	for i in range(n - 1, 0, -1):
		a[i] -= a[i - 1]
		if a[i] < 0:
			val += a[i]
	if val >= 0:
		print('YES')
	else:
		print('NO')
