#include <bits/stdc++.h>

using namespace std;

#define reps(i, n, m) for (int i = (int) (n); i < (int) (m); i++)
#define rep(i, n) reps(i, 0, (n))
#define all(v) (v).begin(), (v).end()

int main() {
    int q; cin >> q;
    map<string, int> m;
    while (q--) {
        int a; cin >> a;
        if (a == 0) {
            string k;
            int x; cin >> k >> x;
            m[k] = x;
        } else if (a == 1) {
            string k; cin >> k;
            cout << m[k] << endl;
        }
    }
}
