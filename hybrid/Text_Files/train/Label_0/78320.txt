cases = int(input())
for _ in range(cases):
	n = int(input())
	nums = list(map(int, input().split()))
	left = 0
	right = 0
	for a in nums:
		if a < left:
			break
		left += 1
	for a in nums[::-1]:
		if a < right:
			break
		right += 1
	if left + right <= n:
		print('No')
	else:
		print('Yes')
