// Problem: C. Nastia and a Hidden Permutation
// Contest: Codeforces - Codeforces Round #720 (Div. 2)
// URL: https://codeforces.com/contest/1521/problem/C
// Memory Limit: 256 MB
// Time Limit: 3000 ms
// 
// Powered by CP Editor (https://cpeditor.org)

#include <bits/stdc++.h> 
using namespace std;
#define int long long int 
#define float  long double
#define pb push_back
#define mp make_pair
#define pii pair<int,int>  
#define ppi pair<pair<int,int>,int>
#define pip pair<int, pair<int,int>>
#define vi vector<int>
#define mii map<int,int>
#define setbits(n) __builtin_popcount(n)
#define yes cout<<"YES"<<endl;
#define no cout<<"NO"<<endl;
#define fi first
#define se second
#define rep(i,n) for(int i=0;i<n;i++)
#define all(v) v.begin(), v.end()
#define showa(a) cout<<a<<endl;
#define showaa(a, b) cout<<a<<' '<<b<<endl;
#define showaaa(a, b, c) cout<<a<<' '<<b<<' '<<c<<endl;
#define show(arr) for (auto i:arr) cout<<i<<' '; 
#define enter cout<<endl;
#define tkxy int x,y; cin>>x>>y; 
#define tkn int n; cin>>n;
#define IOS ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
int mod = 1e9+7;
const int  N = 2e5+5;


void cases()
{

int n; cin>>n;
vi a(n+1,0);
int idx = 0;
for(int i=2;i<=n;i+=2)
{
    cout<<"? "<<1<<" "<<i-1<<" "<<i<<" "<<n-1<<endl;
    int maxi ;
    cin>>maxi;
    if(maxi==-1)
    {
        return;
    }
    
    if(maxi==n)
    {
        a[i] = n;
        idx = i;
    }
    else if(maxi==n-1)
    {
        cout<<"? "<<1<<" "<<i<<" "<<i-1<<" "<<n-1<<endl;
        int maxi2 ; cin>>maxi2; 
    if(maxi2==-1)
    {
        return;
    }
    
    if(maxi2==n)
    {
        a[i-1] = n;
        idx = i-1;
    }
        

    }
}
   if(idx == 0)
   {
   idx = n;
   a[n] = n;
   }

    
for(int i=1;i<=n;i++)
{
    if(a[i]==0)
    {
        cout<<"? "<<2<<" "<<i<<" "<<idx<<" "<<1<<endl;
        int mini; 
        cin>>mini;
        if(mini==-1)
        {
            return;
        }
        a[i] = mini;
    
    }
}
cout<<"! ";
for(int i=1;i<=n;i++)
cout<<a[i]<<" ";
cout<<endl;

    
   
    
    
    
}

int32_t main()
{
    IOS
    
      int t; cin>>t; 
      while(t--)
      {
         cases();
      }
}
         
