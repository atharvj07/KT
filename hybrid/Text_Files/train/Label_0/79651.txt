(a, b, x) = map(int, input().split())
if x % 2 == 1:
	t = x // 2 + 1
	print('1' * (b - t) + '10' * t + '0' * (a - t))
else:
	t = x // 2
	if b > a:
		print('10' * t + '0' * (a - t) + '1' * (b - t))
	else:
		print('0' * (a - t) + '1' * (b - t) + '10' * t)
