for t in range(int(input())):
	n = int(input())
	arr = map(lambda x: int(x) % n, input().split())
	arr = list(arr)
	for i in range(n):
		arr[i] += i
		arr[i] %= n
	arr = sorted(arr)
	true_arr = list(range(n))
	print('YES' if true_arr == arr else 'NO')
