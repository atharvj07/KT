#include<bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  int t = 0;
  int ans = 0;
  for (char c : s) {
    t = (c == 'R' ? t + 1 : 0);
    ans = max(ans, t);
  }
  cout << ans << endl;
}