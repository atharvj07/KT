#include <cstdio>
long long n, a, x, d[55][5005];
int C=2500;
int main() {
	int i, j;
	scanf("%lld%lld", &n, &a);
	d[0][C] = 1;
	for(i=1; i<=n; i++) {
		scanf("%lld", &x);
		x -= a;
		for(j=0; j<=2*C; j++) {
			d[i][j] += d[i-1][j];
			d[i][j+x] += d[i-1][j];
		}
	}
	printf("%lld", d[n][C]-1);
}