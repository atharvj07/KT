#include <bits/stdc++.h>

using namespace std;

int main () {
    double d; cin >> d;
    double ans;
    if(d>1-1e10) ans=max(sqrt(2)*d,floor(d)+1);
    else ans=sqrt(2)*d;
    printf("%.10f\n",ans);
    
    return 0;
}
