for s in [*open(0)][1:]:
	(n, x, y) = map(int, s.split())
	diff = y - x
	for d in range(1, diff + 1):
		if diff % d:
			continue
		if diff // d + 1 > n:
			continue
		k = min((y - 1) // d, n - 1)
		a0 = y - k * d
		for i in range(n):
			print(a0 + i * d, end=' ')
		print('')
		break
