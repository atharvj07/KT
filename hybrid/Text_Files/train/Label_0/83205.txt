from math import ceil

def selected_sum(l, p):
	r = 0
	for i in l:
		r += l[p]
	return r

def solve(n, w, a):
	(l, s) = ([], [])
	for i in range(n):
		l.extend([ceil(a[i] / 2)])
	dif = w - sum(l)
	if dif < 0:
		return -1
	for i in range(n):
		for j in range(i):
			if a[i] > a[j] and l[i] < l[j]:
				s.append([l[j] - l[i], i])
	if selected_sum(s, 0) > dif:
		return -1
	for k in s:
		l[k[1]] += k[0]
	S = [i[0] for i in sorted(enumerate(a), key=lambda x: x[1])]
	dif = w - sum(l)
	if dif < 0:
		return -1
	if dif == 0:
		return l
	while sum(l) != w:
		if l[S[i]] < A[S[i]]:
			l[S[i]] += min(A[S[i]] - l[S[i]], dif)
			dif = w - sum(l)
			if i == 0 and dif > 0:
				return -1
			i -= 1
	return l
(N, W) = [int(i) for i in input().split()]
A = [int(i) for i in input().split()]
if solve(N, W, A) == -1:
	print(solve(N, W, A))
else:
	for (i, item) in enumerate(solve(N, W, A)):
		if i != len(solve(N, W, A)) - 1:
			print(str(item) + ' ', end='')
		else:
			print(item)
