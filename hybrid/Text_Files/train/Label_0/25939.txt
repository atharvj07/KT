#include <iostream>
#include <cmath>
using namespace std;

int main(){
	int dp[100000],n,v[200000],ans;
	cin>>n;
	for(int i=0;i<n;i++){
		cin>>v[i];
	}
	dp[0]=0;
	dp[1]=abs(v[1]-v[0]);
	for(int i=2;i<n;i++){
		dp[i]=min(abs(v[i]-v[i-1])+dp[i-1],abs(v[i]-v[i-2])+dp[i-2]);
	}

	
	cout<<dp[n-1];
}
	