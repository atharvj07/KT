#include <bits/stdc++.h>
const int mod = 1e9 + 7;
using namespace std;
using namespace std;
const int maxn = 3e5 + 55;
const int MAX = 1e3 + 10;
int a[maxn], b[maxn];
int n, k, t;
signed main() {
  cin >> n;
  int ans = 0;
  for (int i = 0; i < n; i++) cin >> a[i];
  sort(a, a + n);
  for (int i = 1; i < n; i += 2) {
    ans += a[i] - a[i - 1];
  }
  cout << ans << '\n';
}
