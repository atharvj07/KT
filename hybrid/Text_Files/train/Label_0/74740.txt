n = int(input())
h = []
for i in range(n):
	h.append(int(input()))
count = h[0] + 1
prev = h[0]
for i in range(1, len(h)):
	if h[i] > prev:
		count += h[i] - prev + 2
		prev = h[i]
	elif h[i] < prev:
		count += prev - h[i] + 2
		prev = h[i]
	elif h[i] == prev:
		count += 2
print(count)
