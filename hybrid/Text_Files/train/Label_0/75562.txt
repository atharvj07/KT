(n, m, k) = map(int, input().split())
b = list(map(int, input().split()))
c = [b[i + 1] - b[i] - 1 for i in range(n - 1)]
c.sort()
if k == n:
	ans = n
	print(ans)
	import sys
	sys.exit(0)
else:
	pass
ans = sum(c[:n - k]) + n
print(ans)
