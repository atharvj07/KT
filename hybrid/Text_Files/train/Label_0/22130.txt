#include <bits/stdc++.h>
using namespace std;
int a, b, c, i, j, n, m;
long long x[100001];
int main() {
  cin >> n;
  for (i = 0; i < n; i++) cin >> x[i];
  cin >> m;
  for (i = 0; i < m; i++) {
    cin >> a >> b;
    x[0] = max(x[0], x[a - 1]);
    cout << x[0] << endl;
    x[0] += b;
  }
  return 0;
}
