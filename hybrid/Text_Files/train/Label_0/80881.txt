try:
	import sys
	import math
	import collections
	M = 10 ** 9 + 7

	def fillPrefixSum(arr, n, prefixSum):
		prefixSum[0] = arr[0]
		for i in range(1, n):
			prefixSum[i] = prefixSum[i - 1] + arr[i]

	def solve():
		(length, q) = map(int, input().split())
		string = input()
		string = string.replace('\r', '')
		d = {}
		count = 1
		for i in range(97, 123):
			d[chr(i)] = count
			count += 1
		arr = []
		for i in string:
			arr.append(d.get(i))
		prefixSum = [0 for i in range(length)]
		fillPrefixSum(arr, length, prefixSum)
		for i in range(q):
			(a, b) = map(int, input().split())
			total = prefixSum[b - 1] - prefixSum[a - 1] + arr[a - 1]
			print(total)
		return
	count1 = 1
	solve()
except EOFError:
	print('')
