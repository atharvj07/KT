import math
n = int(input())
width = int(math.sqrt(n))
start = n - width + 1
ans = list()
while True:
	stop = False
	for v in range(start, start + width):
		if v > 0:
			ans.append(v)
		if v < 1:
			stop = True
	start -= width
	if stop:
		break
print(*ans)
