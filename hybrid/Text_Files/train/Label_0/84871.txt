t = int(input())
for _ in range(t):
	(n, k) = map(int, input().split(' '))
	a = list(map(int, input().split(' ')))
	s = set(a)
	num = len(s)
	if k == 1 and num > 1:
		print(-1)
	else:
		num -= k
		k -= 1
		count = 1
		while num > 0:
			num -= k
			count += 1
		print(count)
