#include <bits/stdc++.h>
using namespace std;
const int MAXH = 1010;
const int MAXW = 1010;
int main() {
  int n, m;
  cin >> n >> m;
  long long x1, y1, x2, y2;
  x1 = abs(n) + abs(m);
  y2 = abs(n) + abs(m);
  y1 = 0;
  x2 = 0;
  if (n < 0) x1 = -x1;
  if (m < 0) y2 = -y2;
  if (x1 < x2)
    cout << x1 << " " << y1 << " " << x2 << " " << y2 << endl;
  else
    cout << x2 << " " << y2 << " " << x1 << " " << y1 << endl;
  return 0;
}
