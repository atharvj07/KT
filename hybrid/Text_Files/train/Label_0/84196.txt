(n, k) = map(int, input().split())
a = list(map(int, input().split()))
(box, num) = (0, 0)
mod = n + 1
for i in range(k):
	m = n % a[i]
	if m < mod:
		mod = m
		box = i + 1
		num = n // a[i]
print(box, num)
