from sys import stdin, stdout
from math import gcd, sqrt, factorial, pi, inf
from collections import deque, defaultdict
from bisect import bisect, bisect_left
from time import time
from itertools import permutations as per
input = stdin.readline
R = lambda : map(int, input().split())
I = lambda : int(input())
S = lambda : input().rstrip('\r\n')
L = lambda : list(R())
P = lambda x: stdout.write(str(x) + '\n')
lcm = lambda x, y: x * y // gcd(x, y)
nCr = lambda x, y: f[x] * inv(f[y] * f[x - y] % N) % N
inv = lambda x: pow(x, N - 2, N)
sm = lambda x: (x ** 2 + x) // 2
N = 10 ** 9 + 7

def first(a, b):
	r = []
	for i in range(len(a)):
		if b[i] == 'R':
			r += (a[i],)
		elif r:
			x = abs(r[-1] - a[i]) // 2
			ans[ind[a[i]]] = x
			ans[ind[r.pop()]] = x
	for i in range(len(a)):
		if ans[ind[a[i]]] != -1:
			a[i] = 0

def sec():
	global a, b
	b = [b[i] for i in range(len(a)) if a[i]]
	a = [i for i in a if i]
	l = []
	r = []
	for i in range(len(a)):
		if b[i] == 'L':
			l += (a[i],)
		else:
			r += (a[i],)
	l.reverse()
	for i in range(len(l) // 2):
		x = abs(l[-1] - l[-2]) // 2 + l[-1]
		ans[ind[l.pop()]] = x
		ans[ind[l.pop()]] = x
	for i in range(len(r) // 2):
		x = abs(r[-1] - r[-2]) // 2 + m - r[-1]
		ans[ind[r.pop()]] = x
		ans[ind[r.pop()]] = x
	if l and r:
		x = abs(r[-1] - l[-1]) // 2 + m - r[-1] + l[-1]
		ans[ind[r.pop()]] = x
		ans[ind[l.pop()]] = x
for _ in range(I()):
	(n, m) = R()
	a = L()
	ind = {}
	for i in range(n):
		ind[a[i]] = i
	dr = S().split()
	p = sorted(zip(a, dr))
	a = [i[0] for i in p]
	dr = [i[1] for i in p]
	e = []
	de = []
	o = []
	do = []
	for i in range(n):
		if a[i] % 2:
			o += (a[i],)
			do += (dr[i],)
		else:
			e += (a[i],)
			de += (dr[i],)
	ans = [-1] * n
	first(e, de)
	first(o, do)
	a = e
	b = de
	sec()
	a = o
	b = do
	sec()
	print(*ans)
