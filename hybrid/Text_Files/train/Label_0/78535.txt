def gcd(a, b):
	if b == 0:
		return a
	return gcd(b, a % b)

def lcm(x, y):
	return int(x * y / gcd(x, y))
for _ in range(int(input())):
	s = input()
	t = input()
	lcm_s = lcm(len(s), len(t))
	s *= lcm_s // len(s)
	t *= lcm_s // len(t)
	if s == t:
		print(s)
	else:
		print(-1)
