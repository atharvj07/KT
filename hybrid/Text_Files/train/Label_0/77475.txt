import math

def solve(n, k):
	k = min(n, k)
	c = 1
	p = 0
	while c <= k:
		c = c * 2
		p = p + 1
	p = p + (n - c) // k
	if (n - c) % k != 0:
		p = p + 1
	return p
t = int(input())
for _ in range(t):
	(n, k) = list(map(int, input().split()))
	print(solve(n, k))
