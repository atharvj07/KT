(w, m) = [int(x) for x in input().split()]
for i in range(101):
	if m % w == 0:
		m /= w
	elif (m - 1) % w == 0:
		m = m // w
	elif (m + 1) % w == 0:
		m = (m + 1) / w
	else:
		print('NO')
		exit()
if m == 0:
	print('YES')
else:
	print('NO')
