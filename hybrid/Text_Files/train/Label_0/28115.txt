#include <bits/stdc++.h>
int main() {
  int i, count = 0, worker, num;
  scanf("%d", &num);
  for (i = 2; i < num; i++) {
    worker = (num - i);
    if (worker % i == 0) {
      count++;
    }
  }
  printf("%d", count + 1);
  return 0;
}
