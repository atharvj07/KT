n = int(input())
arr = [tuple(map(int, input().split())) for i in range(n)]
ans = 0
for i in range(n):
	for j in range(n):
		if arr[i] == arr[j]:
			continue
		if arr[i][0] == arr[j][1]:
			ans += 1
print(ans)
