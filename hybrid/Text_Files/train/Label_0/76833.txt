import time
n = int(input())

def check(k):
	m = n
	t = 0
	while m >= k:
		m -= k
		t += k
		d = m // 10
		m -= d
	t += m
	return 2 * t >= n
low = 1
high = n
while low < high:
	mid = low + (high - low) // 2
	if check(mid):
		high = mid
	else:
		low = mid + 1
print(low)
