n = int(input())
commands = input()
(dx, dy) = (0, 0)
for command in commands:
	if command == 'U':
		dy += 1
	elif command == 'D':
		dy -= 1
	elif command == 'L':
		dx -= 1
	elif command == 'R':
		dx += 1
(dx, dy) = (abs(dx), abs(dy))
print(n - dx - dy)
