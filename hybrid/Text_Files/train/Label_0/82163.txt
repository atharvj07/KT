from math import gcd
inp = int(input())
outp = []

def findSumNum(x):
	a = 0
	while x != 0:
		a += x % 10
		x //= 10
	return a
for i in range(inp):
	g = int(input())
	l = g
	while gcd(l, findSumNum(l)) == 1:
		l += 1
	outp.append(l)
for i in range(len(outp)):
	print(outp[i])
