#include<iostream>
#include<string>
#include<cstdio>
#include<algorithm>
#include<vector>
using namespace std;

vector<int>V[1000];
string S[1000];
void DFS(int a, int b) {//??±?????????????????????
	for (int c = 0; c < a; c++)printf(".");
	cout << S[b] << endl;
	for (int c : V[b]) {
		DFS(a + 1, c);
	}
}
signed main() {
	int a; scanf("%d", &a);
	int b; scanf("%d", &b); 
	cin >> S[0];
	for (int d = 1; d < a; d++) {
		scanf("%d", &b); cin >> S[d];
		V[b - 1].push_back(d);
	}
	DFS(0, 0);
}