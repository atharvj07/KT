#include <bits/stdc++.h>
using namespace std;
int main() {
  long long a, b, c, q;
  cin >> q;
  long long res[q];
  for (int i = 0; i < q; i++) {
    cin >> a >> b >> c;
    res[i] = (a + b + c) / 2;
  }
  for (int i = 0; i < q; i++) {
    cout << res[i] << endl;
  }
  return 0;
}
