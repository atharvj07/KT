for i in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	l = sorted(list(set(a)))
	l1 = [m for m in l if a.count(m) > 1]
	k = []
	for p in l1:
		for b in range(a.count(p) - 1):
			k.append(p)
	print(*l + k)
