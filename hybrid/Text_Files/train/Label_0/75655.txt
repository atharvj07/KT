def main():
	for _ in range(int(input())):
		(a, b, x, y) = map(int, input().split())
		A = x * b
		B = (a - x - 1) * b
		C = a * (b - y - 1)
		D = a * y
		if a == 1:
			A = 0
			B = 0
		if b == 1:
			C = 0
			D = 0
		print(max(max(A, B), max(C, D)))
main()
