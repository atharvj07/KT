(n, k) = map(int, input().split())
s = list(map(int, input().split()))
c = []
for b in range(n):
	a = s.copy()
	for i in range(-100, 100):
		if 0 <= b + i * k <= n - 1:
			a[b + i * k] = 2
	c.append(abs(a.count(-1) - a.count(1)))
print(max(c))
