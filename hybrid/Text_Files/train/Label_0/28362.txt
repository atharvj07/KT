#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x, y;
  while (cin >> n >> x >> y) {
    cout << ((x == n / 2 || x == n / 2 + 1) && (y == n / 2 || y == n / 2 + 1)
                 ? "NO"
                 : "YES")
         << endl;
  }
  return 0;
}
