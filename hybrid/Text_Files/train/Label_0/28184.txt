#include <algorithm>
#include<iostream>
#include<vector>
#include<deque>
#include<queue>
#include<list>
#include<stack>
#include<map>
#include<set>
#include<string>
#include <sstream>

#include<stdlib.h>
#include<string.h>
#include<math.h>
#include<limits.h>

using namespace std;


int main(){
  long ii,jj,kk;
  vector<int> ret;

  int n,q;
  vector<int> a;
  vector<int> swap;
  cin >> n;
  
  a.resize(n);
  swap.resize(n);
  for(ii=0;ii<n;ii++){
    cin >> a[ii];
  }

  cin >> q;

  int b,e;

  for(ii=0;ii<q;ii++){

    cin >> b >> e;
    //cout << "b:" << b << "e:" << e << endl;
    for(jj=b;jj<e;jj++){
      swap[jj] = a[b+e-jj-1];
    }
    
    for(jj=b;jj<e;jj++){
      //cout << swap[jj] << " " ;
      a[jj] = swap[jj];
    }
    
    //cout << endl;
    
  }

  for(ii=0;ii<n;ii++){
    if(ii != n-1){
      cout << a[ii] << " ";
    }
    else{
      cout << a[ii];
    }
  }
  cout << endl;
  return 0;
}

