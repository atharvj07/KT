t = int(input())
for i in range(t):
	(a, b, c, d, k) = map(int, input().split())
	x = a // c if a % c == 0 else a // c + 1
	y = b // d if b % d == 0 else b // d + 1
	if x + y <= k:
		print(x, y)
	else:
		print(-1)
