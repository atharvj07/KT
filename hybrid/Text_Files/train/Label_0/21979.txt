#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
	ll n;
	cin>>n;
	ll a[n];
	for(int i=0;i<n;i++)
		cin>>a[i];
	
	sort(a,a+n);
	ll ans=a[n-1],j=n-2;
	for(int i=n-3;i>=0;i--)
	{
		ans+=a[j];
		i--;
		if(i>=0)
			ans+=a[j];
		j--;
	}
	cout<<ans;
}