#include <bits/stdc++.h>
void zhuanba(long long int num, int *total);
int main() {
  int total = 0;
  long long int n;
  scanf("%lld", &n);
  zhuanba(n, &total);
  printf("%d\n", total);
  return 0;
}
void zhuanba(long long int num, int *total) {
  if (num != 0) {
    long long int get = num % 8;
    if (get == 1) {
      (*total)++;
    }
    num /= 8;
    zhuanba(num, total);
  }
}
