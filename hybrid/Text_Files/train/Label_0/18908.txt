#include <iostream>
#include <math.h>
using namespace std;

int main() {
    int N, ans, m = 0;
    cin >> N;
    for (int i = 0; ; ++i) {
        if (N < m) {
            ans = i - 1;
            break;
        }
        m += pow(2,i);
    }
    cout << ans << endl;
    
    return 0;
}
