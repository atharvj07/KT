from sys import stdin as fin
(k, r) = map(int, fin.readline().split())
cur = 0
while cur < 10 * k:
	cur += r
	if not cur % k:
		print(cur // k)
		break
	cur += 10 - r
	if not cur % k:
		print(cur // k)
		break
else:
	print(10)
