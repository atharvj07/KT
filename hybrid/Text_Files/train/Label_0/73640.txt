import sys
read = sys.stdin.read

def main():
	t = int(input())
	m = map(int, read().split())
	for (x1, y1, x2, y2) in zip(m, m, m, m):
		if x1 == x2 and y1 == y2:
			print(0)
		else:
			ans = abs(x1 - x2) + abs(y1 - y2) + 2
			if x1 == x2 or y1 == y2:
				ans -= 2
			print(ans)
main()
