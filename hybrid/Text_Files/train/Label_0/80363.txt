import math

def solved(n):
	ceil = int(math.ceil(math.sqrt(n)))
	f = int(math.sqrt(n))
	for i in range(1, ceil + 1):
		for j in range(1, ceil + 1):
			if n - i * ceil + j > 0:
				print(n - i * ceil + j, end=' ')
n = int(input())
solved(n)
