from copy import deepcopy
n = int(input())
ls = list(map(int, input().split()))
maxx = -1
ans = []
for i in range(1, n - 1):
	arr = deepcopy(ls)
	arr.remove(ls[i])
	for j in range(n - 2):
		if arr[j + 1] - arr[j] > maxx:
			maxx = arr[j + 1] - arr[j]
	ans.append(maxx)
	maxx = -1
print(min(ans))
