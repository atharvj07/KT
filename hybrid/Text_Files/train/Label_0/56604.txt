import java.util.Scanner;
import java.lang.String;
import java.lang.Math;
 import java.util.Arrays;
 import java.util.Collections;

public class Olymp {
 
 private static Scanner ob;

 public static void main(String[] args){
  ob = new Scanner(System.in);
  int a=ob.nextInt();
  if (a==1) System.out.print("23:59");
  else {
      String b;
      int[] my  = new int[a]; 
      for (int i = 0; i< a;i++){
        b =ob.next();
        int time  =  Character.getNumericValue(b.charAt(0))*60*10  +  Character.getNumericValue(b.charAt(1))*60 +  Character.getNumericValue(b.charAt(3))*10 +  Character.getNumericValue(b.charAt(4));
        my[i] = time;
      }
      Arrays.sort(my);
      int[] my2  = new int[a]; 
      for (int i = 1;i<a;i++){
          my2[i] = my[i] - my[i-1] -1;
      }
    
      my2[0] = my[0] + 24*60 - my[a-1] -1;
       
    Arrays.sort(my2);
    int all = my2[a-1];
      int h1 = all/600;
      all = all - h1*600;
      int h2 = all/60;
      all = all - h2*60;
      int m1 = all/10;
      all = all  - m1*10;
      System.out.print(h1);
            System.out.print(h2);
      System.out.print(":");
      System.out.print(m1);
      System.out.print(all);

  }

  }
}