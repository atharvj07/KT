#include<stdio.h>
#include<map>
typedef long long ll;
int main(){
  ll N,A,i,ans=0;
  std::map<ll,ll> L,R;
  scanf("%lld",&N);
  for(i=0;i<N;i++){
    scanf("%lld",&A);
    L[i+A]++;R[i-A]++;
  }
  for(i=1;i<2*N;i++) ans += L[i]*R[i];
  printf("%lld",ans);
}