from math import factorial
t = int(input())
for _ in range(t):
	(n, m) = map(int, input().split())
	gcd = factorial(n)
	a = list(map(int, input().split()))
	m *= gcd
	summ = 0
	for i in range(n):
		summ += a[i] * gcd
	if summ == m:
		print('YES')
	else:
		print('NO')
