#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <queue>
using namespace std;
typedef long long int ll;

int main(){
    int x,a,b,n; cin >> x >> a >> b >> n;
    for(int i=0;i<n;i++){
        string s; cin >> s;
        if(s=="nobiro"){
            x+=a;
        }
        else if(s=="tidime"){
            x+=b;
        }
        else{
            x=0;
        }
        x=max(x,0);
    }
    cout << x << endl;
}

