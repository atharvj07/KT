
import java.io.*;
import java.util.*;
import java.lang.*;

public class s {

    private static int len;
    private static String base;

    public static void main(String[] args) throws Exception {
        input();
        int b = 2;
        boolean answer = false;
        for (int i = 1; i<len; i++){
            int Number = Arrays.stream(Arrays.stream(base.substring(0, i).split("")).mapToInt(Integer::parseInt).toArray()).sum();
            b = i;
            for (int y = i + 1; y<len + 1 ; y++){
                int Number2 = Arrays.stream(Arrays.stream(base.substring(b, y).split("")).mapToInt(Integer::parseInt).toArray()).sum();
                if (Number == Number2){
                    b = y;
                    if ((y + 1 == len + 1) || Arrays.stream(Arrays.stream(base.substring(y).split("")).mapToInt(Integer::parseInt).toArray()).sum() == 0){
                        answer = true;
                        break;
                    }
                }
            }
            if (answer){
                break;
            }
        }
        System.out.println(answer ? "YES" : "NO");
    }

    private static void input() throws Exception {
        BufferedReader input = new BufferedReader(new InputStreamReader(System.in));
        len = Integer.parseInt(input.readLine());
        base = input.readLine();
        input.close();
    }

}

class method{

    public static long[] sort (long[] a){
        TreeSet <Long> mass = new TreeSet<>();
        long size = a.length;
        for (long i : a){
            mass.add(i);
        }
        long [] answer = new long [(int) size];
        int q = 0;
        for (long i:mass){
            answer[q] = i;
            q++;
        }
        return answer;
    }
    public static int[] sort (int[] a){
        TreeSet <Integer> mass = new TreeSet<>();
        int size = a.length;
        for (int i : a){
            mass.add(i);
        }
        int [] answer = new int [ size];
        int q = 0;
        for (int i:mass){
            answer[q] = i;
            q++;
        }
        return answer;
    }
    public static String[] sort (String[] a){
        TreeSet <String> mass = new TreeSet<>();
        int size = a.length;
        for (String i : a){
            mass.add(i);
        }
        String [] answer = new String [size];
        int q = 0;
        for (String i:mass){
            answer[q] = i;
            q++;
        }
        return answer;
    }
}



