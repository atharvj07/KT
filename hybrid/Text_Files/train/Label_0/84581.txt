t = input()
countOfAs = t.count('a')
if (len(t) + countOfAs) % 2:
	print(':(')
else:
	lenOfS = int((len(t) + countOfAs) / 2)
	if t[:lenOfS].replace('a', '') == t[lenOfS:]:
		print(t[:lenOfS])
	else:
		print(':(')
