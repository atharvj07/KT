import math, sys

def solve():
	n = int(input())
	if n % 3 == 0:
		print(n // 3, n // 3)
		return
	c2 = (n - 1) // 3
	c1 = 1 + c2
	if c1 + 2 * c2 == n:
		print(c1, c2)
	else:
		print(c2, c1)
t = int(input())
while t != 0:
	solve()
	t -= 1
