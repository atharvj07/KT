t = int(input())
ans = [None] * 1001
for i in range(1001):
	if i == 0:
		ans[i] = (0, 0, 0)
	elif i >= 3 and ans[i - 3]:
		(x, y, z) = ans[i - 3]
		x += 1
		ans[i] = (x, y, z)
	elif i >= 5 and ans[i - 5]:
		(x, y, z) = ans[i - 5]
		y += 1
		ans[i] = (x, y, z)
	elif i >= 7 and ans[i - 7]:
		(x, y, z) = ans[i - 7]
		z += 1
		ans[i] = (x, y, z)
	else:
		ans[i] = False
for case in range(t):
	n = int(input())
	if ans[n] == False:
		print(-1)
	else:
		print(*ans[n])
