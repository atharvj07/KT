n = int(input())
L = list(map(int, input().split()))
col = 'white'
w = 0
b = 0
for i in range(n):
	if col == 'white':
		w += (L[i] + 1) // 2
		b += L[i] // 2
		col = 'black'
	else:
		b += (L[i] + 1) // 2
		w += L[i] // 2
		col = 'white'
print(min(w, b))
