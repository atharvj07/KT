for i in range(int(input())):
	n = int(input())
	l = list(map(int, input().split()))
	k1 = 0
	k2 = 0
	for i in range(n):
		if l[i] >= i:
			k1 = i
		else:
			break
	for i in range(n):
		if l[n - i - 1] >= i:
			k2 = n - i - 1
		else:
			break
	if k1 >= k2:
		print('YES')
	else:
		print('NO')
