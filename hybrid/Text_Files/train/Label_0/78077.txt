from math import floor, ceil
from sys import stdin
from os import write
n = int(stdin.readline())
aas = list(map(float, list(stdin)))
bbs = list(map(floor, aas))
s = sum(bbs)
for i in range(n):
	if s == 0:
		break
	if ceil(aas[i]) != bbs[i]:
		bbs[i] += 1
		s += 1
print('\n'.join(map(str, bbs)))
