#include <bits/stdc++.h>
using namespace std;
int main() {
  cin.tie(0);
  cout.tie(0);
  ios_base::sync_with_stdio(0);
  string s;
  cin >> s;
  int cnt = 0;
  for (int i = 0; i < s.size(); i++) cnt += (s[i] == 'a');
  cout << (cnt > s.size() / 2 ? s.size() : cnt * 2 - 1);
  return 0;
}
