import sys
number = sys.stdin.readline()
printed = False
n_chars = len(number)
number = number[:n_chars - 1]
n_chars -= 1
for i in range(n_chars - 1):
	if number[i] == '0':
		print(number[:i] + number[i + 1:])
		printed = True
		break
if not printed:
	print(number[:n_chars - 1])
