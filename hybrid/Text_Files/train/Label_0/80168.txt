def test(a):
	se = set()
	se.add(a[0])
	ans = [a[0]]
	l = 1
	for i in range(1, len(a)):
		if a[i] == a[i - 1]:
			while l in se:
				l += 1
			ans.append(l)
			se.add(l)
		else:
			ans.append(a[i])
			se.add(a[i])
		if ans[i] > a[i]:
			print(-1)
			return
	f = False
	if len(set(ans)) == n:
		print(' '.join(map(str, ans)))
	else:
		print(-1)
	return
t = int(input())
for i in range(t):
	n = int(input())
	a = list(map(int, input().split()))
	test(a)
