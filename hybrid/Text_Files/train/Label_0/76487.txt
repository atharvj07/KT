n = int(input())
arr = {}
for i in range(n):
	s = input()
	arr[s] = i
d = list(arr.items())
from operator import itemgetter
d.sort(key=itemgetter(1), reverse=True)
for i in range(len(d)):
	print(d[i][0])
