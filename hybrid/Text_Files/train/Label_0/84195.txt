(n, k) = map(int, input().split())
a = list(map(int, input().split()))
x = n % a[0]
typ = 0
for i in range(1, k):
	temp = n % a[i]
	if temp < x:
		typ = i
		x = temp
print(typ + 1, (n - x) // a[typ])
