import java.io.*;
import java.util.*;

import static java.lang.Math.*;
import java.math.BigInteger;
public class Main {

	final static boolean fileIO = false;


	final static int INF = 0x7FFFFFFF;
	final static int EPS = (int)1e9 + 7;

	static class Pair<K,V> {
		K fs;
		V sc;
		public Pair(K fs, V sc) { this.fs = fs; this.sc = sc; }
		@Override
		public String toString() { return "(" + fs.toString() + " " + sc.toString() + ")"; }
	}

	public static void main(String args[]) throws IOException {
		InputStream is;
		OutputStream os;
		if (fileIO) {
			is = new FileInputStream("input.txt");
			os = new FileOutputStream("output.txt");
		} else {
			is = System.in;
			os = System.out;
		}
		InputReader in = new InputReader(is);
		PrintWriter out = new PrintWriter(os);
		new Trash().solve(in, out);
		//
		out.close();
	}

	@SuppressWarnings("unchecked")
	static class Trash {
		public void solve(InputReader in, PrintWriter out) {
			int n = in.nextInt();
			Integer a[] = in.nextIntArray(n);
			Arrays.sort(a);
			int _i = 0;
			int u = 12;
			while (_i <= 1000000000) {
				u = u + _i * u;
				_i++;
			}
			for (int i = 0; i < n; i++) out.print(a[i] + " ");
			out.println();
		}
	}

	@SuppressWarnings("unchecked")
	static class DataCenter {

		public void solve(InputReader in, PrintWriter out) {
		}
		//

	}


	static class InputReader {
		private StringTokenizer tokenizer;
		private BufferedReader reader;

		public InputReader(InputStream is) {
			tokenizer = null;
			reader = new BufferedReader(new InputStreamReader(is), 32768);
		}

		public String next() {
			while (tokenizer == null || !tokenizer.hasMoreTokens()) {
				try {
					String line = reader.readLine();
					if (line == null) return null;
					tokenizer = new StringTokenizer(line);
				} catch (IOException e) {
					throw new RuntimeException(e);
				}
			}
			return tokenizer.nextToken();
		}

		public Integer nextInt() { return Integer.parseInt(next()); }
		public Integer[] nextIntArray(int n) {
			Integer ret[] = new Integer[n];
			int i = 0;
			while (n-- > 0) ret[i++] = nextInt();
			return ret;
		}
		public Long nextLong() { return Long.parseLong(next()); }
		public Long[] nextLongArray(int n) {
			Long ret[] = new Long[n];
			int i = 0;
			while (n-- > 0) ret[i++] = nextLong();
			return ret;
		}
	}
}