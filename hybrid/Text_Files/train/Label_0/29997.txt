#include <bits/stdc++.h>
using namespace std;
int n, a[300008];
int main() {
  int n;
  scanf("%d", &n);
  int m = n / 2;
  int x = n / 2;
  int y = 0;
  int q = 1, r = 2;
  for (int i = 1; i <= m + 1; i++) {
    for (int j = 1; j <= y; j++) {
      printf("%d ", q);
      q += 2;
    }
    for (int b = x; b > 0; b--) {
      printf("%d ", r);
      r += 2;
    }
    printf("%d ", q);
    q += 2;
    for (int b = x; b > 0; b--) {
      printf("%d ", r);
      r += 2;
    }
    for (int j = 1; j <= y; j++) {
      printf("%d ", q);
      q += 2;
    }
    printf("\n");
    x--;
    y++;
  }
  x += 2;
  y -= 2;
  for (int i = 1; i <= m; i++) {
    for (int j = 1; j <= y; j++) {
      printf("%d ", q);
      q += 2;
    }
    for (int b = x; b > 0; b--) {
      printf("%d ", r);
      r += 2;
    }
    printf("%d ", q);
    q += 2;
    for (int b = x; b > 0; b--) {
      printf("%d ", r);
      r += 2;
    }
    for (int j = 1; j <= y; j++) {
      printf("%d ", q);
      q += 2;
    }
    printf("\n");
    x++;
    y--;
  }
  return 0;
}
