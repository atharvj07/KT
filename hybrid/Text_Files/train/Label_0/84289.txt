from collections import Counter
from itertools import groupby
input()
B = sorted(map(int, input().split()))
C = Counter(B)
B = [k for (k, _) in groupby(B)]
B += [b for b in B[-2::-1] if C[b] > 1]
print(len(B))
print(*B)
