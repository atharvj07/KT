import static java.lang.Math.*;
import static java.lang.System.currentTimeMillis;
import static java.lang.System.exit;
import static java.lang.System.arraycopy;
import static java.util.Arrays.sort;
import static java.util.Arrays.binarySearch;
import static java.util.Arrays.fill;
import java.util.*;
import java.io.*;

public class Main {

    public static void main(String[] args) throws IOException {
        try {
            if (new File("input.txt").exists())
                System.setIn(new FileInputStream("input.txt"));
        } catch (SecurityException e) {
        }
        new Main().run();
    }

    BufferedReader in;
    PrintWriter out;
    StringTokenizer st = new StringTokenizer("");

    private void run() throws IOException {
        in = new BufferedReader(new InputStreamReader(System.in));
        out = new PrintWriter(System.out);
        solve();
        in.close();
        out.close();
    }

    final int MAXN = 100001;
    char[] sms;
    String[]word = new String[MAXN];
    int n;
    
    int shift__3(int i){
        boolean ok1 = false, ok2 = false;
        while(i < sms.length && sms[i] != '<')
            i++;
        if(i < sms.length && sms[i] == '<')
            ok1 = true;
        while(i < sms.length && sms[i] != '3')
            i++;
        if(i < sms.length && sms[i] == '3')
            ok2 = true;
        if(ok1 && ok2)
            return i+1;
        return -1;
    }
    int shiftChar(int i, char ch){
        boolean ok = false;
        while(i < sms.length && sms[i] != ch)
            i++;
        if(i < sms.length && sms[i] == ch)
            return i+1;
        return -1;
    }
    
    private void solve() throws IOException {
        n = nextInt();
        for(int i = 0; i < n; i++)
            word[i] = nextToken();
        sms = nextToken().toCharArray();
        int len = sms.length;
        int i = 0;
        boolean bad = false;
        for(int w = 0; w < n; w++){
            i = shift__3(i);
            if(i == -1 || i == sms.length){
                bad = true;
                break;
            }
            char[] t = word[w].toCharArray();
            for(int k = 0; k < t.length; k++){
                i = shiftChar(i,t[k]);
                if(i == -1 || i == sms.length){
                    bad = true;
                    break;
                }
            }
            if(bad)
                break;
        }
        if(bad){
            out.println("no");
        } else {
            i = shift__3(i);
            if(i == -1)
                out.println("no");
            else
                out.println("yes");
        }
    }

    void chk(boolean b) {
        if (b)
            return;
        System.out.println(new Error().getStackTrace()[1]);
        exit(999);
    }
    void deb(String fmt, Object... args) {
        System.out.printf(Locale.US, fmt + "%n", args);
    }
    String nextToken() throws IOException {
        while (!st.hasMoreTokens())
            st = new StringTokenizer(in.readLine());
        return st.nextToken();
    }
    int nextInt() throws IOException {
        return Integer.parseInt(nextToken());
    }
    long nextLong() throws IOException {
        return Long.parseLong(nextToken());
    }
    double nextDouble() throws IOException {
        return Double.parseDouble(nextToken());
    }
    String nextLine() throws IOException {
        st = new StringTokenizer("");
        return in.readLine();
    }
    boolean EOF() throws IOException {
        while (!st.hasMoreTokens()) {
            String s = in.readLine();
            if (s == null)
                return true;
            st = new StringTokenizer(s);
        }
        return false;
    }
}
