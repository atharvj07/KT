#include<iostream>
#include<map>
using namespace std;

int ans;

int main(){
  map<int,int> mp;
  int n; cin >> n;
  for(int i=0;i<n;++i){
    int num; cin >> num;
    mp[num]++;
  }
  
  for(auto c:mp) ans += c.second % 2;
  cout << ans << endl;
  return 0;
}