def isLower(char):
	char2 = char.lower()
	if char == char2:
		return True
	else:
		return False

def indexOf(str, char):
	i = 0
	while i < len(str):
		if str[i] == char:
			return i
		i += 1
	return -1
str1 = input()
str2 = input()
str = input()
strToPrint = ''
i = 0
while i < len(str):
	char = str[i]
	islow = isLower(char)
	char = char.lower()
	index = indexOf(str1, char)
	if index == -1:
		new = char
		strToPrint += new
	else:
		new = str2[index]
		if islow:
			strToPrint += new
		else:
			strToPrint += new.upper()
	i += 1
print(strToPrint)
