#include <bits/stdc++.h>
using namespace std;
long n;
int main() {
  ios_base::sync_with_stdio(0);
  cin >> n;
  if (n == 0)
    cout << 1 << endl;
  else
    cout << 4 * (int)(sqrt(2.0) * n + 1e-9) << endl;
  cin.get();
  cin.get();
  return 0;
}
