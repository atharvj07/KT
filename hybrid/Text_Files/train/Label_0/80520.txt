n = int(input())
options = []
for i in range(n):
	a = int(input())
	if i == 0:
		options += [a]
	else:
		length = len(options)
		for b in range(length):
			options += [options[b] - a]
			options[b] += a
for c in options:
	if c % 360 == 0:
		print('YES')
		exit()
print('NO')
