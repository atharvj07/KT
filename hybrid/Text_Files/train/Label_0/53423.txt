
import java.util.*;



import java.io.*;
 
/* Name of the class has to be "Main" only if the class is public. */
public class final2
{
	static long countOddSquares( long m)
	{
	    return (long)Math.pow(m, 0.5);
	            
	}
 
    public static void main (String[] args) 

    {
        Scanner sc = new Scanner(System.in);

        int t=sc.nextInt();
        PrintWriter out=new PrintWriter(System.out);
        Set<Character> st=new HashSet<Character>();
        Set<String> st1=new HashSet<String>();
        int i,j,x=0,y=0,k;
        int d,n,m;
        int c=0,count=0;
        long sum1,sum=0;
        int min1,min;
        int pos=1;  
        String s,s1="";
        long a,b;
        int max=Integer.MIN_VALUE;
        Map<Integer,Integer> hm=new HashMap<Integer,Integer>();
        
        for(;t>0;t--)
        {
        	n=sc.nextInt();
        	s=sc.next();
        	boolean f=true;
        	c=0;
        	count=0;
        	for(i=0;i<s.length();i++)
        	{
        		if(s.charAt(i)=='M')
        			count++;
        		else
        			c++;
        		if(count>c)
        			f=false;
        		//out.println(f);
        	}
        	
        	//out.print(f);
        	if(count*2!=c)
        	{
        		f=false;
        		//out.print("done");
        	}count=c=0;
        	for(i=s.length()-1;i>=0;i--)
        	{
        		if(s.charAt(i)=='M')
        			count++;
        		else
        			c++;
        		if(count>c)
        			f=false;
        	}
        	if(f)
        		out.println("YES");
        	else
        		out.println("No");
        }
        out.close();
        
    
        
        
    }
}