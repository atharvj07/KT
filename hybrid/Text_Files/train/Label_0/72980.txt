def getLeastString(s):
	if len(s) & 1 != 0:
		return s
	x = getLeastString(s[:len(s) // 2])
	y = getLeastString(s[len(s) // 2:])
	return min(x + y, y + x)

def isEquivalent(a, b):
	return getLeastString(a) == getLeastString(b)
a = input()
b = input()
print('YES' if isEquivalent(a, b) else 'NO')
