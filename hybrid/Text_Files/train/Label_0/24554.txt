#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x, y, ans = 0;
  string s;
  cin >> n >> x >> y >> s;
  for (int i = (int)s.size() - 1, j = 0; j < x; --i, ++j) {
    if (j != y && s[i] == '1') ++ans;
    if (j == y && s[i] == '0') ++ans;
  }
  cout << ans << endl;
  return 0;
}
