for _ in range(int(input())):
	n = int(input())
	d = {}
	for i in list(map(int, input().split())):
		if abs(i) in d:
			d[abs(i)] += 1
		else:
			d[abs(i)] = 1
	ans = 0
	for i in list(d.keys()):
		if i == 0:
			ans += 1
		elif d[i] == 1:
			ans += 1
		else:
			ans += 2
	print(ans)
