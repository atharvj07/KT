#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k;
  double l, v1, v2;
  scanf("%d%lf%lf%lf%d", &n, &l, &v1, &v2, &k);
  int g = n / k;
  if (n % k != 0) g++;
  double a = (v1 + v2) * l / (v1 + v2 + 2 * (g - 1) * v1);
  printf("%.7f\n", a / v2 + (l - a) / v1);
  return 0;
}
