import sys
import math
import bisect

def main():
	(n, b1) = map(int, input().split())
	x = 0
	for a in list(map(int, input().split())):
		x = x * b1 + a
	(n, b2) = map(int, input().split())
	y = 0
	for a in list(map(int, input().split())):
		y = y * b2 + a
	if x == y:
		print('=')
	elif x > y:
		print('>')
	elif x < y:
		print('<')
main()
