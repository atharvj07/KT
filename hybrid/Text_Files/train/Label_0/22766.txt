#include <iostream>
#include <string>
using namespace std;

int main(){
  int ans = 0;
  int n;
  cin >> n;
  while(n != 0){
    int m;
    cin >> m;
    string s;
    cin >> s;
    string see = "I";
    for(int i = 0; i < n; i++){
      string temp = "OI";

      see = see+temp;
    }
    int ans =  0;
    string sn = s.substr( 0 , n*2);
    for(int i = 0; i < m - n*2 ; i ++){
      sn = s.substr(i , n*2+1);
      if(sn == see)ans++;
    }
    
  cout << ans << endl;
    cin >> n;
  }


 
}

