(n, l) = map(lambda x: int(x), input().split())
ar = list(map(lambda x: int(x), input().split()))
ar.sort()
ans = 0
for i in range(1, n):
	ans = max(ans, (ar[i] - ar[i - 1]) / 2)
print(max(ans, ar[0] - 0, l - ar[-1]))
