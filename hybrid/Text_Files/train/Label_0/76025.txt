import math, sys
from collections import defaultdict, deque
import bisect as bi

def yes():
	print('YES')

def no():
	print('NO')

def I():
	return int(sys.stdin.readline())

def In():
	return map(int, sys.stdin.readline().split())

def Sn():
	return sys.stdin.readline().strip()
from heapq import heapify, heappop, heappush

def dict(a):
	d = {}
	for x in a:
		if d.get(x, -1) != -1:
			d[x] += 1
		else:
			d[x] = 1
	return d

def find_gt(a, x):
	i = bi.bisect_left(a, x)
	if i != len(a):
		return i
	else:
		return -1

def main():
	try:
		hp = []
		n = I()
		l = []
		for i in range(n):
			s = Sn()
			if s == 'removeMin':
				a = s
			else:
				(a, b) = s.split()
				b = int(b)
			if a == 'insert':
				heappush(hp, b)
				l.append((a, b))
			elif a == 'removeMin':
				if len(hp):
					heappop(hp)
				else:
					l.append(('insert', 1))
				l.append((a, ''))
			else:
				while len(hp) and hp[0] < b:
					l.append(('removeMin', ''))
					heappop(hp)
				if not len(hp):
					heappush(hp, b)
					l.append(('insert', b))
				elif len(hp) and hp[0] != b:
					heappush(hp, b)
					l.append(('insert', b))
				l.append((a, b))
		print(len(l))
		for x in l:
			sys.stdout.write(x[0] + ' ' + str(x[1]) + '\n')
	except:
		pass
M = 998244353
P = 1000000007
for _ in range(1):
	main()
