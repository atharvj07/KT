for testcase in range(int(input())):
	arr = [int(x) for x in input().split()]
	(n, d) = (arr[0], arr[1])
	s = set()
	arr = [int(x) for x in input().split()]
	for ele in arr:
		if ele % d == 0:
			print('YES')
			s.add(ele)
		elif str(d) in str(ele):
			print('YES')
			s.add(ele)
		else:
			ele1 = ele
			while ele > 0:
				ele -= d
				if str(d) in str(ele):
					print('YES')
					s.add(ele1)
					break
			else:
				print('NO')
