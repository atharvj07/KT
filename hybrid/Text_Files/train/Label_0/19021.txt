#include <bits/stdc++.h>
int main() {
  int a, dvd, dvs;
  scanf("%d", &a);
  for (int i = 2; i <= a / i; i++) {
    if (a % i == 0) {
      dvd = i;
      dvs = a / i;
      break;
    }
  }
  printf("%d%d", dvd, dvs);
}
