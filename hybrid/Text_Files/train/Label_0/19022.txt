#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int x, ans;
  cin >> x;
  for (int i = 2; i < int(sqrt(x)) + 1; i++) {
    if (x % i == 0) ans = i;
  }
  cout << ans << x / ans;
}
