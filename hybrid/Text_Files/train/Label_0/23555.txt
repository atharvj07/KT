#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n,m,c;
set <ll> s;
int main(){
	cin>>n>>m;
	while (n) s.insert(n % m),n /= m,c += 1;
	cout<<(c == s.size() ? "YES" : "NO")<<endl;
	return 0;
}