import math
n = int(input())
d = []

def check(p):
	for k in range(2, p):
		if p % (math.pow(2, k) - 1) == 0:
			t = int(p / (math.pow(2, k) - 1))
			return d.append(str(t))
		else:
			continue
for i in range(n):
	b = int(input())
	check(b)
for x in d:
	print(x)
