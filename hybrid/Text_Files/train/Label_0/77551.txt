def main():
	(a, b) = map(int, input().split())
	dp = [0]
	for i in range(1, 10):
		temp = i * b % 10
		dp.append(temp)
		dp[i] += dp[i - 1]
	div = a // b
	ans = dp[-1] * (div // 10)
	ans += dp[int(a // b % 10)]
	print(ans)
t = int(input())
while t:
	main()
	t -= 1
