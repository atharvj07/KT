#include <iostream>
#include <cstdio>
using namespace std;
int main(){
  char ch;
  double bmi,w,h;
  int id;
  while( scanf("%d,%lf,%lf",&id,&w,&h) != EOF)
    {
        bmi = w / (h*h);
      if(bmi >= 25)cout << id << endl;
    }
  return 0;
}