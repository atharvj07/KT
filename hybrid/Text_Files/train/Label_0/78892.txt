import math
test = int(input())
for t in range(test):
	n = int(input())
	x = list(map(int, input().split()))
	m = {}
	for i in range(n):
		if m.get(x[i] - i):
			m[x[i] - i] += 1
		else:
			m[x[i] - i] = 1
	a = 0
	for i in m.values():
		a += i * (i - 1) // 2
	print(a)
