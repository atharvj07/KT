import math
import sys
input = sys.stdin.readline

def inp():
	return int(input())

def inlt():
	return list(map(int, input().split()))

def insr():
	s = input().strip()
	return list(s[:len(s)])

def invr():
	return map(int, input().split())
n = inp()
l = inlt()
dp = [[0 for i in range(2)] for j in range(n)]
for i in range(n):
	if l[i] == 1:
		if max(dp[i - 1][0], dp[i - 1][1]) == dp[i - 1][1]:
			dp[i][0] = dp[i - 1][1] + 1
	elif l[i] == 2:
		if max(dp[i - 1][0], dp[i - 1][1]) == dp[i - 1][0]:
			dp[i][1] = dp[i - 1][0] + 1
	elif l[i] == 3:
		(dp[i][0], dp[i][1]) = (dp[i - 1][1] + 1, dp[i - 1][0] + 1)
mx = 1
cnt = 0
for i in range(n):
	if max(dp[i][0], dp[i][1]) == 0:
		cnt += 1
print(cnt)
