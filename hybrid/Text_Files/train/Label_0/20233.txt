#include <bits/stdc++.h>
int main() {
  int n, i, j, a, b;
  scanf("%d", &n);
  for (i = sqrt(n); i >= 1; i--) {
    for (j = sqrt(n); j <= n; j++)
      if (i * j == n) {
        printf("%d %d\n", i, j);
        break;
      }
    if (i * j == n) break;
  }
  return 0;
}
