qtd = int(input())
for _ in range(qtd):
	p = input()
	h = input()
	tam_p = len(p)
	tam_h = len(h)
	if tam_p > tam_h:
		print('NO')
		continue
	p = sorted(p)
	for i in range(tam_h):
		if p == sorted(h[i:i + tam_p]):
			print('YES')
			break
	else:
		print('NO')
