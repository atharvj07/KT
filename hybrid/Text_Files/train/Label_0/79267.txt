def main():
	n = int(input())
	L = list(map(int, input().split()))
	setL = set(L)
	rep = -1
	for i in setL:
		for j in setL:
			if j % i != 0:
				rep = -1
				break
			else:
				rep = i
		if rep != -1:
			break
	print(rep)
main()
