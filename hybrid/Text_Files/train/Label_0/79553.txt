(n, k, m, D) = map(int, input().split())
ans = 0
for d in range(1, D + 1):
	xmax = min(n // ((d - 1) * k + 1), m)
	if xmax == 0:
		continue
	if n % (xmax * k) >= xmax:
		if n // (xmax * k) == d - 1:
			ans = max(xmax * d, ans)
	elif n // (xmax * k) == d:
		ans = max(xmax * d, ans)
print(ans)
