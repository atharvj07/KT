#include <bits/stdc++.h>
using namespace std;
int i, j, k, l, m, n;
string s, ss, s1;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> s;
  cout << s;
  reverse(s.begin(), s.end());
  cout << s;
}
