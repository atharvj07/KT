import math
from bisect import *
R = lambda : map(int, input().split())
(t,) = R()
while t:
	t -= 1
	(n,) = list(R())
	arr = list(R())
	m = max(arr)
	s = sum(arr)
	avg = math.ceil(s / (n - 1))
	ans = max(avg, m) * (n - 1) - s
	print(ans)
