import java.util.Scanner;
import java.io.OutputStream;
import java.io.IOException;
import java.io.PrintWriter;
import java.io.InputStream;

/**
 * Built using CHelper plug-in
 * Actual solution is at the top
 * @author Anudeep Nekkanti
 */
public class Main {
	public static void main(String[] args) {
		InputStream inputStream = System.in;
		OutputStream outputStream = System.out;
		Scanner in = new Scanner(inputStream);
		PrintWriter out = new PrintWriter(outputStream);
		TaskA solver = new TaskA();
		solver.solve(1, in, out);
		out.close();
	}
}

class TaskA {
    public void solve(int testNumber, Scanner in, PrintWriter out) {
      int h, h1, h2, a, a1, a2, b, b1, b2;
      h1 = in.nextInt();
      a1 = in.nextInt();
      b1 = in.nextInt();

      h2 = in.nextInt();
      a2 = in.nextInt();
      b2 = in.nextInt();

      h = in.nextInt();
      a = in.nextInt();
      b = in.nextInt();


      int ans = 1000*1000*1000;
//      for (int i = 0; i < 201; i++) {
//        for (int j = 0; j < 201; j++) {
//          for (int k = 0; k < 201; k++) {
//            int cur = i*h + j * a + k *b;
//            if(cur >= ans) continue;
//            h1 += i;
//            a1 += j;
//            b1 += k;
//            if(ok(h1, a1, b1, h2, a2, b2)) {
//              ans = cur;
//            }
//            h1 -= i;
//            a1 -= j;
//            b1 -= k;
//          }
//        }
//      }

      int newAns = 1000*1000*1000;
      for (int j = 0; j < 250; j++) {
        for (int k = 0; k < 250; k++) {
          a1 += j;
          b1 += k;
          int cur = j * a + k * b;
          int monsterAttack = Math.max(0, a2 - b1);
          int myAttack = Math.max(0, a1 - b2);
          if(myAttack > 0) {
            int timeToKill = (h2 + myAttack - 1) / myAttack;
            int toTake = timeToKill * monsterAttack;

            int toBuy = Math.max(0, toTake - h1 + 1);
            cur += toBuy * h;

            newAns = Math.min(newAns, cur);
          }
          a1 -= j;
          b1 -= k;
        }
      }
      out.println(newAns);
    }

}

