#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  long long h, l;
  cin >> h >> l;
  long double res = (long double)(l * l - h * h) / (2.0 * h);
  cout << setprecision(12) << fixed << res << '\n';
  return 0;
}
