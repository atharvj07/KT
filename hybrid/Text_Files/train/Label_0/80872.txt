import sys
input = sys.stdin.readline
t = int(input())
for _ in range(t):
	(d, m) = map(int, input().split())
	ans = 1
	i = 0
	while pow(2, i) <= d:
		ans = ans % m * (1 + min(pow(2, i + 1) - pow(2, i), d - pow(2, i) + 1) % m) % m
		i += 1
	print((ans - 1 + m) % m)
