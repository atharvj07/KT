for _ in range(int(input())):
	(k, n, a, b) = list(map(int, input().split()))
	c = k - n * a
	diff = a - b
	if c > 0:
		print(n)
	else:
		turns = (-c + 1 + diff - 1) // diff
		if turns > n:
			print(-1)
		else:
			print(n - turns)
