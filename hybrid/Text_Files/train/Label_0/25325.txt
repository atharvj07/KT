#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    int i, j, k, s = n;
    while (n >= 10) {
      s = n / 10 + s;
      n = n / 10 + (n % 10);
    }
    cout << s << endl;
    n = 0;
  }
}
