def main():
	(k, q, s, res) = (int(input()), input(), set(), ['YES'])
	for (i, c) in enumerate(q):
		if c not in s:
			s.add(c)
			res.append('\n')
			k -= 1
			if not k:
				res.append(q[i:])
				print(''.join(res))
				return
		res.append(c)
	print('NO')
main()
