#include <bits/stdc++.h>
using namespace std;
int arr[1000], n, p, res;
int main() {
  scanf("%d", &n);
  for (int i = 0; i < n; i++) {
    scanf("%d", &p);
    arr[p]++;
  }
  for (int i = 1; i < 1000; i++) res += arr[i] >> 1;
  printf("%d", res >> 1);
}
