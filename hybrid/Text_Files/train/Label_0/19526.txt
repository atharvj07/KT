#include <bits/stdc++.h>

using namespace std;

int main() {
  long long n;
  cin >> n;
  auto ans = 0ll;
  for (auto d = 10ll; d <= n; d *= 5) ans += n / d;
  cout << (n & 1 ? 0 : ans) << endl;
  return 0;
}
