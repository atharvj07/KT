for _ in range(int(input())):
	n = int(input())
	s = input()
	if '0' in s:
		i = s.index('0')
		if i < n // 2:
			print(i + 1, n, i + 2, n)
		else:
			print(1, i + 1, 1, i)
	else:
		print(1, n // 2, n // 2 + 1, n // 2 + n // 2)
