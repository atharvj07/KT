n = int(input())
a = list(map(int, input().split()))
b = list(map(int, input().split()))
swaps = 0
for i in range(1, 6):
	a_count = a.count(i)
	b_count = b.count(i)
	diff = abs(a_count - b_count)
	if diff % 2 != 0:
		swaps = -2
		break
	swaps += diff // 2
print(swaps // 2)
