n = int(input())
N = 2 * n - 2
ans = 0
for i in range(n):
	l_cnt = i
	r_cnt = N - i - n
	if r_cnt < 0:
		break
	m = 4
	p = int(l_cnt > 0) + int(r_cnt > 0)
	m *= 3 ** p
	l_cnt = max(0, l_cnt - 1)
	r_cnt = max(0, r_cnt - 1)
	p = l_cnt + r_cnt
	m *= 4 ** p
	ans += m
print(ans)
