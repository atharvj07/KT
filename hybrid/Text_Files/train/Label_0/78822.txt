from sys import stdin
from collections import Counter

def main():
	(n, k) = map(int, input().split())
	ss = [int(input()) for _ in range(n)]
	sc = [x % 2 for x in Counter(ss).values()]
	result = n - sum(sc) // 2
	print(result)
main()
