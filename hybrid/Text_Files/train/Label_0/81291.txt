from math import ceil
t1 = int(input())
for i in range(t1):
	(n, r) = map(int, input().split())
	x = min(r, n)
	ans = 0
	if n == x:
		ans = x * (x - 1) // 2
		ans += 1
	else:
		ans = x * (x + 1) // 2
	print(ans)
