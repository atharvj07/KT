(l1, r1, l2, r2, k) = [int(x) for x in input().split()]
left = max(l1, l2)
right = min(r1, r2)
if left > right:
	print('0')
elif k <= right and k >= left:
	print(right - left)
else:
	print(right - left + 1)
