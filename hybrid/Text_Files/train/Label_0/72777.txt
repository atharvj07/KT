def check(a, ind):
	co = 0
	for i in range(ind + 1):
		if a[i] == '0':
			co += 1
		else:
			co -= 1
	return co
t = int(input())
for T in range(t):
	n = int(input())
	a = list(input())
	b = list(input())
	i = len(a) - 1
	sw = 0
	c = 0
	good = 0
	c = check(a, len(a) - 1)
	while i >= 0:
		if sw == 0:
			if a[i] == b[i]:
				pass
			else:
				sw = 1
				if c == 0:
					pass
				else:
					good = 1
					break
		elif sw == 1:
			if a[i] != b[i]:
				pass
			else:
				sw = 0
				if c == 0:
					pass
				else:
					good = 1
					break
		if a[i] == '0':
			c -= 1
		elif a[i] == '1':
			c += 1
		i -= 1
	if good == 1:
		print('NO')
	else:
		print('YES')
