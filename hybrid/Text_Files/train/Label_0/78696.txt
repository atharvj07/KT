import math

def print_result(ans):
	print(ans)

def solve(n):
	x = math.floor(math.sqrt(n))
	return x + math.ceil(n / x) - 2
t = int(input())
for i in range(1, t + 1):
	n = int(input())
	ans = solve(n)
	print_result(ans)
