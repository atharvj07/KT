#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, i, ans = 0, a[2205];
  cin >> n >> m;
  for (i = 0; i < n; i++) {
    cin >> a[i];
  }
  sort(a, a + n);
  for (i = 0; i < m; i++) {
    ans = ans + a[i];
  }
  cout << ans;
  return 0;
}
