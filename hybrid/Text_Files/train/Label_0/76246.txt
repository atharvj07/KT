def main():
	n = int(input())
	a = list(map(int, input().split()))
	cnt = [[0] * (1 << 21) for _ in range(2)]
	cnt[1][0] = 1
	(curr, ret) = (0, 0)
	for (i, x) in enumerate(a):
		curr ^= x
		ret += cnt[i % 2][curr]
		cnt[i % 2][curr] += 1
	print(ret)
main()
