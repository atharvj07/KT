#include <bits/stdc++.h>
using namespace std;
int main() {
  int h, m;
  scanf("%d:%d", &h, &m);
  double d1;
  int d2;
  d2 = m * 6;
  if (h == 12)
    h = 0;
  else if (h > 12)
    h -= 12;
  d1 = h * 30;
  d1 += .5 * m;
  cout << d1 << " " << d2;
}
