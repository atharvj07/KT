#include <stdio.h>
#include <string.h>

long int gcd(long int a, long int b)
{
return (b==0 ? a : gcd (b, a%b));
}
int main()
{
    long long a, b;
    while(scanf ("%lld %lld", &a, &b) != EOF)
    {
              long long x = gcd(a,b);
              printf("%d\n", x);
    }
return 0;
}