#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n = 4, i = 0, a[5], result = 0;
  while (n--) cin >> a[i++];
  for (i = 0; i < 3; i++)
    for (long long j = i + 1; j < 4; j++)
      if (a[j] == a[i]) {
        result++;
        break;
      }
  cout << result;
}
