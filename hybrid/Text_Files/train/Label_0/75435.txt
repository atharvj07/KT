t = int(input())
for _ in range(t):
	n = int(input())
	s = input()
	ans = True
	for (a, b) in zip(s[:n // 2], s[::-1]):
		(x, y) = map(ord, (a, b))
		if abs(x - y) not in (0, 2):
			ans = False
			break
	if ans:
		print('YES')
	else:
		print('NO')
