def my_fun(n, k):
	(v, p) = (0, 0)
	while n:
		v += min(k, n)
		n = max(n - k, 0)
		p += n // 10
		n -= n // 10
	return v >= p
m = int(input())
(low, high) = (1, m)
while low <= high:
	mid = (low + high) // 2
	if my_fun(m, mid):
		high = mid - 1
	else:
		low = mid + 1
print(low)
