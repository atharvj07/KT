#include <bits/stdc++.h>
using namespace std;

int main()
{
  int a, b, h, m;
  cin >> a >> b >> h >> m;
  double dh = 30 * h + m / 2.0;
  double dm = 6 * m;
  cout << fixed << setprecision(10) << sqrt(a * a + b * b - 2 * a * b * cos(abs(dh - dm) * M_PI / 180.0)) << "\n";
}