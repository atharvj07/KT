q = int(input())
for u in range(q):
	(a, b, x, y) = [int(i) for i in input().split()]
	if x < a - x - 1:
		c = a - x - 1
	else:
		c = x
	if y < b - y - 1:
		d = b - y - 1
	else:
		d = y
	if c * b > d * a:
		print(c * b)
	else:
		print(d * a)
