import sys
t = int(sys.stdin.readline())
for inp in range(t):
	n = int(sys.stdin.readline())
	s = sys.stdin.readline().strip()
	ans = 'YES'
	for pos in range(n // 2 + n % 2):
		delta = abs(ord(s[pos]) - ord(s[-1 - pos]))
		if delta != 0 and delta != 2:
			ans = 'NO'
	print(ans)
