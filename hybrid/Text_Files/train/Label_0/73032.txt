(n, k) = input().split()
n = int(n)
k = int(k)
a = []
a = input().split()
b = input().split()
for i in range(n):
	(a[i], b[i]) = (int(a[i]), int(b[i]))
min = b[0] // a[0]
for i in range(1, n):
	if min > b[i] // a[i]:
		min = b[i] // a[i]
qty1 = min
baln = [0 for i in range(n)]
f = 1
qty2 = 0
for i in range(n):
	b[i] -= a[i] * min
diff = 0
for i in range(n):
	if a[i] - b[i] > 0:
		diff += a[i] - b[i]
	else:
		baln[i] += b[i] - a[i]
if diff > k:
	k = 0
	qty2 = 0
elif diff == k:
	k = 0
	qty2 += 1
else:
	qty2 += 1
	k -= diff
	while sum(baln) != 0 or f == 1:
		f = 0
		diff = 0
		for i in range(n):
			if a[i] - baln[i] > 0:
				diff += a[i] - baln[i]
				baln[i] = 0
			else:
				baln[i] = baln[i] - a[i]
		if diff > k:
			k = 0
			qty2 += 0
			break
		elif diff == k:
			k = 0
			qty2 += 1
			break
		else:
			k -= diff
			qty2 += 1
print(qty1 + qty2 + k // sum(a))
