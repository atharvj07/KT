(n, k) = map(int, input().split())
s = list(map(int, input().split()))
s = [-1] + s
if n <= k:
	print(s[n])
else:
	sol = s[n]
	for i in range(n - k):
		x = s[n - k - i] + s[n - k + 1 + i]
		if x > sol:
			sol = x
	print(sol)
