T = int(input())
for test in range(T):
	(a, b, c, r) = map(int, input().split())
	if a > b:
		(a, b) = (b, a)
	if c - r > b or c + r < a:
		print(b - a)
	else:
		bottom = max(min(c - r, b) - a, 0)
		upper = max(b - max(c + r, a), 0)
		time = bottom + upper
		print(time)
