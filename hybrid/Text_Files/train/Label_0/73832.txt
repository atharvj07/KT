def solve():
	n = int(input())
	a = list(map(int, input().split()))
	(c, d) = ({}, {})
	for i in a:
		if i not in d:
			d[i] = 0
		d[i] += 1
		if d[i] not in c:
			c[d[i]] = 0
		c[d[i]] += 1
	arr = [i * c[i] for i in c]
	print(n - max(arr))
for _ in range(int(input())):
	solve()
