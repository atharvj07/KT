for _ in range(int(input())):
	n = int(input())
	a = []
	b = 0
	o = []
	z = []
	for x in input():
		if '0' == x:
			if o:
				z += (o.pop(),)
				a += (z[-1],)
			else:
				b += 1
				z += (b,)
				a += (b,)
		elif z:
			o += (z.pop(),)
			a += (o[-1],)
		else:
			b += 1
			o += (b,)
			a += (b,)
	print(b)
	print(*a)
