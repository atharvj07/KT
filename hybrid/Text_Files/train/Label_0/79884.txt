for _ in range(int(input())):
	a = input()
	b = input()
	c = input()
	flag = 1
	for i in range(len(a)):
		if a[i] != b[i]:
			if a[i] == c[i]:
				continue
			elif b[i] == c[i]:
				continue
			else:
				flag = 0
		elif a[i] != c[i]:
			flag = 0
	if flag:
		print('YES')
	else:
		print('NO')
