name = input()
dinstinctLetters = []
for i in range(len(name)):
	if not name[i] in dinstinctLetters:
		dinstinctLetters.append(name[i])
if len(dinstinctLetters) % 2 == 1:
	print('IGNORE HIM!')
else:
	print('CHAT WITH HER!')
