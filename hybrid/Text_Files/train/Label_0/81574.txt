import sys
ip = lambda : sys.stdin.readline().rstrip()
t = int(ip())
for _ in range(t):
	(r, g, b) = map(int, ip().split())
	(a, b, c) = sorted([r, b, g])
	if c > a + b:
		print(a + b)
	else:
		print(c + (a + b - c) // 2)
