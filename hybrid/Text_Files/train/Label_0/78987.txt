n = int(input())
row = ['*'] * n
center = n // 2
for i in range(n):
	new_char = 'D' if i <= center else '*'
	index_to_change = center - min(i, n - i)
	row[index_to_change] = new_char
	index_to_change = center + min(i, n - i)
	row[index_to_change] = new_char
	print(''.join(row))
