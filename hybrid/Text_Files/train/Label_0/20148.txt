#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  ;
  int n;
  cin >> n;
  int sum = n;
  for (int i = 1; i < n; i++) {
    sum += (n - i) * i;
  }
  cout << sum;
  return 0;
}
