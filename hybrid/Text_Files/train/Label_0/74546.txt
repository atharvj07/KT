for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	a.sort()
	(p, q) = (float('-inf'), float('inf'))
	i = 1
	while i < n:
		p = max(p, a[i])
		q = min(abs(a[i] - a[i - 1]), q)
		if p > q:
			break
		i += 1
	print(i)
