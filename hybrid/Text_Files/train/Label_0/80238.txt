(n, k) = map(int, input().split())
l = list(map(int, input().split()))
b = [[] for _ in range(max(l) + 2)]
for a in l:
	count = 0
	while a > 0:
		b[a].append(count)
		count += 1
		a = a // 2
ans = 10000000000000
for a in b:
	if len(a) >= k:
		ans = min(ans, sum(sorted(a)[:k]))
print(ans)
