#include <bits/stdc++.h>
using namespace std;
int main() {
  int a, x, y;
  cin >> a >> x >> y;
  a = a / 2;
  if ((x != a && x != a + 1) || (y != a && y != a + 1))
    cout << "YES" << endl;
  else
    cout << "NO" << endl;
  return 0;
}
