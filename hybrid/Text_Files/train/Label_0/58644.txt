//  package com.company.codeforces;

import com.sun.security.jgss.GSSUtil;

import java.io.*;
import java.util.*;
import java.util.Collection;
import java.util.Scanner;

public class Solution {
    static ArrayList<Integer> tree[];
    static int d[];
    static boolean vis[];
    static int  ans[];
    static int p[];
    static HashMap<Integer,HashSet<Integer>> map;

    public static void main(String[] args) {
        FastReader input = new FastReader();
        PrintWriter out=new PrintWriter(System.out);

        int t = input.nextInt();
        while (t-- > 0) {
            int n = input.nextInt();
            tree = new ArrayList[n + 1];
            for (int i = 0; i <= n; i++) {
                tree[i] = new ArrayList<>();
            }
            int m = input.nextInt();
            for (int i = 0; i < m; i++) {
                int x = input.nextInt();
                int y = input.nextInt();

                tree[x].add(y);
                tree[y].add(x);
            }
            c=0;

//            p=new int[n+1];
            vis=new boolean[n+1];
            ans=new int[n+1];
//              Arrays.fill(p,-1);
//            ans[1]=true;


            dfs(1, 0);
            boolean pos=true;
            for (int i = 1; i <=n ; i++) {
                if (!vis[i]){
                    pos=false;
                    break;
                }
            }
            if (!pos){
                out.println("NO");
                continue;
            }
//            tree = new ArrayList[n + 1];
//            for (int i = 0; i <= n; i++) {
//                tree[i] = new ArrayList<>();
//            }
//            for (int i = 1; i <=n ; i++) {
//                tree[p[i]].add(i);
//
//            }
//            Arrays.fill(vis,false);
//            dfs2(1,0);

            out.println("YES");
           out.println(c);
            for (int i = 1; i <=n ; i++) {
                if (ans[i]==1){
                   out.print(i+" ");
                }

            }
            out.println();


        }
        out.flush();
        out.close();


    }
    static int c=0;
//
//    private static void dfs2(int i,int p) {
//        if ( !vis[i]){
//
//            c++;
//            ans[i]=true;
//            for (int a:map.get(i)) {
//                vis[a]=true;
//            }
//
//        }
//        for (int child:tree[i]) {
//            dfs2(child,i);
//        }
//    }


    private static void dfs(int i, int pa) {

         vis[i]=true;
      if (ans[i]==0) {
          c++;
          ans[i]=1;
          for (int c : tree[i]) {
             ans[c]=2;
          }
      }
        for (int c:tree[i]) {

            if (vis[c]) continue;
            dfs(c,i);
        }

    }

    static class FastReader
    {
        BufferedReader br;
        StringTokenizer st;

        public FastReader()
        {
            br = new BufferedReader(new
                    InputStreamReader(System.in));
        }

        String next()
        {
            while (st == null || !st.hasMoreElements())
            {
                try
                {
                    st = new StringTokenizer(br.readLine());
                }
                catch (IOException  e)
                {
                    e.printStackTrace();
                }
            }
            return st.nextToken();
        }

        int nextInt()
        {
            return Integer.parseInt(next());
        }

        long nextLong()
        {
            return Long.parseLong(next());
        }

        double nextDouble()
        {
            return Double.parseDouble(next());
        }

        String nextLine()
        {
            String str = "";
            try
            {
                str = br.readLine();
            }
            catch (IOException e)
            {
                e.printStackTrace();
            }
            return str;
        }
    }
}