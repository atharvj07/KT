(n, p) = [int(x) for x in input().split()]
l = 1
for mid in range(1, 32):
	k = n - mid * p
	u = 0
	if k < mid:
		continue
	for i in range(50, -1, -1):
		if k >= 2 ** i:
			k -= 2 ** i
			u += 1
		if k == 0:
			break
	if u <= mid and u != 0:
		print(mid)
		break
else:
	print(-1)
