import sys
input = lambda : sys.stdin.readline().rstrip()
n = int(input())
a = sorted(([*map(int, input().split())] for i in range(n)))
(ans, c) = (a[0][1], sum(a[0]))
for w in a[1:]:
	ans += w[1] + max(w[0] - c, 0)
	c = max(c, sum(w))
print(ans)
