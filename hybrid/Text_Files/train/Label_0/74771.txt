def main():
	s = input()[::-1]
	for i in range(0, 1000, 8):
		start = 0
		for c in reversed(str(i)):
			stop = s.find(c, start)
			if stop == -1:
				break
			else:
				start = stop + 1
		else:
			print('YES')
			print(i)
			return
	print('NO')
main()
