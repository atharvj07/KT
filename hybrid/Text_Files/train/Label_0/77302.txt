def main():
	input()
	arr = [int(x) for x in input().strip().split()]
	numbers = set()
	for elem in arr:
		numbers.add(elem)
		if len(numbers) > 3:
			return 'NO'
	if len(numbers) < 3:
		return 'YES'
	(a, b, c) = sorted(numbers)
	if a - b == b - c:
		return 'YES'
	return 'NO'
print(main())
