#include<iostream>
#include<algorithm>
using namespace std;

int N, B[110], sum;
int main() {
	cin >> N;
	for (int i = 1; i < N; i++)
		cin >> B[i];
	sum = B[1] + B[N - 1];
	for (int i = 1; i < N-1; i++)
		sum += min(B[i], B[i + 1]);
	cout << sum;
}