#include<cstdio>

int main(){
    int a, b;
    char str[10000];
    scanf("%d%d",&a,&b);
    for(int i=0;i<a;i++){
        scanf("%s",str);
        puts(str);
        puts(str);
    }
    return 0;
}
