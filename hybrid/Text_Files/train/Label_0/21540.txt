#include <bits/stdc++.h>
using namespace std;
int n, s, d;
int main(void) {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  cin >> n;
  int cur = 0;
  for (int i = 0; i < n; ++i) {
    cin >> s >> d;
    while (s <= cur) s += d;
    cur = s;
  }
  cout << cur << endl;
}
