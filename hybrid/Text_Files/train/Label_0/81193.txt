from itertools import accumulate
import bisect

def main():
	n = int(input())
	arr = list(map(int, input().split()))
	half = (1 + sum(arr)) // 2
	acc = [*accumulate(arr)]
	print(bisect.bisect_left(acc, half) + 1)
main()
