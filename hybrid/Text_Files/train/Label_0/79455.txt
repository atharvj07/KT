import sys
input = sys.stdin.readline
I = lambda : list(map(int, input().split()))
(t,) = I()
for _ in range(t):
	(r, b, d) = I()
	k = max(r, b)
	if d:
		k = (abs(r - b) + d - 1) // d
	if k <= min(r, b):
		print('YES')
	else:
		print('NO')
