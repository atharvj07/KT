from collections import deque
(n, k) = map(int, input().split())
arr = list(map(int, input().split()))
k2 = k
l = deque()
for i in range(n):
	if k == 0:
		break
	elif arr[i] < 0:
		arr[i] = arr[i] * -1
		k -= 1
arr = sorted(arr)
while k > 0:
	arr[0] *= -1
	k -= 1
print(sum(arr))
