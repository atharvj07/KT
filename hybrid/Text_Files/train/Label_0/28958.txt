#include <iostream>
using namespace std;

int GCD(int x, int y){
  int r;
  while(y){
    r=x%y;
    x=y;
    y=r;
  }
  return x;
}

int main(){
    int H,W,C;
    cin >> H >> W >> C;
    cout << H*W/GCD(H,W)/GCD(H,W)*C << endl;
    return 0;
}

