n = int(input())
a = [int(x) for x in input().split()]
cur = 'a'
s = ''
for i in reversed(range(n)):
	for j in range(0, a[i]):
		s += cur * (i + 1)
		cur = ('a', 'b')[cur == 'a']
	for j in reversed(range(i)):
		a[j] -= (i - j + 1) * a[i]
	a[i] = 0
print(s)
