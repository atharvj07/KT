(n, s) = (int(input()), input())
res = max((s.count('X'), 1), (s.count('x'), 0))
if res[1]:
	s = s.replace('X', 'x', res[0] - n // 2)
else:
	s = s.replace('x', 'X', res[0] - n // 2)
print(res[0] - n // 2, s, sep='\n')
