import math
t = int(input())
for _ in range(t):
	(n, m, k) = map(int, input().split())
	cards = n // k
	if m <= cards:
		print(m)
	else:
		remcard = m - cards
		to_each = math.ceil(remcard / (k - 1))
		print(cards - to_each)
