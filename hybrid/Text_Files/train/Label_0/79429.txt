import sys
num_queries = int(input())
while num_queries > 0:
	(a, b, k) = map(int, input().split(' '))
	right = k // 2
	if k % 2 == 1:
		right += 1
	left = k // 2
	print(a * right - b * left)
	num_queries -= 1
