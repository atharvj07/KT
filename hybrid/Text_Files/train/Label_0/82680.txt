from fractions import Fraction as f
r = list(map(int, input().split()))
(a, b) = (r[0], r[1])
(c, d) = (r[2], r[3])
ans = min(abs(f(a * b - c * d * f(a, c) ** 2, a * b)), abs(f(a * b - c * d * f(b, d) ** 2, a * b)))
if ans == 0:
	print('0/1')
else:
	print(ans)
