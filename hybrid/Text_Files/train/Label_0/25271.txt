#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  long long n, t;
  cin >> n >> t;
  const double base = 1.000000011;
  cout << n * pow(base, t) << endl;
  return 0;
}
