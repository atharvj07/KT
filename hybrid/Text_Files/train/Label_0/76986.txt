def fun3(k):
	flag = False
	while h:
		x = h.pop(0)
		if x == 0:
			k += 1
			re.append(x)
		elif x <= k:
			re.append(x)
		else:
			flag = True
			break
	return flag

def fun2(k):
	flag = False
	while j:
		x = j.pop(0)
		if x == 0:
			k += 1
			re.append(x)
		elif x <= k:
			re.append(x)
		else:
			flag = True
			break
	return flag
t = int(input())
while t:
	tem = input()
	t -= 1
	(k, n, m) = map(int, input().split())
	j = list(map(int, input().split()))
	h = list(map(int, input().split()))
	re = []
	flag = False
	while j and h:
		x = min(j[0], h[0])
		if j[0] <= h[0]:
			j.pop(0)
		else:
			h.pop(0)
		if x == 0:
			k += 1
			re.append(x)
		elif x <= k:
			re.append(x)
		else:
			flag = True
			break
	if flag:
		print(-1)
	else:
		flag = fun2(k)
		if flag:
			print(-1)
		else:
			flag = fun3(k)
			if flag:
				print(-1)
			else:
				print(*re)
