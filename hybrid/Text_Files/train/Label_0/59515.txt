function solve(input) {
  var o = 0, _ = 0;
  o = input.match(/o/g) ? input.match(/o/g).length : 0;
  _ = input.length - o;

  return (o === 0) || (_ % o === 0);
}

var x = readline();
print(solve(x) ? 'YES' : 'NO')
