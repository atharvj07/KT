import time
from fractions import gcd

def lcm(a, b):
	return a * b // gcd(a, b)
(n, a, b, p, q) = (int(i) for i in input().split())
start = time.time()
if q > p:
	(a, b, p, q) = (b, a, q, p)
ans = n // a * p + (n // b - n // lcm(a, b)) * q
print(ans)
finish = time.time()
