nm = list(map(str, input().split('|')))
s = list(input())
n = nm[0]
m = nm[1]
while s:
	if len(n) <= len(m):
		n = n + s.pop(0)
	else:
		m = m + s.pop(0)
if len(n) == len(m):
	print(n + '|' + m)
else:
	print('Impossible')
