#include <bits/stdc++.h>
using namespace std;
int main(){
set<char>s;
string S;
cin>>S;
for(int i=0;i<S.size();i++){
    s.insert(S[i]);
}
if(S.size()==s.size())cout<<"yes";
else cout<<"no";
return 0;
}
