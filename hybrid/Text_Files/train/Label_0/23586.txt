#include <bits/stdc++.h>
using namespace std;
long long n, t, k, l, k1, ans, s, a, b, c;
vector<string> v;
set<int> st;
pair<int, int> p[109];
map<string, int> us;
int main() {
  cin >> t;
  for (int i = 0; i < t; i++) {
    cin >> s >> a >> b >> c;
    ans = s / c;
    cout << ans + ans / a * b << endl;
  }
}
