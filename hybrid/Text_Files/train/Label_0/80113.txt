(n, k) = [int(x) for x in input().split()]
tmp = n + 1
lan = 2 ** (n + 1) - 1
while lan > 1:
	if k <= lan // 2:
		lan = lan // 2
		tmp = tmp - 1
	elif k >= lan // 2 + 2:
		k = lan // 2 - lan + k
		lan = lan // 2
		tmp = tmp - 1
	if k == lan // 2 + 1:
		break
print(tmp)
