from collections import deque
t = int(input())
for i in range(t):
	n = int(input())
	a = list(map(int, input().split()))
	d = deque(a)
	m = d.pop()
	count = 0
	for j in range(n - 1):
		temp = d.pop()
		if temp > m:
			count += 1
		m = min(m, temp)
	print(count)
