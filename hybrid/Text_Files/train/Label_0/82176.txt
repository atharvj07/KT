import sys, io, os
if os.environ['USERNAME'] == 'kissz':
	inp = open('in.txt', 'r').readline

	def debug(*args):
		print(*args, file=sys.stderr)
else:
	inp = io.BytesIO(os.read(0, os.fstat(0).st_size)).readline

	def debug(*args):
		pass
for _ in range(int(inp())):
	n = int(inp())
	A = [*map(int, inp().split())]
	A.sort(reverse=True)
	X = []
	xsum = 0
	ans = 'YES'
	for i in range(n):
		if A[2 * i] != A[2 * i + 1]:
			ans = 'NO'
			break
		debug(X, xsum, A[2 * i] - 2 * xsum, 2 * (n - i))
		if (A[2 * i] - 2 * xsum) % (2 * (n - i)) == 0 and A[2 * i] - 2 * xsum > 0:
			x = (A[2 * i] - 2 * xsum) // (2 * (n - i))
			if X and x == X[-1]:
				ans = 'NO'
				break
			X.append(x)
			xsum += x
			debug(X, xsum, A[2 * i])
		else:
			ans = 'NO'
			break
	print(ans)
