n = int(input())
for i in range(n):
	(si, di) = list(map(int, input().split()))
	if i == 0:
		sum = si
	elif si <= sum:
		if si + di <= sum:
			nd = (sum - (si + di)) // di + 1
			sum = si + di + nd * di
		else:
			sum = si + di
	else:
		sum = si
print(sum)
