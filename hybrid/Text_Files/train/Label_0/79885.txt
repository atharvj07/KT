for _ in range(int(input())):
	a = list(input())
	b = list(input())
	c = list(input())
	n = len(a)
	f = 1
	for i in range(n):
		if c[i] in [a[i], b[i]]:
			pass
		else:
			f = 0
	if f == 1:
		print('YES')
	else:
		print('NO')
