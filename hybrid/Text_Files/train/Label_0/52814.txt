import java.io.*;
import java.util.*;

public class codeforces {
	public static PrintWriter out;
	public static class MyScanner {
		BufferedReader br;
		StringTokenizer st;
		public MyScanner() {
			br = new BufferedReader(new InputStreamReader(System.in));
		}
		String next() {
			while (st == null || !st.hasMoreElements()) {
				try {
					st = new StringTokenizer(br.readLine());
				}
				catch (IOException e) {
					e.printStackTrace();
				}
			}
			return st.nextToken();
		}
		int nextInt() {
			return Integer.parseInt(next());
		}
		long nextLong() {
			return Long.parseLong(next());
		}
		double nextDouble() {
			return Double.parseDouble(next());
		}
		String nextLine() {
			String str = "";
			try {
				str = br.readLine();
			}
			catch (IOException e) {
				e.printStackTrace();
			}
			return str;
		}
	}
	// The next two void functions are shuffle functions on an array.
	// This way, quick sort encounters the worst case scenario with low probability.
	// A reason for doing this is if we don't a primitive data array to an object array.
	// For safety reasons, it would be preferable to use object arrays unless doing so expends a lot of time.
	public static void shuffleLongArray(long[] arr) {
		int n = arr.length;
		Random rnd = new Random();
        for (int i=0; i < n; i++) {
        	long tmp = arr[i];
        	int randomPos = i + rnd.nextInt(n-i);
        	arr[i] = arr[randomPos];
        	arr[randomPos] = tmp;
        }
	}
	public static void shuffleIntArray(int[] arr) {
		int n = arr.length;
		Random rnd = new Random();
        for (int i=0; i < n; i++) {
        	int tmp = arr[i];
        	int randomPos = i + rnd.nextInt(n-i);
        	arr[i] = arr[randomPos];
        	arr[randomPos] = tmp;
        }
	}
	public static void main (String[] args) throws IOException {
    	MyScanner sc = new MyScanner();
    	out = new PrintWriter(new BufferedOutputStream(System.out));
    	// Write solution here.
    	int n = sc.nextInt(); // n is odd
    	int[][] grid = new int[n][n];
    	for (int i=0; i < n; i++) {
    		for (int j=0; j < n; j++) {
    			grid[i][j] = -1;
    		}
    	}
    	// for clearing purposes
    	grid[0][0] = 1;
    	grid[0][1] = 0; // tentative; we can flip everything if it's the reverse
    	grid[n-1][n-1] = 0;
    	// this set of given conditions gives rise to an important observation
    	for (int i=0; i < n; i++) {
    		for (int j=0; j < n; j++) {
    			if (grid[i][j] >= 0) continue; // skips the 3 filled in cells
    			int a = i+1;
    			int b = j+1;
    			if (i==1 && j==2) {
    				System.out.println("? 1 2 2 3");
    				System.out.flush();
    				int result = sc.nextInt();
    				if (result==1) grid[1][2] = grid[0][1];
    				else grid[1][2] = 1-grid[0][1];
    			}
    			else if (j >= 2) {
    				System.out.println("? " + a + " " + (b-2) + " " + a + " " + b);
    				System.out.flush();
    				int result = sc.nextInt();
    				if (result==1) grid[i][j] = grid[i][j-2];
    				else grid[i][j] = 1-grid[i][j-2];
    			}
    			else if (j==1 && i==1) {
    				System.out.println("? " + (a-1) + " " + (b-1) + " " + a + " " + b);
    				System.out.flush();
    				int result = sc.nextInt();
    				if (result==1) grid[i][j] = grid[i-1][j-1];
    				else grid[i][j] = 1-grid[i-1][j-1];
    			}
    			else if (i >= 2) {
    				System.out.println("? " + (a-2) + " " + b + " " + a + " " + b);
    				System.out.flush();
    				int result = sc.nextInt();
    				if (result==1) grid[i][j] = grid[i-2][j];
    				else grid[i][j] = 1-grid[i-2][j];
    			}
    		}
    		if (i==1) {
    	    	System.out.println("? 2 1 2 3");
    	    	System.out.flush();
    	    	int temper = sc.nextInt();
    	    	if (temper==1) grid[1][0] = grid[1][2];
    	    	else grid[1][0] = 1-grid[1][2];
    		}
    	}
    	if (n > 3) {
    		int out = 0;
    		int ch = 0;
    		for (int i=0; i < n; i++) {
    			for (int j=0; j < n-3; j++) {
    				if ((grid[i][j] + grid[i][j+1] + grid[i][j+2] + grid[i][j+3]) % 2 == 0) {
    					out = 1;
    					System.out.println("? " + (i+1) + " " + (j+1) + " " + (i+1) + " " + (j+4));
    					System.out.flush();
    					int result = sc.nextInt();
    					if (result == 1) {
    						ch = (grid[i][j] + grid[i][j+3]) % 2;
    					}
    					else {
    						ch = (grid[i][j] + grid[i][j+3] + 1) % 2;
    					}
    					break;
    				}
    			}
    			if (out==1) break;
    		}
    		for (int i=0; i < n-3; i++) {
    			if (out==1) break;
    			for (int j=0; j < n; j++) {
    				if ((grid[i][j] + grid[i+1][j] + grid[i+2][j] + grid[i+3][j]) % 2 == 0) {
    					out = 1;
    					System.out.println("? " + (i+1) + " " + (j+1) + " " + (i+4) + " " + (j+1));
    					System.out.flush();
    					int result = sc.nextInt();
    					if (result == 1) {
    						ch = (grid[i][j] + grid[i+3][j]) % 2;
    					}
    					else {
    						ch = (grid[i][j] + grid[i+3][j] + 1) % 2;
    					}
    					break;
    				}
    			}
    			if (out==1) break;
    		}
    		for (int i=0; i < n; i++) {
    			for (int j=0; j < n; j++) {
    				if ((i+j) % 2 != 0) grid[i][j] = (grid[i][j] + ch) % 2;
    			}
    		}
    	}
    	else { // n = 3 is quite complicated without the ability to draw long enough rows
    		System.out.println("? 1 1 2 3");
    		System.out.flush();
    		int ra = sc.nextInt();
    		System.out.println("? 1 2 3 3");
    		System.out.flush();
    		int rb = sc.nextInt();
    		int ch = 0;
    		if (ra == 1) {
    			ch = (grid[1][2] + 1) % 2;
    			// operation flip if needed
    			if (ch == 1) {
    				grid[0][1] = 1-grid[0][1]; grid[1][0] = 1-grid[1][0]; grid[1][2] = 1-grid[1][2]; grid[2][1] = 1-grid[2][1];
    			}
    		}
    		else if (rb == 1) {
    			ch = grid[0][1];
    			// operation flip if needed
    			if (ch==1) grid[0][1] = 1-grid[0][1]; grid[1][0] = 1-grid[1][0]; grid[1][2] = 1-grid[1][2]; grid[2][1] = 1-grid[2][1];
    		}
    		else {
    			ch = (grid[0][1] + 1) % 2;
    			// operation flip if needed
    			if (ch==1) grid[0][1] = 1-grid[0][1]; grid[1][0] = 1-grid[1][0]; grid[1][2] = 1-grid[1][2]; grid[2][1] = 1-grid[2][1];
    		}
    	}
    	System.out.println("!");
    	for (int i=0; i < n; i++) {
    		for (int j=0; j < n; j++) {
    			System.out.print(grid[i][j]);
    		}
    		System.out.println();
    	}
    	out.close();
	}
}