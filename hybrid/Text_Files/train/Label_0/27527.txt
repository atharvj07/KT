#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e5 + 10;
int main() {
  int N, M;
  scanf("%d%d", &N, &M);
  double ans = 2e100;
  for (int i = 1; i <= N; i++) {
    int c, g;
    scanf("%d%d", &c, &g);
    ans = min(ans, double(c) / g * M);
  }
  printf("%.10lf\n", ans);
}
