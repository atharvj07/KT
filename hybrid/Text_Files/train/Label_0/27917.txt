#include <bits/stdc++.h>
using namespace std;
int main() {
  string s1;
  string s2;
  map<char, set<int> > mapa;
  cin >> s1;
  cin >> s2;
  int i, j, k;
  int sz1 = s1.size();
  for (i = 0; i < sz1; i++) {
    mapa[s1[i]].insert(i);
  }
  int count = 1;
  int sz2 = s2.size();
  bool b = true;
  int pant = -1;
  set<int>::iterator it;
  for (i = 0; i < sz2; i++) {
    if (mapa[s2[i]].size() == 0) {
      b = false;
      break;
    }
    bool achou = false;
    it = mapa[s2[i]].upper_bound(pant);
    if (it != mapa[s2[i]].end() && *it > pant) {
      pant = *it;
    } else {
      pant = *mapa[s2[i]].begin();
      count++;
    }
  }
  if (!b)
    printf("-1\n");
  else
    printf("%d\n", count);
}
