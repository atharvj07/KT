#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  int sum = 0;
  cin >> n;
  for (int i = 1; i < n; ++i) {
    int k = (n - i) % i;
    if (k == 0) {
      ++sum;
    }
  }
  cout << sum;
  return 0;
}
