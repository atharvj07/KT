import math
t = int(input())
while t > 0:
	(a, b, c, d, k) = list(map(float, input().split()))
	ans = 0
	x = math.ceil(a / c)
	y = math.ceil(b / d)
	ans += x + y
	if ans <= k:
		print(x, y)
	else:
		print(-1)
	t -= 1
