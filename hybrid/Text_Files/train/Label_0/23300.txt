#include <bits/stdc++.h>
using namespace std;

int main() {
  int N, M; cin >> N >> M;
  int ans = M / N;
  while(true) {
    if (M % ans == 0) {
      break;
    } else {
      ans--;
    }
  }
  cout << ans << endl;
}