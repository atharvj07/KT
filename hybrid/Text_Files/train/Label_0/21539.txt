#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, res = 0, s, d;
  cin >> n;
  for (int i = 0; i < n; ++i) {
    cin >> s >> d;
    if (res < s)
      res = s;
    else
      res = (max(res - s, 0) / d + 1) * d + s;
  }
  cout << res;
}
