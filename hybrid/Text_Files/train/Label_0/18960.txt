#include <bits/stdc++.h>
using namespace std;
int main() {
  long long int n, k, t, res = 0, i;
  cin >> n;
  k = 2;
  for (i = 1; i <= n; i++) {
    res = (i * (i + 1) * (i + 1) - k);
    k = i;
    cout << res << endl;
  }
}
