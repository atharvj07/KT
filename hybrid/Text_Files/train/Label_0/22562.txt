#include <bits/stdc++.h>
using namespace std;

#define boost ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define f(i,ini,lim) for(int i=ini;i<lim;i++)
#define rf(i,ini,lim) for(int i=ini;i>lim;i--)
#define pb push_back
#define ll long long
#define ins insert
#define ff first
#define ss second
#define yes cout<<"YES\n"
#define no cout<<"NO\n"
#define endl "\n"
#define mp make_pair
#define sz(v) (int)v.size()

#define MOD 1000000007

void solve(){
	string s; cin>>s;
	int n = sz(s);
	int q=0;
	
	f(i,0,n){
	    if(s[i] == '?') q++;
	}
	
	if(q%2==0 and s[0] != ')' and s[n-1] != '(') yes;
	else no;
}

int main() {
	// your code goes here
	boost
	int t=1; 
	cin>>t;
	while(t--) solve();
	return 0;
}