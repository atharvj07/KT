import sys
inp = sys.stdin.readline

def solve():
	n = int(input())
	a = list(input().strip())
	r = []
	while True:
		i = 0
		j = len(a) - 1
		f = []
		b = []
		while True:
			while i < len(a):
				if a[i] == '1':
					break
				i += 1
			while j >= 0:
				if a[j] == '0':
					break
				j -= 1
			if i < j:
				f.append(i + 1)
				b.append(j + 1)
				(a[i], a[j]) = (a[j], a[i])
				z = 1
			else:
				break
		if len(f):
			b.reverse()
			r.append(f + b)
		else:
			break
	print(len(r))
	for i in r:
		print(len(i), end=' ')
		print(' '.join(map(str, i)))

def main():
	for i in range(int(inp())):
		solve()
main()
