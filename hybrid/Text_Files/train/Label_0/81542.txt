(n, k) = map(int, input().split())
s = input()
vis = [0] * 26
ch = '.'
cnt = 0
for i in range(n):
	if s[i] == ch:
		cnt += 1
	else:
		cnt = 1
		ch = s[i]
	if cnt == k:
		vis[ord(ch) - ord('a')] += 1
		ch = '.'
		cnt = 0
print(max(vis))
