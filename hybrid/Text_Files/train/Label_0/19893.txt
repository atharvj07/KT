#include <bits/stdc++.h>
using namespace std;
int n, f[100010], g[100010], ansn = 1, x;
int main() {
  cin >> n;
  for (int i = 2; i <= n; i++) {
    scanf("%d", &x);
    f[i] = f[x] + 1;
    g[f[i]]++;
  }
  for (int i = 1; i <= n; i++) ansn += (g[i] % 2);
  cout << ansn << endl;
  return 0;
}
