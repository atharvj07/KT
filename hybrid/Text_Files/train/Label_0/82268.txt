for _ in range(int(input())):
	(n, kit) = map(int, input().split())
	a = list(map(int, input().split()))
	s = 0
	for i in range(n):
		if kit > 0:
			t = min(a[i], kit)
			a[i] -= t
			kit -= t
			s += t
	a[-1] += s
	print(*a)
