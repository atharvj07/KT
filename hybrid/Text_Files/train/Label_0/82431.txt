def solution(nums, n):
	a = min(nums)
	counter = len(nums) // 2
	i = 0
	for x in nums:
		if x != a:
			print(x, a)
			i += 1
		if i == counter:
			break
t = int(input())
for _ in range(t):
	n = int(input())
	nums = [int(x) for x in input().split()]
	solution(nums, n)
