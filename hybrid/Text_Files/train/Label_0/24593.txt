#include <bits/stdc++.h>
#define r(i,n) for(int i = 0; i<n; i++)
typedef long long ll;
using namespace std;

int main(){
  int k,t;
  cin >> k >> t;
  vector<int> v(t,0);
  r(i,t)cin >> v.at(i);
  sort(v.begin(),v.end());
  cout << max(0,v.at(t-1)*2-k-1)<<endl;
}