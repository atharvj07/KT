#include<iostream>
#include<algorithm>
 
using namespace std;
 
int main() {
	int n, a[100];
	int al = 0;
	cin >> n;
	for (int i = 0;i < n;i++) {
		cin >> a[i];
		al += a[i];
	}
	sort(a, a+n);
	for (int i = n-2;i >= 0;i-=2) {
		al -= 2*a[i];
	}
	cout << al << endl;
	return 0;
}