#include <bits/stdc++.h>

using namespace std;

int main()
{
	int n,i;
	
	cin>>n;
	
	int a[n];
	
	for(i=0; i<n; i++)
	cin>>a[i];
	
	int b[n];
	
	b[0]=0;
	
	b[1]=abs(a[1]-a[0]);
	
	for(i=2; i<n; i++)
	{
		b[i]=min(b[i-2]+abs(a[i]-a[i-2]),b[i-1]+abs(a[i]-a[i-1]));
	}
	
	cout<<b[n-1];
}
