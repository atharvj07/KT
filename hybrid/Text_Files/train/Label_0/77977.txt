I = input
for i in ' ' * int(I()):
	I()
	a = {*map(int, I().split())}
	s = min(a) * max(a)
	d = set()
	i = 2
	while i * i <= s:
		if s % i == 0:
			d |= {i, s // i}
		i += 1
	if a != d:
		s = -1
	print(s)
