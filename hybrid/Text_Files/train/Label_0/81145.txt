for _ in range(int(input())):
	word = input()
	if len(word) % 2 != 0:
		print('NO')
	else:
		ans = 'Yes'
		for i in range(int(len(word) / 2)):
			if word[i] != word[int(i + len(word) / 2)]:
				ans = 'NO'
				break
		print(ans)
