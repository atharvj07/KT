#include <bits/stdc++.h>
using namespace std;
int main() {
  double x, y, i, j, k, a, m = -1;
  cin >> x >> y;
  if (x < 0 || y < 0) {
    cout << -1 << endl;
    return 0;
  }
  cout.precision(10);
  if (y == 0 && x == 0) {
    m = 0;
    cout << fixed << m << endl;
    return 0;
  }
  if (y == 0) {
    m = x / 2;
    cout << fixed << m << endl;
    return 0;
  }
  k = 2.0 * y;
  a = floor((y - x) / k);
  if (x == y) m = y;
  if (a != 0) {
    m = ((y - x) / (2 * a));
    if (m < y) m = -1;
  }
  a = floor((y + x) / (2 * y));
  if (a != 0) {
    if (m != -1) {
      if (m > ((y + x) / (2 * a))) m = ((y + x) / (2 * a));
    } else
      m = ((y + x) / (2 * a));
    if (m < y) m - 1;
  }
  if (m != -1)
    cout << fixed << m << endl;
  else
    cout << -1 << endl;
  return 0;
}
