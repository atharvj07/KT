from sys import stdin, stdout
for i in stdin:
	t = int(i)
	break
for i in range(t):
	for i in stdin:
		n = int(i)
		break
	for i in stdin:
		A = list(map(int, i.strip().split()))
		break
	for i in stdin:
		B = list(map(int, i.strip().split()))
		break
	k = 0
	changed = 0
	q = 1
	for i in range(len(A)):
		if changed == 0:
			if A[i] == B[i]:
				pass
			else:
				k = B[i] - A[i]
				if k <= 0:
					stdout.write('No\n')
					q = 0
					break
				changed = 1
		elif changed == 1:
			if A[i] + k == B[i]:
				pass
			elif A[i] == B[i]:
				k = 0
				changed = 2
			else:
				stdout.write('No\n')
				q = 0
				break
		elif changed == 2:
			if A[i] == B[i]:
				pass
			else:
				stdout.write('No\n')
				q = 0
				break
	if q == 1:
		stdout.write('Yes\n')
