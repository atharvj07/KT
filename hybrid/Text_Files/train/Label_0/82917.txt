import math
t = int(input())
for j in range(t):
	(n, m, k) = map(int, input().split())
	if n // k >= m:
		print(m)
	else:
		diff = m - n // k
		d = k - 1
		ans = n // k - math.ceil(diff / d)
		print(ans)
