#include <stdio.h>
int main( void )
{
	int N ;
	char s;
	scanf("%d\n", &N ) ;
	int R=0, B=0;
	for( int n = 0 ; n < N ; n++ ) {
		scanf("%c", &s ) ;
		s == 'R' ? R++ : B++ ;
	}
	printf("%s\n", (R>B) ? "Yes" : "No" ) ;
	return 0 ;
}