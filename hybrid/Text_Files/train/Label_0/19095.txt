#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, t, num = 0, c = 1;
  cin >> n >> m;
  while (n-- > 0) {
    cin >> t;
    if (num + t <= m)
      num += t;
    else {
      ++c;
      num = t;
    }
  }
  cout << c << endl;
  return 0;
}
