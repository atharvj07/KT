n = int(input())
arr = list(map(int, input().split()))
s = 0
odd = []
for element in arr:
	if element > 0:
		s += element
if s % 2 == 1:
	print(s)
else:
	for element in arr:
		if element % 2 == 1:
			odd.append(abs(element))
	print(s - min(odd))
