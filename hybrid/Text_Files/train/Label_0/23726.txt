#include <bits/stdc++.h>
using namespace std;
long long a[100005] = {};
int main() {
  unsigned long long t, n, m;
  cin >> n;
  unsigned long long k = 2;
  cout << 2 << endl;
  for (long long i = 2; i <= n; i++) {
    t = i * i * i + 2 * i * i + 1;
    cout << t << endl;
  }
  return 0;
}
