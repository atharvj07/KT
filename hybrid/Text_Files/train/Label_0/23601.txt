#include <bits/stdc++.h>
using namespace std;
int main() {
  long long int* T;
  int q, i;
  long long int a, b, c, alisa = 0, bob = 0, temp = 0;
  cin >> q;
  T = new long long int[q];
  for (i = 0; i < q; i++) {
    cin >> a >> b >> c;
    if (a >= c && b >= c) {
      bob = c;
      if (a <= b) {
        alisa = a;
        temp = alisa - bob;
        if (temp > b)
          bob += b;
        else {
          bob += temp;
          b -= temp;
          b /= 2;
          bob += b;
          alisa += b;
        }
      } else {
        alisa = b;
        temp = alisa - bob;
        if (temp > a)
          bob += a;
        else {
          bob += temp;
          a -= temp;
          a /= 2;
          bob += a;
          alisa += a;
        }
      }
    } else if (a >= b && c >= b) {
      bob = b;
      if (a <= c) {
        alisa = a;
        temp = alisa - bob;
        if (temp > c)
          bob += c;
        else {
          bob += temp;
          c -= temp;
          c /= 2;
          bob += c;
          alisa += c;
        }
      } else {
        alisa = c;
        temp = alisa - bob;
        if (temp > a)
          bob += a;
        else {
          bob += temp;
          a -= temp;
          a /= 2;
          bob += a;
          alisa += a;
        }
      }
    } else if (b >= a && c >= a) {
      bob = a;
      if (c <= b) {
        alisa = c;
        temp = alisa - bob;
        if (temp > b)
          bob += b;
        else {
          bob += temp;
          b -= temp;
          b /= 2;
          bob += b;
          alisa += b;
        }
      } else {
        alisa = b;
        temp = alisa - bob;
        if (temp > c)
          bob += c;
        else {
          bob += temp;
          c -= temp;
          c /= 2;
          bob += c;
          alisa += c;
        }
      }
    }
    T[i] = bob;
  }
  for (i = 0; i < q; i++) cout << T[i] << endl;
  return 0;
}
