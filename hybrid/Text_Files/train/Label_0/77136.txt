import sys
from collections import deque
from functools import cmp_to_key
INF = 1000000000.0 + 6
input = sys.stdin.readline

def compareeee(x, y):
	if X[x] + Y[y] > X[y] + Y[x]:
		return 1
	elif X[x] + Y[y] < X[y] + Y[x]:
		return -1
	else:
		return 0

def comparee(x, y):
	return X[x] + Y[y] > Y[x] + X[y]

def bfs(v, x):
	q = deque()
	if x == 0:
		X[v] = 0
	else:
		Y[v] = 0
	q.append(v)
	while len(q) > 0:
		u = q.popleft()
		for xx in G[u]:
			if x == 0:
				if X[xx] > X[u] + 1:
					X[xx] = X[u] + 1
					q.append(xx)
			elif Y[xx] > Y[u] + 1:
				Y[xx] = Y[u] + 1
				q.append(xx)
(n, m, k) = map(int, input().split())
G = [[] for _ in range(n)]
X = [INF for i in range(n)]
Y = [INF for i in range(n)]
tmp = 0
a = list(map(lambda x: int(x) - 1, input().split()))
for i in range(m):
	(u, v) = map(int, input().split())
	u -= 1
	v -= 1
	G[v].append(u)
	G[u].append(v)
bfs(0, 0)
bfs(n - 1, 1)
both = []
for x in a:
	both.append([-X[x] + Y[x], X[x], Y[x]])
both.sort()
ans = 0
yArray = -INF
for i in range(k):
	ans = max(ans, both[i][1] + yArray)
	yArray = max(yArray, both[i][2])
print(min(X[n - 1], ans + 1))
