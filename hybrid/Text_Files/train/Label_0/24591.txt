#include <bits/stdc++.h>
using namespace std;

int k,t,a[105],tot;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> k >> t;
	for (int i=1;i<=t;i++){
		cin >> a[i];
	}
	sort (a+1,a+t+1);
	for (int i=1;i<t;i++){
		tot+=a[i];
	}
	cout << max(0,
	a[t]-(tot+1)) << "\n";
}