from math import gcd
for test_case in range(int(input())):
	n = int(input())
	l = sorted(map(int, input().split()), key=lambda x: x & 1)
	print(sum((len([x for x in l[i + 1:] if gcd(l[i], 2 * x) > 1]) for i in range(n))))
