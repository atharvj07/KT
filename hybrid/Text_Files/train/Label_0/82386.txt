(n, k) = map(int, input().split())
A = list(map(int, input().split()))
ans = 0
for i in range(n):
	if A[i] > k:
		break
	else:
		ans += 1
if i != n - 1:
	for j in range(n - 1, -1, -1):
		if A[j] > k:
			break
		else:
			ans += 1
print(ans)
