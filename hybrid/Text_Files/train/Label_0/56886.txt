import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.util.ArrayDeque;
import java.util.ArrayList;
import java.util.Random;
import java.util.StringTokenizer;

/*
8 7
1 2
2 3
3 4
4 2
4 5
5 6
4 6
6 7

 */
public class d {

	public static void main(String[] args) {
		try {
			FastScanner in = new FastScanner(System.in);
			
			int n = in.nextInt();
			int m = in.nextInt();
			
			Random rand = new Random();
			
			/*ArrayList<Integer>[] edges = new ArrayList[n];
			for(int i = 0; i < n; i++)
				edges[i] = new ArrayList<>();
*/
			//MagicComponents mc = new MagicComponents(n);
			//SCC scc = new SCC(n);
			//boolean[] vis = new boolean[m * 2];
			boolean[] vvis = new boolean[n];
			long[] counts = new long[n];
			DisjointSet dset = new DisjointSet(n + m);
			//long ans = 0;
			long numLoops = 0;
			ArrayDeque<Integer> q = new ArrayDeque<>();
			for(int i = 0; i < m; i++) {
				int a = in.nextInt() - 1;
				int b = in.nextInt() - 1;
				if(i == 0) q.add(a);
				//mc.addEdge(a, b);
				//edges[a].add(b);
				counts[a]++;
				if(a != b)
					counts[b]++;
				else {
					counts[a]--;
					numLoops++;
				}
				dset.union(a, n + i);
				dset.union(b, n + i);
				dset.union(a, b);
				//edges[b].add(a);
			}
			/*int count = 0;
			vvis[q.peek()] = true;
			while(!q.isEmpty()) {
				int cur = q.poll();
				for(int next : edges[cur]) {
					count++;
					if(vvis[next]) continue;
					vvis[next] = true;
					q.add(next);
				}
			}*/
			boolean good = true;
			for(int i = n; i < n + m; i++) {
				if(dset.find(i) != dset.find(n))
					good = false;
			}
			if(!good) {
				System.out.println(0);
				return;
			}
			//mc.run();
			long ans = 0;
			for(int i = 0; i < n; i++) {
				long k = counts[i];
				ans += (k * (k - 1)) / 2;
			}
			ans += ((long) (m - numLoops)) * numLoops;
			ans += (numLoops * (numLoops - 1)) / 2;
			/*
			for(HashSet<Integer> bcc : mc.bccs) {
				if(bcc.size() <= 2) continue;
				System.out.println(bcc);
			}*/
			
			System.out.println(ans);
		} catch(Throwable e) {
			e.printStackTrace();
			System.exit(-1);
		}
	}
	
	static class Edge {
		int u, v, id;
		public Edge(int uu, int vv, int idd) {
			u = uu; v = vv; id = idd;
		}
	}
	
	//@
	static class DisjointSet {
		int[] s, r;
		
		public DisjointSet(int n) {
			s = new int[n]; r = new int[n];
			for (int i = 0; i < n; i++) s[i] = i;
		}
		
		public int find(int i) {
			return s[i] == i ? i : (s[i] = find(s[i]));
		}
		
		public void union(int a, int b) {
			if(find(a) == find(b)) return;
			if(r[a = find(a)] == r[b = find(b)]) r[a]++;
			if(r[a] >= r[b]) s[b] = a; else s[a] = b;
		}
	}
	
	//@
	static class FastScanner {
	    BufferedReader br;
	    StringTokenizer st;
		
	    public FastScanner(InputStream i) {
	        br = new BufferedReader(new InputStreamReader(i));
	        st = new StringTokenizer("");
	    }
				
	    public String next() throws IOException {
	        if(st.hasMoreTokens())
	            return st.nextToken();
	        else
	            st = new StringTokenizer(br.readLine());
	        return next();
	    }

	    public int nextInt() throws IOException {
	        return Integer.parseInt(next());
	    }
	    //#
	    public long nextLong() throws IOException {
	        return Long.parseLong(next());
	    }
	    public double nextDouble() throws IOException {
	        return Double.parseDouble(next());
	    }
	    //$
	}

	
}
