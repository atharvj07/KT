#include <bits/stdc++.h>
using namespace std;
int len;
string str;
int cnt = 0;
int main() {
  cin >> len >> str;
  for (int i = 0; i < (len); i++) {
    if (str[i] == '1')
      cnt++;
    else
      cout << cnt, cnt = 0;
  }
  cout << cnt;
  return 0;
}
