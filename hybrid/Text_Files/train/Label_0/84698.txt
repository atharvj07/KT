trial = int(input())
for i in range(trial):
	variable = list(map(int, input().split()))
	x = variable[0]
	y = variable[1]
	a = variable[2]
	b = variable[3]
	distance = y - x
	speed = a + b
	num = 0
	if distance % speed == 0:
		print(distance // speed)
	else:
		print(-1)
