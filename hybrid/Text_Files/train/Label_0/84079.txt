n = int(input())
higher = True
for _ in range(n):
	a = int(input())
	if a % 2 == 0:
		print(a // 2)
	elif higher:
		print(a // 2 + 1)
		higher = not higher
	else:
		print(a // 2)
		higher = not higher
