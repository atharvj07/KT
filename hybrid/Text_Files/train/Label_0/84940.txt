(n, doc, m, t) = map(int, input().split())
for _ in range(t):
	(chk, idx) = map(int, input().split())
	if chk == 1:
		n += 1
		if idx <= doc:
			doc += 1
	elif idx < doc:
		doc -= idx
		n -= idx
	else:
		n = idx
	print(n, doc)
