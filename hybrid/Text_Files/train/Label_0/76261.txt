(n, k) = map(int, input().split())
arr = list(map(int, input().split()))
lst = [i for i in range(1, n * k + 1) if i not in arr]
for num in arr:
	ans = [num]
	cases = n - 1
	while cases:
		ans.append(lst.pop())
		cases -= 1
	print(*ans)
