from sys import stdin, stdout
from sys import maxsize
input = stdin.readline

def solve():
	pass
test = 1
for t in range(0, test):
	(n, x, y) = list(map(int, input().strip().split()))
	a = [int(x) for x in input().strip().split()]
	if x > y:
		print(n)
	else:
		lessx = 0
		morey = 0
		for el in a:
			if el <= x:
				lessx += 1
				if el + y > x:
					morey += 1
		print(morey // 2 + morey % 2 + lessx - morey)
	ans = solve()
