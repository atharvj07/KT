from math import gcd
answers = []
for i in range(int(input())):
	k = int(input())
	k1 = 100 - k
	if k == 100:
		answers.append('1')
		continue
	f = gcd(k, k1)
	k /= f
	k1 /= f
	answers.append(str(int(k + k1)))
print('\n'.join(answers))
