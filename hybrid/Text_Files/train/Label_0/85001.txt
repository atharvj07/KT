t = int(input())
for i in range(t):
	(n, g, b) = map(int, input().split())
	if g >= b:
		print(n)
		continue
	loop = n // (2 * g)
	if n % (2 * g) == 0:
		loop -= 1
	rest = n - 2 * g * loop
	print(loop * (b + g) + (rest + 1) // 2 + max(rest // 2 - loop * (b - g), 0))
