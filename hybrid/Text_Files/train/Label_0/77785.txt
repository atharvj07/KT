import sys
input = sys.stdin.readline
n = int(input())
a = list(map(int, input().split()))
ans = 0
d = []
for i in a:
	while len(d) and d[-1] < i:
		ans = max(ans, d[-1] ^ i)
		d.pop()
	if len(d):
		ans = max(ans, d[-1] ^ i)
	d.append(i)
d = []
print(ans)
