n = int(input())
ptlist = []
while n > 0:
	xy = input().split()
	for i in range(len(xy)):
		xy[i] = int(xy[i])
	ptlist.append(xy)
	n -= 1
maxsum = 0
for points in ptlist:
	if points[0] + points[1] > maxsum:
		maxsum = points[0] + points[1]
print(maxsum)
