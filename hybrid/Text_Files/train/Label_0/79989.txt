def gcd(x, y):
	if x % y == 0:
		return y
	return gcd(y, x % y)
n = int(input())
for i in range(n):
	(m, d, w) = map(int, input().split())
	z = min(m, d)
	w //= gcd(d - 1, w)
	ans = 0
	fm = z % w
	fn = z // w
	ans += fn * (fn - 1) // 2 * (w - fm) + fn * (fn + 1) // 2 * fm
	print(ans)
