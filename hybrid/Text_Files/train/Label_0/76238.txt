(n, k) = [int(x) for x in input().split()]
s = list(input())
stack = []
for i in range(len(s)):
	if s[i] == '(':
		stack.append(i)
	if s[i] == ')':
		if n != k:
			n -= 2
			s[i] = '_'
			s[stack.pop()] = '_'
		else:
			stack.pop()
for c in s:
	if c != '_':
		print(c, end='')
print()
