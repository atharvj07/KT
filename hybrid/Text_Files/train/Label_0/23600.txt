#include <bits/stdc++.h>
int main() {
  int n;
  std::cin >> n;
  for (int i = 0; i < n; i++) {
    long long a, b, c;
    std::cin >> a >> b >> c;
    std::cout << (a + b + c) / 2 << std::endl;
  }
}
