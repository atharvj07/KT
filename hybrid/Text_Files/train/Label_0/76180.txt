n = int(input(''))
a = list(map(int, input('').split(' ')))
t = sum(a)
m = max(a)
yz = n * m - t
b = []
import math
for i in range(n):
	if m - a[i] != 0:
		b.append(m - a[i])
l = len(b)
g = b[0]
for i in range(l):
	g = math.gcd(b[i], g)
	if g == 1:
		break
print(str(yz // g) + ' ' + str(g))
