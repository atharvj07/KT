def sol():
	import math
	(n, need) = list(map(int, input().split()))
	arr = list(map(int, input().split()))
	have = 0
	t = 0
	for i in range(n):
		have += arr[i]
		need -= min(8, have)
		have -= min(8, have)
		t += 1
		if need <= 0:
			print(t)
			return 0
	print(-1)
sol()
