#include<bits/stdc++.h>
using namespace std;
using ll = long long;
//解ける気がしない

string f( ll n ){
  if( n == 0 ) return "";
  --n;
  return f(n/26) + string(1,'a' + n%26);
}

int main(){
  ll n;cin >> n;
  cout << f(n) << endl;
  
  return 0;
}
