#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k, mod, min, max;
  cin >> n;
  k = n / 7;
  mod = n % 7;
  if (mod >= 2)
    max = 2;
  else
    max = mod;
  if (mod <= 5)
    min = 0;
  else
    min = 1;
  max += 2 * k;
  min += 2 * k;
  cout << min << endl << max;
}
