#include <bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  for (int i = 0; i <= (s.length() >> 1); i++) {
    if (s[i] != s[s.length() - i - 1])
      s.insert(s.begin() + s.length() - i, s[i]);
  }
  cout << s << endl;
  return 0;
}
