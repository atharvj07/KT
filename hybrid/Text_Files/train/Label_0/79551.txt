import sys
import math
[n, k, M, D] = list(map(int, sys.stdin.readline().split(' ')))
best = min(n, M)
for d in range(D):
	ppl = d * k + 1
	x = min(n // ppl, M)
	best = max(best, x * (d + 1))
print(best)
