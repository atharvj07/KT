//package codeforces;
// why i m still newbie :(
import java.util.Scanner;
import java.util.HashMap;
import java.util.TreeMap;
import java.math.BigInteger;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;

import java.util.Stack;

public class q_1 {
	static Scanner scn = new Scanner(System.in);
	static long cunt = 0;

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// System.out.format("%.10f", ans);char c = sc.next().charAt(0);
		
			String str=scn.next();
			int arr[]=new int[str.length()];
			int odd=0,even=0,count=0,ld=0,n=str.length();
			for(int i=0;i<str.length();i++)
			{	
				arr[i]=str.charAt(i)-48;
				if((str.charAt(i)-48)%2==0)
					even++;
					
				else
					odd++;
			}
			ld=arr[arr.length-1];
			//System.out.println(ld);
			
			if(even==0)
				System.out.println("-1");
			else
			{
				for(int i=0;i<n-1;i++)
				{
					if(arr[i]%2==0)
					{	count++;
						if(count!=even&&arr[i]<ld)
						{	
							int temp=arr[i];
							arr[i]=ld;
							arr[n-1]=temp;
							break;
						}
						else if(count==even&&(arr[i]<ld||arr[i]>ld))
						{		
							int temp=arr[i];
							arr[i]=ld;
							arr[n-1]=temp;
							break;
						}
						else if(count<even&&arr[i]>ld)
						{
							arr[i]=arr[i];
							continue;
						}
					}
					else
						arr[i]=arr[i];
				}
				for(int i=0;i<n;i++)
				{
					System.out.print(arr[i]);
				}
				System.out.println();
			}
			
	}
	

	public static String factorial(int n) {
		BigInteger fact = new BigInteger("1");
		for (int i = 1; i <= n; i++) {
			fact = fact.multiply(new BigInteger(i + ""));
		}
		return fact.toString();
	}

	public static int bs(int arr[], int k) {
		int high = 0, low = Integer.MIN_VALUE, ans = 0;
		for (int i = 0; i < arr.length; i++) {
			high += arr[i];
			if (arr[i] > low)
				low = arr[i];
		}
		while (low <= high) {
			int mid = (high + low) / 2;
			if (valid(arr, mid, k)) {
				ans = mid;
				high = mid - 1;
			} else
				low = mid + 1;
		}
		return ans;
	}

	public static boolean valid(int arr[], int mid, int k) {
		int sum = 0, cs = 1;
		for (int i = 0; i < arr.length; i++) {
			if (sum + arr[i] > mid) {
				cs++;
				if (cs > k)
					return false;
				sum = arr[i];
			} else
				sum += arr[i];
		}
		return true;
	}

	public static long gcd(long a, long n) {

		if (a == 0)
			return n;
		return gcd(n % a, a);
	}
	public static String rev(String str)
	{	int n=str.length();
		char ch[]=str.toCharArray();
		for(int i=0;i<str.length()/2;i++)
		{
			char temp=ch[i];
			ch[i]=ch[n-i-1];
			ch[n-i-1]=temp;
		}
		StringBuilder sb=new StringBuilder();
		for(int i=0;i<n;i++)
		{
			sb.append(ch[i]);
		}
		return sb.toString();
	}
}

