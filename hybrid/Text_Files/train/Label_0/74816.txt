n = int(input())
arr = [1] * (n + 1)
p = 2
while p * p <= n:
	if arr[p] == 1:
		for i in range(p * 2, n + 1, p):
			arr[i] = max(arr[i], p, i // p)
	p += 1
arr.sort()
print(' '.join([str(x) for x in arr[2:]]))
