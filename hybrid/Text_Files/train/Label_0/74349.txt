import math, sys
for _ in range(int(input())):
	n = input()
	ans = 10 ** 10
	for i in range(64):
		a = 2 ** i
		s = str(a)
		j = 0
		c = 0
		for i in n:
			if i == s[j]:
				c += 1
				j += 1
			if len(s) == j:
				break
		ans = min(ans, len(n) - c + len(s) - c)
	print(ans)
