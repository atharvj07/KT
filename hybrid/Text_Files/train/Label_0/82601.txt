import sys
from collections import defaultdict
lines = sys.stdin.readlines()
n = int(lines[0])
a = list(map(int, lines[1].split()))
d = defaultdict(int)
for i in a:
	d[i] += 1
for i in d:
	if d[i] % 2 == 1:
		print('Conan')
		sys.exit(0)
print('Agasa')
