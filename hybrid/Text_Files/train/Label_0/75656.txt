N_SETS = int(input())
for _ in range(N_SETS):
	(h, w, hb, wb) = [int(x) for x in input().split()]
	max_h = max(hb, h - hb - 1)
	max_w = max(wb, w - wb - 1)
	print(max(max_h * max_w, h * max_w, max_h * w))
