from fractions import gcd
import sys
(a, b, x, y) = (int(el) for el in sys.stdin.readline().split())
GCD = gcd(x, y)
X = x / GCD
Y = y / GCD
if a >= X and b >= Y:
	n = min(a // X, b // Y)
	print(int(n))
else:
	print('0')
