def main():
	(n, k) = map(int, input().split())
	cnt = [0] * 200001
	for i in map(int, input().split()):
		cnt[i] += 1
	cnt[0] = n
	r = m = c = 0
	for t in reversed(cnt):
		c += t
		if c == n:
			r += m != 0
			break
		if m + c > k:
			r += 1
			m = c
		else:
			m += c
	print(r)
main()
