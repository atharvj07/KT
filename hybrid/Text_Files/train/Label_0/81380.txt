t = int(input())
for j in range(t):
	(n, d) = map(int, input().split())
	h = list(map(int, input().split()))
	s = h[0]
	for i in range(1, n):
		if d < i:
			break
		elif i * h[i] < d:
			s += h[i]
			d -= i * h[i]
		else:
			while i * h[i] > d:
				h[i] -= 1
			s += h[i]
			break
	print(s)
