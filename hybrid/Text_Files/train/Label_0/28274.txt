#include<cstdio>
const int MAXN = 200000;
long long cnt[MAXN + 5];
int main() {
	int N, K;
	scanf("%d%d", &N, &K);
	for(int i=1;i<=N;i++)
		cnt[i%K]++;
	long long ans = 0;
	for(int i=0;i<K;i++)
		if( (2*i)%K == 0 ) ans += cnt[i]*cnt[i]*cnt[i];
	printf("%lld\n", ans);
}