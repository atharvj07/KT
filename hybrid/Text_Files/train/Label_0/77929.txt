(N, H, M) = map(int, input().split())
L = [0] * M
R = [0] * M
X = [0] * M
for i in range(M):
	(L[i], R[i], X[i]) = map(int, input().split())
ans = 0
i = 1
while i <= N:
	best = H
	for j in range(M):
		if L[j] <= i <= R[j]:
			best = min(best, X[j])
	ans = ans + best * best
	i = i + 1
print(ans)
