t = int(input().strip())
all_stamina = []
for _ in range(t):
	(x, z) = map(int, input().split())
	all_stamina.append([x, z])
for (x, z) in all_stamina:
	win_x = x - 1
	win_z = z
	print(win_x, win_z, end=' ')
	print()
