#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  int t, d = 0;
  cin >> t;
  while (t--) {
    int a, b, c, d, e = 0, f;
    cin >> a >> b;
    c = min(a, b);
    d = (a + b) / 3;
    if (c == 0)
      cout << "0" << endl;
    else if (d > a)
      cout << a << endl;
    else if (b < d)
      cout << b << endl;
    else
      cout << d << endl;
  }
}
