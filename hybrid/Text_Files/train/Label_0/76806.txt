(n, m) = map(int, input().split())

def solve():
	for i in range(m):
		current = set(list(map(int, input().split()))[1:])
		for (a, j) in enumerate(current, 1):
			if -j in current:
				break
			elif a == len(current):
				return 'YES'
	return 'NO'
print(solve())
