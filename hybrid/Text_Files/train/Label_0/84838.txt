import queue
q = queue.Queue()
t = int(input())
for i in range(t):
	n = int(input())
	time = 1
	for a in range(n):
		(l, r) = [int(k) for k in input().split()]
		if time < l:
			time = l
		if l <= time <= r:
			print(time, end=' ')
			time = time + 1
		else:
			print(0, end=' ')
	print()
