def solve(n, k, A):
	A.sort()
	best = float('inf')
	for i in range(n - k + 1):
		best = min(best, A[i + k - 1] - A[i])
	return best
(k, n) = map(int, input().split(' '))
in_ = map(int, input().split(' '))
temp = []
for x in in_:
	temp.append(int(x))
print(solve(n, k, temp))
