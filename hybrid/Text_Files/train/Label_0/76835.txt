def solve(n, k):
	sum = 0
	maxx = n
	while maxx > 0:
		c = min(maxx, k)
		sum += c
		maxx -= c
		maxx -= maxx // 10
	return 2 * sum >= n

def binarySearch(l, r):
	ans = r
	while l <= r:
		k = (r + l) // 2
		if solve(n, k):
			ans = k
			r = k - 1
		else:
			l = k + 1
	return ans
import sys
n = int(sys.stdin.readline())
print(binarySearch(1, n))
