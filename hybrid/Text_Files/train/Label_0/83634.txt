s = input()
a = 0
n = len(s)
for i in range(n):
	l = 0
	k = 0
	for j in range(i, n):
		l += s[j] == '('
		l -= s[j] == ')'
		k += s[j] == '?'
		if l + k < 0:
			break
		if k > l:
			(l, k) = (k, l)
		if l == k:
			a += 1
print(a)
