#include <bits/stdc++.h>
using namespace std;
int a[100000];
int main() {
  int i, j, k, n, m;
  cin >> n;
  for (i = 0; i < 2 * n; i++) {
    cin >> a[i];
  }
  sort(a, a + 2 * n);
  if (a[n] == a[n - 1])
    cout << "NO";
  else
    cout << "YES";
  return 0;
}
