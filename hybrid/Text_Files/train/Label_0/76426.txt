for i in range(int(input())):
	s = input()
	t = input()
	si = len(s) - 1
	ti = len(t) - 1
	while si >= 0 and ti >= 0:
		if s[si] == t[ti]:
			si -= 1
			ti -= 1
		else:
			si -= 1
			if si >= 0:
				si -= 1
	print('NO' if ti >= 0 else 'YES')
