#include <bits/stdc++.h>
using namespace std;
long long n, ans = 0;
int main() {
  cin >> n;
  for (int i = 0; i < (n); i++) {
    int p, q;
    cin >> p >> q;
    if (q - p >= 2) ans++;
  }
  cout << ans;
  return 0;
}
