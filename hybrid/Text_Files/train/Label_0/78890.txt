for i in range(int(input())):
	a = int(input())
	l = list(map(int, input().split()))
	d = {}
	for j in range(a):
		l[j] -= j + 1
	for j in l:
		if j in d:
			d[j] += 1
		else:
			d[j] = 1
	r = 0
	for j in d:
		r += d[j] * (d[j] - 1) // 2
	print(r)
