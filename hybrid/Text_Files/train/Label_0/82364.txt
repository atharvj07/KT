def get_possible_combinations(n):
	(a, d) = (1, 1)
	return int(n / 2 * (2 * a + (n - 1) * d))

def main():
	input()
	n = input()
	tot = get_possible_combinations(len(n))
	exc = 0
	for i in range(0, len(n)):
		if int(n[i]) % 2 != 0:
			exc += i + 1
	print(tot - exc)
main()
