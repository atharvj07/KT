#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, a, b, ans;
  cin >> n >> a >> b;
  b %= n;
  ans = (n + a + b) % n;
  if (ans == 0)
    cout << n;
  else
    cout << ans;
  return 0;
}
