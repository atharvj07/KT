#include<bits/stdc++.h>
using namespace std;

int main(){
  double H,W,w,h,x,y;
  cin>>W>>H>>w>>h>>x>>y;
  double ax=min(W/2,(w/2)+x);
  double ay=min(H/2,(h/2)+y);
  double bx=max(-W/2,x-(w/2));
  double by=max(-H/2,y-(h/2));
  //cout<<(ax+bx)
  printf("%.10f\n",(double)(ay+by)/(ax+bx));
}

