#include <bits/stdc++.h>
using namespace std;
int main() {
  long long N;
  scanf("%lld", &N);
  long long total = 0;
  for (int i = 0; i < N; i++) total += (i * (i + 1) / 2) + 1;
  printf("%lld\n", total);
  return 0;
}
