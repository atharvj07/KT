for _ in range(int(input())):
	a = input()
	if a.count('0') == 0:
		print('cyan')
		continue
	else:
		flag = 0
		if sum(list(map(int, a))) % 3 == 0:
			for i in a:
				if int(i) % 2 == 0:
					flag += 1
		if flag >= 2:
			print('red')
		else:
			print('cyan')
