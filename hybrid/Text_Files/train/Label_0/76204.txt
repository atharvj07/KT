R = lambda : map(int, input().split())
(t,) = R()
for _ in [0] * t:
	R()
	a = []
	b = [0]
	s = 0
	for (x, y) in sorted(zip(R(), R()))[::-1]:
		a += (x,)
		s += y
		b += (s,)
	print(min(map(max, a + b, b)))
