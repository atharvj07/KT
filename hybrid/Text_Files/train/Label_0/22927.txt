#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, ans = 0;
  scanf("%d", &n);
  if (n % 2) {
    printf("0");
    return 0;
  }
  int x = (n / 2) / 2;
  if (n % 4 == 0) x--;
  printf("%d", x);
  return 0;
}
