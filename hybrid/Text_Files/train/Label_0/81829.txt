import math
T = int(input())
for t in range(T):
	ang = int(input())
	n = 2
	while True:
		n += 1
		if n * ang > (n - 2) * 180:
			continue
		if ang * n % 180:
			continue
		k = n * ang // ((n - 2) * 90)
		if k >= n:
			continue
		break
	print(n)
