from math import gcd
n = int(input())
li = []
factorial = 1
for i in range(1, n):
	if gcd(n, i) == 1:
		li.append(i)
		factorial = factorial * i % n
if factorial == 1:
	print(len(li))
	print(*li)
else:
	print(len(li) - 1)
	print(*li[:len(li) - 1])
