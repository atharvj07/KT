def solve(n, d):
	d.sort()
	u = set()
	for i in d:
		if i not in u:
			u.add(i)
		else:
			u.add(i + 1)
	print(len(u))

def main():
	t = int(input())
	for i in range(t):
		n = int(input())
		d = input()
		d = [int(i) for i in d.split()]
		solve(n, d)
main()
