(n, k) = map(int, input().split())
l = list(map(int, input().split()))
r = [0] * k
c = 0
for i in l:
	r[i % k] += 1
c += r[0] - r[0] % 2
for i in range(1, len(r) // 2 + 1):
	if i == len(r) - i:
		c += r[i] // 2 * 2
	else:
		c += min(r[i], r[len(r) - i]) * 2
print(c)
