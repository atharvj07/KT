for _ in range(int(input())):
	(n, k) = map(int, input().split())
	l = list(map(int, input().split()))
	for i in range(n - 1):
		if k == 0:
			break
		val = min(l[i], k)
		l[i] -= val
		l[n - 1] += val
		k -= val
	print(*l)
