arr = [int(x) for x in input().split()]
A = arr[0]
D = arr[1]
M = arr[2]
arr = [int(x) for x in input().split()]
G = arr[0]
P = arr[1]
B = arr[2]

def Grapes(A, D, M, G, P, B):
	if G >= A:
		G = G - A
		if G + P >= D:
			G = G - D
			if G + P + B >= M:
				print('YES')
				return 0
	print('NO')
	return 0
Grapes(A, D, M, G, P, B)
