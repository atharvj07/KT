testcases = int(input())
for i in range(testcases):
	moves = input() + 'R'
	index = -1
	ans = 0
	for i in range(len(moves)):
		if moves[i] == 'R':
			ans = max(ans, i - index)
			index = i
	print(ans)
