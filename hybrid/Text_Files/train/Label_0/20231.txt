#include <bits/stdc++.h>
int main(void) {
  int row, col;
  int n;
  scanf("%d", &n);
  for (row = sqrt(n); row > 0; row--) {
    col = n / row;
    if (col * row == n) break;
  }
  printf("%d %d", row, col);
  return 0;
}
