/*
ID: triveni
LANG: JAVA
TASK: C
*/
import java.io.*;
import java.util.*;
import java.math.*;

public class Main {
	public static void main (String [] args) throws IOException {
		OutputWriter outputWriter = new OutputWriter();

		// InputReader in = new InputReader("C.in"); PrintWriter out = outputWriter.getOutputWriter("C.out");
		InputReader in = new InputReader(System.in); PrintWriter out = outputWriter.getOutputWriter(System.out);
		BigInteger x = BigInteger.valueOf(in.nextLong());
		BigInteger y = BigInteger.valueOf(in.nextLong());
		// System.out.println(x.toString());
		// System.out.println(y.toString());
		// System.out.println(x.gcd(y).toString());
		if (x.gcd(y).compareTo(BigInteger.valueOf(1)) != 0) out.println("Impossible");
		else {
			BigInteger lx = BigInteger.valueOf(0);
			BigInteger ly = BigInteger.valueOf(1);
			BigInteger rx = ly;
			BigInteger ry = lx;
			String fans = new String("");
			int iter = 0;
			while(lx.add(rx).compareTo(x) != 0 || ly.add(ry).compareTo(y) != 0){
				BigInteger a = x.multiply(ly).subtract(y.multiply(lx));
				BigInteger b = y.multiply(rx).subtract(x.multiply(ry));
				if(a.compareTo(BigInteger.valueOf(0)) < 0){
					throw new RuntimeException();
				}
				BigInteger lambda = b.divide(a);
				if(b.remainder(a).compareTo(BigInteger.valueOf(0)) == 0) lambda = lambda.subtract(BigInteger.valueOf(1));
				// System.out.println(a.toString() + " " + b.toString() + " " + lambda.toString());
				if(lambda.compareTo(BigInteger.valueOf(0)) > 0){
					rx = rx.add(lx.multiply(lambda));
					ry = ry.add(ly.multiply(lambda));
					fans += lambda.toString();
					fans += "B"; // L
				}
				lambda = a.divide(b);
				if(a.remainder(b).compareTo(BigInteger.valueOf(0)) == 0) lambda = lambda.subtract(BigInteger.valueOf(1));
				// System.out.println(a.toString() + " " + b.toString() + " " + lambda.toString());
				if(lambda.compareTo(BigInteger.valueOf(0)) > 0){
					lx = lx.add(rx.multiply(lambda));
					ly = ly.add(ry.multiply(lambda));
					fans += lambda.toString();
					fans += "A"; // R
				}
				// iter += 1;
				// System.out.print(lx.toString() + "/");
				// System.out.print(ly.toString()+ " ");
				// System.out.print(rx.toString() + "/");
				// System.out.print(ry.toString()+ "\n");
			}
			out.println(fans);
		}
		out.close();
		System.exit(0);
	}
	
	static class InputReader
	{
		public BufferedReader reader;
		public StringTokenizer tokenizer;
		
		public InputReader(InputStream inputStream)
		{
			reader = new BufferedReader(new InputStreamReader(inputStream));
			tokenizer = null;
		}

		public InputReader(String inputFile) throws FileNotFoundException
		{
			reader = new BufferedReader(new FileReader(inputFile));
			tokenizer = null;
		}

		public String nextLine()
		{
			String curr = "";
			try {
				curr = reader.readLine();
			} catch (IOException e) {
				throw new RuntimeException(e);
			}
			return curr;
		}

		public String nextString()
		{
			while(tokenizer == null || !tokenizer.hasMoreTokens()) {
				try {
					tokenizer = new StringTokenizer(nextLine());
				} catch (Exception e) {
					throw new RuntimeException(e);
				}
			}
			return tokenizer.nextToken();
		}

		public int nextInt()
		{
			return Integer.parseInt(nextString());
		}

		public long nextLong()
		{
			return Long.parseLong(nextString());
		}

		public double nextDouble(){
			return Double.parseDouble(nextString());
		}
	}

	static class OutputWriter
	{
		public PrintWriter getOutputWriter(String outputFile) throws IOException
		{
			return new PrintWriter(new BufferedWriter(new FileWriter(outputFile)));
		}
		public PrintWriter getOutputWriter(OutputStream outputStream) throws IOException
		{
			return new PrintWriter(new OutputStreamWriter(outputStream));
		}
	}
}
