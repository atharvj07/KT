#include <bits/stdc++.h>
using namespace std;
int main() {
  string a[8];
  for (int i = 0; i <= 7; i++) cin >> a[i];
  for (int i = 0; i <= 7; i++)
    for (int j = 1; j <= 7; j++)
      if (a[i][j] == a[i][j - 1]) {
        cout << "NO";
        return 0;
      }
  cout << "YES";
}
