import sys
input = sys.stdin.readline
(x, y, z) = map(int, input().split())
d = x - y
if d < 0:
	if z >= abs(d):
		print('?')
	else:
		print('-')
elif d == 0:
	if z > 0:
		print('?')
	else:
		print(0)
elif z >= d:
	print('?')
else:
	print('+')
