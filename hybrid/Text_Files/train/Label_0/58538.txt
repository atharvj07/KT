
import java.util.Scanner;

public class D {

	/**
	 * @param args
	 */
	public static void main(String[] args)
	{
//		String s= "TBBBBBBBBBBBBBBBBBBBBBBTTBBBTBT";
//		int a  = 0, b = 1;
//		for(int i = 0 ;i  <s.length();i++)
//			if(s.charAt(i) == 'T')
//				a += b;
//			else
//				b += a;
//		System.out.println(a + " "+b);
		Scanner scan = new Scanner(System.in);
		int n = scan.nextInt();
		int r = scan.nextInt();
	 	int min = Integer.MAX_VALUE;
	 	String ret = "";
		for (int i = 1; i <= r; i++)if(valid(r, i,n))
		{
			int a = get(r , i, n);
			if (a < min)
			{
				min = a;
				ret = seq.reverse().toString();
			}
			a = get(i , r, n);
			if (a < min)
			{
				min = a;
				ret = seq.reverse().toString();
			}
		}
		if (min == Integer.MAX_VALUE)
			System.out.println("IMPOSSIBLE");
		else
		{
			System.out.println(min);
//			StringBuilder s = new StringBuilder();
//			for (int i = 0; i < n-1; i++)
//				if (ret[i])
//					s.append('T');
//				else
//					s.append('B');
			System.out.println(ret);
		}	
	}
	private static boolean valid(int a, int b , int n)
	{
		int cnt = 0;
		while(b > 0)
		{
			cnt += a/b;
			int t  = b;
			b = a % b;
			a = t;
		}
		return a == 1 && cnt == n;
	}

	static StringBuilder seq;
	static int get(int a , int b, int n)
	{
		seq = new StringBuilder();
		int cnt = 0;
		for(int i = n-1; i  >= 0;i--)
		{
			if(a >= b)
			{
				a -= b;
				if(seq.length() > 0 && seq.charAt(seq.length()-1) == 'T')
					cnt++;
				seq.append('T');
			}else
			{
				b -=a;
				if(seq.length() > 0 && seq.charAt(seq.length()-1) == 'B')
					cnt++;
				seq.append('B');
			}
		}
//		seq.append("T");
//		if(c[0][0] != 0 || c[0][1] != 1)
//			return Integer.MAX_VALUE;
//		int cnt = 0;
//		for(int i = 0; i < n-1;i++)
//			if (c[i][0]+c[i][1] == c[i+1][0])
//				seq[i] = true;
//			else
//				seq[i] = false;
//		if(!seq[0])
//			return Integer.MAX_VALUE;
//		for(int i = 0; i < n-2;i++)
//			if(seq[i] == seq[i+1])
//				cnt++;
		return cnt;
	}

}
