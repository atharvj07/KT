#include<bits/stdc++.h>
using namespace std;
long long a[10000],n,cnt;
int main(){
	cin>>n;for(int i=0;i<n;i++)cin>>a[i];
	sort(a,a+n);
	for(int i=0;i<n;i++){
		if(cnt+1<a[i]){cout<<cnt+1<<endl;return 0;}cnt+=a[i];
	}
	cout<<cnt+1<<endl;
	return 0;
}