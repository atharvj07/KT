#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n;
  scanf("%lld", &n);
  long long bf = 2;
  for (long long i = 1; i <= n; i++) {
    long long a = i * (i + 1) * (i + 1);
    long long b = bf / i;
    printf("%lld\n", a - b);
    bf = i * (i + 1);
  }
}
