import sys
import math
input = sys.stdin.readline
n = int(input())
arr = list(map(int, input().split()))
b = 0
w = 0
for i in range(n):
	if i % 2 == 0:
		b += math.ceil(arr[i] / 2)
		w += arr[i] // 2
	else:
		w += math.ceil(arr[i] / 2)
		b += arr[i] // 2
print(min(b, w))
