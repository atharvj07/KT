#include <bits/stdc++.h>
using namespace std;
int n, u, ans, i;
int main() {
  cin >> n >> u;
  if (n - 1 <= u)
    cout << n - 1;
  else {
    ans = u;
    for (i = 2; i <= n - u; i++) ans += i;
    cout << ans;
  }
  return 0;
}
