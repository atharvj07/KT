#include <bits/stdc++.h>
std::string akp = "\n";
using LL = long long int;
using ULL = unsigned long long int;
using LD = long double;
using namespace std;
int i, j, l, m;
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);
  int x, y;
  cin >> x >> y;
  int x1, x2 = 0, y1 = 0, y2;
  y2 = x1 = abs(x) + abs(y);
  if (x < 0) {
    x1 *= -1;
  }
  if (y < 0) {
    y2 *= -1;
  }
  if (x1 > x2) {
    swap(x1, x2);
    swap(y1, y2);
  }
  cout << x1 << " " << y1 << " " << x2 << " " << y2;
  return 0;
}
