for _ in range(int(input())):
	N = int(input())
	List = [int(x) for x in input().split()]
	flag = 0
	Visited = [False] * N
	for i in range(N):
		X = (i + List[i]) % N
		Visited[X] = True
	for i in range(N):
		if Visited[i] == False:
			flag = 1
			print('NO')
			break
	if flag == 0:
		print('YES')
