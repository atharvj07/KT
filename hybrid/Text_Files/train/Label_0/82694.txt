(n, k) = map(int, input().split())
a = [*map(int, input().split())]
a.sort()
if k == 0:
	if a[0] > 1:
		print(a[0] - 1)
	else:
		print(-1)
else:
	chk = a[k - 1]
	if k < n:
		if a[k] > chk:
			print(a[k - 1])
		else:
			print(-1)
	else:
		print(a[k - 1])
