#include <bits/stdc++.h>
using namespace std;
int a[1005];
int n;
int main() {
  cin >> n;
  for (int i = 1; i <= n; i++) cin >> a[i];
  sort(a + 1, a + n + 1);
  if (a[1] == a[n / 2 + 1])
    cout << "Bob" << endl;
  else
    cout << "Alice" << endl;
  return 0;
}
