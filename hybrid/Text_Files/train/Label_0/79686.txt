(n, r) = map(int, input().split())
data = list(map(int, input().split()))
last = 0
ans = 0
while last < n:
	save = -1
	for i in range(max(0, last - r + 1), min(n, last + r)):
		if data[i] == 1:
			save = i
	if save == -1:
		ans = -1
		break
	else:
		ans += 1
		last = save + r
print(ans)
