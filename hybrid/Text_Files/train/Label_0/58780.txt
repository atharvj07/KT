import java.io.OutputStreamWriter;
import java.io.BufferedWriter;
import java.util.Comparator;
import java.io.OutputStream;
import java.io.PrintWriter;
import java.util.Random;
import java.io.Writer;
import java.util.List;
import java.io.IOException;
import java.util.Arrays;
import java.util.InputMismatchException;
import java.util.ArrayList;
import java.util.NoSuchElementException;
import java.math.BigInteger;
import java.io.InputStream;

/**
 * Built using CHelper plug-in
 * Actual solution is at the top
 * @author Alex
 */
public class Main {
	public static void main(String[] args) {
		InputStream inputStream = System.in;
		OutputStream outputStream = System.out;
		InputReader in = new InputReader(inputStream);
		OutputWriter out = new OutputWriter(outputStream);
		TaskB solver = new TaskB();
		solver.solve(1, in, out);
		out.close();
	}
}

class TaskB {
    public void solve(int testNumber, InputReader in, OutputWriter out){
	    int n = in.ri();
	    int[] a = IOUtils.readIntArray(in, n);
	    ArrayUtils.ss(a);
	    ArrayUtils.reverse(a);
	    if (ArrayUtils.count(a, 0) == 0){
		    out.printLine(-1);
		    return;
	    }
	    int sum = (int) ArrayUtils.sumArray(a);
	    if (sum % 3 == 0){
		    if (a[0] == 0){
			    out.printLine(0);
		    }
		    else{
			    for(int i : a) out.print(i);
		    }
		    return;
	    }
	    ArrayList<Integer> aa = new ArrayList<>();
	    for(int i : a) aa.add(i);
	    if (sum % 3 == 1){ //ignore 0, 3, 6, 9      try delete 1, 4, 7    otherwise delete 2 of 2, 5, 8
		    if(ArrayUtils.count(a, 1) + ArrayUtils.count(a, 4) + ArrayUtils.count(a, 7) > 0){
				for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 1 || aa.get(i) == 4 || aa.get(i) == 7){
					aa.remove(i);
					break;
				}
		    }
		    else if(ArrayUtils.count(a, 2) + ArrayUtils.count(a, 5) + ArrayUtils.count(a, 8) > 1){
			    for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 2 || aa.get(i) == 5 || aa.get(i) == 8){
				    aa.remove(i);
				    break;
			    }
			    for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 2 || aa.get(i) == 5 || aa.get(i) == 8){
				    aa.remove(i);
				    break;
			    }
		    }
		    else{
			    out.printLine(-1);
			    return;
		    }
	    }
	    else{ ////ignore 0, 3, 6, 9   delete 2, 5, 8    otherwise try delete 2 of 1, 4, 7
		    if (ArrayUtils.count(a, 2) + ArrayUtils.count(a, 5) + ArrayUtils.count(a, 8) > 0){
			    for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 2 || aa.get(i) == 5 || aa.get(i) == 8){
				    aa.remove(i);
				    break;
			    }
		    }
		    else if(ArrayUtils.count(a, 1) + ArrayUtils.count(a, 4) + ArrayUtils.count(a, 7) > 1){
			    for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 1 || aa.get(i) == 4 || aa.get(i) == 7){
				    aa.remove(i);
				    break;
			    }
			    for(int i = aa.size()-1; i >= 0; i--) if (aa.get(i) == 1 || aa.get(i) == 4 || aa.get(i) == 7){
				    aa.remove(i);
				    break;
			    }
		    }
		    else{
			    out.printLine(-1);
			    return;
		    }
	    }
	    if (aa.get(0) == 0){
		    out.printLine(0);
	    }
	    else{
		    for(int i : aa) out.print(i);
	    }
    }
}

class InputReader {
	private InputStream stream;
	private byte[] buf = new byte[1024];
	private int curChar;
	private int numChars;
	private SpaceCharFilter filter;

	public InputReader(InputStream stream) {
		this.stream = stream;
	}

	public int read() {
		if (numChars == -1)
			throw new InputMismatchException();
		if (curChar >= numChars) {
			curChar = 0;
			try {
				numChars = stream.read(buf);
			} catch (IOException e) {
				throw new InputMismatchException();
			}
			if (numChars <= 0)
				return -1;
		}
		return buf[curChar++];
	}
	public int ri(){
		return readInt();
	}

	public int readInt() {
		int c = read();
		while (isSpaceChar(c))
			c = read();
		int sgn = 1;
		if (c == '-') {
			sgn = -1;
			c = read();
		}
		int res = 0;
		do {
			if (c < '0' || c > '9')
				throw new InputMismatchException();
			res *= 10;
			res += c - '0';
			c = read();
		} while (!isSpaceChar(c));
		return res * sgn;
	}
	public boolean isSpaceChar(int c) {
		if (filter != null)
			return filter.isSpaceChar(c);
		return isWhitespace(c);
	}

	public static boolean isWhitespace(int c) {
		return c == ' ' || c == '\n' || c == '\r' || c == '\t' || c == -1;
	}
	public interface SpaceCharFilter {
		public boolean isSpaceChar(int ch);
	}
}

class OutputWriter {
	private final PrintWriter writer;

	public OutputWriter(OutputStream outputStream) {
		writer = new PrintWriter(new BufferedWriter(new OutputStreamWriter(outputStream)));
	}
	public void close() {
		writer.close();
	}
	public void print(int i) {
		writer.print(i);
	}

	public void printLine(int i) {
		writer.println(i);
	}

}

class IOUtils {
	public static int[] readIntArray(InputReader in, int size) {
		int[] array = new int[size];
		for (int i = 0; i < size; i++)
			array[i] = in.readInt();
		return array;
	}
}

class ArrayUtils {
	public static long sumArray(int[] array) {
		long result = 0;
		for (int element : array)
			result += element;
		return result;
	}
	public static void reverse(int[] array) {
		for (int i = 0, j = array.length - 1; i < j; i++, j--) {
			int temp = array[i];
			array[i] = array[j];
			array[j] = temp;
		}
	}
	public static int count(int[] array, int value) {
		int result = 0;
		for (int i : array) {
			if (i == value)
				result++;
		}
		return result;
	}
	public static void ss(int[] arr){
		int n = arr.length;
		Random rnd = new Random();
		for(int i=0; i<n; ++i){
			int tmp = arr[i];
			int randomPos = i + rnd.nextInt(n-i);
			arr[i] = arr[randomPos];
			arr[randomPos] = tmp;
		}
		Arrays.sort(arr);
	}
}

