t = int(input())
for _ in range(t):
	(n, m) = map(int, input().split())
	m += 1
	ans = 0
	for i in range(30, -1, -1):
		if not 1 << i & n and 1 << i & m:
			ans += 1 << i
		elif 1 << i & n and (not 1 << i & m):
			break
	print(ans)
