#include <bits/stdc++.h>
using namespace std;
int main() {
  long long a, b;
  long long T;
  cin >> T;
  while (T--) {
    cin >> a >> b;
    if (a - b == 0 || a - b == 1) {
      cout << "NO" << endl;
    } else
      cout << "YES" << endl;
  }
}
