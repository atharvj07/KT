n = int(input())
m = int(input())
ln = len(str(n))
ans = 0
i = 0
while m != 0:
	dgt = m % 10 ** ln
	if dgt < n and len(str(dgt)) == ln:
		ans += dgt * n ** i
		i += 1
		m = m // 10 ** ln
		ln = len(str(n))
	else:
		ln -= 1
print(ans)
