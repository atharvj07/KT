(n, m) = map(int, input().split())
row = dict()
col = dict()
ans = []
for i in range(m):
	(x, y) = map(int, input().split())
	row[x] = row.get(x, 0) + 1
	col[y] = col.get(y, 0) + 1
	a = len(row)
	b = len(col)
	ans.append(n * n - (n * a + n * b - a * b))
print(' '.join(map(str, ans)))
