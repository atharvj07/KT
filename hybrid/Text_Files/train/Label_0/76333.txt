(n, k) = map(int, input().split())
ma = 0
a = str(input()).split()
m = set(a)
import collections
c = collections.Counter(a)
for y in c:
	if int(c[y]) > ma:
		ma = c[y]
while ma % k != 0:
	ma += 1
otv = len(m) * ma - n
print(otv)
