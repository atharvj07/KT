#include <bits/stdc++.h>
using namespace std;
long long int arr[2000006];
long long int a[2000006];
long long int pr[2000006];
vector<long long int> p;
int chk(int r) {
  if (pr[r] == r)
    return r;
  else
    return chk(pr[r]);
}
int cnt = 0, r = 0;
int uni(int c, int p) {
  int u, v;
  u = chk(c);
  v = chk(p);
  if (u != v) {
    pr[u] = v;
  }
}
int main() {
  int n;
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> arr[i];
    a[i] = arr[i];
    if (arr[i] == i) r = i;
    pr[i] = i;
  }
  for (int i = 1; i <= n; i++) {
    uni(i, arr[i]);
  }
  for (int i = 1; i <= n; i++) {
    if (pr[i] == i) {
      if (r) {
        if (arr[i] != r) cnt++;
        arr[i] = r;
      } else {
        if (arr[i] != pr[i]) cnt++;
        r = arr[i] = pr[i];
      }
    }
    if (arr[i] != a[i]) {
    }
  }
  cout << cnt << endl;
  for (int i = 1; i <= n; i++) {
    if (i == 1)
      cout << arr[i];
    else
      cout << " " << arr[i];
  }
  cout << endl;
}
