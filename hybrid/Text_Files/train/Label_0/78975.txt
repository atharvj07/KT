import math
n = int(input())
for _ in range(n):
	(l, r) = map(int, input().split(' '))
	if l == r:
		print(0)
	else:
		m = math.ceil(r % (r / 2 + 1))
		if l <= r / 2 + 1:
			print(m)
		else:
			print(r - l)
