#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  ;
  cin >> t;
  while (t--) {
    string s;
    int i;
    cin >> s;
    int n = s.length();
    if (n == 1) {
      cout << "-1\n";
      continue;
    }
    int flag = 0;
    for (i = 1; i < n; i++) {
      if (s[i] == s[0]) {
      } else {
        sort(s.begin(), s.end());
        cout << s << "\n";
        flag = 1;
        break;
      }
    }
    if (!flag) cout << "-1\n";
  }
}
