n_tests = int(input())
for _ in range(n_tests):
	str = input()
	prev = -1
	maxx = 0
	for i in range(len(str)):
		if str[i] == 'R':
			tmp = i - prev
			prev = i
			if tmp > maxx:
				maxx = tmp
	if len(str) - prev > maxx:
		maxx = len(str) - prev
	print(maxx)
