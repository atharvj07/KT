import math

def sum(x):
	s = 0
	while x > 0:
		s += x % 10
		x = x // 10
	return s
t = int(input())
for i in range(t):
	n = int(input())
	if math.gcd(n, sum(n)) > 1:
		print(n)
	elif math.gcd(n + 1, sum(n + 1)) > 1:
		print(n + 1)
	else:
		print(n + 2)
