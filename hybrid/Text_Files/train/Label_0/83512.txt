I = input
for _ in [0] * int(I()):
	(n, k) = map(int, I().split())
	a = sorted(map(len, I().strip('L').split('W')))
	m = len(a) + k
	while a and a[0] <= k:
		k -= a.pop(0)
	print((2 * min(n, m - 1) - len(a) or 1) - 1)
