t = int(input())
for i in range(t):
	n = int(input())
	l = list(range(1, n + 1))
	ans = []
	for j in range(n - 1):
		a = l.pop()
		b = l.pop()
		l.append((a + b + 1) // 2)
		ans.append((a, b))
	print(l[0])
	for j in range(n - 1):
		print(ans[j][0], ans[j][1])
