t = int(input())
for i in range(t):
	a = list(map(int, input().split()))
	(n, k) = (a[0], a[1])
	key = 0
	for p in range(2, n + 1):
		if n % p == 0:
			key = 1
			break
	if key == 0:
		print(2 * n + 2 * (k - 1))
	elif p == 2:
		print(n + p * k)
	else:
		print(n + p + 2 * (k - 1))
