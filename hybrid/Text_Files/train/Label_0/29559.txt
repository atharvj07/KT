#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  for (int i = 1; i < n; i++)
    cout << ((i % 2 == 0) ? "I love that " : "I hate that ");
  cout << (n % 2 == 0 ? "I love it" : "I hate it") << endl;
  return 0;
}
