#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  for (int i = 2; i <= n; i++) {
    if (n % i == 0) {
      for (int j = 0; j < i / 2; j++) {
        swap(s[j], s[i - j - 1]);
      }
    }
  }
  cout << s << endl;
}
