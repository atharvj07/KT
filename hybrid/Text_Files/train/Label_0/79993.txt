def gcd(a, b):
	while b:
		(a, b) = (b, a % b)
	return a
for _ in range(int(input())):
	(m, d, w) = map(int, input().split())
	g = w // gcd(d - 1, w)
	c = min(m, d)
	v = c // g
	ans = v * (v - 1) // 2 * g
	ans += (c - g * v) * v
	print(ans)
