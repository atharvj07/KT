#include <bits/stdc++.h>
using namespace std;
int main() {
  int h, i, j = 0;
  char ar[1005];
  char br[10000];
  scanf("%s", &ar);
  h = strlen(ar);
  for (i = h - 1; i >= 0; i--) {
    br[j] = ar[i];
    j++;
  }
  printf("%s%s", ar, br);
}
