import functools, math

def main():
	input()
	a = functools.reduce(math.gcd, map(int, input().split()), 0)
	ans = 0
	p = 1
	while p ** 2 <= a:
		if not a % p:
			ans += 1 + (p ** 2 < a)
		p += 1
	print(ans)
main()
