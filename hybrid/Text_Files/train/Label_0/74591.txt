from math import gcd
n = int(input())
ans = []
cal = 1
for i in range(1, n - 1):
	if gcd(i, n) == 1:
		ans.append(i)
		cal = cal * i % n
cal = cal * (n - 1) % n
if cal == 1:
	ans.append(n - 1)
print(len(ans))
print(*ans)
