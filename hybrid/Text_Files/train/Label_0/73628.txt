t = int(input())
for i in range(t):
	[n, k] = list(map(int, input().split()))
	s = input()
	l = [s.count(a) for a in set(s)]
	M = 0
	for j in range(1, n + 1):
		MM = sum(map(lambda x: x // j, l))
		if MM == 0:
			continue
		while k % MM:
			MM -= 1
		M = max(M, MM * j)
	print(M)
