t = int(input())
for i in range(t):
	n = int(input())
	x = [int(x) for x in input().split()]
	xored = [x[0]]
	y = ['0']
	for j in range(n - 1):
		k = xored[j] | x[j + 1]
		k -= x[j + 1]
		y.append(str(k))
		xored.append(x[j + 1] ^ k)
	separator = ' '
	print(separator.join(y))
