import static java.lang.System.*;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.Collection;
import java.util.Comparator;
import java.util.HashSet;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Scanner;
import java.util.Set;

public class E350{

    public static Scanner sc = new Scanner(new BufferedReader(new InputStreamReader(in)));

    //public static Random sc=new Random();

    public void run(){
        int n=sc.nextInt(),m=sc.nextInt(),k=sc.nextInt();
        final Set<Integer> as=new HashSet<>();
        for(int i=0;i<k;i++)as.add(sc.nextInt());
        if(n==k){
            ln(-1);return;
        }
        Integer[] shift=new Integer[n];
        for(int i=0;i<n;i++){
            shift[i]=i+1;
        }
        Arrays.sort(shift,new Comparator<Integer>(){
            @Override
            public int compare(Integer o1, Integer o2) {
                boolean b1=!as.contains(o1),b2=!as.contains(o2);
                if(b1!=b2){
                    if(b1)return 1;
                    if(b2)return -1;
                }
                return 0;
            }
        });

        StringBuilder sb=new StringBuilder();
        int s=0;
        for(int i=0;i<n-1;i++){//n
            if(s>=m)break;
            sb.append(shift[n-1]+" "+shift[i]+br);s++;
        }
        loop:for(int i=2;i<n-1;i++)for(int j=2;j<n-1;j++){//others creek
            if(s>=m)break loop;
            if(!(i<j))continue;
            sb.append(shift[i]+" "+shift[j]+br);s++;
        }

        for(int i=2;i<n-1;i++){//one hand all connect
            if(s>=m)break;
            sb.append(shift[0]+" "+shift[i]+br);s++;
        }
        for(int i=2;i<n-1;i++){
            if(s>=m)break;
            if(as.contains(shift[i]))continue;
            sb.append(shift[1]+" "+shift[i]+br);s++;
        }
        if(m!=s){
            ln(-1);return;
        }
        pr(sb);
   }
    public static void main(String[] _) {
        new E350().run();
    }
    //output lib
    static final String br=System.getProperty("line.separator");
    static final String[] asep=new String[]{""," ",br,br+br};
    static String str(Boolean o){
        return o?"YES":"NO";
    }
//  static String str(Double o){
//      return String.format("%.8f",o);
//  }
    static <K,V> String str(Map<K, V> map){
        StringBuilder sb=new StringBuilder();
        boolean isFirst=true;
        for(Entry<K,V> set:map.entrySet()){
            if(!isFirst)sb.append(br);
            sb.append(str(set.getKey())).append(":").append(str(set.getValue()));
            isFirst=false;
        }
        return sb.toString();
    }
    static <E> String str(Collection<E> list){
        StringBuilder sb=new StringBuilder();
        boolean isFirst=true;
        for(E e:list){
            if(!isFirst)sb.append(" ");
            sb.append(str(e));
            isFirst=false;
        }
        return sb.toString();
    }

    static String str(Object o){
        int depth=_getArrayDepth(o);
        if(depth>0)return _strArray(o,depth);
        Class<?> c=o.getClass();
        if(c.equals(Boolean.class))return str((Boolean)o);
        //if(c.equals(Double.class))return str((Double)o);

        return o.toString();
    }
    static int _getArrayDepth(Object o){
        if(!o.getClass().isArray() || Array.getLength(o)==0) return 0;
        return  1+_getArrayDepth(Array.get(o,0));
    }
    static String _strArray(Object o,int depth){
        if(depth==0) return str(o);
        StringBuilder sb=new StringBuilder();
        for(int i=0,len=Array.getLength(o);i<len;i++){
            if(i!=0)sb.append(asep[depth]);
            sb.append(_strArray(Array.get(o,i),depth-1));
        }
        return sb.toString();
    }
    static void pr(Object... os){
        boolean isFirst=true;
        for(Object o:os){
            if(!isFirst)out.print(" ");
            out.print(o);
            isFirst=false;
        }
    }
    static void ln(){
        out.println();
    }
    static void ln(Object... os){
        for(Object o:os){
            pr(o);ln();
        }
    }
}