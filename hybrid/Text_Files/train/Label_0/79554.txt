for i in range(int(input())):
	(n, a, b) = map(int, input().strip().split())
	s = input()
	c = 1
	for i in range(1, len(s)):
		if s[i] != s[i - 1]:
			c += 1
	if b >= 0:
		print(a * n + b * n)
	else:
		print(a * n + b * (c // 2 + 1))
