def test(no):
	global flag
	D = {}
	no = str(no)
	for j in range(len(no)):
		if no[j] not in D:
			D.update({no[j]: None})
		else:
			flag = 0
(n, m) = map(int, input().split())
flag = 1
for i in range(n, m + 1):
	flag = 1
	test(i)
	if flag == 1:
		print(i)
		break
if flag == 0:
	print('-1')
