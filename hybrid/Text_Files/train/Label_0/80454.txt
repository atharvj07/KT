import sys
(n, k) = map(int, input().split())
arr = input()
dic = {}
for i in arr:
	if i in dic:
		dic[i] = dic[i] + 1
	else:
		dic[i] = 1
for d in dic:
	if dic[d] > k:
		print('NO')
		sys.exit()
print('YES')
