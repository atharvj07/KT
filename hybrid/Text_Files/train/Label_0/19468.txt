#include<iostream>
#include<algorithm>
#include<vector>
#include<queue>
#define lol(i,n) for(ll i=0;i<n;i++)
#define mod 1000000007
typedef long long ll;

using namespace std;

int main(){
    ll h,w,ans=0,a;
    cin>>h>>w;
    lol(i,h)lol(j,w){
	cin>>a;
	ans+=a*(i+1)*(h-i)*(j+1)*(w-j);
    }
    cout<<ans<<endl;
    return 0;
}
