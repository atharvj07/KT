#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, i, t, m = -1, x = 0, y = 0;
  cin >> n;
  while (n--) {
    cin >> t;
    if (t == 1) {
      x++;
      if (y) y--;
    } else {
      y++;
      m = max(m, y);
    }
  }
  cout << x + m;
  return 0;
}
