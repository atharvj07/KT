(n, m) = [int(x) for x in input().split()]
a = [int(x) for x in input().split()]
if n > m:
	print(0)
	exit()
ans = 1
for i in range(n):
	for j in range(i + 1, n):
		ans *= abs(a[i] - a[j])
		ans = ans % m
print(ans)
