#include <bits/stdc++.h>
using namespace std;
char s[111];
bool c,f;
int main() {
  scanf("%s",s);
  for (int i=0; s[i]; i++) if (s[i]=='F') {
    if (c) f=true;
  } else if (s[i]=='C') c=true;
  puts(f?"Yes":"No");
  return 0;
}
