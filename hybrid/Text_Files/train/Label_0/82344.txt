for _ in range(1):
	(n, k) = map(int, input().split())
	s = input()
	s = s + ' '
	x = set(input().split())
	z = 0
	ans = 0
	for i in s:
		if i in x:
			z += 1
		else:
			ans += z * (z + 1) // 2
			z = 0
	print(ans)
