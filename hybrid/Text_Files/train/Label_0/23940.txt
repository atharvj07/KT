#include <stdio.h>
  
int main(void) {
  int n, a, b, i;
  scanf("%d%d%d", &a, &b, &n);
  //printf("start\n");
  for(i = 0; i < n; ++i) {
    int s, t, te;
    scanf("%d%d",&s, &t);
    if(t <= a || s >= b) {te = 1;}
    else {
      printf("1\n");
      return 0;
    }
  }
  printf("0\n");
  return 0;
}
