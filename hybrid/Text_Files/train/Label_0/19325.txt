#include <bits/stdc++.h>
using namespace std;

string str;

int main( void ) {
    
    cin >> str;
    cout << ( count( str.begin(), str.end(), 'g' ) -
              count( str.begin(), str.end(), 'p' ) ) / 2 << endl;
    
    return 0;
}