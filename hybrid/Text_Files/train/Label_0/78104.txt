def f(x, y):
	return x & ~y
for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	ans = [0] * n
	for i in range(1, n):
		ans[i] = f(ans[i - 1] ^ a[i - 1], a[i])
	print(' '.join(map(str, ans)))
