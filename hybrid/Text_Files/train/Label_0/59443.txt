import java.util.*;
import java.math.*;
import java.io.*;

public class input2 {
    public static String s2;
    public static void merge(int arr[], int l, int m, int r) {
        // Find sizes of two subarrays to be merged
        int n1 = m - l + 1;
        int n2 = r - m;

        /* Create temp arrays */
        int L[] = new int[n1];
        int R[] = new int[n2];

        /*Copy data to temp arrays*/
        for (int i = 0; i < n1; ++i)
            L[i] = arr[l + i];
        for (int j = 0; j < n2; ++j)
            R[j] = arr[m + 1 + j];


        /* Merge the temp arrays */

        // Initial indexes of first and second subarrays
        int i = 0, j = 0;

        // Initial index of merged subarry array
        int k = l;
        while (i < n1 && j < n2) {
            if (L[i] <= R[j]) {
                arr[k] = L[i];
                i++;
            } else {
                arr[k] = R[j];
                j++;
            }
            k++;
        }

        /* Copy remaining elements of L[] if any */
        while (i < n1) {
            arr[k] = L[i];
            i++;
            k++;
        }

        /* Copy remaining elements of R[] if any */
        while (j < n2) {
            arr[k] = R[j];
            j++;
            k++;
        }
    }

    // Main function that sorts arr[l..r] using
    // merge()
    public static void sort(int arr[], int l, int r) {
        if (l < r) {
            // Find the middle point
            int m = (l + r) / 2;

            // Sort first and second halves
            sort(arr, l, m);
            sort(arr, m + 1, r);

            // Merge the sorted halves
            merge(arr, l, m, r);
        }
    }

    public static ArrayList<Integer> isSubstring(String s1,int a) {
        int M = s1.length();
        int N = s2.length();
        char c[] = s2.toCharArray();
        ArrayList<Integer>A = new ArrayList<>();
        /* A loop to slide pat[] one by one */
        for (int i = 0; i <= N - M; i++) {
            int j;

            /* For current index i, check for
            pattern match */
            for (j = 0; j < M; j++)
                if (s2.charAt(i + j) != s1.charAt(j))
                    break;

            if (j == M){
             A.add(i+a);
             c[i+a] = 'X';
             s2 = new String(c);
            }
        }
        return A;
    }

    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int t = Integer.parseInt(br.readLine());
        String s = "twone";
        String s1 = "one";
        String s3 = "two";
        for(int x =0;x<t;x++){
           char in[] = br.readLine().toCharArray();
            int n = in.length;
            ArrayList<Integer> out = new ArrayList<>();
            for(int i = 0; i + 2 < n; ++i) {
                if(in[i] == 'o' && in[i + 1] == 'n' && in[i + 2] == 'e') {
                    if(i - 1 >= 0 && in[i - 1] == 'o') out.add(i + 2);
                    else out.add(i + 1);
                }
                if(in[i] == 't' && in[i + 1] == 'w' && in[i + 2] == 'o') {
                    if(i + 3 < n && in[i + 3] == 'o') out.add(i + 2);
                    else out.add(i + 3);
                    i += 2;
                }
            }
            System.out.println(out.size());
            for(int a : out) System.out.print(a + " ");
            System.out.println();
        }
    }
}