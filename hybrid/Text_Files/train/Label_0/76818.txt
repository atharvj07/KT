def main():
	(n, m) = map(int, input().split(' '))
	if m % n > 0:
		print(-1)
		return
	a = m // n
	transforms = 0
	while a > 1:
		if a % 2 == 0:
			a //= 2
		elif a % 3 == 0:
			a //= 3
		else:
			print(-1)
			return
		transforms += 1
	print(transforms)
main()
