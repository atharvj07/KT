def main():
	n = int(input())
	a = list(map(int, input().split()))
	last_pos = {}
	for i in range(n):
		last_pos[a[i]] = i
	(ret, cur) = (1, -1)
	for i in range(n - 1):
		cur = max(cur, last_pos[a[i]])
		if cur == i:
			ret = ret * 2 % 998244353
	print(ret)
main()
