#include <bits/stdc++.h>
using namespace std;
const int MOD = 1000000007;
struct Sync_stdio {
  Sync_stdio() {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
  }
} _sync_stdio;
int main() {
  int n, a;
  cin >> n >> a;
  if (a % 2 == 0) {
    a = n + 2 - a;
  }
  cout << (a - 1) / 2 + 1;
}
