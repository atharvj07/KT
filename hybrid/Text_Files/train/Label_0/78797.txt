from sys import stdin, stdout
x = [[i for i in stdin.readline()] for i in range(4)]
z = 'NO'
for i in range(3):
	for j in range(3):
		f = x[i][j] + x[i][j + 1] + x[i + 1][j] + x[i + 1][j + 1]
		if f.count('#') >= 3 or f.count('.') >= 3:
			z = 'YES'
			break
stdout.write(z)
