import sys
for _ in range(int(input())):
	n = int(input())
	strs = list(input())
	alpha = list('ABCDEFGHIJKLMNOPQRSTUVWXYZ')
	used = []
	flag = True
	for i in range(len(strs) - 1):
		if strs[i + 1] == strs[i]:
			pass
		elif strs[i + 1] in used:
			flag = False
			break
		else:
			used.append(strs[i])
	if flag:
		print('YES')
	else:
		print('NO')
