#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); i++)
#define repd(i,a,b) for (int i = (a); i < (b); i++)
typedef long long ll;
int main(void)
{
    int q, com, x;
    string key;
    map<string, int> M;
    cin >> q;
    
    rep(i, q)
    {
        cin >> com >> key;
        
        if (com == 0)
        {
            cin >> x;
            M[key] = x;
        }
        else
        {
            cout << M[key] << endl;
        }
    }
}

