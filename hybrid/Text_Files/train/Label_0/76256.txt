(n, k) = map(int, input().split())
s = list(map(int, input().split()))
ans = s[-1]
if k >= n:
	print(ans)
else:
	single = 2 * k - n
	for i in range(n - single):
		ans = max(ans, s[i] + s[-single - 1 - i])
	print(ans)
