def distance(x, y):
	a = x + y
	if a % 2 == 0:
		a //= 2
		print(min(a, x), max(a - x, 0))
	else:
		print('-1 -1')
in_t_case = int(input())
for i in range(in_t_case):
	(x, y) = map(int, input().split())
	distance(x, y)
