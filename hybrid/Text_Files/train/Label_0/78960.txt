n = int(input())
s = list(map(int, list(input())))
sm = sum(s)
if sum(s) == 0:
	print('YES')
	exit()
for j in range(1, 901):
	if j >= sm:
		break
	if sm % j != 0:
		continue
	ok = 0
	f = 0
	cur_sm = 0
	for k in range(len(s)):
		cur_sm += s[k]
		if cur_sm > j:
			k -= 1
			break
		elif cur_sm == j:
			cur_sm = 0
	if k == len(s) - 1:
		print('YES')
		exit()
print('NO')
