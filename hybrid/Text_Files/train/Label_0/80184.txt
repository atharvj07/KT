import os, io
input = io.BytesIO(os.read(0, os.fstat(0).st_size)).readline
(n, q, k) = map(int, input().split())
a = list(map(int, input().split()))
for _ in range(q):
	(x, y) = map(int, input().split())
	print(k - 1 + a[y - 1] - a[x - 1] - 2 * (y - x))
