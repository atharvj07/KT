n = int(input())
last = float('inf')
flag = 1
for i in range(n):
	(w, h) = map(int, input().split())
	if flag and max(w, h) <= last:
		last = max(w, h)
	elif flag:
		if min(w, h) > last:
			flag = 0
		else:
			last = min(w, h)
if flag == 1:
	print('YES')
else:
	print('NO')
