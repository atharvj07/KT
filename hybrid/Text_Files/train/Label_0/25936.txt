#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n,i,j,k;
	cin>>n;
	int h[n];
	for (i=0;i<n;i++)
	cin>>h[i];
	int dp[n+1];
	dp[0]=0;
	dp[1]=0;
	dp[2]=abs(h[1]-h[0]);
	for (i=3;i<=n;i++)
	{
		dp[i]=min(dp[i-1]+abs(h[i-1]-h[i-2]),dp[i-2]+abs(h[i-1]-h[i-3]));
	}
	cout<<dp[n]<<endl;
}