import sys
from fractions import gcd
lines = sys.stdin.readlines()
t = int(lines[0])
for i in range(t):
	L = list(map(int, lines[i + 1].split()))
	if gcd(L[0], L[1]) == 1:
		print('Finite')
	else:
		print('Infinite')
