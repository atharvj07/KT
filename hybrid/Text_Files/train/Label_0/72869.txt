import sys, math

def solve():
	s = input()
	ans = 0
	for i in range(len(s)):
		if i != len(s) - 1 and s[i] == s[i + 1]:
			continue
		else:
			if s[i] == '0':
				ans = ans + 1
			if s[i] == '1':
				ans = ans + 0
	ans = min(2, ans)
	print(ans)
t = int(input())
while t != 0:
	solve()
	t -= 1
