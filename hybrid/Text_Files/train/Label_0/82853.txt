from math import ceil
q = int(input())
while q > 0:
	(k, n, a, b) = map(int, input().split())
	x = (k - b * n) / (a - b)
	if k - b * n <= 0:
		ans = -1
	else:
		ans = min(n, ceil(x) - 1)
	print(ans)
	q = q - 1
