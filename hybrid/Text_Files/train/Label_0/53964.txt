import java.io.OutputStream;
import java.io.IOException;
import java.io.InputStream;
import java.io.PrintWriter;
import java.util.StringTokenizer;
import java.io.IOException;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.InputStream;

/**
 * Built using CHelper plug-in
 * Actual solution is at the top
 *
 * @author htvu
 */
public class Main {
  public static void main(String[] args) {
    InputStream inputStream = System.in;
    OutputStream outputStream = System.out;
    InputReader in = new InputReader(inputStream);
    PrintWriter out = new PrintWriter(outputStream);
    FNetworkCoverage solver = new FNetworkCoverage();
    solver.solve(1, in, out);
    out.close();
  }

  static class FNetworkCoverage {
    private static final int MAXN = (int) 1e6 + 2;

    public void solve(int testNumber, InputReader in, PrintWriter out) {
      int[] a = new int[MAXN];
      int[] b = new int[MAXN];
      for (int test = in.nextInt(); test > 0; --test) {
        int n = in.nextInt();
        for (int i = 0; i < n; ++i)
          a[i] = in.nextInt();
        for (int i = 0; i < n; ++i)
          b[i] = in.nextInt();

        long l = a[0] >= b[0] ? 0 : b[0] - a[0];
        long r = b[0];
        boolean found = false;
        while (l <= r) {
          long m = (l + r) / 2;
          boolean ok = true;
          long diff = m;
          for (int i = 0; i < n; ++i) {
            int idx = (i + 1) % n;
            if (a[idx] > b[idx] + diff) {
              ok = false;
              break;
            }
            diff = Math.min(b[idx], b[idx] + diff - a[idx]);
          }
          if (!ok) {
            l = m + 1;
          } else if (diff < m) {
            r = m - 1;
          } else {
            found = true;
            break;
          }
        }
        out.println(found ? "YES" : "NO");
//      out.println("===");
      }
    }

  }

  static class InputReader {
    public BufferedReader reader;
    public StringTokenizer tokenizer;

    public InputReader(InputStream stream) {
      reader = new BufferedReader(new InputStreamReader(stream), 32768);
      tokenizer = null;
    }

    public String next() {
      while (tokenizer == null || !tokenizer.hasMoreTokens()) {
        try {
          tokenizer = new StringTokenizer(reader.readLine());
        } catch (IOException e) {
          throw new RuntimeException(e);
        }
      }
      return tokenizer.nextToken();
    }

    public int nextInt() {
      return Integer.parseInt(next());
    }

  }
}

