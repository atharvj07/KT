#include <bits/stdc++.h>
using namespace std;
long long k, b, n, t, c, kol;
int main() {
  cin >> k >> b >> n >> t;
  c = 1;
  kol = 0;
  while (t >= c) {
    kol++;
    c = c * k + b;
  }
  cout << max(n - kol + 1, (long long)0);
  cin >> k;
  return 0;
}
