from math import gcd

def coprime(a, b):
	return gcd(a, b) == 1
N = int(input())
ans = [1]
count = 1
mod = 1
for x in range(2, N):
	if coprime(x, N):
		ans += (x,)
		count += 1
		mod = mod * x % N
if mod != 1:
	del ans[-1]
	count -= 1
print(count)
print(' '.join(map(str, ans)))
