from math import gcd
for i in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	k = 0
	b = min(a)
	for i in range(n):
		k = gcd(k, a[i] - b)
	print(-1) if len(set(a)) == 1 else print(k)
