for _ in range(int(input())):
	(r, g, b, w) = map(int, input().split())
	mn = min(r, g, b)
	w += mn
	r -= mn
	g -= mn
	b -= mn
	ls = [r, g, b, w]
	odd = 0
	for i in ls:
		if i % 2:
			odd += 1
	if odd <= 1 or (mn > 0 and odd == 3):
		print('YES')
	else:
		print('NO')
