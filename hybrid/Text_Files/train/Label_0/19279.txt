#include <bits/stdc++.h>
using namespace std;
int main() {
  int a, b, m, r0;
  cin >> a >> b >> m >> r0;
  int flag[100000] = {0};
  for (int i = 0;; ++i) {
    r0 = (a * r0 + b) % m;
    if (flag[r0]) {
      cout << i - flag[r0] << endl;
      break;
    }
    flag[r0] = i;
  }
  return 0;
}
