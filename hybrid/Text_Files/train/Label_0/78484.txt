(n, m, k) = map(int, input().split())
if k < n:
	print(str(k + 1) + ' 1')
else:
	k -= n
	y = n - k // (m - 1)
	delta = k % (m - 1)
	if y % 2 == 0:
		x = 2 + delta
	else:
		x = m - delta
	(x, y) = (y, x)
	print(str(x) + ' ' + str(y))
