for _ in range(int(input())):
	(n, x) = [int(a) for a in input().split()]
	progs = [int(a) for a in input().split()]
	progs.sort()
	progs = reversed(progs)
	ans = 0
	tsize = 0
	for p in progs:
		tsize += 1
		score = tsize * p
		if score >= x:
			tsize = 0
			ans += 1
	print(ans)
