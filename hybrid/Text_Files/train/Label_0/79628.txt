def split_digits(n):
	if n % 2 == 0:
		return [2] * (n // 2)
	elif n % 3 == 0:
		return [3] * (n // 3)
	elif n % 5 == 5:
		return [5] * (n // 5)
	else:
		return [1] * n
n = int(input())
digits = split_digits(n)
print(len(digits))
print(' '.join(map(str, digits)))
