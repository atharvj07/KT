#include <iostream>
long long gcd(long long m,long long n){
  if (n==0)return m;
  else{
    return gcd(n,m%n);
  }
  return 0;
}
int main(){
  long long a,b,c,d,tmp,g;
  std::cin >> a>>b>>c>>d;
  --a;
  g=c*d/gcd(c,d);
  tmp=b/c+b/d-b/g-a/c-a/d+a/g;
  std::cout << b-a-tmp << '\n';
}
