import re
evolutions = ['vaporeon', 'jolteon', 'flareon', 'espeon', 'umbreon', 'leafeon', 'glaceon', 'sylveon']
inplen = int(input())
inp = input().strip()
for evolution in evolutions:
	if re.match(inp, evolution) and inplen == len(evolution):
		print(evolution)
		break
