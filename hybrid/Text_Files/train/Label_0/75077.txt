import math
t = int(input())
cnt = 0
for cnt in range(t):
	(a, b) = map(int, input().split())
	c = max(a, b)
	d = min(a, b)
	ans = 0
	if c >= 2 * d:
		print(d)
	else:
		x = math.ceil((2 * d - c) / 3)
		d -= x * 2
		ans = x + d
		print(ans)
