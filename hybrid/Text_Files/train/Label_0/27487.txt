#include <bits/stdc++.h>
using namespace std;
template <typename T, typename U>
inline void chmin(T &x, U y) {
  if (y < x) x = y;
}
const long long oo = 1e9 + 7;
int a;
int main() {
  cin >> a;
  if (a == 1) {
    cout << 1 << " " << 1 << endl;
    cout << 1 << endl;
    return 0;
  } else {
    cout << (a - 1) * 2 << " " << 2 << endl;
    cout << 1 << " " << 2 << endl;
    return 0;
  }
}
