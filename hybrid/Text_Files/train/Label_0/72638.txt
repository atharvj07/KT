import sys
import math
import bisect

def main():
	(s, t) = map(int, input().split())
	(n, d) = map(int, input().split())
	A = [0] * n
	A[0] = s
	A[n - 1] = t
	for i in range(n - 1):
		A[i] = min(A[0] + d * i, A[n - 1] + (n - 1 - i) * d)
	print(sum(A))
main()
