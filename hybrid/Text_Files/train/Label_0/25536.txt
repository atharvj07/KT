#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a;
    cin>>a;
    if(int(a.find('C')) !=-1 && int(a.rfind('F')) !=-1 && a.find('C') < a.rfind('F'))
        cout<<"Yes";
    else
        cout<<"No";
}