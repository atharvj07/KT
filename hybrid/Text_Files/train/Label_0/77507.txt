(n, k) = map(int, input().split())
s = list(input())
for i in range(n):
	if s[i] < 'm':
		a = min(k, ord('z') - ord(s[i]))
		s[i] = chr(ord(s[i]) + a)
		k -= a
	else:
		a = min(k, ord(s[i]) - ord('a'))
		s[i] = chr(ord(s[i]) - a)
		k -= a
if k:
	print('-1')
else:
	print(''.join(s))
