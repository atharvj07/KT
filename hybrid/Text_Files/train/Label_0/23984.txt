#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> groups(n);
  for (int i = 0; i < n; i++) cin >> groups[i];
  sort(groups.begin(), groups.end());
  cout << groups[n / 2] << endl;
}
