#include<cstdio>
#include<algorithm>
using namespace std;
int main(void)
{
	int w,h,c,x,y,i,g,a,b;
	scanf("%d %d %d",&w,&h,&c);
	x=w;	y=h;
	if(x<y)	swap(x,y);
	while(1)	{
		a=x%y;
		if(a==0)	break;
		x=y;	y=a;
	}
	b=(w/y)*(h/y)*c;
	printf("%d\n",b);
	return 0;
}
