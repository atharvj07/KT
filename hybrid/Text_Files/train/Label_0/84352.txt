import sys
n = int(input())
arr = list(map(int, input().split()))
arr.sort(reverse=True)
for idx in range(1, n):
	if arr[idx - 1] <= arr[idx]:
		arr[idx] = arr[idx - 1] - 1
		if arr[idx] == 0:
			print(sum(arr[:idx]))
			sys.exit()
print(sum(arr))
