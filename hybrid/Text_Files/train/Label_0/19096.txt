#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, ans = 0, sum = 0, a, i;
  cin >> n >> m;
  for (i = 0; i < n; i++) {
    cin >> a;
    sum += a;
    if (sum > m) {
      ans++;
      sum = a;
    }
  }
  cout << ans + (sum > 0);
  return 0;
}
