(n, k) = map(int, input().split())
a = list(map(int, input().split()))
if k == 1:
	ans = a[0]
	for i in a:
		ans = min(ans, i)
	print(ans)
elif k == 2:
	print(max(a[0], a[-1]))
else:
	ans = a[0]
	for i in a:
		ans = max(ans, i)
	print(ans)
