#include <bits/stdc++.h>
int a, b, ans;
int main() {
  scanf("%d %d", &a, &b);
  int t = a / b + 1;
  if (t <= 1) {
    printf("-1");
    return 0;
  }
  if (t % 2) t--;
  printf("%.12lf", (a + b) / (double)t);
  return 0;
}
