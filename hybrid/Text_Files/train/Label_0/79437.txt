import math

def main():
	(a, b, c, d) = map(int, input().split())
	sleep = c - d
	if b >= a:
		print(b)
		return
	if sleep <= 0 and b < a:
		print(-1)
		return
	ans = math.ceil((a - b) / sleep)
	final = b + c * int(ans)
	print(final)
t = int(input())
while t:
	main()
	t -= 1
