t = int(input())
for _ in range(t):
	n = int(input())
	(xs, ys) = ([], [])
	for _ in range(2 * n):
		(x, y) = map(int, input().split())
		if x == 0:
			ys.append(y * y)
		else:
			xs.append(x * x)
	xs.sort()
	ys.sort()
	dist = 0
	for (a, b) in zip(xs, ys):
		dist += (a + b) ** 0.5
	print(dist)
