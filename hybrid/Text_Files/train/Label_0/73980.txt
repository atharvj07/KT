def main():
	n = int(input())
	b = list(map(int, input().split()))
	arr = [0] * n
	(l, r) = (0, b[0])
	for i in range(n // 2):
		newl = max(l, b[i] - r)
		newr = b[i] - newl
		(l, r) = (newl, newr)
		(arr[i], arr[n - i - 1]) = (l, r)
	print(' '.join(map(str, arr)))
main()
