#include <bits/stdc++.h>
using namespace std;
int n, a;
double b;
int main() {
  scanf("%d%d", &n, &a);
  b = round(a * n / 180.0);
  if (b < 1) b = 1;
  if (b > n - 2) b = n - 2;
  printf("1 2 %d\n", (int)(n + 1 - b));
  return 0;
}
