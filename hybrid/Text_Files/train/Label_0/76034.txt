from math import ceil
for _ in range(int(input())):
	(n, k) = map(int, input().split())
	p = [*map(int, input().split())]
	ans = 0
	m = n * k - ceil(n // 2) - 1
	added = 0
	for i in range(m, -1, -ceil(n // 2) - 1):
		if added == k:
			break
		ans += p[i]
		added += 1
	print(ans)
