import java.util.*;
import java.awt.*;

public class C
{
	public static void main(String args[])
	{
		Scanner scan = new Scanner(System.in);
		
		int m = scan.nextInt();
		int n = scan.nextInt();
		int r1 = scan.nextInt();
		int c1 = scan.nextInt();
		int r2 = scan.nextInt();
		int c2 = scan.nextInt();
		int dr = Math.abs(r1 - r2);
		int dc = Math.abs(c1 - c2);

		int min = Math.min(dr, dc);
		int max = Math.max(dr, dc);

		if(min == 0 && max <= 4) System.out.println("First");
		else if(min == 1 && max <= 4) System.out.println("First");
		else if(min == 2 && max <= 4) System.out.println("First");
		else if(min == 3 && max <= 3) System.out.println("First");
		else System.out.println("Second");
	}
}