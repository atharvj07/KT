from math import gcd

def lcm(x, y):
	return x * y // gcd(x, y)
(n, a, b, p, q) = map(int, input().split())
cnt_a = n // a
cnt_b = n // b
cnt_ab = n // lcm(a, b)
if p >= q:
	cnt_b -= cnt_ab
else:
	cnt_a -= cnt_ab
print(cnt_a * p + cnt_b * q)
