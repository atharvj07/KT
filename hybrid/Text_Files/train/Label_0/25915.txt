// B - Varied
#include <bits/stdc++.h>
using namespace std;

int main(){
	string s; cin>>s;
	vector<int> c(26);
	for(int i=0; i<s.size(); ++i){
		if(c[s[i]-'a'] == 0) c[s[i]-'a']++;
		else{
			puts("no");
			return 0;
		}
	}
	puts("yes");
}