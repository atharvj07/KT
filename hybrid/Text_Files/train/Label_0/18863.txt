#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
const int N=500+10;
char a[N][N];

int main()
{
	int n,m;scanf("%d %d",&n,&m);
	for(int i=1;i<=n;++i) scanf("%s",a[i]+1);
	for(int i=1;i<=n;++i)
	{
		for(int j=1;j<=m;++j)
			printf("%c",(((i&1)&&j<m)||(!(i&1)&&j==1)||a[i][j]=='#')?'#':'.'); 
		puts("");
	};
	puts("");
	for(int i=1;i<=n;++i)
	{
		for(int j=1;j<=m;++j)
			printf("%c",(((i&1)&&j==m)||(!(i&1)&&j>1)||a[i][j]=='#')?'#':'.'); 
		puts("");
	};
	return 0;
}