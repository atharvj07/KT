t = int(input())
for i in range(t):
	(n, m) = map(int, input().split())
	median = (2 ** m - 1 - n) // 2
	a = [int(input(), 2) for i in range(n)]
	for i in sorted(a):
		if i <= median:
			median += 1
	y = bin(median)[2:]
	print('0' * (m - len(y)) + y)
