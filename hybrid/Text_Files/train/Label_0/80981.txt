for t in range(int(input())):
	(n, c0, c1, h) = map(int, input().split())
	s = input()
	ze = s.count('0')
	on = s.count('1')
	noch = ze * c0 + on * c1
	ch1 = ze * h + n * c1
	ch2 = on * h + n * c0
	print(min(noch, ch1, ch2))
