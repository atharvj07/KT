import sys
input = sys.stdin.readline

def solve():
	(x, y) = map(int, input().split())
	if (x + y) % 2 == 1:
		return [-1, -1]
	if (x + y) // 2 > x:
		return [x, (x + y) // 2 - x]
	return [(x + y) // 2, 0]
for _ in range(int(input())):
	print(*solve())
