import java.util.Scanner;

public class C {
	int prime[];
	int parent[];
	
	int get(int a)
	{
		if(parent[a] == a) return a;
		return parent[a] = get(parent[a]);
	}
	void union(int a, int b)
	{
		parent[get(a)] = get(b);
	}
	
	void init(int n)
	{
		prime = new int[n + 5];
		parent = new int[n + 2];
		for(int i = 1; i <= n; i++) parent[i] = i;
		for(int i = 2; i < n + 5; i++)
			if(prime[i] == 0)
				for(int j = i * 2; j < n + 5; j += i )
					prime[j] = 1;
//		for(int i = 0; i < n; i++)
//			if(prime[i] == 0)
//				System.out.println("Prime: " + i);
	}

	
	void run()
	{
		Scanner in = new Scanner(System.in);
		String line = in.nextLine();
		int size = line.length();
		init(size);

		int fr[] = new int[26];
		for(int i = 0; i < size; i++)
			fr[line.charAt(i) - 'a']++;
		int count = 1;
		for(int i = size / 2 + 1; i <= size;i++)
			if(prime[i] == 0)
				count++;
		int max = -1;
		for(int i = 0; i < 26; i++)
		{
			if(fr[i] == 0) continue;
			if(max == -1 || fr[max] < fr[i])
				max = i;
		}
		if(max == -1) max = 0;
		if(fr[max] < (size - count)) { System.out.println("NO"); return; }
		System.out.println("YES");
		String wr = "";
		char result[] = new char[size];
		for(int i = 0; i < size; i++)
		{
			if(line.charAt(i) != (max + 'a'))
				wr += line.charAt(i);
			result[i] = (char)(max + 'a');
		}
		int wr_i = 0;
		for(int i = size / 2 + 1; i <= size && wr_i < wr.length(); i++)
			if(prime[i] == 0)
				result[i-1] = wr.charAt(wr_i++);
/*
xxxyyyy			
 */
		if(wr_i < wr.length())
			result[0] = wr.charAt(wr_i++);
		String res = new String(result);
		System.out.println(res);
//		for(int i = 2; i <= size;i++)
//			if(prime[i] == 0)
//				for(int j = i + i; j <= size; j += i)
//					union(i,j);
//				
//		HashSet<Integer> set = new HashSet<Integer>();
//		for(int i = 1; i <= size; i++)
//		{
//			if(!set.contains(get(i)))
//				System.out.println(i);
//			set.add(get(i));
////			System.out.println("i = " + i + ", p = " + get(i));
//		}
//		System.out.println(set.size());
		
		
	}
	
	public static void main(String[] args) {
		C a = new C();
		a.run();		
	}

}
