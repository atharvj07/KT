from math import ceil
(k, d, t) = [int(x) for x in input().split()]
n = ceil(k / d) * d
diff = n - k
new_k = k + diff / 2
res = t // new_k * n
if t % new_k <= k:
	res += t % new_k
else:
	res += k + (t % new_k - k) * 2
print(res)
