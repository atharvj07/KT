n = int(input())
seq = [int(x) for x in input().split()]
high = int(n * max(seq))
low = max(seq)
seq_sum = sum(seq)
while low < high:
	mid = (low + high) // 2
	if mid * n - seq_sum >= mid:
		high = mid
	else:
		low = mid + 1
print(low)
