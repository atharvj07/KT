//package CFJULY26;


import java.io.*;
import java.util.*;

public class Q3{

    static class InputReader {
        public BufferedReader reader;
        public StringTokenizer tokenizer;

        public InputReader() {
            reader = new BufferedReader(new InputStreamReader(System.in), 32768);
            tokenizer = null;
        }

        public InputReader(InputStream stream) {
            reader = new BufferedReader(new InputStreamReader(System.in), 32768);
            tokenizer = null;
        }

        public String next() {
            while (tokenizer == null || !tokenizer.hasMoreTokens()) {
                try {
                    tokenizer = new StringTokenizer(reader.readLine());
                } catch (IOException e) {
                    throw new RuntimeException(e);
                }
            }
            return tokenizer.nextToken();
        }

        public char nextChar() {
            return next().charAt(0);
        }

        public int nextInt() {
            return Integer.parseInt(next());
        }

        public long nextLong() {
            return Long.parseLong(next());
        }

        public double nextDouble() {
            return Double.parseDouble(next());
        }

        public int[] nextIntArr(int n) {
            int[] arr = new int[n];
            for (int i = 0; i < n; i++) {
                arr[i] = this.nextInt();
            }
            return arr;
        }

        public Integer[] nextIntegerArr(int n) {
            Integer[] arr = new Integer[n];
            for (int i = 0; i < n; i++) {
                arr[i] = new Integer(this.nextInt());
            }
            return arr;
        }

        public int[][] next2DIntArr(int n, int m) {
            int[][] arr = new int[n][m];
            for (int i = 0; i < n; i++) {
                for (int j = 0; j < m; j++) {
                    arr[i][j] = this.nextInt();
                }
            }
            return arr;
        }

        public int[] nextSortedIntArr(int n) {
            int[] arr = new int[n];
            for (int i = 0; i < n; i++) {
                arr[i] = this.nextInt();
            }
            Arrays.sort(arr);
            return arr;
        }

        public long[] nextLongArr(int n) {
            long[] arr = new long[n];
            for (int i = 0; i < n; i++) {
                arr[i] = this.nextLong();
            }
            return arr;
        }

        public char[] nextCharArr(int n) {
            char[] arr = new char[n];
            for (int i = 0; i < n; i++) {
                arr[i] = this.nextChar();
            }
            return arr;
        }
    }

    public static void main(String[] args) {

        InputReader in = new InputReader();
        int N = in.nextInt();

        int TakeOFF[]=new int[N];
        int Landed[]=new int[N];

        int k=in.nextInt();
        double FinalAns=-1;

        for (int i = 0; i < N; i++)
            TakeOFF[i]=in.nextInt();

        for (int i = 0; i < N; i++)
            Landed[i]=in.nextInt();


        double check=999999999+1,neg;

//
//        for(int i=0;i<N;i++){
//
//            if(i==0){
//                neg=check/TakeOFF[i];
//                check=check-neg;
//                continue;
//            }
//            neg=check/Landed[i];
//            check=check-neg;
//
//            neg=check/TakeOFF[i];
//            check=check-neg;
//        }
//        neg=check/Landed[0];
//        check=check-neg;
//
//        if(check+Math.pow(10,-6)<k) {
//            System.out.println(-1);
//            return;
//        }
//
//


        double L=1,R=999999999+10,mid,fuel;

        while(L<R  && (Math.abs(L-R)>=(int)Math.pow(10,-6))){

            mid=(L+R)/2;

            fuel=mid;
            check=fuel+k;


            for(int i=0;i<N;i++){

                if(i==0){
                  neg=check/TakeOFF[i];
                  check=check-neg;
                  continue;
                }

                if(check<k)
                    break;

                neg=check/Landed[i];
                check=check-neg;

                neg=check/TakeOFF[i];
                check=check-neg;

                if(check<k)
                    break;


            }
            neg=check/Landed[0];
            check=check-neg;

         //   System.out.println(check+"   "+N+"  "+fuel);

//            Double d = check;
//            d = Double.parseDouble(String.format("%.6f", d));  // can be required precision

//            Double d1 = fuel;
//            d1 = Double.parseDouble(String.format("%.6f", d1));  // can be required precision


            if(check>=k){
                FinalAns=fuel;
                R=fuel-Math.pow(10,-6);
            }else{
                L=fuel+Math.pow(10,-6);
            }
        }







        System.out.println(FinalAns);


    }


}






//        HashMap<Integer,Integer> map=new HashMap<>();
//        if(map.containsKey(input[i]))
//            map.put(input[i],map.get(input[i])+1);
//        else
//            map.put(input[i],1);
//
//        for(Map.Entry<Integer,Integer> Test: map.entrySet()){
//            int Key=Test.getKey();
//            int val=Test.getValue();
//        }
