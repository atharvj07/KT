t = int(input())
for _ in range(t):
	(n, x) = map(int, input().split())
	a = list(map(int, input().split()))
	a = list(sorted(a, reverse=True))
	m = 0
	max_sum = 0
	for a_i in a:
		if (max_sum + a_i) / (m + 1) >= x:
			max_sum = max_sum + a_i
			m = m + 1
	print(m)
