#include <bits/stdc++.h>

using namespace std;

int main() {
    long long s; cin >> s;
    int x = (int(1e9) - s % int(1e9)) % int(1e9);
    int y = (s + x) / int(1e9);
    cout << "0 0 1000000000 1 " << x << " " << y << "\n";
    return 0;
}