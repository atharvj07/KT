t = int(input())
for _ in range(t):
	(n, now) = map(int, input().split())
	a = list(map(int, input().split()))
	ans = a[0]
	for i in range(1, n):
		tmp = min(now // i, a[i])
		ans += tmp
		now -= tmp * i
	print(ans)
