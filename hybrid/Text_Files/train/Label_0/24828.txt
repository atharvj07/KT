#include <bits/stdc++.h>
using namespace std;
using ii = pair<int, int>;
using ll = long long;
int32_t main() {
  ios::sync_with_stdio(0);
  int t;
  cin >> t;
  while (t--) {
    ll n;
    cin >> n;
    cout << ll(n / 2) << endl;
  }
}
