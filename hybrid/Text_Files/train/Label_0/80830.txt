t = int(input())
for t in range(t):
	p = int(input())
	lst = list(map(int, input().split()))
	q = max1 = 0
	for i in range(1, p):
		if lst[i - 1] == lst[i]:
			q += 1
		else:
			max1 = max(max1, q)
			q = 0
	max1 = max(max1, q)
	print(max1 + 1)
