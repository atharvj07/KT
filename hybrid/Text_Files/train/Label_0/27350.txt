#include <bits/stdc++.h>
using namespace std;
#define REP(i,n) for(int(i)=0;i<(n);i++)

int main(){
  int n,ans=0,b,c;
  cin>>n>>b;
  c=b;
  ans+=b;
  
  REP(i,n-2){
    cin>>c;
    ans+=min(b,c);
    b=c;
  }
  ans+=c;
  
  cout<<ans<<endl;
}