from collections import defaultdict
def dfs(G, idx, p):
    ans.append(idx)
    vis[idx] = True
    for j in G[idx]:
        if not vis[j]:
            dfs(G, j, idx)

for _ in range(int(input())):
    n = int(input())
    G = defaultdict(list)
    for i in range(n-1):
        a, b = map(int, input().split())
        G[a].append(b)
        G[b].append(a)
    vis = [False] * (n+1)
    ans = []
    dfs(G, 1, 1)
    print(*ans)
