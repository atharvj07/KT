#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  string s;
  cin >> n >> s;
  int cnt = 0;
  for (int i = 0; i < n; i++) {
    cnt += s[i] == '8';
  }
  if (cnt > 0 && n - cnt >= 10) {
    cout << cnt + (n - cnt) / 10;
  } else {
    cout << 0;
  }
  return 0;
}
