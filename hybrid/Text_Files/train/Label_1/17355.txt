#include <iostream>

bool isEven(long long num) {
    return num & 1;
}

int main() {
    long long n;
    std::cin >> n;
    std::cout << (isEven(n) ? "black" : "white") << std::endl;
    return 0;
}
