#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int mod = 1e9 + 7;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, a, b;
    cin >> n >> a >> b;
    if (a + b == n - 1) {
      for (int i = 1; i <= a; i++) { cout << i + 1 << " " << i << " "; }
      cout << n << endl;
    } else {
      cout << "-1\n";
    }
  }
  return 0;
}
