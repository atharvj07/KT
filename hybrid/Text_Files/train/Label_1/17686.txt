#include <bits/stdc++.h>
using namespace std;
#define int long long
int n,a[200005];
int32_t main(){
    cin>>n;
    for(int i=0;i<n;i++) cin>>a[i];
    int mn=*min_element(a,a+n);
    int ans=0;
    for(int i=0;i<n;i++) ans+=a[i]-mn;
    cout<<ans<<endl;
    return 0;
}
