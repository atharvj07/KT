import java.util.*;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int m = sc.nextInt();
        String s = sc.next();
        String t = sc.next();
        if (s.length() % n != 0 || s.length() % m != 0) {
            System.out.println(-1);
            return;
        }
        int l = s.length() / n;
        int r = s.length() / m;
        if (r * m == s.length() && l * n == s.length()) {
            System.out.println(l * n);
        } else {
            System.out.println(-1);
        }
    }
}
