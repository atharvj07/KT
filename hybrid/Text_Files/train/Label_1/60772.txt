import math

for _ in range(int(input())):
    n = list(input())
    if n == ['1']:
        print(0)
        continue

    add = int(math.log2(int(''.join(n))))
    print(-add - (len(n) - 1) + (1 if ''.join(n) == '1' else 2))
