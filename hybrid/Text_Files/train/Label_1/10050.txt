#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> v(n);
  for (int i = 0; i < n; i++) {
    cin >> v[i];
  }
  if (v[n - 1] == 0)
    cout << "UP";
  else if (v[n - 1] == 15)
    cout << "DOWN";
  else
    cout << -1;
  return 0;
}
