#include <iostream>

using namespace std;

int main(int argc, char *argv[]) {
  int l, r, x, y, k;
  cin >> l >> r >> x >> y >> k;
  if (k * x == y * l)
    cout << "YES";
  else
    cout << "NO";
  return 0;
}
