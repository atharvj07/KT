#include <iostream>
using namespace std;
int main() {
  int n, m;
  cin >> n >> m;
  for (int i = n; i < m; i += n) cout << i << ' ';
  for (int i = 1; i <= n; i++) cout << i << ' ';
  for (int i = n + 1; i < m; i += n) cout << i << ' ';
  return 0;
}
