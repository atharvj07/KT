from sys import stdin
from array import array

# Read input
t = int(stdin.readline())
for _ in range(t):
    b = array('i', map(int, stdin.readline().split()))
    a = [b[6] - b[3], b[0], b[3] - b[0]]
    print(*a)
