from math import gcd

for _ in range(int(input())):
    x, p, k = map(int, input().split())
    ans = x
    cnt = 0
    while cnt < k:
        ans += 1
        if gcd(ans, p) == 1:
            cnt += 1
    print(ans)
