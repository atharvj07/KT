#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

int main() {
    string a, b;
    cin >> a >> b;
    string ans = "";
    ans += min(a, b);
    ans += max(a, b);
    cout << ans << endl;
}
