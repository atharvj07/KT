import sys
input = sys.stdin.readline

n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans = []
for i in range(k):
    if all(ai % k == i for ai in a):
        ans.append(i)
print(len(ans))
print(*ans)
