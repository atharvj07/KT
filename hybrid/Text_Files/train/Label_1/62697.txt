def is_square(a1, b1, a2, b2):
  s = sorted([a1, b1, a2, b2])
  return s[0] == s[1] and s[2] == s[3]

for i in range(int(input())):
  a1, b1 = map(int, input().split())
  a2, b2 = map(int, input().split())
  print("Yes" if is_square(a1, b1, a2, b2) else "No")
