#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> x(n);
  for (int& v : x) cin >> v;
  int m = 0;
  sort(x.begin(), x.end());
  for (int i = 1; i < n; i++) {
    int dif = x[i] - x[i - 1];
    m = max(m, dif / 2 - 1);
  }
  cout << m << endl;
}
