#include <bits/stdc++.h>
using namespace std;
int n, x[1001], y[1001];
int main() {
  cin >> n;
  for (int i = 1; i <= n; i++) cin >> x[i] >> y[i];
  sort(x + 1, x + n + 1);
  sort(y + 1, y + n + 1);
  cout << max(x[n] - x[1], y[n] - y[1]) * max(x[n] - x[1], y[n] - y[1]);
}
