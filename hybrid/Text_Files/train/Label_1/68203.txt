import sys
input = lambda: sys.stdin.readline().strip()
n,k=map(int,input().split())
a=[int(x) for x in input().split()]
a.sort()
ans=0
while k<n:
    if a[k]!=a[k-1]:
        ans=max(0,int(a[k]-1)/2)
        a[k]-=ans
        a.sort()
    k+=1
print(ans)
