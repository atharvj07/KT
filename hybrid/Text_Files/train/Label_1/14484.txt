#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int mx[100001] = {};
    while (n--) {
        int x;
        cin >> x;
        mx[x]++;
        mx[x + 1] += mx[x];
        mx[x - 1] += mx[x];
    }
    cout << *max_element(mx, mx + 100001) << "\n";
}
