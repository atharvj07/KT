#include<bits/stdc++.h>
using namespace std;
int main() {
    int t; cin >> t;
    while(t--) {
        int s; cin >> s;
        if(s == 1) cout << 1 << endl;
        else cout << s / 3 + (s % 3 > 0) << endl;
    }
    return 0;
}
