#include <cmath>
#include <iostream>
using namespace std;
int main() {
  long long int a, b;
  cin >> a >> b;
  int c = log2(a);
  int d = log2(b);
  cout << (d - c + (1 << c) == b + 1 ? 1 : 0) << '\n';
}
