#include <iostream>
int main() {
  int A[26], len = 0;
  for (int i = 0; i < 26; i++) { std::cin >> A[i]; }
  std::string s;
  std::cin >> s;
  for (char c : s) len += A[c - 'a'];
  std::cout << (len ? 0 : s.size() - 2) << "\n";
}
