import sys

input = sys.stdin.readlines()
n, m = map(int, input[0].split())
x = list(map(int, input[1].split()))
f = [0] * n
for i in range(n):
    for j in range(m - 1):
        f[i] += abs(x[j] - x[j + 1])
print(*f)
