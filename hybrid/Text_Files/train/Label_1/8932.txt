#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, s, x, sum = 0;
    cin >> n >> s;
    while (n--) {
        cin >> x;
        sum += x;
    }
    cout << (sum <= s ? "YES" : "NO");
}
