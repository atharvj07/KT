import sys

for _ in range(int(input())):
    n, x = map(int, input().split())
    w = list(map(int, input().split()))
    w.sort(reverse=True)
    if any(w[i] + sum(w[i+1:]) == x for i in range(n-1)):
        print("NO")
    else:
        print("YES", *w)
