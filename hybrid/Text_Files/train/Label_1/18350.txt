#include <iostream>
long long const MOD = 1e9+7;
int main() {
  long long l, r;
  std::cin >> l >> r;
  if(l%2==0) l++;
  long long ans = (1+(r%MOD-(l-1)%MOD)*MOD)%MOD*(r-l)/2%MOD;
  std::cout << ans << std::endl;
}
