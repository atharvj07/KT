#include <bits/stdc++.h>
#define Long long long
using namespace std;

int main() {
    Long p, y, last;
    cin >> p >> y;
    for (last = y; last > 0; last--) {
        if (last % p != 0) break;
    }
    cout << (last > 1 ? last : -1) << endl;
    return 0;
}
