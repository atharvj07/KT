#include <iostream>
#include <vector>
using namespace std;
int main() {
  string a, b;
  cin >> a >> b;
  int sa = 0, sb = 0;
  for (auto x : a) { sa += x - '0'; }
  for (auto x : b) { sb += x - '0'; }
  cout << ((sa ^ sb) % 2 == 0 ? "YES" : "NO") << '\n';
  return 0;
}
