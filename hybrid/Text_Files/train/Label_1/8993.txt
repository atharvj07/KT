#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long int p,x,ans,temp;
	cin>>p>>x;
	if(x>10) {cout<<"Impossible"; return 0;}
	if(x==1){cout<<"1"; return 0;}
	temp=1;
	while(p--)
	{
		ans=(ans*10)%x;
		ans=(ans+1)%x;
		temp=(temp*10)%x;
	}
	ans=(ans*temp)%x;
	cout<<ans;
}
