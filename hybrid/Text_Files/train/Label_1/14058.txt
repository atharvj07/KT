#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,ans=0; cin>>n;
    vector<int> a(n);
    for(auto &i : a) cin>>i;
    for(int i=0;i<n;++i){
        if(i+1<n && a[i]==a[i+1]) ++i, ++ans;
    }
    cout<<n-ans<<"\n";
    return 0;
}
