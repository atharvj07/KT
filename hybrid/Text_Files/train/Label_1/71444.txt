import math

x1, y1 = map(int, input().split())
n = int(input())
ans = math.inf
for i in range(n):
    x2, y2, v = map(int, input().split())
    ans = min(ans, math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2) / v)
print(ans)
