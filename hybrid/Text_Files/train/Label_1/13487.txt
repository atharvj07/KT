#include <bits/stdc++.h>

using namespace std;
#define ll long long

const int mod = 1e9 + 7;

int main() {
    ll n, m;
    cin >> n >> m;
    ll ans = (1LL << n) % mod;
    ans = (ans * (m % mod)) % mod;
    cout << ans << "\n";
    return 0;
}
