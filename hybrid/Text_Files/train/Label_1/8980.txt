#include <bits/stdc++.h>
using namespace std;
#define int long long
using pi = pair<int,int>;
#define pb push_back

const int MAXN = 3e4;
int t,n,x,ans[MAXN];

int32_t main(){
	cin >> t;
	while (t--){
		cin >> n;
		int c[3] = {0,0,0};
		for (int i = 0;i<n;i++){
			cin >> x; c[x%3]++;
		}
		sort(c,c+3);
		cout << c[2]-c[0] << "\n";
	}
}
