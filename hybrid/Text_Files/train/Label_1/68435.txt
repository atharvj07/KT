t = int(input().strip())
for _ in range(t):
    n = int(input().strip())
    notes = list(map(int, input().split()))
    seen = set()
    for note in notes:
        if note not in seen:
            seen.add(note)
        else:
            seen.add(note + 1)
    print(len(seen))
