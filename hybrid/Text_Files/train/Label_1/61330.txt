import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n, x = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    cnt = cur = 0
    for i in a:
        cur = max(cur, i)
        if cur * cnt >= x:
          cnt += 1
          cur = i
    print(cnt)
