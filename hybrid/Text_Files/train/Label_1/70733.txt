n, k = map(int, input().split())
s = input()

def find_level(s, k):
    count = [0] * 26
    for char in s:
        count[ord(char) - ord('a')] += 1
    
    level = 0
    for i in range(26):
        level += count[i] // k
    return level

print(find_level(s, k))
