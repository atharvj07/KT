#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int n, a[1000005], sum;
int main() {
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
    sum += a[i];
  }
  sum += 10;
  sum %= 4;
  if (sum == 0)
    cout << 0;
  else
    cout << 4 - sum;
}
