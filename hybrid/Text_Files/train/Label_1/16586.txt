#include <bits/stdc++.h>
using namespace std;
#define pii pair<int, int>
#define ll long long

int main() {
    int x1, y1, x2, y2;
    cin >> x1 >> y1;
    cin >> x2 >> y2;
    cout << abs(x2 - x1) * 2 + abs(y2 - y1) * 2 + 4 << endl;
    return 0;
}
