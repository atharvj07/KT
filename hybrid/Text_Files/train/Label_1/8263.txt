#include <iostream>
using namespace std;

int main() {
  int n;
  cin >> n;
  int r = n % 10;
  int t = n / 10 % 10;
  if (r < 5)
    n -= r;
  else if (r == 5 && t % 2 == 0)
    n -= r;
  else
    n = (n / 10 + 1) * 10;
  cout << n << endl;
  return 0;
}
