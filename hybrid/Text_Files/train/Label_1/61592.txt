import sys
n, p = list(map(int, input().split()))
h = [-1] * p
for i, x in enumerate(int(x) for x in sys.stdin.readlines()):
    y = x % p
    if h[y] != -1:
        print(i + 1)
        sys.exit()
    h[y] = x
print(-1)
