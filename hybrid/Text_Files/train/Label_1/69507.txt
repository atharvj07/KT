def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

def solve():
    t = int(input())
    for _ in range(t):
        p, q = map(int, input().split())
        ans = p
        while ans % q == 0:
            ans = ans // gcd(ans, q)
        print(ans)

solve()
