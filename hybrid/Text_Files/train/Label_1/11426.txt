#include <iostream>

int main() {
    long long n, k, x;
    std::cin >> n >> k;
    x = n;
    while (k--) {
        if (x % 10 > 0)
            x--;
        else
            x /= 10;
    }
    std::cout << x << std::endl;
    return 0;
}
