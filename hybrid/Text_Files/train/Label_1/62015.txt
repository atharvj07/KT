n = input()
ans = 0
for i in range(len(n)):
    for j in range(i+1, len(n)):
        newN = n[:]
        newN[i], newN[j] = newN[j], newN[i]
        if newN[0] != '0':
            ans += 1
print(ans + 1)
