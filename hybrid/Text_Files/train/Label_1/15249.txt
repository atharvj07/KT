#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    ll n,x=0;cin>>n;ll a[n];
    for(int i=0;i<n;i++){cin>>a[i];x=max(x,a[i]);}
    for(int i=0;i<n;i++)a[i]-=x;
    for(int i=0;i<n;i++)cout<<a[i]<<" ";
}
