#include <iostream>

int main () {
    int t;
    std::cin >> t;
    while (t--) {
        long long n, k;
        std::cin >> n >> k;
        if (n % k == 0) std::cout << k << "\n";
        else std::cout << (k + n % k) << "\n";
    }
    return 0;
}
