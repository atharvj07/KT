import sys

input = sys.stdin.readlines()
t = int(input.pop(0))
for _ in range(t):
    n, m, k = map(int, input.pop(0).split())
    h = list(map(int, input.pop(0).split()))
    if h[-1] >= max(h) - k or m > max(h) - min(h): print('YES')
    else: print('NO')
