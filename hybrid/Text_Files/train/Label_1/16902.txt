#include <iostream>
using namespace std;
int main() {
    int c, v0, v1, a, l;
    cin >> c >> v0 >> v1 >> a >> l;
    int pages = 0, days = 0;
    while (pages < c) {
        days++;
        pages += max(v0, min(v1, pages + a));
        pages -= l;
    }
    cout << days << endl;
    return 0;
}
