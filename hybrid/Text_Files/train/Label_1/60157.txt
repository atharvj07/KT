import sys
for _ in range(int(input())):
    x=int(input())
    ans=[]
    for d in range(9,1,-1):
        while x>=d and not d in ans:
            x-=d
            ans.append(d)
    if x==0 and len(set(ans))==len(ans): print(*ans,sep='')
    else:  print(-1)
