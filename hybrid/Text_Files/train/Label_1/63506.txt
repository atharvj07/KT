from sys import stdin

n, k = map(int, stdin.readline().split())

def hyper(n, r):
    r = min(r, n - r)
    res = 1
    for i in range(1, r + 1):
        res = (res * (n - i + 1)) // i
    return res

print(hyper(n - 1, k - 1))
