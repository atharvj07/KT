import sys

n = int(input())
a = [int(x) for x in input().split()]
b = [int(x) for x in input().split()]

diff = [abs(a.count(x) - b.count(x)) for x in range(1, 6)]
if sum(diff) % 2:
    print(-1)
else:
    print(sum(diff) // 2)
