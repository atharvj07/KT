n = int(input())
arr = list(map(int, input().split()))
arr.sort(reverse=True)

sum = 0
for num in arr:
    if (sum + num) % 2 == 0:
        sum += num
    elif num % 2 == 0:
        sum += num

print(sum)
