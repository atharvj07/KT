#include <bits/stdc++.h>
using namespace std;
int main() {
  int q;
  cin >> q;
  while (q--) {
    long long a, b, c;
    cin >> a >> b >> c;
    cout << max(0LL, abs(b - a) - 1) + max(0LL, abs(c - b) - 1)
         << endl;
  }
  return 0;
}
