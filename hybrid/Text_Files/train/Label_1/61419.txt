import math

n, r = map(int, input().split())

# Angle of the center of the star's triangle
theta = math.pi / n

# Area of one triangle
area_triangle = 0.5 * r**2 * math.sin(2 * theta)

# Total area of the star
area_star = n * area_triangle

print(area_star)
