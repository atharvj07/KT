#include <iostream>

using namespace std;

int main() {
  long long n;
  cin >> n;
  long long m = 1;
  while (m * (m + 2) < n) {
    m++;
  }
  long long k = (n - m * (m - 1)) / m;
  cout << k + m << endl;
  return 0;
}
