#include <iostream>
using namespace std;

int main()
{
    int x, y, z, t1, t2, t3;
    cin >> x >> y >> z >> t1 >> t2 >> t3;

    if (t1 * abs(y - x) <= t3 + t2 * abs(y - z) + t3)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;

    return 0;
}
