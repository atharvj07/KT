#include <iostream>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int r, g, b, w;
    cin >> r >> g >> b >> w;
    if (abs(r - b) > 1 || abs(r - g) > 1 || abs(r - w) > 1) {
      cout << "No" << endl;
    } else {
      cout << "Yes" << endl;
    }
  }
  return 0;
}
