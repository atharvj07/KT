#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin>>s;
    int n=s.length(),ans=0;
    for(int i=n-1;i>=0;i--){
        if(s[i]=='0') ans++;
        else{
            ans++;
            while(i>=0 && s[i]=='1') i--;
        }
    }
    cout<<ans<<endl;
}
