#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        long long r, g, b, w;
        cin >> r >> g >> b >> w;
        if ((r + b + g) % 2 == 1) cout << "No" << endl;
        else cout << "Yes" << endl;
    }
}
