#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,m,a,b,c[100001]={},d=0,e=0;
    cin >> n >> m;
    for(int i=0;i<n;i++){
        cin >> a >> b; c[a]+=b; if(b>0) d=1; e=max(e,c[a]);
    }
    cout << ((d==0 || e<=0)?0:e);
}
