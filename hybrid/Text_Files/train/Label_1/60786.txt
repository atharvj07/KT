['import sys\n\ninput = sys.stdin.readline\n\ndef solve(n, a):\n    ans = 0\n    for i in range(n):\n        for j in range(i + 1, n):\n            if (a[i] ^ a[j]) == 0:\n                ans += 1\n    return ans\n\nn = int(input())\na = list(map(int, input().split()))\nprint(solve(n, a))\n']