n = int(input().strip())
chat_list = []

for _ in range(n):
    friend = input().strip()
    if friend in chat_list:
        chat_list.remove(friend)
    chat_list.insert(0, friend)

for friend in chat_list:
    print(friend)
