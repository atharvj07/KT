def count_wows(string):
    count = 0
    cur = 0
    for ch in string:
        if ch == 'o':
            cur = 0
        else:
            cur += 1
            count += max(0, cur - 2)
    return count

string = input()
print(count_wows(string))
