#include <bits/stdc++.h>
using namespace std;

int main()
{
	int n, i, mxor = 0, xr = 0;
	cin >> n;
	int a[n + 1];
	for (i = 1; i <= n; i++)
	{
		cin >> a[i];
		xr ^= a[i];
		if (xr > mxor)
			mxor = xr;
		else
			xr = 0;
	}
	cout << mxor << '\n';
}
