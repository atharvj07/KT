import sys

def main():
    n = int(input())
    b = [int(input()) for _ in range(n)]
    ans = [1] * n
    for i in range(1, n):
        ans[i] = max(ans[i - 1], b[i] - ans[i - 1])
    for val in ans:
        print(val)

main()
