#include <algorithm>
#include <iostream>
using namespace std;

int main()
{
	int n;
	cin >> n;
	string sa, sb;
	cin >> sa >> sb;
	int cnt = 0;
	for (int i = 0; i < n; i++)
		if (sa[i] == '?' || sb[i] == '?')
			cnt++;
	cout << (long long)(1 << cnt) % 1000000007 << endl;
}
