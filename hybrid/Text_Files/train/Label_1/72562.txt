import sys
r=sys.stdin.readlines
t=int(r(1)[0])
for n in map(int,r(t+1)):
    i=1
    while i<n:
        for j in str(i):
            if int(j)!=0 and i%int(j)!=0: break
        else: break
        i+=1
    print(i)
