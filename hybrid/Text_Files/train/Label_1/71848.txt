import sys
input = sys.stdin.readline

t, = map(int,input().split())
for _ in range(t):
    n, = map(int,input().split())
    a = list(map(int,input().split()))
    d = {x:i for i,x in enumerate(a[1:])}
    if a[0]==a[1]:
        ans = 0
    elif a[0] in d:
        ans = max(a[d[a[0]]],a[0]) - a[0]
        if d[a[0]]==1:
            ans = -1
    else:
        ans = -1
    print(ans)
