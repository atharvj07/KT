from sys import stdin
input=stdin.readline

n=int(input())
a=[int(x) for x in input().split()]
ans=0
pref=[0]*n
for i in range(n): pref[i]=(pref[i-1] if i>0 else 0)+a[i]
s=[0]
Min=0
for x in pref:
    Min=min(Min,x)
    if x>=0: s.append(x)
    else: ans+=abs(Min)+1
    Min+=1
print(ans)
