#include <iostream>
using namespace std;
int main() {
  long long x;
  cin >> x;
  bool fl = false;
  for (int i = 1; i * i <= x; i++) {
    if (x % i == 0) {
      if (fl) cout << ' ';
      cout << i << ' ' << x / i;
      fl = true;
    }
  }
  cout << '\n';
  return 0;
}
