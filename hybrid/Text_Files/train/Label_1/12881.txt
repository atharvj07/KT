#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  int ans = -1;
  set<int> s;
  for (int i = 0; i < n; i++) {
    int x;
    cin >> x;
    if (s.count(x))
      ans = i + 1;
    s.insert(x);
  }
  cout << ans << endl;
}
