n, k = map(int, input().split())
ids = list(map(int, input().split()))

left, right = 1, n
while left < right:
    mid = (left + right) // 2
    if mid * (mid + 1) // 2 < k:
        left = mid + 1
    else:
        right = mid

pos = k - (left - 1) * left // 2
print(ids[pos - 1])
