import sys
n,m,k=map(int,input().split())
grid=[[0 for j in range(m+2)] for i in range(n+2)]
for p in range(k):
    x,y=map(int,input().split())
    grid[x][y]^=1
    if grid[x][y-1]+grid[x][y]+grid[x][y+1]+grid[x-1][y]+grid[x+1][y]>1:
        print(p+1)
        sys.exit(0)
print(0)
