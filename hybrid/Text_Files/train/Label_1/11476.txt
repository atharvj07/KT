#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a[100010];
    cin >> n;
    for (int i = 1; i <= n; ++i) cin >> a[i];
    for (int i = 2; i < n; ++i)
        if (a[i - 1] > a[i] && a[i] < a[i + 1])
            return cout << i << ' ' << i + 1, 0;
    return cout << -1, 0;
}
