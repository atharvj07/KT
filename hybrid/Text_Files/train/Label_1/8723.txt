#include <iostream>

int main() {
  int n;
  std::cin >> n;
  int a[2 * n], s[n + 1];
  for (int i = 1; i <= 2 * n; ++i) { std::cin >> a[i]; }
  int j = 1;
  for (int i = 1; i <= 2 * n; ++i) {
    ++s[a[i]];
    j += s[a[i]] & 1;
  }
  std::cout << j - 1 << "\n";
  return 0;
}
