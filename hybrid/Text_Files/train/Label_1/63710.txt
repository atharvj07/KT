for _ in range(int(input())):
    x, y, z = map(int, input().split())
    if x + y + z == max(x, y, z) * 2:
        print("YES")
        print(max(x, y, z), x + y + z - max(x, y, z), x + y + z - max(x, y, z))
    else:
        print("NO")
