#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int k;
    cin >> k;
    if (k % 3 == 0)
      k--;
    else if (k % 3 == 2)
      k -= 2;
    cout << k << "\n";
  }
  return 0;
}
