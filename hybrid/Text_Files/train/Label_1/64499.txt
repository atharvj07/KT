import sys

inputs = []

for line in sys.stdin:
    inputs.append([int(i) for i in line.strip().split()])

for l, v, a, b in inputs[1:]:
    if a <= v:
        print(0)
    else:
        print(l // v - (a - 1) // v - (((b - 2) // v) + (b % v == 1 and a % v > 1)))
