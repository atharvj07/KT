#include <bits/stdc++.h>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long l, r;
    std::cin >> l >> r;
    std::cout << (l % 2 == 0 || r % 2 == 0 ? "YES" : "NO") << "\n";
  }
  return 0;
}
