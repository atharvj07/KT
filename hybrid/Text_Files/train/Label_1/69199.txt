m, k = map(int, input().split())
d, s = map(list, zip(*[map(int, input().split()) for i in range(2)]))
cur, t = 0, 0
for i in range(m):
    need = max(0, d[i] - (s[i] + (t - cur) % k))
    t += need + d[i]
    cur = i + 1
print(t)
