#include<bits/stdc++.h>
typedef long long ll;
using namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll n, k;
    cin >> n >> k;
    ll ans = n / (k + 1);
    cout << ans << ' ' << ans * k << ' ' << n - ans * (k + 1) << '\n';
    return 0;
}
