#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b, c, n;
    cin >> a >> b >> c >> n;
    cout << ((n + a + b + c) % 3 == 0 ? "YES" : "NO") << endl;
  }
  return 0;
}
