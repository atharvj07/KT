from math import sqrt, pi
r, d = map(int, input().split())
n = int(input())
pieces = 0
for _ in range(n):
    x, y, r_i = map(int, input().split())
    if r**2 > (x - r_i)**2 + (y - r_i)**2 >= (r- d)**2:
        pieces += 1
print(pieces)
