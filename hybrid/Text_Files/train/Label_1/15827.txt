#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, m;
    cin >> n >> m;
    cout << (n / 5) * (m / 5) + ((n % 5 == 0) ? ((m % 5 != 0) ? (m / 5) : ((m / 5 - 1) / 5 + 1)) : ((m % 5 == 0) ? (n / 5) : 0));
    return 0;
}
