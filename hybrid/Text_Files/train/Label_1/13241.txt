#include <bits/stdc++.h>

using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long x;
    cin >> x;
    if (x % 2 == 1 || x == 0)
      cout << -1 << endl;
    else
      cout << 2 * x << " " << x << endl;
  }
  return 0;
}
