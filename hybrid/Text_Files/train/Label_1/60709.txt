import math

for _ in range(int(input())):
    k = int(input())
    arr = [int(x) for x in input().split()]
    sum = 0
    for x in arr:
        sum += x
    if sum == 0:
        print(-1)
    else:
        print(math.ceil(k / sum) * 7)
