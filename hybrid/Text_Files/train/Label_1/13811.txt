#include <iostream>
#include <cmath>
using namespace std;

int main() {
  int n, avg, sum = 0;
  cin >> n;
  for(int i = 0; i < n; i++) {
    int m;
    cin >> m;
    sum += m;
  }
  avg = (int)round(((double)sum / n) + 0.5);
  cout << max(0, 5 - avg) << endl;
  return 0;
}
