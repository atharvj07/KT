n = int(input())
claws = list(map(int, input().split()))

alive = 1  # The last person is always alive
max_reach = 0

for i in range(n-2, -1, -1):
    if i > max_reach:
        alive += 1
    max_reach = max(max_reach, i + claws[i])

print(alive)
