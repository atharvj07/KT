#include <iostream>
using namespace std;

int main() {
  int h1, h2, a, b;
  cin >> h1 >> h2 >> a >> b;
  int diff = h2 - h1, up = a - b, day = 0;
  if (up <= 0) { cout << -1; return 0; }
  while (h1 < h2) {
    h1 += up;
    day++;
    if (h1 < 0) h1 = 0;
  }
  cout << day;
  return 0;
}
