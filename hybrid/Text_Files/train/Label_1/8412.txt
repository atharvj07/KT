#include <iostream>

int main() {
  long long n;
  std::cin >> n;
  for (long long i = 1; i <= n; i++) {
    if (n % i == 0 && i <= n / i) {
      std::cout << i << " " << n / i << std::endl;
      break;
    }
  }
}
