from functools import reduce

for _ in range(int(input())):
    n, m = map(int, input().split())
    a = [list(map(int, input().split())) for _ in range(n)]
    for k in range(m):
        a[n-1][k] = max(a[n-1][k], max(a[i][k] for i in range(n)))
    print(sum(map(max, a)))
