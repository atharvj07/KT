#include <bits/stdc++.h>

using namespace std;

const int MAXN = 1e9;

int main() {
    long long n, x, y;
    cin >> n >> x >> y;
    long long ans = min(n, (MAXN + x + y - 2) / (x + y + 1));
    cout << ans << '\n';
    return 0;
}
