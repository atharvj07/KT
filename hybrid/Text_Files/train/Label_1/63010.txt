n = int(input())
a = list(map(int, input().split()))

black, white = 0, 0
for i in range(n):
    if i % 2 == 0:
        black += a[i] // 2
        white += (a[i] + 1) // 2
    else:
        black += (a[i] + 1) // 2
        white += a[i] // 2

print(min(black, white))
