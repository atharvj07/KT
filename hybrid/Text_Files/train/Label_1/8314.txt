#include <cmath>
#include <iomanip>
#include <iostream>
using namespace std;

int main() {
  long long n, r;
  cin >> n >> r;
  cout << fixed << setprecision(10) << 1.0 * n * r * r * sin(2 * M_PI / n) / 2 << endl;
  return 0;
}
