import sys
n,m,k=[int(x) for x in sys.stdin.readline().split()]
a=[int(x) for x in sys.stdin.readline().split()]
a.sort()
ans=0
for i in range(n):
    if m>k:
        ans+=a[i]*(m//k)*k
        m%=k
    if m>0:
        m-=1
        ans+=a[i]
print(ans)
