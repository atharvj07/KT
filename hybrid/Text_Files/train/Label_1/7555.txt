#include <iostream>
using namespace std;

int main() {
  int m, ans = 0;
  cin >> m;
  for (int i = 1; i <= 100000; ++i) {
    int cnt = 0;
    for (int j = 5; j <= i; j *= 5) cnt += i / j;
    if (cnt == m) ans++, cout << i << ' ';
  }
  cout << ans << endl;
  return 0;
}
