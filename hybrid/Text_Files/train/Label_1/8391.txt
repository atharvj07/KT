#include<bits/stdc++.h>
using namespace std;
int main() {
    int t; cin >> t;
    while (t--) {
        long long n, k; cin >> n >> k;
        cout << (n % (k + 1) == 0 ? "Bob" : "Alice") << '\n';
    }
    return 0;
}
