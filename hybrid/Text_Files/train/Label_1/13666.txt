#include <iostream>
using namespace std;
int main() {
  int n, b, p, c = 0;
  cin >> n >> b >> p;
  while (n > 1) {
    c += b * (n / 2) + n / 2;
    n /= 2;
  }
  cout << c << " " << n * p << '\n';
}
