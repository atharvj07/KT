#include <iostream>
using namespace std;

int main() {
  long long p, y;
  cin >> p >> y;
  for (int i = y; i >= 2; i--) {
    if (i % p != 0) {
      cout << i << endl;
      return 0;
    }
  }
  cout << -1 << endl;
  return 0;
}
