#include <bits/stdc++.h>

using namespace std;
typedef long long int ll;

int f(int n) {
  if (n < 4)
    return 0;
  if (n % 2 == 0)
    return (n - 2) / 2;
  else
    return n - 1;
}

int main() {
  int n;
  cin >> n;
  cout << f(n) << endl;
  return 0;
}
