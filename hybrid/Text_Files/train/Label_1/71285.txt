import sys

n = int(input())
a = list(map(int, input().split()))

if n >= 3 and max(a) > sum(a) - max(a):
    sys.stdout.write('-1')

else:
    b = list(a)
    b.sort(reverse=True)
    sys.stdout.write(' '.join(map(str, b)))
