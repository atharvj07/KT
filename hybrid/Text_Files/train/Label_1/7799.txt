#include <bits/stdc++.h>

int main() {
    long long t, x;
    std::cin >> t;
    while (t--) {
        std::cin >> x;
        std::cout << ((x % 2) && (x % 9 != 0) ? "NO" : "YES") << '\n';
    }
    return 0;
}
