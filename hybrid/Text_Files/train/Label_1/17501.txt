#include <iostream>
using namespace std;
int main() {
  long long n, m, a, cur = 0;
  cin >> n >> m;
  for (long long i = 0; i < n; i++) {
    cin >> a;
    cur += a;
    cout << cur / m - (cur - a) / m << " ";
  }
  cout << '\n';
  return 0;
}
