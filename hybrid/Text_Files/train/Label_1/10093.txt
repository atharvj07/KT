#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    set<int> s;
    for (int i = 0; i < n - 1; i++) {
        int a;
        cin >> a;
        s.insert(a);
    }
    for (int i = 1; i <= n; i++) {
        if (s.count(i) == 0) {
            cout << i << endl;
            return 0;
        }
    }
    return 0;
}
