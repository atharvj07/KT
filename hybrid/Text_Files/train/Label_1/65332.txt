import sys

for case in range(int(input())):
    n = int(input())
    nums = [0] * (n + 1)
    for i, num in enumerate(map(int, input().split())):
        nums[num] = i

    print(['NO', 'YES'][sorted(nums) == nums])
