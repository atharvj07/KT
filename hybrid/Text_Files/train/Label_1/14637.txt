#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, sum = 0, ans = 0;
  cin >> n >> m;
  for (int t, i = 0; i != n; ++i) {
    cin >> t;
    sum += t;
    if (sum > m) {
      sum = t;
      ans++;
    }
  }
  if (sum != 0)
    ans++;
  cout << ans << endl;
}
