import collections
for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    cnt = collections.Counter(a)
    mn = min(cnt[x] for x in cnt)
    print(sum(cnt[x] - mn for x in cnt))
