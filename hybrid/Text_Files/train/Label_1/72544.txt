from collections import Counter
for tc in range(int(input())):
    input()
    c = Counter(map(int,input().split()))
    ans='NO'
    for k,v in c.items():
        if v>=3 or v==2 and c[k+1]>=1 or v==2 and c[k-1]>=1:
            ans='YES'
            break
    print(ans)
