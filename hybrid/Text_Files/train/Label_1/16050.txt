#include <iostream>
#include <algorithm>

using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int cnt = 0, ans = 0;
  for (char c: s) {
    if (isupper(c)) {
      cnt = 0;
    } else {
      cnt++;
      ans = max(ans, cnt);
    }
  }
  cout << ans << endl;
}
