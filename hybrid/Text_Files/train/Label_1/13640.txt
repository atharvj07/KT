#include <iostream>

using namespace std;

int main() {
  int l, r, x, y;
  cin >> l >> r >> x >> y;
  if (y % x) cout << 0 << endl;
  else cout << 1ll * (r / y - (l - 1) / y) * (r / x - (l - 1) / x) << endl;
}
