#include <bits/stdc++.h>

using namespace std;

int n, k;

int main() {
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++)
            cout << abs(i - j) * ((k - i - j >= 0) - (k - i - j <= 0)) << " ";
        cout << endl;
    }
    return 0;
}
