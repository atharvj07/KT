#include <iostream>

int main() {
    int k;
    std::cin >> k;
    for (int d = 1; d <= 9; ++d) {
        if (k <= d * 9 * d) {
            std::cout << (k + d - 1) / d % 10 << "\n";
            return 0;
        }
        k -= d * 9 * d;
    }
    std::cout << 0 << "\n";
}
