#include <iostream>

using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  char c = 'a';
  for (int i = 0; i < n; i++) {
    cout << c;
    c++;
    if (c > 'z') c = 'a';
    if (c == 'a' + k - 1) c = 'b';
  }
  cout << endl;
  return 0;
}
