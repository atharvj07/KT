#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  int k[n], m[n][101];
  vector<pair<int, int>> v[n];
  for (int i = 0; i < n; i++) {
    cin >> k[i];
    int sum = 0, a;
    for (int j = 0; j < k[i]; j++) {
      cin >> m[i][j];
      sum += m[i][j];
      v[i].push_back({sum, j + 1});
    }
  }
  int ans = INT_MAX;
  for (int i = 0; i < n; i++) {
    ans = min(ans, v[i][k[i] - 1].first * 5 + (v[i][k[i] - 1].second) * 15);
  }
  cout << ans << "\n";
}
