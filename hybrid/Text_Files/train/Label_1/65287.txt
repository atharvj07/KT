import sys
input = sys.stdin.readline

n, a, b = map(int, input().split())
l = []
for _ in range(n):
    x, vx, vy = map(int, input().split())
    l.append((x, (vy - vx * a) / (vx * b - vy * a)))
    
l.sort()
count = 0
ans = 0
for x, m in l:
    ans += count * 2
    count += (m <= 0)
print(ans)
