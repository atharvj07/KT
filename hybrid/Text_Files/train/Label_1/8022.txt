#include <bits/stdc++.h>
using namespace std;
int main() {
  int x[5];
  for (int i = 0; i < 5; i++) cin >> x[i];
  int sum = 0;
  for (int i = 0; i < 5; i++) sum += x[i];
  if (sum & 1 || sum < 4)
    cout << -1;
  else
    cout << sum / 2;
  return 0;
}
