import sys

inp = list(map(int, input().split()))
p, k = inp[0], inp[1]
if p % k == 0:
    print(-1)
else:
    ans = [p % k]
    p //= k
    while p > 0:
        ans.append(p % k)
        p //= k
    ans.reverse()
    print(len(ans))
    print(*ans)
