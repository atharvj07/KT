#include <iostream>

using namespace std;

typedef long long ll;

const int mod = 1e9 + 7;

int main() {
  ll a, b;
  cin >> a >> b;
  if (b % a != 0) {
    ll s = 0;
    for (ll i = b; i <= a * b; i += b) s = (s + i) % mod;
    cout << s << '\n';
  } else
    cout << 0 << '\n';
  return 0;
}
