#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
  double x;
  cin >> x;
  int i = ceil((x * 10) / 100.0);
  int j = floor((x * 10 + 99.9) / 100.0);
  cout << i << " " << j << endl;
  return 0;
}
