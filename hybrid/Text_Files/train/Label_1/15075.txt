#include <iostream>
#include <string>
#include <set>
using namespace std;

int main() {
  string s;
  cin >> s;
  set<char> se;
  for (char c : s) se.insert(c);
  cout << (se.size() % 2 ? "No" : "Yes") << endl;
  return 0;
}
