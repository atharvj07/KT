#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 2e5 + 5, mod = 1e9 + 7;

int32_t main() {
  int n; cin >> n;
  vector<int> a(n);
  int sum = 0;
  for (int i = 0; i < n; i++) { cin >> a[i]; sum += a[i]; }
  int ans = 0;
  for (int i = 0; i < n; i++)
    ans += (sum - a[i]) * a[i];
  cout << ans / 2 << endl;
  return 0;
}
