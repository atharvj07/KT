#include <iostream>
#include <vector>

int main() {
  int n;
  std::cin >> n;
  std::vector<int> arr(n);
  for (int i = 0; i < n; i++) { std::cin >> arr[i]; }
  int alive = n;
  for (int i = 0; i < n; i++) {
    if (i - arr[i] >= 0) { alive--; }
  }
  std::cout << alive << std::endl;
  return 0;
}
