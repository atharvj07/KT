import sys

t = int(sys.stdin.readline())

for _ in range(t):
    b, p, f = map(int, sys.stdin.readline().split())
    h, c = map(int, sys.stdin.readline().split())
    print(min(b // 2, p) * h + min(b // 2, f) * c)
