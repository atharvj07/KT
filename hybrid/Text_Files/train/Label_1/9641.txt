#include <iostream>

using namespace std;

int main() {
  long long n, m, a, b;
  cin >> n >> m >> a >> b;
  long long pay;
  pay = m - n % m;
  if (pay == m)
    pay = 0;
  cout << pay * min(a, b) << endl;
  return 0;
}
