n = int(input().strip())
a = list(map(int, input().strip().split()))

def can_complete_wall(n, a):
    max_height = max(a)
    for i in range(n):
        if (max_height - a[i]) % 2 != 0:
            return "NO"
    return "YES"

print(can_complete_wall(n, a))
