#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int a, b;
    std::cin >> a >> b;
    if (a == 1 || b == 1 || (a <= b && b - a > 1))
      std::cout << "Finite" << std::endl;
    else
      std::cout << "Infinite" << std::endl;
  }
  return 0;
}
