#include<iostream>
#include<vector>
using std::vector;
int main(){
	int n;
	std::cin>>n;
	vector<int>a(n);
	for(auto&i:a)std::cin>>i;
	int ans=0;
	for(int i=0;i<n;++i){
		int xor_sum=0;
		for(int j=i;j<n;++j){
			xor_sum^=a[j];
			ans=std::max(ans,xor_sum);
		}
	}
	std::cout<<ans<<std::endl;
}
