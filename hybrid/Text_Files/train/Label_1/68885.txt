import sys
from math import sqrt

for _ in range(int(sys.stdin.readline())):
    n = int(sys.stdin.readline())
    print('YES' if n % 2 != 0 or any(n % i == 0 for i in range(3, int(sqrt(n)) + 1, 2)) else 'NO')
