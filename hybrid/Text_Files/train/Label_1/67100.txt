from collections import defaultdict

n, m = map(int, input().split())
ss = defaultdict(list)
for s in sorted(input() for _ in range(n)):
    ss[s[::-1]].append(s)

ans = ''
for s in ss.values():
    for v in s:
        ans += v
        s.reverse()

print(len(ans))
print(ans)
