from sys import stdin

t = int(stdin.readline())

for _ in range(t):
    a = sorted([int(x) for x in stdin.readline().split()])
    b = sorted([int(x) for x in stdin.readline().split()])

    print(sum(max(0, a[i] - b[i]) for i in range(3)))
