#include <iostream>
#include <algorithm>

using namespace std;

int main() {
  long long n, m;
  cin >> n >> m;
  if (m == 1) cout << 2 << endl;
  else if (m == n) cout << n - 1 << endl;
  else cout << m << endl;
  return 0;
}
