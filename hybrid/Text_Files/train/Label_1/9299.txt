#include <iostream>
#include <vector>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int s;
    cin >> s;
    vector<int> v;
    while (s >= 3) {
      v.push_back(3);
      s -= 3;
    }
    if (s > 0) v.push_back(s);
    cout << v.size() << endl;
  }
}
