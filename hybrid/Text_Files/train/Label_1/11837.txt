#include <bits/stdc++.h>

using namespace std;

int a, b, k, n, x, y;

int main() {
  cin >> n >> k >> a >> b;
  y = (k - a + b) / k;
  x = (k - min(a, b) + (n - 1) * k - max(a, b)) / k;
  cout << x << " " << y << endl;
  return 0;
}
