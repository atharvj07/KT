#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    ll n, m;
    cin >> n >> m;
    if (n < m)
        cout << 1;
    else
        cout << ((n + m - 1) / m);
    return 0;
}
