#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n - 1; i++) cin >> a[i];
    long long ans = 0;
    for (int i = 0; i < n - 1; i++) { ans += (a[i] - 1 - i); }
    cout << ans << endl;
    return 0;
}
