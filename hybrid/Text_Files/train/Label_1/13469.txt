#include<bits/stdc++.h>
using namespace std;
string solve(string a, int k)
{
	int n=a.size();
	for(int i=0; i<n-1 && k; i++)
	{
		if(a[i]<a[i+1])
		{
			swap(a[i], a[i+1]);
			k--;
		}
	}
	return a;
}
int main()
{
	string a;
	int k;
	cin>>a>>k;
	cout<<solve(a, k)<<endl;
	return 0;
}
