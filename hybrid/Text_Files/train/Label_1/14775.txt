#include <iostream>
#include <cmath>
#include <iomanip>

using namespace std;

int main() {
    int d, L, v1, v2;
    cin >> d >> L >> v1 >> v2;
    cout << setprecision(15) << (double)(L - d) / (v1 + v2) << endl;
    return 0;
}
