def find_min_coins(N, M, K, L):
    if N-K < L:
        return -1
    min_coins_per_friend = (L + M - 1) // M
    if min_coins_per_friend * M + K > N:
        return -1
    return min_coins_per_friend

N, M, K, L = map(int, input().split())
print(find_min_coins(N, M, K, L))
