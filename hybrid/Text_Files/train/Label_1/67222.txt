N = int(input())
dp = [0] * (N + 1)
dp[1] = 1
for i in range(2, N + 1):
    for j in range(1, i):
        if i - j >= 2:
            dp[i] += dp[j] * dp[i - j] * 2
        else:
            dp[i] += dp[j] * dp[i - j]
dp[N] %= (10 ** 9 + 7)
print(dp[N])
