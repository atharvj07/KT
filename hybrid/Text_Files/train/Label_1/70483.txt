n = int(input())
kills = sorted(int(a) for a in input().split())
ans = 0;
j = prev = 0
for i in range(0, n):
    if kills[i] > prev:
        prev = kills[i]
        j = i
    else:
        ans = max(ans, j - i + 1)
print(ans)
