#include <iostream>
#include <math.h>
using namespace std;
int main() {
    int r, h;
    cin >> r >> h;
    if (h < r) { cout << 0 << endl; return 0; }
    cout << ((h - r) / r + 1) * (h / r + 1) << endl;
    return 0;
}
