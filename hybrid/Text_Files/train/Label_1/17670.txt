#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    map<string, int> cnt;
    while (n--) {
        string s, t;
        cin >> s >> t;
        cnt[t]++;
    }
    cout << cnt["hard"] << "\n";
    return 0;
}
