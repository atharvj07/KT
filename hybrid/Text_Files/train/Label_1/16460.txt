#include <bits/stdc++.h>

const int MAXDIG = 5e5;
int main() {
  int a;
  scanf("%d", &a);
  int n = 0;
  for (int i = 1; i < MAXDIG; i++) {
    n = n * 10 + 1;
    if (n % a == 0 && 1LL * n / a < MAXDIG) {
      printf("%lld\n", 1LL * n / a);
      return 0;
    }
  }
  printf("-1\n");
}
