#include<bits/stdc++.h>
using namespace std;
int freq[27];
int main(){
 int n,k;
 cin>>n>>k;
 string s;
 cin>>s;
 int mx=0, sum=0;
 for(int i=0;i<n;i++){
  freq[s[i]-'A']++;
  sum=min(sum+1,freq[s[i]-'A']);
  mx=max(mx,sum);
 }
 cout<<2*mx-1<<endl;
}
