def solve(n, a):
    ans = 0
    for i in range(n-1):
        if a[i] > a[i+1]:
            x = a[i] - a[i+1] + 1
            ans = max(ans, x.bit_length())
    return ans

t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    print(solve(n, a))
