from collections import defaultdict

n = int(input())
words = input().split(' ')
roots = defaultdict(list)
for word in words:
    key = ''.join(sorted(set(word)))
    roots[key].append(word)

print(len(roots))
