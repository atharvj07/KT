#include <bits/stdc++.h>

using namespace std;

int main() {
  string t;
  cin >> t;
  string s;
  for (int i = 0; i < t.size(); i++) {
    if (i % 2 == 0) {
      s += t[i];
    } else {
      s.insert(0, string(1, t[i]));
    }
  }
  cout << s << endl;
  return 0;
}
