#include<bits/stdc++.h>
using namespace std;
int main() {
    int t, x;
    while (cin >> t) {
        while (t--) {
            cin >> x;
            cout << (360 % (180 - x) == 0 ? "YES" : "NO") << endl;
        }
    }
    return 0;
}
