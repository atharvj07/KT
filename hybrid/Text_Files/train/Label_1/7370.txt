#include <iostream>

using namespace std;

int main() {
  int n, a, b;
  cin >> n >> a >> b;
  if (n % (a + b) == 0) {
    cout << "YES" << endl;
    cout << n / (a + b) << " " << n / (a + b) << endl;
  } else {
    cout << "NO" << endl;
  }
  return 0;
}
