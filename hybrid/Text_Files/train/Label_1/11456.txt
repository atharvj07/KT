#include <bits/stdc++.h>
using namespace std;
long long q, l, r, d, ans;
int main() {
    cin >> q;
    while (q--) {
        cin >> l >> r >> d;
        ans = ((l + d - 1) / d) * d;
        if (ans <= r) ans += d;
        cout << ans << endl;
    }
    return 0;
}
