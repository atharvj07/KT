#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define ull unsigned long long
#define fr(i,a,b) for(ll i=a;i<b;i++)
#define rf(i,a,b) for(ll i=a;i>=b;i--)
#define pb push_back
#define mp make_pair
#define fi first
#define se second
#define ios ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

ll n,k,temp;
string s,ans;

int main()
{
    ios
    ll t;
    cin>>t;
    while(t--)
    {
        k=99999999;
        cin>>n;
        cin>>s;
        ans=s;
        fr(i,1,n+1)
        {
            string t=s;
            reverse(t.begin(),t.begin()+i);
            for(ll m=1;m+i-1<=n;++m)
            {
                if(ans>=t)
                {
                    ans=t;
                    k=min(i,k);
                }
                reverse(t.begin()+m,t.begin()+m+i);
            }
        }
        cout<<ans<<endl<<k<<endl;
    }
    return 0;
}
