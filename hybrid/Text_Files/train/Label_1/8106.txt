#include <iostream>
using std::cin, std::cout;

int main()
{
    int n, t, k, d;
    cin >> n >> t >> k >> d;
    cout << ((t * ((n + k - 1) / k) > d + t || t * ((n + 2 * k - 1) / (2 * k)) <= d + t) ? "YES" : "NO");
    return 0;
}
