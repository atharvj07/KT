#include <iostream>
#include <limits>

int main() {
  int a, b, c, x, y, z;
  std::cin >> a >> b >> c >> x >> y >> z;
  std::cout << ((x <= a && y <= b && z <= c) || (x <= b && y <= c && z <= a) || (x <= c && y <= a && z <= b) ? "Yes" : "No");
  return 0;
}
