#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> ans = {2, 4, 3};
  if (n >= 5) ans.push_back(5);
  cout << ans.size() << '\n';
  for (auto v : ans) cout << v << ' ';
  return 0;
}
