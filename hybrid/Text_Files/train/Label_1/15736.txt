#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, m;
    cin >> n >> m;
    if (m == 0)
      cout << 0 << endl;
    else if (m % 2 == 0)
      cout << m - 1 << endl;
    else
      cout << m << endl;
  }
  return 0;
}
