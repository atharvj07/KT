#include <iostream>

int main() {
  int n;
  std::string s;
  std::cin >> n >> s;
  int cnt = 0;
  for (int i = 0; i < n; i++) {
    if (s[i] == '0') cnt++;
    else break;
  }
  std::cout << cnt << std::endl;
  return 0;
}
