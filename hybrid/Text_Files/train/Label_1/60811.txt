n = int(input())
addresses = list(map(int, input().split()))
x, f = map(int, input().split())

total_fee = 0
for a in addresses:
    if a > x:
        excess = a - x
        transactions = (excess + x + f - 1) // (x + f)
        total_fee += transactions * f

print(total_fee)
