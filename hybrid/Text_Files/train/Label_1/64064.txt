import sys

n = int(sys.stdin.readline())
a = [int(x) for x in sys.stdin.readline().split()]
dp = [i > 0 for i in a]
ans = 0
for i in range(1, n):
    dp[i] = dp[i] or (dp[i - 1] and a[i - 1] < a[i])
    if dp[i]:
        ans += i
print(ans)
