#include <bits/stdc++.h>
using namespace std;
int f[100001];
int main() {
  int n;
  cin >> n;
  string s;
  for (int i = 1; i <= n; i++) {
    cin >> s;
    for (int j = 0; j < f[i - 1]; j++) s += s[j];
    f[i] = s.size();
  }
  cout << f[n] << "\n";
  return 0;
}
