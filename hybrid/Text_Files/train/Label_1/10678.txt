#include <cstdio>

int main() {
    long long t;
    scanf("%lld", &t);
    while (t--) {
        long long n;
        scanf("%lld", &n);
        puts((n % 2 && n > 2) ? "YES" : "NO");
    }
    return 0;
}
