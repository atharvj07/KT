#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    long long n;
    cin >> n;
    if (n < 3)
        cout << 0;
    else if (n % 2) // n is odd
        cout << 2 * n - 4;
    else
        cout << n - 2;
    return 0;
}
