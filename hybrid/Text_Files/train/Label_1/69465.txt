['import sys\n\ndef solve():\n    n, k = map(int, sys.stdin.readline().split())\n    a = list(map(int, sys.stdin.readline().split()))\n    a.sort(reverse=True)\n\n    if k % 2 == 1:\n        return a[k // 2]\n\n    even_max = a[k // 2]\n    odd_max = max(a[k // 2 - 1], a[k // 2 + 1])\n\n    return min(even_max, odd_max)\n\nprint(solve())\n']