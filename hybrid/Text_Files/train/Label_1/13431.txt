#include <iostream>

using namespace std;

int main() {
  long long a, b, c, l;
  cin >> a >> b >> c >> l;
  cout << (a + b > c && b + c > a && a + c > b ? 1 + (l > 0) * (1 + (l > 1) * 3) : 0);
  return 0;
}
