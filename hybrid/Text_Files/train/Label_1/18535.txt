#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,d,m; cin>>n>>d>>m;
    while(m--){
        int x,y; cin>>x>>y;
        if(x>=d && x<=n-d && y>=d && y<=n-d) cout<<"YES\n";
        else cout<<"NO\n";
    }
    return 0;
}
