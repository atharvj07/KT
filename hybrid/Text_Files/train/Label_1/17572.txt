#include <bits/stdc++.h>
using namespace std;
int q, x, y;
char s[550], t[1100];
int main() {
  cin >> q;
  while (q--) {
    cin >> s >> t;
    x = strlen(s), y = strlen(t);
    if (x > y - 1)
      cout << "YES" << endl;
    else
      cout << "NO" << endl;
  }
  return 0;
}
