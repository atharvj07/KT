#include <iostream>
using namespace std;
int dp[61];
int main() {
    int n;
    cin >> n;
    dp[0] = 1;
    for (int i = 1; i <= n; i++) {
        dp[i] = dp[i - 1];
        if (i > 1) dp[i] += dp[i - 2];
        if (i > 2) dp[i] += dp[i - 3];
    }
    cout << dp[n] << endl;
    return 0;
}
