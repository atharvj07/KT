n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans = 0
for i in range(n - k + 1):
    s = 0
    for j in range(i, i + k):
        s += a[j] * (j - i + 1)
    ans = max(ans, s)
print(ans)
