#include <iostream>
#include <cstdlib>

using namespace std;

int main() {
  int t; cin >> t;
  while (t--) {
    long long a, b, c, n; cin >> a >> b >> c >> n;
    if (n % 3 == (a + b + c) % 3) cout << "YES" << endl;
    else cout << "NO" << endl;
  }
  return 0;
}
