#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  map<string, int> m;
  while (n--) {
    string s;
    cin >> s;
    m[s]++;
  }
  for (auto it = m.rbegin(); it != m.rend(); it++) cout << it->first << '\n';
  return 0;
}
