#include <bits/stdc++.h>
using namespace std;

int main() {
  string s;
  int k;
  cin >> s >> k;
  for (int i = 0; i + 1 < s.length() && k; i++) {
    if (s[i] < s[i + 1]) {
      swap(s[i], s[i + 1]);
      k--;
    }
  }
  cout << s << endl;
  return 0;
}
