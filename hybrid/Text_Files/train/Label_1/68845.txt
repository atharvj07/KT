n, m = map(int, input().split())

info = []
for i in range(n):
  info.append(sorted([int(x) for x in input().split()]))

for a in info:
  for x in range(m):
    ans = 1
    for y in range(x + 1, m):
      if a[y] < a[x]: ans = max(a[y] + 1, ans)
    print(ans, end = ' ')
  print('')
