#include <iostream>

int main() {
  long long x;
  std::cin >> x;
  for (long long a = 1; a * a <= x; ++a) {
    if (x % a == 0) {
      std::cout << a << ' ' << x / a << '\n';
      return 0;
    }
  }
  std::cout << -1 << '\n';
  return 0;
}
