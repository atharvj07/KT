#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int q, d;
    cin >> q >> d;
    string s;
    while (q--) {
      cin >> s;
      cout << (s.find(to_string(d)) == string::npos ? "NO" : "YES") << endl;
    }
  }
}
