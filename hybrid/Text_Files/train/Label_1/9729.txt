#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    cin >> s;
    if(s[0] < s[1]){
        cout << s[0] << s.substr(26, 1) << s.substr(1, 25) << endl;
        cout << s.substr(25, 1) << s.substr(2, 25) << endl;
    }
    else{
        cout << "Impossible" << endl;
    }
    return 0;
}
