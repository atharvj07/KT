#include <iostream>
using namespace std;

int main() {
    int n, d, m;
    cin >> n >> d >> m;
    while (m--) {
        int x, y;
        cin >> x >> y;
        cout << ((x <= d || y <= d || x + y >= n || y - x >= d) ? "YES" : "NO")
             << "\n";
    }
    return 0;
}
