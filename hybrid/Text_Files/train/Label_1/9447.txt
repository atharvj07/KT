#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int a, b;
        cin >> a >> b;
        cout << min(a, b) + min(max(a - 1, 0), max(b - 1, 0)) / 2 << endl;
    }
    return 0;
}
