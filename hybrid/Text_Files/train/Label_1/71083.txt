import sys
def solve(s):
    l, r = 0, 0
    for c in s:
        if c == '(': l += 1
        elif c == ')': r += 1
        else:
            if l > 0: l -= 1
            else: return ':('
    return '(' * l + s + ')' * r
print(solve(input()))

