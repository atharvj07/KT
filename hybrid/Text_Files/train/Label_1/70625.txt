n = int(input())
teams = []
for i in range(n):
    t, w = map(int, input().split())
    teams.append((t, w))
teams.sort(key=lambda x: (x[0] > x[1]), reverse=True)
ans = 1
for a, b in teams:
    if a > 0:
        ans += 1
    else:
        break
print(ans)
