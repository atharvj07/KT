#include <iostream>
#include <vector>

int main() {
  int n;
  std::cin >> n;
  std::vector<int> a(n);
  for (int& ai : a) std::cin >> ai;
  for (int i = 1; i < n; i++)
    if (abs(a[i] - a[i - 1]) >= 2) {
      std::cout << "NO";
      return 0;
    }
  std::cout << "YES";
}
