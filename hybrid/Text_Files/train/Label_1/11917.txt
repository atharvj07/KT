#include<bits/stdc++.h>
using namespace std;
int main(){
	int n,i,m,x,nw=0,pm=100,mn=100,c=0,ans=0; cin>>n;
	for(i=0;i<n;i++){cin>>x; if(x<mn) mn=x; if(x>pm) pm=x; c+=x;}
	for(m=mn;m<=pm;m++){
		if((c-m)>=m){
			nw=(c-m)/m;
			if(nw>ans) ans=nw;
		}
	}
	cout<<ans<<endl;
	return 0;
}
