#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long int c, d;
    cin >> c >> d;
    if ((c + d) % 2 == 0 && (c - d) % 2 == 0)
      cout << (c + d) / 2 << endl;
    else
      cout << -1 << endl;
  }
  return 0;
}
