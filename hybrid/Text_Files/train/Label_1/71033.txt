n = int(input())
p = [int(i) for i in input().split()]
p.sort()
ans = 0
for i in range(n):
    if p[i] != i+1:
        if p[i] <= n-i-1:
            ans += p[i]-1
        else:
            ans += n-p[i]
print(ans)
