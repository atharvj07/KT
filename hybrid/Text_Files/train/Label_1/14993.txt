#include <iostream>

using namespace std;

int main() {
    string a, b;
    cin >> a >> b;
    int cnt = 0;
    for (int i = 0; i < a.size(); i++) {
        if (a[i] == b[i] && a[i] == '0') cnt++;
    }
    cout << cnt / 3;
    return 0;
}
