#include <bits/stdc++.h>

int main() {
    int t;
    std::cin >> t;
    while (t--) {
        long long a, b;
        std::cin >> a >> b;
        std::cout << abs(b - a) / 10 + (abs(b - a) % 10 != 0) << std::endl;
    }
    return 0;
}
