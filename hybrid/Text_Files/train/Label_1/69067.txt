import bisect, sys

n, k, m = map(int, input().split())
t = list(map(int, input().split()))
t.sort()

ans, pos, cur = 0, 0, 0
for i in range(n):
    idx = bisect.bisect_left(t, cur + m - k + pos)
    pos += t[idx] - cur
    ans += idx - i
    cur = t[idx]

print(ans)
