import sys
input = sys.stdin.readline
for _ in range(int(input())):
    n = int(input())
    a = sorted(map(int, input().split()), reverse=True)
    tot, ans = sum(a), 0
    for x in a:
        ans += min(x, tot - x)
        tot -= x
    print(ans)
