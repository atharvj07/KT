#include <iostream>
#include <vector>

int main() {
  int n;
  std::string s;
  std::cin >> n >> s;
  int ans = 0, cnt_b = 0;
  for (int i = 0; i < n; ++i) {
    if (s[i] == 'R') {
      if (!cnt_b) ++ans;
      --cnt_b;
    } else ++cnt_b;
  }
  std::cout << ans << std::endl;
  return 0;
}
