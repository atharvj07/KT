#include <bits/stdc++.h>
using namespace std;
const int MAXN = 100001;
int a[MAXN];
int main() {
  int n, k;
  cin >> n >> k;
  int ans = 0;
  for (int i = 1; i <= n; i++) {
    int x;
    cin >> x;
    if (x == 0)
      continue;
    a[++ans] = x;
  }
  cout << ans * 2 << endl;
  return 0;
}
