from sys import stdin

T = int(stdin.readline())
for _ in range(T):
    n = int(stdin.readline())
    power = 0
    for i in range(n):
        monsters, *armors = map(int, stdin.readline().split())
        power = max(power + 1, max(armors) + 1)
    print(power)
