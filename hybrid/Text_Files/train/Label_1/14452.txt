#include <iostream>

int main() {
  long long k, l, r, t, x, y;
  std::cin >> k >> l >> r >> t >> x >> y;
  std::cout << ((l <= k && k + y - t * x >= l && k + y - t * x <= r) ? "Yes" : "No") << "\n";
}
