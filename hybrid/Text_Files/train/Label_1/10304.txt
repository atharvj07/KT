#include <bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define ll long long
int main() { fast; int n, d; cin >> n >> d; int c = 0, ans = 0;
  while (d--) { string s; cin >> s; bool f = true;
    for (int i = 0; i < n; i++) { if (s[i] == '0') { c = 0; f = false; } }
    ans += f; c += f; }
  cout << c << '\n'; }
