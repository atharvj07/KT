import string

for _ in range(int(input())):
    n, a, b = map(int, input().split())
    chars = string.ascii_lowercase[:b] * (n // a)
    if n % a: chars += string.ascii_lowercase[:n % a]
    print(chars)
