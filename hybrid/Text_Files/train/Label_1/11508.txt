#include <bits/stdc++.h>
using namespace std;
int main() {
    int c, d, n, m, k;
    cin >> c >> d >> n >> m >> k;
    int ans = max(0, n * m - k);
    if (!ans)
        cout << 0 << endl;
    else {
        cout << (ans + n - 1) / n << endl;
    }
}
