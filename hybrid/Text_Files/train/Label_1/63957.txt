t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    zeros = a.count(0)
    total_sum = sum(a)
    if total_sum + zeros == 0:
        print(zeros + 1)
    else:
        print(zeros)
