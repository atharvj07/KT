#include <cstdio>
int main() {
  int n, h, l, r;
  scanf("%d%d%d%d", &n, &h, &l, &r);
  int a[n];
  for (int &x : a) scanf("%d", &x);
  int ans = 0;
  for (int i = 0; i < n; i++) if (l + a[i] < h && r + a[i] >= h) ++ans;
  if (l == 0) ++ans;
  printf("%d", ans);
}
