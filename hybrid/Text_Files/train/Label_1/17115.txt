#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, m, p, s=1;
    string a;
    cin >> n >> m >> p;
    cin >> a;
    for(char o; m--; ){
        cin >> o;
        if(o=='L'){
            s--;
            if(!s) s=n;
        }
        else if(o=='R'){
            s++;
            if(s>n) s=1;
        }
        else{
            int t=s, c=0;
            a[s-1]='-';
            s--;
            if(!s) s=n;
            while(c!=2){
                if(a[s-1]=='('){
                    a[s-1]='-';
                    c++;
                }
                s--;
                if(!s) s=n;
            }
        }
    }
    for(int i=0; i<n; i++) if(a[i]!='-') cout << a[i];
    cout << endl;
}
