#include <cstdio>

int main() {
  size_t n, q;
  scanf("%zu%zu", &n, &q);
  size_t mid = (n + 1) / 2;
  while (q--) {
    size_t x, y;
    scanf("%zu%zu", &x, &y);
    size_t num = (x + y) % 2 ? n * n - (x + y - 2) * mid + y : (x + y - 2) * mid + y;
    printf("%zu\n", num);
  }
  return 0;
}
