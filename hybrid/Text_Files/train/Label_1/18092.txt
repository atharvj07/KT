#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    string s;
    cin >> n >> k;
    cin >> s;
    if (s[0] + s[n - 1] - '0' * 2 + count(s.begin(), s.end(), '?') < k)
        cout << "NO";
    else
        cout << "YES";
    return 0;
}
