#include <bits/stdc++.h>
int t, a, b, n, x, y;
int main() {
  scanf("%d", &t);
  while (t--) {
    scanf("%d %d %d", &a, &b, &n);
    if (b > n * a)
      puts("YES");
    else
      puts("NO");
  }
}
