#include <iostream>

int main() {
    int t;
    std::cin >> t;
    while (t--) {
        long long r, b, d;
        std::cin >> r >> b >> d;
        std::cout << ((r + b <= 2 * d) ? "YES" : "NO") << '\n';
    }
}
