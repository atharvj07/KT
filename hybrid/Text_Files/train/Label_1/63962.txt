n, k = map(int, input().split())
a = list(map(int, input().split()))
for i in range(k):
    a.sort()
    if a[0] == 0:
        print(0)
    else:
        print(a[0])
        for j in range(n):
            if a[j] > 0:
                a[j] -= a[0]
