#include <iostream>
using namespace std;

int main() {
  int n;
  cin >> n;
  string a[] = {"I hate ", "I love "};
  string b[] = {"it", "that"};
  string s = "";
  for (int i = 0; i < n - 1; i++) {
    s += a[i % 2] + b[i % 2];
  }
  cout << s + a[(n - 1) % 2] + b[(n - 1) % 2];
  return 0;
}
