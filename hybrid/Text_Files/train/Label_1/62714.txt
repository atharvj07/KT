import sys
input = lambda: sys.stdin.readline().strip()

n, p = map(int, input().split())
a = list(map(int, input().split()))
s = 0
for x in a: s = (s + x) % p
ans = s

for i in range(1, n):
  s = ((s - a[i - 1]) % p + a[i - 1] + a[i]) % p
  ans = max(ans, s)

print(ans)
