import sys

for _ in range(int(input())):
    n = int(input())
    t = list(map(int, input().split()))
    x, a = map(int, input().split())
    y, b = map(int, input().split())
    k = int(input())

    t = sorted(t)

    if (x + y) <= 100: ans = k // (max(t) * ((x + y) / 100)) + 1
    else: ans = -1

    if ans > n: ans = -1
    print(ans)
