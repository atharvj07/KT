#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	ll n, t, ans = 0, sum = 0;
	cin>>n>>t;
	ll a[n];
	for(int i = 0; i<n; i++){
		cin>>a[i];
		sum += a[i];
		if(sum <= t) ans++;
		else break;
	}
	cout<<ans<<endl;
}
