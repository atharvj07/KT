import sys,itertools
input=sys.stdin.readline
n=int(input());t=list(map(int,input().split()));c=[list(input()) for i in range(n)]
for l in range(2,n+1):
 list(itertools.combinations(range(1,n+1),l))
for i,j in itertools.combinations(range(1,n+1),2):
 b,s=c[i-1][j-1],1
 for k in t:
  if c[k-1][i-1]==b and c[k-1][j-1]==b:s+=1
 if s==len(t):return print(*t,-1),print(j,i),sys.exit()
print(-1)
