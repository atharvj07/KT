#include <bits/stdc++.h>
using namespace std;
const int mod = 1e9 + 7;
int main() {
  int n, m, mod1;
  cin >> n >> m >> mod1;
  vector<int> col(n);
  for (int i = 0; i < m; ++i) {
    string s;
    cin >> s;
    for (int j = 0; j < n; ++j) ++col[j], col[j] %= 3;
  }
  int ans = 1;
  for (int i = 0; i < n; ++i) {
    if (col[i] == 2) ans = 2 * ans % mod1;
  }
  cout << ans << endl;
  return 0;
}
