import sys

n, m, k = map(int, input().split())
a = list(map(int, input().split()))

a.sort(reverse=True)
ans = 0
cur = k
for x in a:
    if cur + x >= m:
        ans += 1
        cur = x
    else:
        cur += x

if cur < m: print(-1)
else: print(ans)
