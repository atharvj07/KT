n, l, r, ql, qr = map(int, input().split())
w = list(map(int, input().split()))

lp = l + ql
rp = r + qr
dp = [0] * n
dp[0] = w[0] * lp
for i in range(1, n):
    dl = dp[i - 1] + w[i] * lp
    dr = dp[i - 1] + w[i] * rp if i > 0 else w[i] * rp
    dp[i] = min(dl, dr)
print(dp[n - 1])
