T = int(input().strip())
for _ in range(T):
    s = input().strip()
    groups_of_ones = [len(group) for group in s.split('0') if group]
    groups_of_ones.sort(reverse=True)
    print(sum(groups_of_ones[::2]))
