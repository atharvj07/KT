#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  char s[n + 1];
  cin >> s;
  int ans = 0;
  bool st = 0;
  for (int i = 0; i < n; i++) {
    if (s[i] == 'U') {
      st = !st;
    }
    if (st != (s[i] == 'R')) { ans++; }
  }
  cout << ans << endl;
}
