#include <bits/stdc++.h>

using namespace std;

const long long MOD = 1000000007;

int main() {
  long long n, m;
  cin >> n >> m;
  long long ans = (1LL * n / m + 1) % MOD;
  ans = (ans * ans) % MOD;
  cout << ans << endl;
  return 0;
}
