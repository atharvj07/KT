x, y, l, r = map(int, input().split())
ans = 1
for a in range(0, 65):
    for b in range(0, 65):
        if x**a + y**b >= l and x**a + y**b <= r:
            if x**a + y**b == x**a + y**b + 1:
                ans = max(ans, a+b+1)
print(ans)
