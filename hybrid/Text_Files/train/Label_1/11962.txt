#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a, b, c, d, e, f;
    cin >> n >> a >> b >> c >> d >> e >> f;
    int r1 = max(a, n - c - d);
    int r2 = max(c, n - r1 - d);
    int r3 = max(d, n - r1 - r2);
    cout << r1 << ' ' << r2 << ' ' << r3 << endl;
    return 0;
}
