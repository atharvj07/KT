#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, m, a[55], dif = 1e9;
    cin >> n >> m;
    for (int i = 0; i < m; i++) cin >> a[i];
    sort(a, a + m);
    for (int i = 0; i <= m - n; i++) dif = min(dif, a[i + n - 1] - a[i]);
    cout << dif << endl;
    return 0;
}
