from sys import stdin

for _ in range(int(stdin.readline())):
    n, m, k = map(int, stdin.readline().split())
    x = [0, *map(int, stdin.readline().split()), 1000000]
    y = [0, *map(int, stdin.readline().split()), 1000000]
    p = sorted([tuple(map(int, stdin.readline().split())) for i in range(k)], key=lambda i: i[0])
    ans = 0
    for i in range(k):
        for j in range(i + 1, k):
            if max(abs(p[i][0] - p[j][0]), abs(p[i][1] - p[j][1])) % 1000000 > abs(p[i][0] - p[j][0]) + abs(p[i][1] - p[j][1]):
                ans += 1
    print(ans)
