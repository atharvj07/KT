#include<bits/stdc++.h>
using namespace std;
int main() {
  string s;
  int m;
  cin >> s >> m;
  vector<int> v(m);
  for (int i = 0; i < m; i++) cin >> v[i];
  while (m--) {
    int j = v[m] - 1;
    swap(s[j], s[s.size() - (j + 1)]);
  }
  cout << s << endl;
  return 0;
}
