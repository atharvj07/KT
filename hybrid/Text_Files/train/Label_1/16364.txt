#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    if (!d && !b)
      cout << a << endl;
    else if (!d)
      cout << max(a, b) << endl;
    else
      cout << c + d << endl;
  }
  return 0;
}
