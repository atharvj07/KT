from sys import stdin
n = int(stdin.readline())
cr = [int(x) for x in stdin.readline().split()]
for j in range(1,101):
	flag = True
	for i in range(1,n+1):
		if j==1:k=i
		else:k=cr[k-1]
		if k==i:
			flag = False
			break
	if flag==True:
		print(j)
		exit()
print(-1)
