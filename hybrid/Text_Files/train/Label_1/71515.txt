for t in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    t = [i for i in a if i % 2]
    if t: print(1, t[0]); continue
    e = [i for i in a if i % 2 == 0]
    if len(e) & 1: print(len(e), *e); continue
    print(-1)
