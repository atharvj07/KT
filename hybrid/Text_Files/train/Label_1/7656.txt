#include <iostream>
#include <iomanip>

using namespace std;

int main() {
  long long l, p, q;
  cin >> l >> p >> q;
  cout << fixed << setprecision(6) << 1.0 * l * p / (p + q) << endl;
  return 0;
}
