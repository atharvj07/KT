#include <iostream>
#include <vector>
using namespace std;
int main() {
    int n, s;
    cin >> n >> s;
    vector<int> a(n);
    for (int &x : a) cin >> x;
    int ans = 0;
    for (int i = 1; i <= n; i++) {
        if (i != s && a[i] >= a[a[i]]) ans++;
    }
    cout << ans;
    return 0;
}
