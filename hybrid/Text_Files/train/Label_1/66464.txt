suffixes = ["po", "desu", "masu", "mnida"]
languages = ["FILIPINO", "JAPANESE", "JAPANESE", "KOREAN"]

for _ in range(int(input())):
    sentence = input()
    for suffix, language in zip(suffixes, languages):
        if sentence.endswith(suffix):
            print(language)
