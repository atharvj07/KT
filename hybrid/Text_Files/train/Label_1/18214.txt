#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    string s;
    cin >> s;
    if (s == string(s.rbegin(), s.rend())) {
      if (s[0] == '0')
        cout << "BOB" << endl;
      else
        cout << "ALICE" << endl;
    } else
      cout << "ALICE" << endl;
  }
}
