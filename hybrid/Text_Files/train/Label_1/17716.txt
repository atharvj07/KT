#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n;
  string s;
  cin >> n >> s;
  int ans = 0;
  int b = 0;
  for (char c : s) {
    if (c == 'B') b++;
    else if (b) {
      b--;
      ans++;
    }
  }
  cout << ans << '\n';
}
