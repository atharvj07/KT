#include <map>
#include <iostream>

std::map<int, int> cnt;

int main() {
  std::string input;
  while (std::cin >> input) {
    cnt[input[0]]++, cnt[input[1]]++;
  }
  std::cout << (cnt[input[0]] || cnt[input[1]] ? "YES" : "NO") << std::endl;
}
