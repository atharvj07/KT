#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long r, b, d;
    std::cin >> r >> b >> d;
    if (r == 0 || b == 0)
      std::cout << "NO\n";
    else if (r + b <= d + 2)
      std::cout << "YES\n";
    else
      std::cout << "NO\n";
  }
  return 0;
}
