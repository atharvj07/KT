#include <cstdio>
#include <cstdlib>
using namespace std;

int gcd( int a, int b ) { return b ? gcd( b, a % b ) : a; }

int main() {
    int a, b, c, d;
    scanf( "%d%d%d%d", &a, &b, &c, &d );
    int g = gcd( a * d, b * c );
    printf( "%d/%d", (a*d)/g - (b*c)/g, (a*d)/g );
    return 0;
}
