#include <bits/stdc++.h>
using namespace std;
int main() {
  int s, t;
  char c;
  cin >> s >> t;
  s -= t;
  s = s % 24;
  if (s < 0) s += 24;
  cin >> c;
  printf("%02d:%02d", s / 10 % 24, s % 60);
  return 0;
}
