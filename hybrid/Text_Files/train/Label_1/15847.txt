#include <iostream>
using namespace std;

int main() {
  int l, s, r, p;
  for (int i = 0; i < 4; i++) {
    cin >> l >> s >> r >> p;
    if (l + p + r > 0) {
      cout << "YES" << endl;
      return 0;
    }
  }
  cout << "NO" << endl;
  return 0;
}
