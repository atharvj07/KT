#include <algorithm>
#include <iostream>

using namespace std;

int main() {
  int n, k;
  string s;
  cin >> n >> k;
  cin >> s;
  sort(s.begin(), s.end());
  if (k > n) {
    cout << s + string(k - n, 'a');
  } else {
    cout << s.substr(0, k);
  }
  return 0;
}
