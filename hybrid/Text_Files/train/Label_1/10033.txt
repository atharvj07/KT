#include <iostream>
#include <string>
using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int v = 0;
  for (int i = 0; i < n; i++) {
    int t = 0;
    while (s[i] >= 'A' && s[i] <= 'Z') { t++; i++; }
    if (t > v) v = t;
  }
  cout << v << endl;
}
