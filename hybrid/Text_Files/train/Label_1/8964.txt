#include<bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int n,l,ans=0,i,a[100001];
	cin>>n;
	while(n--){
		cin>>l;
		for(i=0;i<l;i++){
			cin>>a[i];
			if(i>0&&a[i]>a[i-1]){
				ans+=n;
				break;
			}
		}
	}
	cout<<ans<<endl;
}
