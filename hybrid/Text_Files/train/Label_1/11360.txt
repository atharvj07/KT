#include <bits/stdc++.h>
using namespace std;
int main() {
    long long x, y;
    cin >> x >> y;
    if (y % x != 0) {
        cout << -1 << endl;
        return 0;
    }
    cout << (long long)log2(y / x) << endl;
}
