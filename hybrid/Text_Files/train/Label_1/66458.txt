def find_minimal_X(n, Y):
    Y.sort()
    X = []
    for y in Y:
        while y % 2 == 0:
            y //= 2
        if y not in X:
            X.append(y)
    return X

n = int(input())
Y = list(map(int, input().split()))

X = find_minimal_X(n, Y)
print(" ".join(map(str, X)))
