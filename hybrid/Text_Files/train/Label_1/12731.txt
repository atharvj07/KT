#include <iostream>

using namespace std;

int main() {
  long long N, M, K, L;
  cin >> N >> M >> K >> L;
  cout << (((N - K >= L) && (M >= L)) ? ((N - K) / M + ((N - K) % M > 0)) : -1) << endl;
  return 0;
}
