#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x, y, c;
  cin >> n >> x >> y >> c;
  cout << max(0, c - x + 1 - (x <= y ? min(x - 1, max(1, n - y)) : min(n - x, max(1, y - 1))));
}
