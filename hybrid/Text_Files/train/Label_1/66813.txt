['from sys import stdin\n\nn = int(stdin.readline())\ns = list(stdin.readline())\nt = list(stdin.readline())\n\nminhd = n\na = b = -1\n\nfor i in range(n):\n    for j in range(i + 1, n):\n        s[i], s[j] = s[j], s[i]\n        hd = sum(x != y for x, y in zip(s, t))\n        s[i], s[j] = s[j], s[i]\n        if hd < minhd:\n            minhd = hd\n            a, b = i + 1, j + 1\n\nprint(minhd)\nprint(a, b)\n']