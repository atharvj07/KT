#include <iostream>

int main() {
  long long int n, m;
  std::cin >> n >> m;
  if (m == n)
    std::cout << m - 1;
  else if (m > n / 2)
    std::cout << n;
  else
    std::cout << m + 1;
  return 0;
}
