import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int w = in.nextInt();
        int b = in.nextInt();
        double p = (double)(w) / (w + b);
        double diff = (double)1 / (w + b);
        while (w > 0 && b > 0) {
            b -= Math.random() < p ? 1 : 0;
            w -= Math.random() < p ? 0 : 1;
            p = (double)(w) / (w + b);
        }
        System.out.printf("%.9f", p);
    }
}
