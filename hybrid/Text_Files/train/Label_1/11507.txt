#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int c, d, n, m, k;
    cin >> c >> d >> n >> m >> k;
    if (n * m <= k) cout << 0 << endl;
    else {
        cout << min(c + (n * m - k) / n, d + n * m - k) << endl;
    }
    return 0;
}
