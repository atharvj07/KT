#include <iostream>
#include <string>
using namespace std;
int main() {
  string s;
  int ans = 0;
  cin >> s;
  for (int i = 0; i < s.length(); i++) {
    if (s[i] == '1') {
      for (int j = 0; j < i; j++) { ans += 1 << (i - j - 1); }
    }
  }
  cout << ans << endl;
  return 0;
}
