import sys
input = sys.stdin.readline

n=int(input())
*c,=map(int,input().split())
strs=[input().strip() for i in range(n)]
m=10**9+5
e=[(c[i],strs[i]) for i in range(n)]
e.sort(key=lambda x:x[1])
x=e[0][1]
ans=e[0][0]
for v,s in e[1:]:
    if s.startswith(x):
        ans=m
    x=s
    ans=min(ans,v)
print(ans if ans!=m else -1)
