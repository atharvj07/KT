#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int r, b, d;
    cin >> r >> b >> d;
    r < b ? cout << ((r + d >= b) ? "YES\n" : "NO\n") : cout << ((b + d >= r) ? "YES\n" : "NO\n");
  }
  return 0;
}
