#include <iostream>
#include <cmath>
using namespace std;
int main() {
  long long m;
  cin >> m;
  long long n = (long long)(ceil(log2(sqrt(m))));
  if (pow(2, n - 1) * (pow(2, n - 1) + 1) == m)
    cout << n;
  else
    cout << -1;
  return 0;
}
