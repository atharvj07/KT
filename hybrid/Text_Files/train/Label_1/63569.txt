import sys
n, k = map(int, input().split())
s = input()
t = input()
ans = max(s.count('a'), s.count('b')) * n
for i in range(n):
    if s[i] != t[i]:
        ans += abs(s.count(s[i]) - t.count(s[i]))
        break
print(ans)
