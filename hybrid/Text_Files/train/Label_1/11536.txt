#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c, ans = 0;
    cin >> a >> b >> c;
    ans += min(a, b / 2);
    b -= min(a, b / 2) * 2;
    ans += min(c / 2, b);
    cout << ans * 3 << endl;
  }
  return 0;
}
