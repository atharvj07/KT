import sys
n = int(input())
a = input()
names = ["vaporeon", "jolteon", "flareon", "espeon", "umbreon", "leafeon", "glaceon", "sylveon"]
for name in names:
    if len(name) == n and all([a[i] == '.' or a[i] == name[i] for i in range(n)]):
        print(name)
        sys.exit()
