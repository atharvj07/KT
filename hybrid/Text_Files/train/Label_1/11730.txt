#include <iostream>
#include <algorithm>

int main() {
	int t, n, k;
	std::cin >> t;
	while (t--) {
		std::cin >> n >> k;
		if (k >= n - 1) std::cout << 1 << std::endl;
		else std::cout << n - k << std::endl;
	}
}
