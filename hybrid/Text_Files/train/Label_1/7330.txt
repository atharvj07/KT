#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    string s;
    cin >> s;
    int ans = 0;
    for (int i = 0; i < (n + 1) / 2; i++) {
        if (s[i] != s[n - 1 - i]) ans++;
    }
    cout << ans << "\n";
    return 0;
}
