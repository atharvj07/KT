#include <iostream>
using namespace std;
int main() {
  int x, s1 = 0, s2 = 0, s = 0;
  cin >> x;
  while (x--) {
    cin >> s1;
    s += s1;
    s2 += (s % 2 == 0);
  }
  if (s % 2)
    s2--;
  s2--;
  cout << (s2 ? "YES" : "NO") << endl;
}
