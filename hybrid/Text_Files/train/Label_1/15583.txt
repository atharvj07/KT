#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    ll a, b, x, y, z;
    cin >> a >> b >> x >> y >> z;
    ll ans = 2 * x - a + max(y - a - b, 0LL) + max(z - b, 0LL);
    cout << ans << endl;
    return 0;
}
