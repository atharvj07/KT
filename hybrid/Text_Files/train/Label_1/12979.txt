#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int x, y;
    cin >> x >> y;
    cout << (x <= y ? x - 1 : x) << " " << (y <= x ? y - 1 : y) << endl;
  }
  return 0;
}
