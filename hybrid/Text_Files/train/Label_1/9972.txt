#include <iostream>
using namespace std;

int main() {
    int s, v1, v2, t1, t2;
    cin >> s >> v1 >> v2 >> t1 >> t2;
    cout << ((s * v1 + t1 + t2 < s * v2 + t1 + t2) ? "First" : (s * v1 + t1 + t2 == s * v2 + t1 + t2) ? "Friendship" : "Second") << endl;
    return 0;
}
