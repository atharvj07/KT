import sys
input = sys.stdin.readline

n,m=map(int,input().split())
subs=[0]*m
for _ in range(n):
    s,r=map(int,input().split())
    subs[s-1]+=r
sm=0
for sb in subs:
    if sb>0: sm+=sb
    else: sm=0; break
print(sm)
