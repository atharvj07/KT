import math
for _ in range(int(input())):
    n, m, k = map(int, input().split())
    b = [0] * n
    players = [[] for _ in range(m)]
    for i in range(k):
        for x in range(m):
            if b[x] == math.ceil(n/m):
                p = []
                for j in range(math.floor(n/m)):
                    p.append(q[0])
                    q = q[1:]
                players[x].append(p)
            else:
                p = []
                for j in range(math.ceil(n/m)):
                    p.append(q[0])
                    q = q[1:]
                players[x].append(p)
            b[x] += 1
            b[x] %= math.ceil(n/m) + 1
    q = range(1, n+1)
    for g in players:
        for p in g:
            print(len(p), *p)
