#include <cstdio>
#define ll long long
int main() {
  int n, k;
  scanf("%d %d", &n, &k);
  ll sum = 0;
  for (int i = 0; i < n; i++) {
    int x;
    scanf("%d", &x);
    sum += x;
    if (i >= k) sum -= x;
    if (i >= k - 1) printf("%.10f ", (double)(sum) / k);
  }
  puts("");
  return 0;
}
