import sys

n, k = map(int, input().split())
a = list(map(int, input().split()))
s = [False] * (k+1)
ans = 1e9

for x in a:
    for i in range(max(0, k-x+1), k+1):
        if not s[i]:
            s[i] = True
            ans = min(ans, (i+x-1)//x)

print(ans)
