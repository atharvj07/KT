#include <iostream>
#include <string>
using namespace std;

int main() {
  int n;
  cin >> n;
  string s1, s2;
  cin >> s1 >> s2;
  int ans = 0;
  for (int i = 0; i < n; i++) {
    int x = s1[i] - s2[i];
    if (x < 0) x += 10;
    ans += min(x, 10 - x);
  }
  cout << ans << endl;
  return 0;
}
