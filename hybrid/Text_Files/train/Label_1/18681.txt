#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> b(n);
  for (int& e : b) cin >> e;

  int st = 0, res = 0;
  for (int i = 0; i < n; ++i) {
    st += b[i];
    res = max(res, abs(st));
  }
  cout << res << endl;
}
