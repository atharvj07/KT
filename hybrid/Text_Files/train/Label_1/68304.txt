N = int(input())
arr = list(map(int, input().split()))
temp = list(map(int, input().split()))
ans = []
for i in range(N):
    if arr[i] <= temp[i]:
        ans.append(arr[i])
        arr[i] = 0
    else:
        ans.append(temp[i])
        arr[i] -= temp[i]
print(*ans)
