import sys

n, k, a, b = map(int, sys.stdin.readline().split())
pos = [0] * (1 << n)
for i in map(int, sys.stdin.readline().split()): pos[i] = 1
ans = 0
for i in range(1 << n - 1, 1 << n):
    if pos[i]: ans += a
    else: ans += b * bin(i ^ (i ^ (i - (i & -i)))).count("1")
print(ans)
