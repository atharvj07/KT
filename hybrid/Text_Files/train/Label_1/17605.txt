#include <iostream>

using namespace std;

bool is_possible(int a[], int n) {
  int sum = 0;
  for (int i = 0; i < n; i++) sum += a[i];
  return sum % 2 == 0;
}

int main() {
  int a[4];
  for (int i = 0; i < 4; i++) cin >> a[i];
  cout << (is_possible(a, 4) ? "YES" : "NO") << endl;
  return 0;
}
