#include <cstdio>
int main() {
  int p;
  scanf("%d", &p);
  int ans = p - 1;
  for (int i = 2; i * i <= p; i++) {
    if (p % i == 0) {
      while (p % i == 0) p /= i;
      ans -= ans / i;
    }
  }
  if (p > 1) ans -= ans / p;
  printf("%d\n", ans);
}
