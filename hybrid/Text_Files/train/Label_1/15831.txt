#include <iostream>
#include <iomanip>

int main() {
    int h, m, a;
    std::cin >> h >> m >> a;
    m = (m + a) % 60;
    h = (h + (m + a) / 60) % 24;
    std::cout << std::setfill('0') << std::setw(2) << h << ':' << std::setfill('0') << std::setw(2) << m << '\n';
    return 0;
}
