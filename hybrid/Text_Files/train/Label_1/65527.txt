def thanos_sort(arr):
    if arr == sorted(arr):
        return len(arr)
    else:
        mid = len(arr) // 2
        return max(thanos_sort(arr[:mid]), thanos_sort(arr[mid:]))

n = int(input())
arr = list(map(int, input().split()))
print(thanos_sort(arr))
