n, m = map(int, input().split())
v = [tuple(map(int, input().split())) for x in range(n)]
v.sort(key=lambda x: x[1] / x[0], reverse=True)
ans = 0
for w, c in v:
    if m >= w:
        ans += c
        m -= w
    else:
        ans += m / w * c
        break
print(int(ans))
