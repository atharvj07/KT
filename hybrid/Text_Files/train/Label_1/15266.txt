#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 998244353
ll p, n, m, cnt, ans;
unordered_map<ll, ll> mp;
int main() {
	cin >> n >> p >> m;
	for (int i = 0; i < n; i++) {
		ll x; cin >> x;
		cnt += mp[m - x]++, mp[x]++;
	}
	cout << cnt / 2;
	return 0;
}
