#include <iostream>
#include <algorithm>
using namespace std;

int main() {
  string s; cin >> s;
  int odd_cnt = 0;
  for (char c : s)
    odd_cnt += c % 2;
  puts((odd_cnt & 1) || (odd_cnt==0) ? "First" : "Second");
  return 0;
}
