from collections import deque
import sys

n, p = map(int, input().split())
s = deque([0] * (n + 1))
t = list(map(int, input().split()))
for i in sorted(range(n), key=lambda i: t[i]):
    s[i + 1] = max(s[i + 1], max(s[i] + p, t[i]))
    print(s[i + 1])
