#include <iostream>
#include <string>
#include <map>
using namespace std;

int main() {
  string l1, l2, s;
  cin >> l1 >> l2 >> s;
  map<char, char> m;
  for (int i = 0; i < 26; i++) m[l1[i]] = l2[i];
  for (char &c: s) if (isalpha(c)) c = m[c];
  cout << s << endl;
}
