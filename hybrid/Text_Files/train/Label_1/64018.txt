['import sys\ninput = sys.stdin.readline\n\nn, k = map(int, input().split())\na = list(map(int, input().split()))\nb = list(map(int, input().split()))\nmod = 10 ** 9 + 7\n\ndef fastpow(base, power):\n    result = 1\n    while power > 0:\n        if power % 2 == 1:\n            result = (result * base) % mod\n        base = (base * base) % mod\n        power //= 2\n    return result\n\ndp = [[[[-1 for _ in range(2)] for _ in range(2)] for _ in range(k + 1)] for _ in range(n + 1)]\n\ndef solve(idx, prev, start, tight):\n    if idx == n:\n        return 1\n    if dp[idx][prev][start][tight] != -1:\n        return dp[idx][prev][start][tight]\n    ans = 0\n    for digit in range(10):\n        if digit == b[idx // k] and start == 0:\n            continue\n        new_tight = tight\n        if digit != 0:\n            new_tight = False\n        if (idx + 1) % k == 0 and digit * fastpow(10, k - 1) % a[idx // k] != 0:\n            continue\n        ans += solve(idx + 1, digit, start or digit != 0, new_tight)\n        ans %= mod\n    dp[idx][prev][start][tight] = ans\n    return ans\n\nprint(solve(0, 0, 0, True))\n']