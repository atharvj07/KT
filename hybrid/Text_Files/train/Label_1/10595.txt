#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    char c = 'a';
    for (int i = 0; i < n; i++) {
        cout << c;
        c = c == 'a' ? 'b' : c == 'b' ? 'c' : 'a';
    }
    return 0;
}
