#include <iostream>
#include <string>
using namespace std;
int main() {
  string a;
  cin >> a;
  int l = 0, r = a.size() - 1;
  while (l < r) {
    if (a[l] != a[r]) {
      cout << "NIE" << endl;
      return 0;
    }
    l++;
    r--;
  }
  cout << "TAK" << endl;
  return 0;
}
