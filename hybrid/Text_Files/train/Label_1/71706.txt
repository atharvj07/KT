import sys
input = sys.stdin.readline

n = int(input())

x_c = {}
y_c = {}

ans = 0
for _ in range(n):
    x, y = map(int, input().split())
    ans += x_c.get(x + y, 0) + y_c.get(x - y, 0)
    x_c[x + y] = x_c.get(x + y, 0) + 1
    y_c[x - y] = y_c.get(x - y, 0) + 1

print(ans)
