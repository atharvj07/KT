def solve():
    n, b = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    ans = 0
    for i in range(1, n):
        if a[i] - a[i - 1] <= b:
            ans += 1
            b -= a[i] - a[i - 1]
    return ans

print(solve())
