#include <bits/stdc++.h>

int main() {
  int n;
  std::cin >> n;
  std::vector<int> p(n);
  for (int i = 0; i < n; i++) std::cin >> p[i];
  std::sort(p.rbegin(), p.rend());
  std::cout << 1 + std::distance(p.begin(), p.begin() + 1) << ' ' << p[1] << '\n';
}
