#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        long long n;
        cin >> n;
        int ans = 0;
        while (n > 0) {
            ans += (n & 1);
            n >>= 1;
        }
        cout << ans << endl;
    }
}
