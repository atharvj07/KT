#include <iostream>

int main() {
  int h, w;
  std::cin >> h >> w;
  char c;
  bool color = false;
  while (h--) {
    while (w--) {
      std::cin >> c;
      color |= (c == 'C' || c == 'M' || c == 'Y');
    }
  }
  std::cout << (color ? "#Color" : "#Black&White") << std::endl;
  return 0;
}
