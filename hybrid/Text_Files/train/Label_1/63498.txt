for t in range(int(input())):
    n,m=map(int,input().split());a=[list(map(int,input().split()))for i in range(n)]
    for c in range(m):
        a[::][c]=sorted(a[::][c])
    print(sum(max(row)for row in a))
