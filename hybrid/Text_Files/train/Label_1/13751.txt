#include <iostream>

int main() {
  int n;
  std::cin >> n;
  int a = 0, b = 0;
  for (int i = 0; i < n; ++i) {
    int t, x, y;
    std::cin >> t >> x >> y;
    if (t == 1)
      a += x;
    else
      b += x;
  }
  std::cout << (a >= 5 ? "LIVE\n" : "DEAD\n") << (b >= 5 ? "LIVE\n" : "DEAD\n");
}
