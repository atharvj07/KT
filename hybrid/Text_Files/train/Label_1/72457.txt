from sys import stdin
input = stdin.readline

n = int(input())
a = list(map(int, input().split()))
ans = 10**9
for i in range(n-1):
    for j in range(i+1, n):
        g = sum(a[min(i, j)+1 : max(i, j)+1])
        ans = min(ans, (i-j)**2 + g**2)
print(ans)
