import sys

for _ in range(int(input())):
    n, x = map(int, input().split())
    a = list(map(int, input().split()))
    b = list(map(int, input().split()))
    print("Yes" if all(xi + yi <= x for xi, yi in zip(a, sorted(b))) else "No")
