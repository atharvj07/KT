#include <iostream>
#include <string>

int main() {
  std::string s, t;
  std::cin >> s >> t >> t;
  std::cout << (s[0] == t[2] || s[1] == t[0] ? "cw" : (s[0] == t[0] || s[1] == t[2] ? "ccw" : "undefined")) << std::endl;
}
