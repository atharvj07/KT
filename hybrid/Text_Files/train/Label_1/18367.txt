#include <bits/stdc++.h>
using namespace std;

int main() {
    long long r, g, b;
    cin >> r >> g >> b;
    cout << min({r / 3, g / 3, b / 3, (r % 3 + g % 3 + b % 3) / 3}) << endl;
    return 0;
}
