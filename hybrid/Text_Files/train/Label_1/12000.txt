#include <iostream>

using namespace std;

int main() {
  int T;
  cin >> T;
  while (T--) {
    int a, b;
    cin >> a >> b;
    int ans = abs(a - b);
    ans /= 5;
    ans += abs(a - b) % 5 != 0;
    cout << ans << endl;
  }
  return 0;
}
