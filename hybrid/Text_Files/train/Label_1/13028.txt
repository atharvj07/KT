#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int T, ang;
    cin >> T;
    while (T--) {
        cin >> ang;
        if (ang == 90) cout << -1 << endl;
        else cout << 360 / (180 - ang) << endl;
    }
    return 0;
}
