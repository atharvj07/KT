#include <iostream>
#include <string>

int main() {
    int n;
    std::string s;
    std::cin >> n >> s;
    int c = 0;
    for (int i = 0; i < n - 2; ++i) {
        if (s[i] == 'x' && s[i + 1] == 'x' && s[i + 2] == 'x') ++c;
    }
    std::cout << c << std::endl;
}
