#include <iostream>

using namespace std;

int main() {
  long long a, b, n, x;
  cin >> a >> b >> n >> x;
  long long ans = x;
  for (long long i = 0; i < n; ++i) {
    ans = (a * ans + b) % (1000000007);
  }
  cout << ans << endl;
  return 0;
}
