#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, m, k;
    cin >> n >> m >> k;
    if (n % 2 == m % 2 || k % 2 == 0 && k <= n * m / 2)
      cout << "YES\n";
    else
      cout << "NO\n";
  }
}
