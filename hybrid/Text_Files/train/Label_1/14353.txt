#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
  ll a, b, ans = -1;
  cin >> a >> b;
  if (a > b) swap(a, b);
  if (b % a)
    cout << ans;
  else {
    ans = 0;
    while (a != b) {
      b /= 2;
      ans++;
    }
    cout << ans;
  }
  return 0;
}
