#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int a, b, c, d;
        cin >> a >> b >> c >> d;
        int x = a, y = b, z = c;
        cout << x << " " << y << " " << z << "\n";
    }
    return 0;
}
