import sys
input = sys.stdin.readline
 
n, h, a, b, k = map(int, input().split())
 
for _ in range(k):
    t1, f1, t2, f2 = map(int, input().split())
    if t1 == t2:
        ans = abs(f1 - f2)
    else:
        ans = abs(f1 - a) + b - a + abs(f2 - a)
    print(ans)
