#include <bits/stdc++.h>
using namespace std;
string s;
int main()
{
    cin>>s;
    int i=0,j,l=s.size();
    while(i<l)
    {
        if(i+1<l&&s[i]=='1'&&s[i+1]=='4'&&s[i+2]=='1')
        {
            i+=3;
            continue;
        }
        if(i+1<l&&s[i]=='1'&&s[i+1]=='4')
        {
            i+=2;
            continue;
        }
        if(s[i]=='1')
        {
            i++;
            continue;
        }
        cout<<"NO"<<"\n";
        return 0;
    }
    cout<<"YES"<<"\n";
    return 0;
}
