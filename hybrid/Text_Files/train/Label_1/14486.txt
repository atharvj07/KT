#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> freq(100001);
  int mx = 0;
  int a;
  for (int i = 0; i < n; i++) {
    cin >> a;
    mx = max(++freq[a], mx);
  }
  cout << mx << endl;
}
