#include <iostream>
using namespace std;

int main() {
    int m, n;
    double ans = 0;
    cin >> m >> n;
    for (int i = 1; i <= m; i++)
        ans += (double)i / m * max((double)i, ans);
    cout << ans << endl;
    return 0;
}
