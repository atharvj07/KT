n,x = map(int,input().split())
stack = [x]
d = 0
for i in range(n):
    a,b = input().split()
    if a=="+": stack.append(stack[-1]+int(b))
    else:
        if stack[-1]>=int(b): stack[-1]-=int(b)
        else: d+=1
print(stack[-1],d)
