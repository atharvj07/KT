import sys
input = sys.stdin.readline
n, m, a, b = map(int, input().split())
g = [[] for _ in range(n+1)]
for _ in range(m):
    x, y, c = map(int, input().split())
    g[x].append((y, c))
    g[y].append((x, c))
vis = [False] * (n+1)
def dfs(x: int, p: int, t: int):
    vis[x] = True
    ans[x] = min(ans[x], t)
    for y, c in g[x]:
        if not vis[y]:
            dfs(y, x, t+c)
for i in range(1, n+1):
    if not vis[i]:
        ans = [10**18] * (n+1)
        dfs(i, 0, 0)
        print(*ans[1:])
