from collections import defaultdict

n, k = map(int, input().split())
a = list(map(int, input().split()))
d = defaultdict(int)
ans = 0
for ai in a:
    if ai % k == 0:
        x = ai
        while x % k == 0:
            x //= k
        ans += d[x]
    d[ai] += 1
print(ans)
