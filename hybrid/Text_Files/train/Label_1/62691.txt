import math
n = int(input())
vec = []
for i in range(n):
    x, y = map(int, input().split())
    vec.append(y / x)
    vec.append(-y / x)
vec = sorted(list(set(vec)))
ans = 0
for i in range(len(vec) - 1):
    ans = max(ans, (vec[i + 1] - vec[i]) * 180 / math.acos(-1))
print(ans)
