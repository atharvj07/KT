import sys
import math

n = int(input())
a = list(map(int, input().split()))
a.sort()
gcd = 0

for i in range(n - 1):
    gcd = math.gcd(gcd, a[i])
    for j in range(i + 1, n):
        gcd = math.gcd(gcd, math.lcm(a[i], a[j]))

print(gcd)
