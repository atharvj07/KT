import sys
input = sys.stdin.readline

t = int(input())

for _ in range(t):
    n, l, r = map(int, input().split())
    c = list(map(int, input().split()))
    c = [1] * l + [0] * r + [i for i in c]
    
    ans = sum(i != j for i, j in zip(c[:n//2], c[n//2:])) + (n//2 - 1)
    print(ans)
