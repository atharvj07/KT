t = int(input())
for _ in range(t):
    x, y, k = map(int, input().split())
    total_coal = k
    total_sticks_needed = k * y + k - 1
    trades_for_sticks = (total_sticks_needed + x - 2) // (x - 1)
    total_trades = trades_for_sticks + total_coal
    print(total_trades)
