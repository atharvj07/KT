#include <bits/stdc++.h>
using namespace std;
int main() {
  int t, n, d;
  cin >> t;
  while (t--) {
    cin >> n >> d;
    if (n < ceil(1.0 * d / (d + 1)))
      cout << "NO" << endl;
    else
      cout << "YES" << endl;
  }
}
