import sys
input = sys.stdin.readline

for _ in range(int(input())):
    a = [int(x) for x in input().split()]
    a.sort()
    for i in range(a[0], a[-1]+1):
        b = [x for x in a if x>=i and x<=i+len(a)-1]
        print('1' if len(b) == len(set(b)) else '0', end='')
    print()
