from math import inf
input()
for n in map(int,input().split()):
  m=0
  for i in range(n):
    k,*a=map(int,input().split())
    assert k==len(a)
    m=max(max(a)+i+1 if k else 0,m)
  print(max(m,1))
