n, m, k = map(int, input().split())
arr = sorted([(a, i+1) for i, a in enumerate(map(int, input().split()))], reverse=True)

ans = 0
last = -10**9
cnt = 0
for h, i in arr:
    if i == last:
        cnt += 1
    else:
        cnt = 1
    if cnt <= k:
        ans += h
    last = i

print(ans)
