def find_value(n, k):
    length = 2**n - 1
    while True:
        if k == length // 2 + 1:
            return n
        if k > length // 2:
            k -= length // 2 + 1
        length //= 2
        n -= 1

n, k = map(int, input().split())
print(find_value(n, k))
