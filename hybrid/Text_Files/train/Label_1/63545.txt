from sys import stdin
n = int(stdin.readline())
a, b = stdin.readline().strip(), stdin.readline().strip()
ans = sum(1 if a[i] != b[i] else 0 for i in range(n)) + abs(a.count('1') - b.count('1')) // 2
print(ans)
