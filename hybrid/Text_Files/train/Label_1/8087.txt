#include<bits/stdc++.h>
#define ll long long int
using namespace std;
 
int main()
{
    ll t;
    cin>>t;
    while(t--)
    {
        ll n;
        cin>>n;
        vector<ll> a(n);
        for(ll i=0;i<n;i++)
        {
            cin>>a[i];
        }
        sort(a.begin(),a.end());
        ll ans=a[n-1]-a[0];
        for(ll i=1;i<n-1;i++)
        {
            ans=min(ans,a[i+1]-a[i]);
        }
        cout<<ans<<endl;
    }
    return 0;
}
