# The program takes three space-separated integers n, a and b as input.
n, a, b = [int(x) for x in input().split()]

# Find the final entrance after Vasya's walk.
k = (a + b) % n
if k == 0:
    k = n
print(k)
