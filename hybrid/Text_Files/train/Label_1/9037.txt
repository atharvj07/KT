#include <bits/stdc++.h>
using namespace std;
const int mod = 998244353;
int main() {
  long long n, m;
  cin >> n >> m;
  cout << m * ((m - 1) % mod) % mod * ((m - 2) % mod) % mod * 2 % mod;
  return 0;
}
