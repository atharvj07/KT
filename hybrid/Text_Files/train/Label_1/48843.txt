import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int h = sc.nextInt();
        int[] a = new int[h + 1];
        for (int i = 1; i <= h; i++) {
            a[i] = sc.nextInt();
        }
        buildMaxHeap(a);
        for (int i = 1; i <= h; i++) {
            System.out.print(a[i] + " ");
        }
    }

    public static void buildMaxHeap(int[] a) {
        int h = a.length - 1;
        for (int i = h / 2; i >= 1; i--) {
            maxHeapify(a, i, h);
        }
    }

    public static void maxHeapify(int[] a, int i, int h) {
        int l = 2 * i;
        int r = 2 * i + 1;
        int largest = i;
        if (l <= h && a[l] > a[i]) {
            largest = l;
        }
        if (r <= h && a[r] > a[largest]) {
            largest = r;
        }
        if (largest != i) {
            int tmp = a[i];
            a[i] = a[largest];
            a[largest] = tmp;
            maxHeapify(a, largest, h);
        }
    }
}
