#include <iostream>
#include <set>
using namespace std;

int main() {
  string s;
  cin >> s;
  set<char> chars;
  for (char c : s) chars.insert(c);
  int num = (chars.size() + 1) / 2;
  if (s.size() % 2 == 0 && s.size() / 2 == num) cout << "NO" << endl;
  else cout << "YES" << endl;
  return 0;
}
