#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
#define mp make_pair
const ll mod = 1e9 + 7;
ll dx[8]{-1, -1, 0, 1, 1, 1, 0, -1};
ll dy[8]{0, 1, 1, 1, 0, -1, -1, -1};
#define endl '\n'
int a[110], vis[110], ans;

int main() {
    ll t;
    cin >> t;
    while (t--) {
        ll n, x;
        cin >> n;
        map<ll, ll> cnt;
        ans = 0;
        for (int i = 0; i < n; ++i) {
            cin >> x;
            a[i] = x;
            cnt[x]++;
        }
        for (int i = 0; i < n; ++i) {
            if (cnt[a[i]] > 1)
                ans++;
        }
        cout << ans << endl;
    }
    return 0;
}
