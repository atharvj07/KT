n, k = map(int, input().split())
a, b = [], []
for _ in range(n):
    ai, bi = map(int, input().split())
    a.append(ai)
    b.append(bi)
ans = 0
for i in range(n):
    ans += a[i]//k + b[i]//k
ans += min(sum(a) % k, sum(b) % k)
print(ans)
