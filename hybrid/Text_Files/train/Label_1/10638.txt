#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    int n;
    cin >> n;
    vector<int> ans = {1};
    for (int i = 2; ans.back() * i % n == 1; i++) ans.push_back(i);
    cout << ans.size() << '\n';
    for (auto x : ans) cout << x << ' ';
}
