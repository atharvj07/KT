#include<bits/stdc++.h>
using namespace std;
int main(){
  int t; cin >> t; while (t--){
    string s; cin >> s; int ans = 0;
    for (int i = 1; i + 1 < s.size(); ++i){
      if (s[i - 1] == s[i] && s[i] == s[i + 1]) ans++;
    }cout << ans << '\n';
  }
}
