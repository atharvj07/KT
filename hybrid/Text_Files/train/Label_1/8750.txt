#include <iostream>
using namespace std;
int main() {
  string x;
  cin >> x;
  for (int i = x.size() - 2; i >= 0; i--) {
    if (x[i] < x[i + 1]) {
      x[i] = x[i + 1];
    }
  }
  cout << x << endl;
}
