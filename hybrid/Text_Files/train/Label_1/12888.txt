#include <iostream>
#include <cmath>

using namespace std;

int main() {
    long long n, m;
    cin >> n >> m;

    long long ans = (sqrt(n*(n+1)*(2*n+1)/6) + 1) / 2;
    if ((m & 1) && (ans & 1)) ans--;
    cout << ans / m << endl;

    return 0;
}
