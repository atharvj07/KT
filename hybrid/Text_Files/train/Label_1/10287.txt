#include <iostream>
using namespace std;

int main() {
  string l1, l2, s; cin >> l1 >> l2 >> s;
  for (auto &c : s) c = ((c >= 'a' && c <= 'z') ? l2[l1.find(c)] : (c >= 'A' && c <= 'Z' ? char(l2[l1.find(char(c + 32))])-32 : c));
  cout << s;
  return 0;
}
