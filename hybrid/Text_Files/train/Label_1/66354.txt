for _ in range(int(input())):
    n, s, k = map(int, input().split())
    f = [s] + list(map(int, input().split()))
    f.sort()
    ans = float('inf')
    for i in range(1, k+1):
        ans = min(ans, f[i]-f[i-1]//2)
    print(ans)
