mod = 10**9 + 7

n = int(input())
pts = set()
for _ in range(n):
    x, y = map(int, input().split())
    if (x, y) not in pts:
        pts.add((x, y))
        continue
    else:
        pts.remove((x, y))

print(pow(3, len(pts), mod))
