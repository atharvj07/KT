#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, pos, l, r;
    cin >> n >> pos >> l >> r;
    cout << (min(pos - 1, l - 1) + max(n - pos, r - pos) + 
             (pos < l || pos > r)) << endl;
    return 0;
}
