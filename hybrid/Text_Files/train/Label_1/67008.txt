import queue
n=int(input())
t=list(map(int,input().split()))
q=queue.PriorityQueue()
for i in range(len(t)):
    q.put((t[i],i))
    ans=0
    tmp=0
    while not q.empty():
        u,idx=q.get()
        if tmp+u<=t[idx]:
            ans+=1
            tmp+=u
print(ans)
