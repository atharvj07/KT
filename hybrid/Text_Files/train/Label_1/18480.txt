#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int k = min(n, m);
    cout << k << '\n';
    for (int i = 0; i < k; ++i) {
        cout << i + 1 << ' ' << 1 + (i % 2) << '\n';
    }
    return 0;
}
