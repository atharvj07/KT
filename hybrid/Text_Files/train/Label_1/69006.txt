import math

n, m = map(int, input().split())
a = list(map(int, input().split()))
b = []
l = 1
for i in a:
    b.append(i)
    l = math.lcm(l, i)
    if l > m:
        b.pop()
        break

print(l, len(b))
print(*[i + 1 for i in range(len(a)) if a[i] in b])
