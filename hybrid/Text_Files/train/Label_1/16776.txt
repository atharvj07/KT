#include <iostream>
using namespace std;
int main() {
  long long t, n, ans;
  cin >> t;
  while (t--) {
    cin >> n;
    n -= 1;
    ans = ((n + 16) / 12) * 15;
    if (n > 9) {
      ans += ((n - 9) / 8) * 20;
      if (n > 32) ans += ((n - 32) / 10) * 25;
    }
    cout << ans << endl;
  }
}
