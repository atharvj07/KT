#include <iostream>
#include <string>
using namespace std;
int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;
  for (int i = 0; i < k; i++) {
    int j = 0;
    while (j < n && s[j] != 'a' + i) j++;
    if (j != n)
      s.erase(j, 1);
  }
  cout << s << endl;
  return 0;
}
