#include <bits/stdc++.h>
#define mk make_pair
using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  set<char> st;
  for (int i = 0; i < n; i++) st.insert(s[i]);
  int ans = st.size() * 2 - 1;
  if (ans > n) ans = n;
  cout << ans << endl;
  return 0;
}
