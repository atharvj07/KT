#include<bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  set<int> days;
  for (int i = 0; i < n; ++i) {
    int t;
    cin >> t;
    days.insert(t);
  }
  cout << days.size() << endl;
}
