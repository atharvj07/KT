#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, k;
    cin >> n >> k;
    if (k % 2 != 0 || k > n * (n + 1) / 2 || k < n) {
        cout << "0" << '\n';
    } else {
        cout << (k / 2 - n) / 2 << '\n';
    }
    return 0;
}
