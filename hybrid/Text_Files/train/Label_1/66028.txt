n, k = map(int, input().split())
a = [int(x) for x in input().split()]
books = set()
cost = 0
for book in a:
    if book in books:
        books.remove(book)
    else:
        if len(books) == k:
            books.remove(min(books))
        books.add(book)
        cost += 1
print(cost)
