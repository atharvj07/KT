import sys

def main():
    n = int(input())
    a = list(map(int, input().split()))
    ans = 0
    for i in range(n):
        ans += abs(a[i]) // 2
        if i + 1 < n:
            a[i + 1] += a[i] // 2
        a[i] -= a[i] // 2
    print(ans)

main()
