#include <bits/stdc++.h>
using namespace std;
int n, a[105], ans;
int main() {
  cin >> n >> ans;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
    ans = min(ans, (ans + a[i] - 1) / a[i]);
  }
  cout << ans << endl;
  return 0;
}
