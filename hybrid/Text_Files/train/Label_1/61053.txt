import sys
s=input().split('=')
assert s[1]==s[1][0]*len(s[1])
if len(s[0])==len(s[1]) and len(s[0])>1:
    print(''.join(list(s[0][:-1])+list('+')+list(s[0][-1]+s[1])))
elif len(s[0])==len(s[1])+1:
    print(''.join(list(s[0][:-2])+list('+')+list(s[0][-2:]+s[1])))
else:
    print('Impossible')
