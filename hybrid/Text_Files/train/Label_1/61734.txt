l, r = map(int, input().split())
mx = -1
ans = -1
for i in range(2, int(r**0.5) + 1):
    a = l // i
    b = r // i
    if a < b:
        cnt = b - a
    else:
        cnt = 0
    if cnt > mx:
        mx = cnt
        ans = i
print(ans)
