from sys import stdin
from collections import Counter

n, k = map(int, stdin.readline().split())
a = list(int(i) for i in stdin.readline().split())
cnt = Counter(a)
ans = 0
for e in cnt:
  if e * k in cnt and e * k * k in cnt:
    ans += cnt[e] * cnt[e * k] * cnt[e * k * k]
print(ans)

