#include <bits/stdc++.h>
using namespace std;
typedef long double ld;
const ld eps = 1e-9;

int main() {
    ld a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << fixed << setprecision(9) << (1 - (1 - a / b) * (1 - c / d) / (1 - (1 - a / b) * (1 - c / d))) << '\n';
    return 0;
}
