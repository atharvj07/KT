from collections import Counter

n = input()
m = input()
c = Counter(n)
t = Counter(m)
ans = 0
for k in t:
    if k in c:
        ans += min(c[k], t[k])
    else:
        ans = -1
        break
print(ans)
