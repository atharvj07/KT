#include<iostream>
#include<vector>
using namespace std;
int main() {
  int n, k;
  cin >> n >> k;
  if(n > k*(k+1)/2){
    cout << -1 << '\n';
    return 0;
  }
  for(int i=1;i<=n;i++){
    cout << ((i-1)/k)+1 << '\n';
  }
}
