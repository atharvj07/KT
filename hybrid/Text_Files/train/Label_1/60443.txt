import sys

input = sys.stdin.readline

n, k = map(int, input().split())
songs = sorted(zip(map(int, input().split()), map(int, input().split())), key=lambda x: x[1] / x[0], reverse=True)

ans = 0

i = 0
while k > 0:
    ans += songs[i][0] * songs[i][1]
    k -= 1
    i += 1

print(ans)
