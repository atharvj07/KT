#include<bits/stdc++.h>
using namespace std;
int main(){
  long long a1,b1,a2,b2,l,r;
  cin >> a1 >> b1 >> a2 >> b2 >> l >> r;
  cout << l - max(l % a1 == b1 ? (l-b1)/a1-1 : (l-b1)/a1, l % a2 == b2 ? (l-b2)/a2-1 : (l-b2)/a2) << endl;
  return 0;}
