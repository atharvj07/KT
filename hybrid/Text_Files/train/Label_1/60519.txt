def fpow(x, p):
    if p == 0:
        return 1
    if p == 1:
        return x
    val = fpow(x, p // 2)
    val = (val * val) % mod
    if p % 2 != 0:
        val = (val * x) % mod
    return val

n, k = map(int, input().split())
mod = 998244353
print(fpow(2, n - k + 1) * fpow(k, k) % mod)
