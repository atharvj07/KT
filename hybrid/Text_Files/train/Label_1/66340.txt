import sys

k = int(input())
d = [int(x) for x in input().split()]
d = sorted(d) 

ans = []
for i in range(k):
  if d[i] == 0 or d[i] == 1:
    ans.append(d[i])
  else:
    break

ans.reverse()

print(len(ans))
print(" ".join(str(x) for x in ans))
