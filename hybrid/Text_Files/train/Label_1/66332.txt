import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = sorted(map(int, input().split()))
    if len({x for x in a if a.count(x) > 1}) >= 2:
        print("NO")
    else:
        print("YES")
        for i in range(1, n):
            print(i, i + 1)
