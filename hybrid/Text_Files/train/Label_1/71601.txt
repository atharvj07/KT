k = int(input())
table = [input() for _ in range(4)]
timing_count = [0] * 10

for row in table:
    for char in row:
        if char.isdigit():
            timing_count[int(char)] += 1

answer = "YES" if all(count <= 2*k for count in timing_count) else "NO"
print(answer)
