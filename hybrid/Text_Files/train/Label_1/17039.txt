#include <iostream>
#include <unordered_set>

int main() {
  std::string s;
  std::cin >> s;
  std::unordered_set<char> seen;
  for (char c : s) {
    if (seen.count(c) > 0) {
      std::cout << "NO" << std::endl;
      return 0;
    }
    seen.insert(c);
  }
  std::cout << "YES" << std::endl;
}
