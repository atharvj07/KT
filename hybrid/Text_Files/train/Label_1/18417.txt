#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long k;
    cin >> k;
    long long row = sqrt(1 + 4 * (k - 1));
    long long col = k - (row * (row - 1)) / 2;
    cout << row << " " << col << "\n";
  }
}
