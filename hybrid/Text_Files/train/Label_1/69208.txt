import sys
N = int(input())
A = sorted(int(x) for x in input().split())
B = sorted([int(x) for x in input().split()], reverse=True)

sa = 0
sb = 0

while A or B:
	if sa <= sb:
		if A: sa += A.pop()
		else: sb -= B.pop()
	else:
		if B: sb += B.pop()
		else: sa -= A.pop()

print(sa-sb)
