n, m = map(int, input().split())
grid = [input().strip() for i in range(n)]
for i in range(n):
    for j in range(m):
        if grid[i][j] == 'B':
            x, y = i + (i % 2), j + (j % 2)
            break
print(x + 1, y + 1)
