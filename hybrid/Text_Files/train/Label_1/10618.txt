#include <iostream>
#include <string>

int main() {
  std::string s;
  std::cin >> s;
  if (s.find('9') != std::string::npos) {
    std::cout << "Yes";
  } else {
    std::cout << "No";
  }
  return 0;
}
