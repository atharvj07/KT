#include <iostream>
int main() {
    long long x, y, n, ans;
    std::cin >> x >> y >> n;
    if (n & 1) { ans = x - y; } else { ans = x + y; }
    ans = (ans + 1000000007) % 1000000007;
    std::cout << ans << std::endl;
    return 0;
}
