#include<bits/stdc++.h>
using namespace std;
int main(){
  int n,k,joy=-1;
  cin>>n>>k;
  for(int i=1,f,t;i<=n;i++){
    cin>>f>>t;
    if(t<=k)joy=max(joy,f);
    else joy=max(joy,f-t+k);
  }
  cout<<joy<<endl;
}
