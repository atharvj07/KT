import sys

t = int(input())

def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

for _ in range(t):
    x, p, k = map(int, input().split())
    ans = x + 1
    for i in range(k):
        while gcd(ans, p) != 1:
            ans += 1
        ans += 1
    print(ans - 1)
