import sys
input = sys.stdin.readline

n = int(input())
a = [int(x) for x in input().split()]
ans = max(a)
for i in range(n-1):
    ans += min(abs(a[i+1]-a[i]), 2*min(a[i]-1, 1000000-a[i+1]))

print(ans)
