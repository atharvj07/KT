def perform_operations(colors):
    operations = 0
    while True:
        to_delete = []
        for i in range(1, len(colors) - 1):
            if colors[i-1] != colors[i] or colors[i+1] != colors[i]:
                to_delete.append(i)
        if colors and (len(colors) > 1 and colors[0] != colors[1]):
            to_delete.append(0)
        if colors and (len(colors) > 1 and colors[-1] != colors[-2]):
            to_delete.append(len(colors) - 1)
        if not to_delete:
            break
        colors = ''.join([colors[i] for i in range(len(colors)) if i not in to_delete])
        operations += 1
    return operations

colors = input().strip()
print(perform_operations(colors))
