from bisect import bisect_right
n = int(input())
a = list(map(int, input().split()))
m = int(input())
q = list(map(int, input().split()))
a.sort()
for qi in q:
    idx = bisect_right(a, a[qi-1])
    print(sum(a[idx-qi:]) + sum(a[:idx-1]*(len(a[:idx-1]) - len(a[:idx-1])%qi)))

