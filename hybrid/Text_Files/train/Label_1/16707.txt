#include <bits/stdc++.h>
using namespace std;

int a[10];

int main() {
  string s;
  cin >> s;
  for (char c : s) {
    a[c - '0']++;
    if (a[c - '0'] > 1) {
      cout << "Yes";
      return 0;
    }
  }
  cout << "No";
}
