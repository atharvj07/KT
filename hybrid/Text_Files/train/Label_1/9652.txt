#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ll n,q,i;
    cin>>n>>q;
    ll arr[n+1]={0};
    for(i=1;i<=n;i++)
    arr[i]=i;
    while(q--)
    {
        cin>>i;
        cout<<arr[i]<<endl;
        while(i!=1)
        {
            swap(arr[i],arr[i/2]);
            i/=2;
        }
    }
    return 0;
}
