import sys

n, k = [int(x) for x in input().split()]
a = [int(x) for x in input().split()]
freq = dict()
for x in a:
    freq[x] = freq.get(x, 0) + 1

mn = n // k
ans = 0
for x in freq:
    if freq[x] < mn:
        ans += mn - freq[x]

print(ans)
