#include<bits/stdc++.h>
using namespace std;
int main(){
    int t; cin>>t;
    while(t--){
        int n; cin>>n;
        vector<int> a(n);
        for(int i=0;i<n;i++) cin>>a[i];
        bool win=1;
        for(int x: a) win&=!(x&1);
        cout<<(win?"T":"HL")<<endl;
    }
}
