#include <iostream>

using namespace std;

int main() {
  long long n, d, e;
  cin >> n >> d >> e;
  long long ans = n;
  for (long long i = 0; i <= n / d; i++) {
    long long rem = n - i * d;
    ans = min(ans, rem % e);
  }
  cout << ans << endl;
  return 0;
}
