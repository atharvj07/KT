#include <iostream>
using namespace std;
int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    long long ans = 1ll * a * (b - a + 1) * (c - b + 1) * (d - c + 1);
    cout << ans;
    return 0;
}
