#include <bits/stdc++.h>

using namespace std;

bool solve() {
  long long a, b, c;
  cin >> a >> b >> c;
  return __gcd(__gcd(a, b), c) > 1;
}

int main() {
  int t;
  cin >> t;
  for (int i = 0; i < t; ++i) {
    cout << (solve() ? "YES" : "NO") << endl;
  }
  return 0;
}
