import math

a, b = map(int, input().split())
n = int(input())

dist = math.inf
for _ in range(n):
    x, y, v = map(int, input().split())
    d = math.sqrt((a - x) ** 2 + (b - y) ** 2) / v
    dist = min(dist, d)

print(dist)
