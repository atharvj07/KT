#include <cstdio>
#include <cmath>
int main() {
  long long n, x[500000], s[500000], c = 0;
  scanf("%lld", &n);
  for (long long i = 0; i < n / 2; i++) {
    scanf("%lld", &x[i]);
    s[i] = x[i];
    if (i) s[i] += s[i - 1];
  }
  for (long long i = 0; i < n / 2; i++) {
    long long p = sqrt(s[i]);
    if (p * p != s[i]) {
      printf("No\n");
      return 0;
    }
  }
  long long p = sqrt(x[0]), p1 = -1;
  if (p * p != x[0]) {
    printf("No\n");
    return 0;
  }
  for (long long i = 1; i < n / 2; i++) {
    p1 = sqrt(s[i] - s[i - 1]);
    if (p1 * p1 != s[i] - s[i - 1]) {
      printf("No\n");
      return 0;
    }
  }
  printf("Yes\n");
  printf("%lld ", p);
  for (long long i = 0; i < n / 2; i++) {
    printf("%lld ", x[i]);
    p = p1;
    p1 = sqrt(s[i + 1] - s[i]);
    printf("%lld ", p);
  }
}
