#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    int g, sum = 0, fail = 0;
    for (int i = 0; i < n; i++) {
        cin >> g;
        sum += g;
        if (g < 5) fail++;
    }
    cout << (5 * n - sum + fail - 1) / 4 << '\n';
}
