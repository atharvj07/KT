import sys
r=sys.stdin.readline
n,k,a,b,q=map(int,r().split())
l=[0]*(n+2)
d=[0]*(n+2)
for i in range(q):
 o=r().split()
 if o[0]=='1':
  l[int(o[1])]+=int(o[2])
 else:
  p=int(o[1])
  d[p]+=1
  for i in range(p,n-k+2):
   d[i]+=d[i-1]
  for i in range(max(p+k,2),n+1):
   d[i]+=min(l[i],l[i-k-1]+a*(i-(p+k)))
  print(d[n])
