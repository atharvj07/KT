#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m;
  cin >> n >> m;
  map<string, string> mp;
  for (int i = 0; i < m; i++) {
    string s, t;
    cin >> s >> t;
    mp[s] = t;
    mp[t] = s;
  }
  for (int i = 0; i < n; i++) {
    string s;
    cin >> s;
    cout << mp[s] << " ";
  }
  return 0;
}
