#include <bits/stdc++.h>
using namespace std;
long long n, a[1000001], ans;
int main() {
    cin >> n;
    for (long long i = 0; i < n; i++) {
        cin >> a[i];
        if (a[i] == -1) ans = i;
        if (a[i] != -1) ans += a[i];
    }
    cout << ans;
    return 0;
}
