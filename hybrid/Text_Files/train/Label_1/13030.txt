#include <bits/stdc++.h>
using namespace std;
int a[2010][2010],vis[2010];
int main(){
    ios_base::sync_with_stdio(0),cin.tie(0);
    int n,m;cin>>n>>m;
    for(int i=1;i<=n;i++)for(int j=1;j<=m;j++)cin>>a[i][j];
    for(int i=1;i<=n;i++){
        bool on=1;
        for(int j=1;j<=m;j++)if(!a[i][j])on=0;
        if(on){cout<<"YES"<<endl;return 0;} 
    }
    cout<<"NO"<<endl;
}
