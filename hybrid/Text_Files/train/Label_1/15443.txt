#include <bits/stdc++.h>
using namespace std;
const int mx=1e9+7;
int main(){
	int n,ans=0,a,b,c;cin>>n;
	while(n--){
		cin>>a>>b;
		c=__gcd(a,b);
		if(c!=1)ans=__gcd(ans,c);
		else {cout<<-1;return 0;}
	}
	cout<<ans<<endl;
}
