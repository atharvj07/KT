import numpy as np
n,k,M=map(int,input().split())
t=[int(x) for x in input().split()]
t=np.array(t)
t.sort()
ans=0
for i in range(k):
    if(M>=t[i]):
        ans+=1
        M-=t[i]
    else:
        break
if(ans>=k):
    ans+=1
print(ans)
