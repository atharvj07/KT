import math
for _ in range(int(input())):
    n, h = map(int, input().split())
    a = [int(x) for x in input().split()]
    mn = math.inf
    for i in range(1, len(a)):
        x, y = a[i - 1], a[i]
        l = (h - 1 + x - y) // x
        if l < mn:
            mn = l
    print(mn)
