#include <iostream>

using namespace std;

int main() {
  long long n, p;
  cin >> n >> p;
  long long sum = 0;
  while (n--) {
    string s;
    cin >> s;
    sum += s == "half" ? p / 2 : p;
  }
  cout << sum << endl;
  return 0;
}
