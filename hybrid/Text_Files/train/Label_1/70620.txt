import bisect
n = int(input())
teams = [[int(a) for a in input().split()] for i in range(n)]
teams.sort(key=lambda t: t[1]-t[0])
pos = 0
for score, weight in teams:
  if score < weight:
    pos += 1
print(pos)
