#include <iostream>
#include <string>

using namespace std;

int main() {
  long long k;
  cin >> k;

  string s;
  for (int i = 1; i <= 10; i++) {
    for (int j = 1; j <= i; j++) {
      s += to_string(j);
    }
  }

  cout << s[k - 1] << endl;

  return 0;
}
