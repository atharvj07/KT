#include <iostream>

int main() {
  int t;
  std::cin >> t;
  for (int i = 0; i < t; i++) {
    long long n, x;
    std::cin >> n >> x;
    x--;
    long long val = 2 * (x / n) + 1;
    std::cout << val * (x % n) + 1 << '\n';
  }
}
