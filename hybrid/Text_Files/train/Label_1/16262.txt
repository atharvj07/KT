#include <iostream>
using namespace std;
int main() {
    int n, ans = 1;
    long long sum = 0;
    cin >> n;
    while (n--) {
        long long x;
        cin >> x;
        sum += x;
        if (sum < 0) { ans++; sum = 0; }
    }
    cout << ans << endl;
    return 0;
}
