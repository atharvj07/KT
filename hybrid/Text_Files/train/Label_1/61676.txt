import sys

for _ in range(int(input())):
    n, k, d = map(int, input().split())
    a = list(map(int, input().split()))
    ans = set()
    for i in range(n - d + 1):
        ans.update(a[i:i + d])
    print(len(ans))
