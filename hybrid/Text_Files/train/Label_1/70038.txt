t = int(input())
for _ in range(t):
    r, g, b = map(int, input().split())
    colors = sorted([r, g, b])
    if colors[2] > colors[0] + colors[1] + 1:
        print("No")
    else:
        print("Yes")
