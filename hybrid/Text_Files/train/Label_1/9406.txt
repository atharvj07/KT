#include <iostream>
#include <vector>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int b, p, f, h, c;
    cin >> b >> p >> f >> h >> c;
    b /= 2;
    int hh = min(b, p);
    int cc = min(b, f);
    int ans = hh * h + cc * c;
    cout << ans << endl;
  }
}
