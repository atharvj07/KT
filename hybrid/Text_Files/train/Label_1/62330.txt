n, m = map(int, input().split())
c = [0] * n
for l, r in [map(int, input().split()) for _ in range(m)]:
    c[l - 1] += 1
    c[r] -= 1
    
for i in range(1, n):
    c[i] += c[i - 1]
    
print('0' * min(c) + '1' * max(c))
