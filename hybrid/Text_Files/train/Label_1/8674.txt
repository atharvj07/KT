#include <iostream>
#include <vector>

int main() {
  int n;
  std::cin >> n;
  std::string a, b;
  std::cin >> a >> b;
  int ans = 0;
  for (int i = 0; i < n; i++) {
    int x = b[i] - a[i];
    if (x < 0) x += 10;
    int y = 10 - x;
    ans += std::min(x, y);
  }
  std::cout << ans << '\n';
}
