#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define mod 1000000007
int a[20][200005], b[20][200005];
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, k;
        cin >> n >> k;
        int ans = (1 << k) % mod;
        ans = (ans * __builtin_popcount(n)) % mod;
        cout << ans << endl;
    }
    return 0;
}
