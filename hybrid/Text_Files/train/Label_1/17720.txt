#include<bits/stdc++.h>
using namespace std;
map<char,int>mp;
int main(){
    int n;
    cin>>n;
    string s;
    cin>>s;
    for(int i=0;i<n;i++){
        mp[s[i]]++;
    }
    int can_do=mp['B'];
    int ans=min(mp['R'],can_do);
    cout<<ans+can_do<<endl;

    return 0;
}
