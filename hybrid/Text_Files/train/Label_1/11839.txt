#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}

int main() {
    int n, k, a, b;
    cin >> n >> k >> a >> b;
    cout << (a + k - b) / k << ' ' << (a + n * k - b) / k << '\n';
    return 0;
}
