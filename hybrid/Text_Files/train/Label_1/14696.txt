#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    string s;
    long long n;
    cin >> n;
    --n;
    n /= 2;
    long long p = pow(10, n / 2) * 11;
    s = to_string(p);
    n %= 2;
    while (n--) s += s.back();
    cout << s << endl;
    return 0;
}
