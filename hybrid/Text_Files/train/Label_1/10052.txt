#include <iostream>
using namespace std;
int a[93], b[16];
int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) cin >> a[i], b[a[i]]++;
  if (a[n - 1] < 15)
    cout << "UP";
  else if (a[n - 1] == 15 && b[0])
    cout << "DOWN";
  else
    cout << "-1";
  return 0;
}
