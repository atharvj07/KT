#include <cstdio>
#include <cmath>
double f[100005];
int main()
{
    int n;
    double ans;
    scanf("%d", &n);
    f[n] = 1;
    for (int i = n - 1; i >= 1; i--)
        f[i] = f[i + 1] + 1. / (i + 1);
    printf("%.12f\n", f[1]);
    return 0;
}
