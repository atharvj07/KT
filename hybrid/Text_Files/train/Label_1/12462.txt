#include <iostream>
#include <string>

int main() {
    int t, n;
    std::string s;
    std::cin >> t;
    while (t--) {
        std::cin >> n >> s;
        int c = 0;
        for (char ch : s) c += ch == ')';
        std::cout << (c > n - c ? "Yes" : "No") << '\n';
    }
}
