#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
int main() {
  int t;
  cin >> t;
  rep(_, t) {
    int n;
    string s;
    cin >> n >> s;
    rep(i, n) {
      if (s[i] != '?') { continue; }
      if (i == 0) {
        s[i] = 'B';
      } else if (s[i - 1] == 'B') {
        s[i] = 'R';
      } else {
        s[i] = 'B';
      }
    }
    cout << s << endl;
  }
}
