#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, k, ans = 0;
  cin >> n >> k;
  map<int, int> mp;
  for (int i = 0; i < n; ++i) {
    int x;
    cin >> x;
    while (x >= k) x /= k;
    ans += !mp[x]++;
  }
  cout << ans << '\n';
  return 0;
}
