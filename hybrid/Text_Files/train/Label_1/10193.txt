#include <iostream>
using namespace std;
int main() {
  long long n, H;
  cin >> n >> H;
  long long c = H + 1;
  n -= H;
  if (n >= c) {
    cout << c << endl;
    return 0;
  }
  c += (n + 1) / 2;
  cout << c << endl;
}
