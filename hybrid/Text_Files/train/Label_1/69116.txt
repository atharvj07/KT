def modpow(x, y, m):
    if y == 0:
        return 1
    if y == 1:
        return x % m
    v = modpow(x, y // 2, m)
    v = v * v % m
    if y % 2 == 1:
        return v * x % m
    return v

n, k = map(int, input().split())
print(modpow(k, n * n, 10**9 + 7))
