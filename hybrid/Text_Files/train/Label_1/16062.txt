#include <iostream>

using namespace std;

int main() {
  long long n, s, mi = 1e18;
  cin >> n >> s;
  for (long long i = 0, v; i < n; i++) {
    cin >> v;
    if (s >= v) s -= v;
    else mi = min(mi, s);
  }
  cout << (mi == 1e18 ? -1 : mi) << endl;
}
