#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long int ar[10000],sum=0,m=0;
	for(long long int i=0;i<14;i++)
	{
		cin>>ar[i];
		sum+=ar[i];
		if(ar[i]==0)
		continue;
		m=max(ar[i],m);
	}
	sum=sum-m;
	cout<<sum<<endl;
}
