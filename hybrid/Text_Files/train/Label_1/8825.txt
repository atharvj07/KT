#include <iostream>
using namespace std;
int main() {
  int n;
  cin >> n;
  int odd = 0;
  for (int i = 0; i < n; i++) {
    int a;
    cin >> a;
    odd += a % 2;
  }
  cout << (odd == n || odd % 2 == 1 ? "Yes" : "No") << endl;
  return 0;
}
