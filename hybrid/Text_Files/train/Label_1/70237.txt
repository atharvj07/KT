import sys

for _ in range(int(input())):
    n = int(input())
    a = sorted(map(int, sys.stdin.readline().split()))
    ans = [a[0]]
    for i in range(1, n):
        if a[i] != a[i - 1]:
            ans.extend([a[i - 1], a[i]])
    print(*ans)
