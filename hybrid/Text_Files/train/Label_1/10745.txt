#include <iostream>
using namespace std;
int main() {
  int da, db;
  cin >> da >> db;
  if (da > db - 1) {
    cout << -1 << endl;
    return 0;
  }
  cout << da * 100 + 99 << " " << db * 100 << endl;
  return 0;
}
