['import sys\ninput = sys.stdin.readline\n\ndef kruskal(n, edges, s):\n    par = [i for i in range(n+1)]\n    rnk = [1 for i in range(n+1)]\n\n    def find(x):\n        if par[x] != x:\n            par[x] = find(par[x])\n        return par[x]\n\n    def union(x, y):\n        a = find(x)\n        b = find(y)\n        if a == b:\n            return\n        if rnk[a] > rnk[b]:\n            par[b] = a\n            rnk[a] += rnk[b]\n        else:\n            par[a] = b\n            rnk[b] += rnk[a]\n\n    edges.sort(key=lambda e: e[2])\n    ans = 0\n    for x, y, w in edges:\n        if find(x) != find(y):\n            union(x, y)\n            ans += w\n            s -= 2\n            if s == 0:\n                return ans\n    return ans\n\nn, m = map(int, input().split())\na = list(map(int, input().split()))\nedges = []\nfor i in range(m):\n    x, y, w = map(int, input().split())\n    edges.append([x, y, w])\ns = n - 1\nprint(kruskal(n, edges, s))\n']