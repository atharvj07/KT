def count_bricks(x, y, a, b):
    lcm = x * y // gcd(x, y)
    return (b // lcm) - ((a - 1) // lcm)

def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

x, y, a, b = map(int, input().split())
print(count_bricks(x, y, a, b))
