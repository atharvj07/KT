from fractions import Fraction

n, k = map(int, input().split())
a = list(map(int, input().split()))

num_unsorted = sum(a[i] > a[i + 1] for i in range(n - 1))
num_swaps = k

ans = Fraction(num_swaps, num_swaps + num_unsorted) * n * (n - 1) // 2
ans = int(ans) % (10 ** 9 + 7)

print(ans)
