for _ in range(int(input())):
    a, b, p = map(int, input().split())
    s = input()
    for i in range(len(s)):
        if a <= p:
            p -= a
            continue
        if b <= p:
            p -= b
            continue
        print(i+1)
        break
