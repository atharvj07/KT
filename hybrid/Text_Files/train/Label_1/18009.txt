#include <iostream>
using namespace std;
int main() {
    string s;
    cin >> s;
    cout << (stoi(s.substr(0, 1) + s.substr(2, 1) + s.substr(4, 1) + s.substr(3, 1) +
                s.substr(1, 1)) % 100000)
             << endl;
    return 0;
}
