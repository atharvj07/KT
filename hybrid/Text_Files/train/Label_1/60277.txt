from collections import Counter

n = input()
m = input()
if len(n) == len(set(n)) and len(m) <= len(n): print(len(m)); exit()
cnt = Counter(n)
ans = 0
for c in m:
    if cnt[c] > 0:
        cnt[c] -= 1
        ans += 1
print(ans if ans == len(m) else -1)
