#include <bits/stdc++.h>
using namespace std;
int main() {
  int q, n;
  string s, t;
  cin >> q;
  while (q--) {
    cin >> n >> s >> t;
    bool f = true;
    for (int i = 0; i < n; i++)
      if (s.substr(i, 1) != t.substr(n - 1 - i, 1)) f = false;
    cout << (f ? "YES" : "NO") << endl;
  }
}
