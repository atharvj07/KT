#include <iostream>
using namespace std;
int main() {
  string s;
  cin >> s;
  long long wow = 0, ans = 0;
  for (char ch : s) {
    wow = wow * 2 + (ch == 'v');
    ans += wow / 3;
  }
  cout << ans;
  return 0;
}
