import sys
from collections import Counter
n = int(input())
a = [int(x) for x in sys.stdin.readline().split()]
c = Counter(a)
b = []
for x in sorted(set(a), reverse=True):
    if all(x + y not in c or c[x + y] == 1 for y in b):
        b.append(x)
        c[x] -= 1
print(len(b))
print(*b)
