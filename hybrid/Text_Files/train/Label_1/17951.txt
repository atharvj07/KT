#include<bits/stdc++.h>
using namespace std;

int main(){
    int cntpos = 0, cntneg = 0, n,x,y = 1; cin >> n;
    for(int i = 0; i < n; i++){
        cin >> x;
        y *= x;
        (y > 0) ? cntpos++: cntneg++;
    }
    cout << cntneg << ' ' << cntpos << '\n';
}
