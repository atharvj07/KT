#include<bits/stdc++.h>
using namespace std;
int main(){
    long long int n,a,b;
    cin>>n>>a>>b;
    if(a*b>=6*n){
        cout<<a*b<<endl<<a<<" "<<b<<endl;
    }
    else{
        long long int r=ceil(sqrt(6*n));
        cout<<r*r<<endl<<r<<" "<<r<<endl;
    }
    return 0;
}
