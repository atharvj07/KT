import sys
input = sys.stdin.readline

for t in range(int(input())):
    n, k = map(int, input().split())
    a = list(map(int, input().split()))
    need = []
    for x in a:
      if x % k:
        need.append(k - x % k)
    print(sum(need))
