#include <iostream>
using namespace std;
int main() {
  int n, i;
  char s[200];
  cin >> n >> s;
  for (i = 1; i <= n - 4; i++)
    if (s[i] == s[i + 1] && s[i + 1] == s[i + 2] && s[i + 2] == s[i + 3] && s[i + 3] == '*') {
      cout << "yes";
      return 0;
    }
  cout << "no";
  return 0;
}
