#include <iostream>

int main() {
  int n, m, k;
  std::cin >> n >> m >> k;
  int lane = (k - 1) / m + 1;
  int desk = (k - 1) % m + 1;
  char side = ((k - 1) % 2 == 0) ? 'L' : 'R';
  std::cout << lane << ' ' << desk << ' ' << side << std::endl;
  return 0;
}
