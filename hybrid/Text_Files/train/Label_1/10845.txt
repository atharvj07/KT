#include <iostream>
using namespace std;
int main() {
  int a, b, c, ans;
  cin >> a >> b >> c;
  int l1 = a - b, l2 = b - c, l3 = a - c;
  ans = abs(l1) + abs(l2) - abs(l3);
  cout << ans << endl;
  return 0;
}
