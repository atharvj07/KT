#include <iostream>
#include <cstdio>

int main() {
    long long n;
    std::cin >> n;
    n = n & (n - 1);
    long long res = 0;
    while (n > 0) {
        n = n & (n - 1);
        res++;
    }
    std::cout << res << std::endl;
    return 0;
}
