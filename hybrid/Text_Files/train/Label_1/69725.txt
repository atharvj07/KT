n, m = map(int, input().split())
a = [0] * n
b = [0] * n

for i in range(m):
    x, y = map(int, input().split())
    x, y = x - 1, y - 1
    ans = (n - 1 - a[x] - b[y]) * (n - a[x] - b[y])
    print(ans)
    a[x] += 1
    b[y] += 1
