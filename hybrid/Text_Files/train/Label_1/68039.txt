import datetime

date_format = "%Y:%m:%d"

d1 = tuple(int(x) for x in input().strip().split(':'))
d2 = tuple(int(x) for x in input().strip().split(':'))

date1 = datetime.datetime(*d1)
date2 = datetime.datetime(*d2)

diff = date2 - date1
print(diff.days)
