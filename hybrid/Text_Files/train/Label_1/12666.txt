#include <bits/stdc++.h>
using namespace std;
void solve() {
    int a[4], ans = 0;
    cin >> a[0] >> a[1] >> a[2] >> a[3];
    sort(a, a + 4);
    ans = a[2] + a[3] * 100 + a[1] * 1000 + a[0] * 10000;
    cout << ans << "\n";
}
int main() { solve(); }
