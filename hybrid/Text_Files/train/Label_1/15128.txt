#include <iostream>
#include <vector>

int main() {
    int n;
    std::cin >> n;
    std::vector<int> a(n - 1);
    int sum = 0;
    for (int i = 0; i < n - 1; i++) {
        std::cin >> a[i];
        sum += a[i] - i - 1;
    }
    std::cout << sum << '\n';
    return 0;
}
