#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  int bits = 0;
  cin >> n;
  while (n--) {
    int x;
    cin >> x;
    bits |= x;
  }
  cout << __builtin_popcount(bits) << endl;
  return 0;
}
