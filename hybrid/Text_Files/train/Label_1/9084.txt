#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  int a[n];
  int l = 1, mx = 0, mx_id = 0;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
    if (l == a[i]) l++;
    if (l > mx) {
      mx = l;
      mx_id = a[i];
    }
  }
  cout << mx_id << endl;
}
