#include <iostream>
using namespace std;
int main() {
  long long n, m, k;
  cin >> n >> m >> k;
  long long x = (k / (n - 1)) % 2;
  long long y = k % (n - 1) + 1;
  if (!x) y = n - y + 1;
  cout << y << ' ' << (x ? 1 : m);
}
