n, m = map(int, input().split())
c = list(map(int, input().split()))

columns = [0] * n
points = 0

for square in c:
    columns[square - 1] += 1
    if min(columns) > 0:
        points += 1
        columns = [x - 1 for x in columns]

print(points)
