import sys
input = sys.stdin.readline

n = int(input())
arr = sorted([(int(x) for x in input().split()) for _ in range(n)])
l, r = 0, 0
for x, y in arr:
  l = max(l, x)
  r = max(r, y)
print(l + r + 1)
