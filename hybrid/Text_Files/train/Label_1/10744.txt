#include <iostream>
using namespace std;
int main() {
  int a, b;
  cin >> a >> b;
  if (a > b) {
    cout << -1 << endl;
    return 0;
  }
  cout << (b - 1) * 10 + a << ' ' << b * 10 << endl;
  return 0;
}
