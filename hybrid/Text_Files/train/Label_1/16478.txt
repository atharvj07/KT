#include <bits/stdc++.h>
using namespace std;
int n;
string s;
int main() {
  cin >> n >> s;
  int ans = n;
  for (int i = 1; i < n; ++i) {
    if (s[i] == s[i - 1]) --ans;
  }
  cout << ans << endl;
  return 0;
}
