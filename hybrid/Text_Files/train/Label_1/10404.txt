#include <iostream>

using namespace std;

int gcd(long long int a, long long int b) {
  return b == 0 ? a : gcd(b, a % b);
}

int main() {
  long long int m, x;
  cin >> m >> x;
  if (gcd(x, m) == 1) {
    cout << m - 1 << endl;
  } else {
    cout << m << endl;
  }
}
