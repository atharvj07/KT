#include <iostream>
#include <string>
int main() {
  std::string s;
  std::cin >> s;
  std::cout << ((s.find("CODE") != std::string::npos && s.rfind("FORCES") != std::string::npos) ? "YES" : "NO")
            << std::endl;
  return 0;
}
