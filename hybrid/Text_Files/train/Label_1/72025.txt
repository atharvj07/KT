from collections import Counter

for _ in range(int(input())):
    n,k = map(int,input().split())
    s = Counter(list(map(int,input().split())))
    print(max(s.items(),key=lambda x:x[1])[0] + (k+s[0])//2)
