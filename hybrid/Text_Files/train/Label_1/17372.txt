#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
	ll x,y,t,i,sum=3;
	cin>>x>>y;
	x=abs(x);
	y=abs(y);
	if(x==0&&y==0)
		cout<<"0";
	else
	{
		if(x<=y)
		{
			if(x%2==1)
				sum=sum+(x*4)-4;
			else
				sum=sum+(x*4)-3;
		}
		else
		{
			if(y%2==1)
				sum=sum+(y*4)-4;
			else
				sum=sum+(y*4)-3;
		}
		cout<<sum;
	}
	
	return 0;
}
