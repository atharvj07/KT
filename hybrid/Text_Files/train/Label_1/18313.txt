#include <iostream>
using namespace std;
int main() {
  int n, m, k, r, d;
  char s;
  cin >> n >> m >> k;
  k--;
  d = k / m + 1;
  r = k % m + 1;
  s = k % 2 ? 'R' : 'L';
  cout << r << " " << d << " " << s << endl;
  return 0;
}
