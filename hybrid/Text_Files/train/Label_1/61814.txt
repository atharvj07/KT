from sys import stdin
input=stdin.readline
n=int(input().strip())
a=[int(x) for x in input().strip().split()]
import math
b=[0]*n
g=0
mx=0
for i in range(n):
    if a[i]:
        g=math.gcd(a[i],g)
    mx=max(mx,a[i])
if g==1 and mx>1:
    print(-1)
else:
    print(max(a)-min(a))
