#include <cstdio>
const int M = 4004; int u[M], v[M], n, m, x, p, ans[2];
int main(){
    scanf("%d%d", &x, &p); while (p--){
        scanf("%d%d%d", &n, u+p, v+p);
        if (n==1) ans[0]++, ans[1]++;
        else ans[v[p]>x]++, ans[0]--;
    }
    printf("%d %d\n", ans[0], ans[1]);
}
