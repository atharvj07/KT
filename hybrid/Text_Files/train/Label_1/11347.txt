#include <iostream>

int a, b, c, m;
int main() {
  int t;
  std::cin >> t;
  while (t--) {
    std::cin >> a >> b >> c >> m;
    std::cout << (m <= a + b + c - 1 ? "YES" : "NO") << std::endl;
  }
  return 0;
}
