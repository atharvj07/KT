#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
int main() {
    LL n;
    cin >> n;
    for (LL i = (LL)sqrt(n); i > 0; i--) {
        if (n % (i * i) == 0) {
            n = n / i;
        }
    }
    cout << n << endl;
    return 0;
}
