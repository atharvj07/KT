#include <iostream>
#include <algorithm>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int a[4];
    for (int &x : a) cin >> x;
    sort(a, a + 4);
    cout << (a[0] == a[1] || a[2] == a[3] ? "YES" : "NO") << endl;
  }
}
