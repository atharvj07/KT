#include<iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long n;
    cin >> n;
    long ma = 0;
    while (n--) {
      long a;
      cin >> a;
      ma = max(ma, (a + 2) / 3);
    }
    cout << ma << "\n";
  }
}
