#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int mod = 998244353;
const int maxn = 1e9+7;
int main() {
  ll l, r, x, y;
  cin >> l >> r >> x >> y;
  if (x == y) cout << (r - l + 1) / x << endl;
  else cout << ((r / x) - (l - 1) / x) * ((r / y) - (l - 1) / y) << endl;
}
