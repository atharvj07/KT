import sys

s = sys.stdin.readline().strip()
m = int(sys.stdin.readline())
for _ in range(m):
    l, r = map(int, sys.stdin.readline().split())
    if s[l-1:r].find('xyz') + s[l-1:r].find('zyx') + s[l-1:r].find('xzy') == -3:
        print('YES')
    else:
        print('NO')
