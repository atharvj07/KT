t = int(input())
for _ in range(t):
    s = input()
    if s[0] == s[-1]:
        print(0)
    else:
        first = s[0]
        last = s[-1]
        first_occurrence = s.find(last)
        last_occurrence = s.rfind(first)
        print(min(first_occurrence, len(s) - 1 - last_occurrence))
