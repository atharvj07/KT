import sys, math

for _ in range(int(sys.stdin.readline())):
    n = int(sys.stdin.readline())
    s = sys.stdin.readline().strip()
    numL = math.ceil(n/2)
    numR = s.count(")")
    if numR > numL:
        print ("Yes")
    else:
        print ("No")
