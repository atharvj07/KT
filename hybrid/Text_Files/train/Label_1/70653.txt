import math

def solve():
    a, h, w = map(int, input().split())
    if (h % a != 0 or w % a != 0):
        return -1
    x = min(h / a - 1, w / a - 1)
    x = x * a / math.gcd(a, h / (x + 1))
    return x

print(solve())
