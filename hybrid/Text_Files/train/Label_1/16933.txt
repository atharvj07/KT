#include <iostream>
using namespace std;
int a[105], ans;
int main() {
  int k = 0;
  string s;
  cin >> s;
  for (int i = s.size() - 1; i >= 0; i--) {
    if (s[i] == '1') {
      ans += a[k];
      a[k + 1] = a[k] * 4 + 1;
      k++;
    }
  }
  cout << ans;
  return 0;
}
