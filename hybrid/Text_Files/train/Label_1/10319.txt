#include <bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  int odd = 0, n = s.size();
  for (int i = 0; i < 26; ++i) {
    odd += (s[i] - 'a') % 2;
  }
  if (n % 2 == 0)
    odd = !odd;
  cout << (odd ? "Second" : "First") << endl;
  return 0;
}
