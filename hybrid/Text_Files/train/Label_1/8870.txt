#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n;
  cin >> n;
  int k1, k2;
  cin >> k1;
  vector<int> a(k1);
  for (int i = 0; i < k1; i++) { cin >> a[i]; }
  cin >> k2;
  vector<int> b(k2);
  for (int i = 0; i < k2; i++) { cin >> b[i]; }
  int res = 0, winner = 0;
  while (a.size() && b.size()) {
    res++;
    if (a[0] > b[0]) {
      a.push_back(b[0]);
      a.push_back(a[0]);
      b.erase(b.begin());
      a.erase(a.begin());
    } else {
      b.push_back(a[0]);
      b.push_back(b[0]);
      a.erase(a.begin());
      b.erase(b.begin());
    }
  }
  if (a.empty())
    cout << res << " 2" << endl;
  else if (b.empty())
    cout << res << " 1" << endl;
  else
    cout << -1 << endl;
  return 0;
}
