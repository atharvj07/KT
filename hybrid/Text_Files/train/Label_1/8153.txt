#include <bits/stdc++.h>
using namespace std;

#define ll long long

int32_t main() {
    ll a, b, g;
    cin >> a >> b;
    g = __gcd(a, b);
    ll l = (a * b) / g;
    ll ans = (l - a) % l;
    cout << ans << endl;
}
