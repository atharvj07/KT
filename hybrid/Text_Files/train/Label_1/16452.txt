#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    long long l, r, x, y, k;
    cin >> l >> r >> x >> y >> k;
    if (x <= k * l && k * r <= y)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
    return 0;
}
