#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int k;
    cin >> k;
    int x = k + 1, y = 1;
    while (x--) {
      y += (y % 10 != 3 && y % 3 != 0);
    }
    cout << y - 1 << endl;
  }
  return 0;
}
