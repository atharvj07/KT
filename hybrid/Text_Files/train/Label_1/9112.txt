#include <bits/stdc++.h>
using namespace std;
int main() {
  int cur = getchar(), ret = 0;
  while (cur != EOF) {
    while (cur != EOF && cur == getchar())
      ;
    ret++;
    cur = getchar();
  }
  cout << ret << endl;
}
