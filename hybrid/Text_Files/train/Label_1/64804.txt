n,k,m=map(int,input().split());a=list(map(int,input().split()))
b=[0]*n;ind=[];ans=sum(a)
for i in range(n):
    b[i]=a[i];ind+=[i];ans=max(ans,a[i]*n);
    for j in range(k):
        a[i]+=1;ans=max(ans,sum(a)//(n-j-1))
        if j+1==k:ind[i]=0
print('{0:0.12f}'.format(ans/n))
