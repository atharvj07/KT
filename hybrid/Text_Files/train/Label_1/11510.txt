#include <cstdio>
#include <algorithm>

using namespace std;

int main() {
	int c, d, n, m, k;
	scanf("%d %d %d %d %d", &c, &d, &n, &m, &k);
	printf("%d\n", max(0, n * m - (c * n + d * m - k)));
	return 0;
}
