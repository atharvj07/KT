#include <iostream>
#include <set>
using namespace std;
int main() {
  int y;
  cin >> y;
  while (true) {
    set<int> digits;
    y++;
    for (int x = y; x > 0; x /= 10) digits.insert(x % 10);
    if (digits.size() == 4) break;
  }
  cout << y << endl;
  return 0;
}
