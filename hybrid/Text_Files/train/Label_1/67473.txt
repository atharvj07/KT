def find_number(n, x):
    if x % 2 == 1:
        return (x + 1) // 2
    else:
        return find_number(n // 2, x // 2) + n // 2

n, q = map(int, input().split())
for _ in range(q):
    x = int(input())
    print(find_number(n, x))
