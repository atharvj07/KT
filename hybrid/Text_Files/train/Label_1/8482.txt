#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    ll n;
    cin>>n;
    ll h[n];
    for(ll i=0;i<n;i++)
    cin>>h[i];
    stack<ll>st;
    for(ll i=n-1;i>=0;i--)
    {
        while(!st.empty()&&st.top()<=h[i])
        st.pop();
        if(st.empty())
        {
            cout<<h[i]<<" ";
            continue;
        }
        st.push(h[i]);
    }
    return 0;
}
