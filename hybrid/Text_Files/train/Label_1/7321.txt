#include <iostream>
int main() {
  int t;
  long long a, b;
  std::cin >> t;
  for (int i = 0; i < t; i++) {
    std::cin >> a >> b;
    std::cout << (((a+b) & 1) ? "NO\n" : "YES\n");
  }
  return 0;
}
