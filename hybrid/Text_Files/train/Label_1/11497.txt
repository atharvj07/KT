#include <iostream>
#include <vector>
using namespace std;
int main() {
  vector<int> v(4);
  for (int i = 0; i < 4; ++i) cin >> v[i];
  cout << 1ll * (v[3] - v[2] + 1) * (v[3] - v[1] + 1) * (v[3] - v[0] + 1) / 6
       << endl;
  return 0;
}
