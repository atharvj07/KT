#include <iostream>
#include <cmath>
 
using namespace std;
 
int main() {
  long long s;
  cin >> s;
  int ans = 0;
  for (int i = 0; i < 100; i++)
    if ((s >> i) & 1)
      ans += (1 << i);
  cout << ans << endl;
}
