#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    ll s, x;
    cin >> s >> x;
    if (s < x || (s - x) % 2)
        cout << 0 << endl;
    else
        cout << (s - x) / 2 + 1 << endl;
    return 0;
}
