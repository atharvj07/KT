import sys

t = int(input())

for _ in range(t):
    n, l, r, k = map(int, input().split())
    a = list(map(int, input().split()))

    cnt = 0
    cost = 0
    for x in a:
        if l <= x <= r:
            if cost + x <= k:
                cnt += 1
                cost += x
    print(cnt)
