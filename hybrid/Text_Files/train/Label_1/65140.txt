def get_fib_len(arr):
    n = len(arr)
    f = [0] * n
    f[0] = 1
    f[1] = 1
    for i in range(2, n):
        f[i] = f[i - 1] + f[i - 2]
    i = 0
    while i < n and arr[i] == f[i]:
        i += 1
    return i
input()
print(get_fib_len(list(map(int, input().split()))))
