#include <cstdio>

using namespace std;

int main() {
  int cnt;
  scanf("%d", &cnt);
  while (cnt--) {
    int x;
    scanf("%d", &x);
    if (x >= 3) {
      puts("NO");
      return 0;
    }
  }
  puts("YES");
  return 0;
}
