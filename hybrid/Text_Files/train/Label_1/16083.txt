#include <iostream>
#include <vector>
using namespace std;
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
  int n, h, k, t = 0;
  cin >> n >> h >> k;
  while (n--) {
    int ai;
    cin >> ai;
    t += (ai + k - 1) / k;
  }
  cout << t << '\n';
  return 0;
}
