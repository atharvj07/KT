import sys
input = sys.stdin.readline

n, m = map(int, input().split())
name = input()[:-1]
trans = [input().split() for _ in range(m)]
for x, y in trans:
    name = name.replace(x, '%').replace(y, x).replace('%', y)
print(name)
