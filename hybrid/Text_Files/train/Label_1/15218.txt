#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        long long a, b, n;
        cin >> a >> b >> n;
        if (n == 0) cout << a << endl;
        else if (n == 1) cout << b << endl;
        else cout << (a ^ b) << endl;
    }
}
