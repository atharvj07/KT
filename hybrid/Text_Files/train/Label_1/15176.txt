#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int a[100001];
ll b[100001];
ll c[100001];
int main(){
   ll n,m,ans=0; cin>>n;
   for(int i=0;i<n;i++){
       cin>>a[i];
       b[i]=a[i];
   }
   sort(a,a+n);
   for(int i=0;i<n;i++){
       if(b[i]==a[i]) ans++;
   }
   cout<<ans<<endl;
   return 0;
}
