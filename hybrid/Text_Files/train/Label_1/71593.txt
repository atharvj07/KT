import sys

n, m = map(int, sys.stdin.readline().split())
dances = []
for i in range(m):
    dances.append(list(map(int, sys.stdin.readline().split())))

colors = [0] * (n + 1)
for dance in dances:
    colors[dance[0]] = 1
    colors[dance[1]] = 2
    colors[dance[2]] = 3

print(*colors[1:])
