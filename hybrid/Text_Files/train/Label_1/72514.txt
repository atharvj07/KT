for _ in range(int(input())):
    n, k = map(int, input().split())
    s = str(n)
    dig = set()
    for c in s:
        dig.add(c)
    if len(dig) <= k:
        print(n)
        continue
    ans = []
    for c in s:
        ans.append(str(min(k, int(c) + 1)))
    print(''.join(ans))
