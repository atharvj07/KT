#include <bits/stdc++.h>
using namespace std;
using ll = long long;
int main() {
  ios_base::sync_with_stdio(false), cin.tie(NULL);
  ll w, m, k;
  cin >> w >> m >> k;
  ll ans = 0;
  ll rem = m;
  while (ans * k <= w) { rem++; ans++; }
  cout << ans - 1 << '\n';
  return 0;
}
