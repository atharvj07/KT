import math
n=int(input())
a=[]
for i in range(n):a.append(list(map(int,input().split())))
a.sort(key=lambda x:max(x))
ans=0
x=0
y=0
for i in a:
    ans+=abs(i[0]-x)+abs(i[1]-y)
    x=i[0]
    y=i[1]
print(ans)
