#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long n, k, d1, d2;
    std::cin >> n >> k >> d1 >> d2;
    if (k % 2 == d1 % 2 && (k + d1 + d2) % 3 == 0)
      std::cout << "yes\n";
    else
      std::cout << "no\n";
  }
  return 0;
}
