t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    a.sort()
    min_diff = a[-1] - a[0]
    for i in range(1, n):
        min_diff = min(min_diff, a[-1] - a[i] + a[i-1] - a[0])
    print(min_diff)
