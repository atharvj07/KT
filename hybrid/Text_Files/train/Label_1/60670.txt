import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    a.sort(reverse=True)
    if a[0] % a[1] == 0 or a[1] % a[2] == 0:
        print(a[0] + a[1])
    else:
        print(a[0] + a[1] + a[2])
