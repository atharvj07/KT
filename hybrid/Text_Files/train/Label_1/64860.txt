from math import gcd

n = int(input())
a = list(map(int, input().split()))

ans = []
last = float('inf')
for i in range(n):
    for j in range(i + 1, n):
        cur = a[i] * a[j] // gcd(a[i], a[j])
        if cur < last:
            last = cur
            ans = [i + 1, j + 1]

print(*ans)
