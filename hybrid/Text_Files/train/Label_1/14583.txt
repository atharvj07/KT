#include <bits/stdc++.h>
using namespace std;
set<string> st;
int main() {
  int n;
  cin >> n;
  while (n--) {
    string s;
    cin >> s;
    if (st.count(s)) {
      cout << "YES" << endl;
    } else {
      cout << "NO" << endl;
      st.insert(s);
    }
  }
  return 0;
}
