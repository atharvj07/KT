#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, k, d1, d2;
    cin >> n >> k >> d1 >> d2;
    if ((d1 + d2) > (n - k))
      cout << "no\n";
    else
      cout << "yes\n";
  }
  return 0;
}
