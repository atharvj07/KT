import sys
input=sys.stdin.readline

def f(p):
    return sorted([p[i]+p[i+1] for i in range(len(p)-1)])

for T in range(int(input())):
    n=int(input())
    a=list(map(int,input().split()))
    b=a.copy()
    b[0],b[-1]=b[-1],b[0]
    if f(b)==f(a): b[1],b[-2]=b[-2],b[1]
    print(*b)
