import java.util.Scanner;

public class MishaAndVasya {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        int d = scanner.nextInt();

        int mishaPoints = getPoints(a, c);
        int vasyaPoints = getPoints(b, d);

        if (mishaPoints > vasyaPoints) {
            System.out.println("Misha");
        } else if (vasyaPoints > mishaPoints) {
            System.out.println("Vasya");
        } else {
            System.out.println("Tie");
        }
    }

    private static int getPoints(int cost, int time) {
        return Math.max(0, cost - (time / 250) * 100);
    }
}
