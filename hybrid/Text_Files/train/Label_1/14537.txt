#include <string>
#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    string s;
    cin >> s;
    int ans = 0;
    int cnt = 0;
    for (char c : s) {
      cnt += c == '1';
      ans += c == '0' && cnt > 0;
    }
    cout << ans << endl;
  }
}
