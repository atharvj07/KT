#include <bits/stdc++.h>

using namespace std;

int main() {
	long long n, k, m, d;
	cin >> n >> k >> m >> d;
	if (n % (k * d) == 0)
		cout << (n / (k * d)) * m;
	else
		cout << (n / (k * d) + 1) * m;
	return 0;
}
