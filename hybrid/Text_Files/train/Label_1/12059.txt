#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

const int MOD = 1e9 + 7;

int main() {
  int f, w, h;
  cin >> f >> w >> h;
  ll ans = 1;
  if (h > 1) {
    ans = (2 * (f + w)) % MOD;
  } else {
    ans = f % MOD;
  }
  cout << ans << endl;
  return 0;
}
