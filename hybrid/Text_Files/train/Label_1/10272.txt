#include <bits/stdc++.h>

using namespace std;

int main() {
    int a[4];
    for (int &i : a) cin >> i;
    string s;
    cin >> s;
    int ans = 0;
    for (char c : s) ans += a[c - '1' - 1];
    cout << ans << endl;
    return 0;
}
