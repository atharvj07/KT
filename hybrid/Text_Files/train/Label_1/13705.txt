#include <iostream>
#include <vector>
using namespace std;
int main() {
    int n, cur = 0;
    cin >> n;
    vector<int> fr(n);
    for (int i = 0; i < n; ++i) cin >> fr[i];
    for (int i = 1; i < n; ++i) cur += abs(fr[i] - fr[i - 1]);
    cout << cur << endl;
    return 0;
}
