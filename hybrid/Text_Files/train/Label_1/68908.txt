import sys
input=sys.stdin.readline
k=int(input())
s=input()
ans,pre=0,0
for c in s:
    if c==' ':pre+=1
    elif c=='-':pre=max(pre,ans)+1
    else:
        ans=max(ans,pre+1)
        pre=0
print(max(ans,pre+1))
