#include <iostream>
#include <cstdio>
#include <limits.h>
using namespace std;
int main() {
  long long a, b, x, y, z;
  cin >> a >> b >> x >> y >> z;
  cout << max(max(0ll, x * 2 - a), max(0ll, (y + z - a - b) / 2)) << endl;
}
