#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,cnt=1,f=0;
    cin>>n;
    int x[n];
    for(int i=0;i<n;i++)
    {
        cin>>x[i];
        if(i>0&&x[i]!=x[i-1])
            cnt++;
    }
    if(cnt%2)
        cout<<"NO"<<endl;
    else
        cout<<"YES"<<endl;
}
