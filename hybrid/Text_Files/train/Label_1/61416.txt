n, a, b = map(int, input().split())
s = input()
dp = [a] * (n + 1)
for i in range(1, n + 1):
    for j in range(0, i):
        if s[j:i] in s[:j]:
            dp[i] = min(dp[i], dp[j] + b)
        else:
            dp[i] = min(dp[i], dp[j] + a)
print(dp[n])
