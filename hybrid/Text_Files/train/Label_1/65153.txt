import sys

points = [list(map(int, line.split())) for line in sys.stdin.readlines()[1:]]
points.sort(key=lambda x: x[0])

n = len(points)
ans = 1
cur = points[0][1]
for i in range(1, n):
    if points[i][0] - cur >= points[i][1]:
        ans += 1
        cur = points[i][1]

print(ans)
