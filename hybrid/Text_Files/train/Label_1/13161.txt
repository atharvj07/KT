#include <iostream>
#include <set>
using namespace std;
int main() {
  set<int> X, Y;
  for (int i = 0; i < 8; i++) {
    int x, y;
    cin >> x >> y;
    X.insert(x), Y.insert(y);
  }
  cout << (X.size() == 3 && Y.size() == 3 ? "respectable" : "ugly") << "\n";
}
