n = int(input())
a = [tuple(map(int,input().split())) for i in range(n)]
a.sort(key=lambda x:x[0])
can = [False]*1001
for x,y in a:
    if can[x]: continue
    can[x] = True
    can[y] = True
print(n-can.count(True))
