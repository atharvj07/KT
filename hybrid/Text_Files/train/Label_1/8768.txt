#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    vector<int> s(4);
    for (int &x : s) cin >> x;
    sort(begin(s), end(s));
    cout << (s[2] == s[1] + s[0] ? "YES" : "NO") << endl;
  }
  return 0;
}
