#include <iostream>
using namespace std;
int main() {
    long long t, a, b, k, ans;
    cin >> t;
    while (t--) {
        cin >> a >> b >> k;
        ans = (k + 1) * (a - b);
        cout << ans / 2 << endl;
    }
}
