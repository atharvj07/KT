#include<iostream>
#include<vector>
using namespace std;
int main() {
  int n; cin >> n;
  vector<int> a(n);
  for (auto& x : a) cin >> x;
  for (int i = 1; i < n; ++i) if ((a[i - 1] + 1) % n != a[i]) { cout << "No"; return 0; }
  cout << "Yes";
}
