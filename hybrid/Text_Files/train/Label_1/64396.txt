n, m = map(int, input().split())
d = {}
for _ in range(m):
    a, b = input().split()
    if len(a) < len(b):
        d[a] = a
        d[b] = a
    elif len(b) < len(a):
        d[a] = b
        d[b] = b
    else:
        d[a] = a
        d[b] = a
print(*map(lambda x: d[x], input().split()))
