#include <iostream>

using namespace std;

int main() {
  long long n, a, b;
  cin >> n >> a >> b;
  if ((n - b) % (a + b))
    cout << "NO" << endl;
  else {
    cout << "YES" << endl;
    cout << (n - b) / (a + b) << " " << (n - b) % (a + b) / b << endl;
  }
  return 0;
}
