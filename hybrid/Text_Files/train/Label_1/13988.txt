#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int n, m, k;
    std::cin >> n >> m >> k;
    std::cout << ((n - 1 + m - 1 == k) ? "YES" : "NO") << '\n';
  }
  return 0;
}
