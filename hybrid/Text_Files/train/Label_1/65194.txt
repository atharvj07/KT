from collections import defaultdict
n = int(input())
seg = defaultdict(int)
for i in range(n):
    l, r = map(int, input().split())
    seg[l] += 1
    seg[r+1] -= 1
ans = cur = 0
for i in range(101):
    if not i in seg: continue
    cur += seg[i]
    if cur == 1: ans += 1
print(ans)
