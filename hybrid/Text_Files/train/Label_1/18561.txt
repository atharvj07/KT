#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;
    for (long long i = sqrt(n); i >= 1; i--) {
        if (n % (i * i) == 0) {
            n /= i * i;
            break;
        }
    }
    cout << n << endl;
    return 0;
}
