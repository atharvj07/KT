import sys

n, sx, sy = map(int, input().split())
a = [[sx, sy]]
for _ in range(n):
    x, y = map(int, input().split())
    a.append([x, y])
a.sort()

x = a[0]
y = a[-1]
x1 = x[0] + (y[0] - x[0]) // 2
y1 = x[1] + (y[1] - x[1]) // 2
print(n - 1)
print(x1, y1)
