n = int(input())
a = [int(x) for x in input().split()]
d = {}
for x in a:
    d[x] = d.get(x, 0) + 1
mx = 0
for x in d:
    y = d.get(x + 1, 0) + d.get(x - 1, 0)
    if y > mx:
        mx = y
print(mx + d.get(mx, 0))
