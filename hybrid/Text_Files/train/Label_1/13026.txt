#include <bits/stdc++.h>
using namespace std;
int t, a;
int main() {
  cin >> t;
  while (t--) {
    cin >> a;
    if (180 % a == 0 && a != 180)
      cout << 180 / a << endl;
    else if (360 % a == 0)
      cout << 360 / a << endl;
    else
      cout << -1 << endl;
  }
  return 0;
}
