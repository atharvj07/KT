#include <iostream>
#include <string>
using namespace std;
int main() {
  string a, b;
  cin >> a >> b;
  int i = 0, j = 0;
  while (i < a.size() && j < b.size()) {
    if (a[i] == b[j]) { i++; }
    j++;
  }
  if (i == a.size()) cout << b.substr(0, j);
  else cout << "-";
  return 0;
}
