#include <bits/stdc++.h>
using namespace std;
int main() {
  long long l, r;
  cin >> l >> r;
  if ((r - l + 1) % 2)
    cout << "NO", exit(0);
  cout << "YES" << endl;
  for (long long i = l; i <= r; i += 2)
    cout << i << " " << i + 1 << endl;
}
