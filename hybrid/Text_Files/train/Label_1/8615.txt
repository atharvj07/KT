#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n;
  string s;
  cin >> n >> s;
  vector<int> freq(26, 0);
  int mx = 0;
  for (char c : s) {
    freq[c - 'a']++;
    mx = max(mx, freq[c - 'a']);
  }
  cout << n - mx + 1 << "\n";
}
