n, m = map(int, input().split())
a = sorted([(c, w) for w, c in (map(int, input().split()) for _ in range(n))], reverse=True)
ans = 0
for c, w in a:
    if w <= m:
        ans += c
        m -= w
    else: break
print(ans)
