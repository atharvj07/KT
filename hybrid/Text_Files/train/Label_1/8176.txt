#include <iostream>
#include <string>

using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;
  int gi = s.find('G');
  int ti = s.find('T');
  cout << ((abs(gi - ti) <= k) ? "YES" : "NO") << endl;
  return 0;
}
