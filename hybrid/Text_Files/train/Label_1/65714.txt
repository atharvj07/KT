import sys
input = sys.stdin.readline

n = int(input())
P = []
for i in range(n):
    sex, a, b = input().split()
    P += [(int(a), int(b), sex == 'F')]
P.sort()
 
L, ans = [], 0
for p in P:
    a, b, f = p
    if f: L.append((b, 1))
    else: ans += 1 + L.pop(0)[1]
    L.sort()
print(ans)
