n = int(input())
d = {}
for i in range(n):
    s = input()
    for j in range(6):
        for k in range(10):
            if j == 0 and k == 0:
                continue
            t = s[:j] + str(k) + s[j+1:]
            if t not in d:
                d[t] = s
print(min(len(s) for s in d))
