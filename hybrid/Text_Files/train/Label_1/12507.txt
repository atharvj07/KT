#include <bits/stdc++.h>
using namespace std;

int main() {
  int t, n, x, sum;
  cin >> t;
  while (t--) {
    cin >> n >> x, sum = 0;
    for (int i = 0; i < n; i++) {
      int a;
      cin >> a;
      sum += (a % x == 0) ? (a / x) * x : a;
    }
    cout << sum << '\n';
  }
  return 0;
}
