#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long r, b, d;
    std::cin >> r >> b >> d;
    long long diff = std::abs(r - b);
    if (std::min(r, b) >= 1 && diff <= d)
      std::cout << "YES\n";
    else
      std::cout << "NO\n";
  }
  return 0;
}
