def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

n = int(input().strip())
numbers = list(map(int, input().strip().split()))

result = numbers[0]
for i in range(1, n):
    result = gcd(result, numbers[i])

print(result * n)
