import sys
input = sys.stdin.readline
for _ in range(int(input())):
    a, b, c, d = map(int, input().split())
    if b >= a:
        print(b)
    else:
        x = (a - b + d - 1) // d
        k = b + c * x + d * x
        if k >= a:
            print(k)
        else:
            print(-1)
