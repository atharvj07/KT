#include <cstdio>
int main() {
  int n;
  scanf("%d", &n);
  for (int i = 1, c; i <= n; i++) {
    scanf("%d", &c);
    if (c == i) return puts("YES"), 0;
    scanf("%d", &c);
    if (c == i) return puts("YES"), 0;
  }
  puts("NO");
  return 0;
}
