#include <iostream>

using namespace std;

int main() {
  int n;
  cin >> n;
  while (n--) {
    long long a, b;
    cin >> a >> b;
    int ans = 0;
    while (a != 0 && b != 0) {
      if (a < b) swap(a, b);
      ans += a / b;
      a %= b;
    }
    cout << ans << endl;
  }
  return 0;
}
