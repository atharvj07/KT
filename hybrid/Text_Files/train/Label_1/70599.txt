import sys

n = int(input())
orders = [list(map(int, input().split())) for _ in range(n)]
orders.sort(key = lambda o: o[1])

cnt = 0
last = -1
for l, r in orders:
    if l >= last:
        cnt += 1
        last = r
print(cnt)
