n, m = map(int, input().split())
t = list(map(int, input().split()))

ans = [0] * n
for i in range(n):
    sum = t[i]
    for j in range(i+1, n):
        if sum + t[j] <= m:
            sum += t[j]
            ans[j] += 1
        else:
            break
    print(ans[i])
