def find_polynomial(p, k):
    coefficients = []
    while p:
        coefficients.append(p % k)
        p = -(p // k)
    return coefficients

p, k = map(int, input().split())
coefficients = find_polynomial(p, k)
print(len(coefficients))
print(' '.join(map(str, coefficients)))
