import sys
vowels = {'a', 'e', 'i', 'o', 'u'}

def vowelly(k):
  for n in range(1, int(k**0.5)+1):
    if k%n==0 and all(vowels.issubset(set(s)) for s in ['']*k[::k//n] + ['']*n):
      return ''.join(map(''.join, zip(*[iter(vowels*k)]*n)))
  return -1

print(vowelly(int(sys.stdin.read())))
