#include <bits/stdc++.h>
using namespace std;

int n, b, p, x, y;

int main() {
    cin >> n >> b >> p;
    while (n != 1)
        n = (n + 1) / 2, x += n * b;
    cout << x << ' ' << n * p << endl;
    return 0;
}
