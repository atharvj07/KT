#include <iostream>

using namespace std;

int main() {
  long long n;
  cin >> n;
  long long ans = 0, limit = 0;
  for (long long i = 1; i <= n; i++) {
    if (i > limit) {
      limit += ++ans;
    }
  }
  cout << ans << endl;
  return 0;
}
