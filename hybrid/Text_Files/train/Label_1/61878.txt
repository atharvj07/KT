n, k = map(int, input().split())
f = list(map(int, input().split()))
f.sort()
ans = 0
for j in range(0, n, k):
    ans += min(f[j + k - 1], 2 * f[j])
    for i in range(j + 1, j + k):
        ans += abs(f[i] - f[i - 1])
print(ans)
