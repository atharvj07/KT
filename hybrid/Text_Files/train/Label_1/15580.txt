#include <iostream>
#include <limits>
#include <vector>

using namespace std;

int main() {
  long long A, B, x, y, z;
  cin >> A >> B >> x >> y >> z;
  cout << (max(y, 0ll) - min(A, x) + 1 + max(z, 0ll) - min(B, z) + 1) << endl;
  return 0;
}
