#include <iostream>
using namespace std;
int main() {
  int n;
  cin >> n;
  int c = 0;
  for (long long i = 1; i <= n; i *= 10) {
    c += (n / i) * i;
    c += min(i, n % i);
  }
  cout << c << endl;
}
