n, k = map(int, input().split())
s = sorted([input() for i in range(n)], key=len)
def f(x, k):
    for t in s:
        if t.startswith(x):
            if k == 1: return x[-1]
            else: return f(t, k - 1)
    return -1
print('First' if f(s[0], k - 1) != -1 else 'Second')
