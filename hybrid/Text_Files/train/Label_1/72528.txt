x1, y1, x2, y2 = map(int, input().split())
x, y = map(int, input().split())

dx = abs(x2 - x1)
dy = abs(y2 - y1)

if dx % x == 0 and dy % y == 0 and (dx // x) % 2 == (dy // y) % 2:
    print("YES")
else:
    print("NO")
