#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int l, r;
        cin >> l >> r;
        if (2 * l > r + 2)
            cout << "NO" << endl;
        else
            cout << "YES" << endl;
    }
    return 0;
}
