#include <bits/stdc++.h>
using namespace std;

string s;
set<char> allow{'e', 'i', 'n', 't'};
int solve() {
  int cnt = 0;
  for (auto c : s) {
    if (!allow.count(c)) {
      return cnt;
    }
    cnt += c == 'n';
  }
  return cnt / 7;
}
int main() {
  cin >> s;
  cout << solve() << endl;
}
