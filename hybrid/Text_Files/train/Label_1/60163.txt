from sys import stdin

n = int(stdin.readline())
reqs = []
for i in range(n):
    s, d = map(int, stdin.readline().split())
    reqs.append((s, d))

reqs.sort()
start = 0
for s, d in reqs:
    if start < s:
        start = s
    print(start, start + d - 1)
    start += d
