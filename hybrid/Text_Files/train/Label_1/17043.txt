#include <bits/stdc++.h>
using namespace std;
int main() {
  int a[27] = {}, c = 0;
  for (char x; cin >> x;) c |= 1 << x - 'a', a[x - 'a']++;
  cout << (c == (1 << a[0]) && count(a, a + 26, 0) == 25 ? "YES" : "NO") << endl;
}
