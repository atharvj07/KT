import sys

input = sys.stdin.readline

k = int(input())
a = []
for i in range(k):
    n = int(input())
    a.append([int(j) for j in input().split()])

for i in range(k):
    sum1 = sum(a[i])
    for j in range(len(a[i])):
        sum2 = sum1 - a[i][j]
        for t in range(k):
            if t != i and sum(a[t]) - a[t][j] == sum2:
                print("YES")
                print(i + 1, j + 1)
                print(t + 1, j + 1 if sum2 < sum(a[t]) - a[t][j] else j)
                sys.exit()

print("NO")
