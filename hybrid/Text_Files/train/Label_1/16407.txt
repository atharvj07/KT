#include <bits/stdc++.h>

using namespace std;

bool vis[1005];

int32_t main() {
  int n;
  cin >> n;
  unordered_map<string, bool> mp;
  for (int i = 0; i < n; i++) {
    string s;
    cin >> s;
    sort(s.begin(), s.end());
    mp[s] = true;
  }
  cout << mp.size() << endl;
}
