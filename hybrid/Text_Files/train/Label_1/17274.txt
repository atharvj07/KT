#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long x0, n;
    std::cin >> x0 >> n;
    long long ans = x0 + (n * (n + 1) / 2) * (n % 2 ? 1 : -1);
    std::cout << ans << std::endl;
  }
}
