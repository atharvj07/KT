from typing import List

def solve(n: int, a: List[int]) -> int:
    a.sort()
    ans = 0
    for i in range(1, n):
        if sum(a[:i]) == sum(a[i:]):
            ans += 1
    return ans

n = int(input())
a = list(map(int, input().split()))
print(solve(n, a))
