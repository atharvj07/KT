from collections import Counter

for _ in range(int(input())):
    n = int(input())
    skills = Counter(map(int, input().split()))
    ans = min(v//2 for v in skills.values())
    print(ans if ans >= 1 else 0)
