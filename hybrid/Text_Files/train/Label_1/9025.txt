#include <bits/stdc++.h>

using namespace std;

int main() {
  int q;
  cin >> q;
  while (q--) {
    long long l1, r1, l2, r2;
    cin >> l1 >> r1 >> l2 >> r2;
    cout << (l1 == r1 ? l1 : min(r1, l2)) << " " << (l2 == r2 ? l2 : max(l2, r1)) << "\n";
  }
  return 0;
}
