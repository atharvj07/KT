import sys

t = int(input())

for _ in range(t):
    n, m = map(int, input().split())
    a = list(map(int, input().split()))
    b = list(map(int, input().split()))
    c = list(map(int, input().split()))
    if len(set(c)) < n or len(set(a)) != len(set(b)):
        print("NO")
        continue

    planks = {}
    for i in range(n):
        planks[a[i]] = i

    ans = []
    for i in c:
        if i in b and planks[i] != -1:
            ans.append(planks[i] + 1)
            planks[i] = -1
        else:
            print("NO")
            break
    else:
        print("YES")
        print(*ans)
