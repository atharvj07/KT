#include <iostream>

using namespace std;

int main() {
  long long n, m;
  cin >> n >> m;
  cout.precision(15);
  double p1 = 1.0 / n;
  double p2 = 1.0 / n * (1.0 / n - 1.0);
  double p3 = 1.0 - p1 - p2;
  double prob = p1 * p1 + p2 * p3 / 2.0;
  cout << prob << endl;
  return 0;
}
