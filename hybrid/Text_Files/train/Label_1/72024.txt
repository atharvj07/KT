from sys import stdin
input = stdin.readline

for t in range(int(input())):
    n, k = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    print(k // 2 + 1 if a[0] == 0 else k // 2 + 2 if a[0] == 1 else k // 2 + 3 if a[0] == 2 else n + k)
