#include <iostream>
#include <string>

using namespace std;

int main() {
  long long n, ans = 0, pow = 1;
  string k;
  cin >> n >> k;
  for (int i = k.length() - 1; i >= 0; i--) {
    ans += (k[i] - '0') * pow;
    pow *= n;
  }
  cout << ans << endl;
}
