#include <iostream>

using namespace std;

int main() {
  int a, b, c;
  cin >> a >> b >> c;
  a %= b;
  int pos = 0;
  while (a) {
    a *= 10;
    int d = a / b;
    pos++;
    if (d == c) {
      cout << pos << endl;
      return 0;
    }
    a %= b;
  }
  cout << -1 << endl;
  return 0;
}
