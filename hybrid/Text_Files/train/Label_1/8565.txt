#include <iostream>

using namespace std;

int main() {
    int n, s;
    cin >> n >> s;
    int mx = -1;
    while (n--) {
        int x, y;
        cin >> x >> y;
        if (s - x >= 0 && y > mx) mx = y;
    }
    cout << mx << endl;
    return 0;
}
