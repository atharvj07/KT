from sys import stdin

n = 500000
a = [0] * n
q = int(stdin.readline())

for i in range(q):
    t, x, y = map(int, stdin.readline().split())
    x -= 1
    if t == 1:
        a[x] += y
    elif t == 2:
        print(sum(a[i] for i in range(n) if i % x == y))
