#include <bits/stdc++.h>
using namespace std;

int main() {
  long long n, h;
  cin >> n >> h;
  double dh = (double)h / (n - 1);
  for (int i = 1; i < n; i++) {
    double x = sqrt((double)i / n) * h;
    printf("%.12lf ", x);
  }
  return 0;
}
