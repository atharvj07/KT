#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
    int n;cin>>n;
    ll a[n];
    for(int i=0;i<n;i++)cin>>a[i];
    int cnt=0;
    for(int i=1;i<n;i++){
        if(a[i]==1)cnt++;
    }
    if(cnt>=2)cout<<"YES"<<endl;
    else cout<<"NO"<<endl;
    return 0;
}
