#include <iostream>
#include <vector>
using namespace std;
int main() {
    string u, v;
    int x;
    cin >> x >> u >> v;
    if (u == "of")
        if (v == "week")
            cout << 52 << endl;
        else
            cout << 11 << endl;
    else
        if (v == "week")
            cout << x << endl;
        else
            cout << 2 * (x + 1) / 2 - (x / 7) << endl;
    return 0;
}
