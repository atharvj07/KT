t = int(input())
for _ in range(t):
    n, m = map(int, input().split())
    s = input()
    p = list(map(int, input().split())) + [n]
    
    counts = [0]*26
    for position in p:
        for i in range(position):
            counts[ord(s[i]) - ord('a')] += 1
            
    print(*counts)
