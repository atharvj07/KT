#include <iostream>

using namespace std;

int main() {
  int x, y, z, t1, t2, t3;
  cin >> x >> y >> z >> t1 >> t2 >> t3;
  cout << ((abs(y - z) * t2 + t3 * 2 <= abs(y - x) * t1) ? "YES" : "NO") << endl;
  return 0;
}
