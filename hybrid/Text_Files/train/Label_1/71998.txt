for _ in range(int(input())):
    n=int(input());a=list(map(int,input().split()))
    if sum(a)%n!=0:print(-1);continue
    mn=min(a);res=float('inf')
    for i in a:
        if i!=mn:res=min(res,(i-mn+sum(a)%n-1)//(sum(a)//n))
    print(res if res!=float('inf') else -1)
