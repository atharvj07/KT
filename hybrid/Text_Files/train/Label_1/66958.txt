x, y, n = map(int,input().split())
ans = 1e9
den = 1e9
for i in range(1,n+1):
    num = int((x*i+y/2)/y)
    if abs(x/y-num/i)<ans or (abs(x/y-num/i)==ans and i<den):
        ans = abs(x/y-num/i)
        den = i
        res = str(num)+"/"+str(i)
print(res)
