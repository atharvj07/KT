import math

n = int(input())

# Find the smallest square number that is greater than or equal to n
side_length = math.ceil(math.sqrt(n))

# The minimum number of segments required is twice the side length of the square
# because Sofia needs to draw both the vertical and horizontal segments.
print(side_length + math.ceil(n / side_length))
