import sys

n = int(input().strip())
voters = input().strip()
stack = []
for i in range(n):
    if stack:
        if voters[i] == stack[-1]:
            stack.pop()
        else:
            stack.append(voters[i])
    else:
        stack.append(voters[i])
sys.stdout.write(stack[-1])
