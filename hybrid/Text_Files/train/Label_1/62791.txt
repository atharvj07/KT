import sys
input=sys.stdin.readline
for i in range(int(input())):
    s=input()
    op=cl=0
    ans=0
    for c in s:
        if c=='(':op+=1
        else:
            if op==0:ans+=1
            else:op-=1
    print(ans+op)
