def solve(n, k):
    ans = 0
    for i in range(1, len(n)):
        if n[i] == '0':
            continue
        count = i + 1
        if count % k == 0:
            ans += (1 << (count - i - 1))
    return ans % (10**9 + 7)

n = input()
k = int(input())
print(solve(n, k))
