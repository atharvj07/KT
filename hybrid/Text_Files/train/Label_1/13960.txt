#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b, x, y, n;
    cin >> a >> b >> x >> y >> n;
    a -= min(a - x, n);
    b -= min(b - y, n);
    cout << a * b << endl;
  }
  return 0;
}
