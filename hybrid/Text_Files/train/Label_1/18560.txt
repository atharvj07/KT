#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    ll n;
    cin >> n;
    for (ll i = sqrt(n) + 1; i >= 1; --i) {
        if (n % (i * i) == 0) {
            n = n / i;
            break;
        }
    }
    cout << n << '\n';
    return 0;
}
