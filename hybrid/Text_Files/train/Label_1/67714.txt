n, l, x, y = map(int, input().split())
a = [int(x) for x in input().split()]
ans = []
for d in [x, y]:
    lo, hi = 0, n-1
    while lo < hi:
        mi = (lo+hi) // 2
        if a[mi] > d: hi = mi
        else: lo = mi+1
    if a[lo] != d: ans.append(d)
print(len(ans))
print(*ans)
