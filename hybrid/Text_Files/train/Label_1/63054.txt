from collections import defaultdict

n = int(input())
m = defaultdict(int)
for s in [input() for _ in range(n)]:
    t = []
    for x in s:
        m[x] += 1
        t.append(m[x])
    t.sort()
    pow = 1
    num = 0
    for x in t[::-1]:
        num += x * pow
        pow *= 10
    print(num)
