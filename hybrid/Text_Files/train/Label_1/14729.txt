#include <bits/stdc++.h>
using namespace std;
const int N = 105;
int main()
{
    int n, a[N];
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    sort(a + 1, a + n + 1);
    for (int i = 1; i <= n / 2; i++)
        cout << a[i] << ' ' << a[n - i + 1] << ' ';
}
