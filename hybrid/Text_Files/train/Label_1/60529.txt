import sys

input = sys.stdin.readline
t = int(input())

for _ in range(t):
    s = input().strip()
    a, b = 0, 0
    ans = 0
    for ch in s:
        if ch in "([":
            a += 1
        else:
            b += (ch == ")" and a)
            a -= (ch == "]" and b)
    print(b)
