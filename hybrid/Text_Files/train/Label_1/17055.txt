#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,b,d,sum,cnt=0;
    cin>>n>>b>>d;
    sum=b;
    while(n--)
    {
        int x;
        cin>>x;
        if(sum+x<=d) sum+=x;
        else {
            cnt++;
            sum=x;
        }
    }
    cout<<cnt;
}
