#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x0, y0;
  cin >> n >> x0 >> y0;
  set<pair<int, int>> s;
  while (n--) {
    int x, y;
    cin >> x >> y;
    s.insert({y - x, x + y});
  }
  cout << s.size() << endl;
  return 0;
}
