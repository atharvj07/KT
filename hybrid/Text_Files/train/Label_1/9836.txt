#include <iostream>

int main() {
  long long a, b;
  std::cin >> a >> b;
  int ans = 0;
  while (a > 0 && b > 0) {
    ++ans;
    if (a < b) std::swap(a, b);
    a -= b;
  }
  std::cout << ans << '\n';
}
