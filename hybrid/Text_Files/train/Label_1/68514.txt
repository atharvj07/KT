for q in range(int(input())):
    a = [int(i)-1 for i in input().split()]
    s = [[] for _ in range(3002)]
    lp = [-1]*3001
    ans = 0
    for i in range(len(a)):
        lp[a[i]] = i
        for k in s[a[i]]:
            ans += i-k-1
        s[a[i]].append(i)
    print(ans)
