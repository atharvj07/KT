#include <iostream>
using namespace std;
int main() {
    long long n, b, d, sum = 0, ans = 0;
    cin >> n >> b >> d;
    for (long long i = 0, a; i < n; cin >> a, i++) {
        sum += a;
        if (sum > d) sum = a, ans++;
        if (a > b) sum = 0;
    }
    cout << ans;
}
