#include <bits/stdc++.h>
using namespace std;
#define int long long
main() {
  int n, ans = 0;
  cin >> n;
  vector<int> a(n);
  map<int, int> freq;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
    ans += freq[a[i] - 1] + freq[a[i] + 1];
    freq[a[i]]++;
  }
  cout << ans << endl;
}
