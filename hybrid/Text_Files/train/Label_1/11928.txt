#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;

  for (int i = 0; i < k; i++) {
    int j = 0;
    while (j < n && s[j] != 'a') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'b') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'c') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'd') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'e') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'f') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'g') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'h') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'i') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'j') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'k') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'l') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'm') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'n') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'o') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'p') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'q') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'r') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 's') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 't') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'u') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'v') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'w') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'x') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'y') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
    j = 0;
    while (j < n && s[j] != 'z') j++;
    if (j < n) {
      s.erase(j, 1);
      continue;
    }
  }
  cout << s << "\n";
  return 0;
}
