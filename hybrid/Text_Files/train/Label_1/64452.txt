import sys
from collections import defaultdict

tiles = sys.stdin.readline().strip().split()
freq = defaultdict(int)
for tile in tiles:
  freq[tile[1]] += int(tile[0])

for suit in freq:
  if freq[suit] >= 3:
    print(0)
    exit()

print(3 - max(freq.values()))
