from sys import stdin
import math
d=dict()
l=int(stdin.readline().strip())
for x in range(0,l):
 for y in stdin.readline().strip().split(' '):
  if not y:
   continue
  a=int(y)
  if not d.get(a):
   d[a]=[x]
  else:
   d[a].append(x)
for x in d:
 if len(d[x])>1:
  print(d[x][0]+1,d[x][1]+1)
  break
 else:
  0
