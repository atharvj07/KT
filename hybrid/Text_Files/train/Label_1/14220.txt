#include <bits/stdc++.h>
using namespace std;
#define int long long
const int MOD = 1e9 + 7;
char s[100005];
int32_t main() {
  cin >> s;
  int n = strlen(s), ans = 1;
  for (int i = 0; i < n; i++) {
    if (s[i] == 'w' or s[i] == 'm') { ans = ans * 2 % MOD; }
  }
  cout << ans << endl;
}
