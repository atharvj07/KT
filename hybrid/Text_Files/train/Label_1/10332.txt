#include <bits/stdc++.h>
#include <ext/pb_ds/assoc_container.hpp>
#include <ext/pb_ds/tree_policy.hpp>
using namespace __gnu_pbds;
using namespace std;
using namespace chrono;
#define int            long long
#define int_pair       pair<int,int>
#define vi             vector<int>
#define pb             push_back
#define ppb            pop_back
#define all(v)         v.begin(),v.end()
#define mp             make_pair
#define yes            cout<<"YES"<<endl
#define no             cout<<"NO"<<endl
#define trace(x)       cerr<<"#x:"<<x<<endl
#define endl           "\n"
#define ra             ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
typedef tree<int,null_type,less<int>,rb_tree_tag,tree_order_statistics_node_update> ordered_set; // use less_equal for non distinct
template<class T>void in_array(T *a,int n){for(int i=0;i<n;i++)cin>>a[i];}
template<class T>void out_array(T *a,int n){for(int i=0;i<n;i++)cout<<a[i]<<' ';cout<<endl;}
#define google(x) for(int t=0;t<x;t++){cout<<"Case #"<<t+1<<": ";solve();}
const int mod=1e9+7;
const int N=1e6+5;
int hsh_a[N],hsh_b[N];
int32_t main()
{
    ra;
    string a,b;
    cin>>a>>b;
    int n=a.length(),m=b.length();
    if(n!=m)
    {
        no;
        return 0;
    }
    for(int i=0;i<n;i++)
    {
        hsh_a[i+1]=hsh_a[i]+(a[i]-'0');
        hsh_b[i+1]=hsh_b[i]+(b[i]-'0');
    }
    for(int i=0;i<n;i++)
    {
        if(hsh_a[i+1]!=hsh_b[i+1])
        {
            no;
            return 0;
        }
    }
    yes;
    return 0;
}
