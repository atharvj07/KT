#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b, c;
    cin >> a >> b >> c;
    if (a % 2 == b % 2 && b % 2 == c % 2)
      cout << (a + c) / 2 << endl;
    else
      cout << "-1" << endl;
  }
  return 0;
}
