#include <iostream>
#include <set>
int main() {
  std::string s;
  std::cin >> s;
  std::set<char> st(s.begin(), s.end());
  if (st.size() == s.size())
    std::cout << "Yes" << std::endl;
  else
    std::cout << "No" << std::endl;
}
