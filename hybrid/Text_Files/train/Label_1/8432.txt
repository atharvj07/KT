#include <bits/stdc++.h>
using namespace std;
#define int long long
const int N = 1e6 + 7;
int32_t main() {
  int n, a, b, ans = 0;
  cin >> n >> a >> b;
  while (a || b) {
    ans += (a % 2 != b % 2);
    a >>= 1;
    b >>= 1;
  }
  cout << ans << endl;
}
