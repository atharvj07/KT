#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, t, k, d;
    cin >> n >> t >> k >> d;
    if (ceil((n / k) * 1.0 * t) > ceil((n / (2 * k)) * 1.0 * t) + d) cout << "YES" << endl;
    else cout << "NO" << endl;
    return 0;
}
