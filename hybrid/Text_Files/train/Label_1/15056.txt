#include <iostream>
#include <string>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int n;
    std::cin >> n;
    std::string s(n, '8');
    if (n % 2 == 0) { s[n - 1] = '9'; }
    std::cout << s << '\n';
  }
  return 0;
}
