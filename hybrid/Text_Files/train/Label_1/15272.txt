#include <iostream>

using namespace std;

int main() {
  int q;
  cin >> q;
  while (q--) {
    long long n, a, b;
    cin >> n >> a >> b;
    if (n % 2 == 0)
      cout << n / 2 * min(a, 2 * b) << endl;
    else
      cout << (n / 2) * min(a, 2 * b) + min(a, b) << endl;
  }
  return 0;
}
