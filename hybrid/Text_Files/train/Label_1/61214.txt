import sys
input = sys.stdin.readline

def solve():
    n = int(input())
    a = list(map(int, input().split()))
    a.sort(reverse=True)
    if a[0] < 3 or a[1] < 3:
        return 0
    k = (a[0] + a[1] - 4) // 2
    return min(k, n - 2)

for _ in range(int(input())):
    print(solve())
