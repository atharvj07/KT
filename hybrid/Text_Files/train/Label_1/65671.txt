import sys
mod = 10**9 + 7

def mod_pow(a, b):
    if(b == 0): return 1
    p = mod_pow(a, b // 2) % mod
    p = (p * p) % mod
    if(b % 2 == 0): return p
    return (a * p) % mod

num = int(input())
print((mod_pow(2, num) - 1 + mod) % mod)
