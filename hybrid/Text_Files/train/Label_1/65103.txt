n, k = map(int, input().split())
s = set()
ans = []
for i, x in enumerate(map(int, input().split())):
    if x in s:
        ans.append(x)
    else:
        if len(ans) == k:
            ans.pop(0)
        ans.append(x)
        s.add(x)
print(len(ans))
print(*ans)
