#include <iostream>
using namespace std;
typedef long long int ll;
ll f(ll a, ll b) {
  if (a < b) return f(b, a);
  if (a % b == 0) return a / b;
  return f(b, a % b) + 1;
}
int main() {
  ll a, b;
  cin >> a >> b;
  cout << f(a, b) << '\n';
  return 0;
}
