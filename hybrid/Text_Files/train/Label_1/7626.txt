#include <iostream>
using namespace std;
int main() {
    string s;
    cin >> s;
    int n = s.size();
    int pos = 1 << n;
    for (int i = 0; i < n; i++) {
        if (s[i] == 'r') pos >>= 1;
        else pos = (pos >> 1) | (1 << n);
        cout << pos << "\n";
    }
}
