n, s = map(int, input().split())
ps = sorted([(t, f) for f, t in (map(int, input().split()) for _ in range(n))])
ans = s
for t, f in ps:
    if t <= ans: ans += 1
    ans = max(ans, f) + t
print(ans)
