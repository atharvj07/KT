import java.util.*;

public class Main {

    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int vp = input.nextInt();
        int vd = input.nextInt();
        int t = input.nextInt();
        int f = input.nextInt();
        int c = input.nextInt();
        int dist = (t + f) * vd;
        double time = (double) c / (vd - vp);
        System.out.println((int) Math.ceil(time / (t + f)));
    }

}
