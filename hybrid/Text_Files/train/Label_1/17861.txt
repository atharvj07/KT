#include <vector>
#include <iostream>

int main() {
  uint64_t n, t;
  std::cin >> n >> t;
  int ans = 0;
  for (uint64_t i = 1; i <= n; i++) {
    uint64_t mx = i;
    if ((mx ^ t) <= n)
      ans++;
  }
  std::cout << ans << std::endl;
  return 0;
}
