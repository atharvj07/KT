['import sys\ninput = sys.stdin.readline\n\ndef dfs(x, y):\n    if (x, y) in vis: return\n    vis.add((x, y))\n    vis2.add((x, y))\n    for nx, ny in ((x - 1, y - 1), (x, y - 1), (x + 1, y - 1)):\n        if 0 <= nx < m and 0 <= ny < n and (nx, ny) not in vis and not (0 <= nx < m and 0 <= ny < n and (nx, ny) in vis and (nx, ny) not in vis2):\n            dfs(nx, ny)\n\nmod = int(1e9) + 9\nm = int(input())\na = []\nfor _ in range(m):\n    x, y = map(int, input().split())\n    a.append((x, y))\nn = max(y for x, y in a) + 1\na.sort(key = lambda x: x[1])\nans = 0\nvis = set()\nvis2 = set()\nfor x, y in a:\n    if (x, y) in vis: continue\n    dfs(x, y)\n    ans *= m\n    ans %= mod\n    ans += y\n    ans %= mod\nprint(ans)\n']