#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b;
    cin >> a >> b;
    cout << abs((a - b) / 10) + ((a - b) % 10 != 0) << '\n';
  }
  return 0;
}
