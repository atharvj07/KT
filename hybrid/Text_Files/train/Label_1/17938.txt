#include <iostream>

using namespace std;

int main() {
  string s;
  cin >> s;
  int bal = 0;
  for (char c : s) {
    bal += (c == '+' ? -1 : 1);
    if (bal < 0) {
      cout << "No";
      return 0;
    }
  }
  cout << "Yes";
  return 0;
}
