for _ in range(int(input())):
    n, m = map(int, input().split())
    a = [[int(x) for x in input().split()] for _ in range(n)]
    odd = sum(sum(x) for x in a) % 2
    print(['Vivek', 'Ashish'][odd])
