import sys

# Standard Input, return list of integers
def input_list(input_string=""):
    while input_string == "":
        input_string = sys.stdin.readline().strip()
    input_string = input_string.split()
    return list(map(int, input_string))

# Depth First Search
def dfs(root):
    visited[root] = True
    dp[root] = [1]
    for node in adjacent_list[root]:
        if visited[node] == False:
            dfs(node)
            dp[root].append(dp[node][0] + 1)
    dp[root].sort(reverse=True)

n = input_list()[0]
adjacent_list = [[] for _ in range(n)]
visited = [False] * n
dp = [0] * n
for _ in range(n - 1):
    a, b = input_list()
    adjacent_list[a - 1].append(b - 1)
    adjacent_list[b - 1].append(a - 1)
dfs(0)
print(dp[0][1])
