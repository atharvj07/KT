#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    int ans = 100; // initially alexey can use the whole dryer
    while (n--) {
        int l, r;
        cin >> l >> r;
        ans -= (r - l);
    }
    cout << ans << endl;
    return 0;
}
