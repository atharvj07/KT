#include <iostream>
#include <vector>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    vector<int> a(n);
    for (auto &x : a) cin >> x;
    int odd = 0;
    for (auto &x : a) odd += x & 1;
    cout << (odd ? "YES" : "NO") << "\n";
  }
}
