#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long a, b, c, n;
    std::cin >> a >> b >> c >> n;
    std::cout << (((a + b + c + n) % 3 == 0) ? "YES\n" : "NO\n");
  }
  return 0;
}
