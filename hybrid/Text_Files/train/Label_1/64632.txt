import sys
input = sys.stdin.readline

t = int(input())
for x in range(t):
    x = int(input())
    if x <= 1:
        print(-1)
    else:
        if x % 2 == 0:
            print(x, int(x/2))
        else:
            print(x+1, int((x+1)/2))
