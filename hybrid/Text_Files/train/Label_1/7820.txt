#include <iostream>

int main() {
    int t;
    std::cin >> t;
    while (t--) {
        int n;
        std::cin >> n;
        std::cout << (n % 2020 <= n % 2021 ? "YES" : "NO") << '\n';
    }
    return 0;
}
