#include <iostream>
#include <vector>
using namespace std;

int main() {
    int t, x1, y1, x2, y2;
    cin >> t;
    while (t--) {
        cin >> x1 >> y1 >> x2 >> y2;
        cout << abs(x2 - x1) + abs(y2 - y1) << "\n";
    }
    return 0;
}
