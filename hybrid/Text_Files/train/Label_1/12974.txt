#include <algorithm>
#include <cmath>
#include <cstdio>
#include <iostream>
#include <vector>

int main()
{
    std::string a;
    std::cin >> a;
    int n=a.size(), cnt=0;
    for(int i=0;i<n/2;i++){
        cnt+=a[i]!=a[i+3];
    }
    std::cout << cnt << std::endl;
}
