def f(x: int) -> int:
    y = []
    while x:
        y.append(str(x % 10))
        x //= 10
    return int(''.join(y))

t = int(input())
for _ in range(t):
    n = input()
    a = f(f(int(n)))
    print(len({int(n) // f(f(int(n))) for n in [d for d in n if d != '0']}))
