#include <iostream>
using namespace std;

int main() {
    string s;
    int t;
    while (cin >> t) {
        while (t--) {
            cin >> s;
            cout << ((s.length() + 1) & 1 ? "DA" : "NET") << '\n';
        }
    }
}
