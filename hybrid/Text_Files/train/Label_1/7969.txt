#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;
  vector<int> fr(26);
  for (char chr : s) { fr[chr - 'A']++; }
  int mini = *min_element(fr.begin(), fr.begin() + k);
  cout << mini * k << '\n';
  return 0;
}
