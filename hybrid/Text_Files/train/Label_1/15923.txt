#include <iostream>
#include <algorithm>
using namespace std;
int main() {
    int n, k, ans = -1e9;
    cin >> n >> k;
    while(n--) {
        int f, t;
        cin >> f >> t;
        ans = max(ans, (t <= k ? f : f - t + k));
    }
    cout << ans << endl;
}
