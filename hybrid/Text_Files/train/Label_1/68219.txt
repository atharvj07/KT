n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans = a[-1] - a[0]
if k >= n - 1:
    ans = 0
else:
    for i in range(n - 1):
        if k >= i:
            if a[-1 - i] - a[i] < ans:
                ans = a[-1 - i] - a[i]
print(ans)
