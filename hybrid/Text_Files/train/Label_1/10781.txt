#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n;
    cin >> n;
    int sq = sqrt(n);
    int perimeter = 2 * (sq + (n % sq == 0 ? 0 : 1)) + 2 * (sq + 1);
    cout << perimeter << endl;
    return 0;
}
