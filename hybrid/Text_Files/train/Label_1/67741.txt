import heapq

n, m, a, d = map(int, input().split())
t = [int(x) for x in input().split()]
t.append(n * a)

ans = 0
last = -1
for i in t:
    if i - last > d:
        ans += 1
        last = i

print(ans)
