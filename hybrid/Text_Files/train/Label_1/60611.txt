n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans = []
for i in range(k):
    b = a[i:]
    c = 1
    while len(b) + c - 1<= n - i and sum(b[:c]) + a[i] != sum(b[c:]):
        c += 1
    ans.append([c] + b[:c])
    a = a[c:]
for l in ans:
    print(*l)
