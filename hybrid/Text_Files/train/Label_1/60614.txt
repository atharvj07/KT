n, k = map(int,input().split())
a = sorted(map(int,input().split()))
L = [[]] * k

for j in range(k):
    x = max(a)
    while x in L[j]:
        x -= 1
    L[j].append(x)
    a.remove(x)

for l in L:
    print(len(l),*l)
