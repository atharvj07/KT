#include <iostream>

int32_t main() {
  char c;
  int32_t ans = 0, cnt = 0;
  while (std::cin.get(c)) {
    switch (c) {
      case '+':
        if (++cnt == 1) ans++;
        break;
      case '-':
        if (--cnt == 0) ans++;
    }
  }
  std::cout << ans << std::endl;
  return 0;
}
