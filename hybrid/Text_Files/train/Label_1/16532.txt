#include <iostream>
using namespace std;
int main()
{
    int t, x, y;
    cin >> t;
    while (t--)
    {
        cin >> x >> y;
        int ans = abs(x) + abs(y);
        if (x > 0 && y > 0 || x < 0 && y < 0)
            ans += 2;
        cout << ans << endl;
    }
}
