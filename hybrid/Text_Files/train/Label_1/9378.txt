#include <bits/stdc++.h>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long a, b, c;
    std::cin >> a >> b >> c;
    if (a < b) std::swap(a, b);
    if (a < c) std::swap(a, c);
    std::cout << ((a + b > c) ? "YES" : "NO") << '\n';
  }
}
