import sys
import queue

n = int(input())
lis = [[] for _ in range(n+1)]
for i in range(n-1):
    a, b = map(int, input().split())
    lis[a].append(b)
    lis[b].append(a)
adj = [0] * n

def dfs(now, pre):
    global ans
    for v in lis[now]:
        if v == pre: continue
        adj[now] += 1
        dfs(v, now)
        ans += 1
        print(now, v, now)

ans = 0
dfs(1,-1)
print(ans)
