#include <cstdio>

int main() {
  int n;
  scanf("%d", &n);
  int ans = 0, cur = 0;
  for (int i = 0; i < n; ++i) {
    int t;
    scanf("%d", &t);
    int add = cur - t;
    if (add >= 0) cur = t;
    ans += add;
    cur += add;
  }
  printf("%d", ans);
  return 0;
}
