t = int(input().strip())
for _ in range(t):
    a, b = map(int, input().strip().split())
    diff = abs(a - b)
    operations = 0
    while diff > 0:
        operations += 1
        diff -= operations
    print(operations + (diff * -1) * 2)
