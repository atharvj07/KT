import sys
t=int(input())
for _ in range(t):
    n,x=map(int,input().split())
    a=sys.maxsize
    for i in range(n):
        d,h=map(int,input().split())
        if h<=d:a=min(a,x//h)
    if a==sys.maxsize:print(-1)
    else:print(x//a+1)
