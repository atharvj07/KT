#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, arr[10], l = 0;
  cin >> n;
  while (n) {
    arr[l++] = n % 9;
    n -= n % 9;
    n /= 9;
  }
  cout << l << endl;
  for (int i = l - 1; i >= 0; i--) { cout << arr[i] << " "; }
}
