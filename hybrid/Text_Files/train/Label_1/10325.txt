#include <cstdio>
#include <cmath>

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int a, b;
        scanf("%d%d", &a, &b);
        if (a > b) puts("Finite");
        else puts("Infinite");
    }
    return 0;
}
