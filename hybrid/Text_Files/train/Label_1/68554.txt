from collections import Counter
for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    k = max(a) - min(a)
    for x in a:
        k = max(k, (a.count(x) + 1) // 2)
    print(k if k <= max(a) - min(a) else -1)
