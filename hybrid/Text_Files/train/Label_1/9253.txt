#include <bits/stdc++.h>

using namespace std;

const int N = 1e6 + 5;

int a[N];

int main() {
  int n;
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  int mx = *max_element(a + 1, a + n + 1);
  if (mx <= 3 * n)
    cout << "Petr\n";
  else
    cout << "Um_nik\n";
}
