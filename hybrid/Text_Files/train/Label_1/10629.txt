#include <cstdio>
#include <cmath>
int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int x;
        scanf("%d", &x);
        printf("%d\n", int((sqrt(1 + 8 * x) - 1) / 2));
    }
}
