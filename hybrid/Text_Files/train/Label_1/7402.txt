#include<bits/stdc++.h>
using namespace std;
int main(){
    int t,n,p,k,i,x,a[200005];
    for(cin>>t;t--;){
        cin>>n>>p>>k;
        for(x=0;++x<=n;) cin>>a[x];
        sort(a+1,a+1+n);
        for(x=i=0;a[i]<=p && x<k;) p-=a[i++],x++;
        cout<<x<<endl;
    }
    return 0;
}
