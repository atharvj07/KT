#include <cstdio>

int main() {
    int n, a, b;
    scanf("%d%d%d", &n, &a, &b);
    if(b >= 0) printf("%d\n", (a + b - 1) % n + 1);
    else printf("%d\n", (a + b + n - 1) % n + 1);
    return 0;
}
