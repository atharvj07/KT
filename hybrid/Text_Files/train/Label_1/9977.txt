#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int x, h, m;
    cin >> x >> h >> m;
    int ans = ceil(float(60 * h + m) / 7) * x;
    cout << ans << endl;
    return 0;
}
