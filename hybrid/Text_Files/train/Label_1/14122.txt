#include <iostream>
int main() {
    int t;
    std::cin >> t;
    for (int i = 0; i < t; i++) {
        int w, h, n;
        std::cin >> w >> h >> n;
        std::cout << (w % 2 == 0 || h % 2 == 0 ? "YES" : "NO") << std::endl;
    }
    return 0;
}
