h, a = input(), input()
red = {}
for t, _, n, c in [input().split() for _ in range(int(input()))]:
    if c == 'y':
        if not (t, n) in red:
            red[t, n] = int(t)
    else:
        if (t, n) in red:
            print(*red.pop((t, n)), t)
