#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &ai : a) cin >> ai;
  sort(a.begin(), a.end());
  long long sum = a[0] * (long long)n;
  for (int i = 0; i < n - 1; i++) { sum -= a[i]; }
  cout << sum << endl;
}
