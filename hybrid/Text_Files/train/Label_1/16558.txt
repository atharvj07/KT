#include <iostream>
using namespace std;
int main() {
    long long n;
    cin >> n;
    long long digit = n, sum = 0;
    while (digit > 0) {
        digit /= 10;
        sum++;
    }
    sum *= n;
    cout << sum;
}
