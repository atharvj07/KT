#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  string s;
  cin >> n >> s;
  map<char, int> mp;
  for (char c : s) mp[c]++;
  int ans = 0;
  for (auto &[k, v] : mp) ans += v & 1;
  cout << (ans <= 1 ? "Yes" : "No") << endl;
  return 0;
}
