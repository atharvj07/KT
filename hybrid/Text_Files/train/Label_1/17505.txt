#include <bits/stdc++.h>

using namespace std;

int m, n;
vector<int> a;

int main() {
  cin >> n >> m;
  a.assign(n, 0);
  for (auto &x : a) cin >> x;
  int sum = 0;
  for (auto &x : a) {
    cout << (x + sum - 1) / m << ' ';
    sum = (x + sum - 1) % m;
  }
  cout << '\n';
  return 0;
}
