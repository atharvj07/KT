#include<bits/stdc++.h>
using namespace std;
int main() {
  int n, m, k;
  cin >> n >> m >> k;
  int x = (k + 1) / 2;
  int r = (x - 1) / m + 1;
  int d = (x - 1) % m + 1;
  char s = (x % 2) ? 'R' : 'L';
  cout << r << " " << d << " " << s << "\n";
  return 0;
}
