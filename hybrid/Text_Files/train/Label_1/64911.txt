import sys

n, d = map(int, input().split())
a = list(map(int, input().split()))

ans = 0
cur = 0
for x in a:
    cur += x
    if cur < 0:
        ans += 1
        cur = max(x, 0)
    elif cur > d:
        print(-1)
        sys.exit()

print(ans)
