#include <algorithm>
#include <iostream>

int main() {
  long long n, m, k;
  std::cin >> n >> m >> k;
  std::cout << std::min(m / n, m - (m % n > 0) - std::abs(k - (n + 1) / 2) * (m % n == 0)) << std::endl;
  return 0;
}
