#include<bits/stdc++.h>
using namespace std;
int main(){
  int n;cin>>n;
  string s;cin>>s;
  int cnt=0;
  for(int i=s.size()-2;i>=0;i--){
    if(s[i]==s[i+1])cnt++;
    else break;
  }
  if(s.size()-cnt>=4)cout<<1<<endl;
  else cout<<0<<endl;
  return 0;
}
