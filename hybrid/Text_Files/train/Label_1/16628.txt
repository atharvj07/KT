#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  for (int i = n - 1; i > 1; --i) {
    if (__gcd(i, n - i) == 1) {
      cout << n - i << " " << i << endl;
      return 0;
    }
  }
  cout << "-1" << endl;
  return 0;
}
