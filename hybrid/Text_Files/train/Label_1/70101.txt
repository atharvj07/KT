n,k=map(int,input().split())
a=[list(map(int,input().split())) for i in range(n)]
a.sort(key=lambda x:x[1])
joy=-1e9
for f,t in a:
    if t<=k:
        joy=max(joy,f)
    else:
        joy=max(joy,f-(t-k))
print(joy)

