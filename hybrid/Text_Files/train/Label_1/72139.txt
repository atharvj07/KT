from sys import stdin

n = (int(input()) - 1) // 2
L = list(map(int, stdin.readline().split()))
L = L + L
MX = max(L) * 2
L.sort(reverse=1)
ans = 0
for i in range(n):
    ans = max(ans, L[i] + L[i + 1])
    L[i + 1] += L[i]
    MX = max(MX, L[i + 1])
ans = max(ans, MX)
print(ans)
