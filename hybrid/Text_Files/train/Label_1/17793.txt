#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long r, g, b, w, sum;
    cin >> r >> g >> b >> w;
    sum = r + g + b + w;
    if (sum & 1)
      cout << "No" << endl;
    else
      cout << "Yes" << endl;
  }
}
