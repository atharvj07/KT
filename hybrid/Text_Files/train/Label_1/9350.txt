#include<bits/stdc++.h>
using namespace std;
int n, k, ans, cur;
int main() {
    cin >> n >> k;
    for (int i = 0, x; i < n; i++)
        cin >> x, (x < 0 ? cur = k : cur -= (x == 0));
    if (cur > 0) ans = (cur + k - 1) / k * 2 - 1;
    if (cur <= 0) ans = cur <= 0;
    cout << ans << endl;
}
