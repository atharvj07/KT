#include <iostream>
using namespace std;

int main() {
    int da, db;
    cin >> da >> db;
    if (da == db) {
        cout << da << db << endl;
    } else if (da < db) {
        cout << da * 10 + 9 << " " << db * 10 << endl;
    } else {
        cout << -1 << endl;
    }
    return 0;
}
