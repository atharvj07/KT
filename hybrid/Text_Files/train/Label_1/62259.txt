n, m, mod = map(int, input().split())
a = [input() for _ in range(m)]
ans = pow(2, (n - m) * n, mod)
for i in a:
    ans = ans * pow(3, i.count('0'), mod) % mod
    ans = ans * pow(3, i.count('1') - 2, mod) % mod
print(ans)
