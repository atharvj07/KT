#include <iostream>
int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int x;
    std::cin >> x;
    if (x < 3)
      std::cout << x - 1 << std::endl;
    else
      std::cout << x / 3 + ((x % 3 > 0) ? 1 : 0) << std::endl;
  }
  return 0;
}
