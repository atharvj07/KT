import sys
def check(seq):
    C=0
    for x in seq:
        if x==1 or x==2: C^=1
        if x==3 or x==4: C^=2
        if x==5 or x==6: C^=3
    return C==0
inp=iter(sys.stdin.read().splitlines())
for _ in range(int(next(inp))):
    n=int(next(inp))
    seq=[int(x) for x in next(inp)]+[int(x) for x in next(inp)]
    if check(seq): print("YES")
    else: print("NO")
