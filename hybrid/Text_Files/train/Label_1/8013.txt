#include <iostream>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n;
    cin >> n;
    if (n % 2 == 0)
      cout << (n / 2);
    else if (n % 3 != 0)
      cout << (n / 2) + 1;
    else
      cout << (n / 3) + 1;
    cout << endl;
  }
  return 0;
}
