#include <algorithm>
#include <vector>
#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        vector<int> a(4);
        for (int& ai : a) cin >> ai;
        sort(a.begin(), a.end());
        cout << a[0] * a[3] << endl;
    }
    return 0;
}
