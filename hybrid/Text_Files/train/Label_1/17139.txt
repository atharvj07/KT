#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
  int n, k, ans = 0;
  cin >> n >> k;
  vector<pair<int, int>> a(n);
  for (auto &x : a) {
    cin >> x.first >> x.second;
    ans += x.first / k + x.second / k;
  }
  cout << ans << endl;
}
