t = input().strip()
s = []
left = True  # Flag to alternate between left and right insertion
for i in range(len(t)):
    if left:
        s.insert(0, t[i])
    else:
        s.append(t[i])
    left = not left  # Toggle the flag
print("".join(s))
