#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
  string a;
  cin >> a;
  string b;
  for (int i = a.length() - 1; i >= 0; --i) {
    b += a[i];
  }
  cout << a + b << endl;
  return 0;
}
