#include <iostream>

int main() {
  int n;
  std::cin >> n;
  std::string s;
  std::cin >> s;
  int cnt = 0;
  for (int i = 0; i < n - 1; ++i) {
    if (s[i] != s[i + 1]) ++cnt;
  }
  std::cout << cnt + 1 << std::endl;
  return 0;
}
