#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int x, p, k;
    cin >> x >> p >> k;
    for (int i = 1, y = x + 1; k; i++, y++) {
      if (__gcd(p, y) == 1) k--;
      if (k == 0) {
        cout << y << endl;
        break;
      }
    }
  }
}
