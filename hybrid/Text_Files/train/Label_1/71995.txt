import sys

for t in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    idx = sorted(range(n), key=lambda x: a[x])[:n//2+n%2]
    s = sum(a[i] for i in idx)
    print(-1 if s%n else (n-s%n)%n)
