#include <iostream>
using namespace std;
int main() {
  long long n, m;
  cin >> n >> m;
  long long mul = n / 5 * m / 5;
  if (n % 5 > 0) mul += m / 5 + (n % 5) * (m % 5 > 0);
  if (m % 5 > 0) mul += n / 5 + (n % 5 > 0) * (m % 5 > 0);
  cout << mul << endl;
  return 0;
}
