n,L=map(int,input().split())
a=sorted([(2**i-1,c)for i,c in enumerate(map(int,input().split()),1)],key=lambda x:-x[0])
need,ans=L,0
for v,c in a:
    if need<=0:break
    k=need//v
    need-=k*v
    ans+=k*c
print(ans)
