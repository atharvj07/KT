n, pos, l, r = map(int, input().split())
a, b = pos, pos
for i in range(pos, l - 1, -1):
    a = i + 2
for i in range(pos + 1, r + 1):
    b = i - 1
print(abs(pos - l) + abs(pos - r) + min(abs(pos - a), abs(pos - b)))
