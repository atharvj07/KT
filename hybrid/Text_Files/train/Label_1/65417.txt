import sys

def dfs(u, p, vis):
    vis[u] = True
    for v in adj[u]:
        if not vis[v]:
            if dfs(v, u, vis):
                return True
        elif v != p:
            return True
    return False

n, m = map(int, input().split())
adj = [[] for _ in range(n+1)]
for _ in range(m):
    u, v = map(int, input().split())
    adj[u].append(v)

vis = [False] * (n+1)
ans = 'NO'
for i in range(1, n+1):
    if not vis[i]:
        if dfs(i, -1, vis):
            ans = 'YES'
            break

print(ans)
