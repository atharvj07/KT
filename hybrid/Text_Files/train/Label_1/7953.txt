#include <cstdio>

bool f(unsigned int a, unsigned int b) {
    unsigned int t = a & b;
    return t && ((b ^ a) % t == 0);
}

int main() {
    unsigned int t, a, b;
    scanf("%u", &t);
    while (t--) {
        scanf("%u%u", &a, &b);
        puts(f(a, b) ? "YES" : "NO");
    }
}
