#include<bits/stdc++.h>
using namespace std;
int power(int a, int b) {
	if(b==0) return 1;
	if(b==1) return a;
	int tmp = power(a, b/2);
	if(b%2==0) return (tmp*tmp)%10;
	else return ((tmp*tmp)%10*a)%10;
}
int main() {
	int n;
	cin >> n;
	cout << power(1378, n)%10 << endl;
	return 0;
}
