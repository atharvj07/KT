#include <iostream>
#include <string>

using namespace std;

int main() {
  string t;
  cin >> t;
  int n = t.size();
  string s = "";
  for (int i = 0; i < n; i++) {
    if (i % 2 == 0) {
      s += t[i];
    } else {
      s = t[i] + s;
    }
  }
  cout << s << endl;
  return 0;
}
