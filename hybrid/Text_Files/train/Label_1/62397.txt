n, k = map(int, input().split())
a = list(input())
b = list(sorted(set(a), reverse = True))
r = 0
for x in b:
    if k <= 0: break
    if k > a.count(x):
        r += (a.count(x) * a.count(x))
        k -= a.count(x)
    else:
        r += (a.count(x) * k)
        k -= k
print(r)
