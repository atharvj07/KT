import sys

n = int(input())
h = list(map(int, input().split()))
h.insert(0, 0)
mx = 0
ans = 1e9
notmin = 0
for i in range(n, 0, -1):
    if h[i] > mx:
        if notmin:
            ans = min(ans, notmin)
            notmin = 0
    else:
        notmin += 1
    mx = max(mx, h[i])
print(ans)
