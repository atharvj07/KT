n,d=list(map(int,input().split()))
t=list(map(int,input().split()))
ans=-1
for x in range(30):
    sum=0
    temp=x+1
    sum+=temp
    for i in t:
        sum+=(i+temp)
        temp+=i+temp
    if sum<=d:
        ans=max(ans,(sum-5)//5)
print(ans)
