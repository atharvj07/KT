#include <iostream>
#define ll long long
using namespace std;
int main() {
    ll n, a, mx = 0;
    cin >> n;
    for (ll i = 0; i < n; i++) {
        cin >> a;
        mx = max(mx, a);
    }
    cout << mx + 1 << "\n";
}
