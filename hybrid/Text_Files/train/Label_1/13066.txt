#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> ans;
    unordered_map<int, int> mp;
    for (int i = 0; i < n; ++i) {
        int x; cin >> x;
        mp[x]++;
        if (mp[x] == 1) ans.push_back(x);
    }
    cout << ans.size() << endl;
}
