from collections import defaultdict

n = int(input())
x = defaultdict(list)
y = defaultdict(list)
for _ in range(n):
    a, b = map(int, input().split())
    x[a].append(b)
    y[b].append(a)
ans = min(len(v) for v in x.values()) + min(len(v) for v in y.values())
print(ans)
