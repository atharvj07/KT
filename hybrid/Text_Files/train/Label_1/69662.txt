import sys

input = sys.stdin.readline

q = int(input())
for _ in range(q):
    n, m, k = map(int, input().split())
    if k % 2 == (n + m) % 2:
        if 0 <= k // 2 - abs(n - m) <= k // 2:
            print(k // 2 - abs(n - m))
        else:
            print(-1)
    else:
        print(-1)
