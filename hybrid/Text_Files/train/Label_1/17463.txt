#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, k, ans = 0;
    cin >> n >> k;
    vector<int> a(n);
    for (int i = 0; i < n; i++) { cin >> a[i], ans += (a[i] + 1) / 2 > k; }
    cout << ans << endl;
    return 0;
}
