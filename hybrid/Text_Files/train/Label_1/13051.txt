#include <iostream>
#include <vector>

int main() {
  std::string s;
  std::cin >> s;
  int a = 0, b = 0, c = 0;
  for (char ch : s) {
    if (ch == 'A') a++;
    if (ch == 'B') b++;
    if (ch == 'C') c++;
  }
  std::cout << (a && b && c ? "Yes" : "No") << std::endl;
  return 0;
}
