#include <iostream>

int main() {
    int n, m;
    std::cin >> n >> m;
    int day = 0;
    int remain = n;
    while(remain > 0) {
        day++;
        remain -= day;
        remain += m;
    }
    std::cout << day << std::endl;
    return 0;
}
