#include <iostream>
#include <string>
using namespace std;
int main() {
  int n;
  cin >> n;
  string a, b;
  cin >> a >> b;
  int ans = 0;
  for (int i = 0; i < n; i++)
    ans += min(abs(b[i] - a[i]), 10 - abs(b[i] - a[i]));
  cout << ans << endl;
  return 0;
}
