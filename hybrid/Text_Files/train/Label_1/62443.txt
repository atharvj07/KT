import sys
import math

input = sys.stdin.readline

n = int(input())
a = list(map(int, input().strip().split()))
ans = 0
sq = set()
for ai in a:
    x = int(math.sqrt(ai))
    if x*x == ai:
        sq.add(ai)
    else:
        ans += 1

k = (n - len(sq))/2

ans = min(k, len(sq))

print(ans)
