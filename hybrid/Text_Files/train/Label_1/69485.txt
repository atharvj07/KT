for j in range(int(input())):
    x,y=map(int,input().split());a=[[int(j)for j in input().split()]for i in range(x)];ans=True
    for i in range(x):
        for k in range(y):
            if(a[i][k]==0):
                if(ans):
                    ans=False
                else:
                    ans=True
                for b in range(x):
                    a[b][k]=1
                for c in range(y):
                    a[i][c]=1
    if(ans):print("Ashish")
    else:print("Vivek")
