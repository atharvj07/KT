#include <iostream>
#include <string>
using namespace std;

int main() {
  string s;
  int x;
  cin >> x >> s;
  if (s == "of week")
    cout << 52 << endl;
  else
    cout << 12 * (x <= 29) + (x == 30 && !(x % 4 == 0 && (x % 100 != 0 || x % 400 == 0)))
         << endl;
  return 0;
}
