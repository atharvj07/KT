#include <bits/stdc++.h>

int main() {
    std::string s;
    int sum1 = 0, sum2 = 0;
    std::cin >> s;
    for (int i = 0; i < 3; i++) {
        sum1 += s[i] - '0';
        sum2 += s[5 - i] - '0';
    }
    int ans = abs(sum1 - sum2);
    std::cout << ans << std::endl;
}
