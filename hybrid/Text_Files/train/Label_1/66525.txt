def fingerprint(p):
    return sorted([sum(x) for x in zip(p, p[1:] + [p[0]])])

for t in range(int(input())):
    n = int(input())
    p = list(map(int, input().split()))
    q = p[:]
    q.sort()
    p[0], p[p.index(q[-1])] = p[p.index(q[-1])], p[0]
    print(*p)
