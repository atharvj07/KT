#include<iostream>
#include<string>
using namespace std;
int main(){
	int n;
	string s;
	cin>>n>>s;
	for(int i=0,j;i<s.size();){
		for(j=i;j+1<s.size()&&s[j+1]>=s[j];j++);
		if(j==i){
			i++;
			continue;
		}
		i=j+1;
		s.erase(j,1);
	}
	cout<<s.size()<<endl;
	return 0;
}
