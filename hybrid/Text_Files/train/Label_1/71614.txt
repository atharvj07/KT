import sys
input = sys.stdin.readline
res = -1
for i in range(1,int(input())+1):
  l,r = map(int,input().split())
  if l==r: continue
  if res==-1 or (l<=l1 and r>=r1):
    res=i
    l1,r1=l,r
print(res)
