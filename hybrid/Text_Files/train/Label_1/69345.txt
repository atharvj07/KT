from sys import stdin
from fractions import Fraction

n = int(stdin.readline())
a = list(map(int, stdin.readline().split()))
b = sorted(a)
inversions = 0
for i in range(n):
    inversions += a[i] != b[i]
print(Fraction(n * (n - 1) - 2 * inversions, n))
