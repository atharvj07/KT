#include <iostream>

using namespace std;

int main() {
  string s;
  cin >> s;
  int v = 0, ans = 0;
  for (char c : s) {
    if (c == 'V') ++v;
    else ans += v;
  }
  cout << ans << endl;
  return 0;
}
