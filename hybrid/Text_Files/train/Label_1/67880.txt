s = input()
if "(" not in s:
    print(max(len(w) for w in s.split("_")), 0)
else:
    r = [0, 0]
    t = s.split(")")
    for p in t[:-1]:
        if "(" not in p:
            r[0] = max(r[0], max(len(w) for w in p.split("_")))
        else:
            r[1] += p.count("(")
    print(*r)
