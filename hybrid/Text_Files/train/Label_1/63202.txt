table_card = input().strip()
hand_cards = input().strip().split()

rank, suit = table_card

can_play = "NO"
for card in hand_cards:
    if card[0] == rank or card[1] == suit:
        can_play = "YES"
        break

print(can_play)
