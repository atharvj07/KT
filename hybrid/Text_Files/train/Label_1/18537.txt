#include <iostream>

using namespace std;

int main() {
  int n, d, m;
  cin >> n >> d >> m;

  while (m--) {
    int x, y;
    cin >> x >> y;
    cout << (((y - d) <= x && x <= (n - d) && d <= y && y <= (n - d)) ? "YES" : "NO")
         << endl;
  }
}
