import sys

ri = lambda: int(sys.stdin.readline())

for _ in range(int(input())):
    n = ri()
    a = list(map(int, input().split()))
    for i in range(1, n):
        if a[i] > a[0]:
            a[0] = a[i]
    if a[0] == max(a):
        print(a.index(a[0]) + 1)
    else:
        print(-1)
