#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ll a, b, l, r;
    cin >> a >> b >> l >> r;
    if (((r - l + 1 - a) % b) + a > b)
        cout << b;
    else
        cout << ((r - l + 1 - a) % b) + a;
    return 0;
}
