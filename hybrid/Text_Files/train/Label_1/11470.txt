#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int get(ll r) {
    ll t = sqrt(r);
    return t * (t + 1) / 2 - (t / 3) * ((t / 3) + 1) / 2 - (t / 2) * ((t / 2) + 1) / 2;
}

int main() {
    ll a, b;
    cin >> a >> b;
    cout << get(b) - get(a - 1) << '\n';
    return 0;
}
