#include <iostream>
#include <cmath>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int x, n, m;
    std::cin >> x >> n >> m;
    int hp = floor(x / 2.0) + 10;
    if (hp <= 0 || n > 0) std::cout << "YES\n";
    else std::cout << "NO\n";
  }
  return 0;
}
