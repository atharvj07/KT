import re

good = input()
pat  = input()
pat  = pat.replace('?', '([%s])' % good)
pat  = pat.replace('*', '(.*)')
regex = re.compile(pat)

for _ in range(int(input())):
   print(['NO', 'YES'][bool(regex.match(input()))])
