#include <iostream>
using namespace std;

int main() {
  int n, p, k;
  cin >> n >> p >> k;
  if (p > 1) cout << "<< ";
  for (int i = max(1, p - k); i <= min(n, p + k); i++) {
    if (i == p) cout << "(" << p << ") ";
    else cout << i << " ";
  }
  if (p < n) cout << ">>";
  cout << endl;
}
