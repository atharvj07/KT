n, m = map(int, input().split())
debts = [0] * n

for _ in range(m):
    a, b, c = map(int, input().split())
    debts[a-1] -= c
    debts[b-1] += c
    
positive_debts = sum(d for d in debts if d > 0)

print(positive_debts)
