#include <iostream>

using namespace std;

int main() {
  int a, b, c;
  cin >> a >> b >> c;
  if (a + b + c >= 6) {
    cout << min(a - 1, b - 1) << " " << max(a - 1, b - 1) << " " << c - 2;
  } else {
    cout << "Impossible";
  }
  return 0;
}
