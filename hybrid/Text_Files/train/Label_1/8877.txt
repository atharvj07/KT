#include <iostream>
using namespace std;
int main() {
  int n, a;
  cin >> n;
  long long sum = 0;
  while (cin >> a) {
    sum += a > 1;
    if (sum & 1)
      cout << 1 << '\n';
    else
      cout << 2 << '\n';
  }
}
