#include <iostream>
#include <vector>
using namespace std;

int main() {
    int c, d, n, m, k;
    cin >> c >> d >> n >> m >> k;
    int need = n * m - k;
    cout << max(0, (need + n - 1) / n * c) << endl;
    return 0;
}
