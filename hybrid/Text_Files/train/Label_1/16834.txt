#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long a, b, c, d;
    std::cin >> a >> b >> c >> d;
    std::cout << a << ' ' << ((a + b) >> 1) << ' ' << c << '\n';
  }
}
