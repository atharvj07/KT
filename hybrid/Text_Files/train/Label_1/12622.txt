#include <iostream>
#include <string>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    string s;
    cin >> s;
    cout << (s.find('N') == string::npos ? "YES" : "NO") << endl;
  }
  return 0;
}
