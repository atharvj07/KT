#include <iostream>
#include <string>

int main() {
  std::string s; std::cin >> s;
  int b = 0, f = 0, ans = 0;
  for (int i = 0; i < s.size(); ++i) {
    if (s[i] == 'M') ++b;
    else ++f;
    if (b < f) ++ans;
  }
  std::cout << ans << std::endl;
  return 0;
}
