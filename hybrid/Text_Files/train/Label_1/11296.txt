#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int x, n, m;
    cin >> x >> n >> m;
    x = (x + 9) / 10;
    if (x <= n)
      cout << "YES\n";
    else
      cout << "NO\n";
  }
  return 0;
}
