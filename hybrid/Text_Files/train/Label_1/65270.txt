n = int(input())
a = list(map(int, input().split()))
d = {}
for el in a:
    if not el in d: d[el] = [len(d), -1]
    d[el][1] = len(d) - d[el][0]
ans = []
for el in sorted(d):
    if d[el][1] != 0: ans.append([el, d[el][1]])
print(len(ans))
for el in ans:
    print(*el)
