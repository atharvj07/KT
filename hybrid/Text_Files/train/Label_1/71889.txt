n = int(input())
colors = [input() for _ in range(n)]
gems = set(colors)
absent = [gem for gem in ['Purple', 'Green', 'Blue', 'Orange', 'Red', 'Yellow'] if gem.lower() not in gems]
print(len(absent))
for gem in sorted(absent):
    print(gem)
