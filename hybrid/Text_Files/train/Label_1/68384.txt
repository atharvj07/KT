n,m=map(int,input().split());a=[int(x) for x in input().split()];b=[int(x) for x in input().split()];a.sort();b.sort();ans=1e18
if a[0]<0 or b[0]<0:
    ans=a[0]*b[0]
    a.pop(0);b.pop(0)
for x in a:
    for y in b:
        ans=min(ans,x*y)
print(ans)
