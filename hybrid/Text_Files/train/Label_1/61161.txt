n, s = map(int, input().split())
pas = [list(map(int, input().split())) for _ in range(n)]
pas.sort(key=lambda x: x[1])

tot = 0
for f, t in pas:
    w = max(0, t - tot)
    tot += w + s - f
print(tot)
