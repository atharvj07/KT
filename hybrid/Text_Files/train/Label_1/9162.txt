#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k, e = 0, s = 0, tp;
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        cin >> tp;
        e += (tp == 1);
        s += (tp == -1);
    }
    cout << abs(e - s) << '\n';
    return 0;
}
