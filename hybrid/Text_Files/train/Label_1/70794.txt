import sys
input = sys.stdin.readline

n, m = map(int, input().split())
a = [0] * n
for _ in range(m):
    x = int(input())-1
    a[x] += 1
    if a[x] == 1:
        for y in range(n):
            if a[y] > 0:
                a[y] -= 1
        print(1)
