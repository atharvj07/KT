#include <iostream>
using namespace std;
int main() {
    long long a, b, c;
    cin >> a >> b >> c;
    if (a + c == 0) {
        cout << 3 * (b / 2) << endl;
    } else {
        cout << 2 * a + 2 * b + c << endl;
    }
    return 0;
}
