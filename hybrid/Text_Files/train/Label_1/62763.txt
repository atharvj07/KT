from collections import defaultdict
n = int(input())
l = [int(x) for x in input().split()]
cnt = defaultdict(int)
for x in l:
    cnt[x] += 1
ans = []
for x in l:
    if cnt[x] > 0:
        ans.append(x)
        cnt[x] -= 1
        cnt[x * 2] -= 1
        cnt[x * 3] -= 1
print(*ans)
