#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long k;
    cin >> k;
    long long r = 1, c = 1;
    k--;
    while (k >= r) {
      k -= r;
      r++;
      c += r;
    }
    cout << r << " " << c - k << endl;
  }
  return 0;
}
