#include <iostream>

using namespace std;

typedef long long ll;

int main() {
    ll k, n, w;
    cin >> k >> n >> w;
    ll cost = w * (w + 1) / 2 * k;
    if (n >= cost) {
        cout << 0 << endl;
    } else {
        cout << cost-n << endl;
    }
    return 0;
}
