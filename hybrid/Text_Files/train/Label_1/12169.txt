#include <bits/stdc++.h>

using namespace std;

int main() {
  int q;
  cin >> q;
  while (q--) {
    int n;
    cin >> n;
    long long ans = 1;
    while (ans < n) ans *= 3;
    if (ans == n) ans *= 3;
    cout << ans << '\n';
  }
  return 0;
}
