import math

R, x1, y1, x2, y2 = map(int, input().split())
d = math.sqrt((x1 - x2) ** 2 + (y1 - y2) ** 2)
r = (R - d) / 2
if d < 2 * r:
    x, y = (x1 + x2) / 2, (y1 + y2) / 2
    print(x, y, r)
else:
    if R > d / 2:
        print(x1, y1, R)
    else:
        print(x2, y2, R)
