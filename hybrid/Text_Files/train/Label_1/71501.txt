import math
n = int(input())
ans = -1
for i in range(1,int(math.sqrt(n))+1):
    if n%i==0:
        if i*i==n:
            ans = i
        elif int(n/i)*int(n/i)!=n:
            ans = max(ans,int(n/i))
print(ans)
