c, b = {a: len(a[3:]) for a in input().split('\n')}, 1
for a in c:
    if c[a] > max(c.values()): b -= 1
    elif c[a] < min(c.values()): b += 1
print('D' if b > 0 else 'C' if b == 0 else list(c)[0])
