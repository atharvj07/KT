['import sys\ninput = sys.stdin.readline\n\nn, U = map(int, input().split())\nE = list(map(int, input().split()))\nmx = -1\n\nfor i in range(n - 2):\n    for j in range(i + 1, n - 1):\n        if E[j] - E[i] <= U:\n            for k in range(j + 1, n):\n                if E[k] - E[i] <= U:\n                    mx = max(mx, (E[k] - E[j]) / (E[k] - E[i]))\n\nprint(mx if mx > 0 else -1)\n']