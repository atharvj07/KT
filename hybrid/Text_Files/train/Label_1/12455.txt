#include<iostream>
using namespace std;
typedef long long ll;
int main() {
  ll n, k;
  cin >> n >> k;
  if (!(k % (n + 1))) cout << k / (n + 1) << endl;
  else cout << k / (n + 1) + 1 << endl;
  return 0;
}
