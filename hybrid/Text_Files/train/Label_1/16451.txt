#include <iostream>

int main() {
  int l, r, x, y, k;
  std::cin >> l >> r >> x >> y >> k;
  std::cout << ((double) l / x == k || (double) r / x == k || (double) l / y == k || (double) r / y == k ? "YES" : "NO") << std::endl;
}
