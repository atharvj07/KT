n = int(input())
x0 = 0.0
y0 = 0.0
r0 = 1e5
for _ in range(n):
    x, y, r = map(int, input().split())
    x0 = max(x0, x - r)
    y0 = max(y0, y - r)
    r0 = min(r0, r)

x0 = -x0
y0 = -y0
print(max(4, (x0 - r0 + y0 - r0) // 2 + 1))
