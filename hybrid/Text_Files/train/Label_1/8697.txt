#include <iostream>
#include <string>
#include <set>

using namespace std;

int main() {
  string s;
  cin >> s;
  set<string> seen;
  int n = s.size();
  for (int i = 0; i < n; ++i) {
    s = s.substr(1) + s[0];
    seen.insert(s);
  }
  cout << seen.size() << endl;
  return 0;
}
