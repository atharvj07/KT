n, T = map(int, input().split())
a = list(map(int, input().split()))

dp = [0] * 301
for i in range(n):
    for j in range(a[i], 301):
        dp[j] = max(dp[j], dp[j - a[i]] + 1)

total = sum(a)
result = dp[total % n] + (total // n) * T
print(result)
