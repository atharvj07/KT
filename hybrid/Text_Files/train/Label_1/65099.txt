['from sys import stdin\n\ndef make(arr, k):\n    n = len(arr)\n    arr.sort()\n    if sum(arr) <= k:\n        return 0\n    i, ans = 0, n\n    while i < n:\n        if arr[i] <= k:\n            k -= arr[i]\n            i += 1\n        else:\n            ans = min(ans, i + (k // arr[i]))\n            break\n    return ans\n\nfor _ in range(int(stdin.readline())):\n    n, k = map(int, stdin.readline().split())\n    arr = list(map(int, stdin.readline().split()))\n    print(make(arr, k))\n']