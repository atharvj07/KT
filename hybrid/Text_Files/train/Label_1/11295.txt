#include <cstdio>
#include <algorithm>
int main() {
  int t, x, n, m;
  scanf("%d", &t);
  while (t--) {
    scanf("%d%d%d", &x, &n, &m);
    puts((x - 10 * n - 10 * m + 1) % 2 ? "NO" : "YES");
  }
}
