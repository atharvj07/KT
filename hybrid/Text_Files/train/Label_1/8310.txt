#include <iostream>
using namespace std;
int main() {
    int a, b, c, x, y, z;
    cin >> x >> y >> z >> a >> b >> c;
    if (a >= x && (b + c >= y + z))
        cout << "YES";
    else
        cout << "NO";
}
