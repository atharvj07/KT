#include <iostream>
#include <cmath>

int main() {
    int x1, y1, x2, y2, x, y;
    std::cin >> x1 >> y1 >> x2 >> y2 >> x >> y;
    bool possible = (x2 - x1) % x == 0 && (y2 - y1) % y == 0;
    std::cout << (possible ? "YES" : "NO") << '\n';
    return 0;
}
