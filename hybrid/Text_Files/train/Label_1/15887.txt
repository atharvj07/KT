#include <iostream>

int main() {
  int x, y, z, t1, t2, t3;
  std::cin >> x >> y >> z >> t1 >> t2 >> t3;
  std::cout << (abs(x - y) * t1 <= abs(y - z) * t2 + 2 * t3 ? "YES" : "NO") << std::endl;
  return 0;
}
