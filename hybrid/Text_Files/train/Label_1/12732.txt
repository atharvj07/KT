#include <iostream>
using namespace std;

int main() {
    long long n, m, k, l;
    cin >> n >> m >> k >> l;
    if (m * (m - 1) / 2 < l || l > n - k) cout << -1 << endl;
    else cout << max((n - k + l - 1) / m + 1, l) << endl;
}
