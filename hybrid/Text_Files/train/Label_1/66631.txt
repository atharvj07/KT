import math

n, k = map(int, input().split())

red_notebooks = math.ceil((2 * n) / k)
green_notebooks = math.ceil((5 * n) / k)
blue_notebooks = math.ceil((8 * n) / k)

total_notebooks = red_notebooks + green_notebooks + blue_notebooks

print(total_notebooks)
