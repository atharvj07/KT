#include <iostream>

typedef long long LL;
LL gcd(LL a, LL b) {
  while (b) b ^= a ^= b ^= a %= b;
  return a;
}

int main() {
  LL a, b;
  std::cin >> a >> b;
  std::cout << (a + b) * 2 - 2 * gcd(a, b) << std::endl;
}
