#include<iostream>
#include<vector>
using namespace std;
int main(){
  int n, c, ans = 0, x;
  cin >> n >> c;
  vector<int> a(n);
  for(int i = 0; i < n; i++){
    cin >> a[i];
  }
  for(int i = 0; i < n; i++){
    if(a[i] > c){
      ans += a[i] - c;
      c = a[i];
    }
  }
  cout << ans;
}
