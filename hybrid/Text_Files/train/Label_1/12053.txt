#include <algorithm>
#include <iostream>

using namespace std;

typedef long long ll;
const int mod = 1e9 + 7;

int main() {
  ll t;
  cin >> t;
  while (t--) {
    ll n, k;
    cin >> n >> k;
    cout << (n * (1LL << (k - 1)) % mod) << endl;
  }  
}
