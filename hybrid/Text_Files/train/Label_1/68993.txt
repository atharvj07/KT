def maxdigitsum(x):
    maxs = 0
    res = 0
    for i in range(len(str(x))):
        if int(str(x)[i]) > maxs :
            maxs = int(str(x)[i])
    x = int(str(x).replace(str(maxs),"9"))
    return x
x = int(input())
print(maxdigitsum(x))
