#include<iostream>
using namespace std;

int main(){
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    if(n % 3 == 0) cout << 0;
    else if(n % 3 == 1) cout << min(a, min(b, c));
    else cout << min(2 * min(a, min(b, c)), a + b + c - min(a, min(b, c)));
    return 0;
}
