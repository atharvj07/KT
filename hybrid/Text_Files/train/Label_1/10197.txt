#include <cstdio>
#include <algorithm>

int main() {
    int n; scanf("%d", &n);
    int a[n], mx = 1e9;
    for (int i = 0; i < n; i++) {
        scanf("%d", a+i);
        mx = std::min(mx, a[i]);
    }
    int ans = 0;
    for (int i = 0; i < n; i++) ans += a[i] - mx;
    printf("%d\n", ans);
}
