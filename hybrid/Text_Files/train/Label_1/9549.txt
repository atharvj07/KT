#include <iostream>
using namespace std;
long long gcd(long long a, long long b) { return b ? gcd(b, a % b) : a; }
int main() {
  long long a, b;
  cin >> a >> b;
  long long g = gcd(a, b);
  cout << (a + b) * 2 - 2 * (g + a / g + b / g) + 2 * (a / g + b / g);
  return 0;
}
