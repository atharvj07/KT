#include <iostream>
#include <vector>
using namespace std;
int main() {
  string s;
  cin >> s;
  int ans = 0, cnt = 0;
  for (char c : s) {
    if (c == 'a') cnt++;
    if (cnt * 2 > ans * (ans + 1)) ans++;
  }
  cout << ans << endl;
  return 0;
}
