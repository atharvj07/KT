from bisect import bisect_left

N=int(input())
A=[[int(v) for v in input().split()] for i in range(N)]
B=[int(v) for v in input().split()]
A.sort(key=lambda v: (v[1],v[0]))
if B==sorted(B):
    print("YES")
    for i in range(N):
        print(*bisect_left(A,[0,B[i]],lo=i))
else:
    print("NO")
