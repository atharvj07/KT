#include <iostream>
#include <vector>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long a, b;
        cin >> a >> b;
        cout << abs(b - a) / 10 + (abs(b - a) % 10 != 0) << endl;
    }
    return 0;
}
