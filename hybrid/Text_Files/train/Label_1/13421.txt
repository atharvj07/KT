#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
const int LIM = 2e9 + 3;

int main() {
    ll n, k;
    cin >> n >> k;
    ll now = n * (n + 1) / 2ll;
    while (k > now) {
        k -= now;
        n--;
        now = n * (n + 1) / 2ll;
    }
    ll idx = (k - 1) / n + 1;
    ll ord = (k - 1) % n + 1;
    cout << idx << endl;
    return 0;
}
