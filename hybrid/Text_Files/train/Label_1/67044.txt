from collections import defaultdict

ss = input()
suff = defaultdict(int)
for i in ss[::-1]:
    suff[i] = suff[i] + 1
ans = []
pref = defaultdict(int)
for s in ss:
    pref[s] = pref[s] + 1
    ans.append((len(pref), pref[s] * suff[s]))
print(len(ans))
for p in ans:
    print(*p)
