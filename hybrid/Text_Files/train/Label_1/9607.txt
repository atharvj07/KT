#include <iostream>

int main() {
  int n, s;
  std::cin >> n >> s;
  int a[n + 1], b[n + 1];
  for (int i = 1; i <= n; ++i) { std::cin >> a[i]; }
  for (int i = 1; i <= n; ++i) { std::cin >> b[i]; }
  std::cout << ((a[s] || (s != 1 && b[s - 1])) ? "YES" : "NO") << std::endl;
}
