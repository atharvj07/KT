#include <cstdio>
using namespace std;
int main() {
  int t;
  scanf("%d", &t);
  while (t--) {
    int d, k;
    scanf("%d%d", &d, &k);
    puts((d * d) % (2 * k) ? "Utkarsh" : "Ashish");
  }
  return 0;
}
