import sys

n = int(input())
a = list(map(int, input().split()))
v = list(map(int, input().split()))
ans = 0
for i in range(n):
    for j in range(i + 1, n):
        if v[i] * v[j] < 0:
            ans += min(abs(a[i] - a[j]), abs(a[i] - a[j] + v[j] - v[i]))
print(ans)
