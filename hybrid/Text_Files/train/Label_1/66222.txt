n, m = map(int, input().split())
a = [list(map(int, input().split())) for i in range(n)]
ans = 0
for j in range(m):
    b = sorted([a[i][j] for i in range(n)])
    for i in range(n):
        ans += abs(a[i][j] - b[i])
        a[i][j] = b[i]
print(ans)
