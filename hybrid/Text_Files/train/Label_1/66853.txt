import sys
for t in range(int(input())):
    n=int(input())
    d=list(map(int,input().split()))
    d.sort()
    mn=d[1]
    flag=1
    for i in range(2,n):
        if(d[i]%mn==0):
            flag=0
            break
        mn=min(d[i],mn)
    if(flag):print(mn*d[-1]);
    else:print(-1)
