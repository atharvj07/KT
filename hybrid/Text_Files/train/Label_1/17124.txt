#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, d, x, p;
    cin >> n >> d;
    cin >> x;
    p = x;
    for (int i = 1; i < n; i++) {
        cin >> x;
        p = __gcd(p, x - p);
    }
    cout << d / p << endl;
    return 0;
}
