#include <cstdio>
int main() {
  int n, k;
  scanf("%d %d", &n, &k);
  int cnt = 0, ans = 0;
  char c;
  for (int i = 1; i <= n; i++) {
    scanf(" %c", &c);
    if (c == 'N') cnt++; else cnt = 0;
    if (cnt > k) ans = 1;
  }
  puts(ans ? "YES" : "NO");
  return 0;
}
