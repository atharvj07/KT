#include <iostream>

using namespace std;

int main() {
    long long x, y, m;
    cin >> x >> y >> m;
    if (x >= m || y >= m) cout << "0" << endl;
    else if (x + y >= m) cout << "1" << endl;
    else cout << "-1" << endl;
    return 0;
}
