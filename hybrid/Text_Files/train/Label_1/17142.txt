#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k;
  cin >> n >> k;
  int ra = 0, rb = 0;
  for (int i = 0; i < n; ++i) {
    int x, y;
    cin >> x >> y;
    ra += x / k;
    rb += y / k;
  }
  cout << min(ra, rb) << endl;
}
