#include <iostream>

using namespace std;

int main() {
  long long n;
  while (cin >> n) {
    long long ans = 1;
    for (long long i = 2; i * i <= n; i++) {
      if (n % i == 0) {
        ans++;
        if (n / i != i) ans++;
      }
    }
    cout << ans << endl;
  }
  return 0;
}
