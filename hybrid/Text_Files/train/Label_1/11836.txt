#include<bits/stdc++.h>

using namespace std;

int main() {
    long long n, k, a, b, x, y;
    cin >> n >> k >> a >> b;
    x = (a > b) ? (b + k - a) / k : (a + k - b) / k;
    y = (k/2 - max(a, b) + k) / k;
    cout << x << " " << y << endl;
    return 0;
}
