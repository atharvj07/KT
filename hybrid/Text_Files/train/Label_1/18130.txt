#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        string s;
        cin >> n >> s;
        if (n != 11 || s[0] != '8')
            cout << "NO" << endl;
        else
            cout << "YES" << endl;
    }
}
