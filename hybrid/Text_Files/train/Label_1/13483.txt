#include <bits/stdc++.h>
#define int long long
using namespace std;
int32_t main()
{
    int mod = 1e9 + 7;
    int n, m;
    cin >> n >> m;
    int pw2[m + 1];
    pw2[0] = 1;
    for (int i = 1; i <= m; i++)
        pw2[i] = pw2[i - 1] * 2 % mod;
    cout << (pw2[m] - 1 + mod) % mod << "\n";
}
