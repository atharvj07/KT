#include <cstdio>
int main() {
    int n, ans = 0;
    bool last = 0;
    scanf("%d", &n);
    for (int i = 0, x; i < n; i++) {
        scanf("%d", &x);
        ans += last & x > 0;
        last = x > 0;
    }
    printf("%d\n", ans);
    return 0;
}
