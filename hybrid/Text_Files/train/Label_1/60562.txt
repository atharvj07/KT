n = int(input())
rings = [tuple(map(int, input().split())) for _ in range(n)]

rings.sort(key=lambda x: -x[1])

dp = [0] * (n + 1)
dp[0] = 0
for i in range(1, n + 1):
    dp[i] = max(dp[i - 1], dp[i])
    dp[i] = max(dp[i], dp[i - 1] + rings[i - 1][2])

print(max(dp))
