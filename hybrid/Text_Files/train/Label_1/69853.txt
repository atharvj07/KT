import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    a.sort()
    ans = 0
    s = 0
    for ai in a:
        s += ai
        if s * 2 <= ai * (ans + 1):
            ans += 1
    print(ans)
