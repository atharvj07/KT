#include <iostream>

int main() {
  int t;
  while (std::cin >> t) {
    for (int i = 0; i < t; ++i) {
      int n;
      std::cin >> n;
      std::cout << (n % 2 == 1 ? n - 1 : n / 2) << std::endl;
    }
  }
}
