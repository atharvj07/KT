from sys import stdin

l, r = map(int, stdin.readline().split())

mx = 0
ans = 0

for i in range(2, int((r**0.5)+1)):
    cnt = (r//i) - (l-1)//i
    if cnt>mx:
        mx = cnt
        ans = i

print(ans)
