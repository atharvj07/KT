#include <iostream>
#include <vector>

int main() {
    std::string s; std::cin >> s;
    int cnt = 0, ans = 0;
    for (int i = 0; i < (int)s.size(); ++i) {
        if (s[i] == '+') ++cnt;
        else if (s[i] == '-') --cnt;
        if (!cnt) ++ans;
    }
    std::cout << ++ans << "\n";
}
