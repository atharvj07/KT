#include <iostream>
#include <string>
#include <algorithm>
using namespace std;
int main() {
    string s;
    int m, a;
    cin >> s >> m;
    while (m--) {
        cin >> a;
        reverse(s.begin() + a - 1, s.begin() + s.size() - a);
    }
    cout << s;
}
