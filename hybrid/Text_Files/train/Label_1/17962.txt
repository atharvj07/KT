#include <bits/stdc++.h>

using namespace std;

int main() {
    long long a, b;
    cin >> a >> b;
    int ans;

    if (a % 2 == 0) a++;
    if (b % 2 == 0) b--;

    ans = (b - a) / 2 + 1;

    cout << ans << endl;

    return 0;
}
