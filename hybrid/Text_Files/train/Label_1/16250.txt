#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  while (n--) {
    long long a, b;
    cin >> a >> b;
    if (a != b)
      cout << (a > b ? a : b) - (a < b ? a : b) + 1 << endl;
    else
      cout << 0 << endl;
  }
}
