for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    b = list(sorted(a))
    dif = [b[i] - a[i] for i in range(n)]
    g = dif[0]
    for i in dif:
        g = gcd(g, i)
    if g == 0:
        print(-1)
    else:
        print(g)
