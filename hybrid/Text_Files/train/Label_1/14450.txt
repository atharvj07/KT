#include <iostream>

using namespace std;

int main() {
  long long k, l, r, t, x, y;
  cin >> k >> l >> r >> t >> x >> y;

  if (t * x - k > t * y)
    cout << "No" << endl;
  else
    cout << "Yes" << endl;
  return 0;
}
