import string

s = input()
print(['Too weak', 'Correct'][all([len(s)>=5,
                                  any(ch.isupper() for ch in s),
                                  any(ch.islower() for ch in s),
                                  any(ch in string.digits for ch in s)])])
