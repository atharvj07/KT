#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  string ans;
  for (int i = 0; i < n; i++) {
    int mid = (n - i - 1) / 2;
    ans += s[mid];
    s.erase(s.begin() + mid);
    n--;
    i--;
  }
  cout << ans << endl;
  return 0;
}
