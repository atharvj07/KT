#include <iostream>

int main() {
  long long a, b, c;
  std::cin >> a >> b >> c;
  if (a == b || (b > a && (b - a) % c == 0))
    std::cout << "YES" << std::endl;
  else
    std::cout << "NO" << std::endl;
  return 0;
}
