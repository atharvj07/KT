#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &x : a) cin >> x;
  if (n == 1 || *min_element(a.begin(), a.end()) * 2 >= *max_element(a.begin(), a.end()))
    cout << "sjfnb\n";
  else
    cout << "cslnb\n";
}
