n = int(input())
a = list(map(int, input().split()))
dp = [0] * n
dp[0] = a[0]
for i in range(1, n):
    if a[i] == 1: dp[i] = max([0] + dp[:i])
    elif a[i] == 2: dp[i] = max(dp[:i] + [a[i]])
    else: dp[i] = dp[i - 1] + a[i]
    dp[i] = max(dp[i], max(dp[:i] + [0] * i))
print(max(dp))
