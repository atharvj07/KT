#include <bits/stdc++.h>
using namespace std;
#define int long long
const int mod = 1e9 + 7;
const int maxN = 1e5 + 3;
int32_t main() {
    int n, k;
    cin >> n >> k;
    string s;
    cin >> s;
    if (n % 2 == k % 2) {
        cout << "tokitsukaze";
        return 0;
    }
    if (k == 1) {
        cout << "once again";
        return 0;
    }
    bool b = true;
    for (int i = 0; i < n; i += k) {
        b &= s[i] == '1';
    }
    if (!b) {
        cout << "tokitsukaze";
        return 0;
    }
    cout << "quailty";
    return 0;
}
