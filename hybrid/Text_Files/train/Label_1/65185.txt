from collections import defaultdict, deque
d = defaultdict(deque)
for typ, t, x in (map(int, input().split()) for _ in range(int(input()))):
    if typ == 1: d[x].append(t)
    elif typ == 2: d[x].popleft()
    else: print(len(list(filter(lambda y: y<=t, d[x]))))
