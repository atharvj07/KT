#include <iostream>
#include <cmath>
int main() {
    long long int n;
    std::cin >> n;
    long long int s = (long long int)sqrt(n);
    while (s * s != n) {
        s++;
    }
    std::cout << s << ' ' << s * s / s << std::endl;
    return 0;
}
