import sys

def gen_permutation(n):
    p = list(range(n,0,-1))
    for i in range(n):
        pos = (i + p[i]) % n
        p[i], p[pos] = p[pos], p[i]
    return " ".join(map(str,p))

for t in range(int(input())):
    print(gen_permutation(int(input())))
