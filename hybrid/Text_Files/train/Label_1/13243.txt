#include <iostream>

int main() {
    long long n, k;
    std::cin >> n >> k;
    k--;
    if (k < n)
        std::cout << 2 * k + 1 << std::endl;
    else
        std::cout << 2 * (k - n) << std::endl;
}
