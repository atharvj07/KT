#include <cstdio>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
  int n;
  scanf("%d", &n);
  vector<int> b(n);
  for (int i = 0; i < n; i++) scanf("%d", &b[i]);
  sort(b.begin(), b.end());
  printf("%d %d\n", b.back() - b.front(), b.size() * (b.size() - 1) / 2);
}
