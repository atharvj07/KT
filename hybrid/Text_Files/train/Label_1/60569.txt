mod = int(1e9) + 7

n, q = map(int, input().split())
arr = [int(x) for x in input()]
arr.insert(0, 0)
for i in range(1, n + 1):
    arr[i] += arr[i - 1]

for i in range(q):
    l, r = map(int, input().split())
    print((arr[r] - arr[l - 1]) * (1 + arr[r] - arr[l - 1]) % mod)
