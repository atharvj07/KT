#include <iostream>
#include <string>

int main() {
  std::string s;
  int a, b;
  std::cin >> s >> a >> b;
  std::cout << (s[s.size() - 1] == '0' || s.size() == 1 && s == "0" || s.size() > 1 && stoi(s.substr(s.size() - 1)) % b == 0 ? "YES\n" + s.substr(0, s.size() - 1) + "\n" + s.back() : "NO\n");
}
