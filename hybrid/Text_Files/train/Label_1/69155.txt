def f(x, y):
    return (x|y) - y

def optimal_reorder(arr, n):
    arr.sort(key = lambda x: bin(x).count('1'), reverse = True)
    return arr

n = int(input())
arr = list(map(int, input().split()))
print(*optimal_reorder(arr, n))
