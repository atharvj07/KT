n, m, k = map(int, input().split())
a = [list(map(int, input().split())) for i in range(n)]
ans = [0] * m
for j in range(m):
    b = [x[j] for x in a]
    b.sort()
    for i in range(min(k, n)):
        ans[j] += b[i]
        k -= b[i]
        if k == 0:
            break
print(*ans)
