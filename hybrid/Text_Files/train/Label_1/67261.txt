import sys

def solve(x, y):
  if x == y:  return 'Impossible'
  if x > y:
    x, y = y, x
  if y - x > 1:  return 'Impossible'
  if x % 2 == y % 2:  return str(x//2 + y//2) + 'A' + str(x//2 + y//2) + 'B'
  else:  return 'Impossible'

print(solve(*map(int, sys.stdin.readline().split())))
