#include <iostream>
using namespace std;

int main() {
    long long n, x, y;
    cin >> n >> x >> y;
    while (n--) {
        long long a;
        cin >> a;
        cout << (a % (x + y) ? "Vanya" : "Both") << endl;
    }
    return 0;
}
