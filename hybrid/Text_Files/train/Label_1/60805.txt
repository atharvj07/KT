n = int(input())
a = list(map(int, input().split()))
x, f = [int(c) for c in input().split()]
ans = 0
for i in a:
    if i <= x: continue
    if i % x == 0: ans += (i // x - 1) * f
    else: ans += (i // x) * f
print(ans)
