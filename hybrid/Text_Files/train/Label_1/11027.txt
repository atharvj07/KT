#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin>>n;
    vector<int> v(n);
    for(int i=0;i<n;i++)cin>>v[i];
    int ans=0, cnt=0;
    for(auto x:v){
        if(x==0)cnt++;
        if(x==1)ans+=(!cnt);
    }
    cout<<ans;
}
