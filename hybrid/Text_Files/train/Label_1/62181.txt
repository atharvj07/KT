mod = 10**9 + 7

def solve(n, k):
    ans = 0
    val = 1
    while k:
        if k % 2:
            ans = (ans + val) % mod
        k //= 2
        val = (val * n) % mod
    return ans

t = int(input())
for _ in range(t):
    n, k = map(int, input().split())
    print(solve(n, k))
