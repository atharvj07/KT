from collections import defaultdict, deque

n, m = map(int, input().split())
G = defaultdict(list)
for _ in range(n-1):
    u, v, w = map(int, input().split())
    G[u].append([v, w])
    G[v].append([u, w])

Q = list(map(int, input().split()))

def dfs(u, p, maxw):
    if u in dist: return dist[u]
    dist[u] = 1
    for v, w in G[u]:
        if v == p: continue
        if w <= maxw: dist[u] += dfs(v, u, maxw)
    return dist[u]

for q in Q:
    dist = {}
    ans = dfs(1, 1, q)
    ans = ans * (ans - 1) // 2
    print(ans)

