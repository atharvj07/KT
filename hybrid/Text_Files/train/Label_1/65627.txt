import sys
input = sys.stdin.readline

n = int(input())
a = sorted([(x+y,x-y) for x,y in (map(int,input().split())for _ in range(n))])
ans = 0
for x,y in a:
    if y<0:
        ans += -y-1
        y = -1
    ans += x-y
print(ans)
