import sys
input = sys.stdin.readline

n = int(input())
a = sorted([int(x) for x in input().split()])
i, ans = 0, 0
for j in range(n):
    if j > 0 and a[j] - a[j - 1] > 5:
        i = j
    ans = max(ans, j - i + 1)
print(ans)
