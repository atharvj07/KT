#include <bits/stdc++.h>
using namespace std;
int main() {
    int t; cin >> t;
    while (t--) {
        vector<int> v(7);
        for (auto &x : v) cin >> x;
        cout << v[0] << " " << v[3] - v[0] << " " << v[6] - v[3] << '\n';
    }
}
