#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n;
  cin >> n;
  int ans = 0;
  vector<pair<int, int>> v(n);
  for (auto &x : v) cin >> x.first >> x.second;
  for (auto x : v) {
    if (x.second >= 2) ans++;
  }
  cout << ans << endl;
  return 0;
}
