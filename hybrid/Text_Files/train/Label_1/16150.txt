#include <iostream>
using namespace std;
int main() {
  long long int a, b, ans;
  cin >> a >> b;
  if (!b) ans = -1;
  else if (a % b)
    ans = 0;
  else
    ans = b;
  if (ans == -1)
    cout << "infinity" << endl;
  else
    cout << ans << endl;
}
