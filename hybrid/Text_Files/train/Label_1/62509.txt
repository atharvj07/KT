t = int(input())
for _ in range(t):
    n, k = map(int, input().split())
    steps = 0
    while n > 0:
        if n % k == 0:
            n //= k
            steps += 1
        else:
            steps += n % k
            n -= n % k
    print(steps)
