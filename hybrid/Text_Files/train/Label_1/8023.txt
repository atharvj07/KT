#include<bits/stdc++.h>
using namespace std;
int main()
{
    int a[5],i,sum=0,mx=0;
    for(i=0; i<5; i++)
    {
        cin>>a[i];
        sum+=a[i];
        mx=max(mx,a[i]);
    }
    if(sum%2 || mx>sum/2)
        cout<<-1<<endl;
    else
        cout<<(sum-mx)/2<<endl;
    return 0;
}
