#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,i,big,small;
    cin>>n>>m;
    vector<long long int>a(n),b(m);
    map<long long int,int>f;
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<m;i++)
    {
        cin>>b[i];
        f[b[i]]=0;
    }
    sort(a.begin(),a.end());
    sort(b.begin(),b.end());
    if(a[0]<b[m-1])
    {
        cout<<b[m-1]-a[0];
    }
    else
    {
        cout<<"0";
    }
    return 0;
}
