#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n, ans = 0;
  cin >> n;
  vector<int> a(n);
  for (auto &it : a) cin >> it;
  for (int l = 0, r = 0; r < n; ++r) {
    if (l < r && a[r] < a[r - 1]) ++l;
    ans = max(ans, r - l + 1);
  }
  cout << ans << endl;
}
