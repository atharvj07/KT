#include <iostream>
using namespace std;
int main() {
  int x, y, z, t1, t2, t3;
  cin >> x >> y >> z >> t1 >> t2 >> t3;
  cout << (abs(y - x) * t1 <= (abs(x - z) + abs(z - y) + 2) * t2 + t3 ? "YES" : "NO");
  return 0;
}
