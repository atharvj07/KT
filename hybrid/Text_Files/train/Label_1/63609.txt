import sys
n, m, k = map(int, input().split())
a = list(map(int, input().split()))
b = []
for j in range(k):
    for i in range(j+1):
        a[j] = min(a[j], a[i])
    if len(b) == 0 or b[-1] != a[j]:
        b.append(a[j])
if len(b) == k:
    print("YES")
else:
    print("NO")
