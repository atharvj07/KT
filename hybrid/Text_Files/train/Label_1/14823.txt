#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  map<string, int> mp;
  while (n--) {
    string s;
    cin >> s;
    mp[s]++;
    for (auto it : mp) {
      if (it.second > 0) {
        cout << it.first << endl;
        it.second--;
      }
    }
  }
  return 0;
}
