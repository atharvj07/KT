#include <iostream>
#include <cassert>
#include <algorithm>

using std::cin;
using std::cout;

int main() {
    int n, k, m;
    cin >> n >> k >> m;
    k = (k - 1) % n + 1;
    int rem = n - k * (m % (n / k));
    cout << rem * m + (n / k);
    return 0;
}
