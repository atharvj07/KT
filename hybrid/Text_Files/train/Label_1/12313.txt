#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n, erased = 0;
  cin >> n;
  vector<int> a(n);
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
    if (i > 0 && a[i] - a[i - 1] > 1) erased = max(erased, i - 1);
  }
  cout << erased << endl;
  return 0;
}
