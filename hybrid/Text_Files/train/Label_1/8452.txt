#include <bits/stdc++.h>
using namespace std;
string s;
int main() {
  cin >> s;
  for (int k = 0; k < s.length(); k++) {
    if (k + 1 == s.length() || s[k] < s[k + 1]) {
      cout << "Mike" << endl;
    } else {
      cout << "Ann" << endl;
    }
  }
}
