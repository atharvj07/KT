s = input()
w = ''
a = ''
b = ''
for c in s:
    if c in ",;":
        if w.isdigit() and (not a or a[-1] == ','):
            a += w + ','
        elif w:
            b += w + ','
        w = ''
    else:
        w += c
if w.isdigit() and (not a or a[-1] == ','):
    a += w + ','
elif w:
    b += w + ','
print(f'"{a[:-1]}"' if a else '-')
print(f'"{b[:-1]}"' if b else '-')
