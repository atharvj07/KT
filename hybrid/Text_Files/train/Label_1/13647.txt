#include <bits/stdc++.h>
int main() {
  int n;
  std::cin >> n;
  std::string s;
  std::cin >> s;
  int sum = 0;
  int ans = 0;
  for (int i = 0; i < n; i++) {
    sum += (s[i] == '-');
    ans = std::max(ans, sum);
  }
  std::cout << ans << '\n';
}
