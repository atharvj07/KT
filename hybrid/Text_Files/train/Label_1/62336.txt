n, k = map(int, input().split())
a = list(map(int, input().split()))
s = sum(a[:k])
l, avg = a[:k], s / k
for i in range(k, n):
    s += a[i] - a[i - k]
    avg += s / k
    l.append(s / k)
print(*l, sep='\n')
