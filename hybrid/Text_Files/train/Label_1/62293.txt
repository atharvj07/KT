def minlex(x, y):
    j = 0
    for i in range(len(y)-1, -1, -1):
        if y[i] == '0':
            j += 1
        else:
            break
    return j

for _ in range(int(input())):
    x = input()[::-1]
    y = input()[::-1]
    print(minlex(x, y))
