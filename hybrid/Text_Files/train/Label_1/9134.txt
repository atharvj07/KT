#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> t(n);
  int d = 0;
  for (auto &x : t) {
    cin >> x;
    if (d == 0) d = x - t[0];
    else if (d != x - t[0]) {
      d = 0;
      break;
    }
  }
  cout << (d == 0 ? t.back() : t.back() + d);
}
