#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    int n;
    cin >> n;
    ll ma = 0, mi = 1e18;
    for (int i = 0; i < n; i++) {
        ll a;
        cin >> a;
        ma = max(ma, a);
        mi = min(mi, a);
    }
    cout << ma - mi << endl;
    return 0;
}
