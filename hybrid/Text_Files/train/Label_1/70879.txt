import sys

n, q = map(int, input().split())
apps = [0] * n
ans = [0] * q

for i in range(q):
    tp, x = map(int, input().split())
    if tp == 1: apps[x - 1] += 1
    if tp == 2: apps[x - 1] = 0
    if tp == 3: ans[i] = sum(apps[:x])
print(*ans)
