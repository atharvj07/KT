#include <iostream>
int main() {
  int n;
  std::cin >> n;
  bool odd = 1;
  for (int i = 0, x; i < n; i++) {
    std::cin >> x;
    odd &= x % 2;
  }
  std::cout << ((n % 2 && odd) ? "Yes" : "No") << std::endl;
}
