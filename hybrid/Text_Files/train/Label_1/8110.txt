#include <iostream>

int main() {
  int n, t, k, d;
  std::cin >> n >> t >> k >> d;
  if ((n + k - 1) / k * t <= d)
    std::cout << "NO" << std::endl;
  else
    std::cout << "YES" << std::endl;
  return 0;
}
