import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int m = sc.nextInt();
        ArrayList<ArrayList<Integer>> g = new ArrayList<>();
        for (int i=0; i<=n; i++) g.add(new ArrayList<>());
        for (int i=0; i<m; i++) {
            int a = sc.nextInt(), b = sc.nextInt();
            g.get(a).add(b); g.get(b).add(a);
        }
        if (n == 2) {
            System.out.println("YES\n0");
            return;
        }
        for (int i=1, d; i<=n; i++) if (g.get(i).size() >= 3) {
            System.out.println("NO");
            return;
        }
        Queue<Integer> q = new LinkedList<>();
        for (int i=1; i<=n; i++) if (g.get(i).size() == 1) q.add(i);
        m = 0;
        int[] e = new int[m+1];
        while (!q.isEmpty()) {
            int s = q.poll();
            for (int x: g.get(s))
                if (e[g.get(s).indexOf(x)] == 0) {
                    e[g.get(s).indexOf(x)] = (s < x) ? 0 : 1; m++;
                    g.get(x).remove((Integer)s);
                    if (g.get(x).size() == 1) q.add(x);
                }
        }
        System.out.println("YES");
        for (int i=0; i<m; i++) System.out.print(e[i]);
        System.out.println();
    }
}
