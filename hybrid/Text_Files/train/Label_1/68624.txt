import sys
n,k=map(int,input().split())
t=list(map(int,input().split()))
ans,cur=0,0
for x in t:
    if x>=0:
        if cur:
            ans+=1
        cur=0
    else:
        cur+=1
        if cur==k:
            ans+=1
            cur=0
print(ans if cur==0 else -1)
