n, k = map(int, input().split())
a = list(map(int, input().split()))
b = []
ans = 0
for i in reversed(range(9)):
    c = []
    for x in a:
        y = x % k
        if y not in c: c.append(y)
        if y == 0 or k - y in c: ans += len(b)
        b.append(x % k)
print(ans)
