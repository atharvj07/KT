#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int q, d;
    cin >> q >> d;
    for (int i = 0; i < q; i++) {
      int a;
      cin >> a;
      cout << (a >= d ? "YES\n" : "NO\n");
    }
  }
  return 0;
}
