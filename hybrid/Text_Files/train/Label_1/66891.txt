import sys
input = sys.stdin.readline

n, m, k = map(int,input().split())
a = list(map(int,input().split()))
b = [0]*n
for i in range(n):
    if ((i+1)%m==1 or b[i-1]==0):
        b[i] = a[i]-k
    else:
        b[i] = b[i-1]+a[i]-k
print(max(b))
