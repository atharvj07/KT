#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  int a, b;
  int ans = 0, cur = 0;
  int mx = 0;
  for (int i = 0; i < n; i++) {
    cin >> a >> b;
    if (a == b) cur++;
    else
      cur = 0;
    ans = max(ans, cur);
  }
  cout << ans + 1 << endl;
}
