for _ in range(int(input())):
    n = input()
    if n[-1] in '2468':
        print(0)
    else:
        d = [c for c in n if c in '2468']
        if len(d) == 0:
            print(-1)
        else:
            idx = n.find(d[0])
            print(idx + 1)
