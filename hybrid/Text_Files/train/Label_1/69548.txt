import math

n = int(input())
a = list(map(int, input().split()))
p = int(input())

def fact(n): 
    return math.factorial(n)

num = 0
for i in range(n): 
    for j in range(i + 1): 
        if sum(a[0:j]) + a[i] <= p: 
            num += fact(n) / (fact(j) * fact(n - j))

print(num / fact(n))
