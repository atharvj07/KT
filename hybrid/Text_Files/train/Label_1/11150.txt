#include <algorithm>
#include <iostream>
#include <vector>
using namespace std;

int main() {
  string a;
  cin >> a;
  int n = a.size();
  string b = a;
  reverse(a.begin(), a.end());
  b += a;
  cout << b << endl;
  return 0;
}
