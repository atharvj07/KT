import sys
sys.setrecursionlimit(1<<30)

inp = lambda : map(int, input().split())
q, = inp()
for _ in range(q):
    n, = inp()
    a, b = zip(*[inp() for _ in range(n)])
    mn = 0
    for i in range(n):
        if i and a[i] == a[i - 1]:
            mn += min(b[i], b[i - 1])
    print(mn)
