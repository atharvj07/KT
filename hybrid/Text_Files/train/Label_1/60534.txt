for _ in range(int(input())):
    s = input()
    l = []
    for c in s:
        if c in '[(': l.append(c)
        elif c == ')':
            if l and l[-1] == '(': l.pop()
            else: break
        else:
            if l and l[-1] == '[': l.pop()
            else: break
    print(len(l))
