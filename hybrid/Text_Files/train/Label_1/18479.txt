#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  int k = min(n, m);
  cout << k << endl;
  for (int i = 0; i < k; i++) {
    cout << i + 1 << " " << i + 1 << endl;
  }
  return 0;
}
