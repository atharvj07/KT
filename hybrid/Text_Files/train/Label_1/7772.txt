#include <iostream>
using namespace std;

int main() {
  long long n, m, a, d, ans = 0, last = -1e18;
  cin >> n >> m >> a >> d;
  for (int i = 0; i < m; i++) {
    long long t;
    cin >> t;
    ans += (t - last > d);
    last = t;
  }
  ans += ((n * a - last) > d);
  cout << ans << endl;
}
