n, m = map(int, input().split())
rows = set()
cols = set()
for i in range(m):
    x, y = map(int, input().split())
    rows.add(x)
    cols.add(y)
    print((n - len(rows)) * (n - len(cols)), end=' ')
