#define ll long long
#include <cstdio>
int p[100005];
int main() {
  int n, x, l = -1, r;
  scanf("%d", &n);
  for (int i = 1; i <= n; i++) {
    scanf("%d", &x);
    if (x > p[i - 1] && (l == -1 || p[i] > x)) {
      l = i;
    }
    p[i] = x;
  }
  for (r = l + 1; p[r] > p[r + 1] && r < n; r++) {
  }
  if (r == n) {
    for (r = l + 1; p[r] == p[r + 1] && r < n; r++) {
    }
    if (r == n)
      l = -1;
  }
  printf("%d", l);
}
