n, k = map(int, input().split())
a = list(map(int, input().split()))

total_sum = sum(a[:k])
week_sums = [total_sum]

for i in range(k, n):
    total_sum += a[i] - a[i-k]
    week_sums.append(total_sum)

average = sum(week_sums) / (n - k + 1)
print(f'{average:.6f}')
