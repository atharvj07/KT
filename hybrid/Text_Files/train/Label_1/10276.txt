#include <bits/stdc++.h>
using namespace std;
int a[5];
char s[100010];
int main() {
  cin >> a[1] >> a[2] >> a[3] >> a[4] >> s;
  int ans = 0;
  for (int i = 0; s[i]; i++) { ans += a[s[i] - '0']; }
  cout << ans << endl;
  return 0;
}
