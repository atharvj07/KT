#include <iostream>
using namespace std;
int main() {
    int a, b, p = 0, k = 0;
    cin >> a >> b;
    p = a;
    while (p >= b) {
        k++;
        p -= b - 1;
    }
    cout << a + k << endl;
    return 0;
}
