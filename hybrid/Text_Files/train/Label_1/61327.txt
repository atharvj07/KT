for t in range(int(input())):
    n, x = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort(reverse=True)
    cnt, mx = 0, 0
    for y in a:
        mx = max(mx, y)
        cnt += (y * (cnt + 1) >= x)
    print(cnt)
