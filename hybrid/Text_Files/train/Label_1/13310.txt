#include <iostream>

int main() {
  int k;
  std::cin >> k;
  int p = 0, c = 0;
  for (int i = 1; c != k; ++i) {
    int s = 0;
    for (int j = i; j; j /= 10) { s += j % 10; }
    if (s == 10) { p = i; ++c; }
  }
  std::cout << p << '\n';
}
