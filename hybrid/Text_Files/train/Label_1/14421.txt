#include <iostream>

int main() {
  int h, m;
  char c;
  std::cin >> h >> c >> m;
  int h1, m1;
  std::cin >> h1 >> c >> m1;
  int p = 60 * h + m - 60 * h1 - m1;
  if (p < 0) p += 24 * 60;
  std::cout << p / 60 << ":" << (p % 60 < 10 ? "0" : "") << p % 60 << "\n";
}
