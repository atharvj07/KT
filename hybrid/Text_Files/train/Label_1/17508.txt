#include <cstdio>
#include <cstring>
int main() {
    int t;
    char s[102];
    for (scanf("%d", &t); t--;) {
        scanf("%s", s);
        puts(!strcmp(s, s + strlen(s) / 2) ? "YES" : "NO");
    }
    return 0;
}
