#include <bits/stdc++.h>
using namespace std;

int main() {
  string s;
  int m;
  cin >> s >> m;
  while (m--) {
    int a;
    cin >> a;
    reverse(s.begin() + a - 1, s.end() - a + 1);
  }
  cout << s;
}
