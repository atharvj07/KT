#include <bits/stdc++.h>
using namespace std;
int main() {
    long long q, k;
    cin >> q;
    while (q--) {
        cin >> k;
        k--;
        long long n = (sqrt(8 * k + 1) - 1) / 2;
        k -= n * (n + 1) / 2;
        cout << ((k + n) % 10) << "\n";
    }
    return 0;
}
