import sys

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    ans = True
    for i in range(1, n):
        if a[i] < a[i-1]:
            ans = False
            break
    print("YES" if ans else "NO")
