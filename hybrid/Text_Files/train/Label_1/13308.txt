#include<bits/stdc++.h>

using namespace std;

int solve(int k) {
    long long x = 1, sum = 0;
    for(; sum <= k;) {
        sum += 9;
        x *= 10;
    }
    int ans = x + k - sum;
    return ans;
}

int main() {
    int k;  cin >> k;
    cout << solve(k) << '\n';
    return 0;
}
