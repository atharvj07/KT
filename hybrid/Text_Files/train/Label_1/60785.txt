import sys

n = int(input())
a = [int(i) for i in sys.stdin.readline().split()]

XOR = [0]
for i in a:
    XOR.append(XOR[-1] ^ i)

cnt = 0

for i in range(n + 1):
    for j in range(i + 1, n + 1):
        if XOR[j] ^ XOR[i - 1] == 0:
            cnt += 1

print(cnt)
