from collections import Counter
n, m = map(int, input().split())
a = [list(map(int, input().split())) for _ in range(n)]
cnt = Counter([j for i in range(n) for j in a[i] if i != (j - 1) // m])
print(min(sum(cnt.values()) // 2, n - 1))
