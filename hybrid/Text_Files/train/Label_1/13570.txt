#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, sum = 0, x;
    cin >> n;
    while (n--) {
        cin >> x;
        sum += x;
    }
    cout << sum << endl;
    return 0;
}
