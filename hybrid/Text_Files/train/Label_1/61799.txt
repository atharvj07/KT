n = int(input())
a = list(map(int, input().split()))

idx = []
for i in range(n):
    if a[i] == min(a):
        idx.append(i)

ans = idx[1] - idx[0]
for i in range(1, len(idx) - 1):
    ans = min(ans, idx[i + 1] - idx[i])

print(ans)
