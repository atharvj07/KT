from math import ceil
n = int(input())
width, height = 0, 0
for _ in range(n):
    w, h = map(int, input().split())
    width += w * min(1, ceil(n/2))
    height += h * max(1, ceil(n/2))
print(width * height)
