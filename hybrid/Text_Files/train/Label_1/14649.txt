#include <iostream>

using namespace std;

int main() {
  long long n, k;
  cin >> n >> k;
  long long mn = max(0ll, 1ll + k - (n - k + 1) / 2);
  long long mx = max(1ll, k - (n - k) % 2);
  cout << mn << ' ' << mx << '\n';
}
