#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,p,k;
    cin>>n>>p>>k;
    cout<<(p-k<1? "": "<< ");
    for(int i=max(p-k, 1);i<=min(p+k,n);i++)
        cout<<(i==p? "(": " ")<<i<<(i==p? ")": " ");
    cout<<(p+k>n? "": " >>");
    cout<<endl;
    return 0;
}
