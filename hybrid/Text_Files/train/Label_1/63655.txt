n = int(input().strip())
a = [list(map(int, input().split())) for _ in range(n)]

max_values = [max(row) for row in a]
permutation = sorted(range(1, n + 1), key=lambda x: max_values[x - 1])

print(*permutation)
