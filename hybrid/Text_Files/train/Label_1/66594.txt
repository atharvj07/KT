import sys
input = sys.stdin.readline
a, b = map(int, input().split())
s, ans = 0, ''
for i in range(a+b):
    if a:
        ans += 'o'
        a -= 1
        s += 1
    elif b:
        ans += 'x'
        b -= 1
        s -= 1
        if s < 0: s = -1
ans = max(ans, 'x' * (a+b))
print(s * s, ans)
