#include <iostream>
#include <cmath>

using namespace std;

typedef long long ll;

int main() {
  ll x1, y1, x2, y2;
  cin >> x1 >> y1 >> x2 >> y2;
  ll dx = abs(x1 - x2);
  ll dy = abs(y1 - y2);
  ll perimeter = 2 * (dx + dy) + 4;
  cout << perimeter << endl;
  return 0;
}
