import sys

mod=998244353

n=int(input())
arr=list(map(int,input().split()))

ans=[1]*n
arr.insert(0,0)
for i in range(1,n):
    ans[i]=(ans[i-1]*(arr[i]-arr[i-1]+1))%mod
    ans[i]=(ans[i]*(arr[i+1]-arr[i]))%mod
print(ans[n-1])
