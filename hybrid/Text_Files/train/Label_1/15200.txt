#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> v(n);
  for (int &x : v) cin >> x;
  int c = 0, ans = 0;
  for (int x : v)
    if (x > c) {
      ans += (x - 1 - c);
      c = x;
    }
  cout << ans << endl;
  return 0;
}
