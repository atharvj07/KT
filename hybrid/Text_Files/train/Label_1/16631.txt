#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int n;
    cin >> n;
    for (int i = n / 2; i >= 2; i--) {
        if (n % i == 0) {
            cout << n / i - 1 << " " << i << endl;
            return 0;
        }
    }
    cout << "1 2" << endl;
    return 0;
}
