def find_bonus_count(n):
    lcm = 1
    for i in range(2, 11):
        lcm = lcm * i // gcd(lcm, i)
    return n // lcm

def gcd(a, b):
    while b:
        a, b = b, a % b
    return a

n = int(input().strip())
print(find_bonus_count(n))
