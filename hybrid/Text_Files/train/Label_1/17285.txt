#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    string s, t;
    cin >> s >> t;
    n = s.size();
    for (int i = 0; i < n; ++i) {
        if (s[i] != t[i]) cout << "1";
        else cout << "0";
    }
    cout << "\n";
    return 0;
}
