#include <iostream>
#include <cstdio>

using namespace std;

double f(int n) {
    if(n == 1) return 1;
    if(n % 2 == 0) return 1 + f(n/2);
    else return 1 + f(n/2) + 1.0 / (n - n/2);
}

int main() {
    int n;
    while(cin >> n){
        printf("%.12f\n", f(n));
    }
    return 0;
}
