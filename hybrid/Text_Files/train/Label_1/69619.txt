for t in range(int(input())):
    n=int(input())
    l=[list(map(int,input().split())) for i in range(n)]
    l.sort(key=lambda v:v[0])
    q,ans=0,0
    for m,p in l:
        if q>=m:q+=1
        else:ans+=p;q=m+1
    print(ans)
