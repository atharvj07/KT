#include<bits/stdc++.h>
using namespace std;
int n,a[705],flag;
int main(){
    cin>>n;
    for(int i=0;i<n;i++) cin>>a[i];
    sort(a,a+n);
    for(int i=0,j=1;i<n&&j<n;i++){
        int g=__gcd(a[i],a[j]);
        if(g<=1) flag=1,cout<<"No";
        j++;
    }
    if(!flag) cout<<"Yes";
}
