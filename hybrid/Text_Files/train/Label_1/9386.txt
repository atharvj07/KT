#include <iostream>

int main() {
  long long n;
  std::cin >> n;
  long long ans = 0;
  for (long long w = 1; w <= n; ++w) {
    if (n % w == 0) {
      ++ans;
    }
  }
  std::cout << ans << std::endl;
  return 0;
}
