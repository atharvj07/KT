#include <iostream>
#include <map>
#include <string>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  map<char, int> mp;
  bool flag = 0;
  for (char c : s) {
    mp[c]++;
    if (mp[c] > 1) flag = 1;
  }
  cout << (flag ? "Yes" : "No") << endl;
  return 0;
}
