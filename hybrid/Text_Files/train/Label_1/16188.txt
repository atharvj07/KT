#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAXN = 1e9;
int main() {
  int t;
  cin >> t;
  while (t--) {
    ll x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    ll ans = abs(x2 - x1) + abs(y2 - y1);
    cout << ans << endl;
  }
  return 0;
}
