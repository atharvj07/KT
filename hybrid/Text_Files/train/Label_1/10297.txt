#include <iostream>
using namespace std;

int main() {
  int h, m, s, t1, t2;
  cin >> h >> m >> s >> t1 >> t2;
  int hm = h * 60 + m;
  int t = 12 * hm + s;
  cout << (t1 == t2 ? "YES" : ((t1 > t2 && (t1 - t2) <= 6 * (hm + m)) || (t1 < t2 && (t2 - t1) > 6 * (hm + m)) ? "YES" : "NO"))
       << endl;
  return 0;
}
