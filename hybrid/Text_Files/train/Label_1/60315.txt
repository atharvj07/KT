import sys
fin = sys.stdin
for _ in range(int(fin.readline())):
    n = int(fin.readline())
    s, t = fin.readline().rstrip(), fin.readline().rstrip()
    if s == t:
        print('YES')
    else:
        print('NO' if n % 2 == 1 or sum(x != y for x, y in zip(s, t)) > 2 else 'YES')
