#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MOD = 998244853;
int main() {
    ll n, m;
    cin >> n >> m;
    cout << ((n + m) * (n + m + 1)) % MOD / 2 % MOD << endl;
    return 0;
}
