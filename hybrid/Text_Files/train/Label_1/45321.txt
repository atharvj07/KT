import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // Variables
        Scanner scanner = new Scanner(System.in);
        long n = scanner.nextLong(), m = scanner.nextLong(), a = scanner.nextLong(), b = scanner.nextLong(), p = scanner.nextLong(), q = scanner.nextLong();
        long ans = m - (p * n + q);
        System.out.println(Math.max(ans, 0));
    }
}
