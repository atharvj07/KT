import sys

for t in range(int(input())):
    n,k = map(int, input().split())
    s = input()
    ans = s.count('0')
    for i in range(1, n-k):
        if s[i-1] + s[i] + s[i+k] == '010':
            ans -= 1
    print(ans)
