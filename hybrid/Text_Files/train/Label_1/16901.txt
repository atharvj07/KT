#include <bits/stdc++.h>

using namespace std;

int main() {
  int c, v0, v1, a, l;
  cin >> c >> v0 >> v1 >> a >> l;
  int x = v0, d = 1;
  while (x < c) {
    int y = min(v1, x + a);
    x += y - max(0, y - l);
    d++;
  }
  cout << d << endl;
  return 0;
}
