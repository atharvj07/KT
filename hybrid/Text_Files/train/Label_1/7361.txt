#include <bits/stdc++.h>
#define int long long
using namespace std;
signed main() {
    int n, sx, sy, dx, dy, t;
    cin >> n >> sx >> sy >> dx >> dy >> t;
    sx--, sy--, dx--, dy--;
    int x = (sx + dx * t) % n, y = (sy + dy * t) % n;
    x++, y++;
    cout << x << ' ' << y << '\n';
}
