#include<bits/stdc++.h>
using namespace std;
int main(){
    int t, n;
    cin >> t;
    while(t--){
        cin >> n;
        int ans = -1;
        if(n == 1) ans = 0;
        else if(n % 3 == 0) ans = n / 3;
        cout << ans << endl;
    }
    return 0;
}
