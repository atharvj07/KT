#include<bits/stdc++.h>
using namespace std;
#define ll long long
const int mod=1e9+7;
int main(){
	int t; cin>>t;
	while(t--){
		int x; cin>>x;
		string s; cin>>s;
		int n=s.size(),ans=n;
		for(int i=0;i<n;i++){
			if(s[i]!=s[(i+1)%n]) ans+=x;
		}
		cout<<(ans%mod)<<"\n";
	}
	return 0;
}
