#include <cstdio>
#include <iostream>

using namespace std;

int main() {
  int n, ans = 0;
  char c;
  cin >> n;
  int pos = 0;
  while (n--) {
    cin >> c;
    if (c == 'U')
      pos++;
    else
      ans += (pos == 0);
  }
  cout << ans << endl;
  return 0;
}
