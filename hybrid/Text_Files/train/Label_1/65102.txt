n, k = map(int, input().split())
a = list(map(int, input().split()))
s = set()
c = []
for x in a:
    if x in s:
        s.remove(x)
        c.remove(x)
    else:
        c.append(x)
        if len(c) > k: c.pop(0)
    s.add(x)
print(len(c))
print(*c)
