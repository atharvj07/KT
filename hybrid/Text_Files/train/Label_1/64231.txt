n, m = map(int, input().split())
x = list(map(int, input().split()))
pos = {val: idx for idx, val in enumerate(x)}
ans = []
for i in range(1, n+1):
    ans.append(sum(abs(pos[x[j]] - pos[x[j+1]]) for j in range(m-1)))
print(*ans)
