from sys import stdin

n = int(stdin.readline())
tasks = list(map(int, stdin.readline().split()))

mn = 1000000000
mx = 0
for task in tasks:
  mx = max(task, mx)
  mn = min(task, mn)

ans = mx - mn
if ans % 2 == 1:
  ans += 1
ans //= 2
print(ans)
