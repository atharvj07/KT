#include <iostream>
#include <string>
using namespace std;
int main() {
  string s;
  cin >> s;
  if (s.back() % 2 == 0 || s.size() == 1) {
    cout << s;
    return 0;
  }
  cout << s.substr(0, s.size() - 1);
}
