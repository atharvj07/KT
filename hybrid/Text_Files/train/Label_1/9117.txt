#include <iostream>

int main() {
    int t, x, y, a, b;
    std::cin >> t;
    while (t--) {
        std::cin >> x >> y >> a >> b;
        std::cout << std::abs(x-y) * ((a<b)?a:b) << '\n';
    }
    return 0;
}
