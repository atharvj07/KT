#include <iostream>
using namespace std;

int main() {
  int n, m, k;
  cin >> n >> m >> k;
  k--;  // 0-indexing
  int lane = k / m + 1;
  int desk = k % m + 1;
  char side = k % 2 == 0 ? 'L' : 'R';
  cout << lane << ' ' << desk << ' ' << side << endl;
  return 0;
}
