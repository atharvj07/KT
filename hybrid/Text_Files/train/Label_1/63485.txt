n = int(input())
a = input()
b = list(map(int, input().split()))
now = 0
ans = "FINITE"
for i in range(n):
    if a[i] == '>':
        now += b[i]
    else:
        now -= b[i]
    if now > n or now < 1:
        ans = "INFINITE"
        break
print(ans)
