#include<bits/stdc++.h>
using namespace std;

long long a[100];

int main()
{
    int n; cin>>n;
    for(int i=0; i<n; i++) cin>>a[i];
    sort(a, a+n);
    long long b=a[n/2];
    long long ans=0;
    for(int i=0; i<n; i++) ans+=abs(b-a[i])%2;
    cout<<ans<<endl;
    return 0;
}
