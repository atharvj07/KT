#include<bits/stdc++.h>
using namespace std;
int main() {
  int n, m, x;
  cin >> n >> m;
  while (n--) { for (int i = 0; i < m; ++i) { cin >> x; if (x) cout << 1 << ' '; else cout << 0 << ' '; } }
  return 0;
}
