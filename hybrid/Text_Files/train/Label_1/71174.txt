q,m=map(int,input().split());pairs=[]
for i in range(q):
    k,a,b=map(int,input().split())
    if k==1:pairs.append([a,b])
    else:
        l,h=a+pairs[-1][0],b+pairs[-1][1]
        print("YES"if l*h<=m else "NO")
