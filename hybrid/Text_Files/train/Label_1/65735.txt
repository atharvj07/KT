import math

R, x1, y1, x2, y2 = map(int, input().split())

d = math.sqrt((x1 - x2)**2 + (y1 - y2)**2)
x, y = (x1 + x2) / 2, (y1 + y2) / 2
r = math.sqrt((max(R, d / 2) + min(R, d / 2))**2 - ((x1 - x)**2 + (y1 - y)**2))

print(x, y, r)
