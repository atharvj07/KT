#include <iostream>
#include <iomanip>

int main() {
    int n;
    std::cin >> n;
    double ans = 0;
    for (int rem = n; rem > 0; rem--)
        ans += 1.0 / rem;
    std::cout << std::fixed << std::setprecision(12) << ans << '\n';
}
