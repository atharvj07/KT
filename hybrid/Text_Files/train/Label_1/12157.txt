#include <bits/stdc++.h>
using namespace std;
int main() {
    long long a, h, w;
    cin >> a >> h >> w;
    a *= a;
    int ans = (a + w - 1) / w + (a + h - 1) / h - 1;
    if (a * ans >= h * w)
        cout << fixed << setprecision(6) << (double)ans / 2;
    else
        cout << -1;
}
