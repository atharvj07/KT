#include<bits/stdc++.h>
using namespace std;
int gcd(int a,int b){
    if(a==0)return b;
    return gcd(b%a,a);
}
int main(){
    int n,a,b;
    cin>>n;
    int ans=(n-1)/2;
    if(ans&1)a=ans-1,b=n-a;
    else a=ans,b=n-ans;
    if(gcd(a,b)==1)cout<<a<<" "<<b<<endl;
    else cout<<"-1"<<endl;
}
