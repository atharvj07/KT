n = int(input())
last_x = -1
last_k = -1
for i in range(n):
    x, k = map(int, input().split())
    if x < last_x or (x == last_x and k <= last_k):
        print('NO')
        exit(0)
    last_x = x
    last_k = k
print('YES')
