#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, a;
  cin >> n;
  unordered_map<int, int> m;
  while (n--) {
    cin >> a;
    if (a == 1)
      continue;
    if (m[a] == 0)
      cout << 2 << endl;
    else
      cout << 1 << endl;
    m[a]++;
  }
  return 0;
}
