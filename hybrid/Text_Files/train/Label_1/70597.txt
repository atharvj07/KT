import sys
input = sys.stdin.readline

n = int(input())
ans = 0
L = []
for _ in range(n):
    x, y = map(int, input().split())
    L.append((x, y))
L.sort(key=lambda x: x[1])
R = -1e9
for x, y in L:
    if x >= R:
        ans += 1
        R = y
print(ans)
