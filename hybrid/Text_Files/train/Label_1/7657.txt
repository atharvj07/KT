#include <iostream>
#include <iomanip>
using namespace std;

int main() {
  long long int l, p, q;
  cin >> l >> p >> q;
  cout << fixed << setprecision(4) << ((double)l * p) / (p + q) << endl;
  return 0;
}
