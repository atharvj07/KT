#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int d;
        cin >> d;
        int a = (d + 4) / 2;
        if (a % 2 == 1) a++;
        cout << a * a - (d - 1) << endl;
    }
    return 0;
}
