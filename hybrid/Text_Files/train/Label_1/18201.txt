#include <bits/stdc++.h>

using namespace std;

using ll = long long;

const int MOD = 1e9 + 7;

vector<ll> dp(61);

int main() {
  int k;
  cin >> k;
  dp[1] = 6;
  for (int i = 2; i <= k; ++i) {
    dp[i] = (dp[i - 1] * dp[i - 1] * 4) % MOD;
  }
  cout << dp[k] << endl;
  return 0;
}
