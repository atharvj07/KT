def find_divisible_number():
    n = int(input().strip())
    a = list(map(int, input().strip().split()))
    min_val = min(a)
    for val in a:
        if val % min_val != 0:
            return -1
    return min_val

print(find_divisible_number())
