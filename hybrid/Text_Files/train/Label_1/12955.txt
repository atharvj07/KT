#include<bits/stdc++.h>
using namespace std;
int main(){
    int a,b;
    cin>>a>>b;
    set<int> s;
    for(int i=0,c;i<a;++i){
        cin>>c;
        s.insert(c);
    }
    int ans=s.size();
    while((a+b)>ans){
        s.insert(ans+1);
        ans++;
    }
    cout<<ans<<endl;
}
