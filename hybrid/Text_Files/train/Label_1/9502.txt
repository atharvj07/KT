#include <iostream>
using namespace std;
int main() {
    int n, t, ans = 0;
    cin >> n >> t;
    while (t > 0 && n > 0) {
        int tmp;
        cin >> tmp;
        t -= (86400 - tmp);
        n--;
        ans++;
    }
    cout << ans << endl;
    return 0;
}
