#include <iostream>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;

    for (int i = 0; i < n - k + 1; i++)
        cout << (i % 2);

    for (int i = n - k + 1; i < n; i++)
        cout << ((k - 1 - (i - n + k)) % 2);

    cout << "\n";

    return 0;
}
