#include <iostream>

using namespace std;

int main() {
  int score = 0;
  char ch;
  while (cin >> ch) {
    if (ch == '+') score++;
    else score--;
    if (score < 0) {
      cout << "No" << endl;
      return 0;
    }
  }
  cout << "Yes" << endl;
  return 0;
}
