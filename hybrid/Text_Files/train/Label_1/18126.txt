#include<bits/stdc++.h>
const int mod=1e9+7;
using namespace std;
int main() {
  long long n, k;
  cin >> n >> k;
  long long ans = pow(2, n * k % (mod - 1)) - 1;
  ans = (ans + mod) % mod;
  cout << ans << endl;
  return 0;
}
