import sys
for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    a.sort(reverse=True)
    for gcd in a:
        if all(gcd % i == 0 for i in a):
            a.remove(gcd)
            break
    print(*a)
