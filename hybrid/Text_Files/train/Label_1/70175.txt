import sys

T = int(input())
for i in range(T):
    n = int(input())
    s = input()
    
    can = True
    for j in range(n//2):
        if abs(ord(s[j]) - ord(s[n-1-j])) > 1:
            can = False
            break
    
    if can:
        print("YES")
    else:
        print("NO")
