#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    ll n, k, d1, d2;
    cin >> n >> k >> d1 >> d2;
    string s = "no";
    if (d1 + d2 <= k && k % 2 == 0 && (n - k) % 3 == 0) s = "yes";
    cout << s << '\n';
  }
  return 0;
}
