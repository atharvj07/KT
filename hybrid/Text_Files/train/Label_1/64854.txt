import sys

n = int(input())
a = [int(x) for x in input().split()]
b = [int(x) for x in input().split()]
if sum(a) != sum(b):
    print(-1)
    sys.exit()
a.sort()
b.sort()
ans = 0
for i in range(n):
    ans += abs(a[i] - b[i]) // 2
print(ans)
