#include <iostream>
#include <string>

using namespace std;

int main() {
  string s;
  cin >> s;
  int ans = 0;
  int cnt = 0;
  for (char c : s) {
    cnt += (c == '+' ? 1 : -1);
    ans = max(ans, cnt);
  }
  cout << ans + 1 << endl;
  return 0;
}
