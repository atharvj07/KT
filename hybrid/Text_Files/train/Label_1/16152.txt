#include <iostream>

int main() {
    long long int a, b;
    std::cin >> a >> b;
    if (a % b == 0) { std::cout << "infinity" << std::endl; }
    else { std::cout << b << std::endl; }
    return 0;
}
