n, l, v1, v2, k = map(int, input().split())
ans = l / v2
if k == 1:
    ans += l/v1
else:
    time = l / (v2 - v1) * (k - 1)
    rem = n % k
    if rem > 0:
        time += l / v2
    ans += time
print(ans)
