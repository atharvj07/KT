s = input()
ops, ans = 0, 0
stack = []
for c in s:
    if c=='[': ops+=1; stack.append(ops)
    elif c=='|': ans+=1
    elif c==']':
        if ops==0 or ops>stack[-1]: break
        ans+=2 if ops==stack[-1] else 3
        stack.pop()
        ops-=1
print(-1 if ops else ans)
