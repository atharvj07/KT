#include <cmath>
#include <iostream>
using namespace std;
int main() {
  long long n, r;
  cin >> n >> r;
  double angle = 2 * acos(-1) / n;
  cout << (r * sin(angle / 2)) / (1 - sin(angle / 2)) << endl;
  return 0;
}
