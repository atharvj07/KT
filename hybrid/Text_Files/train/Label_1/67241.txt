import sys
n, k, x = map(int, input().split())
a = list(map(int, input().split()))
a.sort(reverse=True)
if x*k > n:
    print(-1)
    sys.exit(0)
tot = sum(a[:x*k]); ans = 0
for i in range(x*k, n):
    ans += a[i] * (i//k - (i-x)//k)
    tot -= a[i-x*k] * (i//k - (i-x)//k)
print(tot + ans)
