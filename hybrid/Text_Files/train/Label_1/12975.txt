#include <iostream>
#include <string>
using namespace std;
int main() {
    string s;
    cin >> s;
    int sum1=0, sum2=0;
    for(int i=0;i<3;i++) {
        sum1+=s[i]-'0';
        sum2+=s[i+3]-'0';
    }
    cout << abs(sum1-sum2) << endl;
}
