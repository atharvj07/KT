n,r=map(int,input().split())
pro=[list(map(int,input().split()))for i in range(n)]
pro.sort(key=lambda x:x[1],reverse=1)
for a,b in pro:
    if r<a:
        print("NO");exit()
    r=r-a+b
print("YES")
