n, s = map(int, input().split())
t = []
for _ in range(n):
    f, ti = map(int, input().split())
    t.append((ti, f))
t.sort()
ans = 0
for ti, f in t:
    if ans < ti:
        ans = ti
    ans += f
print(ans)
