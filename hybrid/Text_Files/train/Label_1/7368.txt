#include <cstdio>
int main() {
  long long n, a, b, x, y, t;
  scanf("%lld %lld %lld", &n, &a, &b);
  for (x = 0; a * x <= n; x++) {
    t = n - a * x;
    if (t % b == 0) {
      y = t / b;
      printf("YES\n%lld %lld\n", x, y);
      return 0;
    }
  }
  printf("NO\n");
  return 0;
}
