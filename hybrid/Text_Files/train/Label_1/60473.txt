for _ in range(int(input())):
  n, k = map(int, input().split())
  f = [d for d in str(n)]
  c = n
  while sum(int(d) for d in f) + sum(int(d) for d in str(c)) <= k:
    c += 1
    f.extend(str(c))
  x = c if c - 1 >= n else -1
  print(x)
