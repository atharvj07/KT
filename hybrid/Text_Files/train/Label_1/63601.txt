n, x, k = map(int, input().split())
a = sorted(list(map(int, input().split())))
ans = 0
for i in range(n):
    for j in range(i, n):
        if (a[j] - a[i]) % x == 0 and (j - i - 1) >= k:
            ans += 1
print(ans)
