#include <iostream>
using namespace std;
int main() {
  string a;
  getline(cin, a);
  a += '+';
  for (char b : a)
    if (b != 'H' and b != 'Q' and b != '9') {
      cout << "No";
      return 0;
    }
  cout << "Yes";
}
