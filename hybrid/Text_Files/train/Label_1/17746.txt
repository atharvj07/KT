#include <cstdio>

long long a, b, x;
int n;

int main() {
    scanf("%I64d%I64d", &a, &b);
    scanf("%d", &n);
    for (int i = n; i >= 1; i--) scanf("%I64d", &x), b = (x * b + a), a = b - (x * a);
    puts((a == b) ? "YES" : "NO");
    return 0;
}
