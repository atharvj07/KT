#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m, z, x, y;
    cin >> n >> m >> z;
    x = z / n;
    y = z / m;
    x *= n;
    y *= m;
    if (x == y)
    {
        cout << 0 << endl;
    }
    else
    {
        cout << min(x, y) - 1 << endl;
    }
    return 0;
}
