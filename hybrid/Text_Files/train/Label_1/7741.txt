#include <iostream>
#include <string>
using namespace std;
int main() {
  int n;
  string s;
  cin >> n >> s;
  int cnt = 0;
  for (int i = 1; i < n; i += 2) { cnt += (s[i] == s[i - 1]); }
  cout << cnt << endl;
  return 0;
}
