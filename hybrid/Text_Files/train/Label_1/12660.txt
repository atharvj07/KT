#include <iostream>
int main() {
    int t;
    long long n, d;
    std::cin >> t;
    while (t--) {
        std::cin >> n >> d;
        std::cout << (n - d >= 0 ? "YES\n" : "NO\n");
    }
    return 0;
}
