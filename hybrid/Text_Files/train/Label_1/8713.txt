#include <iostream>
using namespace std;
int main() {
  int n, x;
  cin >> n >> x;
  while (n--) {
    char c;
    int d;
    cin >> c >> d;
    if (c == '+')
      x += d;
    else
      x = max(0, x - d);
  }
  int ans1 = x, ans2 = 0;
  cout << ans1 << ' ' << ans2 << endl;
}
