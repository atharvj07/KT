from math import gcd

for _ in range(int(input())):
    s = input()
    t = input()
    g = gcd(len(s), len(t))
    if s[:g] == t[:g]:
        print(s[:g] * (len(s) // g + len(t) // g))
    else:
        print(-1)
