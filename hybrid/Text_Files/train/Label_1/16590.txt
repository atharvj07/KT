#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int y = 0;
  int cost = 0;
  for (char c : s) {
    if (c == 'U') y++;
    if (c == 'R' && y > 0) cost++, y--;
  }
  cout << cost << "\n";
  return 0;
}
