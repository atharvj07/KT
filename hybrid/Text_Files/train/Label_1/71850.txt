n, k = map(int, input().split())
s = input()
colors = {}

for color in s:
    if color in colors:
        colors[color] += 1
    else:
        colors[color] = 1

can_distribute = all(value <= k for value in colors.values())

print("YES" if can_distribute else "NO")
