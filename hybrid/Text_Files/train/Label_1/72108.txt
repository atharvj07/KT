import math

n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()

ans = 0
while len(set(a)) > 1:
    b = []
    for x in a:
        if x % 2 == 1:
            b.append(x - 1)
        else:
            b.append(x // 2)
    ans += 1
    a = b

print(ans)
