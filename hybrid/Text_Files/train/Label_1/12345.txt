#include <iostream>
#include <string>

using namespace std;

int main() {
  int n;
  cin >> n;
  string s = "I";
  for (int i = 1; i <= n; i++) {
    if (i % 2 == 1) s += " hate";
    else s += " love";
    if (i < n) s += " that";
  }
  s += " it";
  cout << s << endl;
  return 0;
}
