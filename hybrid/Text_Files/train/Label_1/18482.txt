#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  cout << min(n, m) << endl;
  for (int i = 1; i <= min(n, m); i++) { cout << i << " " << i << endl; }
  return 0;
}
