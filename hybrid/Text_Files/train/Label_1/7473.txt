#include <iostream>
using namespace std;

int main() {
  int a, b, ans = 0;
  cin >> a >> b;
  while (a >= b) {
    ans += a / b;
    a = (a / b) + a % b;
  }
  ans += a;
  cout << ans << endl;
  return 0;
}
