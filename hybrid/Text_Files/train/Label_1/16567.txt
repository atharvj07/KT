#include <bits/stdc++.h>
using namespace std;

int main() {
  long long n, p;
  cin >> n >> p;
  if (p && !(n & 1)) {
    cout << "-1" << endl;
    return 0;
  }
  int ans = 0;
  while (n) {
    ans++;
    n = (n + p) >> 1;
  }
  cout << ans << endl;
}
