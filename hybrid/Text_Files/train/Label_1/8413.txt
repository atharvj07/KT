#include <iostream>
#include <cmath>

int main() {
  unsigned long long n;
  std::cin >> n;
  unsigned long long a = ceil(sqrt(n));
  unsigned long long b = ceil(n / a);
  std::cout << a << ' ' << b << '\n';
}
