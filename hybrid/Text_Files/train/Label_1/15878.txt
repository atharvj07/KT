#include <iostream>
using namespace std;

int main() {
  int n, pos, l, r, a = 1, b = n;
  cin >> n >> pos >> l >> r;
  if (r < pos) {
    swap(l, r);
    swap(a, b);
  }
  cout << min(abs(l - pos), abs(pos - r)) + ((pos > l) + (pos < r)) << endl;
}
