#include<bits/stdc++.h>
using namespace std ;
int main ()
{
    int t ;
    long long ang ;
    cin >> t ;
    while (t--)
    {
        cin >> ang ;
        if (ang % 3 > 0) ang = 180 ;
        else ang = 360 / (180 / ang) ;
        cout << ang << "\n" ;
    }
    return 0 ;
}
