#include <iostream>
int main() {
  int t, x, k, p = -1;
  std::cin >> t;
  while (t--) {
    std::cin >> x >> k;
    if (x < p) {
      std::cout << "NO";
      return 0;
    }
    p = x;
  }
  std::cout << "YES";
  return 0;
}
