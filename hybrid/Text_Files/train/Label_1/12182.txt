#include <iostream>

using namespace std;

int main() {
  int x1, y1, x2, y2, x, y;
  cin >> x1 >> y1 >> x2 >> y2 >> x >> y;
  bool ans = ((x2 - x1) % x == 0) && ((y2 - y1) % y == 0);
  cout << (ans ? "YES" : "NO") << endl;
  return 0;
}
