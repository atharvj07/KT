import sys
n,x=map(int,input().split())
L=[]
for _ in range(n):
    a,b,c=map(int,input().split())
    if b-a+1==x: L+=[(a,b,c)]
if len(L)<2: print(-1)
else:
    L.sort(key=lambda i: i[2])
    if L[0][1]<L[1][0]: print(L[0][2]+L[1][2])
    else: print(-1)
