#include<bits/stdc++.h>
using namespace std;
int main() {
    string s;
    cin >> s;
    for (int i = 0; i < (int)s.size(); i++) {
        cout << (s[i] == *(min_element(s.begin() + i, s.end())) ? "Mike" : "Ann") << "\n";
    }
    return 0;
}
