#include <bits/stdc++.h>

using namespace std;

int a[100001];
int main()
{
    int n, i, ans = 0;
    cin >> n;
    for (i = 0; i < n; i++)
    {
        cin >> a[i];
        if (a[i] > 0) ans++;
    }
    if (ans == 1)
        cout << -1 << endl;
    else
        cout << n - 1 << endl;
}
