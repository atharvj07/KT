import math

def classy(n: int) -> bool:
  cnt = 0
  for d in str(n):
    if d != '0': cnt += 1
  return cnt <= 3

for _ in range(int(input())):
  l, r = map(int, input().split())
  ans = 0
  for a in range(l, r + 1):
    if classy(a):
      ans += 1
  print(ans)
