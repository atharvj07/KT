import sys
input = sys.stdin.readline

n = int(input())
h = list(map(int,input().split()))
inc = [1] * n
for i in range(1, n):
    if h[i] >= h[i - 1]:
        inc[i] = inc[i - 1] + 1
ans = max(inc)
print(ans)
