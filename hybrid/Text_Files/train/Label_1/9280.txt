#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &ai : a) cin >> ai;
  sort(a.begin(), a.end());
  if (a[0] != 1 || a[n - 1] != n) {
    cout << "NO" << endl;
    return 0;
  }
  cout << "YES" << endl;
  return 0;
}
