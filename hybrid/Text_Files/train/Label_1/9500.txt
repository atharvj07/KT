#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, t, ans = 1;
  cin >> n >> t;
  vector<long long> v(n);
  for (int i = 0; i < n; i++) cin >> v[i];
  for (int i = 0, time = 0; i < n; i++) {
    time += v[i];
    if (t <= time) ans = i + 1;
  }
  cout << ans << endl;
}
