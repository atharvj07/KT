import sys

a, b, c, d = map(int, sys.stdin.readline().split())
if a * d == b * c:
    print("0/1")
else:
    p = abs(a * d - b * c)
    q = a * d * b * c
    g = p
    while q > 0:
        p, q = q, p % q
        g = p
    print("{}/{}".format(p // g, q // g))
