#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ll n, x, ans = -1e12 - 1;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> x;
        ll sq = sqrt(x);
        if (sq * sq != x) ans = max(ans, x);
    }
    cout << ans << endl;
    return 0;
}
