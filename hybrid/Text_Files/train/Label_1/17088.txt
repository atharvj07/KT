#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int ans = 0;
  for (int i = n - 1; i >= 0; i--) {
    if (s[i] == '0') {
      ans++;
      break;
    }
  }
  cout << ans << endl;
  return 0;
}
