import sys

input = sys.stdin.readline

n, l, r, x = map(int, input().split())
c = list(map(int, input().split()))
c.sort()

ans = 0
for i in range(n):
    for j in range(i + 1, n):
        if l <= c[j] - c[i] <= r and c[j] - c[i] >= x:
            ans += 1

print(ans)
