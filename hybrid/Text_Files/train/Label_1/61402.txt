['import sys\n\nn, m = map(int, sys.stdin.readline().split())\na = list(map(int, sys.stdin.readline().split()))\nedges = [list(map(int, sys.stdin.readline().split())) for _ in range(m)]\n\n# Sort edges by weight in descending order\nedges.sort(key=lambda x: x[2], reverse=True)\n\n# Initialize the parent array\nparent = [i for i in range(n)]\n\n# Function to find the root of a node\ndef find(x):\n    if parent[x] != x:\n        parent[x] = find(parent[x])\n    return parent[x]\n\n# Function to union two nodes\ndef union(x, y):\n    x_root = find(x)\n    y_root = find(y)\n    if x_root != y_root:\n        parent[x_root] = y_root\n\n# Initialize the maximum weight\nmax_weight = 0\n\n# Iterate over the edges\nfor v1, v2, weight in edges:\n    # Check if the nodes are in different components\n    if find(v1 - 1) != find(v2 - 1):\n        # Union the nodes\n        union(v1 - 1, v2 - 1)\n        # Add the weight to the maximum weight\n        max_weight += weight\n        # Subtract the weights of the vertices\n        max_weight -= a[v1 - 1]\n        max_weight -= a[v2 - 1]\n\n# Print the maximum weight\nprint(max_weight)\n']