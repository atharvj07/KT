#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
    ll n, b;
    cin >> n >> b;
    int ans = 0;
    for (ll i = b; i <= n; i *= b) {
        ans += n / i;
    }
    cout << ans << endl;
}
