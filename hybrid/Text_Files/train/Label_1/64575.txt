import sys
input=sys.stdin.readline
a=[0]
for x in map(int,input().split()):
    xor=0
    for i in range(len(a)):
        xor^=a[i]
        if xor==x:
            print(i+1,*[i+1 for i,e in enumerate(a[1:])if e^xor==x])
            break
    else:
        print(0)
    a.append(x)
