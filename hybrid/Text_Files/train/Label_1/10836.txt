#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    string s;
    cin >> s;
    int n = s.size(), k = 0;
    map<char, int> mp;
    for (char c : s) mp[c]++;
    for (auto e : mp) k += min(1, e.second / 2);
    cout << k << '\n';
  }
  return 0;
}
