#include <iostream>
using namespace std;
long long n, a, b, c;
int main() {
    cin >> n >> a >> b >> c;
    long long ans = n / a;
    n -= (ans * a);
    while (n >= c) {
        n -= c;
        ans++;
        n += b - c;
    }
    cout << ans;
    return 0;
}
