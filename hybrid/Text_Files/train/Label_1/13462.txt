#include <iostream>
#include <cstdio>
#include <vector>
#include <set>
#include <map>
#include <queue>
#include <algorithm>
using namespace std;

const int maxn = 1e6 + 5;

int main() {
  long long n, t;
  cin >> t;
  while (t--) {
    cin >> n;
    cout << ((n + 1) * n) / 2 << "\n";
  }
  return 0;
}
