#include <iostream>
using namespace std;
typedef long long ll;
int main() {
    int t;
    cin >> t;
    while (t--) {
        ll num;
        cin >> num;
        num = num & (num + 1);
        num = num & (num + 1);
        while (num > 0) {
            num >>= 1;
            cout << num << " ";
        }
        cout << "0" << endl;
    }
}
