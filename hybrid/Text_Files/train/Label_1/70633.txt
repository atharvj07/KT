import array
n,m=map(int,input().split())
a=[*map(int,input().split())]
b=[*map(int,input().split())]
_a=sorted(enumerate(a),key=lambda x:x[1],reverse=True)
_b=sorted(enumerate(b),key=lambda x:x[1],reverse=True)
print(*[d if b else -1 for a,d in _a for x,_ in _b if x==a])
