#include<bits/stdc++.h>
using namespace std;
const int maxn=1e5+5;
int main() {
    int n;
    cin>>n;
    int a[maxn];
    set<int> b;
    for (int i=1; i<=n; i++) cin>>a[i], b.insert(i);
    for (int i=1; i<=n; i++) if (a[i]!=i) b.erase(a[i]);
    cout<<b.size()<<endl;
}
