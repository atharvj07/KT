#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int ans = min(n, m);
    cout << ans << endl;
    for (int i = 1; i <= ans; i++) {
        cout << i << " " << i << endl;
    }
    return 0;
}
