import sys

n = int(input())
a = list(map(int, input().split()))
mean = sum(a) / n
ans = -1
for i in a:
    if abs(i - mean) % 2 != 0:
        ans = -1
        break
    ans = max(ans, abs(i - mean) // 2)
print(ans)
