#include <iostream>
#include <string>
using namespace std;

int main() {
  string s;
  int a, b;
  cin >> s >> a >> b;
  int n = s.size();
  for (int i = 1; i <= n / 2; i++) {
    if ((stoll(s.substr(0, i)) % a == 0) && (stoll(s.substr(i)) % b == 0)) {
      cout << "YES" << endl << s.substr(0, i) << endl << s.substr(i) << endl;
      return 0;
    }
  }
  cout << "NO" << endl;
  return 0;
}
