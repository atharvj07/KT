#include <bits/stdc++.h>

const int MOD = 1e9 + 9;

int main() {
    int n, m;
    std::cin >> n >> m;
    int ans = (1 << m) - 1;
    if (n > 1) ans = 1LL * ans * ans % MOD;
    std::cout << ans << "\n";
    return 0;
}
