import math
for _ in range(int(input())):
    n, w = map(int, input().split())
    arr = list(map(int, input().split()))
    arr.sort(reverse=True)
    cur, ans = 0, 0
    for el in arr:
        cur += el
        ans += 1
        if cur >= w:
            break
    print(ans)
