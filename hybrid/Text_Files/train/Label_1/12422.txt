#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int n, m;
    std::cin >> n >> m;
    if (n % 2 == m % 2)
      std::cout << "YES" << '\n';
    else
      std::cout << "NO" << '\n';
  }
  return 0;
}
