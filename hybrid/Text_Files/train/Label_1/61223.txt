import sys

n, T = map(int, sys.stdin.readline().strip().split())
tasks = [(t, q) for t, q in (map(int, x.strip().split()) for x in sys.stdin)]

tasks.sort()
ans = 0
t = 0

for x, y in tasks:
    if t + x <= T:
        t += x
        ans += y
    else:
        break

print(ans)
