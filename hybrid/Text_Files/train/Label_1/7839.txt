#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<char> ops(n);
  vector<int> xs(n);
  for (int i = 0; i < n; ++i) {
    cin >> ops[i] >> xs[i];
  }
  cout << "1\n" << ops[0] << ' ' << xs[0] << endl;
  return 0;
}
