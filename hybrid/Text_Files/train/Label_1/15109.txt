#include <iostream>
#include <algorithm>
using namespace std;
int main() {
	int n;
	cin >> n;
	int x1, y1, x2, y2;
	cin >> x1 >> y1;
	for (int i = 1; i < n; i++) {
		cin >> x2 >> y2;
		if (x1 * (y2 - y1) != x2 * (y1 - y2)) {
			cout << "Yes";
			return 0;
		}
	}
	cout << "No";
}
