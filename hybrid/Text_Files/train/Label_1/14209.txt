#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, ans = 0;
    cin >> n;
    while (n--) {
        long long x;
        cin >> x;
        ans += x;
        if (x == 1)
            ans++;
    }
    cout << ans << '\n';
    return 0;
}
