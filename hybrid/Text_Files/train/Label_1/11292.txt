#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,d,l=0,r=0;
    cin>>n;
    string s;
    cin>>s;
    for(int i=0;i<n;++i)
    {
        cin>>d;
        if(s[i]=='>')r+=d;
        else l+=d;
    }
    if(l==0 || r==0)cout<<"INFINITE";
    else cout<<"FINITE";
    return 0;
}
