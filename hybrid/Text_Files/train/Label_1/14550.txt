#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  long long p = 4;
  while (t--) {
    long long x;
    cin >> x;
    x = x + 1;
    while ((x % 3 == 0) || (x % 10 == 3)) { x++; }
    cout << x << endl;
  }
  return 0;
}
