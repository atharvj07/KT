import sys

H, n = map(int, input().split(" "))
d = list(map(int, input().split()))
now = H
for i, di in enumerate(d, start=1):
    now += di
    if now <= 0:
        print(i)
        sys.exit(0)
print(-1)
