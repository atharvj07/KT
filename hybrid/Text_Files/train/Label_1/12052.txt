#include<bits/stdc++.h>
using namespace std;
int main() {
  vector<int> v;
  int n;
  cin >> n;
  for (int i = 1; i <= n * n; i++)
    v.push_back(i);
  for (int i = 1, j = n * n; j > n * n / 2; i++, j--)
    cout << v[i - 1] << " " << v[j - 1] << "\n";
  return 0;
}
