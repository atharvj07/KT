import sys

n = int(input())
a = list(map(int, input().split()))
last = a[0]
ans = 0
for i in range(1, n):
    if a[i] < last:
        ans = -1
        break
    else:
        ans += (a[i] ^ last > 0)
        last = a[i] ^ last
print(ans)
