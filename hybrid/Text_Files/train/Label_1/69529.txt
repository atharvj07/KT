import sys
import math

def L(x, p):
    y = x + 1
    while math.gcd(p, y) != 1:
        y += 1
    return y

input = sys.stdin.readlines()
t = int(input[0])
for i in range(1, t + 1):
    x, p, k = map(int, input[i].split())
    print(L(x, p) + k - 1)
