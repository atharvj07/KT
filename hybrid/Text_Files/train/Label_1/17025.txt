#include <iostream>

using namespace std;

int main() {
  int n;
  cin >> n;
  for (int i = 1; i <= 1000; i++) {
    if ((n * i + 1) % 2 == 0 || (n * i + 1) % 3 == 0) {
      cout << i << endl;
      break;
    }
  }
  return 0;
}
