import numpy as np

n, m = map(int, input().split())

grid = np.array([list(input()) for _ in range(n)])
x, y = np.where(grid == 'X')
if x[0] == x[-1] or y[0] == y[-1]:
    print('NO')
else:
    print('YES')
