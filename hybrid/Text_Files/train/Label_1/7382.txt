#include <iostream>
#include <vector>
using namespace std;
int main()
{
    int n, k, sum = 0;
    cin >> n >> k;
    vector<int> a(n + 1);
    for (int i = 0; i <= n; i++)
    {
        cin >> a[i];
        sum += a[i];
    }
    cout << 1LL * (k - abs(sum % k)) / 2;
}
