import numpy as np

n = int(input())
max_distrib = np.array(list(map(float, input().split())))
min_distrib = np.array(list(map(float, input().split())))
s = np.sqrt(max_distrib * min_distrib)
s /= s.sum()
print(*s)
print(*np.flip(s))
