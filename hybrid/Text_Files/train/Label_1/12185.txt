#include <bits/stdc++.h>

using namespace std;

int main() {
  int x1, y1, x2, y2, x, y;
  cin >> x1 >> y1 >> x2 >> y2 >> x >> y;
  cout << ((abs(x2 - x1) % x == 0 && abs(y2 - y1) % y == 0) ? "YES" : "NO") << endl;
  return 0;
}
