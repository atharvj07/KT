def calculate_dangerous_triples(n, dislikes, salary_revisions):
    def count_triples():
        result = 0
        for i in range(1, n + 1):
            higher = len([1 for x in dislikes[i] if salaries[x] > salaries[i]])
            result += higher * (len(dislikes[i]) - higher)
        return result
    
    salaries = [i for i in range(n + 1)]
    dislikes = {i: [] for i in range(1, n + 1)}
    
    for dislike in dislikes_input:
        dislikes[dislike[0]].append(dislike[1])
        dislikes[dislike[1]].append(dislike[0])

    results = [count_triples()]
    
    for i, revision in enumerate(salary_revisions, 1):
        salaries[revision] = n + i
        results.append(count_triples())
    
    return results

# Reading input
n, m = map(int, input().split())
dislikes_input = [list(map(int, input().split())) for _ in range(m)]
q = int(input())
salary_revisions = [int(input()) for _ in range(q)]

# Processing and outputting results
results = calculate_dangerous_triples(n, dislikes_input, salary_revisions)
for result in results:
    print(result)
