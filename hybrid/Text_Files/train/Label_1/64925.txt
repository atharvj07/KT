import sys
input = sys.stdin.readline

n,m=map(int,input().split())
skill=[[] for i in range(m+1)]
for i in range(n):
    x,y=map(int,input().split())
    skill[x].append(y)
ans=0
for i in skill[1:]:
    i.sort()
    t=len(i)-len(i)//2
    ans+=sum(i[t:])
print(ans if ans>0 else 0)
