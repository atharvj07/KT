#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main(){
    ll n,x,sum=0; cin>>n;
    set<ll>s;
    for(int i=0;i<n;i++){
        cin>>x;
        s.insert(x);
        sum+=x;
    }
    sum-=*s.rbegin();
    cout<<sum<<'\n';
}
