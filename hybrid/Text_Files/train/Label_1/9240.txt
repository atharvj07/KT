#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long r, b, d;
    cin >> r >> b >> d;
    if (abs(r - b) <= d)
      cout << "YES\n";
    else
      cout << "NO\n";
  }
  return 0;
}
