#include <iostream>
using namespace std;

int main() {
    long long n, m, q;
    cin >> n >> m >> q;
    while (q--) {
        int sx, sy, ex, ey;
        cin >> sx >> sy >> ex >> ey;
        if (sx == ex)
            cout << "YES\n";
        else
            cout << "NO\n";
    }
}
