#include <iostream>
using namespace std;
int main() {
  int h1, m1, h2, m2;
  scanf("%d:%d", &h1, &m1);
  scanf("%d:%d", &h2, &m2);
  int c = (h2 * 60 + m2 - h1 * 60 - m1) / 2;
  printf("%02d:%02d", h1 + c / 60, m1 + c % 60);
  return 0;
}
