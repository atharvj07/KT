#include <iostream>
#include <string>
using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  string s(n, '0');
  for (int i = 0; i < min(k, n - k); ++i) s[i] = '1';
  cout << s << endl;
  return 0;
}
