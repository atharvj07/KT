import sys

n = int(input())
a = list(map(int, input().split()))
ans = 0
cur = a[0]
cnt = 1

for x in a[1:]:
    if x == cur:
        cnt += 1
    else:
        ans += cnt // 2
        cur = x
        cnt = 1

ans += cnt // 2

print(ans)
