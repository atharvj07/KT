#include <bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  int a[26] = {0};
  int odd = 0;
  for (char c : s) a[c - 'a']++;
  for (int i = 0; i < 26; i++) odd += a[i] % 2;
  if (odd <= 1)
    cout << "First" << endl;
  else
    cout << "Second" << endl;
}
