x, k = map(int, input().split())
used = set()
for i in range(k):
    type, num = input().split()
    num = int(num)
    if type == '1':
        used.add(num)
        used.add(num-1)
    else:
        used.add(num)
print(max(0, x-k-len(used)), max(0, x-k-1-len(used)))
