n, T = map(int, input().split())
tasks = [tuple(map(int, input().split())) for _ in range(n)]
tasks.sort(reverse=True, key=lambda x: x[1])

dp = [0] * (T + 1)
for time, interest in tasks:
    for t in range(T, time - 1, -1):
        dp[t] = max(dp[t], dp[t-time] + interest)

print(max(dp))
