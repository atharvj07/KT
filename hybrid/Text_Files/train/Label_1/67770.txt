import sys

for _ in range(int(input())):
    n = int(input())
    p = [int(x) for x in input().split()]
    s = list(input())
    q = sorted(range(1, n + 1), key=lambda x: (s[x - 1], -p[x - 1]))
    print(*q)
