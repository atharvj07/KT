#include <iostream>
using namespace std;

int main() {
  long long n, m, k, ans = 0;
  cin >> n >> m >> k;
  if (n >= 2) ans = 2 * ((m + (n - 1)) / n);
  else ans = m;
  cout << ans << '\n';
  return 0;
}
