#include <iostream>
#include <vector>
const int MOD = 1000000007;
using namespace std;

int main() {
    long long n, k;
    cin >> n >> k;
    long long ans = 0, tmp = 1;
    for (int i = 1; i <= k; i++) {
        tmp = (tmp * n) % MOD;
        ans += tmp;
    }
    cout << ans;
    return 0;
}
