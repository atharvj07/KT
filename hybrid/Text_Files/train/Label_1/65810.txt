def mod_pow(x, p, m):
    if p == 0:
        return 1
    if p == 1:
        return x % m
    val = mod_pow(x, int(p / 2), m)
    val = (val * val) % m
    if p % 2 != 0:
        val = (val * x) % m
    return val % m

n, k = map(int, input().split())
print(mod_pow(n, k, 998244353))
