n = int(input())
c = list(map(int, input().split()))
s, ans = [0] * n, 0
for i in range(n):
	s[i] = s[i - 1] + (c[i] & 1) if i > 0 else c[i] & 1
	if c[i] > 1:
		ans = ans + s[i - 1]
	else:
		ans = ans + s[i]
print(ans)
