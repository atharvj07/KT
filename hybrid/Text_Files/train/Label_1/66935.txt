from sys import stdin

def solve():
    n = int(stdin.readline())
    p = list(map(int, stdin.readline().split()))
    ans = 0
    for i in range(1, n):
        if p[i] < p[i - 1]:
            ans += 1
    return ans

print(solve())
