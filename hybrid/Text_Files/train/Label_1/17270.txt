#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, s;
  cin >> n >> s;
  vector<int> a(n);
  for (int &ai : a) cin >> ai;
  int ans = 0;
  for (int i = 0; i < n; i++) {
    if (i == s - 1) continue;
    if (a[i] < a[a[i] - 1]) ans++;
  }
  cout << ans << endl;
  return 0;
}
