#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, x, s = 0;
    cin >> n;
    for(int i = 0; i < n; ++i){
        cin >> x;
        s += x;
    }
    int mx = 0;
    for(int i = 0; i < n; ++i){
        cin >> x;
        mx = max(mx, x);
    }
    cout << s - mx + 1 << '\n';
}
