def can_sort_ravioli(n, arr):
    for i in range(1, n):
        if abs(arr[i] - arr[i-1]) >= 2:
            return "NO"
    return "YES"

n = int(input().strip())
arr = list(map(int, input().strip().split()))
print(can_sort_ravioli(n, arr))
