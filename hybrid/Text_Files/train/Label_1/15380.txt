#include <bits/stdc++.h>
using namespace std;

int main() {
  int k;
  cin >> k;
  vector<int> r(k);
  for (int i = 0; i < k; i++) { cin >> r[i]; }
  int res = 0;
  sort(r.begin(), r.end());
  for (int i = 1; i < k; i++) { res = max(res, r[i] - r[i - 1] - 1); }
  cout << res << "\n";
}
