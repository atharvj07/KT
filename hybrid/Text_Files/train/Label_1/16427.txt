#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  long long a[n];
  for (int i = 0; i < n; i++) { cin >> a[i]; }
  sort(a, a + n);
  long long ans = a[n - 2];
  for (int i = 0; i < n - 1; i++) {
    ans = max(ans, (a[n - 1] ^ a[i]));
  }
  cout << ans << endl;
  return 0;
}
