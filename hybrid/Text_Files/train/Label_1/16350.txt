#include <bits/stdc++.h>
using namespace std;
string s, t; int n; char a[300005];
int main() {
    cin >> s >> t;
    n = s.size();
    for (int i = 0; i < n; ++i)
        a[i] = min(s[i], t[i]);
    for (int i = 0; i < n; ++i) cout << a[i];
    return 0;
}
