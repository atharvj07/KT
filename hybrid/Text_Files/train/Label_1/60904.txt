t = int(input().strip())
for _ in range(t):
    x, y = map(int, input().strip().split())
    a, b = map(int, input().strip().split())
    cost = 0
    if 2*a < b:
        cost += (abs(x) + abs(y)) * a
    else:
        cost += min(abs(x), abs(y)) * b + abs(x-y) * a
    print(cost)
