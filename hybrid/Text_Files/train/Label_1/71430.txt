import sys

input = sys.stdin.readline

n = int(input())
nums = list(map(int, input().split()))
nums.sort()

ans = sum(nums)
for i in range(n):
    ans = min(ans, nums[i] + (n - i - 1) * nums[0])

print(ans)
