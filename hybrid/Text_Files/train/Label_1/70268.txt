import math

for _ in range(int(input())):
    n, k = map(int, input().split())
    lst = []
    while n > 0:
        tmp = math.ceil(n / k)
        lst.append(tmp)
        n -= tmp
        k -= 1
    print(*lst)
