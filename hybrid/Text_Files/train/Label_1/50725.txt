import java.util.Scanner;

public class ContestPoints {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        int d = scanner.nextInt();

        int mishaPoints = calculatePoints(a, c);
        int vasyaPoints = calculatePoints(b, d);

        if (mishaPoints > vasyaPoints) {
            System.out.println("Misha");
        } else if (vasyaPoints > mishaPoints) {
            System.out.println("Vasya");
        } else {
            System.out.println("Tie");
        }
    }

    private static int calculatePoints(int problemPoints, int submissionTime) {
        return problemPoints - (submissionTime / 25);
    }
}
