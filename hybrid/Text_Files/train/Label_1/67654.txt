t = int(input().strip())
for _ in range(t):
    x, y = map(int, input().strip().split())
    count = 0
    for b in range(1, y + 1):
        a_min = b + 1
        a_max = min(x, (b + 1) * b - 1)
        if a_min <= a_max:
            count += a_max // b - 1
    print(count)
