#include <iostream>

using namespace std;

int main() {
    long long k, l, r, t, x, y;
    cin >> k >> l >> r >> t >> x >> y;
    cout << (x > (r - k) || y < (k - l) || (l - k + x + y) % (x + y) <= t ? "No" : "Yes") << "\n";
}
