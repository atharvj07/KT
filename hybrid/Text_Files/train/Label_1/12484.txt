#include <iostream>
using namespace std;

typedef long long ll;

int main() {
    ll h1, h2, a, b;
    cin >> h1 >> h2 >> a >> b;
    ll day = (h2 - h1 + b - 1) / (a + b);
    if (a > b || (h2 - h1) % (a + b) == 0) { cout << day << endl; }
    else { cout << -1 << endl; }
    return 0;
}
