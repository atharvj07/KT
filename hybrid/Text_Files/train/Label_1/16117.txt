#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int c, d;
    cin >> c >> d;
    if ((c + d) % 2 || c > d)
      cout << "-1\n";
    else
      cout << (c + d) / 2 << "\n";
  }
  return 0;
}
