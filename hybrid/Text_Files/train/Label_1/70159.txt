import sys

input = sys.stdin.readline

t = int(input())

for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))[::-1]
    a.append(0)

    ans = 0
    for x, y in zip(a, a[1:]):
        ans += min(x, y)

    print(ans)
