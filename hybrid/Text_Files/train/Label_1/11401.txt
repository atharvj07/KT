#include <iostream>

using namespace std;

int main() {
  int k1, k2, k3;
  cin >> k1 >> k2 >> k3;
  cout << ((k1 == k2 && k2 == k3) || (k1 + k2 <= k3) || (k2 + k3 <= k1) || (k3 + k1 <= k2) ? "YES" : "NO") << endl;
  return 0;
}
