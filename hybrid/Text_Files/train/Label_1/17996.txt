#include <bits/stdc++.h>
using namespace std;
long long gcd(long long a, long long b) { return (__gcd(a, b) == 1); }
int main() {
  long long b;
  cin >> b;
  if (b == 1) {
    cout << 1;
  } else if (b & 1) {
    cout << 2;
  } else {
    cout << 3;
  }
  return 0;
}
