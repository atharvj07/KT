#include<iostream>
#include<vector>
using namespace std;
int main(){
    int b,k;
    cin>>b>>k;
    vector<int> digits(k);
    int sum=0;
    for(int i=0;i<k;i++){
        cin>>digits[i];
        sum+=digits[i];
    }
    (sum%2==0) ? cout<<"even": cout<<"odd";
    return 0;
}
