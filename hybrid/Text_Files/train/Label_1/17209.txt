#include <iostream>

int main() {
  long long n;
  std::cin >> n;
  n %= 4;
  long long res = 1;
  while (n > 0) {
    if (n % 2) res = (res * 5) % 100;
    n /= 2;
    res = (res * res) % 100;
  }
  printf("%02d", res);
  return 0;
}
