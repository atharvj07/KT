n = int(input())
scores = list(map(int, input().split()))

# Remove scores of 0 as they cannot receive diplomas
scores = [score for score in scores if score > 0]

# Count unique scores since awarding is based on score thresholds
unique_scores = len(set(scores))

print(unique_scores)
