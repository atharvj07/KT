#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while(t--) {
        int x;
        cin >> x;
        if(x >= 2 && x <= 7) cout << 1 << endl;
        else cout << x / 7 + (x % 7 > 0) << endl;
    }
    return 0;
}
