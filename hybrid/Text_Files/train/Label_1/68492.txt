n, m = map(int, input().split())
a = list(map(int, input().split()))

queue = list(range(1, n + 1))
last_child = 0

while queue:
    child = queue.pop(0)
    a[child-1] -= m
    if a[child-1] > 0:
        queue.append(child)
    else:
        last_child = child

print(last_child)
