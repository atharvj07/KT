def pow(a, b, p):
    if b == 0:
        return 1
    if b == 1:
        return a % p
    val = pow(a, b // 2, p)
    val = (val * val) % p
    if b % 2 != 0:
        val = (val * a) % p
    return val % p


n, k, m = map(int, input().split())
print(pow(k, n, m))
