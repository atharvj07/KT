a,b,n,x=map(int,input().split())
def pow(x, n):
    if n == 0:
        return 1
    if n == 1:
        return x
    val = pow(x, n // 2)
    val = val * val
    val %= 1000000007
    if n % 2 != 0:
        val = val * x
    return val % 1000000007
print(pow(a, n) * x + pow(a, n - 1) * b)
