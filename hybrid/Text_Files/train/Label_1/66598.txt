n, k = map(int, input().split())
a = list(map(int, input().split()))
need = 0
for i in range(n):
    if i == 0:
        need = max(0, k - a[i] - k)
    else:
        need = max(0, k - a[i] - a[i - 1])
    a[i] += need
print(need, *a)
