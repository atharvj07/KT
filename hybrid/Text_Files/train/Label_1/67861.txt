from collections import deque

n = int(input())
a = deque(int(x) for x in input().split())

# a.sort()
b = deque(sorted(a))

# simulate the game

x = []
for k in range(n):
    if a[0] <= b[0]:
        a.popleft()
    else:
        b.popleft()
    x.append(a[0])

print(*x)
