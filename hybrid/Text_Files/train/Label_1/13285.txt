#include <bits/stdc++.h>
using namespace std;

int main() {
  int q;
  cin >> q;
  while (q--) {
    long long a;
    cin >> a;
    if (a % 2) {
      cout << a - 2 << endl;
    } else if (a % 4 == 0) {
      cout << a - 4 << endl;
    } else
      cout << a - 1 << endl;
  }
  return 0;
}
