#include <cstdio>
#include <cstring>
int n, a[100001], ans;
int main() {
    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
        scanf("%d", &a[i]), a[i] = a[i] ^ a[i - 1];
    for (int i = 1; i <= n; i++)
        ans += a[i] != a[i - 1];
    printf("%d\n", ans);
    return 0;
}
