import sys

n,k=map(int,input().split())
s=input()

if n<k: sys.stdout.write("-1")
else:
    l=[0]
    for c in s:
        t=sorted(list(set(l-[j for j in l if c<=s[j]])))
        l+=t
        if len(t)>=k:
            sys.stdout.write(str(sum([n-j for j in t[:k]])))
            break
