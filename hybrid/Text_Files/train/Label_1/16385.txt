#include <bits/stdc++.h>
using namespace std;
const int mod = 1e9 + 7;
int main() {
  long long x, k;
  cin >> x >> k;
  long long ans = (x << k) % mod;
  long long y = 1ll << (k - 1);
  ans = (ans - y + mod) % mod;
  cout << ans << '\n';
  return 0;
}
