#include <iostream>

int main() {
  long long x1, y1, x2, y2;
  std::cin >> x1 >> y1 >> x2 >> y2;
  long long w = (x2 - x1) / 2 + 1;
  long long h = y2 - y1 + 1;
  std::cout << w * h << std::endl;
  return 0;
}
