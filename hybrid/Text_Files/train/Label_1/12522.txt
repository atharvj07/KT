#include <bits/stdc++.h>
using namespace std;
int a[100001], b[100001];
int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
    b[a[i] % 2]++;
  }
  cout << (b[0] == n ? 0 : min(b[0], b[1] - 1) + 1) << endl;
  return 0;
}
