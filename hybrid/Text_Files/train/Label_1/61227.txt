['import sys\ninput = sys.stdin.readline\n\nn, t = map(int, input().split())\ntasks = sorted([(t_i, q_i) for t_i, q_i in [map(int, input().split()) for _ in range(n)]], key=lambda x: x[1], reverse=True)\n\ntot = 0\nfor t_i, q_i in tasks:\n    if t_i <= t:\n        tot += q_i\n        t -= t_i\n    else:\n        break\nprint(tot)\n']