#include <iostream>
#include <string>

int main() {
    std::string s;
    getline(std::cin, s);

    for (int k = 0; k < s.length(); ++k) {
        if (k % 2 == 0) {
            std::cout << "Mike\n";
        } else {
            std::cout << "Ann\n";
        }
    }

    return 0;
}
