h1, a1, c1 = map(int, input().split())
h2, a2 = map(int, input().split())

n = (h2 + a2 - 1) // a1
if h1 <= h2 % a1:
    n += 1

print(n)
for i in range(n):
    if h1 <= h2 % a1 or h2 <= 0:
        print("HEAL")
        h1 += c1
    else:
        print("STRIKE")
        h2 -= a1
