ai, phone = input(), input()
ans = 0
i = 0
while i < len(ai) - len(phone) + 1:
    if ai[i: i + len(phone)] == phone:
        ans = max(ans, i + len(phone) - 1)
        i = i + len(phone) - 1
    i += 1
print(ans)
