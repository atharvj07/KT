#include<bits/stdc++.h>
using namespace std;
int main(){
  int n;
  cin >> n;
  vector<int> v(n);
  for(auto &x: v) cin >> x;
  sort(v.begin(), v.end());
  cout << v.size() << " " << (v[v.size() - 1] - v[0] > 1 ? v[v.size() - 1] - v[0] + 1 : v[v.size() - 1] == v[0] ? 1 : 2) << endl;
  return 0;
}
