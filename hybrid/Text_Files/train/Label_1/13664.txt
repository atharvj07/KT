#include <iostream>

using namespace std;

int main() {
  int n, b, p;
  cin >> n >> b >> p;
  int l = 1;
  while (l * 2 <= n) l *= 2;
  cout << l * (n - l + 1) * b << ' ' << p * n << endl;
  return 0;
}
