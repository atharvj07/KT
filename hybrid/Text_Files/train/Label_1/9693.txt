#include <iostream>
using namespace std;
#define ll long long

int main() {
    ll n, x, y, c;
    cin >> n >> x >> y >> c;
    cout << ((c - 1) * 2 > (n - x + 1)) + ((c - 1) * 2 > (n - y + 1));
    return 0;
}
