#include <bits/stdc++.h>
using namespace std;
int n, m, x, y, a[100005], b[100005]; long long ans = 0, ans2;
int main() { cin >> n >> m; while (m--) { cin >> x >> y; if (!a[x]) { ans += n - 1; a[x] = 1; } else ans--; if (!b[y]) { ans += n - 1; b[y] = 1; } else ans--; cout << ans << "\n"; } }
