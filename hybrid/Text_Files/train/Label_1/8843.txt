#include <bits/stdc++.h>
using namespace std;
int n, a[1001], ans;
int main()
{
    cin >> n;
    for (int i = 1; i <= n; ++i) cin >> a[i];
    sort(a + 1, a + n + 1);
    for (int i = 1; i < n; ++i)
        if (a[i + 1] - a[i] == 1) ans++;
    cout << ans << endl;
    return 0;
}
