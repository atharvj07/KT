#include <iostream>

using namespace std;

int main() {
    int n, s;
    cin >> n >> s;
    int a[n + 1], b[n + 1];
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i <= n; i++) cin >> b[i];
    if (a[1] && b[s]) cout << "YES";
    else cout << "NO";
    return 0;
}
