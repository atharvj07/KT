def solve(k):
    ans = []
    for i in range(1 << k):
        s = ""
        for j in range(1 << k):
            if ((i >> j) & 1) == 0: s += "+"
            else: s += "*"
        ans.append(s)
    return ans

k = int(input())
ans = solve(k)
for i in ans:
    print(i)
