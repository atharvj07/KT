import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int t = in.nextInt();
        while (t-- > 0) {
            long L = in.nextLong(), v = in.nextLong(), l = in.nextLong(), r = in.nextLong();
            if (r % v < l % v) r += v;
            System.out.println((r - l) / v + ((l % v == 0 && l >= v) ? 1 : 0));
        }
    }
}
