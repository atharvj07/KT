n, k = map(int, input().split())
a = list(map(int, input().split()))
ans = []
for d in range(k):
    t = 0
    for ai in a:
        t = (t + ai) % k
    if t == d:
        ans.append(d)
print(len(ans))
print(' '.join(str(x) for x in ans))
