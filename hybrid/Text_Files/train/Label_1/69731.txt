def sol(t):
    for _ in range(t):
        x, y, n = map(int, input().split())
        ans = (n // x) * x + y
        if ans <= n:
            print(ans)
        else:
            print(ans - x)

sol(int(input()))
