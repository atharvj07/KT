#include <bits/stdc++.h>
using namespace std;
int main() {
    string s;
    cin >> s;
    int ans = 0, now = 0;
    for (auto c : s) {
        if (c == '-')
            now--;
        else
            now++;
        ans = max(ans, now);
    }
    cout << ans << endl;
    return 0;
}
