import math

a, b = map(int, input().split())
n = int(input())
ans = math.inf
for i in range(n):
    x, y, v = map(int, input().split())
    dist = math.sqrt((x - a) ** 2 + (y - b) ** 2)
    ans = min(ans, dist / v)
print(ans)
