import sys
input=sys.stdin.readline

t=int(input())
for _ in' '*t:
    n=int(input())
    a=[*map(int,input().split())]
    if not all(a[0:i]==a[i:i+i&1]for i in range(1,n))or not n%2:print(-1);continue
    print(max(a)*2,a[0])
