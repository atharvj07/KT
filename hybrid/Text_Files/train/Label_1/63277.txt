from collections import defaultdict
n = int(input())
names = [input() for _ in range(n)]
groups = defaultdict(int)
for name in names:
    name = name.replace('u', 'o').replace('h', 'k')
    groups[name] += 1
print(len(groups))
