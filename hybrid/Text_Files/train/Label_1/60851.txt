for t in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    l = list(map(int, input().split()))
    un = [a[i] for i, j in enumerate(l) if not j]
    un.sort()
    for i, j in enumerate(l):
        if not j:
            a[i] = un.pop(0)
    print(*a)
