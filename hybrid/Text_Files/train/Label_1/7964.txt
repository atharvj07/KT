#include <bits/stdc++.h>
using namespace std;

int main() {
    long long r;
    cin >> r;
    long long t = sqrtl(r);
    if (t * t != r) {
        cout << "NO" << endl;
        return 0;
    }
    long long y = t - 1;
    cout << y << " " << 1 << endl;
    return 0;
}
