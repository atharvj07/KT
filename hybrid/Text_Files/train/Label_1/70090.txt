import sys

n = int(input())
ops = []
for _ in range(n):
    ops.append([int(x) for x in input().split()])
m = int(input())
q = [int(x) for x in input().split()]

def get(i):
    if i <= len(s):
        return s[i - 1]
    j = len(s)
    for op in ops:
        if op[0] == 1:
            j += 1
            if j == i:
                return op[1]
        else:
            l, c = op[1], op[2]
            if j + l * c >= i:
                return get((i - j + l) % l + 1)
    assert False

s = []
for op in ops:
    if op[0] == 1:
        s.append(op[1])
    else:
        l, c = op[1], op[2]
        for _ in range(c):
            s.extend(s[len(s) - l:])

for x in q:
    print(get(x), end=' ')
