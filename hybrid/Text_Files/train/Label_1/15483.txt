#include <algorithm>
#include <iostream>
using namespace std;

int main()
{
    long long x;
    cin >> x;
    if (x % 2 || x < 5)
        cout << -1 << endl;
    else
        cout << ((x - 2) / 2) * 3 + 2 << endl;
    return 0;
}
