#include <iostream>

using namespace std;

int main() {
  int n;
  cin >> n;
  int a[n], ans = 1;
  for (int i = 0; i < n; i++) {
    cin >> a[i];
    if (a[i] == 0) {
      if (ans > 1) {
        ans--;
      }
    } else {
      ans++;
    }
  }
  cout << ans << endl;
  return 0;
}
