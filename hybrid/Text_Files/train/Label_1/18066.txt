#include <bits/stdc++.h>
using namespace std;
long long a[51];
int main() {
  long long n, k, ans = 0;
  cin >> n >> k;
  for (long long i = 1; i <= n; i++) cin >> a[i];
  sort(a + 1, a + n + 1);
  for (long long i = k; i <= n; i++) ans |= a[i];
  cout << ans << endl;
  return 0;
}
