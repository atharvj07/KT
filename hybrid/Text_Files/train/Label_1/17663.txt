#include <iostream>

int main() {
    std::string s;
    std::cin >> s;
    int ans = 0, cnt = 0, n = s.size();
    for (char c: s) {
        cnt += (c == 'V');
        ans += (c == 'K' && cnt > 0);
    }
    std::cout << ans + (cnt > 0) << std::endl;
    return 0;
}
