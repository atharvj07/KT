#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  vector<int> a(n);
  for (int i = 0; i < n; ++i) { cin >> a[i]; }
  sort(begin(a), end(a));
  cout << accumulate(begin(a) + (k - 1) / 2, begin(a) + k, 0ll) << endl;
  return 0;
}
