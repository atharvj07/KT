for _ in range(int(input())):
    n, k = map(int, input().split())
    x = 1
    while n:
        chk = (x % 10 + (x + k) % 10) // 10
        chk += (x + k) // 10
        if n < chk:
            x = -1
            break
        n -= chk
        x = x * 10 + 9
    print(x)
