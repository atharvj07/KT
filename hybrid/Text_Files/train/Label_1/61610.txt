n, x = map(int, input().split())
a = list(map(int, input().split()))
ans = [0] * n
ans[x - 1] = a[x - 1]
for i in range(x, n):
    ans[(i - 1) % n] = a[i % n] - ans[i % n]
for i in range(x - 2, -1, -1):
    ans[i % n] = a[i % n] - ans[i % n]
print(*ans)
