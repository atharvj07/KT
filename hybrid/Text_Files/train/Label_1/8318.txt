#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int n, r;
    cin >> n >> r;
    const double pi = acos(-1.);
    double a = .5 * n * r * r / tan(pi / n) - .5 * n * r * r;
    printf("%.10f\n", a);
}
