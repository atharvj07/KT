n, s = map(int, input().split())
t = []
for i in range(n):
    f, ti = map(int, input().split())
    t.append((f, ti))
t.sort(key=lambda x: x[1])  # sort by arrival times
ans = 0
for x in t:
    if ans < x[1]:
        ans = x[1]
    ans += s - x[0]
print(ans)
