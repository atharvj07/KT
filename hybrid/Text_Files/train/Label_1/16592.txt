#include <iostream>

int main() {
  int n;
  std::string s;
  std::cin >> n >> s;
  int ans = 0;
  for (int i = 0; i < n; ++i) {
    if (s[i] == 'U') {
      ans += i % 2;
    }
  }
  std::cout << ans << std::endl;
  return 0;
}
