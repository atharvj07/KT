#include <iostream>
#include <cstdio>
#include <cmath>
int main() {
  long long n, h, k, a, ans = 0;
  std::cin >> n >> h >> k;
  for (int i = 0; i < n; i++) {
    std::cin >> a;
    ans += ceil((double)a / k);
  }
  std::cout << ans << "\n";
  return 0;
}
