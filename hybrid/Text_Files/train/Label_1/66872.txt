from collections import defaultdict
n=int(input())
a=list(map(int,input().split()))
d=defaultdict(set)
d[0].add(-1)
for i in range(n):
    for x in list(d):
        d[x|a[i]].add(i)
ans=[0]+sorted(list(x for x in d if len(d[x])>0))
print(len(ans)-1)
print(*ans[1:])
