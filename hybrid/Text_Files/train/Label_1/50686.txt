import java.util.*;
import java.io.*;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        long x = scanner.nextLong();
        long y = scanner.nextLong();
        long z = scanner.nextLong();
        long minChizhiksToGive = Math.min(x, y);
        long maxCoconuts = (x + y) / z;
        System.out.println(maxCoconuts + " " + minChizhiksToGive);
    }
}
