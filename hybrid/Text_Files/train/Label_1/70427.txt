def min_resistors(a, b):
    ans = 0
    while a != 0 and b != 0:
        if a >= b:
            ans += a // b
            a %= b
        else:
            ans += b // a
            b %= a
    return ans

a, b = map(int, input().split())
print(min_resistors(a, b))
