l,r=map(int,input().split())
ld=[0]*10
for i in range(l,r+1):
    x=list(str(i))
    if len(x)!=len(set(x)):continue
    flag=1
    for j in x:
        j=int(j)
        if ld[j]==1:
            flag=0
            break
        ld[j]=1
    if flag==1:
        print(i)
        exit()
print(-1)
