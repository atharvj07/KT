n, x_0, y_0 = map(int, input().split())
slopes = set()

for _ in range(n):
    x, y = map(int, input().split())
    dx, dy = x - x_0, y - y_0
    slope = 'inf' if dx == 0 else dy / dx
    slopes.add(slope)

print(len(slopes))
