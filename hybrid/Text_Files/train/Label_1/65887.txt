n, m = map(int, input().split())
teleports = [list(map(int, input().split())) for i in range(n)]
teleports.sort()
cur = 0
for t in teleports:
    if t[0] <= cur < t[1]:
        cur = t[1]
        if cur >= m:
            print("YES")
            exit()
print("NO")
