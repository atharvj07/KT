#include <iostream>
using namespace std;
int main() {
    long long n, m, ans;
    cin >> n >> m;
    ans = (n / 5) * (m / 5);
    if (n % 5 == 0 && m % 5 == 0) ans--;
    cout << ans * 5 << endl;
    return 0;
}
