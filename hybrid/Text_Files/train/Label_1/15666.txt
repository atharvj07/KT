#include <bits/stdc++.h>
using namespace std;
int main() {
    int up, down;
    cin >> up >> down;
    cout << up + 1 << ' ';
    for (int i = 1; i <= up; i++) cout << i << ' ';
    for (int i = up + 2; i <= up + down + 1; i++) cout << i << ' ';
    return 0;
}
