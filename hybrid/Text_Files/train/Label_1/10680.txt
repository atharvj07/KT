#include <iostream>

using namespace std;

int main() {
  long long int t, n;
  cin >> t;
  while (t--) {
    cin >> n;
    cout << ((n & 1) || ((n % 2) && (n % 3)) ? "YES" : "NO") << endl;
  }
  return 0;
}
