import sys
n = int(sys.stdin.readline().rstrip())
a = [int(x) for x in sys.stdin.readline().rstrip().split()]
c = 0
m = max(a)
for i in range(len(a)):
    if(a[i] == m):
        c += 1
        continue
    if(m-a[i] > 1):
        c += m-a[i]-1
        a[i] = m-1
print(c)
