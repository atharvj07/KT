n = int(input())
words = [w for w in input().split()]
res = words[0]
for w in words[1:]:
    ix = len(w) - 1
    while ix >= 0 and res[-ix:] == w[:ix]:
        ix -= 1
    res += w[ix + 1:]
print(res)
