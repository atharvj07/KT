#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  vector<int> a = {4, 4, 4, 4, 4, 4, 4, 4, 4, 4};
  for (char c : s) a[c - '0']--;
  for (int i : a) if (i != 0) cout << "NO" << endl, exit(0);
  cout << "YES" << endl;
}
