from math import gcd
n=int(input())
a=[int(i) for i in input().split()]
ans,k=1e9,0
for i in range(n):
    if a[i]!=-1:
        if not k:k=a[i]
        else:k=gcd(k,a[i]-a[i-1])
    elif k:ans=min(ans,k)
print(ans)
