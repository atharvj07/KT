from collections import Counter

n = int(input())
a = [input() for _ in range(n)]
b = [input() for _ in range(n)]

ca = Counter(a)
cb = Counter(b)
ans = 0
for size, val in ca.items():
    if size == cb[size]: continue
    if size[:-1] == cb[size]: ans += 1
    else: ans += 2
print(ans)
