#include <iostream>
using namespace std;
int main() {
    int q;
    long long k, n, a, b;
    cin >> q;
    while (q--) {
        cin >> k >> n >> a >> b;
        if (k > n * (a - b))
            cout << (n + b - 1) / b << "\n";
        else
            cout << -1 << "\n";
    }
    return 0;
}
