#include <iostream>
#include <string>

using namespace std;

int solve() {
  int n;
  cin >> n;
  string s;
  cin >> s;

  int ans = 0;
  for (int i = 0; i < n - 1; i++) {
    if (s[i] == s[i + 1]) {
      ans++;
    }
  }

  return ans;
}

int main() {
  cout << solve() << endl;
  return 0;
}
