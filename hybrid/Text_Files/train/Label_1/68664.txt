n, m = map(int, input().split())
grid = [list(map(int, input().split())) for _ in range(n)]
k = int(input())
for _ in range(k):
    l, r = map(int, input().split())
    grid.sort(key=lambda x: x[l - 1:r])
    ans = "Yes" if all(x <= y for x, y in zip(grid, grid[1:])) else "No"
    print(ans)
