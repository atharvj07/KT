import sys

sys.stdin.readline()  # read t
for line in sys.stdin:
    l, r = map(int, line.split())
    ans = 0
    for i in range(l, r + 1):
        if len([x for x in str(i) if x != '0']) <= 3:
            ans += 1
    print(ans)
