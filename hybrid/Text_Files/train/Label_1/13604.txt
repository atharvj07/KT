#include <bits/stdc++.h>
using namespace std;
int main() {
  string s; cin >> s;
  map<char, int> m;
  for (char c : s) m[c]++;
  int k = 0;
  for (auto it : m) k = __gcd(k, it.second);
  if (k != 0)
    cout << "Yes";
  else
    cout << "No";
  return 0;
}
