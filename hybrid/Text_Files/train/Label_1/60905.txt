import sys
for _ in range(int(input())):
    x, y = map(int, input().split())
    a, b = map(int, input().split())
    print(min(a * abs(x), b * max(abs(x), abs(y)), a * abs(y) + b * min(abs(x), abs(y))))
