from collections import Counter

n = int(input())
a = Counter(map(int, input().split()))
b = []

for x in sorted(a):
    if a[x] > 1:
        b += [x, x * 2];
        a[x] -= 2
        if a[x]:
            b += [x]
    else:
        b += [x]

print(len(b))
print(*b)
