events = input().strip()
min_people = 0
current_inside = 0

for event in events:
    if event == '+':
        current_inside += 1
    else:
        if current_inside > 0:
            current_inside -= 1
        else:
            min_people += 1

print(min_people + current_inside)
