#include<iostream>
#include<vector>
using namespace std;
vector<int>a;
int main()
{
	int n,m,k;
	cin>>n>>m>>k,a.resize(n);
	for(auto &i:a)cin>>i;
	if(m>k)
	{
		for(int i=0;i<n;++i)
		{
			if(m<=k)break;
			if(a[i]>1){m-=a[i]-1;k+=a[i]-1;}
		}
	}
	if(m<=k)cout<<n;
	else cout<<-1;
	return 0;
}
