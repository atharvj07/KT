#include <iostream>

int main() {
    int t; std::cin >> t;
    while (t--) {
        long long a, b, x; std::cin >> a >> b >> x;
        std::cout << ((a == x || b == x || (a + b) % 2 == x % 2 || (a - b) % 2 == x % 2) ? "YES" : "NO") << std::endl;
    }
    return 0;
}
