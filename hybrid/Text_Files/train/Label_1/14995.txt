#include <bits/stdc++.h>
using namespace std;

int main() {
  string a, b;
  cin >> a >> b;
  int n = a.size(), ans = 0;
  for (int i = 0; i < n; i++) {
    if ((a[i] ^ b[i]) == '0') ans++;
  }
  cout << ans / 3 << '\n';
  return 0;
}
