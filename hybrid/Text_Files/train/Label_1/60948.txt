q=int(input())
a="""What are you doing at the end of the world? Are you busy? Will you save us?"""
ans=[]
for _ in range(q):
	n,k=map(int,input().split())
	if n==1:
		ans.append(a[k-1]if k<=len(a) else ".")
	else:
		t=(k-1)//(len(a))
		k=k%len(a)
		if not k:
			ans.append("?")
		else:
			ans.append(a[k-1])
print(*ans)
