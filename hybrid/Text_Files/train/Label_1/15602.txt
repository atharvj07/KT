#include <iostream>

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  for (int i = 0; i < n; i++) {
    char c = 'A' + (i % 2);
    char d = 'A' + ((i % 2) ^ (m % 2));
    for (int j = 0; j < m; j++) {
      cout << (j % 2 ? d : c);
    }
    cout << endl;
  }
  return 0;
}
