#include <iostream>
#include <string>
using namespace std;
int main() {
  int q; cin >> q;
  while (q--) {
    int n; cin >> n;
    string s, t; cin >> s >> t;
    bool same = true;
    for (int i = 0; i < n; i++) same &= (s[i] == t[i]);
    cout << (same ? "YES" : "NO") << endl;
  }
  return 0;
}
