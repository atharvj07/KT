import sys
for _ in range(int(input())):
    n = int(input())
    a = [int(x) for x in input().split()]
    b = [int(x) for x in input().split()]
    if sum(a) != sum(b) or (len(a) - a.count(1) > len(b) - b.count(1)) or (len(a) - a.count(-1) < len(b) - b.count(-1)): print('NO')
    else: print('YES')
