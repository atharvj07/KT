import sys

input = sys.stdin.readline

n, r = map(int, input().split())
a = []
for _ in range(n):
    a.append(tuple(map(int, input().split())))
a.sort()
ans = 0
l, r = -10**10, 10**10
for x, y in a:
    if l <= x - r <= r:
        ans += 1
    l = x - r
    r = x + r
print(ans)
