q = int(input().strip())
for _ in range(q):
    n = int(input().strip())
    a = list(map(int, input().strip().split()))
    total_sum = sum(a)
    # Calculate the minimum possible equal price
    min_price = (total_sum + n - 1) // n
    print(min_price)
