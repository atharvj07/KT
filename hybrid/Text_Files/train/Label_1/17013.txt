#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        string s;
        cin>>s;
        int f=0;
        for(int i=0; i<n-1; i++)
        {
            if(s[i]=='a')
            {
                int cnta=1,cntb=0,cntc=0;
                for(int j=i+1; j<n; j++)
                {
                    if(s[j]=='a')
                    cnta++;
                    else if(s[j]=='b')
                    cntb++;
                    else if(s[j]=='c')
                    cntc++;
                    if(cnta>cntb && cnta>cntc)
                    {
                        f=1;
                        cout<<j-i+1<<endl;
                        break;
                    }
                }
                if(f)
                break;
            }
        }
        if(!f)
        {
            cout<<-1<<endl;
        }
    }
    return 0;
}
