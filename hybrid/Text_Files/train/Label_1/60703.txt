n, m = tuple(map(int, input().split()))
A = sorted([(int(x), int(y)) for x, y in [input().split() for _ in range(n)]])
x, ans = 0, 0
for p, c in A:
    ans = max(ans, x + c)
    x += max(p - 1, 0)
print(ans)
