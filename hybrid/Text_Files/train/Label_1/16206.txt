#include <iostream>
using namespace std;
int main() {
  int q;
  cin >> q;
  while (q--) {
    long long l, v, a, b;
    cin >> l >> v >> a >> b;
    long long ans = (l + v - 1) / v - (a + v - 1) / v + (a - 1) / v;
    cout << ans << endl;
  }
  return 0;
}
