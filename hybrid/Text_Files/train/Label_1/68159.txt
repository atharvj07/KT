from itertools import *
n, k = map(int, input().split())
p = list(range(n, 0, -1))
k -= 1
for i in range(n - 1):
    if k < n - i - 1:
        break
    j = i + k + 1
    x = p[i]
    p[i] = p[j]
    p[j] = x
print(*p)
