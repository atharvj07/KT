#include<bits/stdc++.h>
using namespace std;
int main(){
	long long x;
	cin>>x;
	vector<long long>v(x);
	long long sum=0,odd=0;
	for(int i=0;i<x;i++){
		cin>>v[i];
		sum+=v[i];
		if(v[i]&1)odd++;
	}
	if(sum&1 || odd)cout<<"First\n";
	else cout<<"Second\n";
	return 0;
}
