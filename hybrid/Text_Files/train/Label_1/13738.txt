#include <iostream>
#include <vector>
using namespace std;

int main() {
  uint64_t n, k, d, m;
  cin >> n >> k >> m >> d;
  if (m >= n / k)
    cout << n / k;
  else
    cout << n / k + (m >= (n % k + d - 1) / d);
  return 0;
}
