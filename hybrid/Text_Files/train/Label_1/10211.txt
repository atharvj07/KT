#include <bits/stdc++.h>

using namespace std;
using ll = long long;

ll f(ll n, ll b) { return n / b + (n % b != 0); }

int main() {
  ll n;
  cin >> n;
  ll ans = 1;
  for (int i = 1; pow(3, i) <= n; i++) ans = max(ans, f(n, pow(3, i)));
  cout << ans << '\n';
}
