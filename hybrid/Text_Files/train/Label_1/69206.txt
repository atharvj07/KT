n,*a = map(int,input().split());a.sort()
n,*b = map(int,input().split());b.sort()
s=t=0
while a and b:
    if a[-1]>b[-1]: s+=a.pop(); continue
    if b[-1]>a[-1]: t+=b.pop(); continue
    a.pop(); b.pop()
print(s-t)
