import sys

n = int(sys.stdin.readline())
in_deg = [0] * n
for i in range(n - 1):
    u, v = map(int, sys.stdin.readline().split())
    u -= 1
    v -= 1
    in_deg[u] += 1
    in_deg[v] += 1
    
ans = 0
for i in range(0, n):
    ans = max(ans, in_deg[i])

print(ans - 1)
