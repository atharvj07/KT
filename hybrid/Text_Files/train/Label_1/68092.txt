n = int(input())
sentence = input().split()
compressed = sentence[0]
for word in sentence[1:]:
    if compressed.endswith(word[:len(compressed)]):
        compressed += word[len(compressed):]
    else:
        compressed += word
print(compressed)
