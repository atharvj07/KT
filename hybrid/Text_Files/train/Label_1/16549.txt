#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    string a;
    cin >> a;
    map<char, vector<int>> pos;
    for (int i = 0; i < n; ++i) pos[a[i]].push_back(i);
    string b = a;
    reverse(b.begin(), b.end());
    bool ok = true;
    for (char c : "trygub") {
      if (pos[c].size() > 1) {
        ok = false;
        break;
      }
    }
    if (!ok) {
      b = a;
    }
    cout << b << endl;
  }
  return 0;
}
