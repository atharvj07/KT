#include <bits/stdc++.h>
using namespace std;

string solve(int n, int k, string t) {
  string s = "";
  for (int i = 0; i < k; i++) s += t;
  return s;
}

int main() {
  int n, k;
  cin >> n >> k;
  string t;
  cin >> t;
  cout << solve(n, k, t) << '\n';
  return 0;
}
