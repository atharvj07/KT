for _ in range(int(input())):
    n,r=map(int,input().split())
    l=list(map(int,input().split()))
    if len(set(l))==1:
        print(l[0]+1)
    elif len(set(l))==len(l):
        print(min(l))
    else:
        print(min(l,key=lambda x:l.count(x))+(l.count(min(l,key=lambda x:l.count(x)))%2))
