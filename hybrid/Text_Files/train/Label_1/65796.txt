n, t = map(int, input().split())
a = list(map(int, input().split()))
l = [1] * (n+1)
ans = 0
for i in range(t):
    for j in range(n):
        l[a[j]] = max(l[a[j]], l[a[j]-n]+1)
        ans = max(ans, l[a[j]])
    a = list(map(lambda x: x - n, a))
print(ans)
