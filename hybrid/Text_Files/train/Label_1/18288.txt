#include <iostream>
#include <vector>

int main() {
  int b, k;
  std::cin >> b >> k;
  std::vector<int> a(k);
  for (int& x : a) std::cin >> x;
  int n = 0;
  for (int x : a) n = n * b + x;
  std::cout << (n & 1 ? "odd" : "even") << std::endl;
}
