n = int(input())
li = []
for i in range(n):
  li.append(list(map(int, input().split())))
li.sort(key = lambda x: x[1])
ans = 0
need = 0
for a, p in li:
  if need <= 0: need = a
  need -= a
  ans += need * p
print(ans)
