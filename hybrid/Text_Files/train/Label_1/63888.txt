t = int(input())
for _ in range(t):
    a, b, c, r = map(int, input().split())
    if a > b:
        a, b = b, a
    left = max(a, c - r)
    right = min(b, c + r)
    print(max(0, b - a - max(0, right - left)))
