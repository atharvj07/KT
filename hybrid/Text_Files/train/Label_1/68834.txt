n = int(input())
a = []
for i in range(n):
    a += [tuple(map(int, input().split()))]
a.sort()
ans = 0
for i in range(n):
    ans = max(ans, a[i][0] - 1)
    ans = (ans + a[i][1]) // a[i][1] * a[i][1]
print(ans)
