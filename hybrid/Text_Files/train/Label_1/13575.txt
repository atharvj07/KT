#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a, ans = 1;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a;
        ans += (a > 0);
    }
    cout << ans << endl;
}
