import sys
import array

def op1(a, b, c, d, z):
    graph[a][b] -= z
    graph[c][d] -= z
    graph[c][b] += z
    graph[a][d] += z

def op2(a):
    graph[a][a] = 0

def check(a, b):
    return a == 0 or b == 0 or a == b

def dfs(x, pre):
    for i in range(1, n+1):
        if graph[x][i] > 0 and check(i, pre):
            dfs(i, x)
            op2(x)
    for i in range(1, n+1):
        if graph[x][i] > 0 and check(x, pre):
            dfs(i, x)

n, m = map(int, input().split())
graph = [[0] * (n + 1) for _ in range(n + 1)]
for i in range(m):
    a, b, c = map(int, input().split())
    graph[a][b] = c

for i in range(1, n+1):
    for j in range(i+1, n+1):
        if graph[i][j] > 0:
            dfs(i, 0)
            dfs(j, 0)

ans = []
for i in range(1, n+1):
    for j in range(i+1, n+1):
        if graph[i][j] > 0:
            ans.append([i, j, graph[i][j]])

print(len(ans))
for answer in ans:
    print(*answer)

