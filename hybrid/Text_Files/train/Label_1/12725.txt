#include <cstdio>
long long n, x, sum = 0, a[200005];
int main() {
  scanf("%lld", &n);
  for (int b = 1; b <= n; b++) { scanf("%lld", &a[b]), sum += a[b]; }
  for (int b = 1; b <= n; b++) {
    if (sum >= (n + 1) / 2 * a[b]) {
      printf("%d", b);
      return 0;
    }
    sum -= a[b];
  }
}
