#include <iostream>
#include <string>

using namespace std;

int main() {
  string s, t;
  cin >> s >> t;
  int cur = 0;
  for (int i = 0; i < t.size(); i++) {
    if (t[i] == s[cur]) cur++;
  }
  cout << cur + 1 << endl;
}
