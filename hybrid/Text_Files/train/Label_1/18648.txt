#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cin >> n;
    vector<int> ans;
    int c = 1;
    for(int i=2; i<=n; i++){
        if((i-c)%2==1) ans.push_back(i), c = i;
    }
    ans.push_back(1);
    cout << ans.size() << endl;
    for(int x: ans) cout << x << ' ';
}
