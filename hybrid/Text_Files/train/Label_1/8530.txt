#include <vector>
#include <iostream>

int main() {
  int n, m;
  std::cin >> n >> m;
  std::vector<int> a(n);
  for (auto &ai: a) { std::cin >> ai; }
  int ans = 0;
  for (auto &ai: a) { ans += ai <= m; }
  std::cout << ans << std::endl;
}
