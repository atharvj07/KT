import sys

n, k = map(int, input().split())
fav = [[] for i in range(n + 1)]
for i in range(k):
    x, y = map(int, input().split())
    fav[x].append(i)
    fav[y].append(i)

ans = k
for i in range(1, n + 1):
    t = len(fav[i])
    if t:
        ans = min(ans, k - t)

print(ans)
