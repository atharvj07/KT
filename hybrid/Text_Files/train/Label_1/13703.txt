#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int i = 0; i < n; i++) cin >> a[i];
  int now = 1;
  long long ans = 0;
  for (int i = 0; i < n; i++) {
    ans += abs(a[i] - now);
    now = a[i];
  }
  cout << ans << '\n';
  return 0;
}
