for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    # DP Approach
    dp = [1] * (n * n)
    for i in range(1, n * n):
        for j in range(i):
            if a[i % n] > a[j % n]:
                dp[i] = max(dp[i], dp[j] + 1)
    print(max(dp))
