#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll MX = 1e12;
int main() {
    ll n;
    cin >> n;
    if (n % 4 <= 1)
        cout << n / 4 * 4;
    else
        cout << (n / 4 - 1) * 4 + 2;
    return 0;
}
