#include <bits/stdc++.h>
using namespace std;

int gcd(int a, int b) { return (b==0)?a:gcd(b,a%b); }
int main() {
  string s; cin >> s;
  int n = s.size(), g = 0;
  for (int i = 0; i < n; ++i) g = gcd(g,n-i);
  cout << n/g << endl;
}
