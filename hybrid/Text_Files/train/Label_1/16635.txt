#include <iostream>
#include <vector>
using namespace std;

int main() {
    long long n, a, b;
    cin >> n >> a >> b;
    vector<long long> x(n);
    for (auto &i : x) cin >> i;
    for (auto &i : x) cout << i - (i * a) / b << " ";
    cout << endl;
}
