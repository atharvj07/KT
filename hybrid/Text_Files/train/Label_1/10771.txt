#include <bits/stdc++.h>

using namespace std;

int main() {
  int n; cin >> n;
  vector<int> p(n / 2);
  for (int &x : p) cin >> x;
  int ans = n / 2;
  sort(p.begin(), p.end());
  for (int i = 0; i < n / 2 - 1; i++) {
    ans += (p[i + 1] - p[i] + 1) / 2;
  }
  cout << ans << endl;
  return 0;
}
