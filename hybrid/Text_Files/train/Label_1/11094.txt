#include<bits/stdc++.h>
using namespace std;
int main() {
  int n, k, a, m, x, cnt = 0, l = a + 1;
  cin >> n >> k >> a;
  cin >> m;
  while (m--) {
    cin >> x;
    if (x <= l)cnt++;
    l = max(x, l + a);
  }
  cout << (cnt == k ? cnt : -1) << endl;
  return 0;
}
