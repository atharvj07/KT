#include <iostream>

using namespace std;

int main() {
    long long n, m, a, b;
    cin >> n >> m >> a >> b;
    if (n % m == 0) cout << 0;
    else {
        long long rem = (n % m);
        if ((m - rem) * a < rem * b) cout << (m - rem) * a;
        else cout << rem * b;
    }
    return 0;
}
