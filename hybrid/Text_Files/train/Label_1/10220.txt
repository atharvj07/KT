#include <bits/stdc++.h>
using namespace std;
int main() {
    int t, p, q;
    cin >> t;
    for (int i = 0; i < t; i++) {
        cin >> p >> q;
        if (p % q == 0) {
            p /= q;
        }
        cout << p << "\n";
    }
    return 0;
}
