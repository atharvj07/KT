n, c1, c2 = map(int, input().split())
a = list(map(int, input()))
p = []
for i in range(n):
    if a[i]: p.append(i)
ans = c1 * n
for i in range(len(p) - 1):
    l = (p[i] + p[i + 1]) // 2
    r = l + 1
    ans = min(ans, (c1 + c2) * (l - p[i])**2 + c1 + c2 * (r - p[i + 1])**2)
print(ans)
