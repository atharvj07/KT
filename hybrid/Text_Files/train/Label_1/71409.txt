import sys
input = sys.stdin.readline

n = int(input())
s = input()
stack = []
ans = 0
for c in s:
    if c == '(':
        stack.append(c)
    else:
        if not stack:
            ans += 1
        else:
            stack.pop()
print(ans if not stack else -1)
