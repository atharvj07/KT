#include <iostream>

int main() {
  char c;
  long long cur = 0, ans = 0;
  while (std::cin.get(c)) {
    if (c == 'v') cur++;
    else if (cur > 0) {
      ans += cur * (cur - 1) / 2;
      cur = 0;
    }
  }
  if (cur > 0) ans += cur * (cur - 1) / 2;
  std::cout << ans << '\n';
}
