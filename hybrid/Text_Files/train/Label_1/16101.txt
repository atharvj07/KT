#include <iostream>
#include <vector>

using namespace std;

int main() {
    long long n, x, y;
    cin >> n >> x >> y;
    if (x > y) swap(x, y);
    long long ans = (n + x - 1) / x;
    if (ans % 2) ans = (ans + x - 1) % (2 * x);
    cout << ans << endl;
    return 0;
}
