#include <bits/stdc++.h>

using namespace std;

const int mod = 1e9 + 7;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int k, p;
  cin >> k >> p;
  long long ans = 1;
  for (int i = 1; i <= k; i++) {
    (ans *= i) %= p;
  }
  for (int i = 1; i <= k; i++) {
    (ans *= i) %= p;
  }
  cout << ans << "\n";
  return 0;
}
