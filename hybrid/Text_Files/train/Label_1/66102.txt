['import sys\ninput = sys.stdin.readline\n\nn,k = map(int,input().split())\ncards = [input().strip() for _ in range(n)]\nans = 0\n\nfor i in range(k):\n    same_or_diff = [0,0]\n    for j in range(n):\n        if cards[j][i] == cards[0][i]:\n            same_or_diff[0] += 1\n        else:\n            same_or_diff[1] += 1\n    \n    if same_or_diff[0] >= 3 or same_or_diff[1] >= 3:\n        ans += same_or_diff[0] * (same_or_diff[0]-1) * (same_or_diff[0]-2)\n        ans += same_or_diff[1] * (same_or_diff[1]-1) * (same_or_diff[1]-2)\n        ans += 6 * same_or_diff[0] * same_or_diff[1] * (n-same_or_diff[0]-same_or_diff[1])\n\nprint(ans//6)\n']