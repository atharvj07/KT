import sys
t = int(input())
for _ in range(t):
    n, p, k = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    ans = 0
    for i in range(0, n, k):
        if p >= a[i]:
            ans += k
            p -= a[i]
        else:
            break
    print(ans)
