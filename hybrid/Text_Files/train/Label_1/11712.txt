#include <iostream>
#include <vector>

using namespace std;

int main() {
  int n;
  cin >> n;
  int ans = 1;
  for (int i = 1; i <= 31; i++) {
    if ((1ll << (2 * i) - 1) <= n) {
      ans = (1ll << (2 * i) - 1);
    }
  }
  cout << ans << endl;
  return 0;
}
