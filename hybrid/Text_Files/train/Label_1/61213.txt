import sys
input = sys.stdin.readline

for t in range(int(input())):
    n = int(input())
    a = sorted(list(map(int, input().split())))
    print(max(0, min(a[-1] - 1, sum(x < a[-1] - 1 for x in a) // 2)))
