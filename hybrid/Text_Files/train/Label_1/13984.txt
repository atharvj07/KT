#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, m, k;
        cin >> n >> m >> k;
        cout << (n == 1 && m == 1 && k == 0 ? "YES" : (k == (n - 1) * m + (m - 1) * n ? "YES" : "NO")) << "\n";
    }
    return 0;
}
