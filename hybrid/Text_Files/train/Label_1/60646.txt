from sys import stdin

n = int(stdin.readline())

s, t = stdin.readline().strip(), stdin.readline().strip()

if s == t:
    print(0)
else:
    ss, tt = s + s, t + t
    if ss.find(t) != -1:
        print(1)
    elif tt.find(s) != -1:
        print(2)
    else:
        print(-1)
