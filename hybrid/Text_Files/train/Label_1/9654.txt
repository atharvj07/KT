#include <algorithm>
#include <iostream>

using namespace std;

int main() {
  typedef long long ll;
  ll n, q, x;
  cin >> n >> q;
  ll last = n;
  ll cnt = 0;
  while (cin >> x) {
    if (x % 2 == 0) {
      cout << x / 2 << endl;
      cnt++;
    } else {
      cout << (x + 1) / 2 + cnt << endl;
    }
  }
}
