#include <bits/stdc++.h>

using namespace std;

int main() {
  int n; cin >> n;
  string s; cin >> s;
  bool f = 0;
  for (int i = 1; i < n; i += 2) if (s[i] != '0') f = 1;
  if (!f) return cout << "NO", 0;
  cout << "YES";
}
