import sys

n = int(input())
a = list(map(int, input().split()))
ans = []
for i in range(n):
    for j in range(n):
        if i != j and a[i] > a[j]:
            a[i], a[j] = a[j], a[i]
            ans.append((i, j))
sys.stdout.write("{}\n".format(len(ans)))
for swap in ans:
    print(*swap)
