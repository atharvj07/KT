import queue
n=int(input())
t=sorted(map(int,input().split()))
q=queue.PriorityQueue()
ans=0
for x in t:
    if q.empty() or q.queue[0]<=x:
        q.put(x+x)
        ans+=1
    else:
        q.get()
        q.put(x+x)
print(ans)
