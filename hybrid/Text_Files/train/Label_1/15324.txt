#include<bits/stdc++.h>
using namespace std;
char s[100005];
int main(){
	scanf("%s",s);
	int a=0, b=0, ans=0;
	for(int i=0; s[i]; i++){
		if(s[i]=='B') ++b;
		else if(s[i]=='a') ++a;
		else if(b && s[i]) --b, ++ans;
	}
	printf("%d",ans);
	return 0;
}
