#include <iostream>
using namespace std;

int main() {
    int n, ans = 0, last = 1;
    cin >> n;
    for (int i = 1; i <= n; i++) {
        int a;
        cin >> a;
        ans += abs(last - a);
        last = a;
    }
    cout << ans << endl;
    return 0;
}
