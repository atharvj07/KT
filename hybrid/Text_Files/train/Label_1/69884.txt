import bisect
n,f=map(int,input().split())
a=[list(map(int,input().split())) for i in range(n)]
a.sort(key=lambda x:x[1]/x[0],reverse=True)
ans=sum(x[0] for x in a[:f])
for x in a[f:]:
    if x[0]>x[1]:
        ans+=x[1]
    else:
        ans+=x[0]
print(ans)
