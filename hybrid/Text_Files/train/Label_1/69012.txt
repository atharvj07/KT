n, r = map(int, input().split())
a = list(map(int, input().split()))
ans = [0] * (len(a) + 1)
for i in range(n):
    if not a[i]:
        ans[i + 1] = ans[i] + 1
    else:
        ans[i + 1] = ans[i]
    ans[min(i + r, n)] = min(ans[min(i + r, n)], ans[i])
print(-1 if ans[n] else ans[n])
