import sys
input = sys.stdin.readline

n = int(input())
d = {}
for _ in range(n):
    x, y = map(int, input().split())
    if x in d:
        d[x].add(y)
    else:
        d[x] = set([y])
print('YES' if len(d) <= 2 else 'NO')
