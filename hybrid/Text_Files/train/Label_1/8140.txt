#include <bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  char c = 'a';
  for (char x : s) {
    if (x >= c) { c = x + 1; }
  }
  if (c == 'z' + 1)
    cout << s;
  else
    cout << -1;
  return 0;
}
