n = int(input())
a = list(map(int, input().split()))
ans = [0] * n
for i in range(n - 1, -1, -1):
    for j in range(i + a[i], n):
        ans[j] += a[i]
        a[j] -= a[i]
    ans[i] += a[i]
print(*ans)
