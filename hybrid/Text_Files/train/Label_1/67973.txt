n, h = map(int, input().split())

# Area of the whole triangle
area = 0.5 * 1 * h

# Area of each piece
sub_area = area / n

# Height of each piece
sub_height = (sub_area * 2 / 1) ** 0.5

# Cuts
cuts = []
for i in range(1, n):
    cuts.append(sub_height * i)

print(*cuts)
