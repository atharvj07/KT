#include <cmath>
#include <iostream>
using namespace std;
int main() {
  int k;
  cin >> k;
  int n = pow(2, k);
  for (int i = 0; i < n; i++) {
    int x = i;
    for (int j = 0; j < n; j++) {
      cout << (x % 2 ? "*" : "+");
      x >>= 1;
    }
    cout << endl;
  }
}
