#include <bits/stdc++.h>
using namespace std;
int main() {
  long long k, a, b, ans = 0;
  cin >> k >> a >> b;
  if ((a + b) % k != 0)
    cout << -1 << endl;
  else
    cout << (a + b) / k << endl;
}
