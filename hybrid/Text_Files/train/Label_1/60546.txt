def sum_of_digits(x):
    return sum(int(digit) for digit in str(x))

def find_max_sum(n):
    a = '9' * (len(str(n)) - 1)
    b = n - int(a)
    return sum_of_digits(a) + sum_of_digits(b)

n = int(input())
print(find_max_sum(n))
