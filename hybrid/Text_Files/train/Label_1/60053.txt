n, m = map(int, input().split())
ans = []
for t in range((m + 1) // 2):
    L = 2 * n - t * 2
    if t < m // 2:
        for i in range(t * 2 + 1, t * 2 + L + 1):
            ans.append(i)
    else:
        for i in range(t * 2 + L, t * 2, -1):
            ans.append(i)
print(*ans)
