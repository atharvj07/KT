#include <map>
#include <set>
#include <vector>
#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    multiset<int> x, y;
    int p;
    cin >> p;
    int t;
    while (p--)
    {
        cin >> t;
        x.insert(t);
    }
    int q;
    cin >> q;
    while (q--)
    {
        cin >> t;
        y.insert(t);
    }
    if (x.size() + y.size() >= n)
        cout << "I become the guy." << endl;
    else
        cout << "Oh, my keyboard!" << endl;
    return 0;
}
