#include <iostream>
using namespace std;
int main() {
	int a[3], b[3];
	int n;
	cin >> a[0] >> a[1] >> a[2];
	cin >> b[0] >> b[1] >> b[2];
	cin >> n;
	if ((a[0] + a[1] + a[2] > n * 5) || (b[0] + b[1] + b[2] > n * 10))
		cout << "NO";
	else
		cout << "YES";
	return 0;
}
