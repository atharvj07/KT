#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long l, r;
    std::cin >> l >> r;
    if (l * 2 > r)
      std::cout << -1 << " " << -1 << std::endl;
    else
      std::cout << l << " " << l * 2 << std::endl;
  }
  return 0;
}
