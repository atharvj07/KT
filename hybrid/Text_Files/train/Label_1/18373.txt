#include <iostream>
using namespace std;

int main() {
    int xp, yp, xv, yv;
    cin >> xp >> yp >> xv >> yv;
    cout << ((yp == yv && xp < xv) || (xp == xv && yp > yv) ? "Polycarp" : "Vasiliy");
    return 0;
}
