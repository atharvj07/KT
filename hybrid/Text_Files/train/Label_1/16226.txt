#include <bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int a, b, x, y;
        cin >> a >> b >> x >> y;
        cout << max({a * b - a - b, x * b, (a - x - 1) * b, a * y, a * (b - 1 - y)}) << '\n';
    }
    return 0;
}
