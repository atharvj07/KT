#include <iostream>
#include <climits>
using namespace std;
int main() {
  int n, s, x, y, mx = INT_MIN;
  cin >> n >> s;
  for (int i = 0; i < n; i++) {
    cin >> x >> y;
    if (s >= x) mx = max(mx, s - x + y);
  }
  (mx == INT_MIN) ? cout << -1 : cout << mx;
}
