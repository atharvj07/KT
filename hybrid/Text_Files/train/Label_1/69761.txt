import sys

for _ in range(int(input())):
    n = int(sys.stdin.readline())
    a = list(map(int, sys.stdin.readline().split()))
    d = []
    for x in a:
        if not d or d[-1] < x: d.append(x)
        else: d.insert(d.index(next((y for y in d if y >= x), d[-1])), x)
    print(*d)
