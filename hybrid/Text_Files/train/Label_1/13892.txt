#include <bits/stdc++.h>
using namespace std;
long long f(long long x, long long y) { return (x | y) - y; }
int main() {
  int n;
  cin >> n;
  vector<long long> a(n);
  for (auto &i : a) cin >> i;
  sort(a.rbegin(), a.rend());
  for (auto i : a) cout << i << " ";
}
