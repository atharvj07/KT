#include <iostream>

using namespace std;

int main() {
  int n, cnt = 0, cur = 0;
  cin >> n;
  while (n--) {
    int a;
    cin >> a;
    if (!a) cur = 0;
    else if (cur) cnt++;
    else cur = 1;
  }
  cout << cnt;
  return 0;
}
