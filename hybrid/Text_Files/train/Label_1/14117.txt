#include <iostream>
#include <algorithm>
using namespace std;
int a[101];
int main() {
  int n;
  cin >> n;
  for (int i = 0; i < n; i++) cin >> a[i];
  sort(a, a + n);
  for (int i = 0; i < n / 2; i++) cout << a[i] << " " << a[n - 1 - i] << "\n";
  return 0;
}
