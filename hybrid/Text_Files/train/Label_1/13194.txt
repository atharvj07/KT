#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    string s; cin >> s;
    int c = 0, las = -1;
    for (int i = 0; i < n; i++) {
        if (s[i] != s[las]) c++, las = i;
    }
    cout << n - c << endl;
    return 0;
}
