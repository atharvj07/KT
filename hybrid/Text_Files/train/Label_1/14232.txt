#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m, r, c, ans = 1, arr[1005], brr[1005];
  cin >> n >> m;
  for (int i = 0; i < n; i++) { cin >> r; }
  for (int i = 0; i < m; i++) { cin >> c; }
  for (int i = 0; i < m; i++) {
    if (c == 0) ans *= 2;
  }
  ans %= 1000000007;
  cout << ans << endl;
  return 0;
}
