for _ in range(int(input())):
    n, m = map(int, input().split())
    a = [*map(int, input().split())]
    b = [*map(int, input().split())]
    ca, cb = n - len(set(a)), m - len(set(b))
    print(min(ca, cb) + (ca > cb))
