#include<bits/stdc++.h>
using namespace std;
const int mod=1e9+7;
#define ll long long
const int N=1e5+10;
ll fact[N];
bool solve(ll n,ll m){
    if(n==1 || m==1) return 1;
    return (n==2 && m==2);
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    ll t;cin>>t;
    while(t--){
        ll n,m;cin>>n>>m;
        solve(n,m)?cout<<"YES\n":cout<<"NO\n";
    }
    
    return 0;
    
}
