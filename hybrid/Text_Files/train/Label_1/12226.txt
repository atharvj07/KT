#include <iostream>
#include <string>

int main() {
  std::string line;
  std::cin >> line;
  int cnt = 0;
  for (int i = 0; i < line.size() - 1; i++) {
    if (line[i] == 'M' && line[i + 1] == 'F') cnt++;
  }
  std::cout << cnt << std::endl;
  return 0;
}
