import sys

for _ in range(int(input())):
    input()
    arr = list(map(int, (input().split())))
    ans = 0
    for x in arr:
        ans = ((x + 2) // 3) + ans
    sys.stdout.write(str(ans) + '\n')
