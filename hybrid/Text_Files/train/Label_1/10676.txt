#include <bits/stdc++.h>
using namespace std;
#define int long long
int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    cin >> n >> m;
    int a[n + 1];
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    while (m--) {
        int l, r;
        cin >> l >> r;
        if (is_sorted(a + l, a + r + 1))
            cout << "Yes\n";
        else
            cout << "No\n";
    }
}
