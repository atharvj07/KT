#include <iostream>
#include <vector>

using namespace std;

int main() {
  int k, a, b, v;
  cin >> k >> a >> b >> v;
  int ans = max((a + v - 1) / v, (a + k - 1) / k);
  ans = min(ans, (b + 1) / 2);
  cout << ans << endl;
  return 0;
}
