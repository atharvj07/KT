#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &x : a) { cin >> x; }
  int mx = 0;
  int j = 0;
  for (int i = 0; i < n; i++) {
    if (j < n && a[j] <= a[i] * 2) {
      j++;
    }
    mx = max(mx, j);
  }
  cout << mx << endl;
}
