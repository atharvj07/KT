import sys
input = sys.stdin.readline

n = int(input())
a = list(map(int, input().split()))

k = 0
for i in range(n):
    for j in range(i + 1, n):
        k = max(k, (min(a[i], a[j]) - 1) // (abs(i - j)))

print(k)
