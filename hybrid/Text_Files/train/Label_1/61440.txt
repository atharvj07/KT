n, m = map(int, input().split())
p = [int(x) for x in input().split()]
ans = 0
for i in range(n):
    for j in range(n):
        if i <= j:
            lst = p[i:j+1]
            lst.sort()
            mid = (len(lst) + 1) // 2 - 1
            if lst[mid] == m:
                ans += 1
print(ans)
