#include <iostream>
using namespace std;
int main() {
    int s, v1, v2, t1, t2;
    cin >> s >> v1 >> v2 >> t1 >> t2;
    cout << (v1 * s + t1 <= v2 * s + t2 ? "First" : v1 * s + t1 > v2 * s + t2 ? "Second" : "Friendship");
}
