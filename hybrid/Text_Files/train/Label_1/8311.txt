#include <iostream>

using namespace std;

int main() {
    int x, y, z, a, b, c;
    cin >> x >> y >> z >> a >> b >> c;
    cout << ((a >= x && (a + b) >= x + y && (a + b + c) >= x + y + z) ? "YES" : "NO")
         << endl;
    return 0;
}
