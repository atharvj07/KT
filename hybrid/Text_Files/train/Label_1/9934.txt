#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    cout << max({e * min({a, d}), f * min({b, c, d}), e * (a - min({a, d})) + f * (min({b, c, d}))});
    return 0;
}
