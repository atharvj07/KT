#include <iostream>

using namespace std;

int main() {
    int n, t, k, d;
    cin >> n >> t >> k >> d;
    if (((t + d) * (n / k) <= t * ((n + k - 1) / k)) || (n % k && (t + d + t) < t * ((n / k) + 1)))
        cout << "YES";
    else
        cout << "NO";
    return 0;
}
