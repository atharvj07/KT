#include <iostream>
#include <string>

using namespace std;

int main() {
    int n;
    cin >> n;
    string s;
    cin >> s;
    string t;
    while (n--) {
        int m = n / 2;
        t += s[m];
        s.erase(s.begin() + m);
    }
    cout << t << endl;
    return 0;
}
