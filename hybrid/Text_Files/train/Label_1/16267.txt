#include <bits/stdc++.h>

using namespace std;

int main() {
  int y, b, r;
  cin >> y >> b >> r;

  int d1 = b - y - 1;
  int d2 = r - b - 1;

  int mx = min({y, b - d1, r - d1 - d2});

  cout << 3 * mx << endl;

  return 0;
}
