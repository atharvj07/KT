import sys
input = sys.stdin.readline

n = int(input())
points = [list(map(int, input().split())) for _ in range(n)]
can = False
for x, y in points:
    for x_, y_ in points:
        if y - y_ != 0 and (x - x_) / (y - y_) * (y - y_) == x - x_:
            can = True
print(['No', 'Yes'][can])
