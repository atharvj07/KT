#include <cstdio>
int main() {
  int n, a[202], ans = 0;
  scanf("%d", &n);
  for (int i = 1; i <= 2 * n; ++i) scanf("%d", &a[i]);
  for (int i = 1; i <= 2 * n; ++i)
    if (a[i] != a[i - 1] && a[i] != a[i + 1]) ++ans;
  printf("%d\n", ans / 2);
}
