#include <iostream>

int main() {
    int q;
    std::cin >> q;
    while (q--) {
        long long n, a, b;
        std::cin >> n >> a >> b;
        std::cout << (n + 1) / 2 * std::min(a, b) << '\n';
    }
}
