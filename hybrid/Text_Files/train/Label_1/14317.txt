#include <iostream>
using namespace std;
const int maxn = 2e3 + 1;
int dp[maxn];

int main() {
    int n;
    cin >> n;
    dp[0] = 1;
    for (int i = 1; i <= n; i++) {
        dp[i] = dp[i - 1] + i;
    }
    cout << dp[n] << endl;
    return 0;
}
