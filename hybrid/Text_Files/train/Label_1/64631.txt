import math

t = int(input())
for i in range(t):
    x = int(input())
    if x < 1 or x % 2 == 1 or x >= 4 * 10 ** 9:
        print(-1)
        continue
    n = math.ceil(math.sqrt(x / 2))
    print(n, n // 2 if n % 2 == 0 else n // 2 + 1)

