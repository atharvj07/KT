#include<iostream>
using namespace std;
int main() {
    string a;
    int m = 0, f = 0;
    while (cin >> a) {
        for (char c : a){
            if (c == 'M') m++;
            else f++;
        } 
    }
    cout << (m ? (m - 1) : 0) << endl;
}
