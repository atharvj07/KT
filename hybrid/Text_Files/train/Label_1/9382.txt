#include<bits/stdc++.h>
using namespace std;
#define ll long long
ll n, w = 1, ans = 1;
int main() {
  cin >> n;
  while (n > 0) {
    if (n >= w) {
      n -= w;
      w++;
      ans++;
    } else {
      w = n;
      ans++;
      break;
    }
  }
  cout << ans << endl;
  return 0;
}
