#include <iostream>
#include <string>
using namespace std;
int main() {
  string a, b, t;
  cin >> a >> b >> t;
  for (char &c : t) {
    if (islower(c)) {
      for (int i = 0; i < 26; i++)
        if (a[i] == c) { c = b[i]; break; }
    }
  }
  cout << t;
}
