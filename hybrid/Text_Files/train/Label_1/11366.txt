#include<bits/stdc++.h>
#define int long long
using namespace std;
const int mod=1e6+3;
int pm(int x,int y){
    int ret=1;
    for(;y;y>>=1){
        if(y&1) ret=ret*x%mod;
        x=x*x%mod;
    }
    return ret;
}
#define ull unsigned long long
int32_t main(){
    int n,c;cin>>n>>c;
    int num=n+(c-1)*n;
    int den=n+c-1;
    cout<<(num*pm(den,mod-2))%mod;
    return 0;
}
