#include <iostream>
using namespace std;
int main() {
  string s;
  cin >> s;
  int ans = 0, now = 0;
  for (char c : s) {
    int next = c - 'a';
    ans += min(abs(next - now), 26 - abs(next - now));
    now = next;
  }
  cout << ans << endl;
  return 0;
}
