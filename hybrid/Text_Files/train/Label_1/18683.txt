#include <bits/stdc++.h>
using namespace std;
#define int long long
int32_t main() {
    int n;
    cin >> n;
    vector<int> b(n);
    for (int i = 0; i < n; i++) cin >> b[i];
    int ans = 0;
    for (int i = 1; i < n; i++) ans += abs(b[i] - b[i - 1]);
    cout << ans << '\n';
    return 0;
}
