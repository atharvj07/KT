import math
a, b = map(int, input().split())
n = int(input())
ans = 1e9
for i in range(n):
    x, y, v = map(int, input().split())
    dist = math.sqrt((a - x) ** 2 + (b - y) ** 2)
    ans = min(ans, dist / v)
print(ans)
