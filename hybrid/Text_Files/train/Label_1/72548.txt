import sys

for _ in range(int(input())):
    n = int(input())
    a = [int(x) - 1 for x in input().split()]
    cnt = [0] * n
    for x in a:
        cnt[x] ^= 1
    if cnt[0] > 1 or sum(cnt) > 2:
        print("YES")
    else:
        print("NO")
