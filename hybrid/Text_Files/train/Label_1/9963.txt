#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, f, k;
    cin >> a >> b >> f >> k;
    if (f > b)
        return cout << "-1", 0;
    cout << (k + f - 1) / f;
    return 0;
}
