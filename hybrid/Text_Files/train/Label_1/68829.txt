n = int(input())
current_day = 0
for _ in range(n):
    s, d = map(int, input().split())
    if current_day < s:
        current_day = s
    else:
        current_day = ((current_day - s) // d + 1) * d + s
print(current_day)
