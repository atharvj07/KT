#include <cstdio>

int main() {
  int t;
  scanf("%d", &t);
  while (t--) {
    int n, m, k;
    scanf("%d%d%d", &n, &m, &k);
    bool ok = k % 2 == 0 && (n - 1 + m - 1 - k) % 2 == 0 && (n - 1) % 2 == 0 && (m - 1) % 2 == 0;
    printf(ok ? "YES\n" : "NO\n");
  }
  return 0;
}
