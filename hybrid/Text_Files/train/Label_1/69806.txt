n,m=map(int,input().split())
a=[set() for i in range(n)]
for i in range(m):
 b=[int(x) for x in input().split()]
 for j in b[1:]:
  if j>0: a[j-1].add(i)
  else: a[-j-1].add(i)
print("YES" if any(all(map(lambda x:x in s,range(n)))for s in a)else"NO")
