#include<bits/stdc++.h>
using namespace std;
int main() {
    int a, b, p = 0;
    cin >> a >> b;
    for (int i = 1; i <= 100; i++) {
        if (a % i == 0 && b % i == 0) p = i;
    }
    cout << p;
    return 0;
}
