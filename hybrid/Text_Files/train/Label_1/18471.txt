#include <bits/stdc++.h>
using namespace std;
int main() {
    string s;
    cin >> s;
    int ans = 0;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == 'w' || (s[i] == 'v' && (i == 0 || s[i - 1] == 'v'))) ans++;
    }
    cout << ans << endl;
    return 0;
}
