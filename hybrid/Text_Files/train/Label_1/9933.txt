#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    int ans = 0;
    ans += min({a, d}) * e;
    d -= ans / e;
    ans += min({b, c, d}) * f;
    cout << ans << endl;
    return 0;
}
