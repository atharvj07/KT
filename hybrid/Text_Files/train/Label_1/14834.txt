#include <iostream>
int main(int argc, char* argv[]) {
  std::string s;
  std::cin >> s;
  int n = s.size();
  int ans = 0;
  for (int i = 0; i < n - 1; i++) {
    if (s[i] == s[i + 1]) ans++;
  }
  std::cout << ans << std::endl;
  return 0;
}
