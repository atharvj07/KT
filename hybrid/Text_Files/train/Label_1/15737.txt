#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, m;
    cin >> n >> m;
    if (n == 1)
      cout << "0\n";
    else if (m % n == 0)
      cout << m / n - 1 << "\n";
    else
      cout << m / n << "\n";
  }
}
