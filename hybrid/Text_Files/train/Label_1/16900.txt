#include<iostream>
using namespace std;
int main()
{
    int c, v0, v1, a, l, DaysRequired = 1;
    cin>>c>>v0>>v1>>a>>l;
    while(v0 + (DaysRequired-1)*a + l < c)
        DaysRequired++;
    cout<<DaysRequired<<endl;
    return 0;
}
