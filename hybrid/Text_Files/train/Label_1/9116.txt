#include <iostream>
#include <vector>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int x, y, a, b;
    cin >> x >> y >> a >> b;
    cout << llabs(x - y) / 2 * b + llabs(x - y) % 2 * a << endl;
  }
  return 0;
}
