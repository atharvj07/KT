#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;
  vector<int> a;
  for (int i = 0; i < n; i++) {
    if (s[i] == '0') a.push_back(i);
  }
  int ans = a[(n - 1 - k) / 2];
  cout << ans << endl;
  return 0;
}
