#include <iostream>

using namespace std;

int main() {
  int x, y, z, t1, t2, t3;
  cin >> x >> y >> z >> t1 >> t2 >> t3;
  if (z != x && z != y && t2 + t3 + abs(z - x) * t2 <= abs(y - x) * t1)
    cout << "YES" << endl;
  else
    cout << "NO" << endl;
  return 0;
}
