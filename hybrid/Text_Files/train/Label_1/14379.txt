#include <iostream>

using namespace std;

int main() {
  long long x, y, z;
  cin >> x >> y >> z;
  long long a = x / z, b = y / z;
  long long g = min(a, b), ans = a + b;
  cout << ans - g << " " << g << endl;
  return 0;
}
