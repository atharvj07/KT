n, q = map(int, input().split())
s = input()
a = [""] * 3
for i in range(q):
    typ, *x = input().split()
    if typ == '+':
        a[int(x[0]) - 1] += x[1]
    else:
        a[int(x[0]) - 1] = a[int(x[0]) - 1][:-1]
    print(["NO", "YES"][all(len(set(el) & set(s)) == len(el) for el in a)])
