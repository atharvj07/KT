for i in range(int(input())):
    keyboard = input()
    word = input()
    last = keyboard.index(word[0])
    time = 0
    for char in word[1:]:
        index = keyboard.index(char)
        time += abs(index - last)
        last = index
    print(time)
