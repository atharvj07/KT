#include <iostream>
int main() {
  long long int t, x, n;
  std::cin >> t;
  while (t--) {
    std::cin >> x >> n;
    std::cout << (x + ((n * (n + 1) / 2) % 2 == x % 2 ? n * (n + 1) / 2 : -(n * (n + 1) / 2)))
              << '\n';
  }
  return 0;
}
