#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> p(n);
  for (int i = 0; i < n; i++) { cin >> p[i]; }
  int l = 0, r = 0;
  for (int i = 0; i < n; i++) {
    if (p[i] == i + 1) { r++; }
  }
  int ans = n - r;
  cout << ans << endl;
  return 0;
}
