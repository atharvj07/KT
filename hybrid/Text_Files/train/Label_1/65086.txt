import datetime

a, ta = map(int, input().split())
b, tb = map(int, input().split())
t = datetime.datetime.strptime(input(), '%H:%M')

cnt = ((t.hour * 60 + t.minute) // ta + 1) * b // tb
print(cnt - 1)
