#include <iostream>
using namespace std;
int main() {
    int x;
    cin >> x;
    if (x <= 5)
        cout << 1 << endl;
    else
        cout << (x / 5) + (x % 5 > 0 ? 1 : 0) << endl;
    return 0;
}
