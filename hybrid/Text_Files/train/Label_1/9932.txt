#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main() {
    long long a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    cout << max(e * min(a, d), f * min(min(b, c), d)) << endl;
}
