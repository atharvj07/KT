import sys
input = sys.stdin.readline

n = int(input())
arr = list(map(int, input().split()))
a = sorted(arr[:n], key=lambda x: (x, arr[n:].count(x)))
b = sorted(arr[n:], key=lambda x: (-x, arr[:n].count(x)))
ans = 0
for i in range(n):
    ans += abs(a[i] - b[i])
print(ans % 998244353)
