from sys import *
setin = lambda x: stdin.readline()[:-1]
inp = lambda x: int(stdin.readline())
n = inp(0)
a = list(map(int, setin(1).split()))
ans = []
for i in range (n - 1):
    if a[i] > 1 and a[i + 1] > 1:
        a[i + 1] = 1
        ans += [1]
print (len(ans))
print (*a, *ans)
