#include <iostream>
#include <vector>

using namespace std;

int main() {
  int a[4];
  for (int i = 0; i < 4; i++) cin >> a[i];
  string s;
  cin >> s;
  int ans = 0;
  for (char c : s) ans += a[c - '1'];
  cout << ans;
  return 0;
}
