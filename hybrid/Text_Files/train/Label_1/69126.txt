import sys
input = sys.stdin.readline

def trim(x):
    return max(min(x, n), 1)

n, m = map(int, input().split())
cost = 0
for x1, y1, x2, y2 in (map(int, input().split()) for _ in range(m)):
    cost += min(trim(x2) - trim(x1), trim(y2) - trim(y1))
print(cost)
