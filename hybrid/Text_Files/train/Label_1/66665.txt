n = int(input().strip())
b = list(map(int, input().strip().split()))

max_b = max(b)
min_b = min(b)
max_diff = max_b - min_b

max_count = b.count(max_b)
min_count = b.count(min_b)

if max_b == min_b:
    ways = (n * (n - 1)) // 2
else:
    ways = max_count * min_count

print(max_diff, ways)
