import math
m = int(input())
primes = list(map(int, input().split()))
mod = int(1e9) + 7
ans = 1

for p in primes:
    cnt = 0
    while m % p == 0:
        cnt += 1
        m //= p
    ans = (ans * (pow(p, cnt, mod) + pow(p, cnt + 1, mod) // p) % mod) % mod

print(ans)
