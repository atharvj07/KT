n, h, l, r = map(int,input().split())
a = [int(x) for x in input().split()]
dp = [0]*(r+1)
for x in a:
    if x<=l:
        dp[x]+=1
    for y in range(max(l,x-h),x):
        dp[x] = max(dp[x],dp[y]+1)
print(max(dp))
