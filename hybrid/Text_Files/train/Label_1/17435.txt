#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, ans = 0;
  cin >> n;
  vector<int> a(n);
  map<int, int> m;
  for (int &x : a) cin >> x, m[x]++;
  for (auto &[x, y] : m) if (y > 1) { ans += y - 1; }
  cout << ans;
  return 0;
}
