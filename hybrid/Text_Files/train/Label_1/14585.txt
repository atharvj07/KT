#include <iostream>
#include <set>
int main() {
    int n; std::cin >> n;
    std::set<std::string> names;
    std::string name;
    while (n--) {
        std::cin >> name;
        std::cout << (names.find(name) != names.end() ? "YES" : "NO") << '\n';
        names.insert(name);
    }
}
