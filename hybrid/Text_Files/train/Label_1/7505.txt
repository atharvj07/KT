#include<bits/stdc++.h>
using namespace std;
int main() {
  int k;
  cin >> k;
  if (k>45) cout << -1;
  else {
    int n = 1000000000000000000ll;
    while (to_string(n).size() < 18 || ((int)to_string(n).size()-to_string(n).size()/2)<k) n++;
    cout << n << "\n";
  }
  return 0;
}
