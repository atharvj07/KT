#include <cstdio>

int main() {
    int n, m, k;
    scanf("%d %d %d", &n, &m, &k);
    long long ans = n;
    ans = ans * m;
    ans = ans % 1000000007;
    if (k == -1) ans = (ans + 1) % 1000000007;
    printf("%lld\n", ans);
    return 0;
}
