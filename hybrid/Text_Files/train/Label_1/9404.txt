#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int b, p, f;
        cin >> b >> p >> f;
        int h, c;
        cin >> h >> c;
        cout << (min(b / 2, min(p, b - (b / 2))) * h + min(b / 2, min(f, b - (b / 2))) * c) << endl;
    }
}
