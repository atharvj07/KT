s,b=map(int,input().split())
a=list(map(int,input().split()))
d=[]
g=[]
for i in range(b):
    x,y=map(int,input().split())
    d.append(x)
    g.append(y)
d.sort()
g.sort()
for x in a:
    k=b-1
    while k>=0 and d[k]>x:
        k-=1
    if k<0:
        print(0)
    else:
        print(g[k])
