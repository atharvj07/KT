#include <cstdio>
#include <cstdlib>
#include <cstring>
#define ll long long
#define int ll
const int N=110000,mod=1e9+7,INF=1<<30;

char s[N];
int32_t n,a[26],ans;
signed main(){
    scanf("%lld",a);
    scanf("%s",s+1);
    for(int i=2;i<=strlen(s+1);++i)
        if(s[i]==s[i-1]&&a[s[i]-97])
            ++ans;
    printf("%lld",ans);
    return 0;
}
