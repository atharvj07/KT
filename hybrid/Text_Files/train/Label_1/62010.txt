n = int(input())
a = list(map(int, input().split()))

sums = {0}
current_sum = 0
inserts = 0

for num in a:
    current_sum += num
    if current_sum in sums:
        inserts += 1
        sums = {current_sum}
    else:
        sums.add(current_sum)

print(inserts)
