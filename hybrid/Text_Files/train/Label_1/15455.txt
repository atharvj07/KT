#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int str, intl, exp;
        cin >> str >> intl >> exp;
        if (exp >= 1 + intl - str)
            cout << exp - intl + 1 << endl;
        else
            cout << 0 << endl;
    }
}
