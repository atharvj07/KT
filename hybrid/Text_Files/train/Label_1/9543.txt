#include <iostream>

using namespace std;

int main() {
  int a, b, c;
  cin >> a >> b >> c;
  cout << ((c % a == 0 || c % b == 0 || (c % a > 0 && c % b > 0 && (c - a) % b == 0 || (c - b) % a == 0)) ? "Yes" : "No");
  return 0;
}
