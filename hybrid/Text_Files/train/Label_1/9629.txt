#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, k;
    cin >> n >> k;
    string t;
    cin >> t;
    string s;
    for (int i = 0; i < k / 2; i++) { s += t; }
    if (k % 2) s += t.substr(0, n);
    cout << s;
}
