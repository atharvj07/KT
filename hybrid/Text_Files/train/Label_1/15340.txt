#include <iostream>
#include <vector>
#include <cmath>
using namespace std;
int gcd(long long a, long long b) {
    return b ? gcd(b, a % b) : a;
}
int main() {
    long long a, b; cin >> a >> b;
    while (b -= a) a = gcd(a, b);
    cout << a << "\n";
}
