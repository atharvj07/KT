#include <iostream>

int main() {
  int n, k;
  std::cin >> n >> k;
  int id[n], ind = 0;
  for (int &i : id) std::cin >> i;
  while (k > ind && ind < n) {
    ind++;
    k -= ind;
  }
  std::cout << id[ind] << "\n";
  return 0;
}
