#include <iostream>
#include <vector>
#define ll long long
using namespace std;
int main() {
    ll n, a;
    cin >> n;
    vector<int> v(n);
    for (ll i = 0; i < n; i++) {
        cin >> v[i];
    }
    if (n == 1) {
        cout << "NO" << endl;
    } else if (v[1] == v[0]) {
        cout << "NO" << endl;
    } else {
        cout << "YES" << endl;
    }
    return 0;
}
