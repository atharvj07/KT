for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    score = 0
    pos = 0
    for i in range(n):
        score += a[pos]
        pos = (pos + a[pos]) % n
    print(score)
