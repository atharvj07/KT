#include <algorithm>
#include <cmath>
#include <iostream>

int solve() {
  long long n, m, a, b;
  std::cin >> n >> m >> a >> b;
  long long r = n % m;
  std::cout << std::min(r * a, (m - r) * b) << '\n';
}

int main() { solve(); }
