#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,a[105],count=0,sum=0;
    cin>>n;
    for(int i=0;i<n;i++)cin>>a[i];
    sort(a,a+n);
    for(int i=0;i<n;i++){
        if(i+1<n && a[i]==a[i+1])i++;
        else count++;
    }
    cout<<(n/2-count)*(n/2-count+1)/2<<endl;
}
