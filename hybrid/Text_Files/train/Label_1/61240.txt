from collections import Counter

n = int(input())
a = Counter()
b = Counter()
for _ in range(n):
    x, y = map(int, input().split())
    a[x] += 1
    b[y] += 1
inter = sum(min(a[x], b[x]) for x in a)
ans = pow(2, n - inter, 1000000007)
print(ans)
