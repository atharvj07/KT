#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<bool> b(n);
  int ans = 0;
  for (int i = 0; i < n; i++) {
    int x;
    cin >> x;
    if (i == 0 or x > b[i - 1]) {
      ans = max(ans, i + 1);
    }
    b[i] = x;
  }
  cout << ans << endl;
}
