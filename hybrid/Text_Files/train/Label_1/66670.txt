import sys

n, *a = map(int, sys.stdin.read().split())
m, *b = a[n:]

sa = sb = i = 0
while i < m:
    if sa == sb:
        sa += a[i]
        sb += b[i]
        i += 1
    elif sa < sb:
        sa += a[i]
        i += 1
    else:
        sb += b[i]
        i += 1
print(i if sa == sb else -1)
