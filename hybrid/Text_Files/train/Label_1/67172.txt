import sys
n=int(input())
trips=[int(x) for x in sys.stdin.read().split()]
ans=[0]*n
t=0
one,ninety,day=[20,50,120]
for i in range(n):
    while t+89<trips[i]: t+=90; ans[i]+=ninety
    while t+1439<trips[i]: t+=1440; ans[i]+=day
    ans[i]+=one
    t=trips[i]
print(*ans)
