import sys
import re

t = int(sys.stdin.readline())
for _ in range(t):
    s = sys.stdin.readline().strip()
    if re.search(r'(\?+)\1', s):
        print(-1)
    else:
        print(re.sub(r'\?', lambda x: 'abc'[x.span()[1] % 3], s))
