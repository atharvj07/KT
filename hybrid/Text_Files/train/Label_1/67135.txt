l, r = map(int, input().split())
check = [0] * 10
for i in range(l, r + 1):
  flag = True
  for j in str(i):
    if check[int(j)]:
      flag = False
      break
    check[int(j)] = True
  if flag:
    print(i)
    exit(0)
print(-1)
