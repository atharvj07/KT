import sys

n, m = map(int, input().split())
a = list(map(int, input().split()))

ans = 0
for x in a:
    ans = max(ans, x % m)

for i in range(n):
    if a[i] % m == ans:
        a[i] += m - ans

print(ans)
print(*a)
