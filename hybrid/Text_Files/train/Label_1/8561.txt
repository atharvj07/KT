#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, s, ans = -1;
  cin >> n >> s;
  for (int i = 0, x, y; i < n; i++) {
    cin >> x >> y;
    if (x <= s) ans = max(ans, (s - x) / 100 * 100 + y);
  }
  cout << ans << "\n";
  return 0;
}
