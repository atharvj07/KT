#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, k, l;
    cin>>n>>k>>l;
    int a[n*k];
    for(int i=0;i<n*k;i++) {cin>>a[i];}
    sort(a,a+n*k);
    int mn = a[0], mx = a[n*k-1];
    if(mx-mn > l) cout<<0;
    else cout<<(mx-mn)*n;
    return 0;
}
