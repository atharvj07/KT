#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, cnt = 0;
    cin >> n;
    set<string> s;
    for (int i = 0; i < n; i++) {
        string a;
        cin >> a;
        sort(a.begin(), a.end());
        s.insert(a);
    }
    cout << s.size() << endl;
}
