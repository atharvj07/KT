#include <bits/stdc++.h>
using namespace std;
int n,k,a[150005];
int main(){
    cin>>n>>k;
    for(int i=1;i<=n;i++){
        cin>>a[i];
        a[i]+=a[i-1];
    }
    int ans=1e9;
    for(int i=k;i<=n;i++){
        ans=min(ans,a[i]-a[i-k]);
    }
    cout<<min_element(a+k,a+n+1)-a;
    return 0;
}
