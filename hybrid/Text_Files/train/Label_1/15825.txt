#include <iostream>
using namespace std;
int main() {
    int n, m, ans = 0;
    cin >> n >> m;
    ans += (n / 5) * m;
    ans += (m / 5) * n;
    if (n % 5 == 0 && m % 5 == 0)
        ans--;
    cout << ans << '\n';
    return 0;
}
