import sys, math
for line in sys.stdin.readlines()[1:]:
    a,b=map(int,line.split())
    c,d=int(int(math.log2(b))-int(math.log2(a))),int(math.log2(a)-int(math.log2(b)))
    if a==b: print(0)
    elif not b&1 and c<=3 and d<=3 and a<=b: print(c+d)
    else: print(-1)
