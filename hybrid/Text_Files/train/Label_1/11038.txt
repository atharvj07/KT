#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int n, a, b, c, d;
    std::cin >> n >> a >> b >> c >> d;
    if (n * (a - b) > c + d || n * (a + b) < c - d)
      std::cout << "No\n";
    else
      std::cout << "Yes\n";
  }
  return 0;
}
