#include <iostream>
#include <vector>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c;
    cin >> a >> b >> c;
    int diff = (abs(b - c) + 1) % 2;
    if (diff)
      cout << diff << '\n';
    else
      cout << 0 << '\n';
  }
  return 0;
}
