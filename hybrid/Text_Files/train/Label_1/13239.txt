#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long x;
    cin >> x;
    long long n = 1, m = 1;
    while (x >= n * n - n) {
      n++;
    }
    m++;
    cout << n << ' ' << m << endl;
  }
}
