#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, x, y;
    cin >> n >> x >> y;
    int d = y - x;
    vector<int> a(n, x + (n - 1) * d);
    cout << x << ' ';
    for (int i = 1; i + 1 < n; i++) { cout << x + i * d << ' '; }
    cout << y << endl;
  }
  return 0;
}
