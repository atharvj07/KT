import sys
input = sys.stdin.readline

n = int(input())
a = list(map(int, input().split()))
a.sort()

ans = 1
cnt = 1
for i in range(1, n):
    if a[i] == a[i - 1]:
        cnt += 1
    else:
        ans = max(ans, cnt)
        cnt = 1

ans = max(ans, cnt)
print(ans)
