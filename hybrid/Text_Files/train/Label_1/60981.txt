t = int(input())
for _ in range(t):
    a, b, x, y = map(int, input().split())
    options = [
        x * b,  # left
        (a - 1 - x) * b,  # right
        y * a,  # top
        (b - 1 - y) * a  # bottom
    ]
    print(max(options))
