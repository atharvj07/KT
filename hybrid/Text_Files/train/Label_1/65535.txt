for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    b = [0] * n
    for i in range(n - 1):
        b[i + a[i] % n] = max(b[i + a[i] % n], a[i] + b[i])
    print(max(b))
