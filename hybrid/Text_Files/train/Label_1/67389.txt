import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n, m, k = map(int, input().split())
    h = list(map(int, input().split()))
    if max(h) - min(h) <= k + m or h[n-1] == 0:
        print('YES')
    else:
        print('NO')
