from collections import Counter

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    b = list(map(int, input().split()))
    ca, cb = Counter(a), Counter(b)
    print(['NO', 'YES'][(ca[-1] - ca[0]) % 3 == (cb[-1] - cb[0]) % 3 and ca == cb])
