#include <bits/stdc++.h>
using namespace std;
const int N = 55;
int n, a[N];
int main() {
    cin >> n;
    int sum = 0;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
        sum += a[i];
    }
    if (sum % 2 == 0)
        puts("Bob");
    else
        puts("Alice");
}
