#include <iostream>
using namespace std;
int main() {
    long long n,m,k,l;
    cin >> n >> m >> k >> l;
    if (l > n - k) cout << -1 << endl;
    else cout << (m - 1) * (n - k) /m + n << endl;
    return 0;
}
