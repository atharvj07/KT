#include <iostream>
using namespace std;
int main() {
    int n, m;
    cin >> n >> m;
    int ans;
    if (n % 2 == 1)
        ans = -1;
    else if (n % m != 0)
        ans = n - n % m + m;
    else
        ans = n;
    cout << ans << endl;
    return 0;
}
