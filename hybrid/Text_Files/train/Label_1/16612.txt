#include <iostream>

using namespace std;

int main() {
  unsigned long long n, m;
  cin >> n >> m;

  unsigned long long day = 1;
  while (true) {
    if (n <= m * day - day * (day + 1) / 2) { break; }
    day++;
  }
  cout << day << endl;
}
