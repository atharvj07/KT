#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b, c, m, t = 0;
    cin >> a >> b >> c >> m;
    cout << ((m <= a + b + c - 3) ? "YES" : "NO") << endl;
  }
  return 0;
}
