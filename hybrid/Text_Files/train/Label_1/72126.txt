from math import gcd

n = int(input())
a = list(map(int, input().split()))

lcm_arr = [a[0]]

for i in range(1,n):
    lcm_arr.append(lcm_arr[-1]*a[i]//gcd(lcm_arr[-1],a[i]))

result = lcm_arr[0]

for i in lcm_arr[1:]:
    result = gcd(result,i)

print(result)
