#include <bits/stdc++.h>

using namespace std;

int main() {
  unsigned long long n, k;
  cin >> n >> k;
  if (k % 2 == 0 && k / 2 <= n) {
    cout << n - k / 2;
  } else {
    cout << 0;
  }
  return 0;
}
