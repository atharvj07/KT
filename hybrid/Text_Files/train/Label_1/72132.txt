n = input()
m = int(input())
pow = 1
rem = int(n[0]) % m
for i in n[1:]:
    pow *= 10
    pow %= m
    rem = (rem * 10 + int(i)) % m

gcd = [pow, m]
while gcd[1]:
    gcd = gcd[1], gcd[0] % gcd[1]
print((pow * rem) // gcd[0])
