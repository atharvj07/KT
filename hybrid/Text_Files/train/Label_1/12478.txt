#include<bits/stdc++.h>
using namespace std;
int main(){
	string s;
	cin >> s;
	int ans = 0, n = s.size();
	for (int i = 1; i < n; ++i)
		ans += min(abs(s[i] - s[i - 1]), 26 - abs(s[i] - s[i - 1]));
	cout << ans << endl;
	return 0;
}
