#include <bits/stdc++.h>
using namespace std;
#define i64 long long int

i64 func(string s) {
  int cnt = 0;
  for (auto c : s) if (c == '0') {
    cnt++;
  }
  return s.size() - cnt;
}
int main() {
  string n;
  while (cin >> n) {
    cout << func(n) << endl;
  }
  return 0;
}
