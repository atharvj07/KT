#include<iostream>
using namespace std;

long long t, l, r, d;

int main() {
    cin >> t;
    while (t--) {
        cin >> l >> r >> d;
        long long x = (l / d) * d;
        if (x < l) x += d;
        if (x <= r) x += d;
        cout << x << endl;
    }
    return 0;
}
