#include<bits/stdc++.h>
using namespace std;
char s[10010];
int main() {
	cin >> s;
	int n = strlen(s);
	set<string> st;
	for (int i = 5;i < n;++i) {
		st.insert(s + i - 2);
		st.insert(s + i - 3);
	}
	cout << st.size() << endl;
	for (auto i : st) {
		cout << i << endl;
	}
}
