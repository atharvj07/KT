#include <iostream>

int main() {
  int n, k;
  std::cin >> n >> k;
  k--;
  int last = 1, index = 0, count = 1;
  while (index + last <= k) {
    index += last;
    count++;
    last *= 2;
  }
  k -= index;
  if (k < count) std::cout << k + 1;
  else std::cout << count + (k == count);
}
