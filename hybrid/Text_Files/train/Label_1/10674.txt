#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m;
  cin >> n >> m;
  vector<long long> a(n);
  for (int i = 0; i < n; i++) cin >> a[i];
  while (m--) {
    int l, r;
    cin >> l >> r;
    if (a[l - 1] <= a[r - 1])
      cout << "Yes\n";
    else
      cout << "No\n";
  }
}
