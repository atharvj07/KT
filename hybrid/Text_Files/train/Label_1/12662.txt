#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        long long n, d;
        cin >> n >> d;
        cout << ((d <= n || d <= n / 2 + 1) ? "YES" : "NO") << '\n';
    }
}
