import sys

n, k = map(int, input().split())
f = [int(x) for x in input().split()]

f.sort()

ans = 0
pos = 1
for x in f:
    if x == pos: continue
    ans += abs(x - pos) + 1
    if pos == 1: ans += abs(x - pos)
    pos = x

print(ans)
