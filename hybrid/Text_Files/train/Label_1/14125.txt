#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int w, h, n;
    cin >> w >> h >> n;
    cout << (((w + h) % 2 == 0 && w * h / 4 >= n) ? "YES" : "NO") << endl;
  }
  return 0;
}
