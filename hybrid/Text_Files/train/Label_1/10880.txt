#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  cout << 1 << ' ';
  for (int i = 2; i <= n; ++i) {
    cout << __gcd(i, n) << ' ';
    n = __gcd(i, n);
  }
  cout << '\n';
  return 0;
}
