from sys import stdin
line = stdin.readline()
weights = []
for i in range(len(line)):
    if line[i] == '1':
        weights.append(i+1)
if len(weights) < 2:
    print("NO")
else:
    print("YES")
    if weights[0] == 1:
        weights = [10] + weights[1:]
    print(*weights[::2][:4])
