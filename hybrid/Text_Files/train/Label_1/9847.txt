#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, k, cur = 0, ans = -1;
  cin >> n >> k;
  while (n--) {
    int x;
    cin >> x;
    cur += x;
    if (cur >= k) {
      ans = n + 1;
      break;
    }
    cur -= min(cur, 8);
  }
  cout << ans << endl;
  return 0;
}
