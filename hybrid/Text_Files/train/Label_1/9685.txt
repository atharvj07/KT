#include <cstdio>
long long n, p, x = 0;
char c[10];
int main() {
  scanf("%lld%lld", &n, &p);
  while (n--) {
    scanf("%s", c);
    c[2] && (x += p / 2);
    c[5] && (x += p / 2 - 1);
  }
  printf("%lld\n", x);
  return 0;
}
