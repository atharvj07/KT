#include <bits/stdc++.h>

using namespace std;

int solve(int h, int n) {
  if (h == 1) return 2;
  return 2 * solve(h - 1, n / 2) + 1;
}

int main() {
  int h, n;
  cin >> h >> n;
  cout << solve(h, n) << endl;
  return 0;
}
