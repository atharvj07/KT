#include <iostream>
using namespace std;
int main() {
  long long k, a, b;
  cin >> k >> a >> b;
  if (a + b == 0 || (a + b - 1) % k != 0)
    cout << -1 << endl;
  else
    cout << (a + b) / k << endl;
  return 0;
}
