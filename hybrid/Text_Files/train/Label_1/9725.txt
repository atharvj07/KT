#include <iostream>

using namespace std;

int main() {
    string p;
    cin >> p;

    int ans = 0;
    for (int i = 0; i < p.size(); i++) {
        if (p[i] != '0') ans++;
    }
    cout << ans << endl;
    return 0;
}
