#include<bits/stdc++.h>
using namespace std;
int main(){
    int n; cin >> n;
    vector<int> a(n);
    for(int &x: a) cin >> x;
    int ans = n;
    for(int l=0, r=0; l<n; l=r){
        while(r<n and a[l]==a[r]) r++;
        ans-=r-l-1;
    }
    cout << ans << endl;
    return 0;
}
