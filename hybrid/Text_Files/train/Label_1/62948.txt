import sys;
n, m, k = map(int, input().split())
x = [int(x) for x in sys.stdin.read().split()]
x.sort()
k = int(x.pop(0))
if k == 1:
    print(0)
else:
    print((x[0] - 1 + x[-1] - x[0] + x[-1] - 3) // 2)
