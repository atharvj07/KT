from sys import stdin

# Read input
n = int(stdin.readline())
points = [tuple(int(x) for x in stdin.readline().split()) for _ in range(n)]

# Calculate min moves
hor = 101
ver = 101
for x, y in points:
    hor = min(hor, y)
    ver = min(ver, x)
    
print(min(hor, ver))
