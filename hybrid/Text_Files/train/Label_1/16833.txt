#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long a, b, c, d;
    cin >> a >> b >> c >> d;
    if (b == c)
      cout << a << " " << b << " " << d << "\n";
    else
      cout << a << " " << b << " " << c << "\n";
  }
  return 0;
}
