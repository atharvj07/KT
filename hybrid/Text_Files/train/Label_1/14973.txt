#include <iostream>

using namespace std;

int main() {
  int v1, v2, t, d;
  cin >> v1 >> v2 >> t >> d;
  int dv = v2 - v1;
  int dt = t - 1;
  cout << v1 * t + (dv + d) * dt * dt / 2;
  return 0;
}
