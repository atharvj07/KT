#include <iostream>

using namespace std;

const long long mod = 1e9 + 7;

int main() {
  int n, x, pos;
  cin >> n >> x >> pos;

  long long ans = n;
  ans *= (n - 1);
  ans %= mod;
  ans *= pos;
  ans %= mod;
  ans *= (n - pos - 1);
  ans %= mod;
  cout << ans << endl;

  return 0;
}
