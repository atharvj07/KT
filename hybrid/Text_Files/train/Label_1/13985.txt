#include <iostream>
using namespace std;
int main() {
  int t, n, m, k;
  cin >> t;
  while (t--) {
    cin >> n >> m >> k;
    cout << ((n + m - 1 <= k && (k - n - m + 1) % 2 == 0) ? "YES" : "NO")
         << '\n';
  }
}
