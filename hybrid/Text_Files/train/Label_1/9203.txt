#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
	ll n, a[200005];
	cin >> n;
	for(int i = 0; i < n; i++) { cin >> a[i]; }
	ll ans = 0;
	for(int i = 1; i <= n; i++) {
		if(a[i - 1] > i) { ans++; }
	}
	cout << ans << endl;
	return 0;
}
