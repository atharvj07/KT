#include <iostream>

using namespace std;

int main() {
  int b, k;
  cin >> b >> k;
  long long sum = 0;
  for (int i = 0; i < k; i++) {
    int a;
    cin >> a;
    sum = sum * b + a;
  }
  if (sum % 2 == 0) {
    cout << "even" << endl;
  } else {
    cout << "odd" << endl;
  }
  return 0;
}
