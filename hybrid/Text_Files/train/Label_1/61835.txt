from collections import Counter

s = input()
t = input()

cnts = Counter(s)
cntt = Counter(t)

ans = []

for c in t:
    if not cntt[c]: break
    cnts[c] -= 1
    ans.append(c)
    cntt[c] -= 1

for c in s:
    if not cnts[c]: break
    cnts[c] -= 1
    ans.append(c)

print("".join(ans))
