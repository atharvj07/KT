from itertools import combinations
n,l,r,x=map(int,input().split())
a=list(map(int,input().split()))
ans=0
for i in range(2,n+1):
    for c in combinations(a,i):
        if l<=sum(c)<=r and max(c)-min(c)>=x: ans+=1
print(ans)
