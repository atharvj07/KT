import sys

input = sys.stdin.readline

n = int(input())
a = [int(x) for x in input().split()]
b = [int(x) for x in input().split()]

last_exit = n
ans = 0
for i in range(n):
    if b[i] < last_exit:
        ans += 1
        last_exit = b[i]
print(ans)
