#include <iostream>
#include <cmath>
using namespace std;

int main() {
  int n, l, v1, v2, k;
  cin >> n >> l >> v1 >> v2 >> k;
  if (n <= k || v1 >= v2) {
    cout << 1.0 * l / v1 << endl;
  } else {
    int trips = (n + k - 1) / k;
    cout << 1.0 * l / v2 * trips << endl;
  }
  return 0;
}
