import sys

input = sys.stdin.readline

n = int(input())
a = []
for _ in range(n):
    l, r = map(int, input().split())
    a.append((l, r))
a.sort(key=lambda x: x[1])
ans = 0
r = -1
for l, _ in a:
    if l >= r:
        r = l
        ans += 1
print(ans)
