import sys

s = sys.stdin.readline().strip()
l = len(s)
ans = 0
for i in range(l):
	for j in range(i, l):
		cnt = 0
		for k in range(i, j + 1):
			if s[k] in "AEIOUY":
				cnt += 1
		ans += cnt / (j - i + 1);
print("{0:.6f}".format(ans))
