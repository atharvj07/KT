import sys

n = int(input().strip())
events = []
for _ in range(n):
    l, r = map(int, input().strip().split())
    events.append((l, 1))
    events.append((r, -1))
events.sort()

cnt = 0
for _, t in events:
    cnt += t
    if cnt > 2:
        print("NO")
        sys.exit()

print("YES")
