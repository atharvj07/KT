#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, d;
    cin >> n >> d;
    vector<int> a(n);
    for (int& x : a) { cin >> x; }
    cout << *upper_bound(a.begin(), a.end(), a.back() - d) - *lower_bound(a.begin(), a.end(), a[0] + d) + 1;
}
