#include <bits/stdc++.h>
using namespace std;typedef long long ll;typedef pair<ll,ll> pi;const ll mod=1e9+7;typedef complex<double> cd;
#define FOR(i,n,m) for(ll i=n;i<=m;i++)
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define endl "\n"
#define pb push_back
#define mp make_pair
#define all(x) x.begin(),x.end()
#define sz(z) (int)z.size()
#define getunique(v) {sort(all(v)); v.erase(unique(all(v)),v.end());}
#define makeunique(v) {sort(all(v)); v.erase(unique(all(v)),v.end());}
#define setdp(v,val) memset(v,val,sizeof(v))
#define all(x) x.begin(),x.end()
#define clr(a,val) memset(a,val,sizeof(a))
#define FORi(a,n) for(ll i=0;i<n;i++)
int main(){
  fastio;
  ll n,m;cin>>n>>m;
  vector<pi> v(n);
  FORi(i,n){
    ll x,y;cin>>x>>y;
    v[i]={y,x};
  }
  sort(all(v));
  ll sum1=0,sum2=0;
  FORi(i,n){
    sum1+=v[i].first;
  }
  ll ans=0;
  for(ll i=n-1;i>=0;i--){
    if(sum1<=sum2)break;
    sum2+=v[i].first;
    ans+=v[i].first;
    sum1-=v[i].first;
  }
  cout<<ans<<endl;
  return 0;
}
