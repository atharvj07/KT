#include<bits/stdc++.h>
using namespace std;
int main() {
  int n, mx = 0;
  cin >> n;
  for (int i = 1; i * i <= n; i++) {
    int x = (1 << i) - 1, y = x * x;
    if (y > n) break;
    if (n % y == 0) mx = max(mx, y);
  }
  cout << mx;
}
