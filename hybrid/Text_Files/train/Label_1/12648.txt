#include<bits/stdc++.h>
using namespace std;
int main() {
  int hy,ay,dy,hm,am,dm;
  cin >> hy >> ay >> dy >> hm >> am >> dm;
  long long need=(long long) ceil((double)(am-ay)*hm/(dy+dm)) + 1;
  cout << max(0ll,need) << '\n';
}
