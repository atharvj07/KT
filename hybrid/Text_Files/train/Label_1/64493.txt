n, k = map(int, input().split())

# Generate the starting sequence
p = list(range(1, n + 1))

# Modify the sequence to meet the condition
if k == 1:
    pass
else:
    for i in range(1, k):
        p[i], p[i + 1] = p[i + 1], p[i]

# Print the modified sequence
print(' '.join(map(str, p)))
