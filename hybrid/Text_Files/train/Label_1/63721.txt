from decimal import Decimal
from sys import stdin
read = stdin.readline

n = int(read())
arr = list(map(int, read().split()))
result = []
for i in range(n):
  x = sum(arr[i:])
  d = str(Decimal(x) / (n - i))
  result.append("{:.9f}".format(float(d)))
print('\n'.join(result))
