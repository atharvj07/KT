n, k = map(int, input().split())
a = list(map(int, input().split()))
prod, sum = 1, a[0]
l = 0; cnt = 0
for r in range(1, n):
    prod *= a[r]
    sum += a[r]
    while prod > k * sum:
        prod //= a[l]
        sum -= a[l]
        l += 1
    if prod == k * sum:
        cnt += 1
print(cnt)
