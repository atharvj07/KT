#include <iostream>

using namespace std;

int main() {
  int k, r;
  cin >> k >> r;
  if (10 % k == 0)
    cout << 1 << endl;
  else if ((10 - r) % k == 0)
    cout << 2 << endl;
  else
    cout << -1 << endl;
  return 0;
}
