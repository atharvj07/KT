#include <iostream>

using namespace std;

int main() {
    long long n, x, y;
    cin >> n >> x >> y;
    if ((x + y) % 2 == n % 2) {
        cout << "White";
    } else {
        cout << "Black";
    }
    return 0;
}
