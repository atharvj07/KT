['import sys\ninput = sys.stdin.readline\n\nk = int(input())\ns = input().split()\nwords = [list(word) for word in s]\nwidths = [len(word[0]) for word in words]\nfor word in words:\n    for i in range(1, len(word)):\n        widths[i] = max(widths[i], len(word[i]) + 1)\n\nlines = []\nline = 0\ncur_width = 0\nwhile line < len(words):\n    if cur_width + widths[line] <= k:\n        cur_width += widths[line]\n        line += 1\n    else:\n        lines.append(cur_width)\n        cur_width = widths[line]\n        line += 1\nlines.append(cur_width)\n\nprint(max(lines))\n']