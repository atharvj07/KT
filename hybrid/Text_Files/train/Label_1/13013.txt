#include <bits/stdc++.h>
using namespace std;
int main() {
  string A, B;
  cin >> A >> B;
  if (A.find('|') == string::npos || B.size() < A.size() - 1) {
    cout << "Impossible";
    return 0;
  }
  cout << B.substr(0, A.size() - 1) << '|' << A.substr(1) + B.substr(A.size() - 1);
}
