#include <algorithm>
#include <iostream>
using namespace std;
int main() {
  int n, m, w, a, b, c;
  cin >> n >> m >> w;
  while (n--) {
    cin >> a;
    if (n <= w - 1) b = max(b, a);
    if (n >= w) c = max(c, a);
  }
  cout << max(c + m, b + 1) << endl;
  return 0;
}
