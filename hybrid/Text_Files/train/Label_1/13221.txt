#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    int n,a,b,c;
    cin >> n >> a >> b >> c;
    ll ans = min((n-1)*(min(a+b,a+c)),(n-1)*(min(b+c,a+c)));
    cout << ans << endl;
    return 0;
}
