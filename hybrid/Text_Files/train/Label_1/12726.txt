#include <bits/stdc++.h>
using namespace std;

int main() {
  long long a, b, t1, t2, x, y, z;
  cin >> a >> b >> t1 >> x >> y >> t2;
  z = (x - a) / t1;
  if (z * t1 + a > x) z--;
  z = (y - b) / t2;
  if (z * t2 + b > y) z--;
  cout << z << endl;
  return 0;
}
