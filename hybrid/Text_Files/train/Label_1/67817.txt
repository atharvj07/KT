n, k = map(int, input().split())
v = list(map(int, input().split()))

v.sort()

dp = [float('inf')] * n
dp[0] = 0

for i in range(k-1, n):
    if i < k:
        dp[i] = v[i] - v[0]
    else:
        for j in range(k):
            dp[i] = min(dp[i], dp[i-j-1] + v[i] - v[i-j])

print(dp[-1])
