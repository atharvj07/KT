#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, a, b, c;
    cin >> n >> a >> b >> c;
    cout << (n + 4 - 1) / 4 * (min({a, (b + a) / 2, (c + b + a) / 3})) << endl;
    return 0;
}
