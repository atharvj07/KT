import sys
input=sys.stdin.readline;
a=input().rstrip();b=input().rstrip();
CNT=[0]*len(a);
for i in range(len(a)-len(b)+1):
  for j in range(len(b)):
    if a[i+j]==b[j]:CNT[i]+=1
print(sum(1 for i in CNT if i%2==0))
