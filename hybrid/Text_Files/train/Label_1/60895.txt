n,c1,c2 = map(int,input().split())
a = list(input())
a = [int(x) for x in a]
a.sort()
ans = c1 * n
now = 0
cnt = 0
for i in a:
    if(i==1):
        now+=1
        ans = min(ans,c1+c2*(now-1)*(now-1) + (cnt)*(c1+c2*(1-1)*(1-1)))
    else:
        cnt+=1
print(ans)
