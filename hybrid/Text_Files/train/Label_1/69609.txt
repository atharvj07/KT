from sys import stdin

n = int(stdin.readline())
a = [int(x) for x in stdin.readline().split()]
a.sort()
ans = 0
for i in range(1, n): ans += min(a[i] - a[i - 1], a[i - 1] + 1000000000 - a[i])
print(ans)
