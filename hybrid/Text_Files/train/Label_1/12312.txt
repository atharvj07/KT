#include<bits/stdc++.h>
using namespace std;
int main(){
    int n, mx = 0, cnt = 1, x;
    cin >> n;
    for(int i=0; i<n-1; i++){
        cin >> x;
        if(x - cnt == 1) cnt++;
        else mx = max(mx, cnt), cnt = 1;
    }
    mx = max(mx, cnt);
    cout << mx << endl;
}
