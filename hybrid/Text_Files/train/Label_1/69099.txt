n, m, k = map(int, input().split())
a = list(map(int, input().split()))

if sum(a) < m or m > k:
    print(-1)
else:
    ans = 0
    a.sort()
    cur = m
    for x in a:
        if cur <= x:
            ans += 1
            cur = k
        cur -= x
    print(ans)
