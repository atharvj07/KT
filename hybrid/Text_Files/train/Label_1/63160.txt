from fractions import Fraction

k = int(input())
nums = list(map(int, input().split()))
mod = 10 ** 9 + 7

num = Fraction(pow(3, sum(nums) - 2, mod), pow(2, sum(nums) - 1, mod))
den = Fraction(1, pow(2, sum(nums) - 1, mod))

print(f"{num.numerator % mod}/{den.denominator % mod}")
