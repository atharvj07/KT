import numpy as np

n = int(input())
a = [int(x) for x in input().split()]
a.sort()
a = np.array(a)
print(n - (len(set(a)) - a.shape[0]))
ans = []
for x in a:
    if x not in ans:
        ans.append(x)
ans.extend([ans[-1] for i in range(n - len(ans))])
print(*ans)
