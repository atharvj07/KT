n = int(input())
orders = [tuple(map(int, input().split())) for _ in range(n)]
orders.sort(key=lambda x: x[1])

max_accepted = 0
current_end = 0
for order in orders:
    if order[0] > current_end:
        max_accepted += 1
        current_end = order[1]

print(max_accepted)
