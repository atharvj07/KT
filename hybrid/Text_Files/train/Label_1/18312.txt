#include <iostream>
#include <cstdio>

using namespace std;

int main() {
  int n, m, k;
  cin >> n >> m >> k;
  int r = (k + m - 1) / m;
  int d = (k + m - 1) % m + 1;
  char s = "LR"[((k - 1) / m) % 2];
  cout << r << ' ' << d << ' ' << s << endl;
  return 0;
}
