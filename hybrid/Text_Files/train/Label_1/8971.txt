#include <iostream>
int main() {
  int n;
  std::cin >> n;
  bool flag = true;
  for (int i = 2; i <= n; i++) {
    int a;
    std::cin >> a;
    flag &= (a == 31 || a == 30 || (i == 2 && a == 28) || (i == 2 && a == 29));
  }
  std::cout << (flag ? "Yes" : "No") << std::endl;
  return 0;
}
