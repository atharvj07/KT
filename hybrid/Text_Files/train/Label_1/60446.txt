import sys
input = sys.stdin.readline

n, k = map(int, input().split())
songs = sorted([(t_i, b_i) for t_i, b_i in (map(int, input().split()) for _ in range(n))], key=lambda x: x[1], reverse=True)
print(sum(t_i for t_i, b_i in songs[:k]) * songs[k-1][1])
