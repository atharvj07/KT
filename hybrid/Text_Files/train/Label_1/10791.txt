#include <iostream>
#include <string>
using namespace std;

int main() {
  int n, ans = 0;
  cin >> n;
  for (int i = 1; i <= n; i++) {
    string s;
    cin >> s;
    if (s[0] == '-' || s[1] == '-')
      ans--;
    else
      ans++;
  }
  cout << ans << endl;
  return 0;
}
