n, m = map(int, input().split())
s = input()
t = input()

if '*' in s:
    pre, suf = s.split('*')
    if len(pre) + len(suf) > len(t) or not t.startswith(pre) or not t.endswith(suf):
        print("NO")
    else:
        print("YES")
else:
    print("YES" if s == t else "NO")
