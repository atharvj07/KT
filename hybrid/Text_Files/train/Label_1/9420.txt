#include <iostream>
using namespace std;
int main()
{
    int k, a, b; cin >> k >> a >> b;
    if (a % k > b % k || (a % k == b % k && (a + b) % k != 0)) { cout << -1 << endl; return 0; }
    cout << (a + b) / k << endl;
    return 0;
}
