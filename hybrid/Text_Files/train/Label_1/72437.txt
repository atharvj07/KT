n = int(input())
h = list(map(int, input().split()))

total = sum(h)
min_height = total // n
remainder = total % n

for i in range(n):
    if i < remainder:
        h[i] = min_height + 1
    else:
        h[i] = min_height

print(*h)
