import sys
input = sys.stdin.readline

n, q = list(map(int, input().split()))
arr = [i for i in range(1, n + 1)]
j = 0

for i in range(q):
    x = int(input()) - 1
    while x >= j:
        arr[j] = arr[(j + x) // 2]
        j = (j + x) // 2 + 1

    print(arr[x])
