import heapq

n = int(input())
pq = []
ans = []
for i in range(n):
    s = input().split()
    if s[0] == 'getMin':
        ans.append('insert ' + s[1])
        ans.append('removeMin')
        ans.append('getMin ' + s[1])
    else:
        ans.append(s[0] + ' ' + s[1])

print(len(ans))
for s in ans:
    print(s)
