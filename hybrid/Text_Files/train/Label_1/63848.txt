import sys
input = sys.stdin.readline
mod = 1000000007
n = int(input())
a, b = input(), input()
if '?' not in a or '?' not in b:
    ans = int(a != b)
else:
    ans = (pow(2, a.count('?')) * pow(2, b.count('?')) - 1) % mod
print(ans)
