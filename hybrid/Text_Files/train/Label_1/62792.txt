for _ in range(int(input())):
    n = int(input())
    s = input()
    ans = 0
    bal = 0
    for x in s:
        if x == ')':
            ans += 1 - bal
        bal += (1 if x == '(' else -1)
    print(ans)
