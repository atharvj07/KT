#include <iostream>

using namespace std;
typedef long long ll;

ll n, m, k;

int main() {
    cin >> n >> m >> k;
    ll r = (k / (m - 1) % n + 1);
    ll c = (k % (m - 1) + 1);
    if (r % 2 == 0) c = m - c + 1;
    cout << r << " " << c << endl;
    return 0;
}
