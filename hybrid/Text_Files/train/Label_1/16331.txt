#include <bits/stdc++.h>

using namespace std;

int main() {
  string a, b;
  cin >> a >> b;
  int n = a.size(), m = b.size(), i = 0, j = 0;
  while (i < n && j < m) {
    if (a[i] == b[j]) {
      j++;
    }
    i++;
  }
  if (j == m) {
    cout << b;
  } else {
    cout << "-";
  }
}
