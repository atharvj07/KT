import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    b = sorted(list(set(a)))
    if len(b) != n or b[0] != 1: print(-1)
    else: print(sum(a[i] > b[i] for i in range(n)))
