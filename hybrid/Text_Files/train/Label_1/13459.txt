#include <iostream>
using namespace std;
int f(int n) {
    return (n & 1 ? (n+1) / 2 * n : n / 2 * (n+1));
}
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        cout << 2 * f(n) - 1 << '\n';
    }
    return 0;
}
