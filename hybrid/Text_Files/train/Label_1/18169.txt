#include <iostream>
#include <string>
using namespace std;
int main() {
  int n; cin >> n;
  string s; cin >> s;
  int ans = 0;
  for (int i = 0, j = 1; i < s.size(); i++) {
    if (s[i] == '1') j *= 10;
    else {
      ans += j;
      j = 1;
    }
  }
  cout << ans << endl;
  return 0;
}
