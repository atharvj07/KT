import sys
input = sys.stdin.readline

def solve():
    s = input().strip()
    l, r, q = 0, 0, 0
    for c in s:
        if c == '(': l += 1
        elif c == ')': r += 1
        else: q += 1
    if l == r and q <= l: print('YES')
    else: print('NO')

for _ in range(int(input())):
    solve()
