from sys import stdin

ans = 0
m = 1001
pos = [0] * m
neg = [0] * m
for i in range(1, int(stdin.readline()) + 1):
    x, y = map(int, stdin.readline().split())
    ans += pos[x + y] + neg[x - y]
    pos[x + y] += 1
    neg[x - y] += 1
print(ans)
