#include <iostream>
#include <algorithm>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long p, a, b, c;
    std::cin >> p >> a >> b >> c;
    std::cout << std::min({p % a, p % b, p % c}) << '\n';
  }
  return 0;
}
