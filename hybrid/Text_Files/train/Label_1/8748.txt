#include <iostream>
#include <string>
using namespace std;
int main() {
  string s;
  cin >> s;
  int n = s.size();
  for (int i = n - 1; i >= 0; i--) {
    if (s[i] > '0') {
      s[i] = '9';
    }
  }
  cout << s << endl;
}
