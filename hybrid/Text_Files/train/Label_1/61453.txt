t = int(input().strip())
for _ in range(t):
    n = int(input().strip())
    a = list(map(int, input().strip().split()))
    total_sum = sum(a)
    max_elem = max(a)
    print(max_elem + (total_sum - max_elem) / (n - 1))
