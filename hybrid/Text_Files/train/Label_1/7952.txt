#include <iostream>

int main() {
  int q;
  std::cin >> q;
  while (q--) {
    unsigned long long a, b;
    std::cin >> a >> b;
    std::cout << (a & b == b ? "YES" : "NO") << std::endl;
  }
  return 0;
}
