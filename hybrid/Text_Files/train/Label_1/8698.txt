#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  string s;
  cin >> s;
  int n = s.size();
  set<string> st;
  st.insert(s);
  for (int i = 1; i < n; ++i) {
    string t = s.substr(i) + s.substr(0, i);
    st.insert(t);
  }
  cout << st.size() << endl;
}
