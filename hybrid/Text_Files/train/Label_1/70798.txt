import sys
cnt = int(input())
for i in range(cnt):
    n = int(input())
    s = input();
    ans = ''
    for j in s:
        if j == '?':
            if ans == '':
                ans += 'B'
            elif ans[-1] == 'B':
                ans += 'R'
            else:
                ans += 'B'
        else:
            ans += j
    print(ans)
