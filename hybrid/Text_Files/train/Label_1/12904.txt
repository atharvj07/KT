#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll n,ans,x[100005],y[100005];

int main()
{
    cin>>n;
    for(ll i=1;i <=n;i++)
    {
        cin>>x[i]>>y[i];
    }
    sort(x+1,x+n+1);
    sort(y+1,y+n+1);
    ans=x[n]+y[n];
    cout<<ans<<'\n';
}
