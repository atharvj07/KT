import sys
input = sys.stdin.readline

n = int(input())
x, y = [0]*(n+1), [0]*(n+1)
for i in range(n):
	x[i+1], y[i+1] = map(int, input().split())

xmax, xmin = max(x), min(x)
ymax, ymin = max(y), min(y)

ans = max(xmax-xmin, ymax-ymin) + 1

print(ans)
