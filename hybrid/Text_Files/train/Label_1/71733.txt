def gcd(a, b):
    while b:
        a, b = b, a%b
    return a

def gcd_range(a, b):
    if a == b:
        return a
    else:
        # If a != b, the greatest common divisor of the range [a, b] is 1
        return 1

a, b = map(int, input().split())
print(gcd_range(a, b))
