#include<bits/stdc++.h>
using namespace std;
typedef pair<long long,long long> pr;
int n;
long long x, y, ans;
int main(){
  cin >> n;
  for(int i = 1; i <= n; i++){
    cin >> x >> y;
    ans += min(x, y) * max(x, y);
  }
  cout << ans;
  return 0;
}
