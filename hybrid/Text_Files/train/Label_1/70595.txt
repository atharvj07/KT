import sys
n = int(input())
l = [None]*n
r = [None]*n
for i in range(n):
  l[i], r[i] = map(int, input().split())
idx = [i for i in range(n)]
idx.sort(key=lambda x: r[x])
ans = 1
last = r[idx[0]]
for i in range(1, n):
  if l[idx[i]] >= last:
    ans += 1
    last = r[idx[i]]
print(ans)
