#include <iostream>
int main() {
  long long x, y, m;
  std::cin >> x >> y >> m;
  if (x >= m || y >= m) std::cout << "0";
  else if ((x + y) >= m) std::cout << "1";
  else std::cout << "-1";
  return 0;
}
