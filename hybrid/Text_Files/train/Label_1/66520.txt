import sys
rl = sys.stdin.readline

def xmagic(a: int, b: int, x: int) -> bool:
	while a != b and a != x and b != x:
		if a > b: a -= b
		if b > a: b -= a
	return a == x or b == x

for _ in range(int(rl())):
	print("YES" if xmagic(*map(int,rl().split())) else "NO")
