from sys import stdin
for _ in range(int(stdin.readline())):
    n, k = map(int, stdin.readline().split())
    p = list(map(int, stdin.readline().split()))
    t = sum(p)
    print(min([p[i] * max(0, (100 * (t - p[i])) // k // 100 - 1) for i in range(n)]))
