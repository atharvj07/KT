#include <bits/stdc++.h>
using namespace std;
int main() {
  int q;
  cin >> q;
  while (q--) {
    string s, t;
    cin >> s >> t;
    if (2 * s.size() - 1 < t.size())
      cout << "NO\n";
    else
      cout << "YES\n";
  }
}
