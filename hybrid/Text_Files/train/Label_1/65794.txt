n, t = map(int, input().split())
a = [int(x) for x in input().split()]
dp = [1] * t
for i in range(1, t):
    for j in range(i):
        if a[j] <= a[i]:
            dp[i] = max(dp[i], dp[j] + 1)
print(max(dp))
