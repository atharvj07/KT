#include <bits/stdc++.h>
using namespace std;
using ll = long long;
const int mod = 1e9 + 7;

int n;
ll a[300001], ans;

int main() {
  cin >> n;
  for (int i = 0; i < n; i++) cin >> a[i];
  sort(a, a + n);
  for (int i = 1; i < n; i++) ans = (ans + a[i] - a[i - 1]) % mod;
  cout << ans;
}
