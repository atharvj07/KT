#include <bits/stdc++.h>

using namespace std;

int main() {
    long long n, sum = 0, a[200005];
    cin >> n;
    for (int i = 0; i < n; ++i) cin >> a[i];
    sort(a, a + n);
    for (int i = 0; i < n - 1; ++i) sum += a[i + 1] - a[i];
    cout << sum << endl;
    return 0;
}
