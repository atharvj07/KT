import sys

comments = sys.stdin.readline().strip().split(',')
depth = 0
d = {}
for i in range(0, len(comments), 3):
    depth = max(depth, comments[i + 1].count(',') + 1)
    d[depth] = d.get(depth, '') + f'{comments[i]}, '
print(depth)
for i in range(1, depth + 1):
    print(d[i][:-2])
