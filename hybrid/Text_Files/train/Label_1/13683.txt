#include <iostream>
using namespace std;

int main() {
  int k, a, b, v;
  cin >> k >> a >> b >> v;
  int rem = a / (k * v);
  if (a % (k * v) != 0) rem += 1;
  (b >= rem) ? cout << rem : cout << -1 << endl;
  return 0;
}
