#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, a, v[3] = {0};
  cin >> n;
  for (int i = 0; i < n; i++) {
    cin >> a;
    v[(i % 3)] += a;
  }
  if (v[0] > v[1]) cout << "chest\n";
  else if (v[1] > v[2]) cout << "biceps\n";
  else cout << "back\n";
}
