#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int t; cin >> t;
    while (t--) {
        int b, p, f, h, c; cin >> b >> p >> f >> h >> c;
        cout << max({0, min(b/2, p) * h + min(b/2, f) * c, min(b/2, p+f) * h}) << endl;
    }
}
