#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){
    ll n,s,ans=0;
    cin>>n>>s;
    for (ll i=1;i<=n;++i){
        ll x=i,sum=0;
        while (x) {sum+=x%10;x/=10;}
        if (i-sum>=s) ans++;
    }
    cout<<ans;
}
