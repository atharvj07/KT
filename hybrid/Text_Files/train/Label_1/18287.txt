#include <iostream>
using namespace std;
int main() {
    int b, k, sum = 0;
    cin >> b >> k;
    for (int i = 0; i < k; i++) {
        int a;
        cin >> a;
        sum += a;
    }
    cout << (sum % 2 == 0 ? "even" : "odd") << endl;
    return 0;
}
