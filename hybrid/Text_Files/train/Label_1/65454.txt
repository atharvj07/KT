import sys

t = int(input())

for i in range(t):
    n = int(input())
    nums = list(map(int, input().split()))
    last = 1
    colors = 1
    for j in nums:
        if j > last:
            colors += 1
            last = j
    print(colors)
