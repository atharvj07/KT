import math

x, y, l, r = map(int, input().split())
ans = 0
for i in range(int(math.log2(r)) + 1):
    for j in range(int(math.log2(r)) + 1):
        if x ** i + y ** j >= l and x ** i + y ** j <= r:
            ans = max(ans, i + j - int(math.log2(x ** i + y ** j)) + 1)
print(ans)
