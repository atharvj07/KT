import sys
input = sys.stdin.readline
n = int(input())
a = list(map(int, input().split()))
ans = set()
for i in range(n):
    for j in range(i):
        ans.add(a[j] ^ a[i])
print(len(ans))
print(*sorted(ans))
