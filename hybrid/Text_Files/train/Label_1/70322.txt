t = int(input())
for _ in range(t):
    n = int(input())
    min_r = float('inf')
    max_l = float('-inf')
    for __ in range(n):
        l, r = map(int, input().split())
        min_r = min(min_r, r)
        max_l = max(max_l, l)
    print(max(0, max_l - min_r))
