#include <iostream>

using namespace std;

typedef long long ll;

int main() {
  ll a, b;
  while (cin >> a >> b) {
    ll ans = 0;
    for (; !(a == b); ++ans) {
      a = min(a, b);
      b = (b - a + 1) / 2;
    }
    cout << ans << endl;
  }
  return 0;
}
