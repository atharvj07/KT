#include <bits/stdc++.h>

using namespace std;

const int mod = 1e9 + 7;

int main() {
  int n, x, pos;
  cin >> n >> x >> pos;
  int ans = 1LL * (pos + 1) * (n - pos) % mod;
  cout << ans << endl;
  return 0;
}
