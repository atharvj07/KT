n = int(input().strip())
a = list(map(int, input().strip().split()))
b = list(map(int, input().strip().split()))

max_val = 0
for l in range(n):
    a_or, b_or = 0, 0
    for r in range(l, n):
        a_or |= a[r]
        b_or |= b[r]
        max_val = max(max_val, a_or + b_or)
print(max_val)
