#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long l, r;
        cin >> l >> r;
        if (l == r) { cout << 0 << endl; continue; }
        cout << (r - l) / 2 + 1 << endl;
    }
    return 0;
}
