#include<iostream>
using namespace std;

int main() {
    long long l1, r1, l2, r2, k;
    cin >> l1 >> r1 >> l2 >> r2 >> k;
    long long m1 = max(l1, l2);
    long long m2 = min(r1, r2);
    if (k >= m1 && k <= m2) m2--;
    cout << max(0LL, m2 - m1 + 1) << endl;
    return 0;
}
