#include <iostream>
#include <vector>
using namespace std;

void solve() {
  int n;
  cin >> n;
  vector<int> p(n + 1);
  for (int i = 1; i <= n; ++i) {
    cin >> p[i];
  }
  cout << (p[p[1]] == 2 ? "YES" : "NO") << '\n';
}

int main() {
  int q;
  cin >> q;
  while (q--) solve();
  return 0;
}
