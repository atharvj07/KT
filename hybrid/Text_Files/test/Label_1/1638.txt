#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  ll x, y;
  cin >> x >> y;
  int a = 0;
  while (x % 2 > 0) x /= 2, a++;
  while (y % 2 == 0) y /= 2, a--;
  if (x == y && a >= 0)
    cout << "YES";
  else
    cout << "NO";
  return 0;
}
