import sys

n, m = map(int, input().split())
A = [input() for i in range(n)]
B = sorted(range(n), key=lambda x: [(i%2 and A[x][i] or chr(ord('Z')-ord(A[x][i]))) for i in range(m)])
print(*map(lambda x: x+1, B), sep="\n")
