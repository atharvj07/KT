#include <cstdio>

typedef long long ll;
const int mod = 1e9 + 7;

int main() {
    long long n; scanf("%lli", &n);
    printf("%lli\n", (n % 2 ? 8ll : 4ll * ((n + 1ll) / 2ll) * ((n + 1ll) / 2ll) + 4ll) % mod);
    return 0;
}
