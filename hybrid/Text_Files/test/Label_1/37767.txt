from sys import stdin

n, m = map(int, stdin.readline().split())
a = list(map(int, stdin.readline().split()))
ans = 0
for i in range(1, n):
    if a[i] < a[i - 1]:
        ans += (a[i - 1] - a[i] + m) // m
        a[i] += (a[i - 1] - a[i] + m) // m
print(ans)
