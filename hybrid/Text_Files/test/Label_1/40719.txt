import sys
input=sys.stdin.readline

t=int(input())
for _ in range(t):
    n,x=map(int,input().split())
    s=input()
    bal=s.count('0')-s.count('1')
    if not bal: ans=-1
    else:
        rem=x%bal
        ans=0
        cur=0
        for i in s:
            cur+=int(i)
            if cur==rem: ans+=1
            cur%=bal
    print(ans)
