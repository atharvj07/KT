#include <iostream>

using namespace std;

int main() {
  long long a, b, w, x, c;
  cin >> a >> b >> w >> x >> c;
  long long sec = c - a;
  if (b >= x)
    sec = (sec + x - 1) / x;
  else
    sec = (sec + w - x - 1) / (w - x);
  cout << sec << endl;
  return 0;
}
