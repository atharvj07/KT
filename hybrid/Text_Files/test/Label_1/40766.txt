import sys
n, q = map(int, input().split())
a = list(map(int, input().split()))
k = list(map(int, input().split()))
ans = [n]
cur = 0
for i in range(q):
    while cur < n and a[cur] <= k[i]:
        k[i] -= a[cur]
        cur += 1
    ans.append(n - cur)
print(*ans, sep='\n')

