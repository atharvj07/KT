#include <iostream>

int main() {
  int black = 0;
  for (int i = 0; i < 4; ++i) {
    for (char c; std::cin >> c;) {
      black += c == '#';
    };
  };
  std::cout << ((black == 4 || black == 8 || black == 12) ? "YES" : "NO") << '\n';
  return 0;
}
