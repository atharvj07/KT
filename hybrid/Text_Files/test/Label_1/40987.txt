from math import gcd

for _ in range(int(input())):
    input()
    a = list(map(int, input().split()))
    ans = 0
    while min(a) != max(a):
        b = [gcd(x, y) for x, y in zip(a, a[1:] + a[:1])]
        ans += 1
        a = b
    print(ans)
