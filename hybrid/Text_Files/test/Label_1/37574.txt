n, q = map(int, input().split())
a = list(map(int, input().split()))

for _ in range(q):
    t, x = map(int, input().split())
    if t == 1:
        a[x-1] = 1 - a[x-1]
    else:
        ones = sum(a)
        if x <= ones:
            print(1)
        else:
            print(0)
