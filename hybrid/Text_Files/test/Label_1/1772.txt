#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, x, y;
    cin >> n >> x >> y;
    cout << min(x, y) << " " << x + y - 1 << endl;
  }
  return 0;
}
