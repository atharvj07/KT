#include <bits/stdc++.h>
using namespace std;
int main() {
    int a, b, c, d, e, mn, mx;
    cin >> a >> b >> c >> d >> e;
    mx = min(a, (e + c - 1) / c) + min(b, e / d);
    mn = max(0, min(a, (e - d + 1) / c)) + max(0, min(b, (e - c + 1) / d));
    cout << mn << " " << mx << endl;
}
