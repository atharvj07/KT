#include<bits/stdc++.h>
using namespace std;
int main()
{
  int a,b,c;cin>>a>>b>>c;
  double d=b*b-4*a*c;
  cout<<fixed<<setprecision(15)<<max(-b+sqrt(d),-b-sqrt(d))/2/a<<endl;
  cout<<fixed<<setprecision(15)<<min(-b+sqrt(d),-b-sqrt(d))/2/a<<endl;
  return 0;
}
