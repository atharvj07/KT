import sys
i=0
n,x=map(int,input().split())
d=[]
for _ in range(n):
    e,h,m=map(int,input().split())
    d+=sorted([(h,e,m)],key=lambda x:(x[0],x[1]))
    i=max(i,h)
j=0
while i>=x:
    h,e,m=d[j]
    if (e^j)%2:
        i+=m
    j+=1
    if j>=len(d):
        break
print(j)
