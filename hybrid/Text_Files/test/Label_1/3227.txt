#include <iostream>
#include <string>
using namespace std;

int main() {
  long long x, y;
  cin >> x >> y;
  if (x <= 1 && y <= 1) {
    cout << "Impossible" << endl;
    return 0;
  }
  if (x == 1)
    cout << "1B" << endl;
  else
    cout << y / x << "B" << 1 << "A" << endl;
  return 0;
}
