#include <iostream>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        long long n, l, r;
        cin >> n >> l >> r;
        if (n >= l && n % l <= r)
            cout << "Yes" << endl;
        else
            cout << "No" << endl;
    }
    return 0;
}
