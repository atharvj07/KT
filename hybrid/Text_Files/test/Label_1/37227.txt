n = int(input())
a = list(map(int, input().split()))
a.sort()
ans = 360
for i in range(1, n):
    ans = min(ans, a[i] - a[i - 1])
for i in range(1, n):
    if a[0] + a[i] > 360:
        ans = min(ans, 360 - a[i])
print(ans)
