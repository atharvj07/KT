#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n, k, i, x, sum = 0;
  cin >> n >> k;
  vector<long long> v(n);
  for (i = 0; i < n; i++) cin >> v[i];
  sort(v.begin(), v.end());
  for (i = 0; i < n - k; i++) sum += v[i + k] - v[i];
  cout << sum << endl;
  return 0;
}
