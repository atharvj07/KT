#include <bits/stdc++.h>
using namespace std;

#define ll long long

const int mod = 1e9 + 9;

int main() {
    ll n;
    cin >> n;

    if (n % 2 == 0) {
        cout << (n / 2 - 1) % mod << '\n';
    } else {
        cout << (n / 2) % mod << '\n';
    }

    return 0;
}
