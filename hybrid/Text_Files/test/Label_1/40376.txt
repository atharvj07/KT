n, k = map(int, input().split())
a = list(map(int, input().split()))
hm = suf = sum(a[:k])
for i in range(k, n):
    suf += a[i] - a[i - k]
    hm = max(hm, (suf + sum(a[:i - k + 1])) / k)
print(hm / k)
