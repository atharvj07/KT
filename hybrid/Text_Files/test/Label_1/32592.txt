import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String X = sc.next();
        int n = X.length();
        while (X.indexOf("ST") != -1) {
            X = X.substring(0, X.indexOf("ST")) + X.substring(X.indexOf("ST") + 2);
        }
        System.out.println(X.length());
    }
}
