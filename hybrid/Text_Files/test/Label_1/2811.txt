#include <vector>
#include <iostream>
int main() {
  int n; std::cin >> n;
  if (n < 3) { std::cout << n - 1 << std::endl; return 0; }
  int m = n / 3;
  std::cout << m * (m + 1) / 2 + (n - m * 3) * (n - m * 3 - 1) / 2 << std::endl;
  return 0;
}
