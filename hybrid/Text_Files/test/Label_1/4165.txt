#include <vector>
#include <iostream>

int main() {
    int n, ans;
    std::cin >> n;
    std::vector<int> v(n);
    std::cin >> v[0];
    for (int i = 1; i < n; i++) {
        std::cin >> v[i];
        if(v[i] == 1 && v[i] == v[i-1]) ans++;
    }
    std::cout << ans << std::endl;
}
