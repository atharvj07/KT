def count_remote_planets():
    N = int(input())
    connections = [0] * (N + 1)
    for _ in range(N - 1):
        u, v = map(int, input().split())
        connections[u] += 1
        connections[v] += 1
    print(sum(1 for i in connections if i == 1))

count_remote_planets()
