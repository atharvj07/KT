#include<bits/stdc++.h>
using namespace std;
typedef long long lld;
typedef long ld;
const lld MOD=(lld)1e9+7;
const lld INF=(lld)1e9+9;
typedef pair<lld,lld> pi;
typedef tuple<lld,lld,lld> ti;
const ld EPS=1e-6;
#define iterate(i,l,r) for(lld i=l;i<=r;i++)
#define get(x) (*x)
#define find(x) (*x).find
#define all(x) (*x).begin(),(*x).end()
#define size(x) (*x).size()
#define lower(x,y) (*x).lower_bound(y)
#define upper(x,y) (*x).upper_bound(y)
#define make(x,y) map<x,y>
#define set(x) set<x>

#define yes cout<<"YES"<<endl
#define no cout<<"NO"<<endl
int main(){
	lld n,d,k;
	cin>>n>>d>>k;
	if(d>k)
		no;
	else if(k<=2)
		no;
	else if(k==3){
		if(d<=2){
			lld ct=0;
			iterate(i,0,d)iterate(j,1,3-i){
				ct++;
				cout<<2*ct<<" "<<2*ct+1<<endl;
			}
		}
		else{
			lld ct=0;
			iterate(i,0,3)iterate(j,1,4-i){
				ct++;
				cout<<2*ct<<" "<<2*ct+1<<endl;
			}
		}
	}
	else if(d>=k){
		lld ct=0;
		iterate(i,0,d-2)iterate(j,1,k-1){
			ct++;
			cout<<2*ct<<" "<<2*ct+1<<endl;
		}
		ct++;
		cout<<2*ct<<" "<<2*ct+2<<endl;
		lld x=2*ct+2;
		iterate(i,0,k-2){
			ct++;
			cout<<2*ct<<" "<<x<<endl;
		}
	}
	else{
		lld ct=0;
		iterate(i,0,k-4)iterate(j,1,n-k-(i+1)){
			ct++;
			cout<<2*ct<<" "<<2*ct+1<<endl;
		}
		lld x=2*ct+2;
		iterate(i,0,k-2){
			ct++;
			cout<<2*ct<<" "<<x<<endl;
		}
		ct++;
		cout<<2*ct<<" "<<2*ct+1<<endl;
		ct++;
		cout<<2*ct<<" "<<2*ct+1<<endl;
	}
	return 0;
}
