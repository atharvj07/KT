for t in range(int(input())):
    n, m = list(map(int, input().split()))
    a = list(map(int, input().split()))
    avg = sum(a)/(n-1)
    a[0] = m
    for i in range(1, n):
        a[i] = max(0,min(int(m - (avg*(n-1) - a[0])),m))
    print(max(a))
