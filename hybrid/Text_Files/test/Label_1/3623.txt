#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    int ans = 0;
    for (int i = 1; i * i <= n; i++) {
        ans += (n % i == 0);
        if (i != n / i) ans += (n % (n / i) == 0);
    }
    cout << ans - 1 << endl;
    return 0;
}
