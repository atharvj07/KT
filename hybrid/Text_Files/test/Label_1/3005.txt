#include<bits/stdc++.h>
using namespace std;
int josephus(int n,int k){
    vector<int> v;
    for(int i=1; i<=n; i++)
        v.push_back(i);
    int i=k-1,ans=0,x=0;
    while(v.size()>1){
        i=i%v.size();
        cout<<v[i]<<" ";
        v.erase(v.begin()+i);
        i+=k-1;
    }
    return x;
}
int main(){
    int n,k;
    cin>>n>>k;
    josephus(n,k);
    return 0;
}
