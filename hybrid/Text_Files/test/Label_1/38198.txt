n, k = map(int, input().split())
msg = list(map(int, input().split()))
last = []
for v in msg:
    if v in last: last.remove(v)
    last.append(v)
    if len(last) > k: last.pop(0)
print(len(last))
print(*last)
