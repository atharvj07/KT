#include <iostream>

using namespace std;

int main() {
    int n, k, ans = 0, time = 0;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        time += 5 * i;
        if (time <= 240 - k)
            ans++;
        else
            break;
    }
    cout << ans << endl;
    return 0;
}
