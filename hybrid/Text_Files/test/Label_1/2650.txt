#include <iostream>

using namespace std;

int main() {
  uint64_t l, r, k;
  cin >> l >> r >> k;

  if (k <= r) {
    cout << 1;
    uint64_t p = k;
    while (p <= r) {
      p *= k;
      if (p >= l)
        cout << ' ' << p;
    }
  } else
    cout << -1;
  cout << endl;
  return 0;
}
