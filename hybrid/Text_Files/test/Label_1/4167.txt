#include <iostream>

using namespace std;

int main() {
  int T;
  cin >> T;
  for (int t = 0; t < T; t++) {
    long long A, B, C, D;
    cin >> A >> B >> C >> D;
    cout << (A > 0 || B > 0 ? "Ya " : "Tidak ")
         << (A > 0 || C > 0 || B > 1 ? "Ya " : "Tidak ")
         << ((D > 0 || C > 0 || B > 1) ? "Ya " : "Tidak ")
         << (C > 0 || D > 0 ? "Ya" : "Tidak") << endl;
  }
  return 0;
}
