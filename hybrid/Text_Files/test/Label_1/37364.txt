t = int(input())
for _ in range(t):
    n = int(input())
    s = input()
    first_one = s.find('1')
    last_zero = s.rfind('0')
    if first_one == -1 or last_zero == -1 or first_one > last_zero:
        print(s)
    else:
        print(s[:first_one] + '0' + s[last_zero + 1:])
