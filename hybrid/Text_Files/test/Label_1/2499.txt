#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long n, m;
    std::cin >> n >> m;
    if (n == 1 || m == 1) {
      std::cout << 1 << '\n';
    } else {
      std::cout << 2 << '\n';
    }
  }
  return 0;
}
