#include <bits/stdc++.h>
using namespace std;
int main() {
  for (int t, n; cin >> t; t--) {
    cin >> n;
    if (n % 2)
      cout << "1\n";
    else
      cout << (n % 4 == 0 ? "2" : "3") << '\n';
  }
}
