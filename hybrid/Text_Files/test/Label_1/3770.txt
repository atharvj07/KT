#include <iostream>

using namespace std;

int main() {
  int x, y, a, b;
  cin >> x >> y >> a >> b;
  int n = (x - a + 1) * (y - b + 1);
  cout << n << "\n";
  for (int i = a; i <= x; i++) {
    for (int j = b; j <= y; j++) {
      cout << i << " " << j << "\n";
    }
  }
  return 0;
}
