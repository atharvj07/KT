#include<bits/stdc++.h>
using namespace std;

bool ok(int a, int b, int c, int d) {
    if(a % c > 0 || b % d > 0) return true;
    return a <= b;
}

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    if(ok(a, b, c, d)) cout << "First" << "\n";
    else cout << "Second" << "\n";
}

