import sys

t = int(input())

for i in range(t):
    n = int(input())
    s = input()
    
    stack = []
    
    for ch in s:
        if not stack or ch == stack[-1]:
            stack.append(ch)
        else:
            stack.pop()
    
    print(''.join(stack))
