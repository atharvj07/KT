#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long a,b,c,m,ans;
    cin>>a>>b>>c;
    ans=min(a,b);
    ans=min(ans,c);
    m=min(a,min(b,c));
    ans+=m*2;
    cout<<ans<<endl;
}
