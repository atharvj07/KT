from collections import defaultdict
n = int(input())
c = defaultdict(list)
for i in range(n):
    a,b = map(int,input().split())
    c[a].append(i)
    c[b].append(i)
for i in c:
    t = c[i]
    print(len(t)-len(set(t)), len(set(t)))
