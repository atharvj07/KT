import sys

n = int(input())
a = [list(map(int, input().split())) for _ in range(n)]
a.sort()
ans, cur, dir = 0, 0, 1
for x, y in a:
    dir *= -1
    ans += y
    cur += x * dir
print(ans - abs(cur))
