n, a, b, k = map(int, input().split())
h = list(map(int, input().split()))
h.sort()
ans, left, tot = 0, 0, 0
for i in h:
    tot += i
    if tot - (left + a) <= 0:
        left += a
        ans += 1
    elif k > 0:
        k -= 1
        left += b
    else: break
print(ans)
