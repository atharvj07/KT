#include <vector>
#include <iostream>

int main() {
    int n; std::cin >> n;
    std::vector<long long> x(n);
    for (int i = 0; i < n; ++i) { std::cin >> x[i]; }
    for (int i = 0; i < n; ++i) {
        std::cout << x[i + 1] - x[i] << " " << x[n - 1] - x[i] << std::endl;
    }
}
