#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, m, ans = 0;
    cin >> n >> m;
    vector<long long> a(m);
    for (long long &i : a) cin >> i;
    cout << a[m - 1] + (m - 1);
}
