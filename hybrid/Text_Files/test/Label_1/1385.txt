#include <bits/stdc++.h>
using namespace std;
int n, k, fr[100005], ans;
int main() {
  cin >> n >> k;
  for (int i = 1, a; i <= n; i++) cin >> a, fr[a]++;
  int mins = n;
  for (int i = 1; i <= k; i++)
    if (fr[i] < mins) mins = fr[i], ans = i;
  cout << ans << endl;
  return 0;
}
