#include <iostream>
using namespace std;

int main() {
    int x, y, z;
    cin >> x >> y >> z;
    if (x + z > y)
        cout << '+';
    else if (x + z < y)
        cout << '-';
    else
        cout << '0';
    return 0;
}
