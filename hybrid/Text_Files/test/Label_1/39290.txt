import sys
from itertools import chain,groupby

n,m,q = map(int,input().split())
a = [[int(v) for v in input().split()] for _ in range(n)]

for x in range(q):
    i,j = map(int,input().split())
    a[i-1][j-1] = 1 - a[i-1][j-1]
    print(max(len(list(v)) for k,v in groupby(chain(*a))))
