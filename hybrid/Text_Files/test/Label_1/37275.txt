n, q = map(int, input().split())
for _ in range(q):
    x, y = map(int, input().split())
    if (x + y) % 2 == 0:
        print(((x - 1) * n + y + (x + y) % 2) // 2)
    else:
        print((((x - 1) * n + y + (x + y) % 2) // 2) + ((n * n + 1) // 2))
