n, p = list(map(int, input().split()))
arr = [0] * n 
for i in range(n): arr[i] = input() == 'halfplus'
ans = p * sum(arr) / 2
for i in range(n-1, -1, -1):
    if arr[i] == 1:
        ans += p; arr[i-1] += 1
        if arr[i-1] == 1: ans -= p // 2
print(ans)
