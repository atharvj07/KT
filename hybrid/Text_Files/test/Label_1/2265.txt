#include <iostream>
int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long x, y;
    std::cin >> x >> y;
    std::cout << ((x == y) || (x % 2 == 0 && 3 * x / 2 == y)
                      || (x > 1 && x - 1 == y) ? "YES\n" : "NO\n");
  }
  return 0;
}
