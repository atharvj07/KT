import java.util.Scanner;

public class IahubinaTree {

    static boolean[] visited;
    static int[] subtreeSize;

    static boolean dfs(int node, int parent, int n, int[] subtreeSize) {
        visited[node] = true;
        subtreeSize[node] = 1;

        for (int child : getChildren(node, n)) {
            if (child == parent) continue;
            if (visited[child]) {
                return false; // Cycle detected
            }
            if (!dfs(child, node, n, subtreeSize)) {
                return false;
            }
            subtreeSize[node] += subtreeSize[child];
        }

        return subtreeSize[node] == subtreeSize[node + 1]; // Check if subtree size matches input
    }

    static int[] getChildren(int node, int n) {
        // Implement your logic to get the children of node
        // This depends on how your tree is stored (e.g., adjacency list, etc.)
        // For example, if using an adjacency list:
        // return adjacencyList.get(node);
        return new int[]{}; // Replace with actual children
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        int[] subtreeSize = new int[n + 1];
        for (int i = 1; i <= n; i++) {
            subtreeSize[i] = scanner.nextInt();
        }

        visited = new boolean[n + 1];
        for (int i = 1; i <= n; i++) {
            if (!visited[i] && dfs(i, 0, n, subtreeSize)) {
                System.out.println("YES");
                return;
            }
        }

        System.out.println("NO");
    }
}
