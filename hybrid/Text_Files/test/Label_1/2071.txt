#include <bits/stdc++.h>
using namespace std;
bool vis[100005]; int pre[100005];
int main() {
  int n; cin >> n;
  if (n == 6) return cout << "NO", 0;
  cout << "YES" << endl;
  if (n % 2 == 0)
    for (int i = 1; i <= n; i++) cout << i << endl;
  else {
    cout << 2 << endl;
    cout << 4 << endl;
    for (int i = 1; i <= n; i++) if (i != 2 && i != 4) cout << i << endl;
  }
  return 0;
}
