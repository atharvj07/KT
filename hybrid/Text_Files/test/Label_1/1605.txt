#include <bits/stdc++.h>
#define int long long
#define maxn (250001)
#define mod (437122297)
#define INF (0x3f3f3f3f3f3f3f)
#define eps (1e-12)
using namespace std;
typedef pair<int, int> pi;
#define fi first
#define se second
int32_t main()
{
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n, m;
    cin >> n >> m;
    int dp[n + 10];
    dp[1] = dp[0] = 1;
    for (int i = 2; i <= n; i += 1)
    {
        dp[i] = (1LL * i * (i + 1)) % m;
        dp[i] = (dp[i] + 1LL * dp[i - 1] * dp[i - 1] % m) % m;
    }
    cout << (dp[n] - 1 + m) % m << '\n';
}
