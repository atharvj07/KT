#include <stdio.h>

int main() {
  int n, a[4];
  char ch;
  scanf("%d + %d - %d + %d = %d", &a[0], &a[1], &a[2], &a[3], &n);
  if (a[0] + a[1] - a[2] + a[3] == n) {
    printf("Possible\n");
    printf("%d + %d - %d + %d = %d\n", a[0], a[1], a[2], a[3], n);
  } else
    printf("Impossible\n");
}
