#include<bits/stdc++.h>
using namespace std;

int a, b, c, d;
int main() {
    cin >> a >> b >> c >> d;
    int mx = max({abs(a - b), abs(b - c), abs(c - a)});
    if (mx <= d) cout << 0 << endl;
    else {
        int add = (mx + d) / d * d - mx;
        cout << add << endl;
    }
    return 0;
}
