msk = 2**30
n = int(input())
b = []; rows = [[i] for i in range(n)]
for i in range(n):
    row = list(map(int,input().split()))
    for j in range(n):
        if row[j] != -1: rows[i].append(row[j])
    b.append(row)
for i in range(n):
    for v in rows[i]:
        for k in range(n):
            if b[k][i] != -1 and b[k][i] & v != b[k][j]:
                break
        else:
            print(v, end = ' ')
            for k in range(n):
                b[k][i] = b[k][i] & v
                b[i][k] = b[k][i]
            break
