n, d = map(int, input().split())
m = int(input())
for _ in range(m):
    x, y = map(int, input().split())
    if abs(x - y) <= d and min(x, y) >= d and max(x, y) <= n - d:
        print('YES')
    else:
        print('NO')
