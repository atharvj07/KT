#include <cstdio>
#include <vector>
const int N = 2e5+1;
char s[N],t[N];
int main(){
    int n;
    scanf("%d%s",&n,s+1);
    int top=0;
    for(int i=1;i<=n;++i){
        if(s[i]==s[i-1]) t[++top]=s[i];
    }
    if(top&1)puts("0");
    else printf("%d\n",top);
}
