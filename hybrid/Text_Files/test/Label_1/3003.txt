#include <bits/stdc++.h>
using namespace std;
int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);
  int a, b, c, d;
  cin >> a >> b >> c >> d;
  cout << max({abs(a - b) / 2 + abs(a - c) / 2, abs(b - c) / 2 + abs(b - a) / 2, abs(c - a) / 2 + abs(c - b) / 2}) << '\n';
}
