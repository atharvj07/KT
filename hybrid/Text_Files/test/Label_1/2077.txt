#include <iostream>
int main() {
  int t;
  std::cin >> t;
  while (t--) {
    int a, b, c;
    std::cin >> a >> b >> c;
    std::cout << std::min(a, std::min(b, c)) + std::min(std::min(a, b), std::min(std::max(a, b), std::max(a, c))) + std::min(std::max(a, b), std::max(b, c)) << std::endl;
  }
}
