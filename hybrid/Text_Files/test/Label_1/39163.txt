t = int(input().strip())
for _ in range(t):
    s = input().strip()
    half_len = len(s) // 2
    if len(s) % 2 == 0 and s[:half_len] == s[half_len:]:
        print("YES")
    else:
        print("NO")
