from collections import deque
for _ in range(int(input())):
    n = int(input())
    s = input()
    d = deque()
    for c in s:
        if d and c == '0' and d[-1] == '1':
            d.pop()
        else:
            d.append(c)
    print(''.join(d))
