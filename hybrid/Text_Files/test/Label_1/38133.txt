from collections import deque
from sys import stdin
input=stdin.readline

def dfs(u, col):
    visited[u]=col
    for v in graph[u]:
        if not visited[v]:
            if dfs(v,3-col)==False:
                return False
        elif visited[v]==col:
            return False
    return True

n,m=map(int,input().split())
arr=list(map(int,input().split()))
graph=[[] for i in range(n)]
for i in range(m):
    a,b=map(int,input().split())
    graph[b].append(a)
visited=[0]*n
ans=0
for i in range(n):
    if not visited[i]:
        if dfs(i,1)==False:
            print(-1)
            exit()
        ans+=1
print(ans)
