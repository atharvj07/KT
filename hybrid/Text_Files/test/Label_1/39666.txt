import math

n, m = list(map(int, input().split()))
changes = []
for i in range(m):
    changes.append(list(map(int, input().split())))

maxAvg = 0
for x, d in changes:
    maxAvg += x + d * (n - 1) / 2

print(maxAvg / n)
