#include <iostream>
int main(){
    long long l, r, k, i=0;
    std::cin >> l >> r >> k;
    while((l<<i)<=r){
        if((l<<i)>=l){
            std::cout << (l<<i) << " ";
        }
        i++;
    }
    return 0;
}
