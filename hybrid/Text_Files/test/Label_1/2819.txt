#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    int ans = 1;
    while (n) {
        if (n % 10) ans *= (n % 10);
        n /= 10;
    }
    cout << ans << '\n';
}
