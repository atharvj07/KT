from collections import defaultdict
n = int(input())
d, u = defaultdict(list), []
for i in range(n-1):
    x, y = map(int, input().split())
    d[x].append(y)
    d[y].append(x)
def dfs(i):
    u.append(i)
    for j in d[i]:
        if j not in u: dfs(j)
dfs(1)
print(*u)
