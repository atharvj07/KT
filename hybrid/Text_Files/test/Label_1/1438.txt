#include <bits/stdc++.h>
using namespace std;
long long n, m, k;
int main()
{
    cin >> n >> m >> k;
    if (n * m < k + 1)
        cout << -1;
    else if (n < m)
        cout << ((n - 1) * m) / (k + 1);
    else
        cout << ((m - 1) * n) / (k + 1);
}
