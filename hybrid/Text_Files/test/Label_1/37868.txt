from sys import stdin, stdout

n = int(stdin.readline().strip())
a = [int(x) for x in stdin.readline().split()]

a.sort()
mn = float("inf")
ans = float("inf")
for t in range(a[0],a[-1]+2):
	cost = 0
	for i in a:
		cost += abs(t-i)
	if cost < mn:
		mn = cost
		ans = t
print(ans,mn)
