import sys
input = sys.stdin.readline

n,k = map(int,input().split())
cards = input().strip()

if(cards[0]==cards[-1]):
    print('tokitsukaze' if k%2 else 'once again')
else:
    print('once again')
