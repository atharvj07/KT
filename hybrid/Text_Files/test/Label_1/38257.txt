import sys
data = []
for line in sys.stdin:
    data.append(line.strip())
count = {}
for num in data[1:]:
    if num not in count: count[num] = 0
    count[num] += 1
print(" ".join(str(count.get(k, 0)) for k in data[1:]))
