#include <iostream>
using namespace std;
int main() {
  long long n;
  cin >> n;
  if (n & 1)
    cout << -1 << endl;
  else {
    for (long long i = n; i > 0; i--) cout << ((i + 1) / 2) << ' ';
    cout << endl;
  }
  return 0;
}
