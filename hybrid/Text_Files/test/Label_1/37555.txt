import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = [input().rstrip() for _ in range(n)]
    print("YES" if sum(len(set(i)) for i in a) <= (len(a) - 1) * 26 else "NO")
