#include <cstdio>
#include <cmath>

long long n, k;

int main() {
    scanf("%lld %lld", &n, &k);
    if (n <= k) printf("1\n");
    else if (k == n + 1) printf("2\n");
    else if (n <= ceil(log2(k) * k)) printf("3\n");
    else printf("-1\n");
}
