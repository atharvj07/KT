#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) cin >> v[i];
    int ans = 0, mn = *min_element(v.begin(), v.end());
    for (int x : v) ans += max(0, -x - mn);
    cout << ans << endl;
}
