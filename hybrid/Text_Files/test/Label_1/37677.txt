import sys, math
input = sys.stdin.readline

n = int(input())
a = list(map(int,input().split()))
can = False
for i in range(1,n):
    if math.gcd(a[i],a[i-1])==1: can = True
if can: print("Yes")
else: print("No")
