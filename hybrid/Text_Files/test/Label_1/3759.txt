#include<bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  set<int> s;
  for(int a, i = 0; i < n; i++) {
    cin >> a;
    s.insert(a);
  }
  int ans = 0;
  while(s.count(++ans));
  cout << ans << endl;
}
