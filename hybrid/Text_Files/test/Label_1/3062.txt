#include <iostream>
#include <vector>
using namespace std;
int main() {
  int a, b;
  cin >> a >> b;
  if (a <= b) {
    cout << 2 * (b - a) + 1 << endl;
  } else {
    cout << 2 * (a - b) << endl;
  }
  return 0;
}
