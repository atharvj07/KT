import collections
for _ in range(int(input())):
    n=int(input())
    a=[int(i) for i in input().split()]
    cnt=collections.Counter(a)
    b=sorted(cnt.keys())
    ans=0
    for i in range(1,len(b)):
        ans+=cnt[b[i-1]]<=cnt[b[i]]
    print(ans)
