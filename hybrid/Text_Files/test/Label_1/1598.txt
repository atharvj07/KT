#include<bits/stdc++.h>
#define vi vector<long long int>
using namespace std;
int main() {
    vi v;
    long long int x;
    while (cin >> x) { v.push_back(x); }
    sort(v.begin(), v.end());
    cout << v[0] << endl;
    return 0;
}
