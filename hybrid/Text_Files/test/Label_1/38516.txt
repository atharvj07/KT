c1, c2, c3, c4 = [int(x) for x in input().strip().split()]
n, m = [int(x) for x in input().strip().split()]
a = [int(x) for x in input().strip().split()]
b = [int(x) for x in input().strip().split()]

print(min(
    sum(a) * c1 + sum(b) * c1,
    max(sum(a) * c2, sum(b) * c2) + min(sum(a), sum(b)) * c1,
    sum(a) * c3 + sum(b) * c3,
    c4
))
