n, m = map(int, input().split())
a = sorted(map(int, input().split()))
ans = pow(a[0] - a[n - 1], n * (n - 1) // 2, m)
for i in range(1, n):
    ans = pow(ans * (a[i] - a[i - 1]), n - i - 1, m)
print(ans)
