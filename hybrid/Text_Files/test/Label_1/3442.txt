#include <cstdio>
#include <algorithm>
using namespace std;

int main() {
    int n;
    scanf("%d", &n);
    int a[n];
    int Min = 1e9;
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
        Min = min(Min, abs(a[i]));
    }
    printf("%d\n", Min);
    return 0;
}
