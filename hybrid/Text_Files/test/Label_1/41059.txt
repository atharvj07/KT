import sys

t = int(input())

for i in range(t):
  n = int(input())
  k = 0
  lst = []
  while n > 0:
    k += 1
    if n % 10 == 0:
      lst.append(pow(10, len(str(n)) - 1))
      n -= pow(10, len(str(n)) - 1)
    else:
      lst.append(n)
      n -= n
  print(k)
  print(*lst)
