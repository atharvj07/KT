#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,x,ans=0;
    cin >> n >> x;
    int d[n];
    for(int i=0 ; i<n ; i++) cin >> d[i];
    for(int i=0 ; i<n ; i++){
        int cur=min(x,d[i]);
        x-=cur;
        ans+=cur*(cur+1)/2;
    }
    cout << ans;
    return 0;
}
