n, M = [int(x) for x in input().split()]
t = [int(x) for x in input().split()]
p = [0] * n
s = 0
for i in range(n - 1, -1, -1):
    s += t[i]
    if s > M:
        p[i] = M // t[i]
        s -= t[i] * p[i]
print(*p)
