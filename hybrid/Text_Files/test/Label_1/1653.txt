#include <iostream>
using namespace std;
int main() {
    long long n, m, kmin, kmax;
    cin >> n >> m;
    kmin = (m - 1) * m / 2;
    kmax = n * (n - 1) / 2;
    cout << kmin << ' ' << kmax << endl;
}
