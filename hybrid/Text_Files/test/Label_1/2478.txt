#include<bits/stdc++.h>
using namespace std;
int main() {
    int n, x, y, a = 0, b = 0;
    cin >> n;
    while (n--) {
        cin >> x >> y;
        a += x; b += y;
    }
    if (a % 2 == b % 2) cout << 0 << endl;
    else cout << -1 << endl;
    return 0;
}
