from sys import stdin
from bisect import bisect_left

input = stdin.readline
n = int(input())
a = list(map(int, input().split()))
a.sort()
ans = 0

for i in range(n-1):
    p = bisect_left(a, -a[i] / 2)
    ans = max(ans, abs(a[p] + a[i]) / 2)

print(ans)
