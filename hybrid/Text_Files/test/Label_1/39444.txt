n = int(input())
a, b, c = 10**9, 10**9, 10**9
for i in range(n):
    ci, si = input().split()
    ci = int(ci)
    for j in si:
        if j == 'A': a = min(a, ci)
        elif j == 'B': b = min(b, ci)
        else: c = min(c, ci)
ans = a + b + c
if ans >= 10**9: ans = -1
print(ans)
