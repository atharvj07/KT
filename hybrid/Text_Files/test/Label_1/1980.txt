#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k, a;
  cin >> n >> k;
  unordered_map<int, int> m;
  while (n--) { cin >> a;
    m[a]++;
  }
  int ans = k * 2;
  for (auto x : m) { ans = min(ans, x.second); }
  cout << k - ans;
}
