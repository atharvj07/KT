import sys
input = sys.stdin.readline

n,k = map(int,input().split())
a = list(map(int,list(input().rstrip())))

ans = 0
x,y = 0,0
for i in range(1,n):
    if a[i]!=a[x]:
        x = i
    if a[i]!=a[y]:
        y = i
        ans = max(ans,y-x-1)
print(ans)
