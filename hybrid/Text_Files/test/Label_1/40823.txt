import sys

# Accept multiple test cases
t = int(input())

# Iterate over each test case
for _ in range(t):
    # Accept the number of council members
    n = int(input())

    # Accept the heights of the council members
    a = [int(x) for x in input().split()]

    # Sort the heights in ascending order
    a.sort()

    # Output the sorted heights
    print(*a)
