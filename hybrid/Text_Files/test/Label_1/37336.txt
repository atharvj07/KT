def can_form_codeforces(banner):
    target = "CODEFORCES"
    n = len(banner)
    m = len(target)
    for i in range(m + 1):
        if banner[:i] + banner[i + n - m:] == target:
            return "YES"
    return "NO"

banner = input().strip()
print(can_form_codeforces(banner))
