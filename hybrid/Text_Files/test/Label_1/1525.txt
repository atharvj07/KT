#include<bits/stdc++.h>
using namespace std;
int main() {
  int x, y, a, b, n = 0;
  cin >> x >> y >> a >> b;
  for (int i = a; i <= x; i++) {
    for (int j = b; j <= y; j++) {
      if (i > j) {
        n++;
        cout << i << ' ' << j << '\n';
      }
    }
  }
  cout << n << '\n';
}
