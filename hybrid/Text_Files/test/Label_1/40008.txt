def find_optimal_string(t):
    if len(set(t)) == 1:
        return t
    else:
        return (t[0] + ('1' if t[0] == '0' else '0')) * len(t)

T = int(input().strip())
for _ in range(T):
    t = input().strip()
    print(find_optimal_string(t))
