n = int(input())
s = input()
room = [0] * 10
for ch in s:
    if ch in 'LR':
        for i in range(10):
            if not room[i]:
                room[i] = 1 if ch == 'L' else -1
                break
    else:
        i = int(ch)
        room[i] = 0
print(''.join(map(str, room)))
