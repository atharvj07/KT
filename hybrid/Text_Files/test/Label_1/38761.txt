import sys

n = int(sys.stdin.readline())
a = list(map(int, sys.stdin.readline().split()))
a.sort()

ans = float('inf')
for i in range(n):
    ans = min(ans, abs(a[i] + a[(i+1)%n] - 180))
    ans = min(ans, abs(a[i] - a[(i+1)%n]))

print(ans)
