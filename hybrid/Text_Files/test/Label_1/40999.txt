q, x = map(int, input().split())
mod_counts = [0] * x
mex = 0

for _ in range(q):
    y = int(input())
    mod_counts[y % x] += 1
    while mod_counts[mex % x] > 0:
        mod_counts[mex % x] -= 1
        mex += 1
    print(mex)
