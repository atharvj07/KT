#include <iostream>

using namespace std;

int main() {
  int n, a, x, b, y;
  cin >> n >> a >> x >> b >> y;
  cout << ((2 * (a > x ? b : x) > n + a + b - x - y && 2 * (a < x ? b : x) <= n + a + b - x - y) ? "YES" : "NO") << endl;
}
