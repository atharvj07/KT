#include <iostream>
using namespace std;
int main() {
  int q;
  cin >> q;
  while (q--) {
    int n;
    cin >> n;
    if (n % 2 == 1 or n <= 3) {
      cout << -1 << endl;
      continue;
    }
    n -= 2;
    if (n % 2 == 0)
      n /= 2;
    cout << n << endl;
  }
  return 0;
}
