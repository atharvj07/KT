#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    std::string s;
    std::cin >> s;
    int ans = 0;
    for (int i = 0; i + 1 < s.size(); i++) {
      if (s[i] == s[i + 1]) {
        ans++;
        i++;
      }
    }
    std::cout << ans << "\n";
  }
  return 0;
}
