import sys
input = sys.stdin.readline

n, k, x = map(int, input().split())
a = sorted([int(x) for x in input().split()])
i, j, ans = 0, 0, 0
while i < n or j < k:
    if j < k and a[i] - x > a[j]:
        j += 1
        ans += 1
    i += 1
print(ans + (n - i + x - 1) // x)
