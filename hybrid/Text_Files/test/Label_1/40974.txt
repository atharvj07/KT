for t in range(int(input())):
    n,x=map(int,input().split())
    a=list(map(int,input().split()))
    c=a.copy()
    for q in a:
        if q%x:break
        for i in range(q//x):c+=q//x
    print(sum(c))
