#include<bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << max({abs(a - b), abs(b - c), abs(c - a)}) / d + (max({abs(a - b), abs(b - c), abs(c - a)}) % d != 0);
    return 0;
}
