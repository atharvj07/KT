#include <bits/stdc++.h>
using namespace std;
int a[100005];
int main() {
  int n, m = 0, x = 1e9;
  cin >> n;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
    if (a[i] > m) {
      x = i;
      m = a[i];
    }
  }
  cout << x << endl;
  return 0;
}
