n, m = map(int, input().split())
a = [int(x) for x in input().split()]
used = []
for x in a:
    if x in used:
        print(0, end='')
    else:
        used.append(x)
        if len(used) == n:
            used = []
            print(1, end='')
        else:
            print(0, end='')
