import sys

for _ in range(int(input())):
    n, k = map(int, input().split())
    arr = list(map(int, input().split()))
    arr.sort()
    ans = arr[n - 1]
    for i in range(min(n - 1, k)):
        ans += arr[i]
    print(ans)
