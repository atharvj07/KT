import math

n = int(input())
x, y = map(int, input().split())

dist_white = x + (n - y)
dist_black = n - x + (n - y)

if dist_white % 2 == dist_black % 2:
    print("White" if dist_white < dist_black else "Black")
else:
    print("White" if dist_white > dist_black else "Black")
