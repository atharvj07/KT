n,k=map(int,input().split())
a=list(map(int,input().split()));b=input()
if ((b.count('R')==0 or b.count('W')==0 or b.count('O')==0) and n>k and k%2==0): print(-1)
else:
    a.sort(reverse=True)
    st="";ot="";rt=""
    ans=[]
    for i in range(n):
        if b[i]=='W': st+=str(a[i])
        elif b[i]=='O': ot+=str(a[i])
        else: rt+=str(a[i])
    if st!="" and ot!="" and rt!="":
        if len(st)%2==0 and len(ot)%2==0 and len(rt)%2==0:
            st=st[::-1];ot=ot[::-1];rt=rt[::-1]
            ans=list(st);ans+=list(ot);ans+=list(rt)
        elif len(st)%2==0 and len(ot)%2==0 and len(rt)%2:
            st=st[::-1];ot=ot[::-1]
            ans=list(st);ans+=list(rt);ans+=list(ot)
        elif len(st)%2==0 and len(ot)%2 and len(rt)%2:
            st=st[::-1]
            ans=list(st);ans+=list(ot);ans+=list(rt)
        elif len(st)%2 and len(ot)%2 and len(rt)%2:
            st=st[::-1];rt=rt[::-1]
            ans=list(st);ans+=list(rt);ans+=list(ot)
        elif len(st)%2 and len(ot)%2 and len(rt)%2==0:
            ot=ot[::-1];rt=rt[::-1]
            ans=list(ot);ans+=list(rt);ans+=list(st)
        else: print(-1);exit()
    elif st!="" and ot=="" and rt!="":
        if len(st)%2==0 and len(rt)%2==0:
            st=st[::-1];rt=rt[::-1]
            ans=list(st);ans+=list(rt)
        elif len(st)%2 and len(rt)%2:
            ans=list(st);ans+=list(rt)
        else: print(-1);exit()
    elif st!="" and ot=="" and rt=="": print(-1);exit()
    elif st=="" and ot!="" and rt!="":
        if len(ot)%2==0 and len(rt)%2==0:
            ot=ot[::-1];rt=rt[::-1]
            ans=list(ot);ans+=list(rt)
        elif len(ot)%2 and len(rt)%2:
            ot=ot[::-1]
            ans=list(ot);ans+=list(rt)
        else: print(-1);exit()
    elif st=="" and ot!="" and rt=="": print(-1);exit()
    elif st=="" and ot=="" and rt!="": ans=list(rt)
    elif st!="" and ot=="" and rt=="": ans=list(st)
    ans=ans[:k]
    print(sum(ans))
