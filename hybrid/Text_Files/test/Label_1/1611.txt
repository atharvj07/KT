#include<bits/stdc++.h>
using namespace std;
int main() {
    long long n, k;
    cin >> n >> k;
    for (long long x = 1; ; ++x)
        if ((x / k) * (x % k) == n) {
            cout << x << "\n";
            return 0;
        }
}
