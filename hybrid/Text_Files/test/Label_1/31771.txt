import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        long k = input.nextLong(), a = input.nextLong(), b = input.nextLong();
        System.out.println((b / k) - (a - 1) / k);
    }
}
