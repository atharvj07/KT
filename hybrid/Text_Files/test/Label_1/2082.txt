#include <cstdio>
int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        int r, b, k;
        scanf("%d %d %d", &r, &b, &k);
        if (r == 1 || b == 1 || r % k == 0 || b % k == 0)
            printf("REBEL\n");
        else
            printf("OBEY\n");
    }
    return 0;
}
