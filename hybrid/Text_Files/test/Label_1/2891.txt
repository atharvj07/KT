#include<bits/stdc++.h>
using namespace std;
int main()
{
  long long a,b,red,green,blue;
  cin>>a>>b;
  red=ceil(((double)2*a)/b);
  green=ceil(((double)5*a)/b);
  blue=ceil(((double)8*a)/b);
  cout<<red+green+blue<<endl;
  return 0;
}
