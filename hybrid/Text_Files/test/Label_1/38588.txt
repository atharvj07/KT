import sys

t = int(input())
for _ in range(t):
    a = input()
    b = list(a)
    n = len(b)
    for i in range(n - 1):
        if (int(b[i]) + int(b[i + 1])) % 2 == 1:
            b[i], b[i + 1] = b[i + 1], b[i]
    print(''.join(b))
