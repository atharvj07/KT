#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &x : a) cin >> x;
  int ans = -1;
  for (int i = 0; i < n - 1; ++i)
    if (a[i + 1] - a[i] > 1) ans = 1;
  cout << ans << '\n';
  return 0;
}
