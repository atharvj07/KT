t = int(input().strip())
for _ in range(t):
    n = int(input().strip())
    reviewers = list(map(int, input().split()))
    upvotes = reviewers.count(1)
    undecided = reviewers.count(3)
    print(upvotes + (undecided + 1) // 2)
