#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int r, g, b;
    cin >> r >> g >> b;
    if (r > (g + b - 1) || g > (r + b - 1) || b > (r + g - 1))
      cout << "No" << endl;
    else
      cout << "Yes" << endl;
  }
}
