import sys
input = sys.stdin.readline
n, a, b, k = map(int, input().split())
h = list(map(int, input().split()))
h.sort(reverse=True)
ans = 0
for x in h:
  if k > 0 and x>b:
    k -= 1
    ans += 1
  else:
    if x > a:
      ans += 1
print(ans)
