n = int(input())
sticks = list(map(int, input().split()))

costs = {}
for t in range(1, 102):
    cost = sum(abs(stick - t) if abs(stick - t) <= 1 else min(abs(stick - t + 1), abs(stick - t - 1)) for stick in sticks)
    costs[t] = cost

opt_t = min(costs, key=costs.get)
print(opt_t, costs[opt_t])
