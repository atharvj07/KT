import sys
input = sys.stdin.readline

n = int(input())
arr = [int(x) for x in input().split()]

arr.sort()
ans = 0
prev = -1e9
for x in arr:
    if x != prev:
        ans += 1
    prev = x
print(ans)
