import sys

def solve():
    n = int(input())
    a = list(map(int, input().split()))
    if sum(a) < n - 1:
        print("NO")
        return
    print("YES", n - 1)
    print(n - 1)
    for i in range(2, n + 1):
        sys.stdout.write("1 " + str(i) + "\n")

solve()
