from sys import stdin

line = stdin.readlines()
n = int(line[0])
st = ""
for i in range(1, n + 1):
    l, r = map(int, line[i].split())
    if l == r:
        st += "()"
    else:
        st += "(" + st + ")" * (r - l - 1)
print(st)
