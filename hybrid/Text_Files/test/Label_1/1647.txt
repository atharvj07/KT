#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, l, r;
    cin >> n >> l >> r;
    if (n % l == 0 || (n % r == 0 && l <= r - n % r))
      cout << "Yes" << endl;
    else
      cout << "No" << endl;
  }
  return 0;
}
