from collections import defaultdict
for t in range(int(input())):
    n = int(input())
    s = input()
    a = defaultdict(int)
    ans = 0
    for i in range(n):
        a[s[i]] += 1
    for c in ['R', 'L']:
        ans += max(0, a[c] - (n + 1) // 2)
    print(ans)
