#include <cstdio>
int main() {
  int n, a[100005], b = 0;
  scanf(" %d", &n);
  for (int i = 0; i < n; i++) scanf(" %d", &a[i]);
  for (int i = 0; i < n; i++) {
    int t = a[i] - 25;
    if (b < t) {
      puts("NO");
      return 0;
    }
    b -= t;
    b += 25;
  }
  puts("YES");
  return 0;
}
