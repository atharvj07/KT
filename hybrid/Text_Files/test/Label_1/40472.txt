n, k = map(int, input().split())
a = list(map(int, input().split()))
b = list(map(int, input().split()))
s, sm, mx = 0, sum(b), 0
for i in range(n):
    s += a[i]
    sm -= b[i]
    if s * k <= sm + b[i] * k:
        mx = s
if mx == 0:
    print(-1)
else:
    print(mx)
