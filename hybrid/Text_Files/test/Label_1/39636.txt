import math

for _ in range(int(input())):
    n, k = map(int, input().split())
    a = math.floor((n - 1) / (k - 1))
    if a > 0 and n > k:
        print(n - k + min(n % k, math.ceil(k / 2)))
    else:
        print(n)
