#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a,b;
    cin>>a>>b;
    set<char>u,v;
    for(auto x:a) u.insert(x);
    for(auto x:b) v.insert(x);
    if(u==v) cout<<-1<<endl;
    else cout<<max(a.length(),b.length())<<endl;
}
