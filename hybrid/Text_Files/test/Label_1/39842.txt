from sys import stdin

a = []
b = []
for _ in range(int(input())):
  n = int(input())
  p = [int(x) for x in input().split()]
  m = int(input())
  q = [int(x) for x in input().split()]
  ans = 0
  for x in p:
    for y in q:
      if x-y > 0 and x-y-min(x-y,y)<n and x-y-min(x-y,y)>=0:
        ans += 1
  print(ans)
