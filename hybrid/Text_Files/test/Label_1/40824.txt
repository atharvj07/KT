from fractions import Fraction

k = int(input())
a = list(map(int, input().split()))

ans = Fraction(1, 2)
for i in a:
    ans *= Fraction(i - i % 4, i)

num, den = ans.numerator, ans.denominator
print(str(num % (10 ** 9 + 7)) + '/' + str(den % (10 ** 9 + 7)))
