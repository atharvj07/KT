#include <bits/stdc++.h>
using namespace std;
int a[50005];
int main() {
    int n;
    cin >> n;
    int mm = 0;
    for (int i = 1; i <= n; i++) cin >> a[i], mm += a[i];
    mm /= n;
    int ans = 0;
    for (int i = 1; i <= n; i++) if (a[i] > mm) ans += a[i] - mm;
    cout << ans << endl;
}
