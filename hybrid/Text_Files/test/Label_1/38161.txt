import collections
n,m=map(int,input().split())
id=collections.deque()
for i in input().split():
    if i in id:
        id.remove(i)
        id.append(i)
    else:
        if len(id)==m:
            id.popleft()
        id.append(i)
print(len(id),*id)
