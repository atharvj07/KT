import numpy as np
n,m = map(int, input().split())
a = np.array([[int(x) for x in y.split()] for y in [input() for _ in range(n)]])
x,s = a[:,1], a[:,0]
idx = np.argsort(x)
t = [s[idx[i]] >= s[idx[max(i+1,len(idx)-1)]] for i in range(len(idx)-1)]
print(sum(t))
