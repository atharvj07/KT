t = int(input())
for _ in range(t):
    n = input()
    length = len(n)
    ans = length
    for i in range(length):
        for j in range(i+1, length):
            if (int(n[i]) * 10 + int(n[j])) % 25 == 0:
                ans = min(ans, (length-j-1) + (j-i-1))
    print(ans)
