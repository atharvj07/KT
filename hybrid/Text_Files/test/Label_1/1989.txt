#include<bits/stdc++.h>
using namespace std;
int main() {
    int a, b;
    cin >> a >> b;
    int ans;
    while (a > 0 && b > 0) {
        if (a < b) swap(a, b);
        a -= 2, b++;
        ans++;
    }
    cout << ans << endl;
    return 0;
}
