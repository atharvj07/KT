#include <iostream>
#include <string>
using namespace std;

int main() {
  string s;
  cin >> s;
  int a = 0, b = 0;
  for (char c : s) {
    if (c == 'a') a++;
    else b++;
  }
  cout << min(a, b) * 2 << "\n";
  return 0;
}
