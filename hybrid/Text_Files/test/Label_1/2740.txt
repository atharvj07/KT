#include <bits/stdc++.h>
using namespace std;
int n, ans;
char s[105];
int main() {
  cin >> n >> s;
  for (int i = 0; i < n; i++) {
    if (s[i] == '?') continue;
    if (i > 0 && s[i] == s[i - 1]) ans = 1;
  }
  if (ans == 1 || n <= 2) cout << "No" << endl; else cout << "Yes" << endl;
}
