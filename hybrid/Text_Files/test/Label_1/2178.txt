#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << (a ? "Ya " : "Tidak ") << (b ? "Ya " : "Tidak ")
         << (c ? "Ya " : "Tidak ") << (d ? "Ya" : "Tidak") << endl;
  }
  return 0;
}
