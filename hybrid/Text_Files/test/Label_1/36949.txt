for _ in range(int(input())):
    n = int(input())
    A = list(map(int, input().split()))
    B = [0] * n
    for i in range(n):
        B[(i - A[i] + n) % n] = (i + A[i]) % n
    print("YES" if A == B else "NO")
