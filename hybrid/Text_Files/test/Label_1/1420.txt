#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long x, y;
    cin >> x >> y;
    cout << (((y - x) % 3 == 0) | ((x - y) % 3 == 0) | (x == y) ? "YES" : "NO") << endl;
  }
  return 0;
}
