n, s = map(int, input().split())
l = sorted([tuple(map(int, input().split())) for i in range(n)])
now = 0
for x, y in l:
    if now + s <= x * 60 + y:
        print(x, y)
        break
    now = x * 60 + y + 1
else:
    mm = l[-1][0] * 60 + l[-1][1] + 60
    print(mm // 60, mm % 60)
