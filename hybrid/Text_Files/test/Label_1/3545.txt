#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, k;
    cin >> n >> k;
    if (k % (2 * n) >= n)
      cout << (k % (2 * n)) - n << endl;
    else
      cout << (k % (2 * n)) << endl;
  }
  return 0;
}
