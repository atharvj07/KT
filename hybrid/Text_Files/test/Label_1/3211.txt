#include <iostream>
#include <string>

int main() {
    std::string name;
    std::cin >> name;
    std::cout << ((name.find("Danil") != -1) || (name.find("Olya") != -1) || (name.find("Slava") != -1) || (name.find("Ann") != -1) || (name.find("Nikita") != -1) ? "YES" : "NO") << std::endl;
    return 0;
}
