for _ in range(int(input())):
    n,k = map(int, input().split())
    if k == 1:
        print(n)
        continue
    if n % k <= k // 2:
        print(n - n % k)
    else:
        print(n - n % k + k // 2)
