from collections import Counter
s = input()
n = int(input())
t = Counter(s)
if all(t[x] <= n for x in t):
    print((len(s)+n-1)//n)
    print(''.join(sorted([x]*t[x] for x in t.keys())))
else:
    print(-1)
