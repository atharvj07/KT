#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;
    int c = 0, ans = 0;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] != s[i % 2 == 0 ? 0 : 1]) c++;
        ans = max(ans, c);
    }
    cout << ans << endl;
    return 0;
}
