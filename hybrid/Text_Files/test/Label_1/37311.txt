['from sys import stdin\ninput = stdin.readline\n\nn, m = map(int, input().split())\na = [0] * n\nfor i in range(m):\n    x, d = map(int, input().split())\n    for j in range(n):\n        a[j] += x + d * abs(j - (i % n))\n\nprint(sum(a) / n)\n']