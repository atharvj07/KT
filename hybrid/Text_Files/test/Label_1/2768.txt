#include <iostream>

using namespace std;

int main() {
  int n;
  cin >> n;
  int t[n];
  for (int i = 0; i < n; i++) cin >> t[i];
  int ans = 15, j = 0;
  for (int i = 0; i < n; i++) {
    if (t[i] - t[j] < 15) continue;
    j = i;
    ans += 15;
  }
  cout << ans << endl;
  return 0;
}
