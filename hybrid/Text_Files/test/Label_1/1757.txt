#include <iostream>

using namespace std;

int main() {
    int n, p;
    cin >> n >> p;
    long double pw = 1.0 - (1.0 - 1.0 / n) * (1.0 - 1.0 / n);
    int ans = 0;
    while (pw < p) {
        ans++;
        pw += 1.0 - (1.0 - 1.0 / n) * (1.0 - 1.0 / n);
    }
    cout << ans << endl;
}
