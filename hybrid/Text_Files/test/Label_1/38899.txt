def solve():
    T = int(input())
    for _ in range(T):
        a, b = map(int, input().split())
        diff = abs(b - a)
        num = 0
        if diff % 5 == 0:
            num = diff // 5
        else:
            num = diff // 5 + 1
        print(num)

solve()
