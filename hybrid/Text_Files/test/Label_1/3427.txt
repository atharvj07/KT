#include <iostream>

int main() {
  std::string s;
  std::cin >> s;
  int ans = 0;
  for (char c : s) {
    if (c == 'A' || c == 'E' || c == 'I' || c == 'O' || c == 'U' || c == 'Y') {
      ans = std::max(ans, c - 'A' + 1);
    }
  }
  std::cout << ans << std::endl;
  return 0;
}
