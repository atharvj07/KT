#include <bits/stdc++.h>
using namespace std;
int main() {
	int n, x, T = -1;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> x;
		if (T == -1) T = x;
		else T = __gcd(T, x);
		if (T == 1) {
			cout << -1;
			return 0;
		}
	}
	cout << T;
}
