#include <iostream>
using namespace std;
int main() {
  long long n, k;
  cin >> n >> k;
  long long ans = 0;
  while (n > 1) {
    ans++;
    n = (n + k - 1) / k;
  }
  if (n == 1)
    cout << ans << '\n';
  else
    cout << -1 << '\n';
  return 0;
}
