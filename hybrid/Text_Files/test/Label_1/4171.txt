#include <iostream>

int main() {

  int n;
  std::cin >> n;
  std::string s;
  std::cin >> s;
  int t = 0;
  for (auto c : s) {
    t += (c == 'S');
  }
  std::cout << ((t > n - t) ? "YES" : "NO") << '\n';
  return 0;
}
