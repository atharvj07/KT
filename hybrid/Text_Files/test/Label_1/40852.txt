import sys

for case in range(int(sys.stdin.readline())):
    a, b, c, d = map(int, sys.stdin.readline().split())
    print("Ya" if a else "Tidak", "Ya" if b else "Tidak", "Ya" if c else "Tidak", "Ya" if d else "Tidak")
