n, A = map(int, input().split())
d = list(map(int, input().split()))

s = sum(d)
for i in range(n):
    min_possible = max(1, A - (s - d[i]))
    max_possible = min(d[i], A - n + 1)
    print(d[i] - (max_possible - min_possible + 1), end=" ")
