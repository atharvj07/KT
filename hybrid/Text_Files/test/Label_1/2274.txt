#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<long long> a(n);
  long long ans = 0;
  for (auto &x : a) {
    cin >> x;
    ans += __builtin_popcountll(abs(x));
  }
  if (n != __builtin_popcountll(ans) && ans % 2)
    ++ans;
  cout << ans << '\n';
}
