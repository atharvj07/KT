#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long n;
    std::cin >> n;
    if (n % 3 == 0) std::cout << (n / 3) * (n / 3 - 1) / 2 << std::endl;
    else std::cout << 0 << std::endl;
  }
  return 0;
}
