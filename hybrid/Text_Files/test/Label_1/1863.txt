#include<bits/stdc++.h>
using namespace std;
int main() {
    int q, a, b, c;
    cin >> q;
    while(q--) {
        cin >> a >> b >> c;
        cout << min({a, b, (a+c+b)/2}) << endl;
    }
    return 0;
}
