#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main(){
    ll n,m;
    cin>>n>>m;
    unordered_map<ll,bool>mp;
    for(ll i=0; i<m; ++i){
        ll x;cin>>x;
        mp[x]=true;
    }
    if(mp[1]||mp[n]){cout<<"NO";return 0;}
    cout<<"YES";
}
