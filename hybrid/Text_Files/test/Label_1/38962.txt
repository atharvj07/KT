n, a = map(int,input().split())
d = list(map(int,input().split()))
d.sort(reverse=True)
for i in range(n):
    if a <= 0:
        break
    b = min(d[i],(a+i)//(i+1))
    a -= b
    d[i] -= b
    print(b)
