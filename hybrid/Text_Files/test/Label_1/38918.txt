from collections import deque
n = int(input())
p = list(map(int, input().split()))
a = list(map(int, input().split()))
q = deque([(1, a[0])])
ans = 0
for _ in range(n-1):
    x, ai = q.popleft()
    ans += ai
    q.append((p[x], ai))
    if a[x]: q.append((p[x], a[x]-1))
print(ans)
