from collections import defaultdict

for t in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    d = defaultdict(int)
    for x in a:
        d[x] += 1
    print(d[max(a)])
