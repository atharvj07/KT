#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, k1, k2;
  cin >> n >> m >> k1 >> k2;
  if (max(k1, k2) * (max(k1, k2) + 1) / 2 <= min(n, m)) cout << "Second" << endl;
  else cout << "First" << endl;
  return 0;
}
