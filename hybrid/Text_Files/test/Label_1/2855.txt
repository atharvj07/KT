#include <iostream>

using namespace std;

const long long MOD = 1e9 + 7;

int main() {
  long long n, k;
  cin >> n >> k;
  long long ans = 1;
  for (long long i = 1; i < n; i++) {
    ans = (ans * (k - i)) % MOD;
  }
  cout << ans << endl;
  return 0;
}
