n = int(input())
a = [int(x) for x in input().split()]
a.sort()
ans = 0
for i in range(n):
    for j in range(n):
        if 2 <= i < j and a[i] * a[j] > ans:
            ans = a[i] * a[j]
print(ans)
