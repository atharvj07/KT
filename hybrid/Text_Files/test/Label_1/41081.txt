n, k = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans_type = 1
ans = n // a[0]
for i in range(1, k):
    cur = n // a[i]
    if cur > ans:
        ans = cur
        ans_type = i + 1
print(ans_type, ans)
