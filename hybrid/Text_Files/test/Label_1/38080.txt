n,k=map(int,input().split())
a=[int(x) for x in input().split()]
r,i=list(),0
for x in a:
    if x not in r: r.append(x)
    i=r.index(x)
    r.pop(i)
    r=[x]+r
    if len(r)>k: r.pop()
print(len(r))
print(*r)
