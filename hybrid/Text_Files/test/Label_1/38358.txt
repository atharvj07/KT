import sys

def check(n):
    return (int(n[:-2]) * 100 + int(n[-2:])) % 60 == 0

for line in sys.stdin.readlines()[1:]:
    line = line.strip()
    if line[:-2] == '0' * len(line[:-2]) or check(line):
        print('red')
    else:
        print('cyan')
