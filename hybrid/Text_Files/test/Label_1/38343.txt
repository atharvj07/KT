import sys
input = sys.stdin.readline
mod = 1000000007

n = int(input())
adj = [[] for _ in range(n + 1)]
for _ in range(n - 1):
    u, v = map(int, input().split())
    adj[u].append(v)
    adj[v].append(u)

x = list(map(int, input().split()))
f = [0] * (n + 1)
def dfs(u, p, beauty):
    f[u] = beauty
    for v in adj[u]:
        if v != p: dfs(v, u, math.gcd(beauty, x[v - 1]))

dfs(1, 0, x[0])
ans = sum(f) % mod
print(ans)
