import sys
input=sys.stdin.readline

n,m=map(int,input().split())
l=[]
for i in range(m):
    x,d=map(int,input().split())
    l.append((x,d))
l.sort(key=lambda x:x[1])
s=0
ans=0
for x,d in l:
    ans+=(x+s)*(n-1)
    s+=x+s
ans+=s*n
print(ans/n**2)
