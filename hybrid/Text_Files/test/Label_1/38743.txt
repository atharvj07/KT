from collections import deque
q = int(input())
books = deque()
for _ in range(q):
    a,b = input().split()
    if a=='L':
        books.appendleft(b)
    elif a=='R':
        books.append(b)
    elif a=='?':
        print(min(books.count(b),len(books)-books.count(b)))
