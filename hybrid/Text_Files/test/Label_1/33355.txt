import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt(), m = in.nextInt(), i = in.nextInt(), j = in.nextInt(), a = in.nextInt(), b = in.nextInt();
        if (((i + a - 1) % a != 0 || (j + b - 1) % b != 0) && ((i - a + 1) % a != 0 || (j - b + 1) % b != 0)) {
            System.out.println("Poor Inna and pony!");
        } else {
            System.out.println(((i - 1) / a + (j - 1) / b + 1) % 2);
        }
    }
}
