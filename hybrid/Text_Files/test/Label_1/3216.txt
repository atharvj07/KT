#include <iostream>

int main() {
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    if ((d - b) % (a + c) == 0 && (d - b) >= 0) {
        std::cout << d + ((d - b) / (a + c)) * (a + c) << std::endl;
    } else {
        std::cout << -1 << std::endl;
    }
    return 0;
}
