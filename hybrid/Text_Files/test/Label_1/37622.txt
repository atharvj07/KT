n = int(input())
a = list(map(int, input().split()))

def d(x, y):
    if abs(x - y) > 1:
        return y - x
    return 0

total_sum = 0
for i in range(n):
    for j in range(i, n):
        total_sum += d(a[i], a[j])

print(total_sum)
