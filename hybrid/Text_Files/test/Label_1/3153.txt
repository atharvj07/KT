#include <cstdio>

using namespace std;

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        long long int x, y, n;
        scanf("%lld%lld%lld", &x, &y, &n);
        printf("%lld\n", n - n % x + y);
    }
    return 0;
}
