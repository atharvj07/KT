#include <iostream>

using namespace std;

int main() {
    int n, a, x, b, y;
    cin >> n >> a >> x >> b >> y;
    if (a == b or a == y or x == b or x == y)
        cout << "YES" << endl;
    else
        cout << "NO" << endl;
}
