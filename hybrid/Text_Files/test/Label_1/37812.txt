from sys import stdin

q = int(stdin.readline())
for i in range(q):
    n, k = map(int, stdin.readline().split())
    s = stdin.readline().strip()
    t = 'R'*k + 'G'*k + 'B'*k
    ans = 0
    for j in range(k):
        if s[(j-1)%n] != t[j]:
            ans += 1
    print(ans)
