#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef vector<int> vi;
#define range(i,a,b) for(int i = (a); i < (b); i++)
#define rep(i,n) range(i,0,n)
#define all(x) (x).begin(), (x).end()
#define rall(x) (x).rbegin(), (x).rend()
int main() {
	int n, k;
	cin >> n >> k;
	string x;
	cin >> x;
	while (true) {
		int m = x.size();
		bool ok = true;
		range (i, k, m) {
			if (x[i] != x[i - k]) ok = false;
		}
		if (ok) break;
		x.push_back('0');
	}
	cout << x.size() << endl;
	cout << x << endl;
}
