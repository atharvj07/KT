#include <iostream>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  int ans = 0;
  for (int i = 0; i < n; ++i) {
    int cnt = 0;
    for (int j = 0; j < 2 * m; ++j) {
      int x;
      cin >> x;
      cnt += x;
    }
    if (cnt > 0) ++ans;
  }
  cout << ans << endl;
}
