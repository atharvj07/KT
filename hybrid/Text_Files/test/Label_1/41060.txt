n = int(input())
a = list(map(int, input().split()))
a.sort()
mp = {}
mx = 0
mn = n
for v in a:
    if v-1 not in mp: mp[v-1] = 0
    if v+1 not in mp: mp[v+1] = 0
    mp[v] = 1
    mx = max(mx, mp[v-1]+mp[v]+mp[v+1]+1)
    mn = min(mn, mp[v-1]+mp[v]+mp[v+1])
print(mn, mx)
