import sys

n = int(input())
code = []

for _ in range(n):
    line = input()
    if '=' in line:
        lval, rval = line.split('=')
        if lval == 'res':
            print(0)
            sys.exit()
    code.append(line)

print(len(code))
print(*code, sep='\n')
