#include <iostream>

using namespace std;

int ways[11] = {};

int main() {
    int n;
    cin >> n;
    ways[0] = 1;
    ways[1] = 4;
    for (int i = 2; i <= n; i++) {
        ways[i] = ways[i - 1] * 4;
    }
    cout << ways[n] << endl;
    return 0;
}
