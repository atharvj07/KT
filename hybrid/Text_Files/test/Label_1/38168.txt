import sys
from collections import defaultdict
input = sys.stdin.readline

def dijkstra(nodes, start):
    dist = [float('inf')] * (nodes + 1)
    visited = [False] * (nodes + 1)
    dist[start] = 0
    while True:
        v = -1
        for i in range(1, nodes + 1):
            if not visited[i] and (v == -1 or dist[i] < dist[v]):
                v = i
        if v == -1:
            break
        visited[v] = True
        for i, w in graph[v]:
            dist[i] = min(dist[i], dist[v] + w)
    return dist

n, s = map(int, input().split())
graph = defaultdict(list)
for _ in range(n - 1):
    a, b = map(int, input().split())
    graph[a].append((b, 1))
    graph[b].append((a, 1))
dist = dijkstra(n, 1)
a, b = dist.index(max(dist)), dist.index(max(dist[dist.index(max(dist)) + 1:]))
dist = dijkstra(n, a)
c = dist[b]
print(s / c)
