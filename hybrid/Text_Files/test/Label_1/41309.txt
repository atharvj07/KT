import heapq

n,m=map(int,input().split())
t=list(map(int,input().split()))
t.sort()
hq=[]
for i in range(n):
  x=m-t[i]
  heapq.heappush(hq,(x,i))
  while hq and hq[0][0]<0:
    _,j=heapq.heappop(hq)
    t.pop(j)
    n-=1
    x-=1
  heapq.heappush(hq,(x,i))
for x,_ in hq:print(n-(x>0))
