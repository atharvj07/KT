import sys
input = sys.stdin.readline
M = 998244353
n = int(input())
a = [int(i) for i in input().split()]
lm = 1
ans = 0
for i in range(n - 1, -1, -1):
    ans = (ans * lm % M + lm * a[i] % M * pow(100 - a[i], M - 2, M) % M) % M
    lm = lm * (100 - a[i]) % M
print((ans + 1) % M)
