import sys
input = sys.stdin.readline

t = int(input())
for _ in range(t):
    s, n, k = map(int, input().split())
    if s >= n * k and s % k == 0:
        print("YES")
    else:
        print("NO")
