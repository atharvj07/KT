#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    cin >> n;
    vector<int> v(n);
    unordered_map<int, int> mp;
    for (int &x : v) {
        cin >> x;
        mp[x]++;
    }
    int mx = 0;
    for (auto &p : mp) { mx = max(mx, p.second); }
    cout << mx << '\n';
}
