#include <iostream>
#include <algorithm>

using namespace std;

int main() {
  long long x, y;
  cin >> x >> y;
  if (x * y == 1)
    cout << "Impossible";
  else
    cout << min(x, y) << (x == min(x, y) ? "B" : "A");
}
