#include <bits/stdc++.h>
using namespace std;

int main() {
  int a, b, c, d;
  cin >> a >> b >> c >> d;
  if ((b - d) % __gcd(a, c) == 0)
    cout << b << endl;
  else
    cout << -1 << endl;
  return 0;
}
