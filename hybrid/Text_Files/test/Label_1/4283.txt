#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long n,i,sum=0,x;
    cin>>n;
    for(i=0;i<n;i++)
    {
        cin>>x;
        sum+=x;
    }
    for(i=0;i<n;i++)
    {
        if(sum<=0){cout<<'-';sum++;}
        else{cout<<'+';sum--;}
    }
    return 0;
}
