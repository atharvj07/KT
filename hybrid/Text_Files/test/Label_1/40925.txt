import math

n = int(input())
a = list(map(int, input().split(' ')))
total = sum(a)
for x in a:
    for j in range(1, int(math.sqrt(x)) + 1):
        if x % j == 0:
            total = min(total, sum(a) - (x // j) + j * (n - 1))
print(total)
