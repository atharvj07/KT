def fast_pow(base, power):
    result = 1
    while power > 0:
        if power % 2 == 1:
            result = (result * base) % 100
        base = (base * base) % 100
        power /= 2
    return result

n = int(input())
print(fast_pow(5, n))
