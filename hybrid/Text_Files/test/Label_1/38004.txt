for _ in range(int(input())):
    n, k = map(int, input().split())
    s = input()
    cnt = [0] * 26
    for i in s:
      cnt[ord(i)-ord('a')] += 1
    mx = 0
    for i in cnt:
      mx = max(mx, i)
    ans = min(n, mx+k-1)
    print(ans)
