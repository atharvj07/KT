#include <iostream>

using namespace std;

int main() {
  int x, y, z;
  cin >> x >> y >> z;

  if (!z) {
    if (x > y)
      cout << "+";
    else if (x < y)
      cout << "-";
    else
      cout << "0";
    return 0;
  }
  cout << "?";
  return 0;
}
