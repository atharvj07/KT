#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  string a;
  cin >> n >> a;
  int ans = 0;
  for (int i = 1; i < n; i++) {
    if (a[i] == 'F' && a[i - 1] == 'S') ans++;
  }
  ans > (n - 1 - ans) ? cout << "YES" : cout << "NO";
}
