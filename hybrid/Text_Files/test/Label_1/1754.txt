#include <iostream>
using namespace std;
int main() {
  long long m;
  cin >> m;
  long long x = 0, n = 0;
  for (long long i = 1; (x + i * i * i) <= m; i++, n++) x += i * i * i;
  cout << n << " " << x << endl;
  return 0;
}
