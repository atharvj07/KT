n,a,b,k=map(int,input().split())
h=list(map(int,input().split()))
h=[i-b for i in h]
h.sort(reverse=True)
ans=0
for i in h:
    if i>=a:continue
    if k<=0:break
    ans+=1
    k-=1
    i+=b
    i=max(0,i-a)
    h.append(i)
    h.sort(reverse=True)
print(ans)
