#include<bits/stdc++.h>
using namespace std;
int main() {
  int a, ta, b, tb, h, m;
  char cc;
  cin >> a >> ta >> b >> tb;
  cin >> h >> cc >> m;
  int c = 0;
  for (int t = h * 60 + m; t < 23 * 60 + 59; t += a) {
    if (t % tb >= ta) { c++; }
  }
  cout << c << endl;
  return 0;
}
