#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while(t--){
        int n;
        char c;
        cin>>n>>c;
        string a;
        cin>>a;
        int ans=0;
        set<int> pos;
        for(int i=0;i<n;i++){
            if(a[i]!=c) pos.insert(i+1);
        }
        cout<<pos.size()<<'\n';
        for(auto i: pos) cout<<i<<' ';
        cout<<'\n';
    }
    return 0;
}
