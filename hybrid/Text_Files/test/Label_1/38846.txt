from math import sqrt, gcd

for _ in range(int(input())):
    n, x = map(int, input().split())
    a = list(map(int, input().split()))
    if x % gcd(x, min(a)) == 0:
        print(x // gcd(x, min(a)))
    else:
        print(-1)
