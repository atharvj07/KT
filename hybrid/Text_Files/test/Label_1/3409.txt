#include <iostream>
#include <string>
using namespace std;
int main() {
  int n;
  cin >> n;
  int cur = 1, len = 1;
  while (n > len * cur) {
    n -= len * cur;
    cur++;
    len *= 10;
  }
  cout << to_string(cur)[(n - 1) % len / cur] << endl;
  return 0;
}
