import sys

n, m, k = map(int, input().split())
p = list(map(int, input().split()))
curpos = {j: i for i, j in enumerate(p)}
ans = 0
for _ in range(n):
    a = list(map(int, sys.stdin.readline().split()))
    for j in a:
        curpos[j] += 1
        ans += curpos[j]
print(ans)
