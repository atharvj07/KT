#include <iostream>
using namespace std;
int main() {
  long long a, b, c;
  cin >> a >> b >> c;
  long long t = min(a + b + c, (a + b) * 2 - 1);
  t = min(t, (a + b) * 3);
  t = min(t, (a + c) * 3);
  t = min(t, (b + c) * 3);
  cout << t << endl;
  return 0;
}
