import sys

for _, line in enumerate(sys.stdin, 1):
    string = line.strip()
    if len(string) % 2 == 1:
        print('NO')
    else:
        print('YES' if string[:len(string) // 2] == string[len(string) // 2:] else 'NO')
