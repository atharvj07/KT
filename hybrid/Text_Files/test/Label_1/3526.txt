#include<bits/stdc++.h>
using namespace std ;
int main()
{
    int n ;
    cin >> n ;
    string s ;
    cin >> s ;
    if(n&1) s.pop_back() ;
    sort(s.begin(),s.end()) ;
    string a="" ;
    a+=s[0] ;
    if(s[0]!=s[1]) a+=s[1] ;
    cout << a << endl ;
    return 0 ;
}
