#include <bits/stdc++.h>
using namespace std;
const long long INF = 1LL<<60;
int main() {
  int t; cin >> t;
  while(t--) {
    long long s, a, b, c; cin >> s >> a >> b >> c;
    cout << (s/c + a - 1) / a * b + s/c << endl;
  }
}
