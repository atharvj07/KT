#include <iostream>

using namespace std;

int64_t max(int64_t a, int64_t b) { return a > b ? a : b; }

int main() {
  int64_t x1, y1, x2, y2, x3, y3, x4, y4, x5, y5, x6, y6;
  cin >> x1 >> y1 >> x2 >> y2 >> x3 >> y3 >> x4 >> y4 >> x5 >> y5 >> x6 >> y6;
  int64_t L = max(x3, x5), R = max(x1, L), B = max(y3, y5), T = min(y1, T);
  cout << ((L < R) && (B < T) ? "YES" : "NO") << endl;
}
