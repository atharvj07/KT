#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int c, Hr, Hb, Wr, Wb;
    cin >> c >> Hr >> Hb >> Wr >> Wb;
    cout << max({c / Wr * Hr, c / Wb * Hb, (c / __gcd(Wr, Wb)) * (Hr + Hb)}) << endl;
    return 0;
}
