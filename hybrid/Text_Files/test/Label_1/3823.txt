#include <bits/stdc++.h>

using namespace std;
const int maxn = 1e5 + 10;
int h[maxn];

int main() {
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> h[i];
	}
	int ans = 1;
	for (int i = 1; i < n; i++) {
		if (h[i] < h[i - 1]) {
			ans++;
		}
	}
	cout << ans << endl;
	return 0;
}
