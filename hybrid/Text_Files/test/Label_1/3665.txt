#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main(){string a,b;cin>>a>>b;vector<int>arr;
for(char& c:a)arr.push_back(c-'0');
sort(arr.begin(),arr.end(),greater<int>());
for(char& c:b){
	for(int i=0;i<arr.size();i++){
		if(c-'0'==arr[i]){
			swap(c,a[i]);
			break;
		}
	}
}
cout<<a<<endl;
}
