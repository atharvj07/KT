import sys, math

def main():
    n, k = map(int, input().split())
    mod = 1000000007

    if k > n - 1 or k % 2 != n % 2:
        print(0)
        return
    print(math.factorial(n) * pow(n - 1, k // 2, mod) * pow(n, (n - k) // 2, mod) % mod)

main()
