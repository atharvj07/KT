import math
h, m = map(int, input().split())
H, D, C, N = map(int, input().split())
delta = m/60 + h - (20 + 0.0 / 60)
bunCount = math.ceil((H + delta * D) / N)
print(round(C * bunCount * (1 if delta >= 0 else 0.8),3))
