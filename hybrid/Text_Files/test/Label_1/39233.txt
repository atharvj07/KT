from sys import stdin
for _ in range(int(stdin.readline())):
    s = stdin.readline().strip()
    st = []
    for c in s:
        if st and st[-1] == c:
            st.pop()
        else:
            st.append(c)
    print(len(st))
