#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    ll a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << max(abs(a - b) + abs(b - c) + abs(c - a) - d, 0LL) << "\n";
    return 0;
}
