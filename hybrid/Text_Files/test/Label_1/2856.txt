#include <iostream>
using namespace std;
int main() {
  int a1, a2, k1, k2, n;
  cin >> a1 >> a2 >> k1 >> k2 >> n;
  cout << max(0, (n + k1 - 1) / k1 + (n + k2 - 1) / k2 - a1 - a2) << ' '
       << min(a1, n / k1) + min(a2, n / k2) << endl;
}
