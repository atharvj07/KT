#include <iostream>

int main() {
  int a, b, c, d;
  std::cin >> a >> b >> c >> d;
  while (b % c != 0) {
    b += a;
  }
  if (b == d)
    std::cout << b << std::endl;
  else
    std::cout << "-1" << std::endl;
  return 0;
}
