for _ in range(int(input())):
	n = int(input())
	a = [0] * n
	for i in range(n):
		l = i
		r = n - 1
		while l < r:
			m = (l + r) // 2
			if a[m] == 0:
				r = m
			else:
				l = m + 1
		if (r - i + 1) & 1:
			a[i + (r - i) // 2] = i + 1
		else:
			a[i + (r - i) // 2 - 1] = i + 1
	print(*a)
