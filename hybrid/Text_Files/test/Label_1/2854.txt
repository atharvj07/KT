#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n;
  cin >> n;
  for (long long i = sqrt(n); i >= 2; i--) {
    if (n % i) continue;
    n -= n / i;
    break;
  }
  cout << n << endl;
  return 0;
}
