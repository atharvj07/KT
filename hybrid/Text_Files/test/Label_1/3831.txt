#include<bits/stdc++.h>
using namespace std;
int main()
{
  int n, m;
  cin >> n >> m;
  vector<int> a(n);
  for (int &x : a) cin >> x;
  int sum = 0;
  for (int x : a) sum = (sum + x) % m;
  if (sum == 0 || n > 1) puts("YES");
  else puts("NO");
  return 0;
}
