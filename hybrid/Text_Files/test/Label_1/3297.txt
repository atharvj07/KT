#include <iostream>
#include <string>

using namespace std;

int main() {
  string s;
  cin >> s;
  cout << (s.find("Danil") != -1 || s.find("Olya") != -1 || s.find("Slava") != -1 || s.find("Ann") != -1 || s.find("Nikita") != -1 ? "YES" : "NO") << endl;
}
