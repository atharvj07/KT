S=input()
open=[0]*4
for c in S:
    ind='<>{}([][])'.index(c)
    if ind&1==0:
        open[ind]+=1
    else:
        open[ind-1]-=1
        if open[ind-1]<0:
            print("Impossible")
            exit(0)
print(sum(abs(x)for x in open))
