from math import gcd
for _ in range(int(input())):
    n=int(input())
    l=[int(x) for x in input().split()]
    ans=0
    while max(l)!=min(l):
        for i in range(n):
            l[i]=gcd(l[i],l[(i+1)%n])
        ans+=1
    print(ans)
