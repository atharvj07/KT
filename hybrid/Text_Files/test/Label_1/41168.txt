import sys
r=range
l=lambda x:int(x,0)

try:
    _,*b=map(l,sys.stdin.read().split())
    d={v:i for i,v in enumerate(b)}
    for j in r(1,max(d.values())+1):
        if j in d: b[d[j]-1]=0
    print(sum(b),*filter(None,b))
except: print("-1")
