#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int l, r;
    cin >> l >> r;
    if (l == 0 && r == 0)
      cout << "1\n";
    else
      cout << 1 + (r - l) / 2 << '\n';
  }
  return 0;
}
