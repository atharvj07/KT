#include <iostream>
#include <string>

using namespace std;

int main() {
  string s;
  getline(cin, s);
  cout << (s.find("Danil") != string::npos || s.find("Olya") != string::npos || s.find("Slava") != string::npos || s.find("Ann") != string::npos || s.find("Nikita") != string::npos ? "YES" : "NO") << endl;
  return 0;
}
