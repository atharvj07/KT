import collections
for _ in range(int(input())):
    n = int(input())
    strings = [collections.Counter(input()) for _ in range(n)]
    if max(a == b for a in strings for b in strings): print('YES')
    else: print('NO')
