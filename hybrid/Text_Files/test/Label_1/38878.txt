n, a, b = map(int, input().split())
s = '.' + input() + '.'
p, q = 0, 0
ans = 0
for i in s:
    if i == '.':
        if q % 2 == 0:
            q += 1
            ans = max(ans, p + q)
        else:
            p += 1
            ans = max(ans, p + q)
    else:
        p = q = 0
print(ans)
