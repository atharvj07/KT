#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    string s;
    cin >> s;
    for (int i = 0; i < (int)s.size(); i++) {
      char c = 'a';
      while (c == s[i]) c++;
      s[i] = c;
    }
    cout << s << "\n";
  }
  return 0;
}
