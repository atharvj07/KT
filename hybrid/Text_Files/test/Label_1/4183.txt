#include <iostream>
#include <cstdio>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        int a, b, c, ans;
        cin >> a >> b >> c;
        ans = min(a, min(b, c));
        ans += min(3, a + b + c - ans);
        cout << ans << endl;
    }
    return 0;
}
