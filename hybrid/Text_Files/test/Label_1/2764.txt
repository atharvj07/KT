#include <iostream>
#include <set>

using namespace std;

int main() {
  int n, k, d;
  cin >> n >> k >> d;
  set<int> s;
  for (int i = 0; i < n; i++) {
    cin >> d;
    s.insert(d % k);
  }
  cout << s.size() << endl;
  for (int i : s) { cout << i << " "; }
  return 0;
}
