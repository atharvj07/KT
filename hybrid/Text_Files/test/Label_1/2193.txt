#include<bits/stdc++.h>
using namespace std; map<pair<char,int>,int> mp; int main(){ string a,b; cin>>a>>b; int n; cin>>n;
while(n--){ int t; char c; int m; char d; cin>>t>>c>>m>>d; if(d=='y') mp[{c,m}]++;
else{ if(mp[{c,m}]++ == 1) cout<<((c=='h')?a:b)<<' '<<m<<' '<<t<<'\n'; } } }
