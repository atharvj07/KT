#include <iostream>
using namespace std;
int main() {
  int a, b, ans = 0;
  cin >> a >> b;
  while (a > 0 || b > 0) {
    if (a > 0) {
      a -= 2;
      ans++;
    }
    if (a > 0 || b > 0) {
      b -= 2;
      ans++;
    }
  }
  cout << ans << endl;
}
