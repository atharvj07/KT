import heapq
import math

n = int(input())
a = []
for i in range(n):
    k, ai = map(int, input().split())
    a.append((1<<k, ai))
a.sort()
ans = 0
cnt = 0
for x, ai in a:
    ans += math.ceil(ai / 4) * 2
    cnt += ai
    if cnt >= 4:
        cnt -= 4
        ans -= 2
print(ans)
