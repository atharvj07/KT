#include<bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int mn = n / m;
    vector<int> res;
    for(int i=0; i<n%m; i++) res.push_back(mn+1);
    for(int i=0; i<(m-n%m); i++) res.push_back(mn);
    for(auto x: res) cout << x << ' ';
    return 0;
}
