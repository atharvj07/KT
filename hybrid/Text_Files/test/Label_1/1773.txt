#include <bits/stdc++.h>
using namespace std;
const int mod=1000000007;
int main(){
    int t,n; cin>>t;
    while(t--){
        cin>>n; vector<int>a(n),sum(n+2,0);
        for(int i=0;i<n;i++){
            cin>>a[i];
            sum[i+1]=sum[i]+a[i];
        }
        int ans=0,mini=2e9;
        for(int i=n-1;i>=0;i--){
            if(sum[n]-sum[i+1]==sum[i]){
                for(int j=i;j>=0;j--)
                    if(sum[n]-sum[j+1]>sum[j]){
                        ans++;
                        mini=min(mini,(sum[j+1]-sum[j]-1)/2);
                    }
            }
            else mini=min(mini,((sum[n]-sum[i+1]-sum[i])/2));
        }
        cout<<max(ans,mini)<<endl;
    }
}
