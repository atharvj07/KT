import math

n, q = map(int, input().split())

for i in range(q):
    x, y = map(int, input().split())
    if (x + y) % 2 == 0:
        print(math.ceil((x + y) / 2 + (x + y) % 2))
    else:
        print(math.ceil((x + y) / 2 + (x + y - 1) % 2 + n ** 2 / 2))
