#include <bits/stdc++.h>
using namespace std;
int main() {
    int x[4];
    for (int i = 0; i < 4; i++) { cin >> x[i]; }
    if ((x[0] <= x[3]) && (x[1] <= x[2])) { cout << 1; } else { cout << 0; }
    return 0;
}
