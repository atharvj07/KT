#include <cstdio>
#include <cmath>
int main() {
  long long k, q;
  scanf("%lld", &q);
  while (q--) {
    scanf("%lld", &k);
    if (k % 9 == 0)
      printf("9");
    else
      printf("%lld", (k - 1) % 9);
  }
  return 0;
}
