n, a, b = map(int, input().split())
left, right = 1, min(a, b)
while left < right:
    mid = (left + right + 1) // 2
    if a // mid + b // mid >= n:
        left = mid
    else:
        right = mid - 1
print(left)
