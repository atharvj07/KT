#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        long long n, l, r;
        cin >> n >> l >> r;
        cout << (l <= n && n <= r ? "Yes" : "No") << endl;
    }
    return 0;
}
