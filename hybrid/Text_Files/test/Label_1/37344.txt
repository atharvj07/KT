digits = input()
for i in range(1, len(digits)):
    for j in range(i+1, len(digits)):
        if int(digits[:i]) + int(digits[i:j]) == int(digits[j:]):
            print(digits[:i]+'+'+digits[i:j]+'='+digits[j:])
