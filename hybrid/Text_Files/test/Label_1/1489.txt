#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int r, b, k;
    cin >> r >> b >> k;
    if (r == b || k == 2)
      cout << "REBEL" << endl;
    else
      cout << "OBEY" << endl;
  }
  return 0;
}
