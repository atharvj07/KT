#include <iostream>
#include <numeric>
#include <algorithm>

using namespace std;

int main() {
  long long n;
  cin >> n;
  long long ans = (n + 9) / 10;
  while (ans * (ans + 9) / 2 < n) ans++;
  cout << ans << "\n";
  return 0;
}
