n, k = map(int, input().split())
s = input().strip()
available_chars = set(input().split())

count, total = 0, 0
for char in s:
    if char in available_chars:
        count += 1
        total += count
    else:
        count = 0

print(total)
