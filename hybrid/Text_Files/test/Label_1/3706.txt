#include <iostream>
#include <vector>
using namespace std;

int main() {
	int n, ans = 0;
	cin >> n;
	vector<int> c(n), a(n), vis(n);
	for (auto &t : c)
		cin >> t;
	for (auto &t : a)
		cin >> t, t--, vis[t]++;
	for (int i = 0; i < n; ++i)
		if (vis[i] == 0)
			ans += c[i];
	cout << ans;
}
