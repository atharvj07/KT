import sys
input = sys.stdin.readline

def dist(x1,y1,x2,y2):
    return ((x1-x2)**2 + (y1-y2)**2)**0.5

n = int(input())
x,y = map(float,input().split())
c = (x,y)
mn = float('inf')
for i in range(1,n):
    x1,y1 = map(float,input().split())
    if y1==0:
        mn = min(mn,abs(x1-x))
    else:
        X = x-((y*x1-y1*x)/(y1))
        Y = ((x*y1-y*x1)/(y1))
        if dist(x,y,X,Y)<=dist(x,y,mn,0):
            mn = dist(x,y,X,Y)
            c = (X,Y)
if mn==float('inf'):
    print(-1)
else:
    print(mn)
