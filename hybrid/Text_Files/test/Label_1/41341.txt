n, l = map(int, input().split())
a = list(map(int, input().split()))

a.sort()

max_diff = max(a[0], l - a[-1]) * 2
for i in range(1, n):
    max_diff = max(max_diff, a[i] - a[i-1])

print(max_diff / 2)
