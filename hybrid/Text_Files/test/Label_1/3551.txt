#include <iostream>
using namespace std;

int main() {
  int l, r, a;
  cin >> l >> r >> a;
  int team_size = min(l, r) * 2;
  if (a > 0) team_size += min(a, max(l, r) - min(l, r));
  cout << team_size << endl;
  return 0;
}
