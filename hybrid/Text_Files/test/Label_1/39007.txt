for _ in range(int(input())):
    n, m = map(int, input().split())
    arr = set()
    for i in range(2**m):
        arr.add(bin(i)[2:].zfill(m))
    for i in range(n):
        arr.remove(input())
    arr = list(sorted(arr))
    print(arr[len(arr)//2])
