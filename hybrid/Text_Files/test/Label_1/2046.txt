#include <iostream>
#include <cmath>
using namespace std;
int main() {
  int q; cin >> q;
  while (q--) {
    int n; cin >> n;
    if (n < 4) cout << -1 << endl;
    else cout << n - (n % 4) << endl;
  }
  return 0;
}
