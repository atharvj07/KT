from bisect import bisect
from math import factorial as fac

n = int(input())
a = [int(x) for x in input().split()]
a.sort()
ans = [0] * n
for i in range(n):
    j = bisect(a, -a[i])
    if j == n: j -= 1
    ans[i] = a[j]
    a[j] = -a[j]
print(*ans)
