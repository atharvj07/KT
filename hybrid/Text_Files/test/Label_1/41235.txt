import sys

input = sys.stdin.readline

t = int(input())

for _ in range(t):
    n, d = map(int, input().split())
    a = list(map(int, input().split()))
    if sum(a) <= d:
        print(sum(a))
    else:
        print(d + min(a))
