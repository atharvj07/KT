for _ in range(int(input())):
    n, d = map(int, input().split())
    a = list(map(int, input().split()))
    a.sort()
    i = n - 1
    while i > 0 and d > 0:
        t = min(d, a[i])
        a[i] -= t
        a[i-1] += t
        d -= t
        i -= 1
    print(a[-1])
