#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
  int n, s;
  cin >> n >> s;
  vector<int> a(n);
  for (int &x : a) cin >> x;
  sort(a.begin(), a.end());
  int ans = 0;
  for (int x : a) ans += abs(x - s);
  cout << ans << endl;
  return 0;
}
