import sys

t = int(sys.stdin.readline())

for _ in range(t):
    k = int(sys.stdin.readline())
    n = int(sys.stdin.readline())
    ans = ""
    for d in str(n):
        if int(d) % 2 == 1:
            ans += d
        elif ans:
            ans += d
    print(len(ans))
    print(ans)
