n = int(input().strip())
b = list(map(int, input().strip().split()))

a = [0] * n
a[0] = b[0]
max_val = max(0, a[0])

for i in range(1, n):
    a[i] = b[i] + max_val
    max_val = max(max_val, a[i])

print(" ".join(map(str, a)))
