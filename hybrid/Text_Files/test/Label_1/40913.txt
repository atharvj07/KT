for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    print(max(a[i]*a[j]*a[k]*a[l]*a[t] for i in range(n) for j in range(i+1, n) for k in range(j+1, n) for l in range(k+1, n) for t in range(l+1, n)))
