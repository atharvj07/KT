#include <iostream>

using namespace std;

int main() {
    int A, B, C, N;
    cin >> A >> B >> C >> N;
    int sum = A + B - C;
    if (sum <= 0 || sum > N)
        cout << -1;
    else
        cout << N - sum;
    return 0;
}
