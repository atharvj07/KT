import sys
n, l = map(int, sys.stdin.readline().split())
a = list(map(int, sys.stdin.readline().split()))
a.sort()
ans = max(a[0], l - a[-1])
for i in range(1, n):
    ans = max(ans, (a[i] - a[i-1])/2)
print(ans)
