#include <iostream>

int main() {
    int t;
    std::cin >> t;
    while (t--) {
        long long n, k;
        std::cin >> n >> k;
        std::cout << (k - 1) % n + 1 << '\n';
    }
    return 0;
}
