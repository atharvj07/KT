s = input().strip()
vowels = 'AEIOUY'
max_dist = 0
last_vowel = -1
for i, c in enumerate(s):
    if c in vowels:
        max_dist = max(max_dist, i - last_vowel)
        last_vowel = i
max_dist = max(max_dist, len(s) - last_vowel)
print(max_dist)
