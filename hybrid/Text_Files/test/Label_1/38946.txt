import sys

t = int(sys.stdin.readline())

for _ in range(t):
    n = int(sys.stdin.readline())
    s = sys.stdin.readline().strip()
    st = []
    for c in s:
        if not st or st[-1] == c:
            st.append(c)
        else:
            st.pop()
    print(*st)
