m, a, b = map(int, input().split())
g = gcd(a, b)

def f(x):
    if x < a and x < b:
        return x + 1
    return (x // g + 1) * (x // g + 2) // 2 * g - max(0, (a + b - x % g - 2)) * (x // g + 1) // 2

print(f(m) - f(-1))
