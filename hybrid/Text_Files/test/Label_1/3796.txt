#include <bits/stdc++.h>
using namespace std;

int main() {
  long long n, x, y;
  cin >> n >> x >> y;
  long long ans = (n / 2) * (2 * x + y);
  if (n % 2) ans += x;
  cout << ans << endl;
  return 0;
}
