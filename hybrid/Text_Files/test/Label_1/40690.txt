t = int(input().strip())
for _ in range(t):
    s = input().strip()
    working = set()
    i = 0
    while i < len(s):
        if i + 1 < len(s) and s[i] == s[i + 1]:
            i += 2
        else:
            working.add(s[i])
            i += 1
    print("".join(sorted(working)))
