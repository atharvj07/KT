#include <iostream>

using namespace std;

int main() {
  long long a, b, c, d, e;
  cin >> a >> b >> c >> d >> e;
  long long mn = max(a, c);
  long long mx = min(b, d);
  if (mn <= mx && mx - mn >= 0) cout << mx - mn;
  else cout << 0;
  return 0;
}
