#include <iostream>
using namespace std;
int main() {
    int n;
    cin >> n;
    long long suf = 0;
    string s(n, '+');
    for (int i = n - 1; i >= 0; i--) {
        cin >> suf;
        if (suf > i)
            s[i] = '-';
        else
            break;
    }
    cout << s << endl;
}
