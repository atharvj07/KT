#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, m, k;
  cin >> n >> m >> k;
  if (k <= m) {
    cout << k << endl;
    return 0;
  }
  int ans = k / m;
  k -= (ans * m);
  cout << k << endl;
  return 0;
}
