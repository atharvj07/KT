#include <iostream>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long int c1, c2, c3, a1, a2, a3, a4, a5;
    cin >> c1 >> c2 >> c3 >> a1 >> a2 >> a3 >> a4 >> a5;
    long long int count = a1 + a2 + a3 + a4 + a5;
    if (count <= c1 + c2 + c3)
      cout << "YES" << endl;
    else
      cout << "NO" << endl;
  }
  return 0;
}
