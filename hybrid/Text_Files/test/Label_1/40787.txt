def solve(n):
    dp = [0] * (n + 1)
    dp[0] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i-2] * 2 + dp[i-1] if i % 2 == 0 else dp[i-1]
    return dp[n] if n % 2 == 0 else 0

n = int(input().strip())
print(solve(n))
