#include <bits/stdc++.h>
#define mod 1000000007
#define ll long long
using namespace std;
int main() {
    string s;
    cin >> s;
    reverse(s.begin(), s.end());
    int p = 0;
    int ans = 0;
    for (int i = 0; i < s.size(); i++) {
        int temp = s[i] - '0';
        p = ((p * 10) + temp) % 4;
        ans += p == 0;
    }
    cout << ans << '\n';
    return 0;
}
