t = int(input())
for _ in range(t):
    n, k = map(int, input().split())
    mice = sorted(map(int, input().split()), reverse=True)
    cat = 0
    saved_mice = 0
    for mouse in mice:
        if mouse + cat < n:
            saved_mice += 1
            cat += n - mouse
    print(saved_mice)
