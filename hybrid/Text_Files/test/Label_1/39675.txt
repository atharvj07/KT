import sys
s=sys.stdin.readline().strip()
t=sys.stdin.readline().strip()
p = [-1] * len(t)
ans = 0
j = 0
for i in range(len(s)):
    if j < len(t) and s[i] == t[j]:
        p[j] = i
        j += 1
    ans = max(ans, i - p[j - 1] - 1)
print(ans)
