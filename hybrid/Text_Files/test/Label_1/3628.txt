#include <bits/stdc++.h>
#define int long long
using namespace std;
int n, k;
void db(int x) { while(x) cout<<x%2, x/=2; cout<<'\n'; }
void solve() {
    cin >> n;
    while(n) {
        if(n%2) k++;
        n/=2;
    }
    cout << k << '\n';
    for(int i=0; i<k; i++) cout << "1 ";
}
signed main() {
    ios::sync_with_stdio(0); cin.tie(0);
    solve();
}
