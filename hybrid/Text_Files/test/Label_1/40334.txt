HP_Y, ATK_Y, DEF_Y = map(int, input().split())
HP_M, ATK_M, DEF_M = map(int, input().split())
h, a, d = map(int, input().split())

need_ATK = max(0, ATK_M - DEF_Y)
need_HP = max(0, ATK_M - DEF_Y - (ATK_Y - DEF_M))

ans = need_HP * h + need_ATK * a
print(ans)
