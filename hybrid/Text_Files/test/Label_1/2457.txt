#include<bits/stdc++.h>
using namespace std;
#define pb push_back
#define pf push_front
#define mp make_pair
#define lb lower_bound
#define ub upper_bound
#define all(v) v.begin(),v.end()
#define rep(i,a,b) for(int i=a;i<b;i++)
typedef vector<int> vi;
typedef vector<vi> vii;
typedef pair<int,int> ii;
typedef long long ll;
const int N=101;
void solve(){
    const int nax=100;
    int n,m,h,a[nax],b[nax],t[nax][nax],ans[nax][nax];
    cin>>n>>m>>h;
    rep(j,0,m)cin>>a[j];
    rep(i,0,n)cin>>b[i];
    rep(i,0,n)rep(j,0,m)cin>>t[i][j];
    rep(i,0,n){
        rep(j,0,m){
            ans[i][j]=a[j]^b[i]^t[i][j];
        }
    }
    rep(i,0,n){
        rep(j,0,m)cout<<ans[i][j]<<" ";
        cout<<endl;
    }
}
int32_t main(){
    solve();
    return 0;
}
