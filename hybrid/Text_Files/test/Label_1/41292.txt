k = int(input())
n = input()

sum_digits = sum(map(int, n))
diff = max(0, k - sum_digits)

if diff == 0:
    print(0)
else:
    digits = sorted(n)
    i = 0
    while diff > 0:
        diff -= (9 - int(digits[i]))
        i += 1
    print(i)
