#include <bits/stdc++.h>
using namespace std;
struct node {
  int x, y;
};
int main() {
  int n;
  cin >> n;
  vector<node> a(n);
  for (int i = 0; i < n; i++) {
    cin >> a[i].x;
    a[i].y = a[i].x;
  }
  sort(a.begin(), a.end(), [](node &a, node &b) { return a.x > b.x; });
  for (int i = 0; i < n; i++) {
    int t = a[i].x;
    if (a[i].y % 2) a[i].x = -a[i].x - 1;
    cout << a[i].x << " ";
  }
}
