n, m, w = map(int, input().split())
a = [int(x) for x in input().split()]
a.sort()
a.reverse()

left = (n+w-1) // w
need = m - left
for x in a:
    if need == 0:
        break
    if x < need:
        need -= x
        x = 0
    else:
        need = 0
        x -= need

print(a[0] + left)
