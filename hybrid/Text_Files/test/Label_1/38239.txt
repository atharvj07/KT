n = int(input().strip())
a = list(map(int, input().split()))

if n == 1:
    print(a[0])
else:
    total_sum = sum(a)
    dp = [0] * n
    dp[1] = a[0]
    for i in range(2, n):
        dp[i] = max(dp[i-1], dp[i-2] + a[i-1])
    print(total_sum + 2 * dp[-1])
