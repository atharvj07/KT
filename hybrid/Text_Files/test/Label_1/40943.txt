from collections import defaultdict
m=int(input());S=list(map(int,input().split()))
G=defaultdict(list)
for x in S:
    for y in G[x]:G[x].remove(y)
    for y in G:
        if x%y==0 and (x//y not in G[y] or x//y==x):G[y].append(x)
    G[x].append(x)
    G[x].sort()
def dfs(x):
    for y in G[x]:
        if y!=x and y not in Vis:
            Vis.add(y);A.append(y);dfs(y)
Vis,A=[x],[x]
dfs(S[0])
if len(Vis)==len(S):print(len(Vis));print(*A)
else:print(-1)
