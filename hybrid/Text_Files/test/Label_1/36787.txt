n, A = map(int, input().split())
ls = list(map(int, input().split()))
if ls.count(A) >= n//2:
    print(-1)
else:
    ls.sort()
    for i in ls:
        if i!=A and ls.count(i) >= n//2:
            print(i)
            break
    else:
        print(-1)
