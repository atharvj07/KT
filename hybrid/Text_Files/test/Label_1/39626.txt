n, q = map(int, input().split())
state = list(range(n))
for i in range(q):
    typ, *par = map(int, input().split())
    if typ == 1:
        state = state[::-1][par[0]:] + state[:par[0]]
    else:
        print(sum(state[par[0]:par[1] + 1]))
