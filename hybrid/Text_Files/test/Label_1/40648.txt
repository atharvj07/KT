import math

for _ in range(int(input())):
    l, r, d = map(int, input().split())
    if l % d == 0:
        ans = l + d
    else:
        ans = math.ceil(l / d) * d
    if ans <= r:
        ans += d
    print(ans)
