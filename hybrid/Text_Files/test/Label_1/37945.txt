import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    arr = list(map(int, input().split()))
    arr.sort()
    if sum(arr[::2]) == sum(arr[1::2]):
        print("YES")
    else:
        print("NO")
