#include <cstdio>

int main() {
  int t, a1, b1, a2, b2;
  scanf("%d", &t);
  while (t--) {
    scanf("%d%d", &a1, &b1);
    scanf("%d%d", &a2, &b2);
    puts((a1 == a2 && b1 == b2) || (a1 == b2 && b1 == a2) ? "Yes" : "No");
  }
  return 0;
}
