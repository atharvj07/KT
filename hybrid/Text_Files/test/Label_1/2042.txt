#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        ll l, r;
        cin >> l >> r;
        ll ans = r - (l - 1);
        if (l > 1) ans--;
        cout << ans << endl;
    }
    return 0;
}
