import sys
input = sys.stdin.readline
for n in map(int,input().split()): a = list(map(int,input().split()))
for m in map(int,input().split()):
    l = [[]]
    for x in a:
        l+=[[x]+y for y in l]+[[x]]
    l.sort()
    for _ in map(int,input().split()):
        for y in l:
            if len(y)==_: print(y[m-1]); break
