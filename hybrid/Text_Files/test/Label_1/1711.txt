#include<bits/stdc++.h>
using namespace std;
int main(){
  int n,k;cin>>n>>k;
  vector<int>a(2*n);
  for(int i=0,j=1;i<n;++i)a[2*i]=j,a[2*i+1]=j+1,++j;
  int steps=0;
  while(steps<k){
    --n;
    swap(a[2*n],a[2*n+1]);
    ++steps;
  }
  for(int u:a)cout<<u<<" ";
}
