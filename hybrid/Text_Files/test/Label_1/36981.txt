n, m, w = map(int, input().split())
a = list(map(int, input().split()))
a.sort()
ans = a[0]
cur = a[0]
cnt = 0
for i in a[1:]:
    if cur + w - 1 >= i:
        cur = i + w - 1
        cnt += 1
    else:
        cnt = 0
        cur = i + w - 1
    ans = max(ans, cur - m + cnt)
print(ans)
