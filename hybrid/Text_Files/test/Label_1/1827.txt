#include <iostream>
using namespace std;

int main() {
  int t;
  cin >> t;
  for (int i = 0; i < t; ++i) {
    int l, r;
    cin >> l >> r;
    int ans = r - l;
    if (l % 10 < r % 10) ++ans;
    if (l == 1) --ans;
    cout << ans << endl;
  }
  return 0;
}
