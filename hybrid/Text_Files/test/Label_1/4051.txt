#include <iostream>

int main() {
    int n;
    std::cin >> n;
    int a[n];
    int ans = 1;
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
        if (a[i] < a[ans - 1]) {
            ans = i + 1;
        }
    }
    std::cout << ans << std::endl;
    return 0;
}
