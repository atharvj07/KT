import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    mn = min(a)
    print('YES' if any(a[i] == mn and a[j] % a[i] == mn for i in range(n) for j in range(i + 1, n)) else 'NO')
