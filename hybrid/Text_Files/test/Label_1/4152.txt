#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    int tt;
    cin >> tt;
    while (tt--) {
        ll n, k;
        cin >> n >> k;
        ll ans = (1 << k) * (1 << k);
        if (k > 0) ans--;
        cout << ans % 1000000007 << endl;
    }
    return 0;
}
