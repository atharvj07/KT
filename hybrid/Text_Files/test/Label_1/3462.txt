#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int gcd(int a,int b){return a==0?b:gcd(b%a,a);}
int main() { ll t, w, b; cin >> t >> w >> b;  cout << (gcd(w, b) - 1) << '/' << t << endl; }
