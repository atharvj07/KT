#include <cstdio>

int main() {
    int T;
    scanf("%d", &T);
    while (T--) {
        long long a, b;
        scanf("%lld %lld", &a, &b);
        long long ans = ((a + b - 1) / b) * b - a;
        printf("%lld\n", ans);
    }
    return 0;
}
