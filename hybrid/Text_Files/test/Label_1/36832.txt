n = int(input())

books = []

for i in range(n):
    t, w = map(int, input().split())
    books.append((t, w))

books.sort(key=lambda x: x[1])

thickness = 0
width = 0

for t, w in books:
    if thickness + t <= width:
        thickness += t
    else:
        width += w

print(thickness)
