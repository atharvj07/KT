#include <iostream>
using namespace std;
int main() {
  string s;
  getline(cin, s);
  bool same = true;
  for (int i = 0; i < s.length() / 2; i++) {
    if (s[i] != s[s.length() - i - 1]) {
      same = false;
      break;
    }
  }
  cout << (same ? "YES" : "NO") << endl;
  return 0;
}
