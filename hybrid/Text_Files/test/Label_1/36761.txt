a, b = map(int, input().split())
s = input()
m = {'U': (0, 1), 'D': (0, -1), 'L': (-1, 0), 'R': (1, 0)}
cx, cy = [0, 0]
for ch in s:
    cx += m[ch][0]
    cy += m[ch][1]
    if cx == a and cy == b:
        print('Yes')
        exit()
print('No')
