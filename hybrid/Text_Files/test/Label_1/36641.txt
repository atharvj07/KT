a = int(input().strip())
b = int(input().strip())

if a > b:
    a, b = b, a

distance = b - a
mid = distance // 2

tiredness = (mid * (mid + 1)) if distance % 2 == 0 else (mid * (mid + 1)) + (mid + 1)

print(tiredness)
