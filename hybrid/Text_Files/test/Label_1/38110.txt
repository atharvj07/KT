for _ in range(int(input())):
    h, n = map(int, input().split())
    p = [[] for _ in range(h + 1)]
    for i in map(int, input().split()):
        p[i].append(1)
        p[i - 1].append(0)
        p[i - 2].append(1)
    print(sum(sum(l) % 2 for l in p))
