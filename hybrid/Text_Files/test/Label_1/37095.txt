import decimal, math

n = int(input())
for i in range(n):
    p, q, b = map(int, input().split())
    d = decimal.Decimal(int(p) / int(q))
    f = str(d.quantize(decimal.Decimal(10) ** -10, decimal.ROUND_HALF_UP))
    print("Finite" if f[-1] != '.' else "Infinite")
