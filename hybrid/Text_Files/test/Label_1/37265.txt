def can_be_obfuscated(s):
    current_char = 'a'
    for char in s:
        if char == current_char:
            continue
        elif ord(char) - ord(current_char) == 1:
            current_char = char
        else:
            return "NO"
    return "YES"

s = input()
print(can_be_obfuscated(s))
