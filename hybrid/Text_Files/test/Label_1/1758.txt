#include <iostream>
using namespace std;

int main() {
    int q;
    cin >> q;
    while (q--) {
        int n;
        cin >> n;
        if (n <= 3) cout << -1 << endl;
        else if (n % 2 == 1) cout << (n - 5) / 2 << endl;
        else cout << n / 2 - (n % 4 == 0) << endl;
    }
}
