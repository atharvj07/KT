#include <iostream>

using namespace std;

int main()
{
    int x, y, z;
    cin >> x >> y >> z;

    if (x + z > y)
        cout << "+";
    else if (y + z > x)
        cout << "-";
    else if (x == y)
        cout << "0";
    else
        cout << "?";
}
