n, m, k = map(int, input().split())
p = list(map(int, input().split()))
pos = [0]*k
for i in range(n):
    a = list(map(int, input().split()))
    for j in a:
        pos[j-1] += p[j-1]
        p = [p[j-1]] + p[j:] + p[:j-1]
print(sum(pos))
