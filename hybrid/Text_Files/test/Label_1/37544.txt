import sys

for t in range(int(input())):
    n, a, b = map(int, input().split())
    if n - 2 < max(a, b):
        print(-1)
    else:
        print(*[i + 1 for _, i in sorted([(a, 0), (b, 1)])] + [i + 1 for i in range(n)])
