#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, k, odd = 0;
    cin >> n >> k;
    for (long long i; n--;) { cin >> i; odd += !(i & 1); }
    odd & 1 ? cout << "Kevin" : cout << "Nicky";
    return 0;
}
