n = int(input())
s = input()

max_pretty = 0
current_pretty = 0
letters = set()

for char in s:
    if char.isupper():
        max_pretty = max(max_pretty, len(letters))
        letters.clear()
    else:
        letters.add(char)

max_pretty = max(max_pretty, len(letters))
print(max_pretty)
