q = int(input())
for _ in range(q):
    n, r = map(int, input().split())
    x = list(map(int, input().split()))
    unique_positions = len(set(x))
    max_position = max(x)
    shots = min(unique_positions, (max_position + r - 1) // r)
    print(shots)
