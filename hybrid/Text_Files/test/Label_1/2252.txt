#include <iostream>

int main() {
  int n, k;
  std::cin >> n >> k;

  k = 240 - k;
  int sum = 0, ans = 0;
  for (int i = 1; i <= n; i++) {
    if (sum + 5 * i <= k) {
      sum += 5 * i;
      ans++;
    }
  }

  std::cout << ans << std::endl;

  return 0;
}
