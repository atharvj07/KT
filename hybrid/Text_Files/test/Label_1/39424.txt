t = int(input())
for _ in range(t):
    a, b, c, d = map(int, input().split())
    if a > b * c:
        print(-1)
    else:
        total_heals = (a + (d * b) - 1) // (d * b)
        print(total_heals * a - b * (total_heals * (total_heals - 1) // 2) * d)
