import math

n = int(input())
arr = list(map(int, input().split()))

g = arr[0]

for a in arr[1:]:
    g = math.gcd(g, a)

ans = 0

for i in range(1, int(math.sqrt(g)) + 1):
    if g % i == 0:
        ans += 1

        if g // i != i:
            ans += 1

print(ans)
