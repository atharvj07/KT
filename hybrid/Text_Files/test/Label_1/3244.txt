#include <cstdio>

int main() {
  long long n, m, i, ans = 0;
  scanf("%lld %lld", &n, &m);
  for (i = 0; i < m; ++i) {
    long long x;
    scanf("%lld", &x);
    ans += (x - 1 + n - ans % n) % n;
  }
  printf("%lld\n", ans);
  return 0;
}
