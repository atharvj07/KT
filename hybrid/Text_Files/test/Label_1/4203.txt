#include <iostream>
using namespace std;
int t, a1, b1, a2, b2;
int main() {
  for (cin >> t; t--;) {
    cin >> a1 >> b1 >> a2 >> b2;
    cout << ((a1 == b1 && a2 == b2) || (a1 == a2 && b1 == b2) ? "Yes\n" : "No\n");
  }
  return 0;
}
