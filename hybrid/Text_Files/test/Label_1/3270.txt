#include <iostream>
using namespace std;
int main() {
    long long n, k, m;
    cin >> n >> k;
    if (n < k || (n + k) % 2)
        cout << "NO" << endl;
    else
        cout << "YES" << endl, cout << (n + k) / 2 << " " << (n - k) / 2 << endl;
}
