import sys
input = sys.stdin.readline

t = int(input())

for _ in range(t):
  n, a, b = map(int, input().split())
  if n % 2 == 0: print(n // 2 * min(a, b * 2))
  else: print(n // 2 * min(a, b * 2) + min(a, b))
