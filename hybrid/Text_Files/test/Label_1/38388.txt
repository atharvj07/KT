import sys

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    k = sum(a) / n
    ans = 0
    for i in range(n):
        for j in range(i + 1, n):
            ans += (k * (n - 2) - (a[i] + a[j])) % 1 == 0
    print(ans)
