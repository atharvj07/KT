#include <iostream>

using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  int ans = 0;
  while (n--) {
    string s;
    cin >> s;
    for (int i = 0; i < 2 * m; i += 2) {
      ans += (s[i] == '1' || s[i + 1] == '1');
    }
  }
  cout << ans << endl;
  return 0;
}
