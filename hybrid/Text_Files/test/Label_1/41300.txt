n = int(input())
pos, ans = (float('inf'), 0), 0
for _ in range(n):
    x, a = map(int,input().split())
    if x < 0:
        pos = min(pos, x)
    elif x > 0:
        ans += a
        if x < pos:
            pos = x
print(ans)
