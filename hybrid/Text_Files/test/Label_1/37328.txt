import math
import sys

input = sys.stdin.readlines()
t = int(input[0])
for i in range(t):
    n, x = map(int, input[2 * i + 1].split())
    a = list(map(int, input[2 * i + 2].split()))
    a.sort()
    ans = math.ceil(x / a[0])
    print(ans)
