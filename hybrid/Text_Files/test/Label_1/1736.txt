#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, k, ans = 0;
  cin >> n;
  vector<int> v(n + 1);
  for (int i = 1, j, l; i <= n; i++) {
    cin >> j >> l;
    if (j > k) v[i] = 1, ans++;
    else if (j <= k && l >= k) v[i] = 1, ans++;
  }
  cout << ans << endl;
  return 0;
}
