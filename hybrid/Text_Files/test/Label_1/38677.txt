import re

s, t = input(), input()
queries = [list(map(int, input().split())) for _ in range(int(input()))]

for a, b, c, d in queries:
    sa = s[a - 1:b]
    tb = t[c - 1:d]
    print(int(re.sub('A+(?=A+)|(B+|C+)', '', sa) == re.sub('A+(?=A+)|(B+|C+)', '', tb)))
