n, m = map(int, input().split())
w = [*map(int, input().split())]
x, y = zip(*(map(int, input().split()) for _ in range(m)))
for i in x+y: w[i-1]-=1
print("ALIVE" if all(w[i-1]>=0 for i in x+y) else "DEAD")
print(*sorted({i for i, j in enumerate(w, 1) if j>0}))
