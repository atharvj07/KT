n, m = map(int, input().split())
a, cnt = [0] * (n + 1), 0
for ai in map(int, input().split()): a[ai] += 1
for ai in a: cnt += not ai
print(''.join(map(str,
    [1 if cnt == i else 0 for i in range(m)]
  ))
)
