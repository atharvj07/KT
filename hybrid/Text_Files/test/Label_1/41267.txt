n = int(input())
a = [int(x) for x in input().split()]
ans = 0
for i in range(1, n+1):
    b = a[:i]
    b.reverse()
    b.extend(a[i:])
    for j in range(1, n+1):
        if j == 1 or b[j] >= b[j-1]: ans = max(ans, j)
print(ans)
