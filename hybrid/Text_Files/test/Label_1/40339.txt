import sys

def solve():
    n, k = map(int, input().split())
    s = input()
    cnt = {}
    mx = 0
    for i in s:
        cnt[i] = cnt.get(i, 0) + 1
        mx = max(mx, cnt[i])
    ans = min(n, (k + mx - 1) // mx * mx)
    print(ans)

t = int(input())
while t > 0:
    solve()
    t -= 1
