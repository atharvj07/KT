#include <bits/stdc++.h>
using namespace std;
#define int long long
main() {
  int n;
  cin >> n;
  int a[n * 2], b[n * 2];
  for (int i = 0; i < n * 2; i++) cin >> a[i], b[i] = a[i];
  sort(a, a + n * 2);
  sort(b, b + n * 2);
  cout << (a[n - 1] - a[0]) * (b[n - 1] - b[0]) << endl;
}
