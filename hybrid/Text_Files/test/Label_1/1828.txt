#include <cstdio>

int main() {
    int n;
    scanf("%d", &n);
    int t[n + 1];
    for(int i = 1; i <= n; i++) scanf("%d", &t[i]);
    int cnt = 1;
    for(int i = 1; i <= n; i++) {
        cnt += cnt - 1;
        cnt += (cnt - 1) * 2 * t[i] + 1;
    }
    printf("%d\n", cnt);
    return 0;
}
