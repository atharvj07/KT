import sys
input = sys.stdin.readline

n = int(input())
a, b = list(map(int, input().split()))
h = list(map(int, input().split()))

b_sum = 0
for i in range(n):
    b_sum += min(h[i], b)
    h[i] = max(0, h[i] - b)

if a < b_sum:
    print(-1)
else:
    print(b_sum * 2)
