#include<bits/stdc++.h>
using namespace std;
int main(){
    int a,ta,b,tb;
    cin>>a>>ta>>b>>tb;
    string s;
    cin>>s;
    int h=stoi(s.substr(0,2)),m=stoi(s.substr(3));
    int intha=(h*60+m)/a,inthb=(h*60+m)/b;
    cout<<max(0,(inthb*tb-intha*ta-m)/ta-1)<<endl;
    return 0;
}
