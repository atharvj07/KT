n = int(input().strip())
groups = list(map(int, input().strip().split()))

ones = groups.count(1)
twos = groups.count(2)

teams = min(twos, ones) + (max(twos, ones) - min(twos, ones)) // 3

print(teams)
