n, k = map(int, input().split())
perms = [list(map(int, input().split())) for _ in range(k)]
pos = [[0] * n for _ in range(k)]

for i in range(k):
    for j in range(n):
        pos[i][perms[i][j]-1] = j

dp = [1] * n
for i in range(1, n):
    for j in range(i):
        if all(pos[p][i] > pos[p][j] for p in range(k)):
            dp[i] = max(dp[i], dp[j] + 1)

print(max(dp))
