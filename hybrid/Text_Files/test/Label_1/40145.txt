n = int(input())
degree = [0] * n

for _ in range(n - 1):
    u, v = map(int, input().split())
    degree[u - 1] += 1
    degree[v - 1] += 1

leaves = sum(d == 1 for d in degree)

print("YES" if leaves == 2 else "NO")
