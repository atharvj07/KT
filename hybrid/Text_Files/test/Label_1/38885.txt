n=int(input())
t=[]
for i in range(n):
	x,a=map(int,input().split())
	t.append({
		"pos":x,
		"val":a
		})
t.sort(key=lambda x:x["pos"])
ans=0
d=1
i=0
while i<n:
	j=i
	sum=0
	while j<n and t[j]["pos"]*d>0:
		sum+=t[j]["val"]
		j+=1
	if sum>ans:	ans=sum
	d*=-1
	i=j
print(ans)
