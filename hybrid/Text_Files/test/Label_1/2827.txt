#include <bits/stdc++.h>
using namespace std;

int main() {
  long long c, r, b, w1, w2;
  cin >> c >> r >> b >> w1 >> w2;
  cout << max({0LL, (c / w1) * r, (c / w2) * b, ((c - w1) / w2) * b + r, ((c - w2) / w1) * r + b}) << endl;
}
