#include<bits/stdc++.h>
using namespace std;
int main(){
  int n;cin>>n;vector<string> a(n),b(n);
  for(string &i:a)cin>>i;for(string &i:b)cin>>i;
  int ans=0;
  for(int i=0;i<n;++i){
    for(int j=0;j<min(a[i].size(),b[i].size());++j){
      if(a[i][j]!=b[i][j])++ans;
    }
  }cout<<ans<<'\n';
}
