s = input().strip()
last_positions = {}
min_k = len(s)

for i, char in enumerate(s):
    if char in last_positions:
        min_k = min(min_k, i - last_positions[char])
    last_positions[char] = i

min_k = min(min_k, len(s) - min(last_positions.values()))

print(min_k)
