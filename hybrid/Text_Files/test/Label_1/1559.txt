#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,a[105],sum=0,x;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        sum+=a[i];
    }
    if(sum%2==0) x=sum/2-1;
    else x=sum/2;
    cout<<x<<'\n';
    return 0;
}
