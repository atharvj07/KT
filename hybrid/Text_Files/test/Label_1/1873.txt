#include <iostream>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c;
    cin >> a >> b >> c;
    cout << min(a, min(b, c)) + min(a, min(b, c + 1)) + min(a + 1, min(b, c)) << endl;
  }
  return 0;
}
