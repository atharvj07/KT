#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    int t;
    vector<int>a;
    while(cin>>t)a.push_back(t);
    sort(a.begin(),a.end());
    cout<<a[0]<<endl;
    return 0;
}
