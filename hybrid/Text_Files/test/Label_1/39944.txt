n = int(input())
s = input()

dp = [0] * 26
res = 0

for char in s:
    i = ord(char) - ord('a')
    dp[i] = max(dp[:i]) + 1
    res = max(res, dp[i])

print(res)
colors = [0] * n
for i in range(n):
    colors[i] = dp[ord(s[i]) - ord('a')]

print(*colors)
