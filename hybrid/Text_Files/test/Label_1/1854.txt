#include <iostream>

using namespace std;

int main() {
  long long n;
  cin >> n;
  n /= 2;
  long long k = 1;
  while (k * 11 <= n)
    k *= 2;
  k *= 5;
  while ((k - 1) * 11 > n)
    k--;
  cout << k << endl;
  return 0;
}
