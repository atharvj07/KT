#include <bits/stdc++.h>
using namespace std;

int main() {
  int n; cin >> n;
  long long tot = 0;
  for (int i = 1; i <= n; i++) {
    long long tmp; cin >> tmp;
    tot += tmp;
  }
  cout << tot % (n + 1) << endl;
  return 0;
}
