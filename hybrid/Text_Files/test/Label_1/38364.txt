import sys
from math import pi
input = sys.stdin.readline

n = int(input())
cakes = [list(map(int, input().split())) for _ in range(n)]
cakes.sort(key=lambda c: (c[1], c[0]))

v = 0
res = 0
for r, h in cakes:
    if v < h:
        v = h
        res += pi*r**2*h
print(res)
