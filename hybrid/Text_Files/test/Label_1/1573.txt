#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long l, r;
    cin >> l >> r;
    long long ans = (r - l) / 2 + 1;
    ans = max(ans, 0LL);
    cout << ans << endl;
  }
  return 0;
}
