import sys
input = sys.stdin.readline

n = int(input())
a = list(map(int, input().split()))
can = False
for i in range(1<<(n+1)):
    s = 0
    for j in range(n):
        if i & (1<<j): s -= a[j]
        else: s += a[j]
    if(s%360 == 0): can = True
print("YES" if can else "NO")
