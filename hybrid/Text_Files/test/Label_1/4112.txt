#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n;
  cin >> n;
  bool found = false;
  while (n--) {
    string s;
    int a, b;
    cin >> s >> a >> b;
    if (a < 2400 && b >= 2400) found = true;
  }
  cout << (found ? "YES" : "NO") << endl;
  return 0;
}
