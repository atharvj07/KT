import sys

n = int(input())
a = [0] + list(map(int, input().split()))
last = [0] * (n + 1)
ans = 0
for i in range(1, n + 1):
    if last[a[i]] == 0:
        ans += 1
        for j in range(last[a[i]], i + 1):
            last[a[j]] = i
print(ans)
