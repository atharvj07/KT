#include <bits/stdc++.h>
using namespace std;
#define int long long
template<typename T> bool chkmin(T& x, const T& y) { return x > y ? x = y, true : false; }
#define set_vect(a,n) vector<int>a(n); for(auto &x : a) cin >> x;
const int N = 1e6 + 5;
int n, a[N];
main() {
    int t; cin >> t;
    while(t--) {
        cin >> n; set_vect(a, n);
        sort(a.begin(), a.end());
        cout << a[0] << ' ' << a[0] << ' ' << a[n - 1] << ' ' << a[n - 1] << '\n';
    }
    return 0;
}
