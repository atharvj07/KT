#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long s, n, k;
    std::cin >> s >> n >> k;
    std::cout << (n <= s - k + 1 ? "YES" : "NO") << '\n';
  }
  return 0;
}
