#include<bits/stdc++.h>
using namespace std;
int main() {
    int t;
    cin >> t;
    while (t--) {
        long long n, x, y, ans1, ans2;
        cin >> n >> x >> y;
        cout << min(x, y) << " " << x + y - (x == y) << endl;
    }
    return 0;
}
