n, x = [int(x) for x in input().split()]
a = sorted([(int(x[1]), int(x[2]), int(x[0]) == 0) for x in [input().split() for _ in range(n)]], key=lambda x: x[0])
candies = 0
last = 2
for h, w, t in a:
    if t != last and x >= h:
        candies += 1
        last = t
        x += w
print(candies)
