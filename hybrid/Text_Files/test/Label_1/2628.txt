#include <iostream>
using namespace std;

int main() {
  long long t;
  cin >> t;
  while (t--) {
    long long x, y, a, b;
    cin >> x >> y >> a >> b;
    if ((y - x) % (a + b) == 0)
      cout << (y - x) / (a + b) << "\n";
    else
      cout << -1 << "\n";
  }
  return 0;
}
