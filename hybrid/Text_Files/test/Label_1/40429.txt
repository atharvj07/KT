import heapq

n = int(input())
adj = [[] for i in range(n+1)]
vis = [False for i in range(n+1)]
maxEdge = 0

for i in range(1,n):
  a,b,c = map(int,input().split())
  maxEdge = max(maxEdge,c)
  adj[a].append((b,c))
  adj[b].append((a,c))

limit = []
for i in range(1,n+1):
  x = int(input())
  heapq.heappush(limit,(x,i))

def dfs(u,p,cnt,dis):
  #print(u,cnt,dis)
  vis[u] = True
  if dis > maxEdge: return
  if cnt == 0:
    ans.append((dis,u))
    return
  
  for v,w in adj[u]:
    if vis[v] == True: continue
    if cnt > limit[0][0]:
      heapq.heappop(limit)
    
    if v < limit[0][1]:
      dfs(v,u,cnt-1,dis+w)
    else:
      dfs(v,u,cnt,dis+w)

ans = []
dfs(1,1,limit[0][0],0)
print(ans[0][0])
