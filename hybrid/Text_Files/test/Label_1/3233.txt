#include <iostream>
using namespace std;
int main() {
  int n, k, a = 0;
  cin >> n >> k;
  for (int i = 1, t = 0; i <= n; i++) {
    t += 5 * i;
    if (t + k <= 240) a++;
  }
  cout << a << endl;
  return 0;
}
