def can_transform(x, y):
    while y > 0:
        if x == y:
            return "YES"
        if y % 2 == 0:
            y //= 2
        elif y % 10 == 1:
            y //= 10
        else:
            break
    return "NO"

x, y = map(int, input().split())
print(can_transform(x, y))
