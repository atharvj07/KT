import sys

n,k=map(int, input().split())
a=[]
for i in range(n):
    x,y=map(int, input().split())
    a.append([x,y])

ans=0
for x,y in a:
    ans+=(x==y)
    for z in a:
        u,v=z
        if (u!=x and u!=y) or (v!=x and v!=y):
            ans+=1
print(ans//2)

