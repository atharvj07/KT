#include <iostream>
#include <cmath>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n;
    cin >> n;
    cout << n / 2 + (long long)sqrt(n) + (long long)cbrt(n) - 1 << endl;
  }
  return 0;
}
