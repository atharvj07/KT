n, m = map(int, input().split())
groups = list(map(int, input().split()))

buses = 0
current_bus_capacity = m

for group in groups:
    if group <= current_bus_capacity:
        current_bus_capacity -= group
    else:
        buses += 1
        current_bus_capacity = m - group

buses += 1
print(buses)
