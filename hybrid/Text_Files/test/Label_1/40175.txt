fmt, t = input(), input()
h, m = map(int, t.split(':'))
if fmt == '12':
    if h > 12:
        h -= 12
        t = f'{h:02d}:{m:02d}'
    if h == 0:
        t = f'12:{m:02d}'
elif fmt == '24':
    if h > 23 or m > 59:
        t = f'{h % 24:02d}:{m % 60:02d}'
print(t)
