#include <iostream>
using namespace std;
int main() {
    int n, a, b;
    cin >> n >> a >> b;
    string s;
    cin >> s;
    int ans = abs(b - a);
    if (s[a - 1] == s[b - 1]) { ans = 0; }
    cout << ans << endl;
    return 0;
}
