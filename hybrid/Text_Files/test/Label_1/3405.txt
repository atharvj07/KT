#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &i : a) { cin >> i; }
  int ans = 0;
  for (int i = 1; i < n - 1; i++) {
    ans += a[i] == 0 && a[i - 1] && a[i + 1];
  }
  cout << ans << endl;
  return 0;
}
