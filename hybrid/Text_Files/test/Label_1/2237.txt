#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, a[100010], ans = 1;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        ans *= a[i];
    }
    cout << ans << "\n";
    return 0;
}
