from typing import List
import math

def gauss(n:int) -> int:
    return (1 + n) * n // 2

n, m = map(int, input().split())
a = [0] * n
for _ in range(m):
    x, d = map(int, input().split())
    for i in range(n):
        a[i] += (x + d * abs(i - (n - 1) // 2))
print(sum(a) / n)
