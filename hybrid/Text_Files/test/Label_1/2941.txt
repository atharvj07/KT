#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define all(x) x.begin(), x.end()
#define rev(x) reverse(all(x))
#define vi vector<int>
#define pii pair<int,int>
#define mset(a, val) memset(a, val, sizeof(a))
#define MP make_pair
#define PB push_back
#define MP make_pair
const int mod = 1e9 + 7;
const int MAX = 2e5 + 5;
const double pi = acos(-1);
void fast(){ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);}
ll binpow(ll a, ll b){ll ans = 1;while(b){if(b % 2) ans = (ans * a) % mod;a = (a * a) % mod;b /= 2;}return ans % mod;}
ll modpow(ll a, ll b){ll ans = 1;while(b){if(b % 2) ans = (ans * a) % mod;a = (a * a) % mod;b /= 2;}return ans;}
ll gcd(ll a, ll b){if(b == 0) return a;return gcd(b, a % b);}
ll lcm(ll a, ll b){return (a / gcd(a, b)) * b;}

int main(){fast();
    ll n, m, k; cin >> n >> m >> k;
    ll l = 1, r = n * m;
    while(l < r){
        ll mid = l + (r - l) / 2;
        ll count = 0;
        for(int i = 1; i <= n; i++){
            count += min(m, mid / i);
        }
        if(count < k) l = mid + 1;
        else r = mid;
    }
    cout << l << endl;
    return 0;
}
