#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, x;
    long long sum = 0;
    cin >> n;
    while (n--) {
        cin >> x;
        sum += x != 0;
    }
    cout << sum - 1 << endl;
}
