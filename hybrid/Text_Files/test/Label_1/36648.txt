import math

hh, mm = map(int, input().split())
H, D, C, N = map(int, input().split())

if hh >= 20:
    C = round(C * 0.8, 3)
if mm > 0:
    H += D * (60 - mm)

need = math.ceil(H / N)
print(need * C)
