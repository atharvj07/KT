import math

k, n, s, p = map(int, input().split())

sheets_needed_per_person = math.ceil(n / s)
total_sheets_needed = sheets_needed_per_person * k

packs_needed = math.ceil(total_sheets_needed / p)

print(packs_needed)
