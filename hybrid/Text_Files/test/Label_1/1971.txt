#include <bits/stdc++.h>
using namespace std;
const int maxn=1e5+10;
int a[maxn],n,t;
int main(){
    cin>>t;
    while(t--){
        cin>>n;
        for(int i=0;i<n;++i) cin>>a[i];
        sort(a,a+n);
        cout<<(long long)a[0]*a[1]*a[2]*a[3]*a[4]<<endl;
    }
    return 0;
}
