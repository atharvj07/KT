n = int(input()); cur = [int(i) for i in input().split()]
cur.sort(); ans = -1
for i in range(1, 1 << n):
    sum = 0
    for j in range(n):
        if i >> j & 1: sum += cur[j]
    if not sum in cur:
        ans = sum; break
print(ans)
