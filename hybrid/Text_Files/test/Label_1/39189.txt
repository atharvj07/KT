t = int(input())
for _ in range(t):
    n = input()
    length = len(n)
    summands = [int(digit + '0'*(length-i-1)) for i, digit in enumerate(n) if digit != '0']
    print(len(summands))
    print(*summands)
