import sys, os, math

n = int(input())
a = list(map(int, input().split()))

s = sum(a)

for i in range(n):
    for j in range(n):
        if a[i] % a[j] == 0:
            s = min(s, sum(a) - a[i] + a[j] * (a[i] // a[j]))

print(s)
