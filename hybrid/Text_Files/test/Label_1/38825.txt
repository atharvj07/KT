from collections import Counter

for q in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    cnt = Counter(a)
    ans = len(cnt) - 1
    if ans < 0: ans = 0
    print(ans)
