for i in range(int(input())):
    k = 0
    for s in [input() for _ in range(int(input()))]:
        l = [0] * len(s)
        for j in range(len(s) // 2 + 1):
            l[j], l[len(s) - 1 - j] = int(s[j]) == 0, int(s[len(s) - 1 - j]) == 0
            k += l[len(s) - 1 - j] == l[j]
    print(k)
