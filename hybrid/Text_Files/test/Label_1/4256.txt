#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long l1, r1, l2, r2, k; cin >> l1 >> r1 >> l2 >> r2 >> k;
    long long ans = max(l2, l1) - min(r2, r1);
    if (k >= max(l2, l1) && k <= min(r2, r1)) ans--;
    cout << max(ans, 0LL) << endl;
    return 0;
}
