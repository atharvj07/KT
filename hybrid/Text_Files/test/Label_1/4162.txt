#include<bits/stdc++.h>
using namespace std;
int main() {
	long long int n, m, k;
	cin >> n >> m >> k;
	if (k > n * m - 1)
		cout << -1 << endl;
	else
		cout << (n * m + k - 1) / k << endl;
	return 0;
}
