from collections import defaultdict
import sys 

n = int(input())
a = list(map(int, input().split()))
d = defaultdict(list)

for i in a:
    d[i].append(len(d[i]))
for val, lst in d.items():
    if len(lst) >= 2:
        print('NO')
        sys.exit()

a.sort()
x = len(set([i%a[-1] for i in a]))
y = a[-1]//x
print('YES')
print(x, y)
