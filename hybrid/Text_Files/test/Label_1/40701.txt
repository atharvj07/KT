n = int(input())
a = list(map(int, input().split()))
b = sorted(a)
ans = sum(b)
for i in b:
    for j in b:
        if i % j == 0:
            ans = min(ans, sum(b) + i - j)
            ans = min(ans, sum(b) - i + j)
print(ans)
