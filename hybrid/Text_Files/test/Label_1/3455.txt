#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i, n) for (ll i = 0; i < n; i++)
template <class T> inline bool chmax(T &a, T b) { if (a < b) { a = b; return 1; } return 0; }
template <class T> inline bool chmin(T &a, T b) { if (a > b) { a = b; return 1; } return 0; }
const ll INF = 1LL << 60;
const ll mod = 1e9+7;
int main() {
  ll n; cin >> n;
  string a, b, c; cin >> a >> b >> c;
  ll an = 0; unordered_map<char, ll> ama;
  rep(i, a.size()) {
    ama[a[i]]++;
    chmax(an, ama[a[i]]);
  }
  ll bn = 0; unordered_map<char, ll> bma;
  rep(i, b.size()) {
    bma[b[i]]++;
    chmax(bn, bma[b[i]]);
  }
  ll cn = 0; unordered_map<char, ll> cma;
  rep(i, c.size()) {
    cma[c[i]]++;
    chmax(cn, cma[c[i]]);
  }
  if (an == bn && bn == cn) cout << "Draw" << endl;
  else if (an > bn && an > cn) cout << "Kuro" << endl;
  else if (bn > an && bn > cn) cout << "Shiro" << endl;
  else if (cn > an && cn > bn) cout << "Katie" << endl;
  return 0;
}
