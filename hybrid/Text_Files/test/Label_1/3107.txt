#include <iostream>
#include <cmath>
using namespace std;

int main() {
  long long m;
  cin >> m;
  long long mx = sqrt(m);
  long long sum = mx * (mx + 1) * (2 * mx + 1) / 6;
  cout << mx << ' ' << sum << endl;
}
