n = int(input())
p = list(map(int, input().split()))
mx, mn = p[-1], p[-1]
ans = 0
for i in range(n - 1, -1, -1):
    mx = max(mx, p[i])
    ans += max(0, mx - p[i])
    mn = min(mn, p[i])
for i in range(n - 1):
    mn = min(mn, p[i])
    ans += max(0, p[i] - mn)
print(ans)
