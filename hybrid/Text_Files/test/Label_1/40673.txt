import sys
input = sys.stdin.readline

n, k = map(int, input().split())
x = list(map(int, input().split()))
ans = n * (n + 1) // 2
for i in x:
    ans -= abs(i - min(x)) + abs(i - max(x)) - 1
print(ans)
