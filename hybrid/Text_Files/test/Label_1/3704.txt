#include <iostream>

using namespace std;

int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int ans = 0;
  for (int i = 0; i < n; i++) {
    if (i > 0 && s[i] == s[i - 1])
      continue;
    ans++;
    while (i < n && s[i] == s[i + 1])
      i++;
  }
  cout << ans;
  return 0;
}
