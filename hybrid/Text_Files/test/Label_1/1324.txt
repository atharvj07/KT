#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int ans = 0;
  for (int i = 0; i < s.size();) {
    char c = s[i];
    int j = i;
    while (s[j] == c && j < s.size()) j++;
    ans++;
    i = j;
  }
  cout << ans << endl;
}
