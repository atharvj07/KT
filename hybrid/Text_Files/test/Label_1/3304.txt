#include<bits/stdc++.h>

using namespace std;

int main(void) {
    int n, a, b, l = 100000;
    cin >> n >> a >> b;
    l = min(l, n * 2 / a);
    l = min(l, n * 2 / b);
    cout << l << endl;
    return 0;
}
