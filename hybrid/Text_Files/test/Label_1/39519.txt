n = int(input())
left_open = 0
right_open = 0

for _ in range(n):
    l, r = map(int, input().split())
    left_open += l
    right_open += r

left_close = n - left_open
right_close = n - right_open

seconds = min(left_open, left_close) + min(right_open, right_close)

print(seconds)
