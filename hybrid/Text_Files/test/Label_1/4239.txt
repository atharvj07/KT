#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    long long l1, r1, l2, r2, k;
    cin >> l1 >> r1 >> l2 >> r2 >> k;
    cout << max({0LL, min(r1, r2) - max(l1, l2), max(l1, l2) - min(r1, r2)}) << endl;
    return 0;
}
