from queue import Queue
n=int(input());a=[int(x) for x in input().split()];X=1<<30;f=lambda x,y:min([x,y],[x^y,X]);b=[[0 for _ in range(10)]for _ in range(10)];
for x in a:
  for y in a:b[x][y]=f(b[x][y],y-x);
d=dict();q=Queue();q.put(0);
while not q.empty():
  x=q.get();
  if x in d:continue;
  if len(d)==len(a)-2:break;
  d[x]=True;
  for y in range(1,10):
    if not (y^x)&(1<<(y^x)) not in d:
      q.put(1<<(y^x))
print(len(a)-len(d))
