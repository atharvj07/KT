y, k, n = map(int, input().split())
x_values = []
x_start = k - y % k
x = x_start
while y + x <= n:
    x_values.append(x)
    x += k

if x_values:
    print(" ".join(map(str, x_values)))
else:
    print(-1)
