#include <bits/stdc++.h>
using namespace std;
#define loop(i, a, n) for (int i = a; i < n; ++i)
typedef long long ll;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    if (n == 2)
      cout << "NO" << endl;
    else
      cout << "YES" << endl;
  }
  return 0;
}
