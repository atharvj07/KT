#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    long long n, k;
    cin >> n >> k;
    k = (k + 1) / 2;
    long long ans = n / k;
    if (ans * k + ans + 1 <= n) { ans++; }
    cout << ans << "\n";
  }
}
