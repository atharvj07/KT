n, k = map(int, input().split())
busy = input()
eaten, maxBreak = 1, 0
for i in range(1, n):
    if busy[i] == '0':
        if eaten < k:
            maxBreak = max(maxBreak, i - eaten)
            eaten += 1
print(maxBreak)
