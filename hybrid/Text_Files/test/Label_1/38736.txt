s = input(); n = len(s); ans = 0; a = [0] * (n // 2 + 1)
for i in range(0, n, 2): a[i // 2] = int(s[i])
for i in range(n // 2):
    if s[2 * i + 1] == '*':
        if i > 0: a[i] *= a[i - 1]
        a[i + 1] *= a[i]
for i in range(n // 2 - 1):
    ans += a[i] * a[n // 2 - 1 - i]
print(ans)
