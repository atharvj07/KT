#include <iostream>
#include <vector>
#include <algorithm>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;
    std::vector<int> v = {a, b, c};
    std::sort(v.begin(), v.end());
    std::cout << (v[0] * 4 <= v[2] ? v[0] * 4 : 0) << std::endl;
    return 0;
}
