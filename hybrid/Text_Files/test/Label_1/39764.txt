import sys
input = sys.stdin.readline

for _ in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    ans = [0] * n
    c = 0
    for i in range(n):
        c += a[i]
        if c >= i + 1:
            c -= i + 1
            ans[i] = 1
    print(*ans)
