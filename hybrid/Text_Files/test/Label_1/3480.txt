#include <iostream>
using namespace std;
int main()
{
    long long int n, m, mini, maxi;
    cin >> n >> m;
    mini = (m - 1) * m / 2;
    maxi = n * (n - 1) / 2;
    cout << mini << " " << maxi << endl;
    return 0;
}
