#include <iostream>

using namespace std;

int main() {
  long long c, hr, hb, wr, wb;
  cin >> c >> hr >> hb >> wr >> wb;
  long long ans = hr * c / wr + hb * (c % wr) / wb;
  cout << ans;
  return 0;
}
