#include <iostream>
int main() {
  int t, a, b, k;
  std::cin >> t;
  if (t > 0) { std::cin >> a >> b; }
  int ca = 0;
  while (--t > 0) {
    std::cin >> a >> b;
    ca += (a > k);
  }
  std::cin >> k;
  std::cout << ca + (k < a) << std::endl;
  return 0;
}
