#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        vector<int> a(4);
        for (int i = 0; i < 4; i++) cin >> a[i];
        for (int i = 0; i < 4; i++) cout << (a[i] > 0 ? "Ya " : "Tidak ");
        cout << endl;
    }
    return 0;
}
