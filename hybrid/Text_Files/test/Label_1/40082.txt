import sys
input = sys.stdin.readline
n, M = map(int, input().split())
t = list(map(int, input().split()))
p = [None] * n
cur = 0
for i in range(n):
    p[i] = max(cur, max(0, cur + t[i] - M))
    cur += t[i]
print(*p, sep='\n')
