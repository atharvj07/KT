def power(x: int, m: int) -> int:
    ret = 1
    while m:
        if m&1: ret = ret*x
        x = x*x
        x %= mod
        ret %= mod
        m >>= 1
    return ret

mod = 998244353
n, m, k = map(int, input().split())
print(power(m, n - k - 1) * power(m - 1, k))
