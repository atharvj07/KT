#include <iostream>

using namespace std;

int main() {
  long long n;
  cin >> n;
  long long ans = 1;
  while (n) {
    long long digit = n % 10;
    if (digit != 0) ans *= digit;
    n /= 10;
  }
  cout << ans << endl;
  return 0;
}
