#include <iostream>
using namespace std;

int main() {
  int a, b, c, n;
  cin >> a >> b >> c >> n;
  if (a + b - c > n || a + c - b > n || b + c - a > n) cout << -1 << endl;
  else cout << n - (a + b + c - 2 * c) << endl;
  return 0;
}
