import math

p, x = map(int, input().split())
if (9 * x) % 10 != 0:
    print('Impossible')
else:
    num = [0] * p
    last = x % 10
    for i in range(p - 1, -1, -1):
        num[i] = last
        last = ((last * 10) % x + 9 * (x // 10) % x) % x
    print(''.join(map(str, num)))
