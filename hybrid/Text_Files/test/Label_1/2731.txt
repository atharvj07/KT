#include<bits/stdc++.h>
using namespace std;

int main(){
	int n,i,mx=0,mn=INT_MAX,x;
	cin>>n;
	while(n--){
		cin>>x;
		mx=max(mx,x);
		mn=min(mn,x);
	}
	if(mx<=2*mn)cout<<0<<endl;
	else cout<<1<<endl;
	return 0;
}
