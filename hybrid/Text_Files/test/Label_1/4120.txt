#include <bits/stdc++.h>
using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, k;
    cin >> n >> k;
    int al1, ar1, al2, ar2;
    cin >> al1 >> ar1 >> al2 >> ar2;
    cout << max(max(0, k - ar2 + al2), max(0, k - ar1 + al1)) << endl;
  }
  return 0;
}
