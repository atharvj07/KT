import sys

n, m = map(int, input().split())
x = list(map(int, input().split()))
p = list(map(int, input().split()))

y = x[0]

for i in range(m):
    if y + p[i] * (n - 1) >= x[-1]:
        print("YES")
        print(y, i + 1)
        sys.exit()

    y += 1

print("NO")
