a = list(map(int, input().split()))
a.sort()
ans = 0
while len(a) >= 2 and a[0] + a[-1] > 1:
    if (a[0] + a[-1]) % 2 == 1:
        ans += 1
        a.pop()
        a.pop(0)
    else:
        d = min(a[0], 106)
        a[0] -= d
        a[-1] += d
print(ans)
