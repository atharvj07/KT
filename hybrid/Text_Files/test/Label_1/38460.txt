import sys

input = sys.stdin.readline

t = int(input())
for _ in range(t):
    n, p, k = map(int, input().split())
    a = sorted(map(int, input().split()))
    i = 0
    ans = 0
    while i < n and p >= a[i]:
        ans += 1
        p -= a[i]
        i += k - 1
    print(ans)
