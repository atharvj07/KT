#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main(){
    ll n,i,ans=0;
    cin>>n;
    vector<ll>v(n);
    for(i=0;i<n;i++){cin>>v[i];}
    sort(v.begin(),v.end());
    for(i=1;i<n;i++){ans+=v[i]-v[0];}
    cout<<ans<<endl;
    return 0;
}
