#define ll long long
#define rep(i,x,y) for(long long i = x; i < y; i++)
#define fs first
#define sc second
#include<bits/stdc++.h>
using namespace std;
ll t,n,x,w,a[200001],b[6]={0},ans;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> t;
    while(t--)
    {
        ans = 0;
        memset(b,0,sizeof(b));
        cin >> n;
        rep(i,0,n)
        {
            cin >> x;
            b[x-1]++;
        }
        rep(i,0,6)
        {
            w = 0;
            for(ll j = 0; j < 6; j++)
            {
                if(i + j > 5) continue;
                w += max(0LL,b[i + j] - b[j]);
            }
            ans = max(ans,w);
        }
        cout << ans << "\n";
    }
}
