import sys

n = int(input())
L = []
for _ in range(n):
    x, a = map(int, input().split())
    L.append((x, a))
L.sort()
ans = 0
cur = 0
for x, a in L:
    if cur < x:
        ans += a
        cur = x + a
print(ans)
