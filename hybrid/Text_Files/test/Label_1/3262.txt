#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    int n, t[32], ans = 0;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> t[i];
    sort(t, t+n);
    for (int i = 0; i < n; i++) ans += (1 << i) * t[i];
    cout << ans << endl;
    return 0;
}
