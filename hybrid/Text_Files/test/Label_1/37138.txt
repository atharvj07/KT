n=int(input());a=[int(i) for i in input().split()]
a.sort()
ans=0
while len(a)>1:
    if a[0]==a[1]:a.pop(0);a.pop(0);continue
    if len(a)>2 and a[0]+a[1]<=a[2]:
        ans+=a[0]+a[1];a.pop(0);a.pop(0);a[0]-=a[0]+a[1]
    else:
        ans+=a[0];a.pop(0)
print(ans)
