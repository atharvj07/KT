#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a, b, t, x, ans;
    cin >> n >> a >> b >> t;
    string s;
    cin >> s;
    x = s.find('h');
    ans = (t + b) / (a + b);
    if (ans > x + 1)
        ans = x + 1;
    cout << ans << endl;
    return 0;
}
