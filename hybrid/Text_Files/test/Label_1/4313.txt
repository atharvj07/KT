#include<bits/stdc++.h>
using namespace std;
int main(){
  int n; cin>>n;
  vector<int> d(n);
  for(int &x:d) cin>>x;
  int s,t; cin>>s>>t;

  if(s==t){
    cout<<0<<'\n';
    return 0;
  }

  int ans = 1e9;
  ans = min(ans,d[s-1]+d[t-1]);
  ans = min(ans,d[n-s+1]+d[n-t+1]);
  cout<<ans<<'\n';
}
