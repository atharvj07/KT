#include <cstdio>
#include <algorithm>
using namespace std;

int n, m, x;

int main() {
  scanf("%d%d", &n, &m);
  x = n / m;
  for (int i = 1; i < m; i += 1) {
    printf("%d ", x);
  }
  printf("%d\n", n - x * (m - 1));
}
