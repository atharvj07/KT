import math
import sys
input=sys.stdin.readline
for i in range(int(input())):
    n,last=map(int,input().split())
    a=sorted([int(x) for x in input().split()])
    d=1
    ans=1
    while d<last:
        d+=min(num for num in a if num>d-num)
        ans+=1
    print(ans)
