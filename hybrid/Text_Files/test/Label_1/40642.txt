import sys
n=int(input())
p=list(map(float,input().split()))
p.sort()
ans=0
lst=0.0
prs=1.0
for i in range(n-1,-1,-1):
    if prs*(1-p[i])>lst:
        lst=prs*(1-p[i])
        ans=prs*p[i]
    prs*=(1-p[i])
print("{0:.9f}".format(ans))
