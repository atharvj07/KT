import math

r = int(input())
root = int(math.sqrt(r - 1))
if root * root != r - 1:
    print("NO")
else:
    x = int((root - 1) / 3)
    y = int((root + 2) / 3)
    if x > 0 and y > 0:
        print(x, y)
    else:
        print("NO")
