#include<bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  int cnt = 0, ans = 0;
  for (char c : s) {
    cnt += (c == 'a');
    ans = max(ans, min(cnt, int(s.size() - cnt)) * 2);
  }
  cout << ans << "\n";
}
