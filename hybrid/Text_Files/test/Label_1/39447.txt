import sys

t = int(input())
for i in range(t):
    n = int(input())
    s = input()
    freq = [0] * 26
    for j in s:
        freq[ord(j) - ord('a')] += 1
    odd = 0
    for j in freq:
        odd += j % 2
    if odd > 1: print(-1)
    else: print((n - max(freq)) // 2)
