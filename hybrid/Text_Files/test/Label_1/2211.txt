#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << (a > 0 ? "Ya " : "Tidak ") << (b > 0 ? "Ya " : "Tidak ")
         << (c > 0 ? "Ya " : "Tidak ") << (d > 0 ? "Ya" : "Tidak") << endl;
  }
  return 0;
}
