#include <iostream>
#include <string>
using namespace std;
int main() {
  string s;
  cin >> s;
  int ans = 0, cnt = 0, last = 0;
  for (char c : s) {
    cnt += (c == s[last]) ? 1 : -1;
    last = c - 'b';
    ans = max(ans, cnt);
  }
  cout << ans + 1 << endl;
  return 0;
}
