#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  long long x, y, n, k;
  cin >> t;
  for (int i = 0; i < t; i++) {
    cin >> x >> y >> n;
    k = n - (n % x);
    if (y > k) k -= x;
    cout << k << endl;
  }
}
