#include <iostream>
#include <sstream>
using namespace std;
char buf[1234567],l[2],r[2]; int n,i,ans; string s;
int main(){ ios::sync_with_stdio(0); cin.tie(0); cout.tie(0); cin>>n;
	for(i=1; i<=n; i++){ cin>>s; if(s!="res=res") ans++; }
	cout<<ans<<'\n';
	for(i=1; i<=n; i++){ cin>>s; if(s!="res=res") cout<<s; } }
