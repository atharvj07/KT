#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int ans = 0;
  while (n--) {
    char x = s[ans];
    if (ans && s[ans] != s[ans - 1]) ans--;
    s[ans++] = x;
  }
  cout << ans << endl;
  return 0;
}
