plan = [input() for _ in range(6)]
for i in range(6):
    for j in range(6):
        if plan[i][j] == '.' and (i == 0 or plan[i - 1][j] == '*') and (j == 0 or plan[i][j - 1] == '*'):
            plan[i] = plan[i][:j] + 'P' + plan[i][j + 1:]
            break
print('\n'.join(plan))
