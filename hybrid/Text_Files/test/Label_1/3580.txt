#include <cstdio>
int main() {
  int n, a, b, x = 0, y = 0;
  scanf("%d", &n);
  while (n--) {
    scanf("%d %d", &a, &b);
    if (a % 2) x++;
    if (b % 2) y++;
  }
  if (x == y)
    printf("0");
  else if (x != y && x + y == n)
    printf("1");
  else
    printf("-1");
}
