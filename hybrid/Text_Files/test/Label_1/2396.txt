#include <iostream>
#include <vector>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long n, m;
    std::cin >> n >> m;
    long long ans = 0;
    if (n > 1 || m > 1) {
      ans = 2;
    } else {
      ans = 1;
    }
    std::cout << ans << "\n";
  }
  return 0;
}
