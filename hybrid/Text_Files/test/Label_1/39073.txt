import sys

def main():
    n, *t = list(map(int, sys.stdin.readline().split()))
    a = list(map(int, sys.stdin.readline().split()))
    if sum(a) != n - 1:
        sys.stdout.write("-1")
        return
    rem = n - 1 - k
    roads = [(1, a[0])]
    for i in range(1, len(a)):
        if rem <= a[i]:
            sys.stdout.write("{} {}\n".format(1, a[i] - rem))
            roads.append((1, a[i] - rem))
            a[i] = rem
            rem = 0
        else:
            rem -= a[i]
            roads.append((1, a[i]))
    for i in range(len(a)):
        if a[i] == 0:
            continue
        for j in range(i + 1, len(a)):
            if a[j] == 0:
                continue
            if a[i] == 1 or a[j] == 1:
                continue
            sys.stdout.write("{} {}\n".format(roads[i][1], roads[j][1]))
            a[i] -= 1
            a[j] -= 1
            
main()
