t = int(input())
for _ in range(t):
    s = input()
    stack = []
    for char in s:
        if stack and ((stack[-1] == 'A' and char == 'B') or (stack[-1] == 'B' and char == 'B')):
            stack.pop()
        else:
            stack.append(char)
    print(len(stack))
