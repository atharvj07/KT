#include <iostream>
using namespace std;

int main() {
  long long n, k, t;
  cin >> n >> k >> t;
  long long ans = (t / k) % 2 ? n - (t % k) : n - (k - (t % k));
  cout << ans << endl;
  return 0;
}
