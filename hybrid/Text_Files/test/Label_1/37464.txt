from collections import defaultdict

s = input()
cnt = 0
sum = 0
mp = defaultdict(int)
mp[0] = 1
for i in range(len(s) - 1, -1, -1):
    if i != len(s) - 1: sum = (sum * 10 + int(s[i + 1])) % 4
    sum = (sum + int(s[i])) % 4
    cnt += mp[sum]
    mp[sum] += 1
print(cnt)
