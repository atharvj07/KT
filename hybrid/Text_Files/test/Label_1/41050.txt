n,m=map(int,input().split())
s=input()
P=[1]*m
for ch in s[::-1]:
    Q=[0]*m
    for i in range(m):
        if ch==chr(ord('a')+i):
            Q[i]+=P[i]+1
        else:
            Q[i]+=P[i]
    P=Q
print(P[0]-1)
