#include<bits/stdc++.h>
using namespace std;

int main() {
  int n, x, sum = 0, neg = 0;
  cin >> n;
  while (n--) cin >> x, sum += abs(x), neg += x < 0;
  cout << sum - min(neg, n / 2) * 2 << endl;
  return 0;
}
