import sys
input = sys.stdin.readline

for t in range(int(input())):
    n = int(input())
    s = input().strip()
    l, r = 0, n - 1
    while l < r and s[l] == '<':
        l += 1
    while r >= 0 and s[r] == '>':
        r -= 1
    print(l + n - 1 - r)
