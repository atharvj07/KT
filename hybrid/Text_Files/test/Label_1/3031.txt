#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n, k, ans = 0, cur = 0;
  cin >> n >> k;
  vector<int> v(n);
  for (int i = 0; i < n; i++) {
    cin >> v[i];
    cur += v[i];
    if (cur <= k)
      ans++;
    else
      break;
  }
  cout << ans;
  return 0;
}
