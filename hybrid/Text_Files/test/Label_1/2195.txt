#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
    ll a, b, c, d;
    cin >> a >> b >> c >> d;
    while(b != d) {
        if(b < d)
            b += a;
        else
            d += c;
    }
    cout << (b == -1 ? -1 : b) << endl;
    return 0;
}
