#include <iostream>

using namespace std;

long long a, b, n, m;

int main() {
  int t;
  cin >> t;
  while (t--) {
    cin >> a >> b >> n >> m;
    cout << ((a >= n && b >= m) || (b >= n && a >= m) ? "Yes" : "No") << endl;
  }
  return 0;
}
