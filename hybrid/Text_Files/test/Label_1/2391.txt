#include <iostream>
using namespace std;

int main() {
  int n, m;
  cin >> n >> m;
  int a = n / m;
  cout << a << ' ';
  if (n % m) cout << a + 1 << ' ';
  while (--m) cout << a << ' ';
  return 0;
}
