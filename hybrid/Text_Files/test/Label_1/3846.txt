#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a[100001];
    cin >> n;
    long long ans = 1;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        ans *= a[i];
    }
    cout << ans << endl;
}
