#include <iostream>
#include <vector>

using namespace std;

int main()
{
	int t; cin >> t;
	while (t--)
	{
		int n, x; cin >> n >> x;
		bool odd = false;
		for (int i = 0; i < n; i++)
		{
			int a; cin >> a;
			odd |= a % 2;
		}
		cout << (odd && x > 0 ? "Yes" : "No") << endl;
	}
	return 0;
}
