from bisect import bisect_left

n = int(input())
a = sorted(map(int, input().split()))
m = 0

for i in range(n // 2):
    j = bisect_left(a, a[i] // 2, i)
    if a[j] == a[i] // 2: j += 1
    if j < n // 2 and a[j] == 2 * a[i]:
        m += a[j] * a[j] - a[i] * a[i]

print(m)
