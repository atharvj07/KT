#include <iostream>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    unsigned long long a, b, n, m;
    cin >> a >> b >> n >> m;
    bool ok = (a + b < n + m || (a == 0 && n == 0) || (b == 0 && m == 0));
    cout << (ok ? "Yes" : "No") << '\n';
  }
  return 0;
}
