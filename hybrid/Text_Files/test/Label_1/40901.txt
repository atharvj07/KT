import sys
for t in range(int(input())):
    n=int(input());a=[*map(int,input().split())];b=sorted(a);x=[*b[::2],*b[1::2]]
    for i in range(n,2*n):b.append(x.pop(0))
    print(' '.join(map(str,b)))
