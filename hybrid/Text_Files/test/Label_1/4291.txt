#include<bits/stdc++.h>
using namespace std;
int main(){ int n; cin>>n; vector<string> a(n); for(auto&i:a) cin>>i; string s; cin>>s; for(int j=0,i=0; i<s.size(); i++){ int k=i; while(s[i]!=a[j][k-i]&&k<a[j].size()&&i<s.size()) i++; if(k==a[j].size()) return puts("no"),0; if(j+1!=n){ if(s[i+1]!='<'||s[i+2]!='3') return puts("no"),0; i+=2; j++; } } cout<<"yes"; }
