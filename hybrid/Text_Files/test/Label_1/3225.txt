#include <bits/stdc++.h>

using namespace std;

int main() {
  int n;
  cin >> n;
  string t;
  cin >> t;
  while (n > 1) {
    int l = 0;
    int r = n - 1;
    while (l < r) {
      swap(t[l], t[r]);
      l++;
      r--;
    }
    n--;
  }
  cout << t << endl;
  return 0;
}
