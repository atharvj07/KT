c = list(map(int, input().split()))
x = 0
if all(c):
	for i in range(3):
		y = i
		for j in range(3):
			y += (c[j] - i) // 3
		x = max(x, y)
	print(x)
else:
	print(c[0] // 3 + c[1] // 3 + c[2] // 3)
