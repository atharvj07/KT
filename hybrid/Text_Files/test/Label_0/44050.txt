for _ in range(int(input().strip())):
	n = int(input().strip())
	L = list(map(int, input().strip().split()))
	L.sort()
	prevMin = 10000000
	for i in range(n - 1):
		if abs(L[i + 1] - L[i]) < prevMin:
			prevMin = abs(L[i + 1] - L[i])
	print(prevMin)
