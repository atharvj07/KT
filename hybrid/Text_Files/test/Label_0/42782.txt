(n, a, b, k) = map(int, input().split())
open = 0
must = 0
must_arr = []
for (i, c) in enumerate(input()):
	if c == '1':
		open = 0
	else:
		open += 1
		if open == b:
			open = 0
			must += 1
			if must >= a:
				must_arr.append(i + 1)
must -= a - 1
print(must)
print(*must_arr)
