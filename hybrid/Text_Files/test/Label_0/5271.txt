#include<bits/stdc++.h>
using namespace std;
int main(){
  double n,d;
  int o=0;
  cin >> n >> d;
  while(n--){
    double a,b;
    cin >> a >> b;
    if(sqrt(a*a+b*b)<=d) {o++;}
  }
  cout << o;
  return 0;
}