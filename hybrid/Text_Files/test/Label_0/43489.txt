I = lambda : map(int, input().split())
(n, k) = I()
a = sorted(input())
p = [a[0]]
b = ord(a[0])
y = b - ord('a')
for i in range(1, n):
	if len(p) == k:
		break
	if ord(a[i]) - b >= 2:
		y += ord(a[i]) - ord('a')
		b = ord(a[i])
		p.append(a[i])
if len(p) < k:
	print(-1)
else:
	print(y + k)
