import sys

def b(l, n, f):
	a = 0
	x = n - 1
	while a <= x:
		m = a + (x - a) // 2
		if l[m] == f:
			return m
		elif l[m] > f:
			x = m - 1
		else:
			a = m + 1
	return None
for i in range(int(sys.stdin.readline())):
	n = sys.stdin.readline()
	if int(n) % 2 == 0:
		print(0)
	elif int(n[0]) % 2 == 0:
		print(1)
	else:
		c = 0
		for i in range(1, len(n) - 1):
			if int(n[i]) % 2 == 0:
				c = 1
				break
		if c == 1:
			print(2)
		else:
			print(-1)
