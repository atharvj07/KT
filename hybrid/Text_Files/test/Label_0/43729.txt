import sys
lst = list(input())
lstg = ['a', 'o', 'u', 'i', 'e']
lsts = ['n']
s = 0
for i in range(len(lst) - 1):
	if lst[i] not in lstg and lst[i] not in lsts:
		if lst[i + 1] not in lstg:
			print('NO')
			sys.exit()
if lst[-1] in lstg or lst[-1] in lsts:
	print('YES')
else:
	print('NO')
