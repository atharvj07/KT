#include <bits/stdc++.h>
using namespace std;

int main() {
  long double a,b,x,ans=0,pi=3.141592653589793;
  cin >> a >> b >> x;
  x/=a;
  if(x*2<=a*b) ans=180/(pi/atan2(b,x*2/b));
  else ans=180/(pi/atan2(2*(b-x/a),a));
  cout << fixed << setprecision(15);
  cout << ans << endl;
}