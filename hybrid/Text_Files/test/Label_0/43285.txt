t = int(input())
for j in range(t):
	n = int(input())
	a = input()
	g = a
	b = 0
	for i in range(n):
		d = a[:i]
		if (n - i) % 2 == 0:
			c = a[i:] + d
		else:
			c = a[i:] + d[::-1]
		if c < g:
			g = c
			b = i + 1
	print(g)
	if b == 0:
		print(1)
	else:
		print(b)
