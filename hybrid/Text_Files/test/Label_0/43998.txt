import sys
input = sys.stdin.readline

def solve():
	n = int(input())
	a = list(map(int, input().split()))
	s = sum(a)
	if s % n != 0:
		print(-1)
		return
	v = s // n
	k = 0
	for i in a:
		k += i > v
	print(k)
for i in range(int(input())):
	solve()
