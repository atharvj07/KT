t = int(input())
while t > 0:
	t = t - 1
	n = int(input())
	a = [int(i) for i in input().split()][:n]
	b = []
	c = []
	for j in range(n):
		if a[j] % 2 == 0:
			b.append(a[j])
		else:
			c.append(a[j])
	ans = c + b
	ans1 = ' '.join([str(l) for l in ans])
	print(ans1)
