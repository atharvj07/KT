#include <bits/stdc++.h>
using namespace std;
int main() {
  int al, ar, bl, br;
  cin >> al >> ar >> bl >> br;
  if ((bl >= ar - 1 && bl <= (ar + 1) * 2) ||
      (br >= al - 1 && br <= (al + 1) * 2))
    cout << "YES";
  else
    cout << "NO";
  return 0;
}
