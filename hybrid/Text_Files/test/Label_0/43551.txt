def is_increasement(a):
	for i in range(len(a) - 1):
		if a[i] < a[i + 1]:
			return False
	return True

def is_decreasement(a):
	for i in range(len(a) - 1):
		if a[i] > a[i + 1]:
			return False
	return True
(n, m) = [int(x) for x in input().split()]
a = [int(x) for x in input().split()]
b = sorted([int(x) for x in input().split()])
start = 0
end = len(b) - 1
aa = list(a)
side = True
for i in range(len(aa)):
	if end < 0 or start >= len(b):
		break
	if aa[i] == 0:
		if side:
			aa[i] = b[start]
			start += 1
			side = False
		else:
			aa[i] = b[end]
			end -= 1
			side = True
if not is_decreasement(aa):
	print('Yes')
else:
	aa = list(a)
	side = False
	end = len(b) - 1
	start = 0
	for i in range(len(aa)):
		if end < 0 or start >= len(b):
			break
		if aa[i] == 0:
			if side:
				aa[i] = b[start]
				start += 1
				side = False
			else:
				aa[i] = b[end]
				end -= 1
				side = True
	if not is_decreasement(aa):
		print('Yes')
	else:
		print('No')
