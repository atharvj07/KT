import math
t = int(input())
for i in range(t):
	(n, k) = [int(x) for x in input().split()]
	l = math.floor((1 + math.sqrt(1 + 8 * (k - 1))) / 2)
	t = 1 + l * (l - 1) // 2
	bpos1 = -l - 1
	bpos2 = -(k - t) - 1
	ans = ['a'] * n
	ans[bpos1] = 'b'
	ans[bpos2] = 'b'
	print(''.join(ans))
