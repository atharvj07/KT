t = int(input())

def count(s, b, e):
	res = 0
	st = 0
	for i in range(len(s)):
		if s[i] == b:
			st += 1
		if s[i] == e:
			if st > 0:
				res += 1
				st -= 1
	return res
for _ in range(t):
	s = input()
	print(count(s, '(', ')') + count(s, '[', ']'))
