import sys
n = int(input())
a = list(map(int, input().split()))
d = dict()
min_v = min(a)
ans = 10 ** 9
for (i, x) in enumerate(a):
	if x == min_v and x in d:
		ans = min(ans, i - d[x])
	d[x] = i
print(ans)
