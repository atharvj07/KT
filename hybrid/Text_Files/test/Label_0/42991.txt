import math
(n, a, q, w) = (int(input()), sorted(map(int, input().split())), 0, 0)
for i in range(1, n):
	q = math.gcd(q, a[i] - a[i - 1])
for i in range(1, n):
	w += abs(a[i] - a[i - 1]) // q - 1
print(w)
