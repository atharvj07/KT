(n, m) = [int(x) for x in input().split()]
if n >= m:
	print(n - m)
else:
	count = 0
	while n != m:
		if n < m:
			if m % 2 == 0:
				m = m / 2
			else:
				m = m + 1
			count += 1
		else:
			count += n - m
			n = m
	print('{0:.0f}'.format(count))
