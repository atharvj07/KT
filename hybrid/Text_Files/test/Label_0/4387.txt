#include <bits/stdc++.h>
using namespace std;
inline int toInt(string s) {
  int v;
  istringstream sin(s);
  sin >> v;
  return v;
}
template <class T>
inline string toString(T x) {
  ostringstream sout;
  sout << x;
  return sout.str();
}
long long x[1001], y[1001];
int main() {
  int a, m;
  cin >> a >> m;
  while (a % 2 == 0) a /= 2;
  while (m % 2 == 0) m /= 2;
  if (a % m == 0) {
    cout << "Yes" << endl;
  } else {
    cout << "No" << endl;
  }
  return 0;
}
