#include<bits/stdc++.h>
using namespace std;

int main(){
  int n;cin>>n;
  vector<int> v(n+1,0);
  for(int i=0;i<n;++i){
    int a;cin>>a;
    v[a]=v[a-1]+1;
  }
  int ans=n;
  for(int i=0;i<n+1;++i) ans=min(ans,n-v[i]);
  cout<<ans<<endl;
}