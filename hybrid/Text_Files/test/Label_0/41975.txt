from math import sqrt

def main(n):
	if not n % 2:
		if sqrt(n // 2).is_integer() or (not n // 2 % 2 and sqrt(n // 4).is_integer()):
			return 'YES'
		else:
			return 'NO'
	else:
		return 'NO'
t = int(input())
for _ in range(t):
	n = int(input())
	print(main(n))
