#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  string s;
  cin >> s;
  int SF = 0, FS = 0;
  for (int i = 0; i < n; i++) {
    if (s.substr(i, 2) == "SF") {
      SF++;
    }
    if (s.substr(i, 2) == "FS") FS++;
  }
  SF > FS ? cout << "YES" : cout << "NO";
}
