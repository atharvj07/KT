from math import sqrt
eps = 1e-05

def check(a, h, x):
	return abs(x * sqrt(4 * h * h + a * a) - a * h) < eps

def main():
	x = float(input())
	for a in range(1, 11):
		for h in range(1, 11):
			if check(a, h, x):
				print(a, h)
				return
main()
