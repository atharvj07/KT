for i in range(int(input())):
	n = int(input())
	a = input().split()
	b = []
	if n % 2 == 0:
		for i in range(n // 2):
			b.append(a[i] + ' ' + a[-i - 1])
		print(' '.join(b))
	else:
		for i in range(n // 2):
			b.append(a[i] + ' ' + a[-i - 1])
		b.append(a[n // 2])
		print(' '.join(b))
