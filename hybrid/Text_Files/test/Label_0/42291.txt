def solve():
	(n, k) = map(int, input().split(' '))
	if k < n:
		print(k)
	else:
		x = k // (n - 1)
		y = x * (n - 1)
		c = k - y
		if c == 0:
			print(n * x - 1)
		else:
			print(c + n * x)
for i in range(int(input())):
	solve()
