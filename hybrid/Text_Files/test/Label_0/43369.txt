import math
(a, b, c) = map(int, input().split())
(x, y, z) = map(int, input().split())
d = math.gcd(c, z)
t = (x - a) // d
ans = max(0, min(b + t * d, y) - max(a + t * d, x) + 1)
t += 1
ans = max(ans, min(b + t * d, y) - max(a + t * d, x) + 1)
print(ans)
