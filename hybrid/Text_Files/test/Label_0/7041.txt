#include <bits/stdc++.h>
using namespace std;
int main() {
  string s;
  cin >> s;
  int c = 0, t = 0;
  for (int i = 0; i < s.length(); i++) {
    if (s[i] == '(') {
      c++;
    } else if (s[i] == ')') {
      if (c > 0) {
        c--;
        t += 2;
      }
    }
  }
  cout << t;
}
