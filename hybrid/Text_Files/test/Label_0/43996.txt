def go():
	n = int(input())
	s = input()
	if n % 2 == 1 or s.count('(') != n // 2:
		return -1
	res = 0
	curs = 0
	prev = 0
	for ss in s:
		curs += 1 if ss == '(' else -1
		if curs == 0 and prev > 0:
			res += prev + 1
			prev = 0
		elif curs < 0:
			prev += 1
	return res
print(go())
