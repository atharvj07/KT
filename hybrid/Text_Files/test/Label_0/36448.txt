
import java.io.BufferedReader;
import java.io.FileNotFoundException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.util.HashSet;
import java.util.LinkedList;
import java.util.StringTokenizer;
import java.util.TreeSet;

/**
 *
 *
 * 3 2 3 5
 * -2 -1 4 -1 2 7 3
 *
 *
 * @author pttrung
 */
public class D {

    public static long MOD = 100000000;
    public static PrintWriter out;

    public static void main(String[] args) throws FileNotFoundException {


        Scanner in = new Scanner();
        //out = new PrintWriter(new FileOutputStream(new File("output.txt")));
        out = new PrintWriter(System.out);
        int n = in.nextInt();
        int m = in.nextInt();
        int h = in.nextInt();
        int t = in.nextInt();
        int[] count = new int[n];
        Point[] e = new Point[m];
        HashSet<Integer>[] map = new HashSet[n];
        for (int i = 0; i < n; i++) {
            map[i] = new HashSet();
        }
        for (int i = 0; i < m; i++) {
            e[i] = new Point(in.nextInt() - 1, in.nextInt() - 1);
            count[e[i].x]++;
            count[e[i].y]++;
            map[e[i].x].add(e[i].y);
            map[e[i].y].add(e[i].x);
        }

        boolean ok = false;
        // System.out.println("Start");
        for (Point p : e) {
            if ((count[p.x] >= h + 1 && count[p.y] >= t + 1) || (count[p.y] >= h + 1 && count[p.x] >= t + 1)) {

                boolean[] same = new boolean[n];

                int less = 0;
                if (count[p.x] + count[p.y] < 2 * (h + t + 1)) {
                    for (int i : map[p.x]) {
                        if (map[p.y].contains(i)) {
                            less++;
                            same[i] = true;

                        }
                    }
                }
                int x = count[p.x] - less;
                int y = count[p.y] - less;

                ok = (x + y + less >= (h + t + 2));

                if (ok) {

                    out.println("YES");
                    if (count[p.x] >= (h + 1) && count[p.y] >= (t + 1)) {
                        out.println((p.x + 1) + " " + (p.y + 1));
                        int total = 0;
                        int take = 0;
                        boolean[] check = new boolean[n];
                        for (int i : map[p.x]) {
                            if (i != p.y && (!same[i] || (count[p.y] - take > (t + 1)))) {
                                if (same[i]) {
                                    take++;
                                }
                                check[i] = true;
                                total++;
                                out.print((i + 1) + " ");
                                if (total == h) {
                                    break;
                                }
                            }
                        }
                        out.println();
                        total = 0;
                        for (int i : map[p.y]) {
                            if (i != p.x && !check[i]) {
                                out.print((i + 1) + " ");
                                total++;
                                if (total == t) {
                                    break;
                                }
                            }
                        }
                        out.println();
                        // System.out.println("Last " + total);
                    } else {
                        out.println((p.y + 1) + " " + (p.x + 1));
                        int total = 0;
                        int take = 0;
                        boolean[] check = new boolean[n];
                        for (int i : map[p.y]) {
                            if (i != p.x && (!same[i] || (count[p.y] - take > (t + 1)))) {
                                if (same[i]) {
                                    take++;
                                }
                                check[i] = true;
                                total++;
                                out.print((i + 1) + " ");
                                if (total == h) {
                                    break;
                                }
                            }
                        }
                        out.println();
                        total = 0;
                        for (int i : map[p.x]) {
                            if (i != p.y && !check[i]) {
                                out.print((i + 1) + " ");
                                total++;
                                if (total == t) {
                                    break;
                                }
                            }
                        }
                        // System.out.println("LKAst" + total);
                    }
                    break;
                }
            }
        }
        if (!ok) {
            out.println("NO");
        }

        out.close();

    }

    public static class Point {

        int x, y;

        public Point(int x, int y) {
            this.x = x;
            this.y = y;
        }
    }

    public static double dist(Point a, Point b) {
        double val = (a.x - b.x) * (a.x - b.x) + (a.y - b.y) * (a.y - b.y);
        return Math.sqrt(val);
    }

    public static double dist(long a, long b, long x, long y) {
        double val = (b - a) * (b - a) + (x - y) * (x - y);
        val = Math.sqrt(val);
        double other = x * x + a * a;
        other = Math.sqrt(other);
        return val + other;
    }

    public static class FT {

        int[] data;

        FT(int n) {
            data = new int[n];
        }

        public void update(int index, int value) {
            while (index < data.length) {
                data[index] += value;
                index += (index & (-index));
            }
        }

        public int get(int index) {
            int result = 0;
            while (index > 0) {
                result += data[index];
                index -= (index & (-index));
            }
            return result;

        }
    }

    public static class Sign {

        char c;
        int x, y;

        public Sign(char c, int x, int y) {
            this.c = c;
            this.x = x;
            this.y = y;
        }
    }

    public static long gcd(long a, long b) {
        if (b == 0) {
            return a;
        }
        return gcd(b, a % b);
    }

    public static long pow(long a, long b) {
        if (b == 0) {
            return 1;
        }
        if (b == 1) {
            return a;
        }
        long val = pow(a, b / 2);
        if (b % 2 == 0) {
            return val * val % MOD;
        } else {
            return val * (val * a % MOD) % MOD;
        }
    }

    static class Scanner {

        BufferedReader br;
        StringTokenizer st;

        public Scanner() throws FileNotFoundException {
            // System.setOut(new PrintStream(new BufferedOutputStream(System.out), true));
            br = new BufferedReader(new InputStreamReader(System.in));
            //br = new BufferedReader(new InputStreamReader(new FileInputStream(new File("input.txt"))));
        }

        public String next() {

            while (st == null || !st.hasMoreTokens()) {
                try {
                    st = new StringTokenizer(br.readLine());
                } catch (Exception e) {
                    throw new RuntimeException();
                }
            }
            return st.nextToken();
        }

        public long nextLong() {
            return Long.parseLong(next());
        }

        public int nextInt() {
            return Integer.parseInt(next());
        }

        public double nextDouble() {
            return Double.parseDouble(next());
        }

        public String nextLine() {
            st = null;
            try {
                return br.readLine();
            } catch (Exception e) {
                throw new RuntimeException();
            }
        }

        public boolean endLine() {
            try {
                String next = br.readLine();
                while (next != null && next.trim().isEmpty()) {
                    next = br.readLine();
                }
                if (next == null) {
                    return true;
                }
                st = new StringTokenizer(next);
                return st.hasMoreTokens();
            } catch (Exception e) {
                throw new RuntimeException();
            }
        }
    }
}