n = int(input())
ele = input().split()
ele = list(map(int, ele))
ele.sort()
while ele[0] != ele[-1]:
	for i in range(len(ele) - 1, 0, -1):
		if ele[i] % ele[0] == 0:
			ele[i] = ele[0]
		else:
			ele[i] = ele[i] % ele[0]
		ele.sort()
print(len(ele) * ele[0])
