import math
t = int(input())
for _ in range(t):
	n = int(input())
	a = list(map(int, input().split()))
	maxi = a[0]
	fin = 0
	for i in range(1, n):
		if a[i] > maxi:
			maxi = a[i]
		elif maxi - a[i] > fin:
			fin = maxi - a[i]
	if fin <= 2:
		print(fin)
	else:
		print(int(math.log(fin, 2)) + 1)
