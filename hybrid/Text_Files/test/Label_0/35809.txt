import java.io.*;
import java.util.*;
import java.math.*;
public class C {
	static class fakeint implements Comparable<fakeint>{
		int it;
		public fakeint(int it) {
			this.it = it;
		}
		public int compareTo(fakeint i) {
			return Long.compare(val[it], val[i.it]);
		}
	}
	public static long val[];
	public static void main(String[] args)throws IOException {
		Scanner sc = new Scanner(System.in);
		PrintWriter out = new PrintWriter(System.out);
		int n = sc.nextInt();
		val =  new long[n+1];
		ArrayList<Integer> left = new ArrayList<>();
		ArrayList<Integer> right = new ArrayList<>();
		for(int i=3; i<=n; ++i) {
			System.out.println("2 1 2 " + i);
			int a = sc.nextInt();
			if(a<0) {
				right.add(i);
			}else {
				left.add(i);
			}
		}
		long max = 0;
		int maxidxl = 0;
		for(int i: left) {
			System.out.println("1 1 2 " + i);
			val[i] = sc.nextLong();
			if(val[i]>max) {
				max = val[i];
				maxidxl = i;
			}
		}
		ArrayList<fakeint> left1 = new ArrayList<>();
		ArrayList<fakeint> right1 = new ArrayList<>();
		ArrayList<fakeint> left2 = new ArrayList<>();
		ArrayList<fakeint> right2 = new ArrayList<>();
		for(int i: left) {
			if(i==maxidxl) continue;
			System.out.println("2 1 " + maxidxl + " " + i);
			int a = sc.nextInt();
			if(a<0) {
				left2.add(new fakeint(i));
			}else {
				left1.add(new fakeint(i));
			}
		}
		max =0;
		int maxidxr = 0;
		for(int i: right) {
			System.out.println("1 1 2 " + i);
			val[i] = sc.nextLong();
			if(val[i]>max) {
				max = val[i];
				maxidxr = i;
			}
		}
		for(int i: right) {
			if(i==maxidxr) continue;
			System.out.println("2 1 " + maxidxr + " " + i);
			int a = sc.nextInt();
			if(a<0) {
				right2.add(new fakeint(i));
			}else {
				right1.add(new fakeint(i));
			}
		}
		Collections.sort(left2);
		Collections.sort(left1);
		Collections.sort(right2);
		Collections.sort(right1);
		System.out.print(0 + " 1 ");
		for(fakeint k: right2) {
			System.out.print(k.it + " ");
		}
		if(maxidxr>0) {
			System.out.print(maxidxr + " ");
		}
		for(int i=right1.size()-1; i>=0; --i) {
			System.out.println(right1.get(i).it + " ");
		}
		System.out.print(2 + " ");
		for(fakeint k: left2) {
			System.out.print(k.it + " ");
		}
		if(maxidxl>0) {
			System.out.print(maxidxl + " ");
		}
		for(int i=left1.size()-1; i>=0; --i) {
			System.out.println(left1.get(i).it + " ");
		}
		out.close();
	}
	static class FastIO {
		InputStream dis;
		byte[] buffer = new byte[1 << 17];
		int pointer = 0;
		public FastIO(String fileName) throws IOException {
			dis = new FileInputStream(fileName);
		}
		public FastIO(InputStream is) throws IOException {
			dis = is;
		}
		int nextInt() throws IOException {
			int ret = 0;
 
			byte b;
			do {
				b = nextByte();
			} while (b <= ' ');
			boolean negative = false;
			if (b == '-') {
				negative = true;
				b = nextByte();
			}
			while (b >= '0' && b <= '9') {
				ret = 10 * ret + b - '0';
				b = nextByte();
			}
 
			return (negative) ? -ret : ret;
		}
		long nextLong() throws IOException {
			long ret = 0;
 
			byte b;
			do {
				b = nextByte();
			} while (b <= ' ');
			boolean negative = false;
			if (b == '-') {
				negative = true;
				b = nextByte();
			}
			while (b >= '0' && b <= '9') {
				ret = 10 * ret + b - '0';
				b = nextByte();
			}
			return (negative) ? -ret : ret;
		}
		byte nextByte() throws IOException {
			if (pointer == buffer.length) {
				dis.read(buffer, 0, buffer.length);
				pointer = 0;
			}
			return buffer[pointer++];
		}
		String next() throws IOException {
			StringBuffer ret = new StringBuffer();
 
			byte b;
			do {
				b = nextByte();
			} while (b <= ' ');
			while (b > ' ') {
				ret.appendCodePoint(b);
				b = nextByte();
			}
 
			return ret.toString();
		}
	}
}
