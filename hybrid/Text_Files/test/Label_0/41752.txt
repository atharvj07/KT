t = int(input())

def check(a, b, x, y, n):
	can = min(a - x, n)
	a -= can
	n -= can
	can = min(b - y, n)
	b -= can
	return a * b
for i in range(t):
	(a, b, x, y, n) = map(int, input().split(' '))
	print(min(check(a, b, x, y, n), check(b, a, y, x, n)))
