tc = int(input())
from math import gcd
while tc != 0:
	tc -= 1
	(a, b, k) = map(int, input().split())
	if a > b:
		(a, b) = (b, a)
	if a * (k - 1) + gcd(a, b) < b:
		print('REBEL')
	else:
		print('OBEY')
