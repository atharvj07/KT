#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m;
  int t;
  cin >> n >> m;
  if (n < m)
    t = n;
  else
    t = m;
  if (t % 2 == 0)
    cout << "Malvika";
  else
    cout << "Akshat";
}
