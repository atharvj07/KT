#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int a, b;
    cin >> a >> b;
    if (a > b) swap(a, b);
    int diff = b - a;
    int q = diff / 10;
    int rem = diff % 10;
    if (rem != 0) ++q;
    cout << q << endl;
  }
  return 0;
}
