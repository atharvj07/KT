q = int(input())
for _ in range(q):
	n = int(input())
	array = [int(_) for _ in input().split(' ')]
	if sum(array) % len(array) > 0:
		flat = sum(array) // len(array) + 1
	else:
		flat = sum(array) // len(array)
	print(flat)
