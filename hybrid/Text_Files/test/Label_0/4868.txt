#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  ;
  int n, ans = 1, mod = 1e6 + 3;
  cin >> n;
  for (int i = 1; i < n; i++) {
    ans *= 3;
    ans %= mod;
  }
  cout << ans << "\n";
}
