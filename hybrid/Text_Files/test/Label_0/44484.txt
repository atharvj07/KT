for _ in range(int(input())):
	(zer, on, tw) = [int(c) for c in input().split()]
	(zero, one, two) = [int(c) for c in input().split()]
	s = 0
	s += min(tw, one) * 2
	k = min(tw, one)
	tw -= k
	one -= k
	if on > one + zero:
		on -= one + zero
		s -= on * 2
	print(s)
