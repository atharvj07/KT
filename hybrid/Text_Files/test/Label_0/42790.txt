import sys

def solve():
	(n, x) = map(int, input().split())
	a = list(set(map(int, input().split())))
	ans = 1
	if ans != a[0]:
		x -= 1
		ans += 1
	while x or ans in a:
		if ans not in a:
			x -= 1
		ans += 1
	print(ans - 1)
t = int(input())
while t:
	t -= 1
	solve()
