import sys
import math
(a, b) = [int(s) for s in input().split()]
if not a % b or not b % a:
	print(0)
	sys.exit(0)
else:
	divs = []
	for i in range(1, math.ceil(abs(a - b) ** (1 / 2)) + 1):
		if abs(a - b) % i == 0:
			divs.append(i)
			divs.append(abs(a - b) // i)
	min_ = 1e+20
	nod = -1
	for i in range(len(divs)):
		k = math.ceil(max(a, b) / divs[i]) * divs[i] - max(a, b)
		if (a + k) * (b + k) / divs[i] < min_:
			min_ = (a + k) * (b + k) / divs[i]
			nod = divs[i]
			k_min = k
	print(k_min)
