(n, k) = map(int, input().split())
l = [(int(i) - int(j), int(i), int(j)) for (i, j) in zip(input().split(), input().split())]
l.sort()
ans = 0
for i in range(n):
	if k > 0:
		ans += l[i][1]
		k -= 1
	else:
		ans += min(l[i][1:])
print(ans)
