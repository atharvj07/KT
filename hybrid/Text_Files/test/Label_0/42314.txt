N = int(input())
arr = input()
arr = [int(x) for x in arr.split(' ')]
left = 0
right = 0
arr.sort()
res = 0
while left < N and right < N:
	if arr[right] > arr[left] + 5:
		res = max(res, right - left)
		left += 1
	else:
		right += 1
res = max(res, right - left)
print(res)
