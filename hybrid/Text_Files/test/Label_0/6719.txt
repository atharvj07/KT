#include <bits/stdc++.h>
using namespace std;
long long MOD = 1e5;
int main() {
  long long tc;
  cin >> tc;
  while (tc--) {
    int n;
    cin >> n;
    int arr[n];
    vector<int> vec;
    for (int i = 0; i < n; i++) {
      cin >> arr[i];
    }
    for (int i = n - 1; i >= 0; i--) cout << arr[i] << " ";
    cout << endl;
  }
  return 0;
}
