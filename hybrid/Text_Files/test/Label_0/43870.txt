def isPrime(n):
	if n <= 1:
		return False
	if n <= 3:
		return True
	if n % 2 == 0 or n % 3 == 0:
		return False
	i = 5
	while i * i <= n:
		if n % i == 0 or n % (i + 2) == 0:
			return False
		i = i + 6
	return True
import math
q = 1
for _ in range(q):
	n = int(input())
	ans = []
	for i in range(n - 1):
		ans.append([i + 1, i + 2])
	ans.append([n, 1])
	if isPrime(n):
		print(len(ans))
		for i in range(len(ans)):
			print(*ans[i])
	else:
		for i in range(n + 1, 3 * n // 2 + 1):
			if isPrime(i):
				ans.append([i - n, i + n // 2 - n])
				break
			else:
				ans.append([i - n, i + n // 2 - n])
		print(len(ans))
		for i in range(len(ans)):
			print(*ans[i])
