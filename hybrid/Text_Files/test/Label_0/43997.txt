from math import sqrt

def main():
	n = int(input())
	min_v = n
	max_v = 0
	for i in range(0, 7):
		c = 0
		for j in range(0, n):
			if (i + j) % 7 == 6 or (i + j) % 7 == 5:
				c += 1
		max_v = max(max_v, c)
		min_v = min(min_v, c)
	print(min_v, max_v)
main()
