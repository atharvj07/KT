#include <bits/stdc++.h>
using namespace std;
int main() {
  long n;
  cin >> n;
  string op;
  long result = 0;
  for (int i = 0; i < n; i++) {
    cin >> op;
    if (op[1] == '+')
      result++;
    else
      result--;
  }
  cout << result << endl;
  return 0;
}
