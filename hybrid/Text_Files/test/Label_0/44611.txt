n = input()
m = input()
total_c = 0
for i in m:
	n_count = n.count(i)
	if n_count == 0:
		total_c = 0
		break
	total_c += min(n_count, m.count(i))
	m = m.replace(i, '')
if total_c == 0:
	total_c -= 1
print(total_c)
