import math

def main():
	t = int(input())
	for i in range(t):
		arr = input().split(' ')
		(arr[0], arr[1]) = (int(arr[0]), int(arr[1]))
		if arr[0] >= arr[1]:
			print('YES')
		else:
			diff = arr[1] - arr[0]
			flag = 0
			w = []
			for i in range(1, arr[0]):
				if math.ceil(arr[1] / (i + 1)) + i <= arr[0]:
					flag = 1
					break
			if flag == 1:
				print('YES')
			else:
				print('NO')
main()
