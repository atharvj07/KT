n = int(input())
ls = list(map(int, input().split()))
ls.sort()
ans = 0
for i in range(1, len(ls)):
	if ls[i] <= ls[i - 1]:
		tmp = ls[i - 1] - ls[i]
		ans += 1 + tmp
		ls[i] = ls[i] + tmp + 1
print(ans)
