#include <bits/stdc++.h>
using namespace std;
const int P = 1e9 + 7;
int main() {
  long long n, k;
  cin >> n >> k;
  long long ans = 1;
  for (int i = 0; i < k - 1; i++) ans = (ans * k) % P;
  for (int i = 0; i < n - k; i++) ans = (ans * (n - k)) % P;
  cout << ans;
  return 0;
}
