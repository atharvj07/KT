for _ in range(int(input())):
	(n, _) = map(int, input().split())
	(length, count) = (0, 0)
	for x in sorted(map(int, input().split()), reverse=True):
		if length < x:
			length = length + n - x
			count = count + 1
		else:
			break
	print(count)
