//
// Source code recreated from a .class file by IntelliJ IDEA
// (powered by Fernflower decompiler)
//

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.util.HashMap;
import java.util.Locale;
import java.util.Map;
import java.util.StringTokenizer;

public class C {
    String fileName = "C";
    BufferedReader br;
    StringTokenizer in;
    PrintWriter out;

    public C() {
    }

    public void solve() throws IOException {
        int n = this.nextInt();
        int[] a = new int[n];
        Map<Integer, Integer> visitTime = new HashMap(n);
        Map<Integer, Integer> roomNumber = new HashMap(n);

        int roomId;
        for(roomId = 0; roomId < n; ++roomId) {
            a[roomId] = this.nextInt();
        }

        roomId = 0;
        int ans = 0;

        for(int i = 0; i < n; ++i) {
            if(!roomNumber.keySet().contains(Integer.valueOf(a[i]))) {
                ++roomId;
                visitTime.put(Integer.valueOf(roomId), Integer.valueOf(i + 1));
                roomNumber.put(Integer.valueOf(i + 1), Integer.valueOf(roomId));
                ++ans;
            } else {
                int roomIdCur = ((Integer)roomNumber.get(Integer.valueOf(a[i]))).intValue();
                roomNumber.remove(Integer.valueOf(a[i]));
                visitTime.put(Integer.valueOf(roomIdCur), Integer.valueOf(i + 1));
                roomNumber.put(Integer.valueOf(i + 1), Integer.valueOf(roomIdCur));
            }
        }

        this.out.print(ans);
    }

    public void run() {
        try {
            this.br = new BufferedReader(new InputStreamReader(System.in));
            this.out = new PrintWriter(System.out);
            this.solve();
            this.out.close();
        } catch (IOException var2) {
            var2.printStackTrace();
            System.exit(1);
        }

    }

    public String nextToken() throws IOException {
        while(this.in == null || !this.in.hasMoreTokens()) {
            this.in = new StringTokenizer(this.br.readLine());
        }

        return this.in.nextToken();
    }

    public int nextInt() throws IOException {
        return Integer.parseInt(this.nextToken());
    }

    public double nextDouble() throws IOException {
        return Double.parseDouble(this.nextToken());
    }

    public long nextLong() throws IOException {
        return Long.parseLong(this.nextToken());
    }

    public static void main(String[] args) throws IOException {
        Locale.setDefault(Locale.US);
        (new C()).run();
    }
}