import java.util.*;
import java.io.*;
public class Main {
public static long sum(long a){
    return ((a*(a+1))/2);
}
public static boolean check(long goal , long n ,long[] arr , long z){
    long pro = 1L;
		long s=1L;
		s = (1 - (long) Math.pow(2,(int) (n))) / (1 - 2);
	//	System.out.println(s);
		long a1=(goal-sum(n-1))/n; long a2 = goal/s;
		long b1 = (goal-sum(n-1))%n; long b2=goal%s;
		if((a1==a2 && b1!=0 && b2!=0) || a1<a2 || a1==0)	 return false;
		else{
		    arr[(int) z] = a1;
		    return true;
		} 
		
}
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		long goal = Long.parseLong(st.nextToken());
		long n = Long.parseLong(st.nextToken());
		long[] ans = new long[(int) n];
		long i=0L;
		if(check(goal,n,ans,i)){
		    i++;
		    long k = n-1;
		    goal -= ans[(int) i - 1];
		    while(i<n){
		    // System.out.println( goal  + " " + (k) +" " +i);
		      if( check(goal,k--,ans,i)) {
		          if(ans[(int)i]>2*ans[(int)i-1]) ans[(int)i] = 2*ans[(int)i-1];
		          i++; goal = goal - ans[(int) (i-1)];}
		    } System.out.println("YES");
		    		for(long c=0L; c<n; c++){
		    		    System.out.print(ans[(int) c] + " ");
		    		}

		}else{
		    System.out.println("NO");
		}
	}}