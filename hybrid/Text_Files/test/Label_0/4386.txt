#include <bits/stdc++.h>
using namespace std;
int main() {
  long long n;
  cin >> n;
  int cnt = 0;
  for (long long i = 1; i < sqrt(n * 1.0) + 1; i++) {
    long long min = (i + 1) * i + i * (i - 1) / 2;
    if (n >= min && (n - min) % 3 == 0) cnt++;
  }
  cout << cnt;
}
