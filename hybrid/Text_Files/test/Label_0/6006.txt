#include <iostream>
#include <cmath>
using namespace std;
int main() {
  int D, E;
  double ans;
  while (cin >> D >> E, D) {
    ans = 200;
    for (int i = 0; i <= D/2; ++i) {
      ans = min(ans, fabs(sqrt(i*i + (D-i)*(D-i)) - E));
    }
    cout << ans << endl;
  }
  return 0;
}