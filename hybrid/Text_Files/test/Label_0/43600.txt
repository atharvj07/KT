n = int(input())
for i in range(n):
	L = [int(e) for e in input().strip().split()]
	c = max(L)
	a = min(L)
	L.remove(a)
	L.remove(c)
	b = L[0]
	if c - a > 2:
		a += 1
		c -= 1
		print(2 * (c - a))
	else:
		print('0')
