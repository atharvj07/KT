#include <bits/stdc++.h>
using namespace std;

int main() {
  int N, K;
  string S;
  cin >> N >> K >> S;

  int m = 1;
  for (int i=0; i<N-1; ++i) {
    if (S[i] != S[i+1]) ++m;
  }

  cout << min(N-1, N - m + 2*K) << "\n";
}
