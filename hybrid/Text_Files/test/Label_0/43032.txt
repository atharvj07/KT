(n, k, m) = list(map(int, input().split()))
t = sorted(map(int, input().split()))
res = 0
for i in range(n + 1):
	rem = m - i * sum(t)
	if rem < 0:
		break
	r = i * (k + 1)
	for j in range(k):
		d = min(rem // t[j], n - i)
		rem -= d * t[j]
		r += d
	res = max(res, r)
print(res)
