def solve():
	(n, k) = map(int, input().split())
	arr = list(map(int, input().split()))
	arr = sorted(arr)
	cat = 0
	ans = 0
	for i in range(k - 1, -1, -1):
		x = arr[i]
		if x <= cat:
			break
		dis = n - x
		cat += dis
		ans += 1
	print(ans)
t = int(input())
for i in range(t):
	solve()
