#include <bits/stdc++.h>
using namespace std;

#define int long long
#define pb push_back 
#define mp make_pair
#define fr first
#define sc second
#define Rep(i, n) for( int i = 0; i < (n); i++ )
#define Rrep(i, a, n) for( int i = (a); i < (n); i++ )
#define All(v) v.begin(), v.end()

typedef pair<int, int> Pii; 
typedef pair<int, Pii> Pip;
const int INF = 1107110711071107;

signed main() {
  int W, H, T;
  int p;
  int data[51][51];
  int used[51][51][51] = {{{0}}};
  Pip P[51];
  int sum = 0;
  
  cin >> W >> H >> T;
  cin >> p;

  Rep(i, p) {
    cin >> P[i].fr >> P[i].sc.fr >> P[i].sc.sc;
  }
  
  Rep(i, H) Rep(j, W) cin >> data[i][j], sum += data[i][j];

  Rep(i, p) {
    if ( data[P[i].sc.fr][P[i].fr] &&  !used[P[i].sc.sc][P[i].sc.fr][P[i].fr]  ) {
      used[P[i].sc.sc][P[i].sc.fr][P[i].fr] =true;
      data[P[i].sc.fr][P[i].fr]++;
    }
  }

  int ans = 0;
  Rep(i, H) Rep(j, W) ans += data[i][j];

  cout << ans-sum << endl;
  
  return 0;
}