#include<iostream>
#include<string>
using namespace std;
 
int main(){
  string S;
  long long cnt=0,sum=0;
  cin >> S;
  for(int i=0;i<S.size();i++){
    if(S[i]=='B')cnt++;
      else sum+=cnt;
  }
  cout << sum << endl;
  return 0;
}