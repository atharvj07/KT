from sys import stdin
SAMPLE = '5 6 3\n5 4 3 2 1\n'

def solve():
	i = 0
	machine = 0
	clock = 0
	while i < N or machine != 0:
		while i < N:
			item = DATA[i]
			if machine + item <= H:
				machine += item
				i += 1
			else:
				break
		(seconds, remainder) = (int(machine / K), machine % K)
		machine = remainder
		if seconds == 0 and machine != 0:
			clock += 1
			machine = 0
		else:
			clock += seconds
	print(clock)

def parse():
	global N, H, K, DATA
	pass
	line = stdin.readline().split()
	(N, H, K) = map(int, line)
	line = stdin.readline().split()
	DATA = [-1 for x in range(N)]
	for i in range(N):
		DATA[i] = int(line[i])
	solve()
parse()
