def answer_query(n, k):
	r = 0
	while n > 0:
		r += n % k
		n -= n % k
		while n % k == 0 and n > 0:
			r += 1
			n //= k
	print(r)
t = int(input())
for _ in range(t):
	(n, k) = map(int, input().split())
	answer_query(n, k)
