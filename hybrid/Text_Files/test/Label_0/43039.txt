def get_index(n):
	if n == '4':
		return 1
	elif n == '7':
		return 2
	else:
		total = 2 ** (len(n) - 1)
		if n[0] == '7':
			total *= 2
		total += get_index(n[1:])
	return total
print(get_index(input()))
