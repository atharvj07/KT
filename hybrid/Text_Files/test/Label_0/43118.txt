from sys import stdin, stdout
nmbr = lambda : int(stdin.readline())
lst = lambda : list(map(int, input().split()))
PI = float('inf')

def slope(x, y):
	global s
	if x == x0:
		s.add(PI)
	else:
		s.add((y - y0) / (x - x0))
for i in range(1):
	(n, x0, y0) = lst()
	s = set()
	for i in range(n):
		(x, y) = lst()
		slope(x, y)
	print(len(s))
