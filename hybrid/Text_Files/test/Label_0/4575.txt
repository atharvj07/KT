#include<bits/stdc++.h>
using namespace std;

typedef pair<int,int> PII;

int c[5];
int main(){
	int a;
	for (int i = 0; i < 6; ++i){
		cin>>a;
		c[a]++;
	}

	int ans=0;
	for (int i = 1; i < 5; ++i){
		if(c[i]==2)ans++;
	}

	if(ans==2)
		cout<<"YES"<<endl;
	else
		cout<<"NO"<<endl;

}