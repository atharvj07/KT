#include <bits/stdc++.h>
using namespace std;
int main() {
  int i, j, k, n;
  cin >> n >> k;
  j = (n - k) / 2;
  for (i = 1; i <= n; i++) {
    if (i % (j + 1) == 0) {
      cout << "1";
    } else {
      cout << "0";
    }
  }
}
