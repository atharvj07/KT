#include <bits/stdc++.h>
using namespace std;
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);
  long long n, ans = 0, x = 1;
  cin >> n;
  while (n > 1) {
    ans += x * (n / 2);
    n = (n + 1) / 2;
    x = x * 2;
  }
  cout << ans;
  return 0;
}
