n = int(input())
s = input()
(best, result) = (n, None)
combinations = ('RGB', 'RBG', 'GRB', 'GBR', 'BRG', 'BGR')
for p in combinations:
	t = p * ((n + 2) // 3)
	c = sum((x != y for (x, y) in zip(s, t)))
	if c < best:
		best = c
		result = t[:n]
print(best, result, sep='\n')
