for _ in range(int(input())):
	s = input()
	d = []
	a = 1
	for i in range(1, len(s)):
		if s[i] == s[i - 1]:
			a += 1
		else:
			d.append((s[i - 1], a))
			a = 1
	d.append((s[-1], a))
	a = []
	for (i, j) in d:
		if j % 2:
			a.append(i)
	a = sorted(list(set(a)))
	print(''.join(a))
