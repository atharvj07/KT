days = int(input())
small = float('inf')
big = 0
for d in [0, 5]:
	off = 0
	for i in range(d, days + d):
		is_weekend = i % 7 in [5, 6]
		if is_weekend:
			off += 1
	small = min(small, off)
	big = max(big, off)
print(small, big)
