from collections import Counter, deque

def lInt(d=None):
	return map(int, input().split(d))
(n, *_) = lInt()
ans = 0
for l in range(1, n + 1):
	for j in range(0, l):
		if j + l <= n:
			ans += 1
print(ans)
