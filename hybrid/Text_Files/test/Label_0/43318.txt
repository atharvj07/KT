import math
t = int(input())

def hello():
	(n, x, a, b) = map(int, input().split())
	beggining = abs(a - b)
	if beggining + x >= n - 1:
		return n - 1
	else:
		return beggining + x
for i in range(t):
	print(hello())
