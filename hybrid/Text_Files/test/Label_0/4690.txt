#include<iostream>
#include<math.h>
using namespace std;
int checkp(int a)
{
	for(int i=2;i<=sqrt(a);i++)
	if(a%i==0) return 1;
	return 0;
}
int main()
{
	int x;
	cin>>x;
	while(checkp(x)==1)
	{
		x+=1;
	}
	cout<<x;
}