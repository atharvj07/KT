import math
t = int(input())
for i in range(t):
	n = int(input())
	data = list(map(int, input().split()))
	m = min(data)
	res = 0
	for i in range(len(data)):
		if data[i] == m:
			res += 1
	print(len(data) - res)
