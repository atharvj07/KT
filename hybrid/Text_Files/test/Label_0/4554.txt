#include <bits/stdc++.h>
const int INF = 1 << 29;
const double PI = acos(-1.0);
using namespace std;
int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(NULL);
  ;
  string s;
  cin >> s;
  cout << 3 << endl;
  cout << "R" << ' ' << s.size() - 1 << endl;
  cout << "L" << ' ' << s.size() << endl;
  cout << "L" << ' ' << 2 << endl;
  return 0;
}
