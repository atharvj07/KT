(n, k) = map(int, input().split())
a = list(input())
j = 0
a = list(set(a))
a.sort()
ans = []
for i in range(len(a)):
	if j >= k:
		break
	if ord(a[i]) - 97 not in ans and ord(a[i]) - 95 not in ans:
		ans.append(ord(a[i]) - 96)
		j += 1
if j < k:
	print(-1)
else:
	print(sum(ans))
