#include <bits/stdc++.h>
using namespace std;
const int MAXN = 1e5 + 5;
bool a[MAXN];
void solve() {
  long long int n;
  cin >> n;
  if (n % 2 == 0)
    cout << n / 2 << '\n';
  else
    cout << -((n + 1) / 2) << '\n';
}
int main() { solve(); }
