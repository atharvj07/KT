#include <bits/stdc++.h>
using namespace std;
const int maxn = 1000000;
int n, m, q, t[maxn], odp;
char s[maxn];
int main() {
  scanf("%d%s", &n, s);
  odp = n;
  for (int i = 0; i < n - 1; i++)
    if (s[i] != s[i + 1]) {
      i++;
      odp--;
    }
  printf("%d", odp);
}
