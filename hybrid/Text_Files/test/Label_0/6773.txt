#include <bits/stdc++.h>
using namespace std;
int m[256];
int main(){
    string s;
    cin>>s;
    for(char c:s){
        m[c]++;
    }
    for(int i=0;i<256;i++){
        if(m[i]%2){
            cout<<"No"; 
            return 0;
        }
    }
cout<<"Yes"; 
    return 0;
}
