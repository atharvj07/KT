#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(void)
{
    ll x,k,d;
    cin >> x >> k >> d;
    x = abs(x);
    ll n = min(k,x/d);
    k -= n;
    x -= n*d;
    if(k%2) cout << abs(x-d) << endl;
    else cout << x << endl;
    return 0;
}