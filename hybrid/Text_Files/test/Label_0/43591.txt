t = int(input())
for _ in range(t):
	n = int(input())
	arr = list(map(int, input().split()))
	s = sum(arr)
	twoSum = 2 * s / n
	hashmap = {}
	cnt = 0
	for e in arr:
		if twoSum - e in hashmap:
			cnt += hashmap[twoSum - e]
		if e in hashmap:
			hashmap[e] += 1
		else:
			hashmap[e] = 1
	print(cnt)
