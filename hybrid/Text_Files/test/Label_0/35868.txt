import java.io.OutputStream;
import java.io.IOException;
import java.io.InputStream;
import java.io.PrintWriter;
import java.io.IOException;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.InputStream;

import java.util.*;

public class Main {

    static int n;
    static int[] a;
    static long[] aa;
    static int[] b;
    static int[] parent;
    static boolean[] visited;
    static boolean[] visited_plus;
    static boolean[] visited_minus;
    static ArrayList<ArrayList<Integer>> edge;
    static long ans_val;
    static ArrayList<Integer> ans_list;
    static ArrayList<Integer> ans_list_tmp;

    static void dfs(int x) {
        for (Integer tmp : edge.get(x)) {
            dfs(tmp);
        }
        visited[x] = true;
        if (aa[x] >= 0) {
            ans_val += aa[x];
            ans_list.add(x+1);
            visited_plus[x] = true;
            if (b[x] != -2) {
                aa[b[x]] += aa[x];
            }
        }
    }

    static void dfs2(int x) {
        visited_minus[x] = true;
        for (Integer tmp : edge.get(x)) {
            dfs2(tmp);
        }
        if (!visited_plus[x]) {
            visited_plus[x] = true;
            ans_val += aa[x];
            ans_list_tmp.add(x+1);
        }
    }

	public static void main(String[] args) {
		InputStream inputStream = System.in;
        OutputStream outputStream = System.out;
        InputReader in = new InputReader(inputStream);
        PrintWriter out = new PrintWriter(outputStream);

        n = in.nextInt();
        a = new int[n];
        aa = new long[n];
        b = new int[n];
        parent = new int[n];
        Arrays.fill(parent, -1);
        edge = new ArrayList<ArrayList<Integer>>();
        for (int i=0;i<n;i++) {
            ArrayList<Integer> add = new ArrayList<Integer>();
            edge.add(add);
        }
        for (int i=0;i<n;i++) {
            a[i] = in.nextInt();
            aa[i] = a[i];
        }
        for (int i=0;i<n;i++) {
            b[i] = in.nextInt()-1;
            if (b[i] != -2) {
                // edge.get(i).add(b[i]);
                edge.get(b[i]).add(i);
                // parent[b[i]] = i;
                parent[i] = b[i];
            }
        }

        // out.println(edge);

        ans_val = 0L;
        ans_list = new ArrayList<Integer>();
        visited = new boolean[n];
        visited_plus = new boolean[n];
        for (int i=0;i<n;i++) {
            if (visited[i]) continue;

            int index = i;
            while (parent[index] != -1) {
                index = parent[index];
            }
            dfs(index);
        }
        visited_minus = new boolean[n];
        for (int i=0;i<n;i++) {
            if (visited_minus[i]) continue;
            int index = i;
            while (parent[index] != -1) {
                index = parent[index];
            }
            ans_list_tmp = new ArrayList<Integer>();
            dfs2(index);
            for (int j=0;j<ans_list_tmp.size();j++) {
                ans_list.add(ans_list_tmp.get(ans_list_tmp.size()-1-j));
            }
        }

            // ans_list_tmp = new ArrayList<Integer>();
            // dfs2(index);
            // for (int j=0;j<ans_list_tmp.size();j++) {
                // ans_list.add(ans_list_tmp.get(ans_list_tmp.size()-1-j));
            // }

            // while (true) {
            //     if (aa[index] >= 0) {
            //         ans_val += aa[index];
            //         ans_list.add(index+1);
            //         visited[index] = true;
            //         if (b[index] != -2) {
            //             aa[b[index]] += aa[index];
            //         }
            //     } else {
            //     }
            //     if (b[index] == -2) break;
            //     index = b[index];
            // }

            // while (true) {
            //     if (!visited[index]) {
            //         visited[index] = true;
            //         ans_val += aa[index];
            //         ans_list.add(index+1);
            //     }
            //     if (parent[index] == -1) break;
            //     index = parent[index];
            // }

        out.println(ans_val);
        StringBuilder sb = new StringBuilder();
        for (int i=0;i<ans_list.size();i++) {
            sb.append(ans_list.get(i));
            if (i != ans_list.size()-1) sb.append(" ");
        }
        out.println(sb);
        // out.println(ans_list);
		out.close();
	}

	static class InputReader {
        public BufferedReader reader;
        public StringTokenizer tokenizer;

        public InputReader(InputStream stream) {
            reader = new BufferedReader(new InputStreamReader(stream), 32768);
            tokenizer = null;
        }

        public String next() {
            while (tokenizer == null || !tokenizer.hasMoreTokens()) {
                try {
                    tokenizer = new StringTokenizer(reader.readLine());
                } catch (IOException e) {
                    throw new RuntimeException(e);
                }
            }
            return tokenizer.nextToken();
        }

        public int nextInt() {
            return Integer.parseInt(next());
        }

        public long nextLong() {
            return Long.parseLong(next());
        }

    }
}
