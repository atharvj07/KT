#include <bits/stdc++.h>
using namespace std;
struct P {
  int x, y;
  bool operator<(const P &a) const { return x < a.x; };
};
int a, b, c, d, i, n, m, e, k, dx[10] = {1, 0, -1, 0, 1, 1, -1, -1},
                               dy[10] = {0, 1, 0, -1, 1, -1, 1, -1};
int o[11];
int l[100101];
int ss[1];
long long x, y, z, mod = 1000000007, hf = (mod + 1) / 2;
char r[200101];
vector<int> v[1];
P u[1];
queue<int> q;
set<int> s;
map<int, int> p[1];
bool as(P a, P b) { return a.y < b.y; }
int gcd(int a, int b) {
  if (a < b) swap(a, b);
  if (!b) return a;
  return gcd(b, a % b);
}
int main() {
  int ttt;
  scanf("%d", &ttt);
  for (int tt = 1; tt <= ttt; tt++) {
    scanf("%d %d %d", &a, &d, &c);
    a = min(a, d);
    d--;
    c /= gcd(d, c);
    x = a / c;
    y = a / c + 1;
    printf("%lld\n", x * (x - 1) / 2 * (c - a % c) + y * (y - 1) / 2 * (a % c));
  }
}
