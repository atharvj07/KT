def orehus(lst):
	best = 1000000000
	for i in range(len(lst)):
		count = 0
		for j in range(len(lst)):
			p = i + j + abs(i - j)
			count += 2 * p * lst[j]
		best = min(best, count)
	return best
n = int(input())
a = [int(x) for x in input().split()]
print(orehus(a))
