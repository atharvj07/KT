import heapq
n = int(input())
arr = list(map(int, input().split()))
sumi = 0
ans = []
for i in arr:
	sumi += i
	heapq.heappush(ans, i)
	while sumi < 0:
		sumi -= ans[0]
		heapq.heappop(ans)
print(len(ans))
