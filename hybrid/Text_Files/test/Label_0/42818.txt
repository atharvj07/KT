(n, m) = input().split(' ')
n = int(n)
m = int(m)

def how_many_moves(n, m):
	for i in range(n):
		if m * 2 * i >= n:
			return m * i
total_moves = how_many_moves(n, m)
if n >= m:
	print(total_moves)
else:
	print(-1)
