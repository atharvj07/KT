import java.util.*;
import java.io.*;
import java.math.*;

public class CC1 {
    public static StreamTokenizer in = new StreamTokenizer(System.in);
    public static boolean bg = true;

    public static void main(String[] args) throws Exception {
        //long start = System.currentTimeMillis();
        int k1 = 1000000;
        long[] l1 = new long[k1+100];
        Arrays.fill(l1, Long.MAX_VALUE);
        l1[0] = 0;
        
        for (int i=0;i<k1+50;i++){
            for (int j=1;j<=9;j++){
                int s1 = i+j;
                boolean ok = false;
                for (;;){
                    if (s1==0) break;
                    if (s1%10==j){
                        ok = true;
                        break;
                    }
                    s1 /= 10;
                }
                if (ok){
                    long temp = l1[i]+1l;
                    if (temp<l1[i+j]){
                        l1[i+j]=temp;
                    }
                }
            }
        }
        
        in = null;
        //pn(Arrays.toString(l1));
        Scanner scan = new Scanner(System.in);
        int n1 = Integer.parseInt(scan.next());
        
        pn(l1[n1]);
        //pn((System.currentTimeMillis()-start)*0.001);
    }

    private static int ni() throws Exception {
        in.nextToken();
        return (int) in.nval;
    }

    private static long nl() throws Exception {
        in.nextToken();
        return (long) in.nval;
    }

    private static double nd() throws Exception {
        in.nextToken();
        return in.nval;
    }

    private static void pn(Object o1) {
        System.out.println(o1);
    }
}
