#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, f = 10, i = 1;
    cin >> n;
    int buildings = (n % 10 - 1) * 10;
    while (n > 0) {
      n /= f;
      buildings += i++;
    }
    cout << buildings << endl;
  }
  return 0;
}
