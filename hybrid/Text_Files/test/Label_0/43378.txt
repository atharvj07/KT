n = int(input())
coins = [int(x) for x in input().split()]
s = sum(coins)
maxim = n - s
for i in range(len(coins)):
	for j in range(i, len(coins)):
		new_sum = s + len(coins[i:j + 1]) - 2 * sum(coins[i:j + 1])
		if new_sum > maxim:
			maxim = new_sum
print(maxim)
