for _ in range(int(input())):
	(x, y) = map(int, input().split())
	(a, b) = map(int, input().split())
	if a * 2 <= b:
		print((abs(x) + abs(y)) * a)
	else:
		minimum = min(x, y)
		maximum = max(x, y)
		print(minimum * b + (abs(maximum) - abs(minimum)) * a)
