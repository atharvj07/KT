for _ in range(int(input())):
	n = int(input())
	(s, g, c, m) = (input(), False, 0, 0)
	for el in s:
		if el == 'A' and (not g):
			g = True
		if el == 'P' and g:
			c += 1
		if el == 'A' and g:
			m = max(c, m)
			c = 0
	print(max(m, c))
