for __ in range(int(input())):
	(A, B, C, D) = map(int, input().split())
	ai = bi = ci = di = 'Ya'
	if (A + B) % 2 == 1:
		ci = di = 'Tidak'
	else:
		ai = bi = 'Tidak'
	if A + D == 0:
		ai = di = 'Tidak'
	if B + C == 0:
		bi = ci = 'Tidak'
	print(ai, bi, ci, di)
