/*
 * I just can't stop loving you. :)

 */




import java.io.BufferedReader;
import java.io.BufferedWriter;
import java.io.File;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.util.Scanner;
import java.util.StringTokenizer;
 
public class A{
    static class FastReader
    {
        BufferedReader br;
        StringTokenizer st;
 
        public FastReader()
        {
            br = new BufferedReader(new
                     InputStreamReader(System.in));
        }
 
        String next()
        {
            while (st == null || !st.hasMoreElements())
            {
                try
                
                {
                    st = new StringTokenizer(br.readLine());
                }
                catch (IOException  e)
                {
                    e.printStackTrace();
                }
            }
            return st.nextToken();
        }
 
        int nextInt()
        {
            return Integer.parseInt(next());
        }
 
        long nextLong()
        {
            return Long.parseLong(next());
        }
 
        double nextDouble()
        {
            return Double.parseDouble(next());
        }
 
        String nextLine()
        {
            String str = "";
            try
            {
                str = br.readLine();
            }
            catch (IOException e)
            {
                e.printStackTrace();
            }
            return str;
        }
    }
 
    public static long max(long a , long b){
		return a>b? a:b;
	}

   
    public static boolean check(int v){
    	for(int x=0;x<v;x++){
    		if(a[x]<req[x]){
    			return false;
    		}
    		
    	}
    	return true;
    }
    
    public static int a[],req[],feed[][];
    
    public static void main(String[] args) throws IOException
    {
        //FastReader scn=new FastReader();
    	Scanner scn=new Scanner(System.in);
       
		int n=scn.nextInt();
		String a[]=new String[n];
		for(int i=0;i<n;i++){
			a[i]=(scn.next());
		}
		
		int c=0;
		boolean zero=false, isunbeaut=false;
		String unbeaut=null;
	 for(int i=0;i<n;i++){
		 
			if(a[i].equals("0")){
				zero=true; break;
			}
			
			String s= a[i];
			int tc=0;
			for(int j=0;j<s.length();j++){
				if(s.charAt(j)=='0'){
					tc++;
				}
			}
			if(tc==s.length()-1 && s.charAt(0)=='1'){
				c+=tc;
			}
			else { unbeaut=a[i];
			isunbeaut=true;}
			
			
		}
	
	 	if(zero){
	 		System.out.print(0);
	 	}
	 	else if(isunbeaut){
	 		System.out.print(unbeaut);
	 		for (int i = 0; i < c; i++) {
				System.out.print(0);
			}
	 	}
	 	else{
	 		System.out.print(1);
	 		for (int i = 0; i < c; i++) {
				System.out.print(0);
			}
	 	}
		
		System.out.println();
    }
}

/*
 * BufferedReader f = new BufferedReader(new FileReader("hole.in"));
		PrintWriter out = new PrintWriter(new BufferedWriter(new FileWriter(
				"hole.out")));
		
        int v=f.read();	req=new int[v]; a=new int[v];
        for(int i=0;i<v;i++){
        	req[i]=f.read();
        }
        int g=f.read();	feed=new int[g][v];
        for(int i=0;i<g;i++){
        	for(int j=0;i<v;i++){
        		feed[i][j]=f.read();
        	}
        }
        
        //use bit vector to loop around all combinations
        int p=(int)Math.pow(2, g);
        
        for(int k=1;k<=p;k++){
        	int i=k,l=0; 
        	for(int j=p-1;j>=0;j--){
        		
        		if(((1<<j)&i) == 1){
        			l++;
        			//add up all to the a[]
        			for(int x=0;x<v;x++){
        				a[x]+=feed[j][x];
        			}
        		}
        	}
        	//check
        	if(check(v)==true){
        		System.out.print(l+" ");
        		//print bit numbers for i
        		for(int j=0;j<=p-1;j++){
        			if(((1<<j)&i) == 1){
        				System.out.print((j+1)+" ");
        			}
        		}
        	}
        }
        
        System.out.println();
        f.close(); out.close();*/
