n = int(input())
nums = list(map(int, input().split()))
nums.sort()
check = [0 for _ in range(101)]
ans = 0
for i in nums:
	for j in nums:
		if i % j == 0 and check[j] == 0:
			check[j] = 1
			ans += 1
			break
		if i % j == 0 and check[j] != 0:
			break
print(ans)
