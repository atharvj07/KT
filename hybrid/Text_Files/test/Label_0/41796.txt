def solve(s, a, b):
	x = int(s[0]) % a
	y = 0
	temp = 1
	aa = []
	bb = []
	bbb = [0] * len(s)
	for i in range(0, 11):
		aa.append(i % a)
		bb.append(i % b)
	temp2 = bb[1]
	bbb[0] = bb[1]
	for i in range(1, len(s)):
		y = y % b * (bb[10] % b)
		y = y % b
		y = y + bb[int(s[i])] % b
		y = y % b
		temp2 = temp2 * bb[10] % b
		bbb[i] = temp2
	temp3 = aa[10]
	if x == 0 and y == 0:
		return 1
	for i in range(1, len(s) - 1):
		x = x % a * (aa[10] % a)
		x = x % a
		x = x + aa[int(s[i])] % a
		x = x % a
		z = bb[int(s[i])] * bbb[len(s) - 1 - i]
		z = z % b
		y = y - z
		y = y % b
		if x == 0 and y == 0:
			return i + 1
	return -1
s = input()
(a, b) = map(int, input().split())
i = solve(s, a, b)
if i == -1:
	print('NO')
else:
	while i < len(s) and s[i] == '0':
		i += 1
	if i == len(s):
		print('NO')
	else:
		print('YES')
		print(s[0:i])
		print(s[i:])
