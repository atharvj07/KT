#include <bits/stdc++.h>
using namespace std;
double pi = 3.1415926536;
const int oo = (int)1e9;
const long long OO = numeric_limits<long long>::max();
int dx[] = {0, 1, 0, -1};
int dy[] = {1, 0, -1, 0};
int di[] = {0, 0, 1, -1, 1, 1, -1, -1};
int dj[] = {1, -1, 0, 0, 1, -1, 1, -1};
int f1[] = {0, 0, 0, 1, 1, 1, -1, -1, -1};
int f2[] = {-1, 0, 1, -1, 0, 1, -1, 0, 1};
bool mem[10000];
int main() {
  ios_base ::sync_with_stdio(false);
  cout.tie(NULL);
  int n;
  cin >> n;
  vector<string> myvec(n);
  for (int i = 0; i < n; i++) {
    cin >> myvec[i];
    for (int j = 0; j < ((int)(myvec[i]).size()); j++) {
      myvec[i][j] = tolower(myvec[i][j]);
    }
  }
  string message, temp;
  char letter;
  cin >> message >> letter;
  temp = message;
  for (int i = 0; i < ((int)(message).size()); i++) {
    temp[i] = tolower(temp[i]);
  }
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < ((int)(temp).size()); j++) {
      if (((int)(temp).size()) - j >= ((int)(myvec[i]).size())) {
        if (temp.substr(j, ((int)(myvec[i]).size())) == myvec[i]) {
          for (int z = 0; z < ((int)(myvec[i]).size()); z++) {
            mem[j + z] = 1;
          }
        }
      }
    }
  }
  for (int i = 0; i < ((int)(message).size()); i++) {
    if (mem[i]) {
      if (tolower(message[i]) == letter) {
        if (message[i] == 'a')
          message[i] = 'b';
        else if (message[i] == 'A')
          message[i] = 'B';
        else {
          char c = tolower(message[i]);
          if (c == message[i])
            message[i] = 'a';
          else
            message[i] = 'A';
        }
      } else {
        char c = tolower(message[i]);
        if (c == message[i]) {
          message[i] = letter;
        } else {
          message[i] = toupper(letter);
        }
      }
    }
  }
  cout << message;
}
