(n, m, k) = map(int, input().split())
a = sorted(list(map(int, input().split())))
a.reverse()
t = k
ans = 0
d = False
if t >= m:
	d = True
if not d:
	ans = -1
	for i in range(n):
		t += a[i] - 1
		if t >= m:
			ans = i + 1
			flag = True
			break
print(ans)
