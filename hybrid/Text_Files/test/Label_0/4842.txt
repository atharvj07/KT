#include <bits/stdc++.h>
using namespace std;
long long a, b, i, j, prod = 1;
int main() {
  cin >> a >> b;
  for (i = a + 1; i <= b && prod % 10; i++) prod = prod * (i % 10) % 10;
  cout << prod;
  return 0;
}
