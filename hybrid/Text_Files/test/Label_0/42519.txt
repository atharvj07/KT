import math
for _ in range(int(input())):
	(n, d) = map(int, input().split())
	if n >= d:
		print('YES')
	else:
		f = 0
		for i in range(1, n + 1):
			if i + math.ceil(d / (i + 1)) <= n:
				f = 1
				break
		if f == 1:
			print('YES')
		else:
			print('NO')
