s = input().rstrip()
n = len(s)
stack = []
for ch in s:
	if len(stack) <= 1 or not stack[-1] == stack[-2] == ch:
		stack.append(ch)
s = ''.join(stack)
stack = []
for ch in s:
	if len(stack) <= 2 or not (stack[-3] == stack[-2] and stack[-1] == ch):
		stack.append(ch)
print(''.join(stack))
