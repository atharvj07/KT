import java.io.PrintWriter;
import java.util.Arrays;
import java.util.*;

public class Main {
    /*public class Period implements Comparable<Period> {
        public int L,R;
        public Period(int l, int r) {
            L = l;
            R = r;
        }
        public int compareTo(Period that) {
            if (this.L < that.L) return -1;
            else if (this.L == that.L) return 0;
            else return 1;
        }
    }*/

    public static void main(String[] args) {
        class Period implements Comparable<Period> {
            public int L,R;
            public Period(int l, int r) {
                L = l;
                R = r;
            }
            public int compareTo(Period that) {
                if (this.L < that.L) return -1;
                else if (this.L == that.L) return 0;
                else return 1;
            }

        }

        class SortByLast implements Comparator<Period> {
            public int compare(Period a, Period b) {
                if (a.R < b.R) return -1;
                if (a.R == b.R) return 0;
                return 1;
            }
        }
        Scanner in = new Scanner(System.in);
        PrintWriter out = new PrintWriter(System.out);
        //declaring array of time periods to play chess
        int n = in.nextInt();
        Period[] chess = new Period[n];
        for (int i = 0; i < n; i++) {
            int l = in.nextInt();
            int r = in.nextInt();
            chess[i] = new Period(l,r);
        }
        Arrays.sort(chess, new SortByLast());
        //declaring array of time periods to code
        int m = in.nextInt();
        Period[] code = new Period[m];
        for (int i = 0; i < m; i++) {
            int l = in.nextInt();
            int r = in.nextInt();
            code[i] = new Period(l,r);
        }
        Arrays.sort(code);
        //computing max rest period
        int max = 0;
        /*for (int i = 0; i < n; i++)
            for (int j = 0; j < m; j++) {
                if (chess[i].L - code[j ].R > max) max = chess[i].L - code[j].R;

            }*/
        max = code[m-1].L - chess[0].R;
        Arrays.sort(chess);
        Arrays.sort(code, new SortByLast());/*
        for (int i = 0; i < m; i++)
            for (int j = 0; j < n; j++) {
                if (code[i].L - chess[j].R > max) max = code[i].L - chess[j].R;
            }*/
        if (max < chess[n-1].L - code[0].R) max = chess[n-1].L - code[0].R;
        if (max < 0) max = 0;
        out.println(max);

        out.flush();
    }

}
