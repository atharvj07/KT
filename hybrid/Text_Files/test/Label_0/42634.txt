from math import inf

def main():
	(n, a, b) = map(int, input().split())
	s = set()
	for i in range(n):
		(x, y) = map(int, input().split())
		if a - x != 0:
			s.add((b - y) / (a - x))
		else:
			s.add(inf)
	print(len(s))
main()
