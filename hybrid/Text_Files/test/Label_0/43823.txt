import sys
input = sys.stdin.readline
(n, q) = list(map(int, input().split()))
x = [int(input()) for i in range(q)]
for num in x:
	num = 2 * n - num
	while True:
		if num % 2 == 0:
			num = num // 2
		else:
			break
	print(n - num // 2)
