(n, m) = map(int, input().split())
child = list(map(int, input().split()))
(last, count) = (0, 0)
for i in range(n):
	cc = child[i] // m if child[i] % m == 0 else child[i] // m + 1
	if count <= cc:
		last = i + 1
		count = cc
print(last)
