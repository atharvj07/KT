#include <bits/stdc++.h>
using namespace std;
int main() {
  int T;
  cin >> T;
  for (int i = 0; i < T; i++) {
    long long a, b;
    cin >> a >> b;
    long long x = 10;
    int k = 0;
    while (b + 1 >= x) {
      k++;
      x *= 10;
    }
    cout << a * k << endl;
  }
}
