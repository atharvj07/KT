#include <iostream>

int main(void)
{
	using namespace std;

	int	a, b, k, i;

	cin >> a >> b >> k;
	i = a;
	while(i <= b){
		if(a+k > i || b-k < i){
			cout << i << endl;
		}
		i++;
	}

	return 0;
}
