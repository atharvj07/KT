(w1, h1, w2, h2) = (int(i) for i in input().split(' '))

def area(w, h):
	return 2 + 2 * h + w

def mod(n1, n2):
	if n1 > n2:
		return n1 - n2
	else:
		return n2 - n2
if w1 != w2:
	print(area(w1, h1) + area(w2, h2) + mod(w1, w2))
else:
	print(area(w1, h1) + area(w2, h2))
