from math import log2
t = int(input())
for _ in range(0, t):
	n = int(input())
	aa = [int(i) for i in input().split()]
	ans = 0
	for i in range(1, n):
		if aa[i - 1] > aa[i]:
			ans = max(ans, int(log2(aa[i - 1] - aa[i])) + 1)
			aa[i] = aa[i - 1]
	print(ans)
