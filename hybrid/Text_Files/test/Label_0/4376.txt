#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < n; i++)
using namespace std;

int main() {
    int n, ans;
    cin >> n;
    vector<int> h(n);
    rep(i, n) cin >> h.at(i);
    ans = h.at(0);
    rep(i, n - 1) ans += max(0, h.at(i + 1) - h.at(i));
    cout << ans << endl;
}