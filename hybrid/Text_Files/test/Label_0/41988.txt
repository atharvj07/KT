def main():
	(s, t) = (input(), input())
	s = s[::-1]
	t = t[::-1]
	length_s = len(s)
	length_t = len(t)
	try:
		index = next((i for i in range(min(length_s, length_t)) if s[i] != t[i]))
		print(length_t - index + length_s - index)
	except StopIteration:
		print(abs(length_s - length_t))
main()
