def main():
	a = input().lstrip('0')
	b = input().lstrip('0')
	if len(a) < len(b):
		print('<')
	elif len(a) > len(b):
		print('>')
	else:
		for (i, j) in zip(a, b):
			if i < j:
				print('<')
				return
			elif i > j:
				print('>')
				return
		print('=')
main()
