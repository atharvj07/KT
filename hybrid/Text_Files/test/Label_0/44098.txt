(a, b) = [int(x) for x in input().split()]
if b > a:
	print('-1')
elif b == a:
	print(b)
elif b == 1:
	print(a // 2 + 1)
else:
	counter = b
	if 2 * counter < a:
		while 2 * counter < a:
			counter += b
	print(counter)
