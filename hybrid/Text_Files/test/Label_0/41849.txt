import math as m

def solve():
	(x, y, k) = map(int, input().split())
	s = 1
	src = y * k
	tsr = src + k
	diff = x - 1
	count = 0
	p = tsr - 1
	a = p // diff
	if p % diff != 0:
		a += 1
	a += k
	return a
for _ in range(int(input())):
	print(solve())
