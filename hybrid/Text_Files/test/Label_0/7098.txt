#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a,b;
    scanf("%d%d",&a,&b);
    int res=0;
    res+=max(0,(4-a)*100000);
    res+=max(0,(4-b)*100000);
    if(a==1&&b==1) res+=400000;
    printf("%d\n",res);
    return 0;
}
