(n, d) = map(int, input().split())
li = list(map(int, input().split()))
ans = 2
length = len(li)
for i in range(0, length - 1):
	dif = li[i + 1] - li[i]
	if dif == 2 * d:
		ans += 1
	elif dif > 2 * d:
		ans += 2
print(ans)
