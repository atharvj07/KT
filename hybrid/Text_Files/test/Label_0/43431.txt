def solve(x, y, a, b):
	if x == 0 and y == 0:
		return 0
	return min(min(x, y) * b + abs(x - y) * a, (x + y) * a)
for _ in range(int(input())):
	(x, y) = map(int, input().split())
	(a, b) = map(int, input().split())
	print(solve(x, y, a, b))
