#include <bits/stdc++.h>
using namespace std;
int cs = 0;
int main() {
  int t, p, q, n, m, i, j, k, x, y, a, b;
  long long c, d, z, r, sum, sum2;
  cin >> t;
  while (t--) {
    cin >> n;
    long long ara[n];
    for (i = 0; i < n; ++i) {
      cin >> ara[i];
    }
    bool f = false;
    for (i = 1; i < n; ++i) {
      if (abs(ara[i] - ara[i - 1]) >= 2) {
        f = true;
        break;
      }
    }
    if (!f)
      cout << "NO\n";
    else {
      cout << "YES\n" << i << ' ' << i + 1 << endl;
    }
  }
  return 0;
}
