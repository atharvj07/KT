#include<bits/stdc++.h>
using namespace std;
int main()

{
	int n,k,i=1,g,h;
	cin>>n>>k;
	set<int>p;
	while(n--)
	{
		p.insert(i);
		i++;
	}
	while(k--)
	{
		cin>>g;
		while(g--)
		{
			cin>>h;
			if(p.count(h)==1)
			p.erase(h);
		}
	}
	cout<<p.size();
}
