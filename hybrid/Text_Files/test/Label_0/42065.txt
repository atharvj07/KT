for _ in range(int(input())):
	(n, k) = map(int, input().split())
	s = input()
	ones = s.count('1')
	ans = ones
	for i in range(k):
		pre = 0
		for j in range(i, n, k):
			if s[j] == '1':
				pre += 1
			else:
				pre -= 1
			pre = max(pre, 0)
			ans = min(ans, ones - pre)
	print(ans)
