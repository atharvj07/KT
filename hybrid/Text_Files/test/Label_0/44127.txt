(n, b, v) = (int(input()), list(map(int, input().split())), [])
for i in range(n):
	if b[i] == 1:
		v.append(i)
if len(v) == 0:
	print(0)
elif len(v) == 1:
	print(1)
else:
	ans = 1
	for i in range(1, len(v)):
		ans *= v[i] - v[i - 1]
	print(ans)
