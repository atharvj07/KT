(n, L, a) = map(int, input().split())
break_num = 0
right = 0
for i in range(n):
	(t, l) = map(int, input().split())
	if t - right >= a:
		break_num += (t - right) // a
	right = t + l
break_num += (L - right) // a
print(break_num)
