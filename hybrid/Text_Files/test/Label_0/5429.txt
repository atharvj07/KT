#include <bits/stdc++.h>
using namespace std;
const int mod = 1e9 + 7;
int main() {
  int N, K;
  cin >> N >> K;
  int start = 1, end = N, mid;
  while (start <= end) {
    mid = (start + end) / 2;
    if (mid + 1 > N) break;
    cout << 1 << " " << mid << ' ' << mid + 1 << endl;
    fflush(stdout);
    string str;
    cin >> str;
    if (str == "TAK")
      end = mid - 1;
    else
      start = ++mid;
  }
  int a1 = mid;
  if (mid >= 2) {
    start = 1, end = mid - 1;
    int mid1 = (start + end) / 2;
    cout << 1 << ' ' << mid1 << ' ' << mid1 + 1 << endl;
    fflush(stdout);
    string str;
    cin >> str;
    if (str == "NIE")
      start = mid + 1, end = N;
    else
      start = 1, end = mid - 1;
  } else {
    start = mid + 1, end = N;
  }
  while (start <= end) {
    mid = (start + end) / 2;
    if (mid + 1 > N) break;
    cout << 1 << " " << mid << ' ' << mid + 1 << endl;
    fflush(stdout);
    string str;
    cin >> str;
    if (str == "TAK")
      end = mid - 1;
    else
      start = ++mid;
  }
  int a2 = mid;
  cout << 2 << ' ' << min(a1, a2) << ' ' << max(a2, a1) << endl;
  return 0;
}
