t = int(input())
for _ in range(t):
	n = int(input())
	lst = list(map(int, input().split()))
	i = 0
	j = len(lst) - 1
	s = [0] * len(lst)
	for k in range(len(lst)):
		if k % 2 == 0:
			s[k] = lst[i]
			i += 1
		else:
			s[k] = lst[j]
			j -= 1
	for i in range(len(s)):
		print(s[i], ' ', end='')
