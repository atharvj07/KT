a = input()
n = len(a)
ans = 1
block = 0
for i in range(n):
	if i == n - 1 or int(a[i]) + int(a[i + 1]) != 9:
		if block > 0 and block % 2 == 0:
			ans *= block // 2 + 1
		block = 0
	else:
		block += 1
print(ans)
