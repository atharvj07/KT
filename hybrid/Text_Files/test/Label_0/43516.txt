def solve(N, A):
	thre = N * 4 + N // 2
	if N % 2 > 0:
		thre += 1
	A.sort()
	s = sum(A)
	c = 0
	while s < thre:
		s += 5 - A[c]
		c += 1
	return c

def main():
	N = int(input())
	A = [int(e) for e in input().split(' ')]
	assert len(A) == N
	print(solve(N, A))
main()
