import java.util.*;
import static java.lang.Math.*;

public class Main{
  public static void main(String[] args){
    new Main().run();
  }
  
  void run(){
    Scanner sc = new Scanner(System.in);
    int t = sc.nextInt();
    for(;t>0;t--){
      int n = sc.nextInt();
      int m = sc.nextInt();
      int x1 = sc.nextInt();
      int y1 = sc.nextInt();
      int x2 = sc.nextInt();
      int y2 = sc.nextInt();
      int dx = abs(x1-x2);
      int dy = abs(y1-y2);
      int mnx = min(x1, x2);
      int mxx = max(x1, x2);
      int mny = min(y1, y2);
      int mxy = max(y1, y2);
      int w = n - dx;
      int h = m - dy;
      long ans = 1l * n * m - (1l * w * h * 2 - calc(mnx, mny, w, h, mxx, mxy, w, h));
      System.out.println(ans);
      /*
      if(x1 == x2){
        int mn = min(y1, y2);
        int mx = max(y1, y2);
        int left = m - 1 - dy;
        int right = dy;
        if(left < right)System.out.println(1l * n * (right - left - 1));
        else System.out.println(0);
      }else if(y1 == y2){
        int mn = min(x1, x2);
        int mx = max(x1, x2);
        int left = n - 1 - dx;
        int right = dx;
        if(left < right)System.out.println(1l * m * (right - left - 1));
        else System.out.println(0);
      }else{
        int a1 = n - dx;
        int b1 = 0;
        int a2 = 0;
        int b2 = m - dy;
        long dif = calc(a1, b1, dx, dy, a2, b2, dx, dy);
        long ans = 2l * dx * dy - dif;
        System.out.println(ans);
      }
      */
    }
  }
  
  long calc(int a1, int b1, int w1, int h1, int a2, int b2, int w2, int h2){
    if(a1 + w1 > a2 && b1 + h1 > b2)
      return 1l*(a1+w1-a2)*(b1+h1-b2);
    return 0;
  }
}
