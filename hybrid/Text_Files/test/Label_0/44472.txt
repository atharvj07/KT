import math
import sys
import collections

def In():
	return map(int, sys.stdin.readline().split())
input = sys.stdin.readline

def krillgame():
	(l, r, x, y, k) = In()
	for i in range(x, y + 1):
		if l <= k * i <= r:
			print('YES')
			break
	else:
		print('NO')
krillgame()
