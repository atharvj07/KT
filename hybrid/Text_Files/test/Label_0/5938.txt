#include <bits/stdc++.h>
int main() {
  char str[100009], str2[100009];
  int i, len, len2, max;
  scanf("%s %s", &str, &str2);
  len = strlen(str);
  len2 = strlen(str2);
  max = len;
  if (len2 > max) {
    max = len2;
  }
  if (strcmp(str, str2) != 0) {
    printf("%d\n", max);
  } else
    printf("-1\n");
  return 0;
}
