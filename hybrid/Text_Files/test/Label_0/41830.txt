from math import sqrt
from math import ceil
n = int(input())
s_n = int(ceil(sqrt(n)))
for x in range(2, n, +1):
	if n % x == 0:
		print(int(1 + (n - x) / 2))
		exit(0)
	if x > s_n:
		print(1)
		exit(0)
print(1)
