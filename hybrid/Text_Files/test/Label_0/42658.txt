(n, m) = map(int, input().split())
a = [None] * n
for i in range(n):
	s = input()
	ns = ''
	cur = 0
	flag = 1
	for j in range(m):
		if j % 2:
			ns += chr(155 - ord(s[j]))
		else:
			ns += s[j]
	a[i] = [ns, i]
print(*[v[1] + 1 for v in sorted(a)])
