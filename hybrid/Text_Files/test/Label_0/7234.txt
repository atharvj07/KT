#include <bits/stdc++.h>
using namespace std;
int32_t main() {
  ios_base::sync_with_stdio(0), cin.tie(0);
  long long l, r;
  cin >> l >> r;
  if (l & 1) l++;
  if (l + 2 <= r)
    cout << l << " " << l + 1 << " " << l + 2 << "\n";
  else
    cout << -1 << "\n";
}
