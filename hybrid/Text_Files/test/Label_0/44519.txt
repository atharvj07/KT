for inp in range(int(input())):
	n = int(input())
	s = input() + 'A'
	s = list(s)
	s.reverse()
	m = 0
	for i in range(s.count('A')):
		ind = s.index('A')
		s = s[ind + 1:]
		if m < ind:
			m = ind
	print(m)
