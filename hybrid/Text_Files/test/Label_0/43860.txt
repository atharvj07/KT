n = int(input())
arr = list(map(int, input().split()))
used = [0] * 7005
arr.sort()
res = 0
for i in range(n):
	if not used[arr[i]]:
		used[arr[i]] = 1
	else:
		for j in range(arr[i], 7001):
			if not used[j]:
				used[j] = 1
				break
			else:
				res += 1
print(res)
