(n, s) = map(int, input().split())
mn = 1000
ans = 0
for i in range(n):
	(x, y) = map(int, input().split())
	cost = x + y / 100
	if cost <= s:
		mn = min(mn, x)
		if y == 0:
			continue
		ans = max(ans, min(99, 100 - y))
if mn == 1000:
	ans = -1
print(ans)
