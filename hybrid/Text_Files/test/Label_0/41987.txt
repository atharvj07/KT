import sys

def solve(a, b):
	d = abs(a - b)
	d1 = d // 2
	d2 = d - d1
	s = (d1 + 1) * d1 // 2 + (d2 + 1) * d2 // 2
	return s
(a, b) = [int(v) for v in sys.stdin.readlines()]
res = solve(a, b)
print(res)
