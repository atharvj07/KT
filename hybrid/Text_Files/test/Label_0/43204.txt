t = int(input())
for i in range(t):
	(p, a, b, c) = map(int, input().split())
	a1 = p // a + 1
	b1 = p // b + 1
	c1 = p // c + 1
	l = [a1 * a - p, b1 * b - p, c1 * c - p]
	if (p % a != 0 and p % b != 0 and (p % c != 0)) and l != [a, b, c]:
		print(min(l))
	else:
		print(0)
