#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

ll X,Y;

int main() {
  cin >> X >> Y;
  if(X < Y) swap(X,Y);

  if(X - Y > 1) cout <<"Alice"<<endl;
  else cout <<"Brown"<<endl;
}
