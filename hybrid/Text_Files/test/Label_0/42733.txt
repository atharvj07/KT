import bisect
n = int(input())
a = list(map(int, input().split()))
d = []
ms = []
for i in range(n):
	idx = bisect.bisect_left(ms, -a[i])
	if idx == len(ms):
		d.append([a[i]])
		ms.append(-a[i])
	else:
		ms[idx] = -a[i]
		d[idx].append(a[i])
for x in d:
	print(*x)
