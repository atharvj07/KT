import static java.lang.Math.max;
import static java.lang.Math.min;
import static java.lang.Math.abs;
import java.util.*;
import java.io.*;

public class x1473D2
{
    public static void main(String hi[]) throws Exception
    {
        BufferedReader infile = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer st = new StringTokenizer(infile.readLine());
        int T = Integer.parseInt(st.nextToken());
        StringBuilder sb = new StringBuilder();
        while(T-->0)
        {
            st = new StringTokenizer(infile.readLine());
            int N = Integer.parseInt(st.nextToken());
            int M = Integer.parseInt(st.nextToken());
            char[] arr = infile.readLine().toCharArray();
            int[] prefix = new int[N];
            int[] prefmax = new int[N];
            int[] prefmin = new int[N];
            int curr = arr[0] == '+' ? 1 : -1;
            if(curr == 1)
                prefmax[0] = 1;
            else
                prefmin[0] = -1;
            prefix[0] = curr;
            for(int i=1; i < N; i++)
            {
                if(arr[i] == '+')
                    curr++;
                else
                    curr--;
                prefix[i] = curr;
                prefmax[i] = max(prefmax[i-1], curr);
                prefmin[i] = min(prefmin[i-1], curr);
            }
            LazySegTree suffmax = new LazySegTree(N);
            LazySegTree suffmin = new LazySegTree(N);
            int dex = N-1;
            Query[] qq = new Query[M];
            for(int i=0; i < M; i++)
            {
                st = new StringTokenizer(infile.readLine());
                int L = Integer.parseInt(st.nextToken())-1;
                int R = Integer.parseInt(st.nextToken())-1;
                qq[i] = new Query(L, R, i);
            }
            Arrays.sort(qq);
            int[] res = new int[M];
            for(Query q: qq)
            {
                int L = q.left;
                int R = q.right;
                while(R < dex)
                {
                    int delta = -1;
                    if(arr[dex] == '+')
                        delta = 1;
                    suffmax.update(dex, N-1, delta);
                    suffmin.update(dex, N-1, -delta);
                    dex--;
                }
                if(R == N-1)
                {
                    if(L == 0)
                        res[q.dex] = 1;
                    else
                        res[q.dex] = prefmax[L-1]-prefmin[L-1]+1;
                }
                else
                {
                    int maxtemp = suffmax.query(R+1, N-1);
                    int mintemp = -suffmin.query(R+1, N-1);
                    int max = maxtemp;
                    int min = mintemp;
                    if(L > 0)
                    {
                        max = max(prefmax[L-1], prefix[L-1]+maxtemp);
                        min = min(prefmin[L-1], prefix[L-1]+mintemp);
                    }
                    else
                    {
                        max = max(0, max);
                        min = min(0, min);
                    }
                    res[q.dex] = max-min+1;
                }
            }
            for(int x: res)
                sb.append(x+"\n");
        }
        System.out.print(sb);
    }
    public static void print(int[] arr)
    {
        //for debugging only
        for(int x: arr)
            System.out.print(x+" ");
        System.out.println();
    }
}
/*
1
4 2
+-++
3 3
1 3


1
8 1
-+--+--+
2 5

 */
 class Query implements Comparable<Query>
{
    public int left;
    public int right;
    public int dex;

    public Query(int a, int b, int i)
    {
        left = a;
        right = b;
        dex = i;
    }
    public int compareTo(Query oth)
    {
        return oth.right-right;
    }
}
class LazySegTree
{
    //definitions
    private int NULL = -1;
    private int[] tree;
    private int[] lazy;
    private int length;

    public LazySegTree(int N)
    {
        length = N;   int b;
        for(b=0; (1<<b) < length; b++);
        tree = new int[1<<(b+1)];
        lazy = new int[1<<(b+1)];
    }
    public int query(int left, int right)
    {
        //left and right are 0-indexed
        return get(1, 0, length-1, left, right);
    }
    private int get(int v, int currL, int currR, int L, int R)
    {
        if(L > R)
            return NULL;
        if(L <= currL && currR <= R)
            return tree[v];
        propagate(v);
        int mid = (currL+currR)/2;
        return comb(get(v*2, currL, mid, L, Math.min(R, mid)),
                get(v*2+1, mid+1, currR, Math.max(L, mid+1), R));
    }
    public void update(int left, int right, int delta)
    {
        add(1, 0, length-1, left, right, delta);
    }
    private void add(int v, int currL, int currR, int L, int R, int delta)
    {
        if(L > R)
            return;
        if(currL == L && currR == R)
        {
            //exact covering
            tree[v] += delta;
            lazy[v] += delta;
            return;
        }
        propagate(v);
        int mid = (currL+currR)/2;
        add(v*2, currL, mid, L, Math.min(R, mid), delta);
        add(v*2+1, mid+1, currR, Math.max(L, mid+1), R, delta);
        tree[v] = comb(tree[v*2], tree[v*2+1]);
    }
    private void propagate(int v)
    {
        //tree[v] already has lazy[v]
        if(lazy[v] == 0)
            return;
        tree[v*2] += lazy[v];
        lazy[v*2] += lazy[v];
        tree[v*2+1] += lazy[v];
        lazy[v*2+1] += lazy[v];
        lazy[v] = 0;
    }
    private int comb(int a, int b)
    {
        return Math.max(a,b);
    }
}