import math
n = int(input())
l = list(map(int, input().split()))
(x, f) = map(int, input().split())
total = 0
for j in range(n):
	if l[j] > x:
		total += math.ceil((l[j] - x) / (x + f))
print(total * f)
