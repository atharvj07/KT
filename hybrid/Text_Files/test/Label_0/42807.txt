for i in range(int(input())):
	(x, y) = input().split()
	(a, b) = input().split()
	(x, y, a, b) = (int(x), int(y), int(a), int(b))
	if b >= 2 * a:
		print((x + y) * a)
	else:
		print(min(x, y) * b + a * (max(x, y) - min(x, y)))
