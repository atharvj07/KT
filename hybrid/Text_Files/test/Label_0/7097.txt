#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
     
    long long int t;
    cin>>t;
    while(t--){
         long long int n,m,tmp,temp;
         cin>>n;
          long long int prefix1[n+1];
          prefix1[0]=0;
        
        long long int arr1[n];
        temp=0;
        for (int i=0;i<n;i++){
            cin>>tmp;
            arr1[i]=tmp;
            temp+=arr1[i];
            prefix1[i+1]=temp;
            
            
        }
        cin>>m;
        temp=0;
        long long int arr2[m];
         long long int prefix2[m+1];
         prefix2[0]=0;
         for (int i=0;i<m;i++){
            cin>>tmp;
            arr2[i]=tmp;
            temp+=arr2[i];
            prefix2[i+1]=temp;
            
        }
        
        /* long long int dp[n+1][m+1];
         for (int i=0;i<n;i++){
             for (int j=0;j<m;j++){
                 dp[i][j]=0;
             }
         }*/
         
        /* for (int i=0;i<=n;i++){
             for(int j=0;j<=m;j++){
                 if (i==0 and j!=0){
                     dp[i][j]=dp[i][j-1]+arr2[j-1];
                 }
                 else if (i!=0 and j==0){
                     dp[i][j]=dp[i-1][j]+arr1[i-1];
                 }
                 
                 else{
                     if (dp[i-1][j]+arr1[i-1] >= dp[i][j-1]+arr2[j-1]){
                          dp[i][j]=dp[i-1][j]+arr1[i-1];
                     }
                     else{
                         dp[i][j]=dp[i][j-1]+arr2[j-1];
                     }
                 }
                 
             }
         }
         
        cout<<dp[n][m]<<"\n"; */
        long long int ans=-999999;
        for (int i=0;i<=n;i++){
             for (int j=0;j<=m;j++){
                 if (ans<=prefix1[i]+prefix2[j]){
                     ans=prefix1[i]+prefix2[j];
                 }
                 
             }
         }
        cout<<ans<<"\n"; 
        
        
    }
    return 0;
}