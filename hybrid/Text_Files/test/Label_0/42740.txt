for _ in range(int(input())):
	(n, k) = map(int, input().split())
	c = list(map(int, input().split()))
	s = set(c)
	ans = 10 ** 9
	for item in s:
		i = 0
		count = 0
		while i < n:
			if c[i] == item:
				i += 1
			else:
				i += k
				count += 1
		ans = min(ans, count)
	print(ans)
