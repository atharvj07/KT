(n, k) = map(int, input().split())
a = list(map(int, input().split()))
add_walks = 0
for i in range(1, n):
	walks_needed = max(k - a[i] - a[i - 1], 0)
	a[i] += walks_needed
	add_walks += walks_needed
print(add_walks)
print(' '.join(map(str, a)))
