(n, m) = map(int, input().split())
for i in range(n):
	s = input()
	o = ''
	for j in range(m):
		if s[j] == '.':
			if (i + j) % 2 == 0:
				o += 'B'
			else:
				o += 'W'
		else:
			o += s[j]
	print(o)
