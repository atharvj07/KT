#include<iostream>
#include<iomanip>
#include<string>
#include<set>
#include<vector>
#include<utility>
#include<queue>
#include<algorithm>
#include<functional>
using namespace std;
#define LL long long
#define MOD 100000
#define inf 1999999999

int main() {
	int a, b, x;
	cin >> a >> b >> x;
	int cost = 999999999;
	for (int i = 0; i < 100; i++) {
		for (int j = 0; j < 100; j++) {
			if (i * 1000 + j * 500 < x) continue;
			cost = min(cost, a*i + b * j);
		}
	}
	cout << cost << endl;
	return 0;
}

