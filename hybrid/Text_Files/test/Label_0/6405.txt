#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, x, l, r, p = 1, sum = 0;
  cin >> n >> x;
  for (int i = 0; i < n; i++) {
    cin >> l >> r;
    while (p + x <= l) p += x;
    sum += r - p + 1;
    p = r + 1;
  }
  cout << sum << endl;
  return 0;
}
