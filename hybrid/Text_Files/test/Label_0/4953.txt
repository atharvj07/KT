#include <bits/stdc++.h>
using namespace std;
long long calc(string &second) {
  long long ans = 0;
  for (long long i = 0; i < second.size(); i++) {
    ans *= 10;
    ans += (second[i] - '0');
  }
  return ans;
}
long long pw[20];
int main() {
  pw[0] = 1;
  for (int i = 1; i < 20; i++) {
    pw[i] = 10LL * pw[i - 1];
  }
  int n;
  cin >> n;
  for (long long i = 0; i < n; i++) {
    char a;
    cin >> a >> a >> a >> a;
    string second;
    cin >> second;
    long long val = calc(second);
    long long p = 1989 - 1;
    long long num = 0;
    for (int i = second.size() - 1; i >= 0; i--) {
      num += (long long)(second[i] - '0') * pw[second.size() - 1 - i];
      long long cur = 0;
      while (1) {
        if (cur * pw[second.size() - i] + num > p) {
          p = cur * pw[second.size() - i] + num;
          break;
        }
        cur++;
      }
    }
    cout << p << "\n";
  }
}
