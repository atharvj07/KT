#include <bits/stdc++.h>
int main() {
  int n, A[100001] = {0};
  scanf("%d", &n);
  for (int i = 0; i < n; i++) scanf("%d", &A[i]);
  for (int i = 0; i < n; i++) printf("%d ", A[i] + A[i + 1]);
  return 0;
}
