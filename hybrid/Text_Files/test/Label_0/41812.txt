(n, k) = list(map(int, input().split()))
s = input()
for i in range(k):
	p = ''
	(l, r, c1, c2) = input().split()
	for j in range(n):
		if j in range(int(l) - 1, int(r)):
			if s[j] == c1:
				p += c2
			else:
				p += s[j]
		else:
			p += s[j]
	s = p
print(p)
