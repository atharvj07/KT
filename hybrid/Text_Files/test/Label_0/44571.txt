res = []
for i in range(int(input())):
	(n, k) = map(int, input().split())
	a = sorted(list(map(int, input().split())), reverse=True)
	cat = 0
	cnt = 0
	for i in a:
		if i <= cat:
			break
		cat += n - i
		cnt += 1
	res.append(cnt)
print(*res, sep='\n')
