n = int(input())
a = list(map(int, input().split()))
min = min(a)
ans = []
for i in range(n):
	if a[i] == min:
		ans.append(i)
now = 9999999
for i in range(1, len(ans)):
	if ans[i] - ans[i - 1] < now:
		now = ans[i] - ans[i - 1]
print(now)
