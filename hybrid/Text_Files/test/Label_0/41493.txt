def solve(n, m, res):
	ans = 0
	negs = 0
	f = 0
	x = 100000
	for i in range(n):
		for j in range(m):
			if res[i][j] < 0:
				negs += 1
			if res[i][j] == 0:
				f = 1
			ans += abs(res[i][j])
			x = min(x, abs(res[i][j]))
	if f == 1:
		return ans
	elif negs % 2 != 0:
		return ans - 2 * x
	else:
		return ans
t = int(input())
while t:
	(n, m) = map(int, input().split())
	res = []
	for _ in range(n):
		res.append(list(map(int, input().split())))
	print(solve(n, m, res))
	t -= 1
