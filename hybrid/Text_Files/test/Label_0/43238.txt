t = int(input())
for te in range(0, t):
	(n, k) = [int(x) for x in input().split()]
	c = int(0)
	while n != 0:
		c += n % k
		n = n - n % k
		if n == 0:
			break
		if n % k == 0:
			n = n // k
			c += 1
			if n == 0:
				break
	print(c)
