answer = 0
(clients, day, smoke) = (int(x) for x in input().split())
available = []
stop = 0
for i in range(clients):
	info = [int(x) for x in input().split()]
	answer += (info[0] - stop) // smoke
	stop = info[0] + info[1]
answer += (day - stop) // smoke
print(answer)
