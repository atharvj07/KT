(n, k) = map(int, input().rstrip().split(' '))
m = max(n, k)
n = min(n, k)
total = m * (n // 5)
z = n % 5 + m % 5
f = 0
if z > 5:
	f = z % 5
else:
	f = 0
total = total + n % 5 * (m // 5) + z // 5 + f
print(total)
