import math
first_line = list(map(int, input().split()))
(n, h) = first_line
total_area = 0.5 * h
area = total_area / 3
ans = ''
for i in range(n - 1):
	ans += str(math.sqrt(2 * h * (total_area * (i + 1) / n)))
	ans += ' '
print(ans.strip())
