for _ in range(int(input())):
	n = int(input())
	l = list(map(int, input().split()))
	a = l.index(max(l))
	b = l.index(min(l))
	diff = abs(a - b)
	a_n = n - a
	b_n = n - b
	a += 1
	b += 1
	print(min(max(a, b), max(a_n, b_n), a + b_n, a_n + b, a + diff, b + diff, a_n + diff, b_n + diff))
