n = int(input())
a = []
for i in range(n):
	(l, r) = map(int, input().split())
	a.append([l, r, i + 1])
a.sort(key=lambda f: (f[0], -f[1]))
large = 0
for i in range(1, n):
	if a[i][1] <= a[large][1]:
		print(str(a[i][2]) + ' ' + str(a[large][2]))
		exit()
	else:
		large = i
print('-1 -1')
