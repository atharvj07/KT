for i in range(int(input())):
	n = int(input())
	d = dict()
	for i in range(n):
		s = input()
		for x in s:
			d[x] = d.get(x, 0) + 1
	l = 'YES'
	for x in d.keys():
		if d[x] % n != 0:
			l = 'NO'
			break
	print(l)
