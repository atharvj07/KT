for _ in range(int(input())):
	(x, y) = map(int, input().split())
	(a, b) = map(int, input().split())
	count = 0
	if x != 0 or y != 0:
		m = min(x, y)
		x -= m
		y -= m
		count += min(m * b, m * (2 * a))
	if x != 0:
		count += x * a
	else:
		count += y * a
	print(count)
