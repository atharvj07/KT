(n, m) = map(int, input().split())
a = list(map(int, input().split()))
for i in range(m):
	(l, r, x) = map(int, input().split())
	if x < l or x > r:
		print('Yes')
	else:
		cnt = 0
		for j in range(l, r + 1):
			cnt += a[j - 1] < a[x - 1]
		if l + cnt == x:
			print('Yes')
		else:
			print('No')
