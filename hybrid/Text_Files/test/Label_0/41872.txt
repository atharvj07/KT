m = int(input())
for j in range(m):
	n = int(input())
	a = list(map(int, input().split()))
	max_ = a[0]
	maxdif = 0
	for x in a:
		if max_ - x > maxdif:
			maxdif = max_ - x
		if x > max_:
			max_ = x
	i = 0
	while maxdif > 0:
		maxdif //= 2
		i += 1
	print(i)
