#include <bits/stdc++.h>
using namespace std;
int main() {
  int x, y, z, t1, t2, t3;
  cin >> x >> y >> z >> t1 >> t2 >> t3;
  int stair = abs(x - y) * t1;
  int ev = t2 * (abs(x - z) + abs(x - y)) + 3 * t3;
  if (stair < ev)
    cout << "NO\n";
  else
    cout << "YES\n";
  return 0;
}
