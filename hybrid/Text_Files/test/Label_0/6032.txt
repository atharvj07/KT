#include <bits/stdc++.h>
using namespace std;
long long fourpow(int n) {
  if (n == 0)
    return 1;
  else
    return 4 * fourpow(n - 1);
}
long long ans(int n) {
  return ((n - 3) * 9 * fourpow(n - 3) + 6 * fourpow(n - 2));
}
int main() {
  int n;
  cin >> n;
  cout << ans(n);
}
