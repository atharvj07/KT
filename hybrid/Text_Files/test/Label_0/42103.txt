def solve():
	s = input()
	(os, op, cnt) = (0, 0, 0)
	for i in s:
		if i == '(':
			op += 1
		elif i == '[':
			os += 1
		elif i == ')' and op > 0:
			op -= 1
			cnt += 1
		elif i == ']' and os > 0:
			os -= 1
			cnt += 1
	print(cnt)
t = int(input())
for _ in range(t):
	solve()
