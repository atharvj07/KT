for _ in range(int(input())):
	s = str(input())
	t = str(input())
	p = str(input())
	if len(s) > len(t):
		print('NO')
	elif s == t:
		print('YES')
	else:
		count = -1
		e = 0
		for i in s:
			for j in range(len(t)):
				if i == t[j] and count < j:
					count = j
					e = e + 1
					break
		if e == len(s):
			y = 0
			c = s + p
			c1 = list(s + p)
			for l in t:
				if l in c1:
					c1.remove(l)
					y = y + 1
			if y == len(t):
				print('YES')
			else:
				print('NO')
		else:
			print('NO')
