def solver(n):
	if n == 1:
		return 0
	elif n == 2:
		return 1
	elif n % 2 != 0:
		return 1 + solver(n - 1)
	else:
		return 1 + solver(n / (n / 2))
t = int(input())
for _ in range(t):
	n = int(input())
	print(solver(n))
