t = int(input())
mod = 10 ** 9 + 7
for i in range(t):
	(n, k) = tuple([int(x) for x in input().split()])
	solve = 0
	bits = reversed(bin(k)[2:])
	for (k, v) in enumerate(bits):
		if v == '1':
			solve += n ** k
	print(solve % mod)
