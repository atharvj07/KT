import math as m
for _ in range(int(input())):
	(n, k) = [int(i) for i in input().split()]
	if n == k:
		print(n)
	elif n < k:
		if n > k // 2:
			print(k // 2)
		else:
			print(n)
	else:
		k1 = n % k
		n -= k1
		if k1 <= k // 2:
			print(n + k1)
		else:
			k1 = k // 2
			print(n + k1)
