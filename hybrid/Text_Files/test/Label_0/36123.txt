import java.util.*;
public class dk{
public static void main(String[] args){
		Scanner scn=new Scanner(System.in);
		int mT=scn.nextInt();
		int mtcs=0;
		int arr[]=new int[5];
		StringBuilder res=new StringBuilder();
		A: while (mtcs++ < mT) {
			long ma = scn.nextLong(), mb = scn.nextLong(), c = scn.nextLong();
			if (ma >= c) {
				res.append("-1 ");
			} else
				res.append("1 ");
			if (c / mb >= ma)
				res.append("-1\n");
			else
				res.append(mb + "\n");
		}
		System.out.println(res);
        
	}
	}