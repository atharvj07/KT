for _ in range(int(input())):
	(n, v, u) = map(int, input().split())
	a = [int(i) for i in input().split()]
	cost = min(u + v, u + u)
	prev = a[0]
	for i in range(1, n):
		if abs(a[i] - prev) > 1:
			cost = 0
			break
		if abs(a[i] - prev) == 1:
			cost = min(u, v)
		prev = a[i]
	print(cost)
