import sys

def main():
	(n, x) = [int(i) for i in input().split()]
	ans = 0
	s = 1
	for k in range(n):
		(l, r) = [int(i) for i in input().split()]
		ans += (l - s) % x + r - l + 1
		s = r + 1
	print(ans)
main()
