import java.util.*;
import java.io.*;
public class spematr{
public static long mod = 0,res=0;
public static HashMap<Long,Long> dp= new HashMap<Long,Long>();//[501][501][501];
public static PrintWriter out = new PrintWriter(System.out);//HACER FLUSHHHH
public static void main(String args[]) throws IOException{
BufferedReader lector = new BufferedReader(new InputStreamReader(System.in));
String tmp[] =(lector.readLine()).split(" ");
int a = Integer.parseInt(tmp[0]);
int b = Integer.parseInt(tmp[1]);
mod = Long.parseLong(tmp[2]);
int cant[] = new int[a];
for(int n = 0;n<b;n++){
String t = lector.readLine();
for(int m=0;m<t.length();m++)
if(t.charAt(m)=='1')cant[m]++;
}
int cant1 = 0;
int cant2 = 0;
for(int n = 0;n<cant.length;n++){
if(cant[n]==0)cant2++;
else if(cant[n]==1)cant1++;
}
System.out.println(hagale(a-b,cant1,cant2));
}
public static long hagale(long a,long b,long c){
//System.out.println("-->"+a+" "+b+" "+c);
long k = a*501L*501L+b*501L+c;
boolean esta = dp.keySet().contains(k);
if(esta)return dp.get(k);
if(a==0)
return (b==0 && c==0)?1:0;
long res = 0;
if(b>1)//
res+=(hagale(a-1,b-2,c)*((b*(b-1))/2))%mod;
if(c>0 && b>0)
res+=((((hagale(a-1,b,c-1)*b)%mod)*c)%mod)%mod;
if(c>1)
res+=(hagale(a-1,b+2,c-2)*((c*(c-1))/2))%mod;
res%=mod;
while(res<0)res+=mod;
dp.put(k,res);
return res;
}
}
