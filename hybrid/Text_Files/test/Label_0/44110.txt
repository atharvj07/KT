import sys
import math
from functools import reduce
n = int(input())
for x in range(n):
	(n, k) = map(int, sys.stdin.readline().split())
	counter = 0
	while n != 0:
		if n % k == 0:
			n //= k
			counter += 1
		else:
			t = n % k
			n -= t
			counter += t
	print(counter)
