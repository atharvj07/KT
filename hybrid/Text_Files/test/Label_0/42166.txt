import sys
t = int(sys.stdin.readline())
ans = []
for _ in range(t):
	n = int(sys.stdin.readline())
	arr = sys.stdin.readline().split()
	res = arr[0]
	for s in arr[1:]:
		if s[0] == res[-1]:
			res += s[1]
		else:
			res += s
	if len(res) < n:
		res += 'a'
	ans.append(res)
print('\n'.join(ans))
