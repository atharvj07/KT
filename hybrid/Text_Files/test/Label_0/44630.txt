def main():
	t = int(input(''))
	while t > 0:
		lista = input('').split()
		calcular(lista[0], lista[1], lista[2], lista[3])
		t -= 1

def calcular(s, a, b, c):
	s = int(s)
	a = int(a)
	b = int(b)
	c = int(c)
	comprando = s // c
	gratis = comprando // a * b
	print(comprando + gratis)
main()
