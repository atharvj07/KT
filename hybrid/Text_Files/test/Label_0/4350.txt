#include <bits/stdc++.h>
using namespace std;
int main() {
  long long a1, a2, a3, b1, b2, b3;
  cin >> a1 >> b1;
  cin >> a2 >> b2;
  cin >> a3 >> b3;
  string ans = "NO";
  if (a2 <= a1 && a3 <= a1) {
    if (b2 + b3 <= b1) ans = "YES";
  }
  if (a2 <= a1 && b3 <= a1) {
    if (b2 + a3 <= b1) ans = "YES";
  }
  if (b2 <= a1 && a3 <= a1) {
    if (a2 + b3 <= b1) ans = "YES";
  }
  if (b2 <= a1 && b3 <= a1) {
    if (a2 + a3 <= b1) ans = "YES";
  }
  if (a2 <= b1 && a3 <= b1) {
    if (b2 + b3 <= a1) ans = "YES";
  }
  if (a2 <= b1 && b3 <= b1) {
    if (b2 + a3 <= a1) ans = "YES";
  }
  if (b2 <= b1 && a3 <= b1) {
    if (a2 + b3 <= a1) ans = "YES";
  }
  if (b2 <= b1 && b3 <= b1) {
    if (a2 + a3 <= a1) ans = "YES";
  }
  cout << ans;
  return 0;
}
