#include <bits/stdc++.h>
#pragma comment(linker, "/STACK:1024000000,1024000000")
using namespace std;
int main() {
  ios::sync_with_stdio(false);
  map<int, int> mp;
  int tmp, leg, body, head, f = 0;
  for (int i = 0; i < 6; i++) {
    cin >> tmp;
    mp[tmp]++;
  }
  if (mp.size() == 3 || mp.size() == 2 || mp.size() == 1) {
    if (mp.size() == 1) {
      cout << "Elephant" << endl;
      return 0;
    }
    for (map<int, int>::iterator it = mp.begin(); it != mp.end(); ++it) {
      if (it->second >= 4) {
        leg = it->first;
        it->second -= 4;
        if (it->second == 0) {
          mp.erase(it->first);
        }
        f = 1;
        break;
      }
    }
    if (!f) {
      cout << "Alien" << endl;
      return 0;
    }
    if (mp.size() == 1) {
      cout << "Elephant" << endl;
      return 0;
    }
    if (mp.size() == 2) {
      map<int, int>::iterator it = mp.begin();
      int a = it->first;
      ++it;
      int b = it->first;
      if (a < b || b < a) {
        cout << "Bear" << endl;
        return 0;
      }
    } else {
      cout << "Alien" << endl;
      return 0;
    }
  } else {
    cout << "Alien" << endl;
    return 0;
  }
  return 0;
}
