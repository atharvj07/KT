n = map(int, input())
a = list(map(int, input().split()))
a.sort()
Max = 1000000000000
ans = 1000000000000
for x in range(1, 101):
	sum = 0
	for y in a:
		k = abs(x - y)
		k = min(k, abs(k - 1))
		sum += k
	if sum < Max:
		ans = x
		Max = sum
print(ans, end=' ')
print(Max)
