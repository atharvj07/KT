import math
n = int(input())
flag = 0
for x in range(1, int(math.sqrt(n))):
	if (pow(x, 2) + x + 1 - n) / (-2 * x) == int((pow(x, 2) + x + 1 - n) / (-2 * x)):
		print(x, (pow(x, 2) + x + 1 - n) // -2 * x)
		flag = 1
		break
if flag == 0:
	print('NO')
