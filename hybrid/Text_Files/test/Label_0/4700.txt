#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef pair<int, int> P;

#define pb push_back
#define rep(i, a, n) for(int i = (a); i < (n); i++)
#define dep(i, a, n) for(int i = (a); i >= (n); i--)
#define mod 1e9+7

__attribute__((constructor))
void initial() {
  cin.tie(0);
  ios::sync_with_stdio(false);
}


int main() {
  int w;
  cin >> w;
  string ans = "0";

  rep(i, 1, w + 1) {
    dep(j, ans.length() - 1, 0) {
      if(ans[j] == '-' || ans[j] == '0') {
        ans[j] = ans[j] == '-' ? '0' : '+';
        rep(k, j + 1, ans.length()) {
          ans[k] = '-';
        }
        break;
      }
      if(j == 0 && ans[j] == '+') {
        int c = ans.length();
        ans = '+';
        rep(k, 0, c) ans += '-';
      }
    }
  }

  cout << ans << endl;
}