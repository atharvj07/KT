#include <iostream>

using namespace std;

int main() {
	int n;
	long long int a, b;
	cin >> n >> a >> b;
	int c = 0;
	long long int d;
	for (int i = 0; i < n; i++) {
		cin >> d;
		if (a <= d && d < b) {
		} else {
			c++;
		}
	}
	cout << c << endl;
	return 0;
}