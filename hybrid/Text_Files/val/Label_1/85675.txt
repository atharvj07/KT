#include<bits/stdc++.h>
using namespace std;
int main() {
    string a[2], b[2];
    for(int i = 0; i < 2; i++) cin >> a[i];
    for(int i = 0; i < 2; i++) cin >> b[i];
    if(a[0] == b[0] && a[1] == b[1]) cout << "YES" << endl;
    else cout << "NO" << endl;
}
