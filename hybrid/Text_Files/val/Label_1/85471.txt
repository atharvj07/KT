#include <iostream>

int fib[1001], n;

int main() {
    std::cin >> n;
    fib[1] = fib[2] = 1;
    for (int i = 3; i <= n; ++i) {
        fib[i] = fib[i - 1] + fib[i - 2];
    }
    for (int i = 1; i <= n; ++i) {
        std::cout << (fib[i] & 1 ? 'O' : 'o');
    }
    return 0;
}
