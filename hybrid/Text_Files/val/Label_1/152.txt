import sys

input = sys.stdin.readline

t = int(input())

for _ in range(t):
    n = int(input())
    a = input()
    ans = 0
    tmp = 0
    for x in a:
        if x == '*':
            ans += tmp
            tmp = 0
        else:
            tmp += 1
    print(ans)
