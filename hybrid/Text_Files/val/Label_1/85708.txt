#include <iostream>
using namespace std;
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int n, sum = 0;
    cin >> n;
    while (n--) { cin >> sum; if (sum % 180 != 0) { cout << "NO"; return 0; } }
    cout << "YES";
}
