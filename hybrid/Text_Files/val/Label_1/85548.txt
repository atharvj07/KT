#include <iostream>
#include <vector>

using namespace std;

int main() {
  long long a, b;
  cin >> a >> b;

  long long x = b / a; // x = the integer part of a/b
  long long y = a % b; // y = the remainder of a/b

  cout << x + y << endl;
  return 0;
}
