import sys
input = sys.stdin.readline

mod = 1000000007
n, k = map(int, input().split())
p = list(map(int, input().split()))
p.sort(reverse=True)
ans = 0
for i in range(k-1, n):
    ans = (ans + p[i] * (i+1)) % mod
print(ans)
