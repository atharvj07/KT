#include<bits/stdc++.h>
using namespace std;
vector<char>v[500];
int main()
{
  int t,n,a,b,c,d,k,x,y,z,i,j;
  char ch;
  string s;
  cin>>t;
  while(t--)
  {
    cin>>n>>a>>b;
    c=n/a;
    ch='a';
    k=0;
    for(i=1;i<=c;i++)
    {
      for(j=1;j<=a;j++)
      {
        s+=ch;
        k++;
        if(k==b) k=0,ch++;
        if(ch>'z') ch='a';
      }
    }
    while(k>0)
    {
      s+=ch;
      k++;
      if(k==b) k=0,ch++;
      if(ch>'z') ch='a';
    }
    cout<<s<<endl;
  }
  return 0;
}
