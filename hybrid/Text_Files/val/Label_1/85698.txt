#include <iostream>
#include <iomanip>

int main() {
  int h, m;
  char c;
  std::cin >> h >> c >> m;
  int mins = (60 - m) + h * 60 + 1;
  while (mins % 100 != mins / 100) {
    mins++;
  }
  std::cout << mins % 60 << std::endl;
}
