import sys
n,m,k=map(int,input().split())
a=[list(map(int,input().split()))for i in range(n)]
f=[[0]*m for i in range(n)]
ans=0
def dfs(x,y):
    global ans
    ans+=1
    f[x][y]=1
    for dx,dy in[(1,0),(0,1),(-1,0),(0,-1)]:
        nx,ny=x+dx,y+dy
        if 0<=nx<n and 0<=ny<m and a[nx][ny]==a[x][y] and not f[nx][ny]:
            dfs(nx,ny)

for x in range(n):
    for y in range(m):
        if not f[x][y]:
            dfs(x,y)
            if ans>1:
                print(-1)
                sys.exit()
            ans=0

print(sum([a[x][y]!=0 for x in range(n) for y in range(m)]))
