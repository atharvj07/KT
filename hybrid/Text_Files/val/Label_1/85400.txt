#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  vector<uint32_t> a(t);
  for (auto &x : a) cin >> x;
  for (auto x : a) cout << x + 1 << "\n";
  return 0;
}
