import sys

input = sys.stdin.readline

n,A,R,M = map(int,input().split())
h = list(map(int,input().split()))
h.sort()

ans = 0
for i in range(0,n-1):
    if h[i] < h[i+1]:
        ans += (A * (h[i+1] - h[i]))
    else:
        ans += (R * (h[i] - h[i+1]))

print(ans)
