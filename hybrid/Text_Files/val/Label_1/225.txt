from sys import stdin

n, m = map(int, stdin.readline().split())
banned = set()

for _ in range(m):
    x, y = map(int, stdin.readline().split())
    banned.add((x, y))

ans = 0

if 2 * m >= n:
    ans = 0
elif n % 2 == 0:
    ans = n // 2
else:
    ans = (n + 1) // 2

print(ans)
