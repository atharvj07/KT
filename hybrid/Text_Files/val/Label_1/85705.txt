#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, cur = 0;
    cin >> n;
    while (n--) {
        int a;
        cin >> a;
        cur += a;
        if (cur >= 360) cur %= 360;
    }
    cout << (cur == 0 ? "YES" : "NO");
}
