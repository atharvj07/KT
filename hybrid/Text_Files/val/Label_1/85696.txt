#include <bits/stdc++.h>
using namespace std;

int main() {
  string t;
  cin >> t;
  int hh = stoi(t.substr(0, 2)), mm = stoi(t.substr(3, 2)), sm = mm % 10 + 10 * (mm / 10);
  cout << (hh % 10 * 10 + hh / 10 == sm ? 0 : 60 - mm % 10) << endl;
  return 0;
}
