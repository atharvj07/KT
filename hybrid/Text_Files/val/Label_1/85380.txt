#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, k;
  cin >> n >> k;
  string s;
  cin >> s;
  while (k > n) {
    s += s;
    n *= 2;
  }
  s = s.substr(0, k);
  cout << s << endl;
  return 0;
}
