import sys

input = sys.stdin.readline
for i in range(int(input())):
    n, x, y = map(int, input().split())
    a = [1] * (n)
    p = (y - x) // (n - 1)
    for j in range(1, n):
        a[j] = a[j - 1] + p
    a[-1] = y
    print(*a)
