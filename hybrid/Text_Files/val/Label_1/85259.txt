#include <iostream>
using namespace std;
int main() {
  int a, b, c;
  cin >> a >> b >> c;
  cout << max(0, (a + b > c ? 0 : c - a - b - 1) + max(0, (b + c > a ? 0 : a - b - c - 1)) + max(0, (a + c > b ? 0 : b - a - c - 1))) << endl;
}
