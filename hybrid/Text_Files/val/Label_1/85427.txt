#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n, k, ans = 0;
    cin >> n >> k;
    while (k) {
        ans ^= (n - k);
        k /= 2;
    }
    cout << ans << endl;
    return 0;
}
