s = input().strip()
delimiters = ".,?!"
q = False
last = 0

for i in range(len(s)):
	if s[i] == '"':
		q = not q
	if s[i] == ' ' and not q:
		if i - last > 0:
			print("<" + s[last: i] + ">")
		last = i + 1

if last < len(s):
	print("<" + s[last:] + ">")
