#include <iostream>

int main() {
    size_t n;
    std::cin >> n;
    size_t l = 0, h = 1;
    while (l + h < n) {
        h++;
        l += h;
    }
    std::cout << h << std::endl;
    return 0;
}
