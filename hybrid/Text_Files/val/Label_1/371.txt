import sys
input = sys.stdin.readline
for t in range(int(input())):
    n=int(input())
    l=[0]*n;  d=0
    for i in input().split():
        if not d%2: l[d]=int(i)
        else: l[d]+=int(i)
        d^=1
    print(max(l))
