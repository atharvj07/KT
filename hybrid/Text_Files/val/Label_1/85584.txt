#include<bits/stdc++.h>
using namespace std;
int main() {
    long long n, k, t, arr[101];
    cin >> n >> k;
    for(long long i = 0; i < n; i++)
        cin >> arr[i];
    sort(arr, arr + n);
    long long ans = arr[k - 1];
    cout << ans << endl;
}
