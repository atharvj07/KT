#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, a[101], ans = 0;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
        if (a[i] == n) ans = max(ans, i);
        if (a[i] == 1) ans = max(ans, n - 1 - i);
    }
    cout << ans << endl;
}
