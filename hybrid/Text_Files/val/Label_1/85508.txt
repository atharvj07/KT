#include<bits/stdc++.h>
using namespace std;
const double eps=1e-7;
int main()
{
    int k,q;
    cin>>k>>q;
    while(q--)
    {
        int p;
        cin>>p;
        int ans=ceil(log2(1.0*k/(p-eps)));
        cout<<ans<<endl;
    }
}
