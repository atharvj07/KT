import sys

for _ in range(int(input())):
    n, q = map(int, input().split())
    st = input()
    for i in range(q):
        l, r = map(int, input().split())
        sb = st[l - 1:r]
        if sb.count("0") * sb.count("1") > 0:
            print("YES")
        else:
            print("NO")
