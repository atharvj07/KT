import sys

for _ in range(int(input())):
    n = int(input())
    s = list(input())
    t = list(input())
    if s == t:
        print("Yes")
    else:
        print('Yes' if all(a != b for a, b in zip(s, t)) else 'No')
