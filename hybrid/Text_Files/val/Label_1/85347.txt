#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin >> t;
  while (t--) {
    int n, x, y, d;
    cin >> n >> x >> y >> d;
    int ans = abs(x - y);
    if (ans % d == 0) cout << ans / d << endl;
    else cout << -1 << endl;
  }
  return 0;
}
