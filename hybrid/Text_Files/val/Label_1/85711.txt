#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
    ll n, k, a[400005];
    cin >> n;
    for (k = 0; k < n; k++) cin >> a[k];
    sort(a, a + n);
    cout << a[n / 2 - 1] - a[n / 2 - 2];
}
