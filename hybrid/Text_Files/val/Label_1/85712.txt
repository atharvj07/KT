#include <bits/stdc++.h>
using namespace std;
typedef long long LL;
int main() {
    int n;
    cin >> n;
    vector<LL> v(n);
    for (auto &x : v) cin >> x;
    sort(v.begin(), v.end());
    cout << v[n / 2] - v[n / 2 - 1] << '\n';
}
