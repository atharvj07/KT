#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef unsigned long long ull;

const ll MX = 1ULL<<31;

int main() {
    int t;
    scanf("%d", &t);
    while (t--) {
        ull a;
        scanf("%lld", &a);
        printf("%lld\n", a + 1ULL);
    }
    return 0;
}
