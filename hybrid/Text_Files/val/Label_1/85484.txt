#include<bits/stdc++.h>
using namespace std;
int main(){
    long long n,ans=0,a,b,now=0;
    cin>>n;
    for(int i=1;i<=n;i++){
        cin>>a;
        if(now>=a) now-=a;
        else{
            b=a-now;
            now=0;
            ans+=b;
        }
    }
    cout<<ans<<"\n";
    return 0;
}
