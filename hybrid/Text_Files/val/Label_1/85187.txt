#include <iostream>
using namespace std;
int main() {
  long long m, k, n, x, y;
  cin >> n >> k >> m;
  if (n == 1)
    cout << k % m;
  else if ((x = (k + (k % 2 == 1) - 1) / 2) <= 0)
    cout << 0;
  else
    cout << (m - 1 + (m - 1 % x) - 1 + m - ((m - 1 % x == 0) ? 1 : 0)) % m;
  return 0;
}
