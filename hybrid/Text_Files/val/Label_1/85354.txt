#include <bits/stdc++.h>
using namespace std;

int main() {
  int n, res = 0;
  cin >> n;
  while (n--) {
    int k, a;
    cin >> k >> a;
    res = max(res, k);
  }
  cout << res + 1 << endl;
  return 0;
}
