n, t, c = map(int, input().split())
a = list(map(int, input().split()))
ans = 0
L = R = sum = 0
while R < n:
    sum += a[R]
    while sum > t and L <= R:
        sum -= a[L]
        L += 1
    if sum <= t:
        ans += R - L + 1
    R += 1
print(ans)
