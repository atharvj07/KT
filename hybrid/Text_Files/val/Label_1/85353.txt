#include <bits/stdc++.h>
using namespace std;

int main() {
  int n;
  cin >> n;
  long long mx = 0;
  for (int i = 0; i < n; i++) {
    long long k, a;
    cin >> k >> a;
    mx = max(mx, k);
  }
  cout << mx + 1 << endl;
  return 0;
}
