n = int(input())
h = [int(x) for x in input().split()]
ans = 0
need = 0
for i in range(n):
    if h[i] >= need:
        need = h[i]
    else:
        ans += need - h[i] + 1
        need = need + 1
print(ans)
