import sys

n = int(input())
s = input()
t = s[0]
cnt = 0
for c in s[1:]:
    if c == t[-1]:
        t += ('R' if c == 'G' else 'G' if c == 'B' else 'B')
        cnt += 1
    else:
        t += c
print(cnt)
print(t)
