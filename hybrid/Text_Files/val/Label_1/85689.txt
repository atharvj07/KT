#include <iostream>
#include <vector>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> a(n);
  for (int &i : a) cin >> i;
  int l = -1, r = -1;
  for (int i = 0; i < n; i++) {
    if (a[i] == 1) l = i;
    if (a[i] == n) r = i;
  }
  cout << max(r, n - 1 - l);
}
