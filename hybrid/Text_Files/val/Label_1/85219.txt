#include <iostream>

using namespace std;

int main() {
  int n, k, ans;
  string s, t;
  cin >> n >> k >> s >> t;
  ans = (s[0] == t[0] ? k + 1 : k);
  for (int i = 1; i < n; i++) {
    if (s[i] == t[i])
      ans += k;
    else
      break;
  }
  cout << ans << endl;
}
