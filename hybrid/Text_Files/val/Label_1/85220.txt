#include <iostream>
#include <string>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    string s, t;
    cin >> s >> t;
    int ans = 2 * (n + 1);
    if (s == t) ans--;
    cout << ans << endl;
}
