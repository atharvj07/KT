#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main() {
  ll a, b, c;
  cin >> a >> b >> c;
  ll x = max({a, b, c}), y = min({a, b, c});
  cout << (x - y - 1) / 2;
  return 0;
}
