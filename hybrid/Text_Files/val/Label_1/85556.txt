#include <bits/stdc++.h>
using namespace std;

int main() {
	int n; cin >> n;
	string s, t; cin >> s >> t;
	if (s[0] == s[1] && t[0] == t[1]) {
		cout << "NO" << endl;
		return 0;
	}
	cout << "YES" << endl;
	for (int i = 0; i < n; i++) cout << "abc";
	cout << endl;
}
