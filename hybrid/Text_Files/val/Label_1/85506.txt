#include <bits/stdc++.h>
using namespace std;
int main() {
  int k, q;
  cin >> k >> q;
  while (q--) {
    int p;
    cin >> p;
    p = 1000 - p;
    int ans = log((double)(1ll << k) * (1000.0 - p) / 2000) / log(2.0) + 1;
    cout << ans << endl;
  }
}
