s = input()
t = input()
max_len = 0
last_idx = [0] * 26
for i in range(len(t)):
    max_len = max(max_len, i - last_idx[ord(t[i]) - ord('a')])
    last_idx[ord(t[i]) - ord('a')] = i + 1
print(max_len)
