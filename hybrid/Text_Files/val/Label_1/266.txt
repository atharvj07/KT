import sys

for _ in range(int(input())):
    n = int(input())
    a = sorted([int(x) for x in input().split()])
    if len(a) == 1:
        ans = 0
    else:
        ans = a[int(len(a) / 2)] - a[int(len(a) / 2) - 1]
    print(ans)
