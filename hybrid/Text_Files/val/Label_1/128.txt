mod = 998244353

def pow(a, b):
    if b == 0:
        return 1
    if b == 1:
        return a
    if b % 2 == 0:
        return pow(a * a % mod, b // 2)
    return a * pow(a * a % mod, b // 2) % mod

n = int(input())

if n % 2 == 1:
    print(0)
else:
    print(pow(2, n // 2) - 1)
