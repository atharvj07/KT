#include<bits/stdc++.h>
using namespace std;
int main(){
  long long n,a[100002],i,ans=0;
  cin>>n;
  for(i=0;i<n;i++)
  {
    cin>>a[i];
    if(i){ans=max(ans,a[i]);}
  }
  for(i=0;i<n;i++)
  {
    ans+=a[i]-ans;
  }
  cout<<ans;
}
