#include <cstdio>
#include <algorithm>
using namespace std;
int main(){
    long long n, m, k;
    scanf("%lld%lld%lld", &n, &m, &k);
    if(n*m%k!=0){
        puts("NO");
        return 0;
    }
    long long s=n*m/k;
    if(s>m){
        s=m;
    }
    long long l=1;
    while(l*s<k){
        ++l;
    }
    long long c=s;
    printf("YES\n%lld %lld\n%lld %lld\n%lld %lld\n", 0, 0, 0, s, l, s);
    return 0;
}
