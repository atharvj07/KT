import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt(), m = sc.nextInt();
        int[] p = new int[n], s = new int[n], b = new int[n];
        for (int i = 0; i < n; i++) {
            p[i] = sc.nextInt();
            s[i] = sc.nextInt();
            b[i] = sc.nextInt();
        }
        int[] inc = new int[m], pref = new int[m];
        for (int i = 0; i < m; i++) {
            inc[i] = sc.nextInt();
            pref[i] = sc.nextInt();
        }
        for (int i = 0; i < m; i++) {
            int count = 0;
            for (int j = 0; j < n; j++) {
                if (p[j] <= inc[i] && s[j] >= inc[i] && Math.abs(b[j] - pref[i]) <= inc[i] - p[j]) {
                    count++;
                }
            }
            System.out.print(count + " ");
        }
        System.out.println();
    }
}
