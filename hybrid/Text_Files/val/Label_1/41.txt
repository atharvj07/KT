import sys

for q in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    if len(set(a)) < 4 or (sum(a) % 4 != 0):
        sys.stdout.write('NO\n')
    else:
        sys.stdout.write('YES\n')
