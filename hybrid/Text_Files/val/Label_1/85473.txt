#include <iostream>

int main() {
  int n;
  std::cin >> n;
  for (int i = 1, a = 1, b = 1; i <= n; ++i) {
    if (a <= n) {
      std::cout << 'O';
      a += b;
      b = a - b;
    } else {
      std::cout << 'o';
    }
  }
  return 0;
}
