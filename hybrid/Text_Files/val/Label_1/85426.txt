#include <bits/stdc++.h>
using namespace std;

int main() {
  long long n, k;
  cin >> n >> k;
  if (k % 2 == 0) cout << 0 << endl;
  else if (n % 2 == 0) cout << k / 2 << endl;
  else cout << k / 2 + 1 << endl;
}
