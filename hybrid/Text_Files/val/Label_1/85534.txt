#include <bits/stdc++.h>
using namespace std;
int main() {
  int n;
  cin >> n;
  vector<int> v(n), cnt(150002);
  for (auto& x : v) {
    cin >> x;
    cnt[x]++;
  }
  int ans = 0;
  for (int i = 0; i <= 150001; i++)
    ans = max(ans, cnt[i] + cnt[i + 1]);
  cout << ans << endl;
  return 0;
}
