#include <algorithm>
#include <iostream>
#include <limits>

int main() {
  int n;
  std::cin >> n;
  int x[n];
  for (int i = 0; i < n; i++) { std::cin >> x[i]; }
  std::sort(x, x + n);
  std::cout << x[n / 2] - x[n / 2 - 1] << std::endl;
  return 0;
}
