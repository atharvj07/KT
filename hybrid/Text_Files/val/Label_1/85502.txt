#include <bits/stdc++.h>
using namespace std;
int n;
int main() {
  cin >> n;
  vector<int> a(n);
  for (auto &x : a) { cin >> x; }
  sort(a.begin(), a.end());
  cout << ((a[0] == a[n - 1]) ? "Agasa" : "Conan") << endl;
}
