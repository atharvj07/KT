#include <iostream>
#include <vector>

int main() {
    int n;
    std::cin >> n;
    std::vector<long long int> a(n);
    long long int sum = 0;
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
        sum += a[i];
    }
    std::cout << (sum - n + 1) << std::endl;
    return 0;
}
