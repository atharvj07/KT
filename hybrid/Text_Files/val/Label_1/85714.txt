#include <iostream>
#include <algorithm>
using namespace std;
int main() {
  int n, i, x[200000];
  cin >> n;
  for (i = 0; i < n; i++) { cin >> x[i]; }
  sort(x, x + n);
  cout << x[n / 2] - x[n / 2 - 1];
}
