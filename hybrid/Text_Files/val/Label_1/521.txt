import sys
input = sys.stdin.readline

t = int(input())
for _ in range(t):
    n, x, y = map(int, input().split())
    d = (y - x) // (n - 1)
    for i in range(x, y + d, d):
        print(i, end=' ')
    print()
