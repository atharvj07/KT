from sys import stdin
input = stdin.readline

t=int(input().strip())
for _ in range(t):
    x,y,p,q=map(int,input().strip().split())
    num=x//p*q
    den=y//p*q
    if num*q!=den*p: print(-1)
    else: print(den-y)
