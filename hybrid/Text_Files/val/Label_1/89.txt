import sys

n = int(input())

h = sorted(list(map(int, input().split())))
b = list(map(int, input().split()))

mx = {}

i = n-1
j = n
while i >= 0:
    while h[i] < h[j - 1]:
        j -= 1
    mx[h[i]] = b[j - 1]
    i -= 1

ans = 0
for x in h:
    ans += mx[x]

print(ans)
