#include<bits/stdc++.h>
using namespace std;
int main(){
    double h,d,c,n,hh,mm;
    cin >> hh >> mm >> h >> d >> c >> n;
    double ans = ((h + (24 * 60 - hh * 60 - mm) * d) / n);
    cin.ignore();
    cout << ceil(ans * (hh < 20 ? c : 1.2 * c)) << endl; 
}
