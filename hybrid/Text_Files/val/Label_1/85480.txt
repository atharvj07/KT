#include <iostream>

int main() {
  long long n, h, ans = 0, add = 0, x;
  std::cin >> n;
  for (int i = 0; i < n; ++i) {
    std::cin >> h;
    ans += add < h ? h - add++ : 0;
  }
  std::cout << ans << "\n";
}
