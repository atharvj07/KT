#include <bits/stdc++.h>
using namespace std;
int main() {
  long n;
  cin >> n;
  long count = 0, mini = 1e9;
  for (long i = 0; i < n; i++) {
    long x;
    cin >> x;
    mini = min(x, mini);
    if (x == mini) count++;
  }
  cout << count << endl;
  return 0;
}
