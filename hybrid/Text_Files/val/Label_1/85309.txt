#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
  ll a, b;
  cin >> a >> b;
  if (a == 0 || b == 0)
    cout << -a * a + b * b << endl;
  else
    cout << a * a - b * b << endl;
  while (a--) cout << "o";
  while (b--) cout << "x";
  return 0;
}
