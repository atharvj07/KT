#include <iostream>

int main() {
  int t;
  std::cin >> t;
  while (t--) {
    long long int n, m;
    std::cin >> n >> m;
    int mex = 0;
    while ((n | mex) <= m) mex++;
    std::cout << mex << '\n';
  }
}
