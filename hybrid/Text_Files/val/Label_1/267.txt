from sys import stdin
input=stdin.readline
for __ in range(int(input())):
    n=int(input())
    a=list(sorted(int(x) for x in input().split()))
    x,y=a[n//2-1],a[n//2]
    print(min(x-a[0],a[-1]-y))
