#include <iostream>
#include <vector>

using namespace std;

int main() {
  int t;
  cin >> t;
  vector<int> res(t);
  for (int i = 0; i < t; i++) {
    long long a;
    cin >> a;
    a++;
    res[i] = a;
  }
  for (int i = 0; i < t; i++) { cout << res[i] << endl; }
  return 0;
}
