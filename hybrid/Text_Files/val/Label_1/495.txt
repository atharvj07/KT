import sys

for t in range(int(input())):
    n = int(input())
    a = list(map(int, input().split()))
    ans = 0
    last = 1
    for i in a:
        if i == last:
            last += 1
        else:
            ans += 1
    print(ans)
