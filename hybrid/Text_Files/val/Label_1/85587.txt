#include<bits/stdc++.h>
#define int long long
using namespace std;
inline int getsum(int x){int sum=0;while(x){sum+=x%10;x/=10;}return sum;}
int32_t main(){ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);int x;cin>>x;int y=(int)sqrt(x+0.5);if(y*y!=x){cout<<-1<<endl;return 0;}int sum=getsum(y);if(sum*sum+sum*y-x){cout<<-1<<endl;return 0;}cout<<y<<endl;return 0;}
