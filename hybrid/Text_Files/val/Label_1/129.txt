import sys

MOD = int(998244353)

def add(x: int, y: int) -> int:
    return (x + y) % MOD

def mul(x: int, y: int) -> int:
    return (x * y) % MOD

n = int(sys.stdin.readline())

if n % 2 == 0:
    ans = pow(2, n // 2 - 1, MOD)
else:
    ans = 0

print(ans)
