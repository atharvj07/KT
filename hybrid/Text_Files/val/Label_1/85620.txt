#include <iostream>
#include <vector>
using namespace std;

int main() {
  int n; cin >> n;
  int last=0;
  bool ok=true;
  for(int i=0; i<n && ok; i++) {
    int a; cin >> a;
    if(a) {
      if(last) ok=false;
      last=i+1;
    }
  }
  cout << (ok?"YES":"NO") << '\n';
}
