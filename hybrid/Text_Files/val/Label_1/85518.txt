#include <iostream>
#include <string>
using namespace std;
#define rep(i, a, n) for (int i = a; i < n; i++)
const int MAX_N = 2000 + 5;
char c[MAX_N];
int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, a, b;
        cin >> n >> a >> b;
        int idx = 0;
        char d = 'a';
        rep(i, 0, a - 1) { c[i] = d++; }
        int len = n / a * a;
        rep(i, a - 1, len) { c[i] = c[i - (a - 1)]; }
        int rem = n - len;
        rep(i, 0, rem) { c[len + i] = c[len + i - 1] + 1; }
        rep(i, 0, n) { cout << c[i]; }
        cout << "\n";
    }
    return 0;
}
