#include<iostream>
using namespace std;
int main()
{
    int x;
    while(cin >> x)
    {
        if(x==1 || x==3 || x==5 || x==7 || x==9 || x==11 || x==13 || x==15)
            cout << 1 << "\n";
        else
            cout << 0 << "\n";
    }
    return 0;
}
