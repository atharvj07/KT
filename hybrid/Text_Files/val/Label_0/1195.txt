import math
for i in range(int(input())):
	(x, y) = map(int, input().split())
	if x > y:
		y = y * math.ceil(x / y)
		print(math.ceil(y / x))
	elif y > x:
		print(math.ceil(y / x))
	else:
		print('1')
