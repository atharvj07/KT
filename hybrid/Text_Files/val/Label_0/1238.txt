def f(a, c):
	if a % c > c - a % c:
		return c - a % c
	else:
		return a % c
(x, y, z) = map(int, input().split())
if x // z + y // z == (x + y) // z:
	r = 0
else:
	rx = f(x, z)
	ry = f(y, z)
	if rx < ry:
		r = rx
	else:
		r = ry
print((x + y) // z, r)
