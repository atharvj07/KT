import java.io.*;
import java.math.*;
import java.util.*;
public class B {
	static class FastReader {
        BufferedReader br;
        StringTokenizer st;

        public FastReader() {
            br = new BufferedReader(new
                    InputStreamReader(System.in));
        }

        String next() {
            while (st == null || !st.hasMoreElements()) {
                try {
                    st = new StringTokenizer(br.readLine());
                } catch (IOException e) {
                    e.printStackTrace();
                }
            }
            return st.nextToken();
        }

        int nextInt() {
            return Integer.parseInt(next());
        }

        long nextLong() {
            return Long.parseLong(next());
        }

        double nextDouble() {
            return Double.parseDouble(next());
        }

        String nextLine() {
            String str = "";
            try {
                str = br.readLine();
            } catch (IOException e) {
                e.printStackTrace();
            }
            return str;
        }
    }

    static FastReader s = new FastReader();
    static PrintWriter out = new PrintWriter(System.out);

    private static int[] rai(int n) {
        int[] arr = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = s.nextInt();
        }
        return arr;
    }

    private static int[][] rai(int n, int m) {
        int[][] arr = new int[n][m];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                arr[i][j] = s.nextInt();
            }
        }
        return arr;
    }

    private static long[] ral(int n) {
        long[] arr = new long[n];
        for (int i = 0; i < n; i++) {
            arr[i] = s.nextLong();
        }
        return arr;
    }

    private static long[][] ral(int n, int m) {
        long[][] arr = new long[n][m];
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                arr[i][j] = s.nextLong();
            }
        }
        return arr;
    }

    private static int ri() {
        return s.nextInt();
    }

    private static long rl() {
        return s.nextLong();
    }

    private static String rs() {
        return s.next();
    }
    static void merge(int arr[],int p,int q,int r)
    {
    	int i,j,k;
    	int L[]=new int[q-p+2];
    	int R[]=new int[r-q+1];
    	L[q-p+1]=Integer.MAX_VALUE;
    	R[r-q]=Integer.MAX_VALUE;
    	for(i=0;i<=q-p;i++)
    		L[i]=arr[p+i];
    	for(i=0;i<r-q;i++)
    		R[i]=arr[q+1+i];
    	i=0;
    	j=0;
    	for(k=p;k<=r;k++)
    	{
    		if(L[i]<R[j])
    		{
    			arr[k]=L[i];
    			i++;
    		}
    		else
    		{
    			arr[k]=R[j];
    			j++;
    		}
    	}
    }
    static void mergesort(int arr[],int p,int r)
    {
    	if(p<r)
    	{
    		int q=(p+r)/2;
    		mergesort(arr,p,q);
    		mergesort(arr,q+1,r);
    		merge(arr,p,q,r);
    	}
    }
    static int binarySearch(int arr[],int key,int l,int u)
    {
    	int mid=(l+u)/2;
    	if(l>u)
    		return -1;
    	else if(arr[mid]==key)
    		return 1;
    	else if(arr[mid]<key)
    		return binarySearch(arr,key,mid+1,u);
    	else
    		return binarySearch(arr,key,l,mid-1);
    }
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		FastReader sc=new FastReader();
		int t,n,i,j,k,c=0,cons=0,sum1=0,sum2=0,p=0,q=0,s1,s2,a,b,fg=0,min=Integer.MAX_VALUE,max=0,avg;
		String str;
		long sum=0,x,y,s=0;
		BigInteger f=BigInteger.valueOf(1);
		t=sc.nextInt();
		while(t-->0)
		{
			n=sc.nextInt();
			int arr[]=new int[n];
			for(i=0;i<n;i++)
				arr[i]=sc.nextInt();
			for(i=1;i<n-1;i++)
			{
				if((arr[i]>arr[i-1]&&arr[i]>arr[i+1])||(arr[i]<arr[i-1]&&arr[i]<arr[i+1]))
				{
					c++;
					cons++;
				}
				else
				{
					max=Math.max(cons, max);
					cons=0;
				}
			}
			max=Math.max(cons, max);
			if(max>3)
				max=3;
			if(max==2)
			{
				max=0;
				cons=0;
				for(i=1;i<n-1;i++)
				{
					if((arr[i]>arr[i-1]&&arr[i]>arr[i+1])||(arr[i]<arr[i-1]&&arr[i]<arr[i+1]))
					{
						cons++;
						if(cons==2)
						{
							q++;
							if((i-3>=0&&i+2<n)&&arr[i]>arr[i-1]&&arr[i]>arr[i-2]&&arr[i-2]<arr[i-3]&&arr[i-1]<arr[i+1]&&arr[i+1]>arr[i+2])
								p++;
							else if((i-3>=0&&i+2<n)&&arr[i]<arr[i-1]&&arr[i]<arr[i-2]&&arr[i-2]>arr[i-3]&&arr[i-1]>arr[i+1]&&arr[i+1]<arr[i+2])
								p++;
						}
					}
					else
					{
						max=Math.max(cons, max);
						cons=0;
					}
				}
				max=Math.max(cons, max);
				if(p==q)
					max=1;
				else
					max=2;
			}
			System.out.println(c-max);
			c=0;
			cons=0;
			p=0;
			q=0;
			max=0;
		}
	}
}
