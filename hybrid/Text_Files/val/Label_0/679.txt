t = int(input())
for _ in range(t):
	(n, k) = map(int, input().split())
	if n % 2 == 0:
		if k % n != 0:
			print(k % n)
		else:
			print(n)
	else:
		a = n // 2
		b = k + (k - 1) // a
		if b % n != 0:
			print(b % n)
		else:
			print(n)
