#include<bits/stdc++.h>
using namespace std;

const int maxn=300000+10;
long long n,a[maxn],ans=0;

int main()
{
	scanf("%lld",&n);
	for(int i=1;i<=3*n;i++) scanf("%lld",&a[i]);
	sort(a+1,a+1+3*n);
	long long mid=n;
	for(int i=(3*n)-1;i>=mid+1;i-=2) ans+=a[i];
	printf("%lld\n",ans);
}