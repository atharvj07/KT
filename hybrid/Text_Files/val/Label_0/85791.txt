#include <bits/stdc++.h>
const int N = 4e5 + 5;
using namespace std;
int get_second(pair<int, int> i) { return i.second; }
int trail_zero(int n) {
  while (n % 10 == 0) n /= 10;
  return n;
}
map<string, long long int> mx;
map<long long int, long long int> my;
map<int, pair<int, int>> m1;
vector<pair<string, int>> v;
vector<int> ans;
char s111[N];
long long int p2[N], p3[N];
long long int a[N], n1[N], b[N];
long long int n, S;
int dx[] = {0, -1, 0, 1};
int dy[] = {1, 0, -1, 0};
int ax[] = {0, 1, 2, 3, 4};
char s1[500][500];
int main() {
  int n;
  cin >> n;
  if (!(n % 2)) return cout << -1, 0;
  for (int i = 0; i < n; i++) cout << i << " ";
  cout << endl;
  for (int i = 0; i < n; i++) cout << i << " ";
  cout << endl;
  for (int i = 0; i < n; i++) cout << (i + i) % n << " ";
}
