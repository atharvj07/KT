(n, k) = map(int, input().split())
s = input()
x = ''
for i in range(n):
	if i == 0:
		x += s[i]
		r = k // n + 1
		q = (s * r)[:k]
	else:
		r = k // len(x) + 1
		p = (x * r)[:k]
		if p < q:
			break
		x += s[i]
r = k // len(x) + 1
print((x * r)[:k])
