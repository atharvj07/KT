n = int(input())
string = input()

def min(a, b):
	return a if a < b else b
cur = ''
ans = n
for i in range(len(string)):
	if string.startswith(cur + cur):
		ans = min(ans, n - i + 1)
	cur += string[i]
print(ans)
