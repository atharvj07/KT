#include <iostream>
#include <algorithm>
using namespace std;

int day(int x, int y) {
    return (x + y - 1) / y;
}

int L, A, B, C, D;
int main() {
    cin >> L >> A >> B >> C >> D;
    cout << L - max(day(A, C), day(B, D)) << endl;
    return 0;
}