import sys
input = sys.stdin.readline
t = int(input())
for tests in range(t):
	(n, k) = map(int, input().split())
	if n % 2 == 0:
		print((k - 1) % n + 1)
		continue
	ch = n // 2
	print((k - 1 + (k - 1) // ch) % n + 1)
