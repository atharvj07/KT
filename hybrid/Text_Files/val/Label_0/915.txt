(n, k) = [int(x) for x in input().split()]
a = [int(x) for x in input().split()]
sa = set(a)
if 0 in sa:
	sa.remove(0)
lsa = list(sa)
lsa.sort()
ds = 0
for i in range(k):
	if i >= len(lsa):
		print(0)
	else:
		print(lsa[i] - ds)
		ds = lsa[i]
