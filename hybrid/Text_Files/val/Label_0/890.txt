(n, p, q) = map(int, input().split())
s = input()
subs = []
temp = s
while n > 0 and p <= n:
	if n % q == 0:
		break
	subs.append(s[:p])
	s = s[p:]
	n = len(s)
while n > 0 and q <= n:
	subs.append(s[:q])
	s = s[q:]
	n = len(s)
if n == 0:
	print(len(subs))
	for i in range(len(subs)):
		print(subs[i])
else:
	print('-1')
