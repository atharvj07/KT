import java.io.*; 
import java.math.BigInteger;
import java.util.*;
  
public class Main {
	
    static class FastScanner
    { 
        BufferedReader br; 
        StringTokenizer st; 
  
        public FastScanner() 
        { 
            br = new BufferedReader(new
                     InputStreamReader(System.in)); 
        } 
  
        String next() 
        { 
            while (st == null || !st.hasMoreElements()) 
            { 
                try
                { 
                    st = new StringTokenizer(br.readLine()); 
                } 
                catch (IOException  e) 
                { 
                    e.printStackTrace(); 
                } 
            } 
            return st.nextToken(); 
        } 
  
        int nextInt() 
        { 
            return Integer.parseInt(next()); 
        } 
  
        long nextLong() 
        { 
            return Long.parseLong(next()); 
        } 
  
        double nextDouble() 
        { 
            return Double.parseDouble(next()); 
        } 
  
        String nextLine() 
        { 
            String str = ""; 
            try
            { 
                str = br.readLine(); 
            } 
            catch (IOException e) 
            { 
                e.printStackTrace(); 
            } 
            return str; 
        } 
    } 
  
    public static void main(String[] args) throws IOException {
        FastScanner sc=new FastScanner();
        int N = sc.nextInt();
        int[][] grid = new int[N+1][N+1];
        grid[1][1] = 1;
        grid[N][N] = 0;
        for (int j = 1; j <= N-2; j+=2) {
        	System.out.println("? " + 1 + " " + j + " " + 1 + " " + (j+2));
        	System.out.flush();
        	int res = sc.nextInt();
        	if (res == -1)
        		return;
        	if (res == 1) {
        		grid[1][j+2] = grid[1][j];
        	} else {
        		grid[1][j+2] = (grid[1][j]+1)%2;
        	}
        }
        
        for (int i = 1; i <= N-2; i += 2) {
        	for (int j = 1; j <= N; j += 2) {
            	System.out.println("? " + i + " " + j + " " + (i+2) + " " + j);
            	System.out.flush();
            	int res = sc.nextInt();
	        	if (res == -1)
	        		return;
            	if (res == 1) {
            		grid[i+2][j] = grid[i][j];
            	} else {
            		grid[i+2][j] = (grid[i][j]+1)%2;
            	}
            	
            	if (j < N) {
                	System.out.println("? " + i + " " + j + " " + (i+1) + " " + (j+1));
                	System.out.flush();
                	res = sc.nextInt();
		        	if (res == -1)
		        		return;
                	if (res == 1) {
                		grid[i+1][j+1] = grid[i][j];
                	} else {
                		grid[i+1][j+1] = (grid[i][j]+1)%2;
                	}
            	}
        	}
        }
        
        //Arbitrary Decision: Set grid[1][2] = 0 (could be 1). Check later.
        grid[1][2] = 0;
        for (int j = 2; j <= N-3; j+=2) {
        	System.out.println("? " + 1 + " " + j + " " + 1 + " " + (j+2));
        	System.out.flush();
        	int res = sc.nextInt();
        	if (res == -1)
        		return;
        	if (res == 1) {
        		grid[1][j+2] = grid[1][j];
        	} else {
        		grid[1][j+2] = (grid[1][j]+1)%2;
        	}
        }
        for (int i = 1; i <= N-2; i += 2) {
        	for (int j = 2; j <= N-1; j += 2) {
            	System.out.println("? " + i + " " + j + " " + (i+2) + " " + j);
            	System.out.flush();
            	int res = sc.nextInt();
	        	if (res == -1)
	        		return;
            	if (res == 1) {
            		grid[i+2][j] = grid[i][j];
            	} else {
            		grid[i+2][j] = (grid[i][j]+1)%2;
            	}
            	
            	System.out.println("? " + i + " " + j + " " + (i+1) + " " + (j+1));
            	System.out.flush();
            	res = sc.nextInt();
	        	if (res == -1)
	        		return;
            	if (res == 1) {
            		grid[i+1][j+1] = grid[i][j];
            	} else {
            		grid[i+1][j+1] = (grid[i][j]+1)%2;
            	}
        	}
        }
        
        for (int i = 2; i <= N-1; i+=2) {
        	System.out.println("? " + i + " " + 1 + " " + (i+1) + " " + 2);
        	System.out.flush();
        	int res = sc.nextInt();
        	if (res == -1)
        		return;
        	if (res == 1) {
        		grid[i][1] = grid[i+1][2];
        	} else {
        		grid[i][1] = (grid[i+1][2]+1)%2;
        	}
        }
        
        //Find a position to consider
        
        int val = -1;
        
        palindromeSearch:
        for (int i = 1; i <= N; i++) {
        	for (int j = 1; j <= N; j++) {
        		int[] moves = new int[4];
        		moves[0] = grid[i][j];
        		for (int m1 = 0; m1 < 2; m1++) {
        			for (int m2 = 0; m2 < 2; m2++) {
        				for (int m3 = 0; m3 < 2; m3++) {
        					int down = (1-m1) + (1-m2) + (1-m3);
        					int right = m1 + m2 + m3;
        					if (i + down > N || j + right > N) {
        						continue;
        					}
        					moves[1] = grid[i+(1-m1)][j+m1];
        					moves[2] = grid[i+(1-m1)+(1-m2)][j+m1+m2];
        					moves[3] = grid[i+down][j+right];
        					
        					if ((moves[0] == moves[3] && moves[1] == moves[2])) {
        						//4-Palindrome!
        			        	System.out.println("? " + i + " " + j + " " + (i+down) + " " + (j+right));
        			        	System.out.flush();
        			        	int res = sc.nextInt();
        			        	if (res == -1)
        			        		return;
        			        	if (res == 1) {
        			        		val = 0;
        			        	} else {
        			        		val = 1;
        			        	}
        			        	break palindromeSearch;
        					}
        					
        					if ((moves[0] != moves[3] && moves[1] != moves[2])) {
        			        	System.out.println("? " + i + " " + j + " " + (i+down) + " " + (j+right));
        			        	System.out.flush();
        			        	int res = sc.nextInt();
        			        	if (res == -1)
        			        		return;
        			        	if (res == 1) {
        			        		val = 1;
        			        	} else {
        			        		val = 0;
        			        	}
        			        	break palindromeSearch;
        					}
        				}
        			}
        		}
        	}
        }
        if (val == 1) {
        	//You made the wrong guess for grid[1][2].
        	for (int i = 1; i <= N; i++) {
        		for (int j = 1; j <= N; j++) {
        			if ((i+j)%2 == 1) {
        				grid[i][j] = 1-grid[i][j];
        			}
        		}
        	}
        }
        
        System.out.println("!");
        for (int i = 1; i <= N; i++) {
        	StringBuilder s = new StringBuilder();
        	for (int j = 1; j <= N; j++) {
        		s.append(grid[i][j]);
        	}
        	System.out.println(s);
        }
    }
}