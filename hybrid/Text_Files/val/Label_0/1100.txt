import math
for _ in range(int(input())):
	n = int(input())
	a = list(map(int, input().split()))
	cnt = 0
	mp = {}
	for i in a:
		x = math.floor(math.log2(i))
		if x in mp.keys():
			mp[x] += 1
		else:
			mp[x] = 1
	for i in mp:
		cnt += mp[i] * (mp[i] - 1) // 2
	print(cnt)
