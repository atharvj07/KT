#include <bits/stdc++.h>
using namespace std;
bool compare(const pair<string, long long int> &a,
             const pair<string, long long int> &b) {
  if (a.second > b.second)
    return true;
  else
    return false;
}
int main() {
  long long int n, t, m, k, X, Y, D, S;
  string s1, s2, s;
  cin >> n;
  int a[n][2];
  for (int(i) = 0; (i) < (int)(n); ++(i)) cin >> a[i][0];
  for (int(i) = 0; (i) < (int)(n); ++(i)) cin >> a[i][1];
  long long int x;
  double count = 0;
  for (long long int i = 0; i < 100000; i++) {
    for (int(j) = 0; (j) < (int)(n); ++(j)) {
      x = i % a[j][0];
      if (x == a[j][1]) {
        count++;
        break;
      }
    }
  }
  double ans = count / 100000.0;
  cout << ans;
  return 0;
}
