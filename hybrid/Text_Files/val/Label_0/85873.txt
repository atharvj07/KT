#include <bits/stdc++.h>

using namespace std;

int n,A[500010];
int main()
{
	scanf("%d",&n);
	for(int i=1;i<=n*3;i++) scanf("%d",&A[i]);
	sort(A+1,A+1+3*n);
	long long ans=0;
	for(int i=3*n;i>n;i-=2)
		ans+=A[i-1];
	cout<< ans <<endl;
	return 0;
} 