#include <cstdio>

using namespace std;

int main()
{
	int day, a, b, c, d;
	
	scanf("%d %d %d %d %d", &day, &a, &b, &c, &d);
	
	while(a > 0 || b > 0){
		
		a -= c;
		b -= d;
		day--;
	}
	
	printf("%d\n", day);
	
	return (0);
}