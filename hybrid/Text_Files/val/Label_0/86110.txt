#include <bits/stdc++.h>
using namespace std;
int main() {
  int n, m;
  cin >> n >> m;
  int rem = n % m;
  int temp = n / m;
  for (int i = 0; i < rem; i++) cout << temp + 1 << " ";
  for (int i = rem; i < m; i++) cout << temp << " ";
  return 0;
}
