#include<bits/stdc++.h>
using namespace std;
int main()
{
  int n,k;
  cin >> n >> k;
  int h[n],c[n],i,j;
  
  for(i=0;i<n;i++)
    cin >> h[i];
  
    c[0]=0;
  
  for(i=1;i<n;i++)
  { 
    c[i]=INT_MAX;
    for(j=i-1;j>=i-k&&j>=0;j--)
    c[i]=min(abs(h[i]-h[j])+c[j],c[i]);
  }
  cout<<c[n-1];
}