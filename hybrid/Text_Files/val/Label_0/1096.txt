from collections import defaultdict
for _ in range(int(input())):
	n = int(input())
	a = [len(bin(int(i))[2:]) for i in input().split()]
	cnt = [0] * (max(a) + 1)
	for i in a:
		cnt[i] += 1
	ans = 0
	for i in cnt:
		ans += i * (i - 1) // 2
	print(ans)
