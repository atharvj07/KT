#include <bits/stdc++.h>
long long int n, m, a, b;
int main() {
  std::cin >> n >> m >> a >> b;
  long long int res1 = n % m * b;
  long long int res2 = ((n / m + 1) * m - n) * a;
  std::cout << std::min(res1, res2) << std::endl;
  return 0;
}
