#include <iostream>

using namespace std;

int main() {
  for (;;) {
    int sum;
    cin >> sum;
    if (!sum) return 0;
    for (int i = 0; i < 9; i++) {
      int p;
      cin >> p;
      sum -= p;
    }
    cout << sum << endl;
  }
}