t = int(input())
for _ in range(t):
	s = input()
	cnt = dict()
	c = 0
	for i in range(len(s)):
		if s[i] == '+':
			c += 1
		else:
			c -= 1
		if c not in cnt.keys():
			cnt[c] = i
	res = 0
	for init in range(-min(cnt.keys())):
		res += cnt[-init - 1] + 1
	res += len(s)
	print(res)
