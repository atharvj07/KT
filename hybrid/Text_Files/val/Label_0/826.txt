t = int(input())
import math
for _ in range(t):
	(n, x) = map(int, input().split(' '))
	a = list(map(int, input().split(' ')))
	if x in a:
		print(1)
		continue
	ma = max(a)
	lj = x / ma
	if lj < 1:
		print(2)
		continue
	print(math.ceil(lj))
