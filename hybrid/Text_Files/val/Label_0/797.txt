def checker(s):
	result = len(s)
	(cur, mx) = (0, 0)
	for i in range(len(s)):
		if s[i] == '+':
			cur += 1
		else:
			cur -= 1
		if mx > cur:
			mx = cur
			result += i + 1
	return result
for _ in range(int(input())):
	t = input()
	print(checker(t))
