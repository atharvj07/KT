#include <bits/stdc++.h>
using namespace std;
#define int long long
 
 signed main() {
 		int n,m,j=0,i=0,mx=0;
 		cin >>n>>m;
 		vector<int>v,v1;
 		string s,s1;
 		cin >>s>>s1;
 		while(i<m && j<n){
 			if(s[j]!=s1[i])
 			j++;
 			else{
 				v.push_back(j);
 				j++;
 				i++;}
 			if(i==m)
 			break;
 		}
 		i=m-1;
 		j=n-1;
 		while(i>=0 && j>=0){
 			if(s[j]!=s1[i])
 			j--;
 			else{
 				v1.push_back(j);
 				j--;
 				i--;}
 			if(i<0)
 			break;
 		}
 		reverse(v1.begin(),v1.end());
 		/*for(int i=0;i<v.size();i++)
 		cout<<v[i];
 		cout<<endl;
 		for(int i=0;i<v1.size();i++)
 		cout<<v1[i];
 		cout<<endl;*/
 		for(int i=1;i<v.size();i++){
 			mx=max(mx,v1[i]-v[i-1]);
 			//mx=max(m,v[i]-v1[i-1]);
 		}
 		cout<<mx;
	return 0;
}