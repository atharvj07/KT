for _ in range(int(input())):
	N = int(input())
	array = [int(x) for x in input().split()]
	while array[0] == 0:
		del array[0]
	while array[-1] == 0:
		del array[-1]
	N = len(array)
	counter = 0
	for i in array:
		if i == 0:
			counter += 1
	print(counter)
