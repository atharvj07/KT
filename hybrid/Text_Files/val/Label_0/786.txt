def main():
	n = int(input())
	array = sorted([int(x) for x in input().split()])
	(mx, sm) = (array[0], array[0])
	for i in range(1, n):
		cnt = array[i] - sm
		sm += cnt
		mx = max(mx, cnt)
	print(mx)
tests = int(input())
while tests > 0:
	tests -= 1
	main()
