#include <bits/stdc++.h>
using namespace std;
int main() {
  int m, n;
  cin >> n >> m;
  string a = "";
  bool l = 0;
  int b, aa;
  for (int j = 0; j < m; j++) cin >> b >> aa;
  for (int i = 0; i < n; i++) {
    if (l)
      a += "1";
    else
      a += "0";
    l = l ^ 1;
  }
  cout << a << endl;
}
