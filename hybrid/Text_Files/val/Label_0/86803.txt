import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
import java.util.StringTokenizer;

/**
 * Created by nischal.k on 02/10/16.
 */
public class Main {
    public static void main(String[] args) throws java.lang.Exception {
        Reader.init(System.in);
//        int t = Reader.nextInt();
//        while ((t--)>=0) {
//            String org = Reader.next();
//            String transform = Reader.next();
//            int dp[][] = new int[org.length()][transform.length()];
//            for (int i = 0; i < org.length(); i++) {
//                for (int j = 0; j < transform.length(); j++) {
//                    if (org.charAt(i) == transform.charAt(j)) {
//                        if (i >= 1 && j >= 1) {
//                            dp[i][j] = dp[i - 1][j - 1] + 1;
//                        } else {
//                            dp[i][j]=1;
//                        }
//                    } else {
//                        dp[i][j] = maxxer(dp, i, j);
//                    }
//                }
//            }
//            System.out.println(dp[org.length() - 1][transform.length() - 1]);
//        }
        int n,m;
        n = Reader.nextInt();
        m = Reader.nextInt();
        int[] ar= new int[105];
        List<Pair> data = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            int a = Reader.nextInt();
            int b = Reader.nextInt();
            data.add(new Pair(a,b));
        }
//        Comparator<Pair> comparator = (Comparator<Pair>) (pair, t1) -> {
//            if(pair.getFirst() ==t1.getFirst()) {
//                return pair.getSecond() - t1.getSecond();
//            }
//            return pair.getFirst() - t1.getFirst();
//        };
//        data.sort(comparator);
        boolean flag = true;

        for (int i = 0; i < data.size(); i++) {
            for (int j = data.get(i).getFirst(); j < data.get(i).getSecond(); j++) {
                ar[j]=1;
            }
            if(ar[data.get(i).getSecond()]==0) {
                ar[data.get(i).getSecond()]=99;
            }
        }
        for (int i = 0; i < m; i++) {
            if(ar[i]==0 || ar[i]==99) {
                flag= false;
                break;
            }
        }
        if(ar[m]<1) {
            flag=false;
        }
//        int i = 0,j=0,k=0;
//        while (data.get(0).getFirst()==0 && true) {
//            k=data.get(i).getSecond();
//            if(k>=m) {
//                flag= true;
//                break;
//            }
//            int max = i;
//            for (int l = 0; l < data.size(); l++) {
//                if(data.get(l).getFirst()<=k && max < data.get(l).getSecond()) {
//                    max = l;
//                }
//            }
//            if(i==max) {
//                break;
//            }
//            i=max;
//        }

        if(flag) {
            System.out.println("YES");
        }else {
            System.out.println("NO");

        }
    }
//        int n = Reader.nextInt();
//        int m = Reader.nextInt();

    static int maxxer(int[][] dp, int i, int j) {
        int min = -1;
        if(i-1>=0) {
            min = Math.max(min, dp[i-1][j]);
        }
        if(j-1>=0) {
            min = Math.max(min, dp[i][j-1]);
        }
        return min;
    }

}
class Pair {
    int first;
    int second;

    public Pair(int first, int second) {
        this.first = first;
        this.second = second;
    }

    public int getFirst() {
        return first;
    }

    public int getSecond() {
        return second;
    }
}
class Node {
    int start;
    int end;
}
class Reader {
    static BufferedReader reader;
    static StringTokenizer tokenizer;

    /**
     * call this method to initialize reader for InputStream
     */
    static void init(InputStream input) {
        reader = new BufferedReader(
                new InputStreamReader(input));
        tokenizer = new StringTokenizer("");

    }

    /**
     * get next word
     */
    static String next() throws IOException {
        while (!tokenizer.hasMoreTokens()) {
//            TODO add check for eof if necessary
            tokenizer = new StringTokenizer(
                    reader.readLine());
        }
        return tokenizer.nextToken();
    }

    static long nextLong() throws IOException {
        return Long.parseLong(next());
    }

    static int nextInt() throws IOException {
        return Integer.parseInt(next());
    }

    static double nextDouble() throws IOException {
        return Double.parseDouble(next());
    }
}

