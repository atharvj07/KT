for _ in range(int(input())):
	(n, k) = [int(x) for x in input().split()]
	if n % 2 == 0:
		if k % n == 0:
			print(n)
		else:
			print(k % n)
	else:
		m = n // 2
		add = (k - 1) // m
		k += add
		if k > n:
			if k % n == 0:
				print(n)
			else:
				print(k % n)
		else:
			print(k)
