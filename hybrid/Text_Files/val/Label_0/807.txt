(n, v) = map(int, input().split())
N = 3003
t = [0] * N
for i in range(n):
	(a, b) = map(int, input().split())
	t[a] += b
res = 0
for i in range(1, N):
	c = v
	res += min(c, max(t[i - 1], 0))
	c -= max(t[i - 1], 0)
	if c > 0:
		res += min(c, t[i])
		t[i] = max(t[i] - c, 0)
print(res)
