 #include <bits/stdc++.h>
using namespace std;
 
int main() {
	long long t;
	cin>>t;
	//t=1;
	while(t--){
	    long long n,i,c=0; cin>>n;
	    long long a[n+1],b[n+1],m[n+1]; 
	    for(i=0;i<n;i++) cin>>a[i];
	    for(i=n-1;i>=0;i--){
	        c=max(c,a[i]);
	        if(c>0) b[i]=1,c--;
	        else b[i]=0;
	    }
	    for(i=0;i<n;i++) cout<<b[i]<<" ";
	    cout<<endl;
	}
	return 0;
}