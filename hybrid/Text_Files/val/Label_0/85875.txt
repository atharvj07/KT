#include <iostream>
#include <algorithm>
int N,M;
long long a[300001]={};
using namespace std;


int main() {

	cin>>N;
	for(int i=0;i<3*N;++i)cin>>a[i];
	sort(a,a+3*N);

	long long R=0;
	for(int i=3*N-2;i>N-1;i=i-2)R+=a[i];

	cout << R<< endl;
	return 0;
}
