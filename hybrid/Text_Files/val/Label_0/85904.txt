#include <bits/stdc++.h>
#pragma comment(linker, "/STACK:50000000")
using namespace std;
const long long mod = 1000000007;
const long long md = 1234567891;
const char aa = 'a';
const double eps = 1E-9;
const int mil = 1000000;
const int ths = 1000;
const int maxn = 1001;
const int dr[] = {0, -1, 0, 1};
const int dc[] = {1, 0, -1, 0};
const int mxx = 32;
const int inf = (int)1E9;
bool A[1200000];
int main() {
  memset(A, 0, sizeof(A));
  int a[100001];
  int n;
  cin >> n;
  for (int i = 0; i < (int)(n); i++) cin >> a[i];
  a[n] = (1 << (20)) - 1;
  for (int i = 0; i < (int)(n); i++) {
    int b = 0;
    int j = i;
    int s = 0;
    do {
      s |= a[j];
      b |= a[j + 1];
      ++j;
      A[s] = 1;
    } while ((b & a[i]) != a[i]);
  }
  int res = 0;
  for (int i = 0; i < (int)((1 << (20))); i++) res += A[i];
  cout << res << endl;
}
