#include <bits/stdc++.h>
using namespace std;
void conan() {
  std::ios_base::sync_with_stdio(0);
  cin.tie(0);
  cout.tie(0);
}
int main() {
  int a, ta, b, tb, h, m, c = 0, c2 = 0, x1, y1, y2;
  cin >> a >> ta >> b >> tb;
  scanf("%d:%d", &h, &m);
  x1 = h * 60 + m;
  y1 = x1 + ta;
  for (int i = 300; i < min(y1, 1440); i += b) {
    y2 = i + tb;
    if (y2 > x1) c++;
  }
  cout << c;
  return 0;
}
