from math import log
for _ in range(int(input())):
	n = int(input())
	arr = [0] * 102
	dq = list(map(int, input().split()))
	ans = 0
	for i in dq:
		arr[int(log(i, 2))] += 1
	for i in arr:
		ans += i * (i - 1) // 2
	print(ans)
