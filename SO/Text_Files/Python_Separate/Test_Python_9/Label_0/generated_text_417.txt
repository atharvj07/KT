from pyspark.sql import SparkSession
from pyspark.sql.functions import col

spark = SparkSession.builder.getOrCreate()

def fix(df1, df2):
    for f in df1.schema.fields:
        df2 = df2.withColumn(f.name, col(f.name).cast(f.dataType))
    return df2

df2 = fix(df1, df2)
