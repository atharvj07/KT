import numpy as np
import pandas as pd

def csm(x):
    x = np.array(x, dtype=np.float64)
    x[np.isnan(x)] = 0
    return np.cumsum(x)

df = pd.DataFrame({'col': [1, 2, 3, np.nan, 4, 5, 6]})
print(csm(df['col']))
