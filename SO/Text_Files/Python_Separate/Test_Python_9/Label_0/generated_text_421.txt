import click

@click.command()
@click.option('--v', default=1, show_default=True, help='a val.', is_eager=True)
def run(v):
    src = "usr" if "--v" in click.get_os_args() else "def"
    print(v, src)

if __name__ == "__main__":
    run()
