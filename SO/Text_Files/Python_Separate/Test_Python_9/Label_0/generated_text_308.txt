from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class InputData(BaseModel):
    process_id: int

@app.post("/do_something/")
async def do_something(data: InputData):
    return {"process_id": data.process_id}
