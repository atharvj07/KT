import numpy as np

def f(v, p):
    i, j = np.ix_(p, p)
    return v[i, j]

v = np.arange(9).reshape(3, 3)
p = np.array([[0, 1, 2], [1, 2, 0], [0, 2, 1], [2, 1, 0]])

out = np.array([f(v, x) for x in p])
print(out)
