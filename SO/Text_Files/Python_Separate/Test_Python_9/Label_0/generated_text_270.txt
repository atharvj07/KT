from pydantic import BaseModel, validator

class Win(BaseModel):
    size: tuple[int, int]

    @validator('size', pre=True)
    def transform(cls, v):
        x, y = v.split('x')
        return int(x), int(y)

fields = {'size': '1920x1080'}
win = Win(**fields)
