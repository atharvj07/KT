import polars as pl

df = pl.DataFrame({
    "ABC": ["foo", "bar", "foo"],
    "A": [1, 2, 3],
    "B": [2, 1, None],
    "C": [1, 2, 3]
})

df = df.with_columns(
    pl.col([pl.Int64]).apply(lambda x: np.median(x), return_dtype=pl.Float64).alias("Horizontal Median")
)

print(df)
