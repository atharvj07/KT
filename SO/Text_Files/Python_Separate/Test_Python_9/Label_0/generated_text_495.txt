import polars as pl

df = pl.DataFrame({"x": [1.2, 1.3, 3.4, 3.5]})

df = df.with_columns(
    pl.col("x").rolling(window_size=3, min_periods=1, offset=-2).agg(pl.list()).alias("y")
)

print(df)
