import polars as pl
import numpy as np

df = pl.DataFrame(
    {
        'A': [0, 0, 0, 1, None, 1],
        'B': [1, 2, 2, 1, 1, np.nan]
    }
)

df = df.drop_nulls().filter(~pl.col("B").is_nan())
print(df)