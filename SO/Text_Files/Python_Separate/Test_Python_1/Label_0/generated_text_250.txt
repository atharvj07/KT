import ctypes

# Get the address of integer 1
one = ctypes.cast(id(1), ctypes.POINTER(ctypes.c_int))
# Change the value at that address to 2
one.contents.value = 2

# Test case
assert 1 == 2
print(1 == 2)  # True
