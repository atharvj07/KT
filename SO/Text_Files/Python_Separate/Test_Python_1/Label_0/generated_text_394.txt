from datetime import datetime

for i in range(10_000_000):
    first = datetime.utcnow().timestamp()
    second = datetime.utcnow().timestamp()

    assert first <= second, f"{first=} {second=} {i=}"
