from django.db import models
from django.conf import settings

class AbstractModel(models.Model):
    created_at = models.DateTimeField(
        auto_now_add=True,
        blank=True,
        null=True,
    )

    created_by = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.SET_NULL,
        related_name='%(class)s_created_by',  # Dynamic related_name based on child class
        blank=True,
        null=True,
    )

    class Meta:
        abstract = True


# Example child class
class MyModel(AbstractModel):
    name = models.CharField(max_length=100)


# Test case to check dynamic related_name
my_instance = MyModel.objects.create(name="Test", created_by=user_instance)
print(my_instance.created_by)  # This should reference the correct related_name based on MyModel
