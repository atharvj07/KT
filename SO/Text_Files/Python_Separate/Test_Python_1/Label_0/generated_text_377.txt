onnx.checker.check_model(onnx_model)
   
bash
   pip install --upgrade onnx-tf
   
bash
   pip install onnx-tf==1.8.0
   
python
   import tensorflow as tf
   from tensorflow.python.framework import graph_util
   from onnx_tf.backend import prepare

   # Define the input signature for the model's inputs
   concrete_func = tf.function(model).get_concrete_function(tf.TensorSpec(shape=[None, 3, 800, 800], dtype=tf.float32))

   # Prepare the model for export
   tf_rep = prepare(onnx_model)
   tf_rep.export_graph('./model.pb', signatures=concrete_func)
   
bash
   pip install --upgrade tensorflow
   
python
   import logging
   logging.basicConfig(level=logging.DEBUG)
   