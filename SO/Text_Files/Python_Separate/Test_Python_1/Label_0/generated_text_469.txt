from fastapi import APIRouter, UploadFile, File
from pydantic import BaseModel
from app.models.schemas.files import FileInResponse

router = APIRouter()

class FileInCreate(BaseModel):
    file: UploadFile = File(...)

@router.post("", name="files:create-file", response_model=FileInResponse, response_model_exclude_unset=True)
async def create(file: FileInCreate):
    pass
