from pydantic import BaseModel, Field

class User(BaseModel):
    id: int
    global_: bool = Field(..., alias="global")

# Test case
data = {"id": 1, "global": True}
user = User(**data)
print(user.dict(by_alias=True))  # Output: {'id': 1, 'global': True}
