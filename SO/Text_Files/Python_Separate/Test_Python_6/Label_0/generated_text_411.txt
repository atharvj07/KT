import numpy as np
import pandas as pd

arr = np.array([1, 2, 3, np.nan, 4, 5, 6])
df = pd.DataFrame({'column': [1, 2, 3, np.nan, 4, 5, 6]})

cumsum_arr = np.cumsum(arr)
cumsum_df = np.cumsum(df['column'])

print(cumsum_arr)
print(cumsum_df)
