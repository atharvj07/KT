import polars as pl

df = pl.DataFrame({
    "ABC": ["foo", "bar", "foo"],
    "A": [1, 2, 3],
    "B": [2, 1, None],
    "C": [1, 2, 3]
})

# You can use a custom expression to calculate horizontal median
df_with_median = df.with_columns(
    pl.col(pl.Int64).apply(lambda x: np.median(x), return_dtype=pl.Float64).alias("Horizontal Median")
)

print(df_with_median)
