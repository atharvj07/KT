import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import pmdarima as pm
from pmdarima.arima import auto_arima
from sklearn.metrics import r2_score

data = pd.DataFrame(data_removed)

data['date'] = pd.to_datetime(data['date'])
data.set_index('date', inplace=True)

arima_data = data[['value']]

train_data = arima_data[:-24]
test_data = arima_data[-24:]

plt.plot(train_data, label="Training Data")
plt.plot(test_data, label="Test Data")

arima_model = auto_arima(train_data, start_p=0, d=1, start_q=0,
    max_p=5, max_d=5, max_q=5, start_P=0, D=1, start_Q=0, 
    max_P=5, max_D=5, max_Q=5, m=12, seasonal=True, 
    error_action='warn', trace=True, stepwise=True, 
    suppress_warnings=True, random_state=42, n_fits=50)

print(arima_model.summary())

prediction = pd
