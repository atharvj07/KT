import rsa
from Crypto.PublicKey import RSA
from Crypto.Cipher import PKCS1_v1_5
from Crypto.Random import get_random_bytes

# Convert the modulus and exponent to integers
modulus = int.from_bytes(modulus_bytes, byteorder='big')
exponent = int.from_bytes(exponent_bytes, byteorder='big')

# Create RSA key object
pubkey = RSA.construct((modulus, exponent))

# Prepare the data
data = (string1 + string2).encode()

# Encrypt using PKCS1_v1_5
cipher = PKCS1_v1_5.new(pubkey)
encrypted_data = cipher.encrypt(data)

# Alternatively, using rsa module
pubkey_rsa = rsa.PublicKey(modulus, exponent)
encrypted_data_rsa = rsa.encrypt(data, pubkey_rsa)
