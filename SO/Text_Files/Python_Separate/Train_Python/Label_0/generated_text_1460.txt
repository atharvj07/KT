for i in range(n + 1):
    for j in range(n + 1 - i):  # Limit j so that k is always non-negative
        k = n - i - j
        # Now, i + j + k == n is always true

for j in range(n + 1 - i):  # Limit j so that k is always non-negative
    k = n - i - j
    # Now, i + j + k == n is always true