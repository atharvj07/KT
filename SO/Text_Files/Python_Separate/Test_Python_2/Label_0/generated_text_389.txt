import polars as pl

def drop_nan_rows(df: pl.DataFrame) -> pl.DataFrame:
    return df.filter(~df.select(pl.all().is_nan()).row(any))

df = pl.DataFrame(
    {
        'A': [0, 0, 0, 1, None, 1],
        'B': [1, 2, 2, 1, 1, float('nan')]
    }
)

df = drop_nan_rows(df)
