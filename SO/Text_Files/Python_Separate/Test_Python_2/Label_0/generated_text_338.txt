# app.py
from fastapi import FastAPI, Depends
from pymongo import MongoClient
from typing import Callable

app = FastAPI()

def build_db(name: str):
    def close():
        return MongoClient()[name]
    return close

@app.post("/notification/feed")
async def route_receive_notifications(db: Callable[[], MongoClient] = Depends(build_db("someDB"))):
    # Your endpoint logic here
    pass

# test_endpoint.py
from fastapi.testclient import TestClient
import pytest
from app import app, build_db

def mock_db(name: str):
    def close():
        return "Mocked DB"
    return close

@pytest.fixture
def override_dependencies():
    app.dependency_overrides[build_db] = mock_db
    yield
    app.dependency_overrides.clear()

def test_route_receive_notifications(override_dependencies):
    client = TestClient(app)
    response = client.post("/notification/feed")
    assert response.status_code == 200
