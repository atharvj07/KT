import polars as pl

def add_predictions_column(df, predictions):
    return df.with_columns(pl.Series("predictions", predictions))

# Example usage
df = pl.DataFrame({"a": [1, 2, 3, 4, 5]})
predictions = [10, 20, 30, 40, 50]
df = add_predictions_column(df, predictions)
print(df)
