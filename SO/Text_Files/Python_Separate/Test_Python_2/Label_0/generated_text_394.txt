from datetime import datetime, timezone

def test_utc_monotonic():
    for i in range(10_000_000):
        first = datetime.now(timezone.utc)
        second = datetime.now(timezone.utc)

        assert first <= second, f"{first=} {second=} {i=}"
