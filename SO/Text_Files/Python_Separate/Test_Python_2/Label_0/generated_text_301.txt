import polars as pl

def filter_by_date(df: pl.DataFrame, column_name: str, date_str: str) -> pl.DataFrame:
    return df.filter(pl.col(column_name) >= pl.lit(date_str).str.strptime(pl.Date, "%Y-%m-%d"))
