import pandas as pd

def load_and_set_frequency(file_path):
    df = pd.read_csv(file_path, parse_dates=['date'])
    df = df.set_index('date').asfreq('H')
    return df

df = load_and_set_frequency('XBT_60.csv')
