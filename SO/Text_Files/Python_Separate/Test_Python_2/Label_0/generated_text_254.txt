import sys

def handle_exception_and_create_dict():
    try:
        raise ValueError('foo')
    except:
        exc_info = sys.exc_info()
        try:
            return dict(id=1, **{'id': 2})
        except Exception as e:
            raise e from exc_info[1]

handle_exception_and_create_dict()
