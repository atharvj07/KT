from typing import get_args, Literal

def parse_values(ann):
    if isinstance(ann, str):
        # do sth
        pass
    if isinstance(ann, int):
        # do sth
        pass
    if hasattr(ann, '__origin__') and ann.__origin__ is Literal:
        # do sth
        pass
