import asyncio
import pytest
import uuid
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.future import select
from faker import Faker
from my_app.models import User  # Replace with actual model imports

fake = Faker()

class AsyncFactory:
    async def create_user(self, session: AsyncSession, **kwargs):
        user = User(
            id=uuid.uuid4(),
            name=kwargs.get("name", fake.name()),
            email=kwargs.get("email", fake.email()),
        )
        session.add(user)
        await session.commit()
        return user

@pytest.fixture
async def user_factory(session: AsyncSession):
    factory = AsyncFactory()
    async def _create_user(**kwargs):
        return await factory.create_user(session, **kwargs)
    return _create_user
