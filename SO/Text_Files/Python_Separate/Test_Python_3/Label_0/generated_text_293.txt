from pydantic import BaseModel, Field

class User(BaseModel):
    id: int
    global_: bool = Field(..., alias="global")

def save_to_db(data: dict):
    return User(**data)

print(save_to_db({"id": 1, "global": True}))
