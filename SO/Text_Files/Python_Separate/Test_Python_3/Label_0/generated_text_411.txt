import numpy as np
import pandas as pd

df = pd.DataFrame({'column': [1, 2, 3, np.nan, 4, 5, 6]})

def custom_cumsum(series):
    return np.cumsum(series.fillna(0))

custom_cumsum(df['column'])
