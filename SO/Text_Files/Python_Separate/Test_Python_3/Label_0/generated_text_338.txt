from fastapi.testclient import TestClient
from pymongo import MongoClient
from my_app import app, build_db

def override_build_db():
    def mock_db():
        return build_singleton_whatever(MongoClient, "testDB")
    return mock_db

app.dependency_overrides[build_db] = override_build_db

client = TestClient(app)
