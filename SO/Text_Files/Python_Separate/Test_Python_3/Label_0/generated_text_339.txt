import pandas as pd

def transform_dataframe(df):
    return df.groupby(["A", "B"])["C"].apply(lambda x: pd.Series(x.values)).unstack().reset_index().rename(columns=lambda x: f"C{x+1}" if isinstance(x, int) else x)
