import pandas as pd

def markdown_to_dataframe(text):
    return pd.read_csv(pd.io.common.StringIO('\n'.join(text.splitlines()[2:])), sep='|', skipinitialspace=True).iloc[:, 1:].rename(columns=str.strip)
