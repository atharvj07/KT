import zstandard as zstd

def process_zst_file(filename, do_something):
    with open(filename, 'rb') as f, zstd.ZstdDecompressor().stream_reader(f) as reader:
        for line in reader.read().decode().splitlines():
            do_something(line)
