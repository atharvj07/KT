import polars as pl

def mask_columns(df: pl.DataFrame) -> pl.DataFrame:
    return df.with_columns(
        pl.when(pl.col("A").is_in(["foo", "spam"])).then("XX").otherwise(pl.col(c)).alias(c)
        for c in ["L", "G"]
    )
