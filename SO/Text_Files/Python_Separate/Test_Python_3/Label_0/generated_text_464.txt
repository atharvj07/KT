import random

def generate_numbers():
    while True:
        result = [random.randint(2, 6) for _ in range(10)]
        if sum(result) == 40:
            return result

print(generate_numbers())
