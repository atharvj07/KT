dockerfile
FROM python:3.9

WORKDIR /project

COPY poetry.lock pyproject.toml
RUN pip install poetry

ARG CI_JOB_TOKEN
RUN --mount=type=secret,id=ci_job_token poetry config http-basic.gitlab-pypi-repo gitlab-ci-token $(cat /run/secrets/ci_job_token)
RUN poetry install --no-interaction

bash
# In your Docker build command
DOCKER_BUILDKIT=1 docker build --secret id=ci_job_token,src=path_to_token_file .
