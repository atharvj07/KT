import polars as pl
import numpy as np

df = pl.DataFrame({
    'A': [0, 0, 0, 1, None, 1],
    'B': [1, 2, 2, 1, 1, np.nan]
})

df = df.filter(~df.select(pl.all().is_nan()).row(0))
df = df.drop_nulls()
