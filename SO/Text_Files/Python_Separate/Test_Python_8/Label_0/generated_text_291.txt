When training a custom dataset with YOLOv8 (and likely YOLOv5), the imgsz parameter specifies the size to which input images are resized before feeding them into the model. If you set imgsz=640, for instance, all images will be resized to 640x640, regardless of their original aspect ratio.

This resizing operation does not crop the image; instead, it typically applies letterboxingÃÂÃÂadding padding to maintain the aspect ratio while fitting the image into the desired dimensions. This avoids distortion while ensuring consistent input size for training and inference.

However, resizing still impacts object detection because smaller objects may lose details, and the padding can slightly alter how features are learned. ItÃÂÃÂs generally best to match imgsz to the size used during pretraining for optimal performance.