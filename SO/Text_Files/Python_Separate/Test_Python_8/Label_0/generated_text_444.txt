import numpy as np

arr = np.arange(1, 17)

stride = 8
chunk_size = 4

a = arr[np.arange(0, len(arr), stride)[:, None] + np.arange(chunk_size)].ravel()
b = arr[np.arange(chunk_size, len(arr), stride)[:, None] + np.arange(chunk_size)].ravel()

a, b
