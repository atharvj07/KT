import numpy as np
import numpy.typing as npt

def foo(x: npt.NDArray[np.int32]): 
    if x.shape != (4,):  
        raise ValueError("Expected shape (4,), got " + str(x.shape)) 
    print(x)  

arr = np.array([1, 2, 3, 4], dtype=np.int32)  
foo(arr)
