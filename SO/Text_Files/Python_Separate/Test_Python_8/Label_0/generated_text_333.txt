# scripts/build.py
import subprocess

def run():
    cmd = ["poetry", "run", "pyinstaller", "main.py", "--collect-submodules", "application", "--onefile", "--name", "myapi"]
    subprocess.run(" ".join(cmd), shell=True)
