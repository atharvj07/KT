import polars as pl
import duckdb

pldf = pl.DataFrame({'mynum': [1,2,3,4]})

# DuckDB connection
con = duckdb.connect("scratch.db")

# Using to_sql method instead of write_database
pldf.write_sql('test_table', con)

# Closing the connection
con.close()
