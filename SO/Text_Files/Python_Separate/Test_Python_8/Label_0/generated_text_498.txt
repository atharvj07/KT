import pandas as pd
import attr

@attr.s
class MyAttrsClass:
    i = attr.ib()
    s = attr.ib()

data = [MyAttrsClass("a", 1), MyAttrsClass("b", 2)]

df = pd.DataFrame([vars(obj) for obj in data])
