import pandas as pd
from tqdm import tqdm

def func1(txt):
    # Example function (replace with actual logic)
    return txt.lower()

tqdm.pandas()

df = pd.DataFrame({'textbody': ['Text A', 'Text B', 'Text C']})

df['result'] = df['textbody'].progress_apply(lambda x: func1(x))
