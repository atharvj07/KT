from fastapi import FastAPI, Request
import time

app = FastAPI()

@app.middleware("http")
async def add_proc_time(req: Request, call_next):
    start = time.time()

    resp = await call_next(req)

    proc_time = time.time() - start
    resp.headers["X-Proc-Time"] = str(proc_time)
    return resp
