from dataclasses import dataclass
from typing import TypeVar, Callable

T = TypeVar('T')

def sth_else(klass: T) -> T:
    return klass

def my_dataclass(klass: T) -> T:
    return sth_else(dataclass(klass))

@my_dataclass
class TestB:
    a: int
    b: str
