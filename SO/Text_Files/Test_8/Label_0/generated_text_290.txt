import re

s = "aaabbbbcca"
matches = re.finditer(r"([a-z])\1*", s)

substrings = []
for match in matches:
    group = match.group()
    for i in range(len(group)):
        substrings.append(group[:i+1])

print(substrings)
