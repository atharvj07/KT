import polars as pl
import random

# Create a dataframe
df = pl.DataFrame({
    "Q": [1, 2, 3, 4, 5]
})

# Add a column filled with random values
df = df.with_columns(
    pl.col("Q").apply(lambda _: random.random()).alias("Prob")
)

# Show the dataframe
print(df)
