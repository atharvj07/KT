# _custom_build.py
from setuptools.command.build_ext import build_ext as _build_ext
from setuptools import Extension
from Cython.Build import cythonize
import os

class build_ext(_build_ext):
    def run(self):
        if self.distribution.ext_modules is None:
            self.distribution.ext_modules = []
        extensions = [Extension("your_module", ["your_module.pyx", "your_cpp_file.cpp"])]
        self.distribution.ext_modules.extend(cythonize(extensions))
        super().run()

# pyproject.toml
[build-system]
requires = ["setuptools>=61.0", "numpy>=1.17", "cython>=0.18"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages]
find = {}

[tool.setuptools.cmdclass]
build_ext = "_custom_build.build_ext"

# Editable Install with Build
pip install --editable . --config-settings editable_mode=compat

# Test Case
import your_module
print(your_module.some_function())
