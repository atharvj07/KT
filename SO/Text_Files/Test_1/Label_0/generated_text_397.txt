import polars as pl

df = pl.DataFrame(
    {
        "col1": ["a", "b", "a", "b", "c"],
        "col2": ["val1", "val2", "val1", "val3", "val3"]
    }
)

result = df.group_by("col1").agg(pl.col("col2").str.concat(","))

print(result)
