[tool.poetry.scripts]
start = "main:start"
builddist = "scripts.builddist:main"

python
import subprocess

def main():
    subprocess.run([
        "poetry", "run", "pyinstaller", 
        "main.py", "--collect-submodules", "application", 
        "--onefile", "--name", "myapi"
    ])

sh
poetry run builddist
