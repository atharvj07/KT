android {
    defaultConfig {
        versionName "1.2.3"
    }
}

kotlin
val versionName = BuildConfig.VERSION_NAME
val label = findViewById<Label>(R.id.versionLabel)
label.text = "Version: $versionName"

java
String versionName = BuildConfig.VERSION_NAME;
Label label = findViewById(R.id.versionLabel);
label.setText("Version: " + versionName);

xml
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <application
        android:label="@string/app_name"
        android:versionName="1.2.3">
    </application>
</manifest>

kotlin
val versionName = packageManager.getPackageInfo(packageName, 0).versionName
label.text = "Version: $versionName"

java
String versionName = getPackageManager().getPackageInfo(getPackageName(), 0).versionName;
label.setText("Version: " + versionName);

kotlin
@Test
fun testVersionNameDisplayed() {
    val versionName = BuildConfig.VERSION_NAME
    assertEquals("1.2.3", versionName)
}
