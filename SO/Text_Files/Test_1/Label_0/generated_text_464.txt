import random

def generate_numbers(target_sum, count, min_val, max_val):
    while True:
        numbers = [random.randint(min_val, max_val) for _ in range(count)]
        if sum(numbers) == target_sum:
            return numbers

result = generate_numbers(40, 10, 2, 6)
print(result)
