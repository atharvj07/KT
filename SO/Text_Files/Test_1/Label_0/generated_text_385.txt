import pandas as pd
import pyarrow as pa

# Construct PyArrow-backed DataFrame directly
data = {
    "col1": pa.array([1, 2, 3]),
    "col2": pa.array(["a", "b", "c"]),
}
df = pd.DataFrame(data, dtype_backend="pyarrow")

print(df)
print(df.dtypes)
