#!/bin/bash

conda init bash

# Check if the environment exists
if conda env list | grep -q "RUN_ENV"; then
    conda activate RUN_ENV
else
    conda env create --name RUN_ENV --file ../run_env.yml -q --force
    conda activate RUN_ENV
fi

python run_app.py
conda deactivate
