import numpy as np
import numpy.typing as npt
from typing import Annotated

Shape = Annotated[np.ndarray, (4,)]
Int32Array = Shape[npt.NDArray[np.int32]]

def foo(x: Int32Array) -> None:
    print(x)

arr = np.array([1, 2, 3, 4], dtype=np.int32)
foo(arr)  # Test case
