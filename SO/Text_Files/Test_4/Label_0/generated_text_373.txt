import polars as pl

df = pl.DataFrame({
"Q": [1, 2, 0, 4, -1]
})

df = df.with_columns(
pl.when(pl.col("Q") > 0)
.then(pl.lit(None).fill_null(pl.Series([random.random() for _ in range(df.height)])))
.otherwise(pl.lit(1))
.alias("Prob")
)

print(df)