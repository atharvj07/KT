import polars as pl

df = pl.DataFrame({
"x": [[1], [1, 2], [1, 2, 3]],
"y": [[True], None, [True, False, True]]
})

df = df.with_columns(
y=pl.when(df["y"].is_null())
.then(pl.repeat(None, df["x"].list.len()))
.otherwise(df["y"])
)

df = df.explode(["x", "y"])
print(df)