import polars as pl

data = {
    "a": [[1], [2], [3, 4], [5, 6, 7]],
    "b": [[], [8], [9, 10], [11, 12]],
}

df = pl.DataFrame(data)

df = df.with_columns([
    pl.col("a").arr.head(pl.col("b").arr.len()),
    pl.col("b").arr.head(pl.col("a").arr.len())
]).explode(["a", "b"])
