import xarray as xr
import numpy as np

x = [x1, x2, x3, x4]
y = [y1, y2, y3, y4]

ds = xr.open_dataset("data.nc")

subset = np.array([ds.sel(longitude=xi, latitude=yi, method='nearest').values for xi, yi in zip(x, y)]).flatten()
