import matplotlib.pyplot as plt
import numpy as np

fig, ax = plt.subplots()

x = np.arange(3)
heights = [1234, 5678, 91011]

bars = ax.bar(x, heights)

for bar in bars:
    ax.bar_label(bars, labels=[f"{bar.get_height():,.2f}"], label_type="edge")

plt.show()
