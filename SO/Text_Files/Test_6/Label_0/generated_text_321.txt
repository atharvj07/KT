import pandas as pd
from tqdm import tqdm

tqdm.pandas()

def func1(text):
    return len(text)  

df = pd.DataFrame({"textbody": ["sample text"] * 10000})

df["result"] = df["textbody"].progress_apply(lambda x: func1(x))
