import polars as pl

df = (
    pl.DataFrame(
        {
            "value": values
        }
    )
    .with_columns(
        pl.col("value").ewm_mean(alpha=2/(9+1), adjust=False, min_periods=9).alias("ema_9")
    )
)

df
