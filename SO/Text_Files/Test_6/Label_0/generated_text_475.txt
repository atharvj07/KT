import polars as pl
import duckdb

pldf = pl.DataFrame({'mynum': [1, 2, 3, 4]})
con = duckdb.connect(database="scratch.db", read_only=False)

# Writing DataFrame to DuckDB using SQL
pldf.write_sql("CREATE TABLE test_table AS SELECT * FROM ?", con)

con.close()
