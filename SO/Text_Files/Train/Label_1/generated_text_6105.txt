(df.select(pl.all().is_null().all())
    .unpivot()
    .filter(pl.col('value')==False)
    .select('variable')
    .collect()
    .to_series()
    .to_list())

(df.select(
    df.select(pl.all().is_null().all())
      .unpivot()
      .filter(pl.col('value')==False)
      .select('variable')
      .collect()
      .to_series()
      .to_list())
.collect())