$ cat tst.awk
NR==1 {
    hdr = $0
    next
}
$NF != prev {
    out = (((blockCnt++) % X) + 1) &quot;.csv&quot;
    if ( blockCnt &lt;= X ) {
        print hdr &gt; out
    }
    prev = $NF
}
{ print &gt; out }