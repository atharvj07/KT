#include &lt;stdio.h&gt;

int main(int argc, char *argv[])
{
    int n;
    while (1){
        scanf(&quot;%d&quot;, &amp;n);
        printf(&quot;%d\n&quot;, n);
        fflush(stdout);  /* I've added this line only. */
    }
    return 0;
}