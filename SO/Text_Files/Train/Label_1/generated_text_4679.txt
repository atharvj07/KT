public class fÅMÃ¡solgatÃ³ implements ActionListener {

    JButton b;
    JLabel label;
    JTextField tfield;
    JFrame frame;

    // this is still a no-arg constructor, but unlike in your code,
    // this is defined by you, and not automatically by Java
    public fÅMÃ¡solgatÃ³(){
        frame = new JFrame();
        b = new JButton("MÃ¡solÃ¡s");
        label = new JLabel("");
        tfield = new JTextField();
        frame.add(b);
        frame.add(label);
        frame.add(tfield);
        b.addActionListener(this);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setVisible(true);
    }

    @Override
    public void actionPerformed(ActionEvent e) {
        label.setText(tfield.getText());
    }
}