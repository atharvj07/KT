public class RequestUserDto {
    @Password
    private String password;

    // Getters and setters
}

String message() default "{messages.validation.password}";

messages.validation.password=The password must be valid.

String message() default "The password must be valid.";

<div class="text-danger" th:if="${#fields.hasErrors('password')}" th:errors="*{password}"></div>

<div class="text-danger" th:if="${bindingResult != null and bindingResult.hasFieldErrors('password')}" th:errors="*{password}"></div>

if (bindingResult.hasErrors()) {
    model.addAttribute("bindingResult", bindingResult);
    model.addAttribute("userDto", userDto);
    return USER_REGISTER_PAGE;
}

<p th:each="error : ${bindingResult.allErrors}" th:text="${error}"></p>

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-validation</artifactId>
</dependency>

@Password
private String password;
// Getters and setters
model.addAttribute("bindingResult", bindingResult);
model.addAttribute("userDto", userDto);
return USER_REGISTER_PAGE;
<groupId>org.springframework.boot</groupId>
<artifactId>spring-boot-starter-validation</artifactId>