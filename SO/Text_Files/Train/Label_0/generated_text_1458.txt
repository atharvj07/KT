List<EventReminder> reminderOverrides = new ArrayList<>();
reminderOverrides.add(new EventReminder().setMethod("popup").setMinutes(1440)); // 1 day before
reminderOverrides.add(new EventReminder().setMethod("email").setMinutes(2880)); // 2 days before

event.setReminders(new Event.Reminders()
        .setUseDefault(false) // Don't use default (since it won't work)
        .setOverrides(reminderOverrides));

DateTime startDateTime = new DateTime(dateFormat.format(today) + "T00:00:00Z"); 
DateTime endDateTime = new DateTime(dateFormat.format(today) + "T23:59:59Z");

event.setStart(new EventDateTime().setDateTime(startDateTime).setTimeZone("UTC"))
     .setEnd(new EventDateTime().setDateTime(endDateTime).setTimeZone("UTC"))
     .setReminders(new Event.Reminders().setUseDefault(true)); 

    .setUseDefault(false) // Don't use default (since it won't work)
    .setOverrides(reminderOverrides));
 .setEnd(new EventDateTime().setDateTime(endDateTime).setTimeZone("UTC"))
 .setReminders(new Event.Reminders().setUseDefault(true));