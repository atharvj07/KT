import org.dom4j.Document;
import org.dom4j.Node;
import org.dom4j.DocumentHelper;
import java.util.List;

Document doc = reader.read(new StringReader(xml));

// Use selectNodes to select all nodes that satisfy the XPath
List<Node> valueNodes = doc.selectNodes("/X/X1/X2[@code='b']/X3/value");

// Iterate through the nodes and print or process them
for (Node valueNode : valueNodes) {
    System.out.println(valueNode.getText()); // This will print "x3b11" and "X3b12"
}

x3b11
X3b12

    import org.dom4j.Document;
    import org.dom4j.io.SAXReader;
    import org.dom4j.Node;
    import java.io.StringReader;
    import java.util.List;
    
System.out.println(valueNode.getText()); // This will print "x3b11" and "X3b12"
```java
import org.dom4j.Document;
import org.dom4j.io.SAXReader;
import org.dom4j.Node;
import java.io.StringReader;
import java.util.List;
```