int x = 100 * (position % 8);
int y = 100 * (position / 8);

public Piece(int Id, int color, int position) {
    dragged = false;
    this.Id = Id;
    this.color = color;

    // Calculate the position based on the provided position
    this.x = 100 * (position % 8);  // Use 'this.x' to refer to the instance variable
    this.y = 100 * (position / 8);  // Use 'this.y' to refer to the instance variable

    System.out.println(x);  // This should print the calculated x position

    tx = AffineTransform.getTranslateInstance(x, y);  // Set transformation with correct x, y
    init(x, y);  // Initialize transformation
}

public Board(){
    board = new Piece[8][8];
    for (int i = 0; i < 8; i++) {
        board[1][i] = new Pawn(i, 1, 8 + i); // Adjusted pawn position logic
    }

    for (int i = 0; i < 8; i++) {
        board[6][i] = new Pawn(i, 0, 48 + i); // Adjusted pawn position logic
    }
}

dragged = false;
this.Id = Id;
this.color = color;
// Calculate the position based on the provided position
this.x = 100 * (position % 8);  // Use 'this.x' to refer to the instance variable
this.y = 100 * (position / 8);  // Use 'this.y' to refer to the instance variable
System.out.println(x);  // This should print the calculated x position
tx = AffineTransform.getTranslateInstance(x, y);  // Set transformation with correct x, y
init(x, y);  // Initialize transformation
board = new Piece[8][8];
for (int i = 0; i < 8; i++) {
    board[1][i] = new Pawn(i, 1, 8 + i); // Adjusted pawn position logic
}
for (int i = 0; i < 8; i++) {
    board[6][i] = new Pawn(i, 0, 48 + i); // Adjusted pawn position logic
}