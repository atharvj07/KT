import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;

public abstract class Animal<T> {
    private final String name;
    private final int age;
    private final T info;

    // Constructor
    @JsonCreator
    public Animal(@JsonProperty("name") String name, @JsonProperty("age") int age, @JsonProperty("info") T info) {
        this.name = name;
        this.age = age;
        this.info = info;
    }

    // Getters
    public String getName() {
        return name;
    }

    public int getAge() {
        return age;
    }

    public T getInfo() {
        return info;
    }
}

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonUnwrapped;

public class Dog extends Animal<DogInfo> {
    // Dog-specific properties
    private final int barkLoudness;
    private final int treatsToday;

    // Constructor
    @JsonCreator
    public Dog(@JsonProperty("name") String name,
               @JsonProperty("age") int age,
               @JsonProperty("barkLoudness") int barkLoudness,
               @JsonProperty("treatsToday") int treatsToday) {
        super(name, age, new DogInfo(barkLoudness, treatsToday));
        this.barkLoudness = barkLoudness;
        this.treatsToday = treatsToday;
    }

    // Getters
    public int getBarkLoudness() {
        return barkLoudness;
    }

    public int getTreatsToday() {
        return treatsToday;
    }
}

public class DogInfo {
    private final int barkLoudness;
    private final int treatsToday;

    @JsonCreator
    public DogInfo(@JsonProperty("barkLoudness") int barkLoudness,
                   @JsonProperty("treatsToday") int treatsToday) {
        this.barkLoudness = barkLoudness;
        this.treatsToday = treatsToday;
    }

    // Getters
    public int getBarkLoudness() {
        return barkLoudness;
    }

    public int getTreatsToday() {
        return treatsToday;
    }
}

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;

public class Cat extends Animal<CatInfo> {
    // Cat-specific properties
    private final String color;

    // Constructor
    @JsonCreator
    public Cat(@JsonProperty("name") String name,
               @JsonProperty("age") int age,
               @JsonProperty("color") String color) {
        super(name, age, new CatInfo(color));
        this.color = color;
    }

    // Getter
    public String getColor() {
        return color;
    }
}

public class CatInfo {
    private final String color;

    @JsonCreator
    public CatInfo(@JsonProperty("color") String color) {
        this.color = color;
    }

    // Getter
    public String getColor() {
        return color;
    }
}

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;

public class Fish extends Animal<FishInfo> {
    // Fish-specific properties
    private final String blub;

    // Constructor
    @JsonCreator
    public Fish(@JsonProperty("name") String name,
                @JsonProperty("age") int age,
                @JsonProperty("blub") String blub) {
        super(name, age, new FishInfo(blub));
        this.blub = blub;
    }

    // Getter
    public String getBlub() {
        return blub;
    }
}

public class FishInfo {
    private final String blub;

    @JsonCreator
    public FishInfo(@JsonProperty("blub") String blub) {
        this.blub = blub;
    }

    // Getter
    public String getBlub() {
        return blub;
    }
}

{
  "name": "Fido",
  "age": 5,
  "info": {
    "barkLoudness": 8,
    "treatsToday": 2
  }
}

private final String name;
private final int age;
private final T info;
// Constructor
@JsonCreator
public Animal(@JsonProperty("name") String name, @JsonProperty("age") int age, @JsonProperty("info") T info) {
    this.name = name;
    this.age = age;
    this.info = info;
}
// Getters
public String getName() {
    return name;
}
public int getAge() {
    return age;
}
public T getInfo() {
    return info;
}
// Dog-specific properties
private final int barkLoudness;
private final int treatsToday;
// Constructor
@JsonCreator
public Dog(@JsonProperty("name") String name,
           @JsonProperty("age") int age,
           @JsonProperty("barkLoudness") int barkLoudness,
           @JsonProperty("treatsToday") int treatsToday) {
    super(name, age, new DogInfo(barkLoudness, treatsToday));
    this.barkLoudness = barkLoudness;
    this.treatsToday = treatsToday;
}
// Getters
public int getBarkLoudness() {
    return barkLoudness;
}
public int getTreatsToday() {
    return treatsToday;
}
private final int barkLoudness;
private final int treatsToday;
@JsonCreator
public DogInfo(@JsonProperty("barkLoudness") int barkLoudness,
               @JsonProperty("treatsToday") int treatsToday) {
    this.barkLoudness = barkLoudness;
    this.treatsToday = treatsToday;
}
// Getters
public int getBarkLoudness() {
    return barkLoudness;
}
public int getTreatsToday() {
    return treatsToday;
}
// Cat-specific properties
private final String color;
// Constructor
@JsonCreator
public Cat(@JsonProperty("name") String name,
           @JsonProperty("age") int age,
           @JsonProperty("color") String color) {
    super(name, age, new CatInfo(color));
    this.color = color;
}
// Getter
public String getColor() {
    return color;
}
private final String color;
@JsonCreator
public CatInfo(@JsonProperty("color") String color) {
    this.color = color;
}
// Getter
public String getColor() {
    return color;
}
// Fish-specific properties
private final String blub;
// Constructor
@JsonCreator
public Fish(@JsonProperty("name") String name,
            @JsonProperty("age") int age,
            @JsonProperty("blub") String blub) {
    super(name, age, new FishInfo(blub));
    this.blub = blub;
}
// Getter
public String getBlub() {
    return blub;
}
private final String blub;
@JsonCreator
public FishInfo(@JsonProperty("blub") String blub) {
    this.blub = blub;
}
// Getter
public String getBlub() {
    return blub;
}
"barkLoudness": 8,
"treatsToday": 2