import kotlinx.coroutines.*
import kotlinx.coroutines.jdk8.*
import kotlin.coroutines.EmptyCoroutineContext

fun <T> callSuspendFunctionReflectively(method: Method, target: Any, args: Array<Any?>): CompletableFuture<T> {
    val function = ReflectJvmMapping.getKotlinFunction(method)

    // Launch the coroutine and wrap it in a CompletableFuture
    return FutureKt.future(Dispatchers.Default, CoroutineStart.DEFAULT) { scope, continuation ->
        // Call the suspend function
        KCallables.callSuspend(function, getSuspendedFunctionArgs(target, args), continuation)
    }
}

fun getSuspendedFunctionArgs(target: Any, args: Array<Any?>): Array<Any?> {
    // Adjust this logic based on how you're reflecting the arguments for the suspended function
    return args
}

val function = ReflectJvmMapping.getKotlinFunction(method)
// Launch the coroutine and wrap it in a CompletableFuture
return FutureKt.future(Dispatchers.Default, CoroutineStart.DEFAULT) { scope, continuation ->
    // Call the suspend function
    KCallables.callSuspend(function, getSuspendedFunctionArgs(target, args), continuation)
}
// Adjust this logic based on how you're reflecting the arguments for the suspended function
return args