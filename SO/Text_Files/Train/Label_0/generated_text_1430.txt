@Override
public void onResponse(@NonNull Call call, @NonNull Response response) throws IOException {
    String jsonStr = response.body().string();
    Log.e(TAG, "Response from url: " + jsonStr);
    if (jsonStr != null) {
        try {
            JSONObject jsonObj = new JSONObject(jsonStr);
            JSONArray records = jsonObj.getJSONArray("records");

            resultList.clear(); // Clear old data before adding new ones

            for (int i = 0; i < records.length(); i++) {
                JSONObject c = records.getJSONObject(i);
                HashMap<String, String> result = new HashMap<>();

                result.put("league", c.getString("league"));
                result.put("season", c.getString("season"));
                result.put("name", c.getString("name"));
                result.put("officialName", c.getString("officialName"));
                result.put("address", c.getString("address"));
                result.put("telephone", c.getString("telephone"));
                result.put("fax", c.getString("fax"));
                result.put("website", c.getString("website"));
                result.put("founded", c.getString("founded"));
                result.put("teamSize", c.getString("teamSize"));
                result.put("averageAge", c.getString("averageAge"));
                result.put("foreigners", c.getString("foreigners"));
                result.put("nationaTeamPlayers", c.getString("nationaTeamPlayers"));
                result.put("teamValue", c.getString("teamValue"));
                result.put("venue", c.getString("venue"));
                result.put("venueCapacity", c.getString("venueCapacity"));

                resultList.add(result);
            }

            runOnUiThread(new Runnable() {
                @Override
                public void run() {
                    ListAdapter adapter = new SimpleAdapter(TeamInfo.this, resultList,
                            R.layout.list_item_team,
                            new String[]{"league", "season", "name", "officialName", "address",
                                    "telephone", "fax", "website", "founded", "teamSize",
                                    "averageAge", "foreigners", "nationaTeamPlayers", "teamValue",
                                    "venue", "venueCapacity"},
                            new int[]{R.id.league, R.id.season, R.id.name, R.id.officialName,
                                    R.id.address, R.id.telephone, R.id.fax, R.id.website, R.id.founded,
                                    R.id.teamSize, R.id.averageAge, R.id.foreigners, R.id.nationaTeamPlayers,
                                    R.id.teamValue, R.id.venue, R.id.venueCapacity});

                    lv.setAdapter(adapter);
                }
            });

        } catch (final JSONException e) {
            Log.e(TAG, "Json parsing error: " + e.getMessage());
            runOnUiThread(() -> Toast.makeText(getApplicationContext(),
                    "Json parsing error: " + e.getMessage(), Toast.LENGTH_LONG).show());
        }
    } else {
        Log.e(TAG, "Couldn't get json from server.");
        runOnUiThread(() -> Toast.makeText(getApplicationContext(),
                "Couldn't get json from server. Check LogCat for possible errors!",
                Toast.LENGTH_LONG).show());
    }
}

String jsonStr = response.body().string();
Log.e(TAG, "Response from url: " + jsonStr);
if (jsonStr != null) {
    try {
        JSONObject jsonObj = new JSONObject(jsonStr);
        JSONArray records = jsonObj.getJSONArray("records");
        resultList.clear(); // Clear old data before adding new ones
        for (int i = 0; i < records.length(); i++) {
            JSONObject c = records.getJSONObject(i);
            HashMap<String, String> result = new HashMap<>();
            result.put("league", c.getString("league"));
            result.put("season", c.getString("season"));
            result.put("name", c.getString("name"));
            result.put("officialName", c.getString("officialName"));
            result.put("address", c.getString("address"));
            result.put("telephone", c.getString("telephone"));
            result.put("fax", c.getString("fax"));
            result.put("website", c.getString("website"));
            result.put("founded", c.getString("founded"));
            result.put("teamSize", c.getString("teamSize"));
            result.put("averageAge", c.getString("averageAge"));
            result.put("foreigners", c.getString("foreigners"));
            result.put("nationaTeamPlayers", c.getString("nationaTeamPlayers"));
            result.put("teamValue", c.getString("teamValue"));
            result.put("venue", c.getString("venue"));
            result.put("venueCapacity", c.getString("venueCapacity"));
            resultList.add(result);
        }
        runOnUiThread(new Runnable() {
            @Override
            public void run() {
                ListAdapter adapter = new SimpleAdapter(TeamInfo.this, resultList,
                        R.layout.list_item_team,
                        new String[]{"league", "season", "name", "officialName", "address",
                                "telephone", "fax", "website", "founded", "teamSize",
                                "averageAge", "foreigners", "nationaTeamPlayers", "teamValue",
                                "venue", "venueCapacity"},
                        new int[]{R.id.league, R.id.season, R.id.name, R.id.officialName,
                                R.id.address, R.id.telephone, R.id.fax, R.id.website, R.id.founded,
                                R.id.teamSize, R.id.averageAge, R.id.foreigners, R.id.nationaTeamPlayers,
                                R.id.teamValue, R.id.venue, R.id.venueCapacity});
                lv.setAdapter(adapter);
            }
        });
    } catch (final JSONException e) {
        Log.e(TAG, "Json parsing error: " + e.getMessage());
        runOnUiThread(() -> Toast.makeText(getApplicationContext(),
                "Json parsing error: " + e.getMessage(), Toast.LENGTH_LONG).show());
    }
} else {
    Log.e(TAG, "Couldn't get json from server.");
    runOnUiThread(() -> Toast.makeText(getApplicationContext(),
            "Couldn't get json from server. Check LogCat for possible errors!",
            Toast.LENGTH_LONG).show());
}