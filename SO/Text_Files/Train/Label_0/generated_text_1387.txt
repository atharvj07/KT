@Test
   public void invoke_ShouldThrow_MyCustomException() {
       Exception exception = assertThrows(MyCustomException.class, () -> {
           myServiceToTest.invoke(null);
       });
       System.out.println("Caught exception: " + exception);
       assertTrue(exception instanceof MyCustomException);
   }
   
   @Test
   public void invoke_ShouldThrow_MyCustomException() {
       MyService myService = new MyService();
       assertThrows(MyCustomException.class, () -> {
           myService.invoke(null);
       });
   }
   
   Exception exception = assertThrows(MyCustomException.class, () -> {
       myServiceToTest.invoke(null);
   });
   System.out.println("Caught exception: " + exception);
   assertTrue(exception instanceof MyCustomException);
   MyService myService = new MyService();
   assertThrows(MyCustomException.class, () -> {
       myService.invoke(null);
   });