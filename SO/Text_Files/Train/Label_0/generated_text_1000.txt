Match(Option.of(clientId)).of(
    Case($None(), run(() -> { ... })),
    Case($Some($(StringUtils::isBlank)), run(() -> { ... })),
    Case($(), run(() -> { ... }))
);

Option<String> clientIdOption = Option.of(clientId); // Wrap safely

Match(clientIdOption).of(
    Case($None(), run(() -> { // Case when clientId is actually null
        metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
        log.error("Client ID not present in the request header: [{}]", clientId);
        throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
    })),
    Case($Some($(StringUtils::isBlank)), run(() -> { // Case when clientId is blank
        metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
        log.error("Client ID not present in the request header: [{}]", clientId);
        throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
    })),
    Case($Some($()), id -> { // Case when clientId is present
        if (isClientIdNotAllowed(id)) {
            log.error(LOG_CLIENT_ID_NOT_ALLOWED, id);
            metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_ALLOWED, type, id);
            throw new ValidationException(EX_ALLOWED_CLIENT_ID_ERROR);
        }
    })
);

   Option<String> clientIdOption = Option.of(clientId);
   
Option.of(clientId)
    .filter(StringUtils::isNotBlank) // Keep only non-blank values
    .peek(id -> { // Only applies if clientId is non-null and non-blank
        if (isClientIdNotAllowed(id)) {
            log.error(LOG_CLIENT_ID_NOT_ALLOWED, id);
            metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_ALLOWED, type, id);
            throw new ValidationException(EX_ALLOWED_CLIENT_ID_ERROR);
        }
    })
    .onEmpty(() -> { // Triggers if Option is empty (null or blank)
        metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
        log.error("Client ID not present in the request header: [{}]", clientId);
        throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
    });

Case($None(), run(() -> { ... })),
Case($Some($(StringUtils::isBlank)), run(() -> { ... })),
Case($(), run(() -> { ... }))
Case($None(), run(() -> { // Case when clientId is actually null
    metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
    log.error("Client ID not present in the request header: [{}]", clientId);
    throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
})),
Case($Some($(StringUtils::isBlank)), run(() -> { // Case when clientId is blank
    metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
    log.error("Client ID not present in the request header: [{}]", clientId);
    throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
})),
Case($Some($()), id -> { // Case when clientId is present
    if (isClientIdNotAllowed(id)) {
        log.error(LOG_CLIENT_ID_NOT_ALLOWED, id);
        metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_ALLOWED, type, id);
        throw new ValidationException(EX_ALLOWED_CLIENT_ID_ERROR);
    }
})
.filter(StringUtils::isNotBlank) // Keep only non-blank values
.peek(id -> { // Only applies if clientId is non-null and non-blank
    if (isClientIdNotAllowed(id)) {
        log.error(LOG_CLIENT_ID_NOT_ALLOWED, id);
        metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_ALLOWED, type, id);
        throw new ValidationException(EX_ALLOWED_CLIENT_ID_ERROR);
    }
})
.onEmpty(() -> { // Triggers if Option is empty (null or blank)
    metricsService.recordValidationStageMetrics(CLIENT_ID_NOT_PRESENT, type, BLANK);
    log.error("Client ID not present in the request header: [{}]", clientId);
    throw new ValidationException(EX_REQUEST_CLIENT_ID_EMPTY);
});