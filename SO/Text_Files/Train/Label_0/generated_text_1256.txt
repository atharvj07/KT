public static void Game(String uniqueID) throws FileNotFoundException {
    Scanner s3 = new Scanner(gameData);
    String coins = "0"; // Default value in case the ID is not found

    while (s3.hasNextLine()) {
        String fileLine = s3.nextLine();
        String[] parts = fileLine.split(","); // Split line into parts

        if (parts.length == 2 && parts[0].trim().equals(uniqueID)) {
            coins = parts[1].trim(); // Assign coins value if ID matches
            break;
        }
    }

    s3.close(); // Always close scanner
    System.out.println("Coins: " + coins);
}

Scanner s3 = new Scanner(gameData);
String coins = "0"; // Default value in case the ID is not found
while (s3.hasNextLine()) {
    String fileLine = s3.nextLine();
    String[] parts = fileLine.split(","); // Split line into parts
    if (parts.length == 2 && parts[0].trim().equals(uniqueID)) {
        coins = parts[1].trim(); // Assign coins value if ID matches
        break;
    }
}
s3.close(); // Always close scanner
System.out.println("Coins: " + coins);