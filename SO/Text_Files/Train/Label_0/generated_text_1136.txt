import com.ibm.mq.jms.MQQueue;
import com.ibm.mq.jms.MQQueueConnectionFactory;
import com.ibm.mq.jms.MQMessage;
import com.ibm.mq.jms.MQSession;
import com.ibm.mq.MQException;
import com.ibm.mq.MQConstants;
import org.springframework.jms.core.JmsTemplate;
import org.springframework.beans.factory.annotation.Autowired;

public void send(Request mqRequest) throws JMSException {
    log.info("sending with message [{}] to queue [{}].", mqRequest.getMessage(), QUEUE_NAME);

    // Create an MQQueue object for the destination queue
    MQQueue destinationQueue = new MQQueue(QUEUE_NAME);

    // Create the MQ message to send
    MQMessage mqMessage = new MQMessage();
    
    // Set MQIIH headers manually
    // Set the StrucId and other IIH-specific headers
    mqMessage.setStringProperty("StrucId", "IIH "); // Ensure this is the correct value for IIH
    mqMessage.setIntProperty("StrucLength", 84);   // Correct structure length for IIH (should be 84)
    mqMessage.setIntProperty("Version", 1);         // Set version for IIH (usually 1)
    
    // Set other properties for the MQIIH header (optional, depends on the application)
    mqMessage.setStringProperty("Authenticator", "********");
    mqMessage.setStringProperty("Format", "********");
    mqMessage.setStringProperty("ReplyToFormat", "********");
    mqMessage.setStringProperty("LTermOverride", "********");
    mqMessage.setStringProperty("MFSMapName", "********");
    mqMessage.setIntProperty("SecurityScope", 'C');
    mqMessage.setIntProperty("CommitMode", '0');
    
    // Set the actual message content (mqRequest.getMessage()) in the body of the message
    mqMessage.writeUTF(mqRequest.getMessage());  // Assuming your message is a UTF string

    // Set the destination queue
    destinationQueue.setQueueName(QUEUE_NAME);

    // Use the JmsTemplate to send the message with MQIIH headers
    jmsTemplate.convertAndSend(destinationQueue, mqMessage);

    log.info("Message with MQIIH headers sent to queue [{}].", QUEUE_NAME);
}

log.info("sending with message [{}] to queue [{}].", mqRequest.getMessage(), QUEUE_NAME);
// Create an MQQueue object for the destination queue
MQQueue destinationQueue = new MQQueue(QUEUE_NAME);
// Create the MQ message to send
MQMessage mqMessage = new MQMessage();
// Set MQIIH headers manually
// Set the StrucId and other IIH-specific headers
mqMessage.setStringProperty("StrucId", "IIH "); // Ensure this is the correct value for IIH
mqMessage.setIntProperty("StrucLength", 84);   // Correct structure length for IIH (should be 84)
mqMessage.setIntProperty("Version", 1);         // Set version for IIH (usually 1)
// Set other properties for the MQIIH header (optional, depends on the application)
mqMessage.setStringProperty("Authenticator", "********");
mqMessage.setStringProperty("Format", "********");
mqMessage.setStringProperty("ReplyToFormat", "********");
mqMessage.setStringProperty("LTermOverride", "********");
mqMessage.setStringProperty("MFSMapName", "********");
mqMessage.setIntProperty("SecurityScope", 'C');
mqMessage.setIntProperty("CommitMode", '0');
// Set the actual message content (mqRequest.getMessage()) in the body of the message
mqMessage.writeUTF(mqRequest.getMessage());  // Assuming your message is a UTF string
// Set the destination queue
destinationQueue.setQueueName(QUEUE_NAME);
// Use the JmsTemplate to send the message with MQIIH headers
jmsTemplate.convertAndSend(destinationQueue, mqMessage);
log.info("Message with MQIIH headers sent to queue [{}].", QUEUE_NAME);
 - `StrucId`: Set this to `"IIH "` to specify that it is an MQIIH message.
 - `StrucLength`: Set to `84`, which is the standard length of the IIH header.
 - `Version`: Set to `1` for the initial version.