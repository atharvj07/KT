int list = 1;
   String str = "XY";

   switch(list) {
       case 1:
           // Handle case 1
           break;
       case 2:
           // Handle case 2
           break;
       default:
           // Handle default case for int
   }

   switch(str) {
       case "XY":
           // Handle case XY for String
           break;
       default:
           // Handle default case for String
   }
   
   int list = 1;
   String listStr = Integer.toString(list);

   switch(listStr) {
       case "1":
           // Handle case for "1"
           break;
       case "2":
           // Handle case for "2"
           break;
       default:
           // Handle default case
   }
   
   if (list == 1) {
       // Handle case 1
   } else if (list == 2) {
       // Handle case 2
   } else if ("XY".equals(str)) {
       // Handle case XY for String
   }
   
   Map<String, Runnable> map = new HashMap<>();
   map.put("1", () -> { /* Handle case 1 */ });
   map.put("2", () -> { /* Handle case 2 */ });
   map.put("XY", () -> { /* Handle XY */ });

   String listStr = Integer.toString(list);
   map.getOrDefault(listStr, () -> { /* Default case */ }).run();
   
   case 1:
       // Handle case 1
       break;
   case 2:
       // Handle case 2
       break;
   default:
       // Handle default case for int
   case "XY":
       // Handle case XY for String
       break;
   default:
       // Handle default case for String
   case "1":
       // Handle case for "1"
       break;
   case "2":
       // Handle case for "2"
       break;
   default:
       // Handle default case
   // Handle case 1
   // Handle case 2
   // Handle case XY for String