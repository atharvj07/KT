import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;

public abstract class AbstractData {
    public final String id;
    public final String type;
    public final String name;

    @JsonCreator
    public AbstractData(@JsonProperty("id") String id,
                        @JsonProperty("type") String type,
                        @JsonProperty("name") String name) {
        this.id = id;
        this.type = type;
        this.name = name;
    }
}

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;

import java.io.IOException;

public class AbstractDataDeserializer extends JsonDeserializer<AbstractData> {
    private final ObjectMapper mapper = new ObjectMapper();

    @Override
    public AbstractData deserialize(JsonParser p, DeserializationContext ctxt) throws IOException {
        JsonNode node = p.getCodec().readTree(p);
        String type = node.has("type") ? node.get("type").asText() : "unknown";

        if ("type1".equals(type)) {
            return mapper.treeToValue(node, Type1Data.class);
        } else if ("type2".equals(type)) {
            return mapper.treeToValue(node, Type2Data.class);
        } else {
            return mapper.treeToValue(node, DefaultData.class);
        }
    }
}

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;

@JsonDeserialize(using = AbstractDataDeserializer.class)
public abstract class AbstractData {
    // Keep the same fields and constructor
}

import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.core.type.TypeReference;
import java.util.List;

public class Main {
    public static void main(String... args) throws Exception {
        String json = """
        {
          "data": [
            { "id": "id1", "type": "type1", "name": "John" },
            { "id": "id2", "type": "type2", "name": "Rebecca" },
            { "id": "id3", "type": "unknown", "name": "Peter" }
          ]
        }
        """;

        ObjectMapper mapper = new ObjectMapper();
        DataWrapper wrapper = mapper.readValue(json, DataWrapper.class);

        for (AbstractData data : wrapper.data) {
            System.out.println(data.getClass().getSimpleName() + " -> " + data.name);
        }
    }
}

class DataWrapper {
    public List<AbstractData> data;
}

Type1Data -> John
Type2Data -> Rebecca
DefaultData -> Peter

public final String id;
public final String type;
public final String name;
@JsonCreator
public AbstractData(@JsonProperty("id") String id,
                    @JsonProperty("type") String type,
                    @JsonProperty("name") String name) {
    this.id = id;
    this.type = type;
    this.name = name;
}
private final ObjectMapper mapper = new ObjectMapper();
@Override
public AbstractData deserialize(JsonParser p, DeserializationContext ctxt) throws IOException {
    JsonNode node = p.getCodec().readTree(p);
    String type = node.has("type") ? node.get("type").asText() : "unknown";
    if ("type1".equals(type)) {
        return mapper.treeToValue(node, Type1Data.class);
    } else if ("type2".equals(type)) {
        return mapper.treeToValue(node, Type2Data.class);
    } else {
        return mapper.treeToValue(node, DefaultData.class);
    }
}
// Keep the same fields and constructor
public static void main(String... args) throws Exception {
    String json = """
    {
      "data": [
        { "id": "id1", "type": "type1", "name": "John" },
        { "id": "id2", "type": "type2", "name": "Rebecca" },
        { "id": "id3", "type": "unknown", "name": "Peter" }
      ]
    }
    """;
    ObjectMapper mapper = new ObjectMapper();
    DataWrapper wrapper = mapper.readValue(json, DataWrapper.class);
    for (AbstractData data : wrapper.data) {
        System.out.println(data.getClass().getSimpleName() + " -> " + data.name);
    }
}
public List<AbstractData> data;