public class CustomLabel extends JLabel {
       public CustomLabel(String text) {
           super(text.isEmpty() ? " " : text);
       }
   }
   
   public CustomLabel(String text) {
       super(text.isEmpty() ? " " : text);
   }