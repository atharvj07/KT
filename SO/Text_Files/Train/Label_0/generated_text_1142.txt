package com.example.plugin;

import android.content.Context;
import android.os.Bundle;

import com.getcapacitor.Plugin;
import com.getcapacitor.annotation.CapacitorPlugin;

@CapacitorPlugin(name = "MyPlugin")
public class MyPlugin extends Plugin {

    @Override
    public void load() {
        super.load();

        // Access the context using getContext() method
        Context context = getContext();

        // Example of using the context to get system services
        if (context != null) {
            // For example, to get the LocationManager system service
            Object locationManager = context.getSystemService(Context.LOCATION_SERVICE);

            // You can now interact with the LocationManager or other system services
        }
    }
}

LocationManager locationManager = (LocationManager) context.getSystemService(Context.LOCATION_SERVICE);

@Override
public void load() {
    super.load();
    // Access the context using getContext() method
    Context context = getContext();
    // Example of using the context to get system services
    if (context != null) {
        // For example, to get the LocationManager system service
        Object locationManager = context.getSystemService(Context.LOCATION_SERVICE);
        // You can now interact with the LocationManager or other system services
    }
}