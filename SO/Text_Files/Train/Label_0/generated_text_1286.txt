boolean[][] mazeArray = new boolean[width][height]; // Your maze
int[] location = {1, 0}; // Starting position (row, column)

// Set the exit position (row, column)
int exitRow = 3;
int exitColumn = 4;

while (location[0] != exitRow || location[1] != exitColumn) {
    // Your traversal logic here
    
    // Example update for location, this will depend on your traversal algorithm
    // For example, move right:
    if (location[1] < mazeArray[0].length - 1 && !mazeArray[location[0]][location[1] + 1]) {
        location[1]++;
    }
    // Check for other movements (up, down, left) depending on your maze traversal logic
}

// If the loop exits, the location is equal to the exit position
System.out.println("Exit reached at position: " + location[0] + ", " + location[1]);

   while (location[0] != exitRow || location[1] != exitColumn)
   
// Your traversal logic here
// Example update for location, this will depend on your traversal algorithm
// For example, move right:
if (location[1] < mazeArray[0].length - 1 && !mazeArray[location[0]][location[1] + 1]) {
    location[1]++;
}
// Check for other movements (up, down, left) depending on your maze traversal logic