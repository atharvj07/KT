from("rest:get:/services/javadsl/weather/{city}?produces=application/json")
         .outputType(WeatherDto.class)
         .process(this::getWeatherData);
     
     camel:
       component:
         servlet:
           mapping: /services/*
           enabled: true
     
   from("rest:get:/services/javadsl/weather/{city}?produces=application/json")
       .routeId("weather-route")
       .log("Received request for weather in city: ${header.city}")
       .outputType(WeatherDto.class)
       .process(this::getWeatherData);
   
   from("direct:get-weather")
       .log("Direct call received")
       .outputType(WeatherDto.class)
       .process(this::getWeatherData);
   
@Component
@RequiredArgsConstructor
public class RestJavaDsl extends RouteBuilder {

    private final WeatherDataProvider weatherDataProvider;

    @Override
    public void configure() throws Exception {
        // Ensure proper mapping and log
        from("rest:get:/services/javadsl/weather/{city}?produces=application/json")
            .routeId("weather-route")
            .log("Received request for weather in city: ${header.city}")
            .outputType(WeatherDto.class)
            .process(this::getWeatherData);
    }

    private void getWeatherData(Exchange exchange) {
        String city = exchange.getMessage().getHeader("city", String.class);
        WeatherDto currentWeather = weatherDataProvider.getCurrentWeather(city);

        Message message = new DefaultMessage(exchange.getContext());
        message.setBody(currentWeather);
        exchange.setMessage(message);
    }
}

camel:
  component:
    servlet:
      mapping: /services/*
      enabled: true

<dependency>
    <groupId>org.apache.camel.springboot</groupId>
    <artifactId>camel-servlet-starter</artifactId>
    <version>${camel.version}</version>
</dependency>
<dependency>
    <groupId>org.apache.camel.springboot</groupId>
    <artifactId>camel-rest-starter</artifactId>
    <version>${camel.version}</version>
</dependency>

 ```java
 from("rest:get:/services/javadsl/weather/{city}?produces=application/json")
     .outputType(WeatherDto.class)
     .process(this::getWeatherData);
 ```
 This ensures that the endpoint matches the correct URL structure: `http://localhost:8080/services/javadsl/weather/{city}`.
 ```yaml
 camel:
   component:
     servlet:
       mapping: /services/*
       enabled: true
 ```
 This configuration ensures that the `/services/*` path is handled by Camel's servlet.
   .routeId("weather-route")
   .log("Received request for weather in city: ${header.city}")
   .outputType(WeatherDto.class)
   .process(this::getWeatherData);
   .log("Direct call received")
   .outputType(WeatherDto.class)
   .process(this::getWeatherData);
private final WeatherDataProvider weatherDataProvider;
@Override
public void configure() throws Exception {
    // Ensure proper mapping and log
    from("rest:get:/services/javadsl/weather/{city}?produces=application/json")
        .routeId("weather-route")
        .log("Received request for weather in city: ${header.city}")
        .outputType(WeatherDto.class)
        .process(this::getWeatherData);
}
private void getWeatherData(Exchange exchange) {
    String city = exchange.getMessage().getHeader("city", String.class);
    WeatherDto currentWeather = weatherDataProvider.getCurrentWeather(city);
    Message message = new DefaultMessage(exchange.getContext());
    message.setBody(currentWeather);
    exchange.setMessage(message);
}
servlet:
  mapping: /services/*
  enabled: true
<groupId>org.apache.camel.springboot</groupId>
<artifactId>camel-servlet-starter</artifactId>
<version>${camel.version}</version>
<groupId>org.apache.camel.springboot</groupId>
<artifactId>camel-rest-starter</artifactId>
<version>${camel.version}</version>