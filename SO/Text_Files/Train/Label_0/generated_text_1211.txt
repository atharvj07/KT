private static byte[] getNewKeyAndIvThatSupportPBKDF(char[] source, byte[] salt) throws EncryptionDecryptionException {
    try {
        // OpenSSL default PBKDF2 iterations (check OpenSSL docs, often 10,000)
        int iterations = 10000;  
        int keyIvLength = keylen + ivlen; // 32-byte key + 16-byte IV = 48 bytes

        PBEKeySpec spec = new PBEKeySpec(source, salt, iterations, keyIvLength * 8);
        SecretKeyFactory skf = SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256");
        return skf.generateSecret(spec).getEncoded();
    } catch (InvalidKeySpecException | NoSuchAlgorithmException e) {
        throw new EncryptionDecryptionException(e.getMessage(), e);
    }
}

echo -n "password" | openssl enc -aes-256-cbc -a -k secretKey -pbkdf2 -iter 10000

private static byte[] getKeyAndIv(final byte[] passAndSalt) throws NoSuchAlgorithmException {
    MessageDigest md = MessageDigest.getInstance("MD5"); // OpenSSL legacy method
    byte[] keyAndIv = new byte[keylen + ivlen];
    byte[] hash = new byte[0];

    int copied = 0;
    while (copied < keyAndIv.length) {
        md.update(hash);
        md.update(passAndSalt);
        hash = md.digest();
        
        int len = Math.min(hash.length, keyAndIv.length - copied);
        System.arraycopy(hash, 0, keyAndIv, copied, len);
        copied += len;
    }

    return keyAndIv;
}

echo -n "password" | openssl enc -aes-256-cbc -a -k secretKey

try {
    // OpenSSL default PBKDF2 iterations (check OpenSSL docs, often 10,000)
    int iterations = 10000;  
    int keyIvLength = keylen + ivlen; // 32-byte key + 16-byte IV = 48 bytes
    PBEKeySpec spec = new PBEKeySpec(source, salt, iterations, keyIvLength * 8);
    SecretKeyFactory skf = SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256");
    return skf.generateSecret(spec).getEncoded();
} catch (InvalidKeySpecException | NoSuchAlgorithmException e) {
    throw new EncryptionDecryptionException(e.getMessage(), e);
}
MessageDigest md = MessageDigest.getInstance("MD5"); // OpenSSL legacy method
byte[] keyAndIv = new byte[keylen + ivlen];
byte[] hash = new byte[0];
int copied = 0;
while (copied < keyAndIv.length) {
    md.update(hash);
    md.update(passAndSalt);
    hash = md.digest();
    
    int len = Math.min(hash.length, keyAndIv.length - copied);
    System.arraycopy(hash, 0, keyAndIv, copied, len);
    copied += len;
}
return keyAndIv;