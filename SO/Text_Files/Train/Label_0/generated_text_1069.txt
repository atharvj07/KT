return apiService.getAll(entry)
      .flatMap(response -> {
          if (response.getId() != null) {
              // Do some logic
              return Mono.just("some Mono");
          } else {
              return Mono.empty();
          }
      })
      .switchIfEmpty(Mono.empty()) // Ensure that if the Flux is empty, we provide an empty Mono to avoid the `last()` error
      .last() // Now it is safe to call last() as switchIfEmpty has ensured there is at least an empty signal
      .switchIfEmpty(Mono.empty()) // In case `last()` doesn't emit, you can return an empty Mono here as well
      // More flatMap operators can follow

  .flatMap(response -> {
      if (response.getId() != null) {
          // Do some logic
          return Mono.just("some Mono");
      } else {
          return Mono.empty();
      }
  })
  .switchIfEmpty(Mono.empty()) // Ensure that if the Flux is empty, we provide an empty Mono to avoid the `last()` error
  .last() // Now it is safe to call last() as switchIfEmpty has ensured there is at least an empty signal
  .switchIfEmpty(Mono.empty()) // In case `last()` doesn't emit, you can return an empty Mono here as well
  // More flatMap operators can follow