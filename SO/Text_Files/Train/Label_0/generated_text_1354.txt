#include <stdio.h>
     #include <stdlib.h>
     #include <opcodes.h>  // Or equivalent depending on the exact library

     void disassemble_function(void *func_addr) {
         // Use libopcodes to disassemble starting at func_addr
         // You may need to use functions like 'disassemble' to break down the instructions
     }

     int main() {
         void *func_addr = (void *)some_function;  // Address of the function
         disassemble_function(func_addr);
         return 0;
     }
     
 - First, you'd need to locate the memory address of the function you're interested in. This can be done by using `dlopen` (for dynamically linked libraries) or `mmap`/`/proc/self/maps` (for static executables) to locate the starting address of the function in memory.
 - You could use **libopcodes** (from the `binutils` suite) to disassemble the code at that memory address. Libopcodes provides the functionality to decode the machine code into assembly instructions.
 - You'd need to reverse-engineer the function to find where the loop starts and ends, and identify the conditional jump instructions (e.g., `jz`, `jne`, etc.) that control the loop.
 - A typical way to estimate the size of a function is to look at the symbol information provided by the compiler. In a simple case, functions are often marked with a symbol in the ELF headers, which you can access through tools like `objdump`.
 - However, if youÂre disassembling at runtime (without access to source code), youÂll need to either use debug information (if available) or infer the function's boundaries based on its disassembled instructions.
 Here's a simplified view of how you might use `libopcodes`:
 ```c
 #include <stdio.h>
 #include <stdlib.h>
 #include <opcodes.h>  // Or equivalent depending on the exact library
 void disassemble_function(void *func_addr) {
     // Use libopcodes to disassemble starting at func_addr
     // You may need to use functions like 'disassemble' to break down the instructions
 }
 int main() {
     void *func_addr = (void *)some_function;  // Address of the function
     disassemble_function(func_addr);
     return 0;
 }
 ```
 You'll need to link against the `libopcodes` library and possibly deal with different architectures (e.g., x86, ARM) if you're targeting multiple systems.