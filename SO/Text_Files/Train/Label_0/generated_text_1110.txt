import java.io.IOException;
import java.util.concurrent.*;

public class AudioProcessor {
    private long lastMethodBCallTime = System.currentTimeMillis(); // Timestamp when methodB is last called
    private final int N = 5; // N seconds, you can adjust this as needed
    private final ScheduledExecutorService scheduler = Executors.newSingleThreadScheduledExecutor();

    public AudioProcessor() {
        // Schedule a task to check if methodB was called recently
        scheduler.scheduleAtFixedRate(() -> {
            long currentTime = System.currentTimeMillis();
            if (currentTime - lastMethodBCallTime > N * 1000) {
                methodC();
            }
        }, 0, 1, TimeUnit.SECONDS); // Check every second (you can adjust this interval)
    }

    public void methodB(byte[] audioData) throws IOException {
        // Update the timestamp each time methodB is called
        lastMethodBCallTime = System.currentTimeMillis();

        byte[] transcodedVoice = transcodeVoice(audioData);
        recognizer.acceptWaveForm(transcodedVoice, transcodedVoice.length);
    }

    public void methodC() {
        // Do something
        System.out.println("Method B stops getting data");
    }

    private byte[] transcodeVoice(byte[] audioData) {
        // Transcoding logic here (example placeholder)
        return audioData;
    }

    // Dummy recognizer class for the sake of illustration
    private static class Recognizer {
        public void acceptWaveForm(byte[] transcodedVoice, int length) {
            // Process the audio data
        }
    }

    private Recognizer recognizer = new Recognizer(); // Example recognizer instance

    public static void main(String[] args) throws InterruptedException {
        AudioProcessor processor = new AudioProcessor();

        // Simulating methodB calls
        processor.methodB(new byte[]{1, 2, 3});
        Thread.sleep(3000); // Wait 3 seconds
        processor.methodB(new byte[]{4, 5, 6});
        Thread.sleep(7000); // Wait 7 seconds to trigger methodC()

        // Wait for the scheduler to trigger methodC if methodB hasn't been called
        Thread.sleep(10000); // Adjust this as needed
    }
}

private long lastMethodBCallTime = System.currentTimeMillis(); // Timestamp when methodB is last called
private final int N = 5; // N seconds, you can adjust this as needed
private final ScheduledExecutorService scheduler = Executors.newSingleThreadScheduledExecutor();
public AudioProcessor() {
    // Schedule a task to check if methodB was called recently
    scheduler.scheduleAtFixedRate(() -> {
        long currentTime = System.currentTimeMillis();
        if (currentTime - lastMethodBCallTime > N * 1000) {
            methodC();
        }
    }, 0, 1, TimeUnit.SECONDS); // Check every second (you can adjust this interval)
}
public void methodB(byte[] audioData) throws IOException {
    // Update the timestamp each time methodB is called
    lastMethodBCallTime = System.currentTimeMillis();
    byte[] transcodedVoice = transcodeVoice(audioData);
    recognizer.acceptWaveForm(transcodedVoice, transcodedVoice.length);
}
public void methodC() {
    // Do something
    System.out.println("Method B stops getting data");
}
private byte[] transcodeVoice(byte[] audioData) {
    // Transcoding logic here (example placeholder)
    return audioData;
}
// Dummy recognizer class for the sake of illustration
private static class Recognizer {
    public void acceptWaveForm(byte[] transcodedVoice, int length) {
        // Process the audio data
    }
}
private Recognizer recognizer = new Recognizer(); // Example recognizer instance
public static void main(String[] args) throws InterruptedException {
    AudioProcessor processor = new AudioProcessor();
    // Simulating methodB calls
    processor.methodB(new byte[]{1, 2, 3});
    Thread.sleep(3000); // Wait 3 seconds
    processor.methodB(new byte[]{4, 5, 6});
    Thread.sleep(7000); // Wait 7 seconds to trigger methodC()
    // Wait for the scheduler to trigger methodC if methodB hasn't been called
    Thread.sleep(10000); // Adjust this as needed
}