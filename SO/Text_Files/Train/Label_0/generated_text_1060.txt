import java.util.function.BiPredicate;

public class StringUtils {
    public static final BiPredicate<String, String> compareStrings = (s1, s2) -> s1 != null && s1.equals(s2);
}

import java.util.function.BiFunction;

public class StringUtils {
    public static final BiFunction<String, String, Boolean> compareStrings = (s1, s2) -> s1 != null && s1.equals(s2);
}

  BiPredicate<String, String> areEqual = (s1, s2) -> s1 != null && s1.equals(s2);
  System.out.println(areEqual.test("apple", "apple")); // true
  
  BiFunction<String, String, String> compareAndReturnMessage = (s1, s2) -> {
      if (s1 != null && s1.equals(s2)) {
          return "Strings are equal!";
      } else {
          return "Strings are not equal!";
      }
  };
  System.out.println(compareAndReturnMessage.apply("apple", "apple")); // Strings are equal!
  
public static String compareStrings(String s1, String s2) {
    return s1 != null && s1.equals(s2) ? "Equal" : "Not Equal";
}

public static final BiPredicate<String, String> compareStrings = (s1, s2) -> s1 != null && s1.equals(s2);
public static final BiFunction<String, String, Boolean> compareStrings = (s1, s2) -> s1 != null && s1.equals(s2);
  if (s1 != null && s1.equals(s2)) {
      return "Strings are equal!";
  } else {
      return "Strings are not equal!";
  }
return s1 != null && s1.equals(s2) ? "Equal" : "Not Equal";