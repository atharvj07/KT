vnfc.audit.ignore.list.tas[0]=ims_rmt_server
vnfc.audit.ignore.list.tas[1]=contact
vnfc.audit.ignore.list.ims[0]=product
vnfc.audit.ignore.list.ims[1]=kafka
vnfc.audit.ignore.list.uag[0]=realm
vnfc.audit.ignore.list.uag[1]=Cassandra

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.context.annotation.Configuration;
import java.util.Map;
import java.util.List;

@Configuration
@ConfigurationProperties(prefix = "vnfc.audit.ignore.list")
public class MyAppProperties {
    private Map<String, List<String>> map;

    public Map<String, List<String>> getMap() {
        return map;
    }

    public void setMap(Map<String, List<String>> map) {
        this.map = map;
    }
}

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;
import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

@Component
public class MyAppProperties {

    @Value("#{${vnfc.audit.ignore.list}}")
    private Map<String, String> rawProperties;

    public Map<String, List<String>> getParsedMap() {
        return rawProperties.entrySet().stream()
                .collect(Collectors.toMap(
                        Map.Entry::getKey,
                        entry -> Arrays.asList(entry.getValue().split(","))
                ));
    }
}

private Map<String, List<String>> map;
public Map<String, List<String>> getMap() {
    return map;
}
public void setMap(Map<String, List<String>> map) {
    this.map = map;
}
@Value("#{${vnfc.audit.ignore.list}}")
private Map<String, String> rawProperties;
public Map<String, List<String>> getParsedMap() {
    return rawProperties.entrySet().stream()
            .collect(Collectors.toMap(
                    Map.Entry::getKey,
                    entry -> Arrays.asList(entry.getValue().split(","))
            ));
}