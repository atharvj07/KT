String cadenaAmb0 = afegir0Multiple8(encriptar);  // Your string to encrypt
  byte[] cadenaRes = cadenaAmb0.getBytes();  // Convert to bytes
  
  // Clave
  byte[] hexclaub = DatatypeConverter.parseHexBinary(claveCifradoRedsa);
  SecretKeySpec key = new SecretKeySpec(hexclaub, "AES");
  
  // Vector de inicializaciÃ³n (nonce for GCM)
  String v = vectorInicial;  // You need to use a nonce (number used once)
  IvParameterSpec ivectorSpecv = new IvParameterSpec(v.getBytes("UTF-8"));
  
  // Encriptem amb GCM
  Cipher cipher = Cipher.getInstance("AES/GCM/NoPadding");
  cipher.init(Cipher.ENCRYPT_MODE, key, ivectorSpecv);
  
  byte[] encrypted = cipher.doFinal(cadenaRes);  // Encrypted data
  
  // Optionally, you can append the authentication tag to the ciphertext
  byte[] tag = cipher.getIV();  // Store the tag for verification during decryption