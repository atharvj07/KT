[pytest]
     testpaths = tests
     
   PYTHONPATH=.
   
pytest --rootdir=product-testing

  from conftest import grpc_pages  # ? BAD! This breaks pytest's discovery.
  
 ```ini
 [pytest]
 testpaths = tests
 ```