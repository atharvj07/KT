@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@TestPropertySource(properties = "server.internalPort=${random.int(20000,30000)}")
public abstract class AbstractSomethingIntegrationTest {

    @LocalServerPort
    private int port;

    @Value("${server.internalPort}")
    private int internalPort;

    @Test
    void testPorts() {
        System.out.println("Randomized server.port: " + port);
        System.out.println("Randomized server.internalPort: " + internalPort);
    }
}

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public abstract class AbstractSomethingIntegrationTest {

    @LocalServerPort
    private int port;

    @Value("${server.internalPort}")
    private int internalPort;

    @DynamicPropertySource
    static void configureInternalPort(DynamicPropertyRegistry registry) {
        int randomInternalPort = SocketUtils.findAvailableTcpPort();
        registry.add("server.internalPort", () -> randomInternalPort);
        System.out.println("Randomized server.internalPort: " + randomInternalPort);
    }

    @Test
    void testPorts() {
        System.out.println("Randomized server.port: " + port);
        System.out.println("Randomized server.internalPort: " + internalPort);
    }
}

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public abstract class AbstractSomethingIntegrationTest {

    @LocalServerPort
    private int port;

    @Value("${server.internalPort}")
    private int internalPort;

    @BeforeAll
    static void setup() {
        int randomInternalPort = SocketUtils.findAvailableTcpPort();
        System.setProperty("server.internalPort", String.valueOf(randomInternalPort));
        System.out.println("Randomized server.internalPort: " + randomInternalPort);
    }

    @Test
    void testPorts() {
        System.out.println("Randomized server.port: " + port);
        System.out.println("Randomized server.internalPort: " + internalPort);
    }
}

@LocalServerPort
private int port;
@Value("${server.internalPort}")
private int internalPort;
@Test
void testPorts() {
    System.out.println("Randomized server.port: " + port);
    System.out.println("Randomized server.internalPort: " + internalPort);
}
@LocalServerPort
private int port;
@Value("${server.internalPort}")
private int internalPort;
@DynamicPropertySource
static void configureInternalPort(DynamicPropertyRegistry registry) {
    int randomInternalPort = SocketUtils.findAvailableTcpPort();
    registry.add("server.internalPort", () -> randomInternalPort);
    System.out.println("Randomized server.internalPort: " + randomInternalPort);
}
@Test
void testPorts() {
    System.out.println("Randomized server.port: " + port);
    System.out.println("Randomized server.internalPort: " + internalPort);
}
@LocalServerPort
private int port;
@Value("${server.internalPort}")
private int internalPort;
@BeforeAll
static void setup() {
    int randomInternalPort = SocketUtils.findAvailableTcpPort();
    System.setProperty("server.internalPort", String.valueOf(randomInternalPort));
    System.out.println("Randomized server.internalPort: " + randomInternalPort);
}
@Test
void testPorts() {
    System.out.println("Randomized server.port: " + port);
    System.out.println("Randomized server.internalPort: " + internalPort);
}