import numpy
import polars

df = (polars
  .DataFrame(dict(
    j=numpy.random.randint(10, 99, 20),
  ))
  .with_row_count()
  .select(
    g=polars.col('row_nr') // 3,
    j='j'
  )
  .with_column(
    'rn', polars.col('j').rank().over('g') - 1
  )
)

print(df)