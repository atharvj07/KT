import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.DatePicker;
import javafx.scene.layout.StackPane;
import javafx.stage.Stage;

import java.time.LocalDate;
import java.util.Locale;

public class DatePickerLocalization extends Application {
    @Override
    public void start(Stage primaryStage) {
        // Set locale to German
        Locale.setDefault(Locale.GERMANY);

        DatePicker datePicker = new DatePicker();
        datePicker.setValue(LocalDate.now());

        StackPane root = new StackPane(datePicker);
        Scene scene = new Scene(root, 300, 200);
        
        primaryStage.setTitle("Localized DatePicker");
        primaryStage.setScene(scene);
        primaryStage.show();
    }

    public static void main(String[] args) {
        launch(args);
    }
}

   Locale.setDefault(Locale.GERMANY);
   
import java.time.format.DateTimeFormatter;
import java.util.Locale;

DateTimeFormatter formatter = DateTimeFormatter.ofPattern("MMMM yyyy", Locale.GERMANY);
System.out.println(LocalDate.now().format(formatter));  // Output: Juni 2024

@Override
public void start(Stage primaryStage) {
    // Set locale to German
    Locale.setDefault(Locale.GERMANY);
    DatePicker datePicker = new DatePicker();
    datePicker.setValue(LocalDate.now());
    StackPane root = new StackPane(datePicker);
    Scene scene = new Scene(root, 300, 200);
    
    primaryStage.setTitle("Localized DatePicker");
    primaryStage.setScene(scene);
    primaryStage.show();
}
public static void main(String[] args) {
    launch(args);
}