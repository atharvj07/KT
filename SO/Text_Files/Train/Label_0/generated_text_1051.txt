void update(){
  if(up){
    y-=7.5;
  }
  else if(down){
    y+=7.5;
  }
  else if(left){
    x-=7.5;
  }
  else if(right){
    x+=7.5;
  }
}

void update(){
  if (up) {
    y -= 7.5;
  }
  if (down) {
    y += 7.5;
  }
  if (left) {
    x -= 7.5;
  }
  if (right) {
    x += 7.5;
  }
}

void player(float x, float y){
    this.x = x;
    this.y = y;
}

Player(float x, float y) {
    this.x = x;
    this.y = y;
}

p = new Player(500, 500);

y-=7.5;
y+=7.5;
x-=7.5;
x+=7.5;
y -= 7.5;
y += 7.5;
x -= 7.5;
x += 7.5;
this.x = x;
this.y = y;
this.x = x;
this.y = y;