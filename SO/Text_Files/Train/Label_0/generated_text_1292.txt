import javafx.scene.media.*;
import javafx.util.Duration;

public class AudioTest {
    public static void main(String[] args) {
        String audioURI = "file:/path/to/your/audio/file.mp3";
        Media media = new Media(audioURI);
        MediaPlayer mediaPlayer = new MediaPlayer(media);

        mediaPlayer.setCycleCount(MediaPlayer.INDEFINITE);
        
        // Fade-in and fade-out effect
        mediaPlayer.setOnEndOfMedia(() -> {
            mediaPlayer.seek(Duration.ZERO); // Restart audio from the beginning
            mediaPlayer.play();
        });
        
        mediaPlayer.setVolume(0.0); // Start with volume at 0
        mediaPlayer.play();

        // Gradually fade in
        new Thread(() -> {
            try {
                for (int i = 0; i <= 100; i++) {
                    Thread.sleep(10); // Adjust fade duration
                    mediaPlayer.setVolume(i / 100.0);
                }
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }).start();
    }
}

import java.io.File;
import java.net.URI;
import javafx.scene.media.*;

public class AudioTest {
    public static void main(String[] args) {
        URI audioURI = new File("filePath").toURI();
        
        AudioClip audio = new AudioClip(audioURI.toString());
        audio.play();
        
        audio.setOnEndOfMedia(new Runnable() {
            @Override
            public void run() {
                audio.seek(Duration.ZERO); // Jump back to start
                audio.play(); // Play it again
            }
        });
        
        new Scanner(System.in).next(); // to prevent the program from closing immediately
    }
}

import javafx.scene.media.*;
import javafx.util.Duration;

public class AudioTest {
    public static void main(String[] args) {
        String audioURI = "file:/path/to/your/audio/file.mp3";
        Media media = new Media(audioURI);
        MediaPlayer mediaPlayer = new MediaPlayer(media);

        mediaPlayer.setCycleCount(MediaPlayer.INDEFINITE); // Loop indefinitely
        mediaPlayer.setOnEndOfMedia(() -> {
            mediaPlayer.seek(Duration.ZERO); // Jump back to the start
            mediaPlayer.play(); // Start playing again
        });
        
        mediaPlayer.play();
        
        new Scanner(System.in).next(); // to prevent the program from closing immediately
    }
}

public static void main(String[] args) {
    String audioURI = "file:/path/to/your/audio/file.mp3";
    Media media = new Media(audioURI);
    MediaPlayer mediaPlayer = new MediaPlayer(media);
    mediaPlayer.setCycleCount(MediaPlayer.INDEFINITE);
    
    // Fade-in and fade-out effect
    mediaPlayer.setOnEndOfMedia(() -> {
        mediaPlayer.seek(Duration.ZERO); // Restart audio from the beginning
        mediaPlayer.play();
    });
    
    mediaPlayer.setVolume(0.0); // Start with volume at 0
    mediaPlayer.play();
    // Gradually fade in
    new Thread(() -> {
        try {
            for (int i = 0; i <= 100; i++) {
                Thread.sleep(10); // Adjust fade duration
                mediaPlayer.setVolume(i / 100.0);
            }
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }).start();
}
public static void main(String[] args) {
    URI audioURI = new File("filePath").toURI();
    
    AudioClip audio = new AudioClip(audioURI.toString());
    audio.play();
    
    audio.setOnEndOfMedia(new Runnable() {
        @Override
        public void run() {
            audio.seek(Duration.ZERO); // Jump back to start
            audio.play(); // Play it again
        }
    });
    
    new Scanner(System.in).next(); // to prevent the program from closing immediately
}
public static void main(String[] args) {
    String audioURI = "file:/path/to/your/audio/file.mp3";
    Media media = new Media(audioURI);
    MediaPlayer mediaPlayer = new MediaPlayer(media);
    mediaPlayer.setCycleCount(MediaPlayer.INDEFINITE); // Loop indefinitely
    mediaPlayer.setOnEndOfMedia(() -> {
        mediaPlayer.seek(Duration.ZERO); // Jump back to the start
        mediaPlayer.play(); // Start playing again
    });
    
    mediaPlayer.play();
    
    new Scanner(System.in).next(); // to prevent the program from closing immediately
}