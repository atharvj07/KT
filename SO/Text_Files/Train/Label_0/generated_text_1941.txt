import contextlib

class Foo:
    @contextlib.contextmanager
    @classmethod
    def state(cls):
        try:
            yield
        finally:
            pass

# When using the context manager, call the class method on the class itself
with Foo.state():
    pass