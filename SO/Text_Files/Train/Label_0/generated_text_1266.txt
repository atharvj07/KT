import lombok.Builder;
import lombok.NonNull;
import lombok.Value;

@Value
@Builder(toBuilder = true)
public class Testing {
    
    @NonNull private String id;
    @NonNull private TestEnum test;
    private String message;

    public static TestingBuilder builder() {
        return new TestingBuilder() {
            @Override
            public Testing build() {
                if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
                    throw new IllegalArgumentException("test must be ALPHA or BETA");
                }
                return super.build();
            }
        };
    }
}

import lombok.Builder;
import lombok.NonNull;
import lombok.Value;

@Value
@Builder(toBuilder = true)
public class Testing {

    @NonNull private String id;
    @NonNull private TestEnum test;
    private String message;

    private Testing(String id, TestEnum test, String message) {
        if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
            throw new IllegalArgumentException("test must be ALPHA or BETA");
        }
        this.id = id;
        this.test = test;
        this.message = message;
    }
}

import lombok.Builder;
import lombok.NonNull;
import lombok.Value;

@Value
@Builder(toBuilder = true)
public class Testing {
    
    @NonNull private String id;
    @NonNull private TestEnum test;
    private String message;

    public static class TestingBuilder {
        public TestingBuilder test(TestEnum test) {
            if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
                throw new IllegalArgumentException("test must be ALPHA or BETA");
            }
            this.test = test;
            return this;
        }
    }
}

@NonNull private String id;
@NonNull private TestEnum test;
private String message;
public static TestingBuilder builder() {
    return new TestingBuilder() {
        @Override
        public Testing build() {
            if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
                throw new IllegalArgumentException("test must be ALPHA or BETA");
            }
            return super.build();
        }
    };
}
@NonNull private String id;
@NonNull private TestEnum test;
private String message;
private Testing(String id, TestEnum test, String message) {
    if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
        throw new IllegalArgumentException("test must be ALPHA or BETA");
    }
    this.id = id;
    this.test = test;
    this.message = message;
}
@NonNull private String id;
@NonNull private TestEnum test;
private String message;
public static class TestingBuilder {
    public TestingBuilder test(TestEnum test) {
        if (test != TestEnum.ALPHA && test != TestEnum.BETA) {
            throw new IllegalArgumentException("test must be ALPHA or BETA");
        }
        this.test = test;
        return this;
    }
}