public void tryLockExample() {
    if (lock.tryLock()) {
        try {
            // Critical section
            ++counter;
        } finally {
            lock.unlock();
        }
    } else {
        // Lock was not acquired, handle alternative behavior
    }
}