java -XX:+UnlockCommercialFeatures -XX:+FlightRecorder -XX:StartFlightRecording=filename=recording.jfr
   
   jcmd <PID> JFR.start name=allocations settings=profile
   
jcmd <PID> VM.native_memory summary

  ./profiler.sh -e alloc -t -i 5ms -f profile.html <PID>
  
public class MemoryTracker {
    private static final ThreadLocal<Long> allocatedBytes = ThreadLocal.withInitial(() -> 0L);

    public static void allocate(long bytes) {
        allocatedBytes.set(allocatedBytes.get() + bytes);
    }

    public static long getAllocatedBytes() {
        return allocatedBytes.get();
    }
}

byte[] data = new byte[1024]; // 1 KB allocation
MemoryTracker.allocate(1024);
System.out.println("Thread allocated: " + MemoryTracker.getAllocatedBytes() + " bytes");

private static final ThreadLocal<Long> allocatedBytes = ThreadLocal.withInitial(() -> 0L);
public static void allocate(long bytes) {
    allocatedBytes.set(allocatedBytes.get() + bytes);
}
public static long getAllocatedBytes() {
    return allocatedBytes.get();
}