import com.fasterxml.jackson.databind.ObjectMapper;
import com.mongodb.client.MongoCollection;
import org.bson.Document;
import java.util.List;
import java.util.stream.Collectors;
import java.util.ArrayList;

public class Main {
    private static final ObjectMapper objectMapper = new ObjectMapper();

    public static List<Person> getPersons(MongoCollection<Document> collection, int offset, int limit) {
        List<Document> documents = collection.find()
                .skip(offset)
                .limit(limit)
                .into(new ArrayList<>());

        // Convert Documents to Person objects
        return documents.stream()
                .map(doc -> objectMapper.convertValue(doc, Person.class))
                .collect(Collectors.toList());
    }
}

import com.mongodb.client.MongoClients;
import com.mongodb.client.MongoCollection;
import com.mongodb.MongoClientSettings;
import org.bson.codecs.configuration.CodecRegistry;
import org.bson.codecs.configuration.CodecRegistries;
import org.bson.codecs.pojo.PojoCodecProvider;

public class Main {
    public static void main(String[] args) {
        // Set up CodecRegistry for POJOs
        CodecRegistry pojoCodecRegistry = CodecRegistries.fromRegistries(
                MongoClientSettings.getDefaultCodecRegistry(),
                CodecRegistries.fromProviders(PojoCodecProvider.builder().automatic(true).build()));

        MongoCollection<Person> collection = MongoClients.create("mongodb://localhost:27017")
                .getDatabase("mydb")
                .getCollection("people", Person.class)
                .withCodecRegistry(pojoCodecRegistry);

        List<Person> persons = collection.find()
                .skip(offset)
                .limit(limit)
                .into(new ArrayList<>()); // Works directly

        System.out.println(persons);
    }
}

private static final ObjectMapper objectMapper = new ObjectMapper();
public static List<Person> getPersons(MongoCollection<Document> collection, int offset, int limit) {
    List<Document> documents = collection.find()
            .skip(offset)
            .limit(limit)
            .into(new ArrayList<>());
    // Convert Documents to Person objects
    return documents.stream()
            .map(doc -> objectMapper.convertValue(doc, Person.class))
            .collect(Collectors.toList());
}
public static void main(String[] args) {
    // Set up CodecRegistry for POJOs
    CodecRegistry pojoCodecRegistry = CodecRegistries.fromRegistries(
            MongoClientSettings.getDefaultCodecRegistry(),
            CodecRegistries.fromProviders(PojoCodecProvider.builder().automatic(true).build()));
    MongoCollection<Person> collection = MongoClients.create("mongodb://localhost:27017")
            .getDatabase("mydb")
            .getCollection("people", Person.class)
            .withCodecRegistry(pojoCodecRegistry);
    List<Person> persons = collection.find()
            .skip(offset)
            .limit(limit)
            .into(new ArrayList<>()); // Works directly
    System.out.println(persons);
}