private <S> void aaa(List<S> list) {
    list.add(list.get(0));  // Now this works because list is a List<S>
}

private void aaa() {
    List<S> list = (List<S>) this.list;  // Cast the wildcard list to a specific type
    list.add(list.get(0));
}

class Foo<T> {
    List<T> list;

    private void aaa() {
        list.add(list.get(0));  // This works because list is a List<T>
    }
}

list.add(list.get(0));  // Now this works because list is a List<S>
List<S> list = (List<S>) this.list;  // Cast the wildcard list to a specific type
list.add(list.get(0));
List<T> list;
private void aaa() {
    list.add(list.get(0));  // This works because list is a List<T>
}