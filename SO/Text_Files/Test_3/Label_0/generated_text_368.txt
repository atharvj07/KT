from icu import Collator, Locale

def create_multi_locale_sorter(locales):
    collators = [Collator.createInstance(Locale(loc)) for loc in locales]

    def sort_key(s):
        return tuple(coll.getSortKey(s) for coll in collators)

    return sort_key
