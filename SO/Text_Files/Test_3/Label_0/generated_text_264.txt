from fastapi import FastAPI, Request, Response

app = FastAPI()

@app.get("/")
def read_root():
    return {"Hello": "World"}

@app.post("/input")
def input_request(request: Request):
    body = request.stream()  # Use stream() for sync access to the raw body
    return Response(content=body, media_type="application/xml")
