from llama_cpp import Llama

def run_gguf_inference(model_path: str, prompt: str, max_tokens: int = 100):
    llm = Llama(model_path=model_path)
    return llm(prompt, max_tokens=max_tokens)["choices"][0]["text"]

print(run_gguf_inference("path/to/model.gguf", "Hello, how are you?"))
