def flatten(arr):
    return [item for sublist in arr for item in (flatten(sublist) if isinstance(sublist, list) else [sublist])]

arr_1 = [1, [2, 3], 4, 5]
arr_2 = [1, [2, 3], [[4, 5]]]

print(flatten(arr_1))
print(flatten(arr_2))
