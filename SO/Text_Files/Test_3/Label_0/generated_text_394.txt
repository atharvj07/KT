from datetime import datetime
import time

for i in range(10_000_000):
    time.sleep(0)  # Add a small sleep to avoid tight loop issues
    first = datetime.utcnow()
    second = datetime.utcnow()

    assert first <= second, f"{first=} {second=} {i=}"
