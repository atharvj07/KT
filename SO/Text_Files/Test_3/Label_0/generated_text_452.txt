import polars as pl

def fill_nulls_with_another_column(df: pl.DataFrame, col1: str, col2: str) -> pl.DataFrame:
    return df.with_columns(
        pl.col(col1).fill_null(pl.col(col2)).alias(col1)
    )
