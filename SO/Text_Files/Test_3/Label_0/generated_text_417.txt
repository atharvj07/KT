from pyspark.sql.functions import col

def apply_schema(df1, df2):
    for column in df1.columns:
        df2 = df2.withColumn(column, col(column).cast(df1.schema[column].dataType))
    return df2
