protected void fetchAll() {
    final DatabaseReference reference = FirebaseDatabase.getInstance().getReference("Category_wise");
    reference.addValueEventListener(new ValueEventListener() {
        @Override
        public void onDataChange(@NonNull DataSnapshot dataSnapshot) {
            for (DataSnapshot categorySnapshot : dataSnapshot.getChildren()) {
                for (DataSnapshot companySnapshot : categorySnapshot.getChildren()) {
                    String companyName = Objects.requireNonNull(companySnapshot.child("companyName").getValue()).toString();
                    String totalInvestors = Objects.requireNonNull(companySnapshot.child("totalInvestors").getValue()).toString();
                    // Add data to your model and pass it to adapter if needed
                    SCard model = new SCard(companyName, totalInvestors);
                    // Do something with model (like adding to list or updating RecyclerView)
                }
            }
        }

        @Override
        public void onCancelled(@NonNull DatabaseError databaseError) {}
    });
}
