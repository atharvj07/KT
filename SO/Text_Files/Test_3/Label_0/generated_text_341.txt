import polars as pl

def remove_null_rows(df: pl.DataFrame) -> pl.DataFrame:
    col_list = [col for col in df.columns if col.startswith("eat")]
    return df.filter(~pl.fold(True, lambda acc, s: acc & s.is_null(), pl.all().exclude("id")))

