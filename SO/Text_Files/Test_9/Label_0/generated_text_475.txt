import polars as pl
import duckdb

df = pl.DataFrame({'mynum': [1, 2, 3, 4]})
con = duckdb.connect("scratch.db")
con.execute("CREATE TABLE IF NOT EXISTS test_table (mynum INTEGER)")
df.write_database(table_name='test_table', connection=con)
con.close()
