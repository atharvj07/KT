import serial
import time

def wr(ser, msg):
    ser.write(msg.encode())

def rd(ser):
    return ser.readline().decode().strip()

ser = serial.Serial('/dev/ttyUSB0', 115200, timeout=1)
time.sleep(2)

wr(ser, 'hello\n')
print(rd(ser))

ser.close()
