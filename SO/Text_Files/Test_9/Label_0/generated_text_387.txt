from pydantic import BaseModel, Field, field_validator

class Mod(BaseModel):
    num: int = Field(...)

    @field_validator("num", mode="before")
    def chk(cls, v):
        return int(v)

m = Mod(num="123")
print(m)
