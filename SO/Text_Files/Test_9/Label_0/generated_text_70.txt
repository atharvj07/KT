List<InsertOneModel<Document>> out1 = puzzles.parallelStream()
    .map(gson::toJson)
    .map(Document::parse)
    .map(InsertOneModel::new)
    .toList();

List<InsertOneModel<Document>> out2 = puzzles.parallelStream()
    .map(p -> new InsertOneModel<>(Document.parse(gson.toJson(p))))
    .toList();
