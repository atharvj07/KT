ENVS=$(conda env list | grep "RUN_ENV" )
if [[ -z "$ENVS" ]]; then
   conda env create --name RUN_ENV --file ../run_env.yml -q
   conda activate RUN_ENV
   exit
else
   conda activate RUN_ENV
fi
python run_app.py
conda deactivate
