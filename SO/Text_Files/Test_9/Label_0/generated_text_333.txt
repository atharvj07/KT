# scripts/build.py
import subprocess

def main():
    subprocess.run(
        ["pyinstaller", "main.py", "--collect-submodules", "application", "--onefile", "--name", "myapi"],
        check=True
    )
