import inspect
import re

def fct1():
    frm = inspect.stack()[1]
    src = inspect.getsource(frm.frame)
    pat = rf"(\w+)\s*=\s*{frm.function}\("
    mtc = re.search(pat, src)
    return {"name": mtc.group(1)} if mtc else None

a = fct1()
print(a)
