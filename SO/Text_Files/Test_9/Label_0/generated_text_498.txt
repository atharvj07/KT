import attr
import pandas as pd

@attr.s
class MyDataClass:
    i = attr.ib()
    s = attr.ib()

lst = [MyDataClass("a", 1), MyDataClass("b", 2)]

df = pd.DataFrame([x.__dict__ for x in lst])
