import random

def generate_numbers():
    while True:
        numbers = [random.randint(2, 6) for _ in range(10)]
        if sum(numbers) == 40:
            return numbers

print(generate_numbers())
