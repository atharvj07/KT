import xarray as xr

def select_1d_xarray(DS, x, y):
    return xr.concat([DS.sel(longitude=xi, latitude=yi, method='nearest') for xi, yi in zip(x, y)], dim="index")

# Example usage:
# subset = select_1d_xarray(Dataset, [x1, x2, x3, x4], [y1, y2, y3, y4])
