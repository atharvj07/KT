import zstandard as zstd

def process_zst_file(filename, process_line):
    with open(filename, "rb") as f:
        dctx = zstd.ZstdDecompressor()
        with dctx.stream_reader(f) as reader:
            for line in reader.read().decode().splitlines():
                process_line(line)
