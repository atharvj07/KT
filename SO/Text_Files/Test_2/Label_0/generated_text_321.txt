import pandas as pd
from tqdm import tqdm

def apply_with_progress(df, func, column):
    tqdm.pandas(desc="Processing")
    return df[column].progress_apply(func)

# Example usage
# df = pd.DataFrame({'textbody': ['some text', 'another text', 'more text']})
# df['processed'] = apply_with_progress(df, func1, 'textbody')
