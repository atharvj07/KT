import pandas as pd
import attr

@attr.s(auto_attribs=True)
class MyAttrsClass:
    i: int
    s: str

def attrs_to_dataframe(objects):
    return pd.DataFrame([attr.asdict(obj) for obj in objects])

df = attrs_to_dataframe([MyAttrsClass("a", 1), MyAttrsClass("b", 2)])
