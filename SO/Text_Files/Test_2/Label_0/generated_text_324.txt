import pandas as pd

def glimpse(df, n=5):
    print(f'Rows: {df.shape[0]}')
    print(f'Columns: {df.shape[1]}')
    for col in df.columns:
        dtype = df[col].dtype
        values = ', '.join(map(str, df[col].head(n).tolist()))
        print(f'$ {col} <{dtype}> {values}')
