import polars as pl

def reorder_columns(df, first_columns):
    remaining_columns = [col for col in df.columns if col not in first_columns]
    return df.select(first_columns + remaining_columns)
