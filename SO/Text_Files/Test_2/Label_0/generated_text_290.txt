import re

def get_uniform_substrings(s: str):
    substrings = []
    for match in re.finditer(r'([a-z])\1*', s):
        group = match.group(0)
        substrings.extend(group[:i] for i in range(1, len(group) + 1))
    return substrings

print(get_uniform_substrings("aaabbbbcca"))
