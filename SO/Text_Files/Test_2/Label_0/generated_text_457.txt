import numpy as np

def interleave_split(arr: np.ndarray, group_size: int = 4, step: int = 8) -> list[np.ndarray]:
    num_groups = step // group_size
    return [arr[i::step] for i in range(num_groups)]

arr = np.arange(1, 17)
a, b = interleave_split(arr)
print(a)
print(b)
