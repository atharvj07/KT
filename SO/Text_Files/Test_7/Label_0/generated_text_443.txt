import polars as pl

df = pl.DataFrame(
    {
        "a": [1, 8, 3],
        "b": [4, 5, None],
    }
)

df = df.with_columns(
    pl.when(pl.col("a") >= pl.col("b"))
    .then(pl.lit("a"))
    .otherwise(pl.lit("b"))
    .alias("max")
)

print(df)
