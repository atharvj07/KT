import pandas as pd
import numpy as np

df = pd.DataFrame({
    'A': ['a', 'a', 'a', 'c', 'c', 'c', 'c', 'c', 'e', 'e', 'e', 'e'],
    'B': ['b', 'b', 'b', 'd', 'd', 'd', 'd', 'd', 'b', 'b', 'b', 'b'],
    'C': [1, 2, 3, 7, 8, 5, 6, 3, 4, 3, 2, 1]
})

df_out = df.groupby(['A', 'B']).apply(lambda x: pd.Series(x['C'].values)).unstack(fill_value=np.nan)
df_out.columns = [f'C{i+1}' for i in range(df_out.shape[1
