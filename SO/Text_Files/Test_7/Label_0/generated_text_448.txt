FROM python:3.9

WORKDIR /p

COPY poetry.lock pyproject.toml .
RUN pip install poetry

COPY . /p

ARG CI_JOB_TOKEN
RUN --mount=type=secret,id=ci_token poetry config http-basic.gitlab-pypi-repo gitlab-ci-token $(cat /run/secrets/ci_token) && \
    poetry install --no-interaction
