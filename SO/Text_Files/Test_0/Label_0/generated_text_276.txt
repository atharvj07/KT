import polars as pl
import datetime as dt

df = pl.DataFrame({
    'time': [dt.datetime.now()]
})

df = df.with_columns(
    pl.col("time").dt.date().alias("date")
)

print(df)

'time': [dt.datetime.now()]
pl.col("time").dt.date().alias("date")