import polars as pl

df = pl.DataFrame({"x": [1.2, 1.3, 3.4, 3.5]})

df = df.with_columns(
    y=pl.concat_list([
        pl.col("x").shift(x) for x in range(2, -2, -1)
    ]).list.drop_nulls()
)

print(df)