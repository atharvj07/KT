properties
# Add this to your application.properties or application.yml
spring.native.proxy-configurations=proxy-config.json

json
// Create proxy-config.json in your resources directory
[
  {
    "interfaces": ["jakarta.servlet.http.HttpServletRequest"]
  }
]
