@Test
void testUnHappy() {
    Long invalidId = -1L;
    when(happyService.getById(invalidId)).thenThrow(UnhappyException.class);

    mockMvc.perform(get("/happy/{happyId}", invalidId))
           .andExpect(status().isNotFound());
}
