import com.amazonaws.services.s3.AmazonS3;
import com.amazonaws.services.s3.model.S3Object;
import com.amazonaws.services.s3.AmazonS3ClientBuilder;
import java.io.InputStream;

public class S3Image {

    public static InputStream getImageFromS3(String bucketName, String imageName) {
        AmazonS3 s3Client = AmazonS3ClientBuilder.defaultClient();
        S3Object s3Object = s3Client.getObject(bucketName, imageName);
        return s3Object.getObjectContent();
    }

    public static void main(String[] args) {
        String bucketName = "your-bucket-name";
        String imageName = "image_name.jpg";
        InputStream imageStream = getImageFromS3(bucketName, imageName);

        // Pass the imageStream to JasperReports as image source
        // For example, in JasperReports, you would use the following expression to use the imag
