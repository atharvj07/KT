String os = System.getProperty("os.name", "").toLowerCase();
String arc = System.getProperty("os.arch", "").toLowerCase();
if (os.contains("mac") || os.contains("darwin")) {
    _zipName = chromeForTesting 
        ? (arc.contains("aarch64") || arc.contains("arm") ? "chromedriver-mac-arm64.zip" : "chromedriver-mac-x64.zip") 
        : (arc.contains("aarch64") || arc.contains("arm") ? "chromedriver_mac64_arm64.zip" : "chromedriver_mac64.zip");
}
