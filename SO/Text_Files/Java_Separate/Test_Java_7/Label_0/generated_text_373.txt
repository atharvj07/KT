({ "ROLE_CUSTOMERGROUP", "ROLE_TRUSTED_CLIENT", "ROLE_CUSTOMERMANAGERGROUP" })
@GetMapping(value = "/test")
@ResponseBody
public TestListWsDTO getTest(
        @RequestParam(required = false, defaultValue = DEFAULT_FIELD_SET) final String fields,
        @RequestParam final String userId,
        @RequestParam final String siteParam) {
    
    try {
        // Setting user session
        UserModel user = userFacade.getUserForUID(userId);
        if (user != null) {
            sessionService.setAttribute("currentUser", user);
            sessionService.setAttribute("currentSite", siteParam);
        } else {
            throw new AccessDeniedException("User not found.");
        }

        final CustomerData customerData = customerFacade.getCurrentCustomer();
        if (userFacade.isAnonymousUser()) {
            throw new AccessDeniedException("Anonymous user is not allowed");
        }

        return new TestListWsDTO(); // Replace with actual response

    } catch (Exception e) {
        throw new AccessDeniedException("Error setting user in session: " + e.getMessage());
    }
}
