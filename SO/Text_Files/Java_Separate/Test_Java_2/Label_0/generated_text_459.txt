public static <T, U extends TaskResult<T>> List<U> run(List<? extends ITask<T>> tasks, Class<U> resultClass) {
    return tasks.stream()
            .map(task -> resultClass.cast(task.make()))
            .collect(Collectors.toList());
}
