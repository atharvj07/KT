// In the Service class (onStartCommand method)
if (action != null && action.equals(ACTION_STOP_SERVICE)) {
    killService();
    Intent openApp = new Intent(this, MainActivity.class);
    openApp.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_CLEAR_TASK);
    startActivity(openApp);
}

// Code for the PendingIntent in the notification builder
Intent stopActionIntent = new Intent(this, TimerService.class);
stopActionIntent.setAction(ACTION_STOP_SERVICE);
PendingIntent stopActionPendingIntent = PendingIntent.getService(this, 1, stopActionIntent, PendingIntent.FLAG_IMMUTABLE);

timerNotificationBuilder.addAction(R.drawable.stop, "Stop", stopActionPendingIntent);

// Test case
@Test
public void testServiceOpensMainActivity() throws Exception {
    Intent stopActionIntent = new Intent(getApplicationContext(), TimerService.class);
    stopActionIntent.setAction(ACTION_STOP_SERVICE);
    PendingIntent stopActionPendingIntent = PendingIntent.getService(getApplicationContext(), 1, stopActionIntent, PendingIntent.FLAG_IMMUTABLE);

    Notification notification = new NotificationCompat.Builder(getApplicationContext(), CHANNEL_ID)
            .setContentTitle("Timer")
            .setContentText("Timer is running")
            .addAction(R.drawable.stop, "Stop", stopActionPendingIntent)
            .build();

    NotificationManagerCompat notificationManager = NotificationManagerCompat.from(getApplicationContext());
    notificationManager.notify(1, notification);

    // Simulate button click
    stopActionPendingIntent.send();

    // Check if MainActivity was opened (mock or using instrumentation test)
    assertTrue(isActivityStarted(MainActivity.class)); // Assume helper method for test assertion
}
