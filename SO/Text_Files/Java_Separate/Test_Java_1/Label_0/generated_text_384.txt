String osName = System.getProperty("os.name", "").toLowerCase();
String arch = System.getProperty("os.arch", "").toLowerCase();
String _zipName = "";

if(osName.contains("darwin") || osName.contains("mac")) {
    if (arch.contains("arm")) {
        _zipName = chromeForTesting ? "chromedriver-mac-arm64.zip" : "chromedriver_mac_arm64.zip";
    } else {
        _zipName = chromeForTesting ? "chromedriver-mac-x64.zip" : "chromedriver_mac64.zip";
    }
}

// Test case
public static void main(String[] args) {
    System.setProperty("os.name", "Mac OS X");
    System.setProperty("os.arch", "arm64");
    String zipName = new YourClass().getZipName(true); // Pass chromeForTesting as true or false
    System.out.println(zipName); // Expected output: chromedriver-mac-arm64.zip
}
