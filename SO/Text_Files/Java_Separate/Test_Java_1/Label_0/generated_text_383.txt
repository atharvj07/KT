@SupportedAnnotationTypes("*")
@AutoService(Processor.class)
public class EnumAnnotationProcessor extends AbstractProcessor {

    private boolean firstRound = true;

    @Override
    public boolean process(Set<? extends TypeElement> annotations, RoundEnvironment env) {
        if (firstRound) {
            firstRound = false;
            return false; // skip processing this round
        }

        annotations.forEach(enumAnnotation -> {
            env.getElementsAnnotatedWith(enumAnnotation).forEach(customAnnotation -> {
                env.getElementsAnnotatedWith(customAnnotation).forEach(element -> {
                    // Process the element using the generated annotation
                });
            });
        });
        return false;
    }
}

@SupportedAnnotationTypes("com.company.generator.EnumAnnotation")
@AutoService(Processor.class)
public class FirstPassCollector extends AbstractProcessor {

    @Override
    public boolean process(Set<? extends TypeElement> annotations, RoundEnvironment roundEnv) {
        // Process annotations in the first pass
        return false;
    }
}

// Test case to check functionality

@GenerateAnnotation
public enum MyEnum {
    VALUE_ONE, VALUE_TWO
}

@EnumAnnotation
public @interface MyEnumAnnotation {
    MyEnum value();
}

// Expected generated annotation @MyEnumAnnotation
@MyEnumAnnotation(value = MyEnum.VALUE_ONE)
public class SomeClass {
    @MyEnumAnnotation(value = MyEnum.VALUE_TWO)
    public void someMethod() {}
}
