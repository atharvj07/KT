class DslMock {
    def mock

    DslMock() {
        mock = mock(Dsl)
        when(mock.invokeMethod(eq("error"), any() as Object[])).thenAnswer { inv ->
            throw new AbortException((inv.arguments[1] as Object[])[0] as String)
        }
    }
}
