 boolean isValid(ProductExportFilters f, ConstraintValidatorContext c) {
    if (f == null) {
        c.disableDefaultConstraintViolation();
        c.buildConstraintViolationWithTemplate("Filters cannot be null").addConstraintViolation();
        return false;
    }
    try {
        DateTimeFormatter fmt = DateTimeFormatter.ISO_DATE_TIME;
        LocalDateTime s = null, e = null;
        if (f.getStartDate() != null) s = LocalDateTime.from(fmt.parse(f.getStartDate()));
        if (f.getEndDate() != null) e = LocalDateTime.from(fmt.parse(f.getEndDate()));
        if (s == null && e == null) return true;
        if (s == null || e == null) {
            c.disableDefaultConstraintViolation();
            c.buildConstraintViolationWithTemplate("Both dates must be provided").addConstraintViolation();
            return false;
        }
        if (!s.equals(e) && s.isAfter(e)) {
            c.disableDefaultConstraintViolation();
            c.buildConstraintViolationWithTemplate("Start date must be before end date").addConstraintViolation();
            return false;
        }
        return true;
    } catch (DateTimeException ex) {
        c.disableDefaultConstraintViolation();
        c.buildConstraintViolationWithTemplate("Invalid date format").addConstraintViolation();
        ret
