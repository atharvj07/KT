var m = Map.of("test_first", 1, "test_second", 2);

var p = m.entrySet().stream()
    .collect(Collectors.toMap(
        e -> CaseFormat.LOWER_UNDERSCORE.to(CaseFormat.LOWER_CAMEL, e.getKey()),
        Map.Entry::getValue
    ));

var j = new ObjectMapper().writeValueAsString(p);
