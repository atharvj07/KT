@RestController
@RequestMapping("/prefix")
public abstract class BaseController {
}

@Slf4j
@RestController
@AllArgsConstructor
@RequestMapping("/families")
@RolesGuard(role = {Roles.Manager, Roles.Employee})
public class MyController extends BaseController {
    // Full path will be: /prefix/families
}

@RequestMapping("/prefix")
public interface PrefixController {
}

@Slf4j
@RestController
@AllArgsConstructor
@RequestMapping("/families")
@RolesGuard(role = {Roles.Manager, Roles.Employee})
public class MyController implements PrefixController {
    // Full path will be: /prefix/families
}

app.api.prefix=/prefix

@Slf4j
@RestController
@AllArgsConstructor
@RequestMapping("${app.api.prefix}/families")
@RolesGuard(role = {Roles.Manager, Roles.Employee})
public class MyController {
    // Full path will be: /prefix/families (configurable)
}

// Full path will be: /prefix/families
// Full path will be: /prefix/families
// Full path will be: /prefix/families (configurable)