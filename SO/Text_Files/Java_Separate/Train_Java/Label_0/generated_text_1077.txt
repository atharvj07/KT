import java.util.HashMap;
import java.util.Map;
import java.util.Optional;

class Animal {
    void feed(String food) {
        System.out.println("Feeding animal with: " + food);
    }
}

public class Zoo {
    public static void main(String[] args) {
        Map<Integer, Animal> zoo = new HashMap<>();

        // Fill the zoo map (example)
        zoo.put(1, new Animal());

        String food = "apple";
        // Using Optional to handle null gracefully
        Optional.ofNullable(zoo.get(1))
                .ifPresent(a -> a.feed(food)); // Feed if the animal exists
    }
}

import java.util.HashMap;
import java.util.Map;
import java.util.function.Consumer;

class Animal {
    void feed(String food) {
        System.out.println("Feeding animal with: " + food);
    }
}

public class Zoo {

    public static void main(String[] args) {
        Map<Integer, Animal> zoo = new HashMap<>();

        // Fill the zoo map (example)
        zoo.put(1, new Animal());

        String food = "apple";
        getAndUse(zoo, 1, a -> a.feed(food)); // Call the utility method
    }

    // Custom utility method that gets the animal and performs the given action
    public static <T> void getAndUse(Map<Integer, T> map, Integer key, Consumer<T> action) {
        T value = map.get(key);
        if (value != null) {
            action.accept(value); // Apply the action (feed in this case)
        }
    }
}

import java.util.HashMap;
import java.util.Map;

class Animal {
    void feed(String food) {
        System.out.println("Feeding animal with: " + food);
    }
}

public class Zoo {
    public static void main(String[] args) {
        Map<Integer, Animal> zoo = new HashMap<>();

        // Fill the zoo map (example)
        zoo.put(1, new Animal());

        String food = "apple";
        // Use computeIfPresent to handle feeding
        zoo.computeIfPresent(1, (id, animal) -> {
            animal.feed(food); // Feed the animal if present
            return animal; // Return the animal back to the map
        });
    }
}

import java.util.HashMap;
import java.util.Map;

class Animal {
    void feed(String food) {
        System.out.println("Feeding animal with: " + food);
    }
}

public class Zoo {
    public static void main(String[] args) {
        Map<Integer, Animal> zoo = new HashMap<>();

        // Fill the zoo map (example)
        zoo.put(1, new Animal());

        String food = "apple";
        // Use getOrDefault to ensure you have a fallback (or return null)
        Animal animal = zoo.getOrDefault(1, null);
        if (animal != null) {
            animal.feed(food);
        }
    }
}

void feed(String food) {
    System.out.println("Feeding animal with: " + food);
}
public static void main(String[] args) {
    Map<Integer, Animal> zoo = new HashMap<>();
    // Fill the zoo map (example)
    zoo.put(1, new Animal());
    String food = "apple";
    // Using Optional to handle null gracefully
    Optional.ofNullable(zoo.get(1))
            .ifPresent(a -> a.feed(food)); // Feed if the animal exists
}
void feed(String food) {
    System.out.println("Feeding animal with: " + food);
}
public static void main(String[] args) {
    Map<Integer, Animal> zoo = new HashMap<>();
    // Fill the zoo map (example)
    zoo.put(1, new Animal());
    String food = "apple";
    getAndUse(zoo, 1, a -> a.feed(food)); // Call the utility method
}
// Custom utility method that gets the animal and performs the given action
public static <T> void getAndUse(Map<Integer, T> map, Integer key, Consumer<T> action) {
    T value = map.get(key);
    if (value != null) {
        action.accept(value); // Apply the action (feed in this case)
    }
}
void feed(String food) {
    System.out.println("Feeding animal with: " + food);
}
public static void main(String[] args) {
    Map<Integer, Animal> zoo = new HashMap<>();
    // Fill the zoo map (example)
    zoo.put(1, new Animal());
    String food = "apple";
    // Use computeIfPresent to handle feeding
    zoo.computeIfPresent(1, (id, animal) -> {
        animal.feed(food); // Feed the animal if present
        return animal; // Return the animal back to the map
    });
}
void feed(String food) {
    System.out.println("Feeding animal with: " + food);
}
public static void main(String[] args) {
    Map<Integer, Animal> zoo = new HashMap<>();
    // Fill the zoo map (example)
    zoo.put(1, new Animal());
    String food = "apple";
    // Use getOrDefault to ensure you have a fallback (or return null)
    Animal animal = zoo.getOrDefault(1, null);
    if (animal != null) {
        animal.feed(food);
    }
}