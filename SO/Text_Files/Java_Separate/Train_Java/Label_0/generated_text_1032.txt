public static void setNewPlayeData() {
    if (exoPlayer != null) {
        exoPlayer.stop(); // Stop current playback
        exoPlayer.clearMediaItems(); // Clear old media
    }

    MediaItem mediaItem = MediaItem.fromUri(Uri.parse(DataUtility.playingUrl));
    exoPlayer.setMediaItem(mediaItem);
    exoPlayer.prepare();
    exoPlayer.play(); // Start playing the new media

    try {
        ThePlayingActivity.myPlayerView.setPlayer(exoPlayer);
    } catch (Exception e) {
        Log.d("background", "setNewPlayeData: Failed to set player to view " + e.getLocalizedMessage());
    }
}

@Override
public int onStartCommand(Intent intent, int flags, int startId) {
    if (exoPlayer == null) {
        createPlayer();
    } else {
        // Resume playing if paused
        if (exoPlayer.getPlaybackState() == Player.STATE_IDLE || exoPlayer.getPlaybackState() == Player.STATE_ENDED) {
            exoPlayer.prepare();
            exoPlayer.play();
        }
    }

    return START_STICKY;
}

@Override
public void onDestroy() {
    super.onDestroy();
    if (exoPlayer != null) {
        exoPlayer.stop();
        exoPlayer.release(); // Release the player properly
        exoPlayer = null;
    }
    if (playerNotificationManager != null) {
        playerNotificationManager.setPlayer(null);
    }
}

if (BackgroundPlayingService.exoPlayer == null) {
    Intent serviceIntent = new Intent(this, BackgroundPlayingService.class);
    startService(serviceIntent); // Ensure only one instance
} else {
    BackgroundPlayingService.setNewPlayeData();
    myPlayerView.setPlayer(BackgroundPlayingService.exoPlayer);
}

if (exoPlayer != null) {
    exoPlayer.stop(); // Stop current playback
    exoPlayer.clearMediaItems(); // Clear old media
}
MediaItem mediaItem = MediaItem.fromUri(Uri.parse(DataUtility.playingUrl));
exoPlayer.setMediaItem(mediaItem);
exoPlayer.prepare();
exoPlayer.play(); // Start playing the new media
try {
    ThePlayingActivity.myPlayerView.setPlayer(exoPlayer);
} catch (Exception e) {
    Log.d("background", "setNewPlayeData: Failed to set player to view " + e.getLocalizedMessage());
}
if (exoPlayer == null) {
    createPlayer();
} else {
    // Resume playing if paused
    if (exoPlayer.getPlaybackState() == Player.STATE_IDLE || exoPlayer.getPlaybackState() == Player.STATE_ENDED) {
        exoPlayer.prepare();
        exoPlayer.play();
    }
}
return START_STICKY;
super.onDestroy();
if (exoPlayer != null) {
    exoPlayer.stop();
    exoPlayer.release(); // Release the player properly
    exoPlayer = null;
}
if (playerNotificationManager != null) {
    playerNotificationManager.setPlayer(null);
}
Intent serviceIntent = new Intent(this, BackgroundPlayingService.class);
startService(serviceIntent); // Ensure only one instance
BackgroundPlayingService.setNewPlayeData();
myPlayerView.setPlayer(BackgroundPlayingService.exoPlayer);