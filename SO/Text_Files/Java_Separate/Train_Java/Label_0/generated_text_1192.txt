n = 0
     t1: n++ (n becomes 1), m = n (m = 1), prints 1
     t2: n++ (n becomes 2), m = n (m = 2), prints 2
     
     n = 0
     t2: n++ (n becomes 1), m = n (m = 1), prints 1
     t1: n++ (n becomes 2), m = n (m = 2), prints 2
     
     t1: n++ (reads 0, writes 1)
     t2: n++ (reads 0, writes 1)
     
     t1: n++ (reads 0, writes 1)
     t2: n++ (reads 1, writes 2)
     t1: m = n (m = 2), prints 2
     t2: m = n (m = 2), prints 2
     
     t1: n++ (reads 0, writes 1)
     t2: n++ (reads 1, writes 2), m = 1
     t1: m = n (m = 2)
     t1 prints 2
     t2 prints 1
     
import java.util.concurrent.atomic.AtomicInteger;

public class Main {
    
    public static void main(String args[]){ 
        Thread t1 = new SimpleThread();
        Thread t2 = new SimpleThread();
        t1.start();
        t2.start();
    }
    
    static class SimpleThread extends Thread {
        
        private static AtomicInteger n = new AtomicInteger(0);
        
        public void run() {
            int m = n.incrementAndGet();
            System.out.println(m);
        }
    }
}

 ```
 n = 0
 t1: n++ (n becomes 1), m = n (m = 1), prints 1
 t2: n++ (n becomes 2), m = n (m = 2), prints 2
 ```
 Output: **`1 2`**
 ```
 n = 0
 t2: n++ (n becomes 1), m = n (m = 1), prints 1
 t1: n++ (n becomes 2), m = n (m = 2), prints 2
 ```
 Output: **`1 2`** (Same as above)
 ```
 t1: n++ (reads 0, writes 1)
 t2: n++ (reads 0, writes 1)
 ```
 - `m = 1` in both threads
 - Both print `1`
 Output: **`1 1`**
 ```
 t1: n++ (reads 0, writes 1)
 t2: n++ (reads 1, writes 2)
 t1: m = n (m = 2), prints 2
 t2: m = n (m = 2), prints 2
 ```
 Output: **`2 2`**
 ```
 t1: n++ (reads 0, writes 1)
 t2: n++ (reads 1, writes 2), m = 1
 t1: m = n (m = 2)
 t1 prints 2
 t2 prints 1
 ```
 Output: **`2 1`**
public static void main(String args[]){ 
    Thread t1 = new SimpleThread();
    Thread t2 = new SimpleThread();
    t1.start();
    t2.start();
}
static class SimpleThread extends Thread {
    
    private static AtomicInteger n = new AtomicInteger(0);
    
    public void run() {
        int m = n.incrementAndGet();
        System.out.println(m);
    }
}