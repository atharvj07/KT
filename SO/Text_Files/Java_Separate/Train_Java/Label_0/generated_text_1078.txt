SslContextFactory.Server sslContextFactory = new SslContextFactory.Server();
sslContextFactory.setWantClientAuth(true); // optional based on your needs
sslContextFactory.setNeedClientAuth(true); // optional based on your needs

// Disable SNI check
sslContextFactory.setSniRequired(false);

- **JettyÂs SNI handling** should generally be **case-insensitive**. However, it's possible that **case sensitivity** might still be influencing some internal aspects of JettyÂs SNI handling, particularly if there are bugs or inconsistencies in how Jetty compares the SNI value to the certificate's Subject Alternative Names (SANs).
- **Recommendation**: Check if you're using the latest version of Jetty (11.0.11 in your case), as itÂs always good to ensure that you have the latest bug fixes related to TLS and SNI. Sometimes, bugs like this are addressed in newer versions.
- The **SNI in the certificate should match the SNI** sent by the client. However, **you do not need to add both the uppercase and lowercase variations** of the same domain in the certificateÂs SAN (Subject Alternative Name). Doing this is generally not necessary, as SNI handling in TLS is case-insensitive. The only requirement is that the domain in the SNI matches exactly one of the SAN entries.
- Enable **debug logging** in Jetty for the SSL/TLS layer (which you've already done, but further fine-tuning might help).
- Look for any warnings or errors related to SSL/TLS during the handshake process.
- If possible, capture the complete TLS handshake and inspect the SNI field to ensure it's being sent by the client.