<dependencies>
    <!-- TestNG -->
    <dependency>
        <groupId>org.testng</groupId>
        <artifactId>testng</artifactId>
        <version>7.5</version>
        <scope>test</scope>
    </dependency>

    <!-- Mockito -->
    <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>5.2.0</version>
        <scope>test</scope>
    </dependency>

    <!-- Mockito TestNG Integration -->
    <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-testng</artifactId>
        <version>5.2.0</version>
        <scope>test</scope>
    </dependency>
</dependencies>

import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
import org.testng.Assert;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Test;

public class ClassBTest {

    @Mock
    private EntityClass entity;  // Mocking EntityClass

    @InjectMocks
    private ClassB classB;  // Inject mock into ClassB

    @BeforeMethod
    public void setUp() {
        MockitoAnnotations.openMocks(this);  // Initialize mocks before each test
    }

    @Test
    public void testSomeMethod() {
        // Arrange: Define the mock behavior
        String input = "test-input";
        String expectedOutput = "mocked-response";
        
        Mockito.when(entity.execute(input)).thenReturn(expectedOutput);

        // Act: Call the method under test
        String result = classB.someMethod(input);

        // Assert: Verify the expected outcome
        Assert.assertEquals(result, expectedOutput, "Method should return mocked response");

        // Verify that the mock method was called exactly once
        Mockito.verify(entity).execute(input);
    }
}

[INFO] Running ClassBTest
[INFO] Tests run: 1, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.123 s - in ClassBTest
[INFO] BUILD SUCCESS

<!-- TestNG -->
<dependency>
    <groupId>org.testng</groupId>
    <artifactId>testng</artifactId>
    <version>7.5</version>
    <scope>test</scope>
</dependency>
<!-- Mockito -->
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-core</artifactId>
    <version>5.2.0</version>
    <scope>test</scope>
</dependency>
<!-- Mockito TestNG Integration -->
<dependency>
    <groupId>org.mockito</groupId>
    <artifactId>mockito-testng</artifactId>
    <version>5.2.0</version>
    <scope>test</scope>
</dependency>
@Mock
private EntityClass entity;  // Mocking EntityClass
@InjectMocks
private ClassB classB;  // Inject mock into ClassB
@BeforeMethod
public void setUp() {
    MockitoAnnotations.openMocks(this);  // Initialize mocks before each test
}
@Test
public void testSomeMethod() {
    // Arrange: Define the mock behavior
    String input = "test-input";
    String expectedOutput = "mocked-response";
    
    Mockito.when(entity.execute(input)).thenReturn(expectedOutput);
    // Act: Call the method under test
    String result = classB.someMethod(input);
    // Assert: Verify the expected outcome
    Assert.assertEquals(result, expectedOutput, "Method should return mocked response");
    // Verify that the mock method was called exactly once
    Mockito.verify(entity).execute(input);
}