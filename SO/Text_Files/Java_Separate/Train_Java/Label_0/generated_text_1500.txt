String url = "jdbc:sqlserver://<host>:<port>;databaseName=<db>";
String user = "<user>";
String password = "<password>";

// Enable logging for JDBC
Properties properties = new Properties();
properties.put("user", user);
properties.put("password", password);
properties.put("logWriter", System.out);  // This directs SQL output to the console

Connection conn = DriverManager.getConnection(url, properties);

public void executeStoredProcedureWithLogging() {
    try (Connection conn = dataSource.getConnection()) {
        // Enable capturing of output from the SQL Server
        conn.createStatement().execute("SET NOCOUNT ON;");
        
        // Set up a statement to execute your stored procedure
        try (CallableStatement stmt = conn.prepareCall("{call yourStoredProc()}")) {
            stmt.execute();

            // If you're expecting any output (PRINT statements), fetch them here.
            // This is an example of how you'd handle capturing messages via the connection
            // Note: You might need to handle this depending on your SQL Server JDBC driver

            // Example of getting the messages
            SQLServerDataSource dataSource = new SQLServerDataSource();
            dataSource.setUser("user");
            dataSource.setPassword("password");
            dataSource.setServerName("localhost");
            dataSource.setDatabaseName("yourDb");

            // Use the SQLServerDataSource to set up logging in the connection
            Connection connection = dataSource.getConnection();
            connection.setAutoCommit(true);
        }
    } catch (SQLException e) {
        // Log any exceptions
        logger.error("Error while executing stored procedure", e);
    }
}

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class YourService {
    private static final Logger logger = LoggerFactory.getLogger(YourService.class);

    public void executeStoredProcedure() {
        try (Handle handle = dbi.open()) {
            handle.createCall("{call yourStoredProc()}")
                  .bind("param", value)
                  .invoke();

            // Assuming you have logic here to capture output (e.g., using CallableStatement or SQLServer specific methods)
            logger.info("Stored procedure executed successfully, check DB output for print statements");
        } catch (Exception e) {
            logger.error("Error executing stored procedure", e);
        }
    }
}

import java.sql.*;

public class StoredProcWithPrint {
    private static final Logger logger = LoggerFactory.getLogger(StoredProcWithPrint.class);

    public void executeStoredProcedure() {
        String query = "{call yourStoredProc()}";
        
        try (Connection conn = dataSource.getConnection()) {
            try (CallableStatement stmt = conn.prepareCall(query)) {
                stmt.execute();
                
                // Check for any warnings (this is where SQL Server's PRINT output may end up)
                SQLWarning warning = stmt.getWarnings();
                if (warning != null) {
                    logger.info("Received PRINT statement output: {}", warning.getMessage());
                }
            }
        } catch (SQLException e) {
            logger.error("Error executing stored procedure", e);
        }
    }
}

try (Connection conn = dataSource.getConnection()) {
    // Enable capturing of output from the SQL Server
    conn.createStatement().execute("SET NOCOUNT ON;");
    
    // Set up a statement to execute your stored procedure
    try (CallableStatement stmt = conn.prepareCall("{call yourStoredProc()}")) {
        stmt.execute();
        // If you're expecting any output (PRINT statements), fetch them here.
        // This is an example of how you'd handle capturing messages via the connection
        // Note: You might need to handle this depending on your SQL Server JDBC driver
        // Example of getting the messages
        SQLServerDataSource dataSource = new SQLServerDataSource();
        dataSource.setUser("user");
        dataSource.setPassword("password");
        dataSource.setServerName("localhost");
        dataSource.setDatabaseName("yourDb");
        // Use the SQLServerDataSource to set up logging in the connection
        Connection connection = dataSource.getConnection();
        connection.setAutoCommit(true);
    }
} catch (SQLException e) {
    // Log any exceptions
    logger.error("Error while executing stored procedure", e);
}
private static final Logger logger = LoggerFactory.getLogger(YourService.class);
public void executeStoredProcedure() {
    try (Handle handle = dbi.open()) {
        handle.createCall("{call yourStoredProc()}")
              .bind("param", value)
              .invoke();
        // Assuming you have logic here to capture output (e.g., using CallableStatement or SQLServer specific methods)
        logger.info("Stored procedure executed successfully, check DB output for print statements");
    } catch (Exception e) {
        logger.error("Error executing stored procedure", e);
    }
}
private static final Logger logger = LoggerFactory.getLogger(StoredProcWithPrint.class);
public void executeStoredProcedure() {
    String query = "{call yourStoredProc()}";
    
    try (Connection conn = dataSource.getConnection()) {
        try (CallableStatement stmt = conn.prepareCall(query)) {
            stmt.execute();
            
            // Check for any warnings (this is where SQL Server's PRINT output may end up)
            SQLWarning warning = stmt.getWarnings();
            if (warning != null) {
                logger.info("Received PRINT statement output: {}", warning.getMessage());
            }
        }
    } catch (SQLException e) {
        logger.error("Error executing stored procedure", e);
    }
}