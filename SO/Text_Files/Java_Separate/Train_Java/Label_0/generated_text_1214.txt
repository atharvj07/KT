// Create X-axis labels (same size as the number of bars)
List<String> barLabels = new ArrayList<>();
barLabels.add("Bar 1"); // Start directly from 0

for (int i = 2; i <= dataVals.size(); i++) { // Start from 2
    barLabels.add("Bar " + i);
}

// Set the custom labels for the X-axis
xAxis.setValueFormatter(new IndexAxisValueFormatter(barLabels));

xAxis.setGranularity(1f);  // Ensure one label per bar
xAxis.setLabelCount(dataVals.size(), true); // Force exact labels
xAxis.setCenterAxisLabels(true); // Align labels with bars

barLabels.add("Bar " + i);