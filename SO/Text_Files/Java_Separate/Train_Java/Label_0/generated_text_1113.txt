public interface PersonVisitor {
    void visit(Student student);
    // Add methods for other subclasses if necessary
}

public abstract class Person {
    private int id;
    private String name;

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    // Abstract method to accept a visitor
    public abstract void accept(PersonVisitor visitor);
}

public class Student extends Person {

    private int studentNo;

    public int getStudentNo() {
        return studentNo;
    }

    public void setStudentNo(int studentNo) {
        this.studentNo = studentNo;
    }

    @Override
    public void accept(PersonVisitor visitor) {
        visitor.visit(this);  // Call the appropriate method on the visitor
    }
}

public class PersonManager implements PersonVisitor {

    public void add(Person person) {
        person.accept(this);  // Let the person accept the visitor
    }

    @Override
    public void visit(Student student) {
        // Handle student-specific features
        System.out.println("Saved: " + student.getName());
        System.out.println("Saved: " + student.getId());
        System.out.println("Student No: " + student.getStudentNo());  // Access student-specific properties
    }

    // Implement methods for other subclasses if necessary
}

public class Main {
    public static void main(String[] args) {
        Person student = new Student();
        student.setName("John Doe");
        student.setId(123);
        ((Student) student).setStudentNo(456);  // Set student-specific property

        PersonManager manager = new PersonManager();
        manager.add(student);  // This will call the visit method for Student
    }
}

public abstract class Person {
    private int id;
    private String name;

    public int getId() {
        return id;
    }

    public void setId(int id) {
        this.id = id;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    // Abstract method for subclasses to implement
    public abstract void specialFeatures();
}

public class Student extends Person {

    private int studentNo;

    public int getStudentNo() {
        return studentNo;
    }

    public void setStudentNo(int studentNo) {
        this.studentNo = studentNo;
    }

    @Override
    public void specialFeatures() {
        System.out.println("Student No: " + studentNo);
    }
}

public class PersonManager {

    public void add(Person person) {
        System.out.println("Saved: " + person.getName());
        System.out.println("Saved: " + person.getId());

        // Call the abstract method to handle subclass-specific behavior
        person.specialFeatures();
    }
}

public class Main {
    public static void main(String[] args) {
        Person student = new Student();
        student.setName("John Doe");
        student.setId(123);
        ((Student) student).setStudentNo(456);

        PersonManager manager = new PersonManager();
        manager.add(student);  // This will call the specialFeatures method for Student
    }
}

void visit(Student student);
// Add methods for other subclasses if necessary
private int id;
private String name;
public int getId() {
    return id;
}
public void setId(int id) {
    this.id = id;
}
public String getName() {
    return name;
}
public void setName(String name) {
    this.name = name;
}
// Abstract method to accept a visitor
public abstract void accept(PersonVisitor visitor);
private int studentNo;
public int getStudentNo() {
    return studentNo;
}
public void setStudentNo(int studentNo) {
    this.studentNo = studentNo;
}
@Override
public void accept(PersonVisitor visitor) {
    visitor.visit(this);  // Call the appropriate method on the visitor
}
public void add(Person person) {
    person.accept(this);  // Let the person accept the visitor
}
@Override
public void visit(Student student) {
    // Handle student-specific features
    System.out.println("Saved: " + student.getName());
    System.out.println("Saved: " + student.getId());
    System.out.println("Student No: " + student.getStudentNo());  // Access student-specific properties
}
// Implement methods for other subclasses if necessary
public static void main(String[] args) {
    Person student = new Student();
    student.setName("John Doe");
    student.setId(123);
    ((Student) student).setStudentNo(456);  // Set student-specific property
    PersonManager manager = new PersonManager();
    manager.add(student);  // This will call the visit method for Student
}
private int id;
private String name;
public int getId() {
    return id;
}
public void setId(int id) {
    this.id = id;
}
public String getName() {
    return name;
}
public void setName(String name) {
    this.name = name;
}
// Abstract method for subclasses to implement
public abstract void specialFeatures();
private int studentNo;
public int getStudentNo() {
    return studentNo;
}
public void setStudentNo(int studentNo) {
    this.studentNo = studentNo;
}
@Override
public void specialFeatures() {
    System.out.println("Student No: " + studentNo);
}
public void add(Person person) {
    System.out.println("Saved: " + person.getName());
    System.out.println("Saved: " + person.getId());
    // Call the abstract method to handle subclass-specific behavior
    person.specialFeatures();
}
public static void main(String[] args) {
    Person student = new Student();
    student.setName("John Doe");
    student.setId(123);
    ((Student) student).setStudentNo(456);
    PersonManager manager = new PersonManager();
    manager.add(student);  // This will call the specialFeatures method for Student
}