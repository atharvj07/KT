class MyClass {
    MyAttribute myAttribute;

    MyClass(MyAttribute myAttribute) {
        this.myAttribute = myAttribute;
    }

    MyAttribute getMyAttribute() {
        return myAttribute;
    }
}

class MySubClass extends MyClass {
    MySubClass(MySubAttribute mySubAttribute) {
        super(mySubAttribute);
    }

    // Override getMyAttribute to return MySubAttribute
    @Override
    MySubAttribute getMyAttribute() {
        return (MySubAttribute) super.getMyAttribute(); // Cast to MySubAttribute
    }
}

class MyAttribute {
    void doSomething() {
        System.out.println("Doing something in MyAttribute");
    }
}

class MySubAttribute extends MyAttribute {
    @Override
    void doSomething() {
        super.doSomething();
        System.out.println("Doing something else in MySubAttribute");
    }

    void doSomethingElse() {
        System.out.println("Doing something else in MySubAttribute");
    }
}

public class Main {
    public static void main(String[] args) {
        MySubAttribute mySubAttribute = new MySubAttribute();
        MySubClass mySubClass = new MySubClass(mySubAttribute);

        // This will now return MySubAttribute because of the overridden method
        MySubAttribute subAttribute = mySubClass.getMyAttribute();
        subAttribute.doSomethingElse(); // Calls the method in MySubAttribute
    }
}

Doing something in MyAttribute
Doing something else in MySubAttribute

class MyClass {
    MyAttribute myAttribute;

    MyClass(MyAttribute myAttribute) {
        this.myAttribute = myAttribute;
    }

    MyAttribute getMyAttribute() {
        if (myAttribute instanceof MySubAttribute) {
            return (MySubAttribute) myAttribute;
        }
        return myAttribute;
    }
}

class MySubClass extends MyClass {
    MySubClass(MySubAttribute mySubAttribute) {
        super(mySubAttribute);
    }
}

class MyAttribute {
    void doSomething() {
        System.out.println("Doing something in MyAttribute");
    }
}

class MySubAttribute extends MyAttribute {
    @Override
    void doSomething() {
        super.doSomething();
        System.out.println("Doing something else in MySubAttribute");
    }

    void doSomethingElse() {
        System.out.println("Doing something else in MySubAttribute");
    }
}

public class Main {
    public static void main(String[] args) {
        MySubAttribute mySubAttribute = new MySubAttribute();
        MySubClass mySubClass = new MySubClass(mySubAttribute);

        // Get MySubAttribute instance and cast if necessary
        MySubAttribute subAttribute = (MySubAttribute) mySubClass.getMyAttribute();
        subAttribute.doSomethingElse(); // Calls the method in MySubAttribute
    }
}

MyAttribute myAttribute;
MyClass(MyAttribute myAttribute) {
    this.myAttribute = myAttribute;
}
MyAttribute getMyAttribute() {
    return myAttribute;
}
MySubClass(MySubAttribute mySubAttribute) {
    super(mySubAttribute);
}
// Override getMyAttribute to return MySubAttribute
@Override
MySubAttribute getMyAttribute() {
    return (MySubAttribute) super.getMyAttribute(); // Cast to MySubAttribute
}
void doSomething() {
    System.out.println("Doing something in MyAttribute");
}
@Override
void doSomething() {
    super.doSomething();
    System.out.println("Doing something else in MySubAttribute");
}
void doSomethingElse() {
    System.out.println("Doing something else in MySubAttribute");
}
public static void main(String[] args) {
    MySubAttribute mySubAttribute = new MySubAttribute();
    MySubClass mySubClass = new MySubClass(mySubAttribute);
    // This will now return MySubAttribute because of the overridden method
    MySubAttribute subAttribute = mySubClass.getMyAttribute();
    subAttribute.doSomethingElse(); // Calls the method in MySubAttribute
}
MyAttribute myAttribute;
MyClass(MyAttribute myAttribute) {
    this.myAttribute = myAttribute;
}
MyAttribute getMyAttribute() {
    if (myAttribute instanceof MySubAttribute) {
        return (MySubAttribute) myAttribute;
    }
    return myAttribute;
}
MySubClass(MySubAttribute mySubAttribute) {
    super(mySubAttribute);
}
void doSomething() {
    System.out.println("Doing something in MyAttribute");
}
@Override
void doSomething() {
    super.doSomething();
    System.out.println("Doing something else in MySubAttribute");
}
void doSomethingElse() {
    System.out.println("Doing something else in MySubAttribute");
}
public static void main(String[] args) {
    MySubAttribute mySubAttribute = new MySubAttribute();
    MySubClass mySubClass = new MySubClass(mySubAttribute);
    // Get MySubAttribute instance and cast if necessary
    MySubAttribute subAttribute = (MySubAttribute) mySubClass.getMyAttribute();
    subAttribute.doSomethingElse(); // Calls the method in MySubAttribute
}