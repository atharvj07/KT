table1.getColumns().addListener((ListChangeListener<TableColumn<Student, ?>>) change -> {
    while (change.next()) {
        System.out.println("Columns changed!");

        // Synchronize the column order in table2
        table2.getColumns().setAll(table1.getColumns());
    }
});

while (change.next()) {
    System.out.println("Columns changed!");
    // Synchronize the column order in table2
    table2.getColumns().setAll(table1.getColumns());
}