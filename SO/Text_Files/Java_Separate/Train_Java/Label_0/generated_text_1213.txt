import java.lang.management.ManagementFactory;
import java.lang.management.MemoryMXBean;
import java.lang.management.MemoryUsage;

public class MemoryMonitor {
    private static final MemoryMXBean MEMORY_MX_BEAN = ManagementFactory.getMemoryMXBean();

    public static double getHeapUsageRatio() {
        MemoryUsage heapUsage = MEMORY_MX_BEAN.getHeapMemoryUsage();
        return (double) heapUsage.getUsed() / heapUsage.getMax(); // Returns a fraction (e.g., 0.75 means 75% full)
    }
}

int FLUSH_THRESHOLD_PERCENT = 75;

while (tokens.hasNext()) {
    String token = interner.intern(tokens.next());
    if (prevToken != null) {
        Pair<String, String> bigram = new ImmutablePair<>(prevToken, token);
        bigramCounts.computeIfAbsent(bigram, k -> new LongAdder()).increment();

        // Dynamically check memory pressure
        boolean tooMuchMemoryPressure = MemoryMonitor.getHeapUsageRatio() > (FLUSH_THRESHOLD_PERCENT / 100.0);
        if (tooMuchMemoryPressure) {
            doSomeFancyAnalysis(bigramCounts);
            bigramCounts.clear();
        }
    }
    prevToken = token;
}

   import java.lang.management.GarbageCollectorMXBean;
   import java.lang.management.ManagementFactory;
   import java.util.List;

   public class GCMonitor {
       private static final List<GarbageCollectorMXBean> GC_BEANS =
               ManagementFactory.getGarbageCollectorMXBeans();

       public static long getTotalGCCount() {
           return GC_BEANS.stream().mapToLong(GarbageCollectorMXBean::getCollectionCount).sum();
       }
   }
   
   static long lastGCCount = 0;

   boolean tooMuchMemoryPressure = MemoryMonitor.getHeapUsageRatio() > 0.75 ||
                                   GCMonitor.getTotalGCCount() > lastGCCount + 5;

   lastGCCount = GCMonitor.getTotalGCCount();
   
   java -Xms4G -Xmx8G -XX:+UseG1GC -XX:InitiatingHeapOccupancyPercent=70
   
private static final MemoryMXBean MEMORY_MX_BEAN = ManagementFactory.getMemoryMXBean();
public static double getHeapUsageRatio() {
    MemoryUsage heapUsage = MEMORY_MX_BEAN.getHeapMemoryUsage();
    return (double) heapUsage.getUsed() / heapUsage.getMax(); // Returns a fraction (e.g., 0.75 means 75% full)
}
String token = interner.intern(tokens.next());
if (prevToken != null) {
    Pair<String, String> bigram = new ImmutablePair<>(prevToken, token);
    bigramCounts.computeIfAbsent(bigram, k -> new LongAdder()).increment();
    // Dynamically check memory pressure
    boolean tooMuchMemoryPressure = MemoryMonitor.getHeapUsageRatio() > (FLUSH_THRESHOLD_PERCENT / 100.0);
    if (tooMuchMemoryPressure) {
        doSomeFancyAnalysis(bigramCounts);
        bigramCounts.clear();
    }
}
prevToken = token;
   private static final List<GarbageCollectorMXBean> GC_BEANS =
           ManagementFactory.getGarbageCollectorMXBeans();
   public static long getTotalGCCount() {
       return GC_BEANS.stream().mapToLong(GarbageCollectorMXBean::getCollectionCount).sum();
   }
                               GCMonitor.getTotalGCCount() > lastGCCount + 5;