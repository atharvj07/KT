jdbc:mysql://localhost:3306/your_database?useSSL=false&serverTimezone=UTC
   
   HikariDataSource dataSource = new HikariDataSource();
   dataSource.setJdbcUrl("jdbc:mysql://localhost:3306/your_database?useSSL=false&serverTimezone=UTC");
   dataSource.setUsername(Data.USER.getValue());
   dataSource.setPassword(Data.PASS.getValue());

   try (Connection conn = dataSource.getConnection()) {
       DSLContext create = DSL.using(conn, SQLDialect.MYSQL);
       // Perform your query
   }
   
   DSLContext create = DSL.using(conn, SQLDialect.MYSQL);
   // Perform your query