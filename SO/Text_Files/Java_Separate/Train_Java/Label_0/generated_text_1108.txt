@Override
public boolean equals(Object o) {
    if (this == o) return true;  // Same object, equal
    if (o == null || getClass() != o.getClass()) return false;  // Check for null and class equality
    MyEntity that = (MyEntity) o;
    return id != null && id.equals(that.id);  // Compare the ID field
}

@Override
public boolean equals(Object o) {
    if (this == o) return true;  // Same object, equal
    if (o == null || getClass() != o.getClass()) return false;  // Check for null and class equality
    Book book = (Book) o;
    return isbn != null && isbn.equals(book.isbn);  // Compare the isbn field
}

@Override
public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass()) return false;
    Book book = (Book) o;
    return isbn != null && isbn.equals(book.isbn);  // Use NaturalId (isbn)
}

@Override
public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass()) return false;
    Book book = (Book) o;
    return isbn != null && isbn.equals(book.isbn);  // Use NaturalId
}

@Override
public int hashCode() {
    return isbn != null ? isbn.hashCode() : 0;  // Use NaturalId in hashCode
}

if (this == o) return true;  // Same object, equal
if (o == null || getClass() != o.getClass()) return false;  // Check for null and class equality
MyEntity that = (MyEntity) o;
return id != null && id.equals(that.id);  // Compare the ID field
if (this == o) return true;  // Same object, equal
if (o == null || getClass() != o.getClass()) return false;  // Check for null and class equality
Book book = (Book) o;
return isbn != null && isbn.equals(book.isbn);  // Compare the isbn field
if (this == o) return true;
if (o == null || getClass() != o.getClass()) return false;
Book book = (Book) o;
return isbn != null && isbn.equals(book.isbn);  // Use NaturalId (isbn)
if (this == o) return true;
if (o == null || getClass() != o.getClass()) return false;
Book book = (Book) o;
return isbn != null && isbn.equals(book.isbn);  // Use NaturalId
return isbn != null ? isbn.hashCode() : 0;  // Use NaturalId in hashCode