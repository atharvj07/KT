import org.springframework.stereotype.Service;
import org.springframework.web.reactive.function.client.WebClient;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.http.MediaType;
import reactor.core.publisher.Mono;

@Service
public class WeatherService {

    private final String APIKEY = "###";  // Your API key
    private final WebClient webClient;

    public WeatherService() {
        // Initialize WebClient with base URL
        this.webClient = WebClient.create("http://api.openweathermap.org/data/2.5/weather");
    }

    public Mono<OptumWeather> getWeatherByZipCode(String zipCode) {
        ObjectMapper obj = new ObjectMapper(); // For converting JSON to POJOs

        // Build the WebClient request
        return webClient
            .get()
            .uri(uriBuilder -> uriBuilder
                .queryParam("zip", zipCode)
                .queryParam("units", "imperial")  // Temperature in Fahrenheit
                .queryParam("appid", APIKEY)
                .build())
            .accept(MediaType.APPLICATION_JSON)
            .retrieve()
            .bodyToMono(String.class) // Get the response body as a String
            .map(response -> {
                try {
                    // Deserialize the response into a FullWeather object
                    FullWeather weatherFull = obj.readValue(response, FullWeather.class);

                    // Map the FullWeather object to the OptumWeather object
                    OptumWeather weather = new OptumWeather();
                    weather.setName(weatherFull.getName());
                    weather.setTemp(weatherFull.getMain().getTemp());
                    weather.setFeelsLikeTemp(weatherFull.getMain().getFeelsLike());

                    return weather;
                } catch (IOException e) {
                    // Handle deserialization error
                    e.printStackTrace();
                    return null;
                }
            });
    }
}

// In a controller or any other method
OptumWeather weather = weatherService.getWeatherByZipCode("94043").block();

@GetMapping("/weather/{zipCode}")
public Mono<OptumWeather> getWeather(@PathVariable String zipCode) {
    return weatherService.getWeatherByZipCode(zipCode);
}

private final String APIKEY = "###";  // Your API key
private final WebClient webClient;
public WeatherService() {
    // Initialize WebClient with base URL
    this.webClient = WebClient.create("http://api.openweathermap.org/data/2.5/weather");
}
public Mono<OptumWeather> getWeatherByZipCode(String zipCode) {
    ObjectMapper obj = new ObjectMapper(); // For converting JSON to POJOs
    // Build the WebClient request
    return webClient
        .get()
        .uri(uriBuilder -> uriBuilder
            .queryParam("zip", zipCode)
            .queryParam("units", "imperial")  // Temperature in Fahrenheit
            .queryParam("appid", APIKEY)
            .build())
        .accept(MediaType.APPLICATION_JSON)
        .retrieve()
        .bodyToMono(String.class) // Get the response body as a String
        .map(response -> {
            try {
                // Deserialize the response into a FullWeather object
                FullWeather weatherFull = obj.readValue(response, FullWeather.class);
                // Map the FullWeather object to the OptumWeather object
                OptumWeather weather = new OptumWeather();
                weather.setName(weatherFull.getName());
                weather.setTemp(weatherFull.getMain().getTemp());
                weather.setFeelsLikeTemp(weatherFull.getMain().getFeelsLike());
                return weather;
            } catch (IOException e) {
                // Handle deserialization error
                e.printStackTrace();
                return null;
            }
        });
}
return weatherService.getWeatherByZipCode(zipCode);