openapi-generator-cli generate -i your_api_spec.yaml -g spring

import io.swagger.parser.OpenAPIParser;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.paths.PathItem;

OpenAPI openAPI = new OpenAPIParser().readLocation("path-to-your-openapi.yaml", null, null);
PathItem pathItem = openAPI.getPaths().get("/your-endpoint");

// Extract request schema
RequestBody requestBody = pathItem.getPost().getRequestBody();
Schema requestSchema = requestBody.getContent().get("application/json").getSchema();

// Extract response schema
Response response = pathItem.getPost().getResponses().get("200");
Schema responseSchema = response.getContent().get("application/json").getSchema();

ObjectMapper objectMapper = new ObjectMapper();
String schemaJson = objectMapper.writeValueAsString(responseSchema);

@RestController
@RequestMapping("/api/v1")
public class SchemaController {

    @GetMapping("/response-schema")
    public ResponseEntity<String> getResponseSchema() {
        // Extract the schema using the method from above
        Schema responseSchema = getResponseSchemaFromOpenAPI(); // Use the parsing logic
        String schemaJson = convertSchemaToJson(responseSchema);
        
        return ResponseEntity.ok(schemaJson);
    }
}

Schema schema = new Schema(); // Your extracted schema
schema.setExample("new_dynamic_value"); // Dynamically modify the schema

// Serialize it back to JSON format
ObjectMapper objectMapper = new ObjectMapper();
String modifiedSchemaJson = objectMapper.writeValueAsString(schema);

@GetMapping("/response-schema")
public ResponseEntity<String> getResponseSchema() {
    // Extract the schema using the method from above
    Schema responseSchema = getResponseSchemaFromOpenAPI(); // Use the parsing logic
    String schemaJson = convertSchemaToJson(responseSchema);
    
    return ResponseEntity.ok(schemaJson);
}