pc1, value1, value2
pc2, value3, value4

sc1, value5, value6
sc2, value7, value8

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.junit.jupiter.api.ParameterizedTest;
import org.junit.jupiter.api.MethodSource;
import static org.junit.jupiter.api.Assertions.assertEquals;

import java.util.List;

class MyTest {

    // This would be your main calculation logic method
    public Object calculate(List<String> purchaseConfigs, List<String> salesConfigs, String inputValue) {
        // Your calculation logic here
        return "result";
    }

    // Method to read the configuration from CSV or any other source
    static List<List<String>> loadConfigsFromCSV(String fileName) {
        // You can use a library like OpenCSV or manually read the CSV here
        // Assuming you read and return as a list of List<String> for each row
        return List.of(List.of("pc1", "value1", "value2"), List.of("pc2", "value3", "value4"));
    }

    // Test that reads external files and generates parameters dynamically
    @ParameterizedTest
    @MethodSource("provideTestData")
    void testWithExternalConfig(List<String> purchaseConfigs, List<String> salesConfigs, String inputValue, String expectedValue) {
        // When
        Object result = calculate(purchaseConfigs, salesConfigs, inputValue);

        // Then
        assertEquals(expectedValue, result);
    }

    // This method is the @MethodSource for parameterized test
    static List<Object[]> provideTestData() {
        // Externalize your CSV or JSON files loading here
        List<List<String>> purchaseConfigs = loadConfigsFromCSV("purchase_configs.csv");
        List<List<String>> salesConfigs = loadConfigsFromCSV("sales_configs.csv");

        // Return combinations of configurations, input_value, and expected_value
        return List.of(
            new Object[] { purchaseConfigs.get(0), salesConfigs.get(0), "input1", "expected1" },
            new Object[] { purchaseConfigs.get(1), salesConfigs.get(1), "input2", "expected2" }
        );
    }
}

@ParameterizedTest
@CsvFileSource(resources = "/test-data.csv", numLinesToSkip = 1)
void testWithCSVFile(String purchaseConfig, String salesConfig, String inputValue, String expectedValue) {
    // When
    Object result = calculate(List.of(purchaseConfig), List.of(salesConfig), inputValue);

    // Then
    assertEquals(expectedValue, result);
}

// This would be your main calculation logic method
public Object calculate(List<String> purchaseConfigs, List<String> salesConfigs, String inputValue) {
    // Your calculation logic here
    return "result";
}
// Method to read the configuration from CSV or any other source
static List<List<String>> loadConfigsFromCSV(String fileName) {
    // You can use a library like OpenCSV or manually read the CSV here
    // Assuming you read and return as a list of List<String> for each row
    return List.of(List.of("pc1", "value1", "value2"), List.of("pc2", "value3", "value4"));
}
// Test that reads external files and generates parameters dynamically
@ParameterizedTest
@MethodSource("provideTestData")
void testWithExternalConfig(List<String> purchaseConfigs, List<String> salesConfigs, String inputValue, String expectedValue) {
    // When
    Object result = calculate(purchaseConfigs, salesConfigs, inputValue);
    // Then
    assertEquals(expectedValue, result);
}
// This method is the @MethodSource for parameterized test
static List<Object[]> provideTestData() {
    // Externalize your CSV or JSON files loading here
    List<List<String>> purchaseConfigs = loadConfigsFromCSV("purchase_configs.csv");
    List<List<String>> salesConfigs = loadConfigsFromCSV("sales_configs.csv");
    // Return combinations of configurations, input_value, and expected_value
    return List.of(
        new Object[] { purchaseConfigs.get(0), salesConfigs.get(0), "input1", "expected1" },
        new Object[] { purchaseConfigs.get(1), salesConfigs.get(1), "input2", "expected2" }
    );
}
// When
Object result = calculate(List.of(purchaseConfig), List.of(salesConfig), inputValue);
// Then
assertEquals(expectedValue, result);