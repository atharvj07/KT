when(Util.utilFuncCallingService(service)).thenReturn("mock-response");
     
     String realResponse = Util.utilFuncCallingService(service);  // This calls doSomething()
     when(realResponse).thenReturn("mock-response");  // But this is meaningless
     
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.powermock.api.mockito.PowerMockito;
import org.powermock.core.classloader.annotations.PrepareForTest;
import org.powermock.modules.junit4.PowerMockRunner;

@RunWith(PowerMockRunner.class)
@PrepareForTest(Util.class)
public class MyClassTest {
    @Mock
    private Service service;

    @Before
    public void setup() {
        PowerMockito.mockStatic(Util.class);
        PowerMockito.when(Util.utilFuncCallingService(service)).thenReturn("mock-response");
    }

    @Test
    public void test() {
        MyClass myClass = new MyClass(service);

        myClass.method();  // Should now correctly return "mock-response"
    }
}

public class MyClass {
    private final Service service;
    private final Util util;  // Injected instance instead of static

    public MyClass(Service service, Util util) {
        this.service = service;
        this.util = util;
    }

    public void method() {
        System.out.println("Response: " + util.utilFuncCallingService(service));
    }
}

@RunWith(MockitoJUnitRunner.class)
public class MyClassTest {
    @Mock
    private Service service;
    
    @Mock
    private Util util;

    @Before
    public void setup() {
        when(util.utilFuncCallingService(service)).thenReturn("mock-response");
    }

    @Test
    public void test() {
        MyClass myClass = new MyClass(service, util);
        myClass.method();
    }
}

 ```java
 when(Util.utilFuncCallingService(service)).thenReturn("mock-response");
 ```
 - Instead, it would **actually execute** `Util.utilFuncCallingService(service)` during test setup, calling `service.doSomething()`, which explains why `"Util class calling doSomething on service"` is printed **twice**.
 ```java
 String realResponse = Util.utilFuncCallingService(service);  // This calls doSomething()
 when(realResponse).thenReturn("mock-response");  // But this is meaningless
 ```
@Mock
private Service service;
@Before
public void setup() {
    PowerMockito.mockStatic(Util.class);
    PowerMockito.when(Util.utilFuncCallingService(service)).thenReturn("mock-response");
}
@Test
public void test() {
    MyClass myClass = new MyClass(service);
    myClass.method();  // Should now correctly return "mock-response"
}
private final Service service;
private final Util util;  // Injected instance instead of static
public MyClass(Service service, Util util) {
    this.service = service;
    this.util = util;
}
public void method() {
    System.out.println("Response: " + util.utilFuncCallingService(service));
}
@Mock
private Service service;
@Mock
private Util util;
@Before
public void setup() {
    when(util.utilFuncCallingService(service)).thenReturn("mock-response");
}
@Test
public void test() {
    MyClass myClass = new MyClass(service, util);
    myClass.method();
}