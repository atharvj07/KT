SELECT c
FROM C c
JOIN FETCH c.b b
JOIN b.a a
WHERE a.otherField = :otherFieldValue
AND c.id = (
    SELECT MAX(c2.id)
    FROM C c2
    JOIN c2.b b2
    JOIN b2.a a2
    WHERE a2.id = a.id
    GROUP BY b2.id
)