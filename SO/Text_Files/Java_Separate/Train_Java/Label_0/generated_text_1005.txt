import org.apache.activemq.artemis.core.config.CoreQueueConfiguration;
import org.apache.activemq.artemis.core.config.impl.ConfigurationImpl;
import org.apache.activemq.artemis.core.server.ActiveMQServer;
import org.apache.activemq.artemis.core.server.ActiveMQServers;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.Collections;

@Configuration
public class ArtemisConfig {
    @Bean
    public ActiveMQServer activeMQServer() throws Exception {
        ConfigurationImpl config = new ConfigurationImpl();
        config.setPersistenceEnabled(false);
        config.setSecurityEnabled(false);
        config.addAcceptorConfiguration("tcp", "tcp://0.0.0.0:61616");
        config.setQueueConfigurations(Collections.singletonList(new CoreQueueConfiguration()
                .setName("my-queue-1")
                .setAddress("my-queue-1")
                .setDurable(true)
        ));

        ActiveMQServer server = ActiveMQServers.newActiveMQServer(config);
        server.start();
        return server;
    }
}

# Remove these as they conflict with the custom broker setup
# spring.artemis.mode=embedded
# spring.artemis.embedded.server-id=54321
# spring.artemis.embedded.queues=my-queue-1
# spring.artemis.embedded.enabled=true

@Bean
public ActiveMQConnectionFactory activeMQConnectionFactory() {
    return new ActiveMQConnectionFactory("tcp://localhost:61616");
}

@Bean
public JmsTemplate jmsTemplate() {
    JmsTemplate template = new JmsTemplate();
    template.setConnectionFactory(activeMQConnectionFactory());
    template.setPubSubDomain(false);
    template.setDefaultDestinationName(defaultDestination);
    return template;
}

@Bean
public ActiveMQServer activeMQServer() throws Exception {
    ConfigurationImpl config = new ConfigurationImpl();
    config.setPersistenceEnabled(false);
    config.setSecurityEnabled(false);
    config.addAcceptorConfiguration("tcp", "tcp://0.0.0.0:61616");
    config.setQueueConfigurations(Collections.singletonList(new CoreQueueConfiguration()
            .setName("my-queue-1")
            .setAddress("my-queue-1")
            .setDurable(true)
    ));
    ActiveMQServer server = ActiveMQServers.newActiveMQServer(config);
    server.start();
    return server;
}
return new ActiveMQConnectionFactory("tcp://localhost:61616");
JmsTemplate template = new JmsTemplate();
template.setConnectionFactory(activeMQConnectionFactory());
template.setPubSubDomain(false);
template.setDefaultDestinationName(defaultDestination);
return template;