@Component
public class FileProcessingTasklet implements Tasklet {

    @Autowired
    private MyRepository myRepository;

    @Override
    @Transactional
    public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
        List<MyRecord> records = fetchRecordsFromFile();
        
        for (MyRecord record : records) {
            if (validateRecord(record)) {
                myRepository.save(record);  // Save each record immediately
            }
        }

        return RepeatStatus.FINISHED;
    }

    // Example of a record validation method
    private boolean validateRecord(MyRecord record) {
        // Validation logic
        return true;
    }

    // Example of a method to fetch records
    private List<MyRecord> fetchRecordsFromFile() {
        // File reading logic
        return new ArrayList<>();
    }
}

@Autowired
private JdbcTemplate jdbcTemplate;

@Override
public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
    List<MyRecord> records = fetchRecordsFromFile();
    
    for (MyRecord record : records) {
        if (validateRecord(record)) {
            jdbcTemplate.update("INSERT INTO my_table (column1, column2) VALUES (?, ?)", record.getField1(), record.getField2());
            // Commit logic could be done here (e.g., commit manually after every record)
        }
    }

    return RepeatStatus.FINISHED;
}

@Bean
public ItemWriter<MyRecord> itemWriter() {
    return new ItemWriter<MyRecord>() {
        @Override
        @Transactional
        public void write(List<? extends MyRecord> items) throws Exception {
            for (MyRecord record : items) {
                if (validateRecord(record)) {
                    myRepository.save(record); // Save each record immediately
                }
            }
        }
    };
}

@Bean
public Step step1() {
    return stepBuilderFactory.get("step1")
        .<MyRecord, MyRecord> chunk(1)  // Commit after every record
        .reader(itemReader())
        .processor(itemProcessor())
        .writer(itemWriter())
        .build();
}

@Bean
public Step step() {
    return stepBuilderFactory.get("step")
        .tasklet(myTasklet())
        .listener(new StepExecutionListener() {
            @Override
            public void beforeStep(StepExecution stepExecution) {
                // Optionally open or configure a transaction here
            }

            @Override
            public ExitStatus afterStep(StepExecution stepExecution) {
                // Optionally commit or roll back
                return ExitStatus.COMPLETED;
            }
        })
        .build();
}

@Override
public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
    List<MyRecord> records = fetchRecordsFromFile();
    
    for (MyRecord record : records) {
        if (validateRecord(record)) {
            myRepository.save(record);  // Save immediately
        }
        // You could also return `RepeatStatus.CONTINUABLE` for more complex flows
    }

    return RepeatStatus.FINISHED;
}

@Autowired
private MyRepository myRepository;
@Override
@Transactional
public RepeatStatus execute(StepContribution contribution, ChunkContext chunkContext) throws Exception {
    List<MyRecord> records = fetchRecordsFromFile();
    
    for (MyRecord record : records) {
        if (validateRecord(record)) {
            myRepository.save(record);  // Save each record immediately
        }
    }
    return RepeatStatus.FINISHED;
}
// Example of a record validation method
private boolean validateRecord(MyRecord record) {
    // Validation logic
    return true;
}
// Example of a method to fetch records
private List<MyRecord> fetchRecordsFromFile() {
    // File reading logic
    return new ArrayList<>();
}
List<MyRecord> records = fetchRecordsFromFile();
for (MyRecord record : records) {
    if (validateRecord(record)) {
        jdbcTemplate.update("INSERT INTO my_table (column1, column2) VALUES (?, ?)", record.getField1(), record.getField2());
        // Commit logic could be done here (e.g., commit manually after every record)
    }
}
return RepeatStatus.FINISHED;
return new ItemWriter<MyRecord>() {
    @Override
    @Transactional
    public void write(List<? extends MyRecord> items) throws Exception {
        for (MyRecord record : items) {
            if (validateRecord(record)) {
                myRepository.save(record); // Save each record immediately
            }
        }
    }
};
return stepBuilderFactory.get("step1")
    .<MyRecord, MyRecord> chunk(1)  // Commit after every record
    .reader(itemReader())
    .processor(itemProcessor())
    .writer(itemWriter())
    .build();
return stepBuilderFactory.get("step")
    .tasklet(myTasklet())
    .listener(new StepExecutionListener() {
        @Override
        public void beforeStep(StepExecution stepExecution) {
            // Optionally open or configure a transaction here
        }
        @Override
        public ExitStatus afterStep(StepExecution stepExecution) {
            // Optionally commit or roll back
            return ExitStatus.COMPLETED;
        }
    })
    .build();
List<MyRecord> records = fetchRecordsFromFile();
for (MyRecord record : records) {
    if (validateRecord(record)) {
        myRepository.save(record);  // Save immediately
    }
    // You could also return `RepeatStatus.CONTINUABLE` for more complex flows
}
return RepeatStatus.FINISHED;