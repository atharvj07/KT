@Override
public void serialize(Zoo zoo, JsonGenerator jg, SerializerProvider sp) throws IOException {
    jg.writeStartObject();
    for (Animal animal: zoo.animals) {
        jg.writeNullField(animal.getClass().getSimpleName()); // ? This writes only the class name, not fields
    }
    jg.writeEndObject();
}

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.WRAPPER_ELEMENT)
@JsonSubTypes({
    @JsonSubTypes.Type(value = Dog.class, name = "Dog")
})
public abstract class Animal {}

import com.fasterxml.jackson.annotation.JsonProperty;

public class Dog extends Animal {
    @JsonProperty
    public String breed;
    @JsonProperty
    public String age;

    public Dog() {}  // Jackson needs a default constructor

    public Dog(String breed, String age) {
        this.breed = breed;
        this.age = age;
    }
}

import com.fasterxml.jackson.dataformat.xml.annotation.JacksonXmlElementWrapper;
import com.fasterxml.jackson.dataformat.xml.annotation.JacksonXmlRootElement;
import java.util.*;

@JacksonXmlRootElement(localName = "Zoo")
public class Zoo {
    @JacksonXmlElementWrapper(useWrapping = false)  // Prevents nested <animals> tag
    public List<Animal> animals = new ArrayList<>();
}

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.dataformat.xml.XmlMapper;

public class TestZoo {
    public static void main(String[] args) {
        XmlMapper xmlMapper = new XmlMapper();
        try {
            Zoo zoo = new Zoo();
            Dog dog = new Dog("Collie", "6");
            zoo.animals.add(dog);

            System.out.println(xmlMapper.writerWithDefaultPrettyPrinter().writeValueAsString(zoo));
        } catch (JsonProcessingException e) {
            e.printStackTrace();
        }
    }
}

<Zoo>
  <Dog>
    <breed>Collie</breed>
    <age>6</age>
  </Dog>
</Zoo>

jg.writeStartObject();
for (Animal animal: zoo.animals) {
    jg.writeNullField(animal.getClass().getSimpleName()); // ? This writes only the class name, not fields
}
jg.writeEndObject();
@JsonSubTypes.Type(value = Dog.class, name = "Dog")
@JsonProperty
public String breed;
@JsonProperty
public String age;
public Dog() {}  // Jackson needs a default constructor
public Dog(String breed, String age) {
    this.breed = breed;
    this.age = age;
}
@JacksonXmlElementWrapper(useWrapping = false)  // Prevents nested <animals> tag
public List<Animal> animals = new ArrayList<>();
public static void main(String[] args) {
    XmlMapper xmlMapper = new XmlMapper();
    try {
        Zoo zoo = new Zoo();
        Dog dog = new Dog("Collie", "6");
        zoo.animals.add(dog);
        System.out.println(xmlMapper.writerWithDefaultPrettyPrinter().writeValueAsString(zoo));
    } catch (JsonProcessingException e) {
        e.printStackTrace();
    }
}
<breed>Collie</breed>
<age>6</age>