import org.eclipse.jetty.server.Request;
import org.eclipse.jetty.server.handler.ResourceHandler;
import org.eclipse.jetty.util.URIUtil;
import org.eclipse.jetty.http.HttpURI;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.io.IOException;
import java.util.Map;

public class MyJettyHandler extends ResourceHandler {

    @Override
    public void handle(String target, Request baseRequest, HttpServletRequest request, HttpServletResponse response)
            throws IOException, ServletException {

        StringBuilder fileURL = new StringBuilder(target);

        // Remove session from URL and put it on env
        ProcessURLSubst(fileURL, env);

        // Map WOPI query to new query with changed QueryString
        WOPIRedirectURL(baseRequest, fileURL, qs, session);

        if (qs.size() > 0) {
            // Clone the original request to modify it
            Request modifiedRequest = cloneRequest(baseRequest, qs);

            // Handle static file (with the modified request)
            String new_path = sFileURL.toString();
            modifiedRequest.setMethod("GET");

            // Set the new URI
            HttpURI newHttpURI = new HttpURI(new_path);
            modifiedRequest.setHttpURI(newHttpURI);

            // Set the context (if necessary)
            modifiedRequest.setContext(modifiedRequest.getContext(), new_path);

            super.handle(new_path, modifiedRequest, request, response);
        }
    }

    private Request cloneRequest(Request originalRequest, Map<String, String> newQueryParameters) {
        // Create a new Request object based on the original
        Request clonedRequest = new Request(originalRequest.getServer(), originalRequest.getConnector(), originalRequest.getInputStream());

        // Copy headers, attributes, and other properties from the original request
        clonedRequest.setAttributes(originalRequest.getAttributes());

        // Set the new query parameters
        clonedRequest.setQueryParameters(newQueryParameters);

        return clonedRequest;
    }

    private void ProcessURLSubst(StringBuilder fileURL, Map<String, Object> env) {
        // Your logic to remove session from URL and put it in the environment
    }

    private void WOPIRedirectURL(Request baseRequest, StringBuilder fileURL, Map<String, String> qs, Object session) {
        // Your logic to modify the WOPI query with GET/POST and other parameters
    }
}

@Override
public void handle(String target, Request baseRequest, HttpServletRequest request, HttpServletResponse response)
        throws IOException, ServletException {
    StringBuilder fileURL = new StringBuilder(target);
    // Remove session from URL and put it on env
    ProcessURLSubst(fileURL, env);
    // Map WOPI query to new query with changed QueryString
    WOPIRedirectURL(baseRequest, fileURL, qs, session);
    if (qs.size() > 0) {
        // Clone the original request to modify it
        Request modifiedRequest = cloneRequest(baseRequest, qs);
        // Handle static file (with the modified request)
        String new_path = sFileURL.toString();
        modifiedRequest.setMethod("GET");
        // Set the new URI
        HttpURI newHttpURI = new HttpURI(new_path);
        modifiedRequest.setHttpURI(newHttpURI);
        // Set the context (if necessary)
        modifiedRequest.setContext(modifiedRequest.getContext(), new_path);
        super.handle(new_path, modifiedRequest, request, response);
    }
}
private Request cloneRequest(Request originalRequest, Map<String, String> newQueryParameters) {
    // Create a new Request object based on the original
    Request clonedRequest = new Request(originalRequest.getServer(), originalRequest.getConnector(), originalRequest.getInputStream());
    // Copy headers, attributes, and other properties from the original request
    clonedRequest.setAttributes(originalRequest.getAttributes());
    // Set the new query parameters
    clonedRequest.setQueryParameters(newQueryParameters);
    return clonedRequest;
}
private void ProcessURLSubst(StringBuilder fileURL, Map<String, Object> env) {
    // Your logic to remove session from URL and put it in the environment
}
private void WOPIRedirectURL(Request baseRequest, StringBuilder fileURL, Map<String, String> qs, Object session) {
    // Your logic to modify the WOPI query with GET/POST and other parameters
}