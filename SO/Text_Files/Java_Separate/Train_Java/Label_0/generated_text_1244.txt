@Service
public class CustomLogService {

  @Autowired
  private MySpringDataRepo mySpringDataRepo;

  @Transactional(propagation = Propagation.REQUIRES_NEW)  // Ensures a new transaction
  public CustomLog save(CustomLog customLog) {
     return mySpringDataRepo.save(customLog);
  }

  @Transactional(propagation = Propagation.REQUIRES_NEW)
  public CustomLog update(CustomLog customLog) {
     return mySpringDataRepo.save(customLog);
  }
}

@Async
public void convertFile(String documentId) {

    CustomLog customLog = new CustomLog();
    customLog.setStatus("IN_PROGRESS");
    customLog = customLogService.save(customLog); // Ensure it's persisted before async thread

    try {
       doConvertFunction(documentId);    
    } catch (Exception e) {
      customLog.setStatus("FAIL");
      customLogService.update(customLog);
      return;
    }

    customLog.setStatus("SUCCESS");
    customLogService.update(customLog);
}

@Bean
public PlatformTransactionManager transactionManager(EntityManagerFactory emf) {
    return new JpaTransactionManager(emf);
}

spring.jpa.properties.javax.persistence.transactionType=JTA
spring.transaction.default-timeout=30

@Resource(lookup = "java:comp/DefaultManagedExecutorService")
private ManagedExecutorService executorService;

executorService.submit(() -> convertFile(documentId));

 return mySpringDataRepo.save(customLog);
 return mySpringDataRepo.save(customLog);
CustomLog customLog = new CustomLog();
customLog.setStatus("IN_PROGRESS");
customLog = customLogService.save(customLog); // Ensure it's persisted before async thread
try {
   doConvertFunction(documentId);    
} catch (Exception e) {
  customLog.setStatus("FAIL");
  customLogService.update(customLog);
  return;
}
customLog.setStatus("SUCCESS");
customLogService.update(customLog);
return new JpaTransactionManager(emf);