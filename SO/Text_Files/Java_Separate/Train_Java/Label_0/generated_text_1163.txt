peopleTableView.getSelectionModel().clearSelection();
   
   peopleTableView.getSelectionModel().getSelectedItems().addListener((observable, oldValue, newValue) -> {
       selectedPeople.clear();
       selectedPeople.addAll(newValue);
   });
   
   List<Person> selectedItems = new ArrayList<>(peopleTableView.getSelectionModel().getSelectedItems());
   ObservableList<Person> filteredData = filteredPeople;
   
   // Find the intersection of selected items and filtered data
   selectedItems.retainAll(filteredData);
   
   // Manually select the remaining items after filtering
   peopleTableView.getSelectionModel().clearSelection();
   for (Person person : selectedItems) {
       peopleTableView.getSelectionModel().select(person);
   }
   
   selectedPeople.clear();
   selectedPeople.addAll(newValue);
   peopleTableView.getSelectionModel().select(person);