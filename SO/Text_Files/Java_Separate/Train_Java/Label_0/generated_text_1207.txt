@RestController
@RequestMapping(APIConstants.API_BASE_URI + APIConstants.MYCONTROLLER)
public class MyController extends APIRESTResource {

   @GetMapping("/activate")
   public ResponseEntity<String> activateAccount(
         @PathVariable("service") String srvc, 
         @RequestParam(name = "accId", required = true) String accIdStr) { // ? Use @RequestParam

      HttpStatus status = HttpStatus.OK;
      MediaType type = MediaType.TEXT_PLAIN;
      String payload = "Activated";

      try {
         // ? Process account IDs correctly
         List<Long> accountIdList = extractAccountIdsAsList(accIdStr);

         String datasource = getDatasourceForService(srvc);
         AccountsServiceImpl mgr = new AccountsServiceImpl(datasource);
         mgr.activateAccounts(accountIdList);

         HttpHeaders headers = new HttpHeaders();
         headers.setContentType(type);
         return new ResponseEntity<>(payload, headers, status);
      } catch (Exception ex) {
         return new ResponseEntity<>(ex.getMessage(), HttpStatus.INTERNAL_SERVER_ERROR);
      }
   }

   private List<Long> extractAccountIdsAsList(String accIdStr) throws Exception {
      String fieldName = "accId";
      validateRequiredField(fieldName, accIdStr);

      accIdStr = accIdStr.replace(" ", ""); // Remove any spaces

      String[] accIds = accIdStr.contains(",") ? accIdStr.split(",") : new String[]{accIdStr};

      List<Long> accIdList = new ArrayList<>();
      for (String accId : accIds) {
         validateFieldValueType(fieldName, accId, Long.TYPE);
         accIdList.add(Long.valueOf(accId)); // ? Use Long.valueOf() instead of `new Long()`
      }

      return accIdList;
   }
}

     @PathVariable("service") String srvc, 
     @RequestParam(name = "accId", required = true) String accIdStr) { // ? Use @RequestParam
  HttpStatus status = HttpStatus.OK;
  MediaType type = MediaType.TEXT_PLAIN;
  String payload = "Activated";
  try {
     // ? Process account IDs correctly
     List<Long> accountIdList = extractAccountIdsAsList(accIdStr);
     String datasource = getDatasourceForService(srvc);
     AccountsServiceImpl mgr = new AccountsServiceImpl(datasource);
     mgr.activateAccounts(accountIdList);
     HttpHeaders headers = new HttpHeaders();
     headers.setContentType(type);
     return new ResponseEntity<>(payload, headers, status);
  } catch (Exception ex) {
     return new ResponseEntity<>(ex.getMessage(), HttpStatus.INTERNAL_SERVER_ERROR);
  }
  String fieldName = "accId";
  validateRequiredField(fieldName, accIdStr);
  accIdStr = accIdStr.replace(" ", ""); // Remove any spaces
  String[] accIds = accIdStr.contains(",") ? accIdStr.split(",") : new String[]{accIdStr};
  List<Long> accIdList = new ArrayList<>();
  for (String accId : accIds) {
     validateFieldValueType(fieldName, accId, Long.TYPE);
     accIdList.add(Long.valueOf(accId)); // ? Use Long.valueOf() instead of `new Long()`
  }
  return accIdList;