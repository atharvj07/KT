package com.expirement;

import java.net.URL;
import java.net.URLClassLoader;

public class Main {
    public static void main(String[] args) throws Exception {
        f();
        try {
            // Wait for a while to give time for GC to run
            Thread.sleep(5000);
        } catch (InterruptedException ex) {
            throw new RuntimeException(ex);
        }
        // Add a breakpoint here to check whether the class has been unloaded
    }

    public static void f() throws Exception {
        // Create a custom URLClassLoader
        URLClassLoader cl = new URLClassLoader(new URL[]{
            Main.class.getProtectionDomain().getCodeSource().getLocation()
        });

        // Load a class dynamically
        Class<?> c = cl.loadClass("com.expirement.Loadable1");

        // Close the class loader and nullify the references
        cl.close();
        c = null;
        cl = null;

        // Suggest garbage collection
        System.gc();
    }
}

public static void main(String[] args) throws Exception {
    f();
    try {
        // Wait for a while to give time for GC to run
        Thread.sleep(5000);
    } catch (InterruptedException ex) {
        throw new RuntimeException(ex);
    }
    // Add a breakpoint here to check whether the class has been unloaded
}
public static void f() throws Exception {
    // Create a custom URLClassLoader
    URLClassLoader cl = new URLClassLoader(new URL[]{
        Main.class.getProtectionDomain().getCodeSource().getLocation()
    });
    // Load a class dynamically
    Class<?> c = cl.loadClass("com.expirement.Loadable1");
    // Close the class loader and nullify the references
    cl.close();
    c = null;
    cl = null;
    // Suggest garbage collection
    System.gc();
}