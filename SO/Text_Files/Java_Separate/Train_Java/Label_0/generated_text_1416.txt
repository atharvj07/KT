public static final String IS_RUNNING_IN_PRODUCTION = "{? = call FDS_APPS.FDS_USR_SEC_PKG2.is_running_in_production}";

public String isRunningInProduction() throws DaoException {
        
    String inProduction = "";
        
    try {

        // Prepare the callable statement with no parameters
        List<SqlParameter> paramList = new ArrayList<SqlParameter>();

        Map<String, Object> resultMap = jdbcTemplate.call(new CallableStatementCreator() {

            public OracleCallableStatement createCallableStatement(Connection connection) throws SQLException {

                // Prepare the callable statement
                OracleCallableStatement callableStatement = (OracleCallableStatement) connection
                        .prepareCall(IS_RUNNING_IN_PRODUCTION);
                
                // Register the return value of the function
                callableStatement.registerOutParameter(1, Types.CHAR);

                return callableStatement;
            }
        }, paramList);

        // Get the result from the callable statement
        inProduction = (String) resultMap.get(1);
                                  
    } catch (Exception e) {
        LOGGER.error("Error while determining if running in prod or not, PROC_NAME::[" + IS_RUNNING_IN_PRODUCTION + "]," + e);
        throw new DaoException("Error while retrieving " + IS_RUNNING_IN_PRODUCTION + e);
    }
        
    return inProduction;    
}

    
String inProduction = "";
    
try {
    // Prepare the callable statement with no parameters
    List<SqlParameter> paramList = new ArrayList<SqlParameter>();
    Map<String, Object> resultMap = jdbcTemplate.call(new CallableStatementCreator() {
        public OracleCallableStatement createCallableStatement(Connection connection) throws SQLException {
            // Prepare the callable statement
            OracleCallableStatement callableStatement = (OracleCallableStatement) connection
                    .prepareCall(IS_RUNNING_IN_PRODUCTION);
            
            // Register the return value of the function
            callableStatement.registerOutParameter(1, Types.CHAR);
            return callableStatement;
        }
    }, paramList);
    // Get the result from the callable statement
    inProduction = (String) resultMap.get(1);
                              
} catch (Exception e) {
    LOGGER.error("Error while determining if running in prod or not, PROC_NAME::[" + IS_RUNNING_IN_PRODUCTION + "]," + e);
    throw new DaoException("Error while retrieving " + IS_RUNNING_IN_PRODUCTION + e);
}
    
return inProduction;