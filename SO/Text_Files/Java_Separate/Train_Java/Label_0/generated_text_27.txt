@Override
public int getItemViewType(int position) {
    int notesCount = notesList.size();
    if (position < notesCount) {
        return LAYOUT_ONE;
    } else {
        return LAYOUT_TWO;
    }
}