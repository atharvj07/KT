GL30.glVertexAttribPointer(0, 3, GL30.GL_FLOAT, false, 7 * 4, 0);
GL30.glVertexAttribPointer(1, 4, GL30.GL_FLOAT, false, 7 * 4, 7 * 3);

GL30.glVertexAttribPointer(1, 4, GL30.GL_FLOAT, false, 7 * 4, 3 * 4);

gl_Position = vec4(position * 1000, 1);

gl_Position = vec4(position, 1.0);

gl_Position = vec4(position * 2.0 - 1.0, 1.0);

GL30.glBufferData(GL30.GL_ARRAY_BUFFER, buf, GL30.GL_STATIC_DRAW);

GL30.glBufferData(GL30.GL_ARRAY_BUFFER, buf, GL30.GL_DYNAMIC_DRAW);

System.out.println(GL30.glGetShaderInfoLog(vertexShader));
System.out.println(GL30.glGetShaderInfoLog(fragmentShader));
System.out.println(GL30.glGetProgramInfoLog(program));

buf.flip(); // Before reading
for (int i = 0; i < buf.limit() / 4; i++) {
    System.out.println(buf.getFloat(i * 4));
}

System.out.println(buf.getFloat(i * 4));