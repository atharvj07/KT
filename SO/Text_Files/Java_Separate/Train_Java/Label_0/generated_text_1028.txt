public static <T> boolean isOverlaps(T obj) { ... }

public interface HasX {
    float getX();
}

public class A implements HasX {
    public float getX() {
        return 10.5f;  // Example value
    }
}

public class B implements HasX {
    public float getX() {
        return -5.3f;  // Example value
    }
}

public static <T extends HasX> boolean isOverlaps(T obj) {
    if (obj == null) return false;
    return obj.getX() >= 0;  // Now it compiles!
}

public static boolean isOverlaps(Object obj) {
    if (obj == null) return false;
    
    try {
        Method method = obj.getClass().getMethod("getX");
        float x = (float) method.invoke(obj);
        return x >= 0;
    } catch (NoSuchMethodException | IllegalAccessException | InvocationTargetException e) {
        return false; // Method not found or inaccessible
    }
}

float getX();
public float getX() {
    return 10.5f;  // Example value
}
public float getX() {
    return -5.3f;  // Example value
}
if (obj == null) return false;
return obj.getX() >= 0;  // Now it compiles!
if (obj == null) return false;
try {
    Method method = obj.getClass().getMethod("getX");
    float x = (float) method.invoke(obj);
    return x >= 0;
} catch (NoSuchMethodException | IllegalAccessException | InvocationTargetException e) {
    return false; // Method not found or inaccessible
}