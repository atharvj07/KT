<dependency>
         <groupId>org.jline</groupId>
         <artifactId>jline</artifactId>
         <version>3.21.0</version>
     </dependency>
     
     dependencies {
         implementation 'org.jline:jline:3.21.0'
     }
     
   import org.jline.reader.LineReader;
   import org.jline.reader.LineReaderBuilder;
   import org.jline.terminal.Terminal;
   import org.jline.terminal.TerminalBuilder;

   public class Main {
       public static void main(String[] args) throws Exception {
           Terminal terminal = TerminalBuilder.terminal();
           LineReader reader = LineReaderBuilder.builder().terminal(terminal).build();

           System.out.println("Test asynchronous input started, write anything below:");

           String input = "";
           while (!input.equalsIgnoreCase("quit") && !input.equalsIgnoreCase("exit")) {
               input = reader.readLine("> "); // Non-blocking read
               System.out.println("INPUT RECEIVED: " + input);
           }
       }
   }
   
public class Main {
    public static void main(String[] args) {
        System.console().writer().println("Test asynchronous input started, write anything below:");

        String input;
        while (!(input = System.console().readLine("> ")).equalsIgnoreCase("quit")) {
            System.out.println("INPUT RECEIVED: " + input);
        }
    }
}

import java.util.Scanner;

public class Main {
    private static volatile String input = "";

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        Thread inputThread = new Thread(() -> {
            while (true) {
                input = scanner.nextLine();
            }
        });
        inputThread.setDaemon(true);
        inputThread.start();

        System.out.println("Test asynchronous input started, write anything below:");

        while (!input.equalsIgnoreCase("quit") && !input.equalsIgnoreCase("exit")) {
            try {
                Thread.sleep(200);
                if (!input.isEmpty()) {
                    System.out.println("INPUT RECEIVED: " + input);
                    input = "";
                }
            } catch (InterruptedException ignored) {}
        }

        System.out.println("Exiting...");
    }
}

 ```xml
 <dependency>
     <groupId>org.jline</groupId>
     <artifactId>jline</artifactId>
     <version>3.21.0</version>
 </dependency>
 ```
 ```gradle
 dependencies {
     implementation 'org.jline:jline:3.21.0'
 }
 ```
   public static void main(String[] args) throws Exception {
       Terminal terminal = TerminalBuilder.terminal();
       LineReader reader = LineReaderBuilder.builder().terminal(terminal).build();
       System.out.println("Test asynchronous input started, write anything below:");
       String input = "";
       while (!input.equalsIgnoreCase("quit") && !input.equalsIgnoreCase("exit")) {
           input = reader.readLine("> "); // Non-blocking read
           System.out.println("INPUT RECEIVED: " + input);
       }
   }
public static void main(String[] args) {
    System.console().writer().println("Test asynchronous input started, write anything below:");
    String input;
    while (!(input = System.console().readLine("> ")).equalsIgnoreCase("quit")) {
        System.out.println("INPUT RECEIVED: " + input);
    }
}
private static volatile String input = "";
public static void main(String[] args) {
    Scanner scanner = new Scanner(System.in);
    Thread inputThread = new Thread(() -> {
        while (true) {
            input = scanner.nextLine();
        }
    });
    inputThread.setDaemon(true);
    inputThread.start();
    System.out.println("Test asynchronous input started, write anything below:");
    while (!input.equalsIgnoreCase("quit") && !input.equalsIgnoreCase("exit")) {
        try {
            Thread.sleep(200);
            if (!input.isEmpty()) {
                System.out.println("INPUT RECEIVED: " + input);
                input = "";
            }
        } catch (InterruptedException ignored) {}
    }
    System.out.println("Exiting...");
}