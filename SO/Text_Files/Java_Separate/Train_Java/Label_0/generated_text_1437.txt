@TableField(value = "role")
private String role;

@TableField(value = "module")
private String module;

public static String byteArrayToBitString(byte[] bytes) {
    StringBuilder sb = new StringBuilder();
    for (byte b : bytes) {
        sb.append(String.format("%8s", Integer.toBinaryString(b & 0xFF)).replace(' ', '0'));
    }
    return sb.toString();
}

public static byte[] bitStringToByteArray(String bitString) {
    int length = bitString.length();
    byte[] data = new byte[length / 8 + (length % 8 == 0 ? 0 : 1)];
    for (int i = 0; i < bitString.length(); i++) {
        int byteIndex = i / 8;
        data[byteIndex] = (byte) ((data[byteIndex] << 1) | (bitString.charAt(i) == '1' ? 1 : 0));
    }
    return data;
}

@Insert("INSERT INTO agri_person_relation (role, module, ...) VALUES (CAST(#{role} AS BIT(2)), CAST(#{module} AS BIT(3)), ...)")
int insertN(PersonRelationDO personRelationDO);

@MappedJdbcTypes(JdbcType.OTHER)
@MappedTypes(byte[].class)
public class BitTypeHandler extends BaseTypeHandler<byte[]> {

    @Override
    public void setNonNullParameter(PreparedStatement ps, int i, byte[] parameter, JdbcType jdbcType) throws SQLException {
        PGobject pgObject = new PGobject();
        pgObject.setType("bit");
        pgObject.setValue(byteArrayToBitString(parameter)); 
        ps.setObject(i, pgObject);
    }

    @Override
    public byte[] getNullableResult(ResultSet rs, String columnName) throws SQLException {
        return bitStringToByteArray(rs.getString(columnName));
    }

    @Override
    public byte[] getNullableResult(ResultSet rs, int columnIndex) throws SQLException {
        return bitStringToByteArray(rs.getString(columnIndex));
    }

    @Override
    public byte[] getNullableResult(CallableStatement cs, int columnIndex) throws SQLException {
        return bitStringToByteArray(cs.getString(columnIndex));
    }
}

mybatis-plus:
    type-handlers-package: cn.iocoder.yudao.module.agri.utils

StringBuilder sb = new StringBuilder();
for (byte b : bytes) {
    sb.append(String.format("%8s", Integer.toBinaryString(b & 0xFF)).replace(' ', '0'));
}
return sb.toString();
int length = bitString.length();
byte[] data = new byte[length / 8 + (length % 8 == 0 ? 0 : 1)];
for (int i = 0; i < bitString.length(); i++) {
    int byteIndex = i / 8;
    data[byteIndex] = (byte) ((data[byteIndex] << 1) | (bitString.charAt(i) == '1' ? 1 : 0));
}
return data;
@Override
public void setNonNullParameter(PreparedStatement ps, int i, byte[] parameter, JdbcType jdbcType) throws SQLException {
    PGobject pgObject = new PGobject();
    pgObject.setType("bit");
    pgObject.setValue(byteArrayToBitString(parameter)); 
    ps.setObject(i, pgObject);
}
@Override
public byte[] getNullableResult(ResultSet rs, String columnName) throws SQLException {
    return bitStringToByteArray(rs.getString(columnName));
}
@Override
public byte[] getNullableResult(ResultSet rs, int columnIndex) throws SQLException {
    return bitStringToByteArray(rs.getString(columnIndex));
}
@Override
public byte[] getNullableResult(CallableStatement cs, int columnIndex) throws SQLException {
    return bitStringToByteArray(cs.getString(columnIndex));
}
type-handlers-package: cn.iocoder.yudao.module.agri.utils