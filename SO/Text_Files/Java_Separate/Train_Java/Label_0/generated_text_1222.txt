import org.springframework.stereotype.Component;
import org.springframework.web.servlet.HandlerInterceptor;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import java.util.*;

@Component
public class RequestParamInterceptor implements HandlerInterceptor {

    private final Set<String> allowedParams = Set.of("id", "name", "dateofbirth");

    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
        Map<String, String[]> params = request.getParameterMap();
        for (String param : params.keySet()) {
            if (!allowedParams.contains(param)) {
                response.sendError(HttpServletResponse.SC_BAD_REQUEST, "Unknown request parameter: " + param);
                return false;
            }
        }
        return true;
    }
}

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.InterceptorRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Autowired
    private RequestParamInterceptor requestParamInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(requestParamInterceptor);
    }
}

@GetMapping(path = "/users/search")
public ResponseEntity<List<User>> getUserLike(
    @RequestParam Map<String, String> allParams,
    @RequestParam(name = "id", required = false) Long id,
    @RequestParam(name = "name", required = false) String name,
    @RequestParam(name = "dateofbirth", required = false) @DateTimeFormat(pattern = "dd-MM-yyyy") LocalDate dateOfBirth
) {
    Set<String> allowedParams = Set.of("id", "name", "dateofbirth");

    for (String param : allParams.keySet()) {
        if (!allowedParams.contains(param)) {
            throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "Unknown request parameter: " + param);
        }
    }

    return userService.getUserLike(id, name, dateOfBirth);
}

/users/search?id=1&myunknownparam=unknownParam

{
    "status": 400,
    "error": "Bad Request",
    "message": "Unknown request parameter: myunknownparam"
}

private final Set<String> allowedParams = Set.of("id", "name", "dateofbirth");
@Override
public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
    Map<String, String[]> params = request.getParameterMap();
    for (String param : params.keySet()) {
        if (!allowedParams.contains(param)) {
            response.sendError(HttpServletResponse.SC_BAD_REQUEST, "Unknown request parameter: " + param);
            return false;
        }
    }
    return true;
}
@Autowired
private RequestParamInterceptor requestParamInterceptor;
@Override
public void addInterceptors(InterceptorRegistry registry) {
    registry.addInterceptor(requestParamInterceptor);
}
@RequestParam Map<String, String> allParams,
@RequestParam(name = "id", required = false) Long id,
@RequestParam(name = "name", required = false) String name,
@RequestParam(name = "dateofbirth", required = false) @DateTimeFormat(pattern = "dd-MM-yyyy") LocalDate dateOfBirth
Set<String> allowedParams = Set.of("id", "name", "dateofbirth");
for (String param : allParams.keySet()) {
    if (!allowedParams.contains(param)) {
        throw new ResponseStatusException(HttpStatus.BAD_REQUEST, "Unknown request parameter: " + param);
    }
}
return userService.getUserLike(id, name, dateOfBirth);
"status": 400,
"error": "Bad Request",
"message": "Unknown request parameter: myunknownparam"