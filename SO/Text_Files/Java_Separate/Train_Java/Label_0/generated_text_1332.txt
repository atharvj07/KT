Graphics2D g2d = bi.createGraphics();
   // Get DPI scaling factor (125% for example)
   double scalingFactor = 1.25; // Adjust based on system scaling
   g2d.scale(scalingFactor, scalingFactor);
   
   public class DrawPanel extends JComponent {
       @Override
       public void paint(Graphics gr) {
           Graphics2D g = (Graphics2D) gr;
           g.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
           g.setRenderingHint(RenderingHints.KEY_RENDERING, RenderingHints.VALUE_RENDER_QUALITY);
           // Direct drawing here, without using BufferedImage
           g.setFont(new Font("SansSerif", Font.BOLD, 36));
           g.setColor(Color.BLACK);
           g.drawString("Test String", 5, 40);
           g.setStroke(new BasicStroke(6.0001f, BasicStroke.CAP_BUTT, BasicStroke.JOIN_BEVEL));
           g.drawRect(100, 70, 100, 100);
       }
   }
   
   gSave.drawRenderedImage(bi, null);
   
   @Override
   public void paint(Graphics gr) {
       Graphics2D g = (Graphics2D) gr;
       g.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
       g.setRenderingHint(RenderingHints.KEY_RENDERING, RenderingHints.VALUE_RENDER_QUALITY);
       // Direct drawing here, without using BufferedImage
       g.setFont(new Font("SansSerif", Font.BOLD, 36));
       g.setColor(Color.BLACK);
       g.drawString("Test String", 5, 40);
       g.setStroke(new BasicStroke(6.0001f, BasicStroke.CAP_BUTT, BasicStroke.JOIN_BEVEL));
       g.drawRect(100, 70, 100, 100);
   }