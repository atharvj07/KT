TextTitle xLabel = new TextTitle("Time (s)", new Font("SansSerif", Font.PLAIN, 12));
xLabel.setPosition(RectangleEdge.BOTTOM);
chart.addSubtitle(xLabel);

TextTitle yLabel = new TextTitle("Force (mN)", new Font("SansSerif", Font.PLAIN, 12));
yLabel.setPosition(RectangleEdge.LEFT);
chart.addSubtitle(yLabel);

XYItemRenderer renderer = plot.getRenderer();
renderer.setBaseItemLabelsVisible(true);
renderer.setBaseItemLabelGenerator((dataset, series, item) -> 
        String.format("%.2f", dataset.getYValue(series, item))); // Formatting values

renderer.setBaseItemLabelFont(new Font("SansSerif", Font.BOLD, 10));
renderer.setBaseItemLabelPaint(Color.BLACK);
plot.setRenderer(renderer);

ValueMarker marker2 = new ValueMarker(4, Color.GREEN, new BasicStroke(2.0f)); 
plot.addDomainMarker(marker2, Layer.FOREGROUND);

NumberAxis rightAxis = new NumberAxis("Secondary Axis Label");
plot.setRangeAxis(1, rightAxis);
plot.mapDatasetToRangeAxis(1, 1);  // Map dataset index 1 to axis index 1

    String.format("%.2f", dataset.getYValue(series, item))); // Formatting values