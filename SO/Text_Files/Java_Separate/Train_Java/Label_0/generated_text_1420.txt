// Create a custom record search
CustomRecordSearch customRecordSearch = new CustomRecordSearch();

// Define search filters - searching by recordname (example: name field)
SearchStringField nameFilter = new SearchStringField();
nameFilter.setOperator(SearchStringFieldOperator.is);
nameFilter.setSearchValue("MyRecordName");  // Replace with the desired record name
customRecordSearch.setName(nameFilter);

// Define any other filters or columns as needed (optional)
SearchColumnStringField customFieldColumn = new SearchColumnStringField();
customFieldColumn.setFieldId("custrecord_custom_field"); // Replace with actual field ID
customRecordSearch.setColumns(new SearchColumn[] {customFieldColumn});

// Perform the search
SearchResult searchResult = netsuiteSoapClient.getPort(true).search(customRecordSearch);

// Process the search results
if (searchResult.getStatus().getIsSuccess()) {
    // Iterate over results
    Record[] records = searchResult.getRecordList().getRecord();
    for (Record record : records) {
        // Process each record (convert to custom record object, etc.)
        CustomRecord customRec = (CustomRecord) record;
        System.out.println("Custom Record ID: " + customRec.getInternalId());
        // Continue processing
    }
} else {
    System.out.println("Error in search: " + searchResult.getStatus().getStatusDetail());
}

SearchResult searchResult = netsuiteSoapClient.getPort(true).search(customRecordSearch);
if (searchResult.getTotalRecords() > searchResult.getPageSize()) {
    int pageCount = (int) Math.ceil(searchResult.getTotalRecords() / (double) searchResult.getPageSize());
    for (int i = 1; i <= pageCount; i++) {
        customRecordSearch.setPageIndex(i);
        searchResult = netsuiteSoapClient.getPort(true).search(customRecordSearch);
        // Process the results for each page
    }
}

CustomRecordRef customRecRef = new CustomRecordRef();
customRecRef.setInternalId("XXX");  // Replace with the actual internal ID
customRecRef.setScriptId("customrecord_lc_mapping");  // Replace with the actual Script ID

// Retrieve the specific record by ID
CustomRecord customRec = (CustomRecord) netsuiteSoapClient.getPort(true).get(customRecRef);
System.out.println("Retrieved Record: " + customRec.getInternalId());

// Iterate over results
Record[] records = searchResult.getRecordList().getRecord();
for (Record record : records) {
    // Process each record (convert to custom record object, etc.)
    CustomRecord customRec = (CustomRecord) record;
    System.out.println("Custom Record ID: " + customRec.getInternalId());
    // Continue processing
}
System.out.println("Error in search: " + searchResult.getStatus().getStatusDetail());
int pageCount = (int) Math.ceil(searchResult.getTotalRecords() / (double) searchResult.getPageSize());
for (int i = 1; i <= pageCount; i++) {
    customRecordSearch.setPageIndex(i);
    searchResult = netsuiteSoapClient.getPort(true).search(customRecordSearch);
    // Process the results for each page
}