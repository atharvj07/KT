import com.fasterxml.jackson.annotation.JsonIdentityInfo;
import com.fasterxml.jackson.annotation.ObjectIdGenerators;

@JsonIdentityInfo(generator = ObjectIdGenerators.PropertyGenerator.class, property = "id")
public class MyType {
    private String id;
    private String longText;

    // Getters and setters
    public String getId() {
        return id;
    }

    public void setId(String id) {
        this.id = id;
    }

    public String getLongText() {
        return longText;
    }

    public void setLongText(String longText) {
        this.longText = longText;
    }
}

import com.fasterxml.jackson.annotation.JsonIdentityReference;

import java.util.List;
import java.util.Map;

public class MyData {

    private List<MyType> types;

    @JsonIdentityReference(alwaysAsId = true)
    private Map<MyType, Integer> countMap;

    // Getters and setters
    public List<MyType> getTypes() {
        return types;
    }

    public void setTypes(List<MyType> types) {
        this.types = types;
    }

    public Map<MyType, Integer> getCountMap() {
        return countMap;
    }

    public void setCountMap(Map<MyType, Integer> countMap) {
        this.countMap = countMap;
    }
}

import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.KeyDeserializer;

public class MyTypeKeyDeserializer extends KeyDeserializer {

    @Override
    public Object deserializeKey(String key, DeserializationContext ctxt) {
        // Assuming you have a method to fetch MyType based on id
        // For example, querying an existing list or map of MyType instances by id
        MyType myType = getMyTypeById(key); // You need to implement this method

        return myType;
    }

    private MyType getMyTypeById(String id) {
        // Here, you would query your data source to find the correct MyType by its ID.
        // This could involve looking up the ID in a cache or a list that holds your MyType instances.
        // For the sake of the example, let's assume it's simple:
        MyType myType = new MyType();
        myType.setId(id); // You may need to populate other fields as well
        return myType;
    }
}

import com.fasterxml.jackson.databind.module.SimpleModule;

public class MyDataModule extends SimpleModule {
    public MyDataModule() {
        // Register the custom key deserializer for Map<MyType, Integer>
        addKeyDeserializer(MyType.class, new MyTypeKeyDeserializer());
    }
}

import com.fasterxml.jackson.databind.ObjectMapper;

public class Main {
    public static void main(String[] args) {
        try {
            ObjectMapper mapper = new ObjectMapper();
            // Register the custom module with the ObjectMapper
            mapper.registerModule(new MyDataModule());

            String json = "{\n" +
                    "\"types\": [\n" +
                    "  {\"id\": \"123\", \"longText\": \"very long text 1 ...\"},\n" +
                    "  {\"id\": \"456\", \"longText\": \"very long text 2 ...\"}\n" +
                    "],\n" +
                    "\"countMap\": {\n" +
                    "  \"123\": 100,\n" +
                    "  \"456\": 200\n" +
                    "}\n" +
                    "}";

            // Deserialize the JSON into MyData object
            MyData myData = mapper.readValue(json, MyData.class);

            // Print out the deserialized MyData object
            System.out.println(myData.getCountMap()); // Should correctly map MyType keys
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

private String id;
private String longText;
// Getters and setters
public String getId() {
    return id;
}
public void setId(String id) {
    this.id = id;
}
public String getLongText() {
    return longText;
}
public void setLongText(String longText) {
    this.longText = longText;
}
private List<MyType> types;
@JsonIdentityReference(alwaysAsId = true)
private Map<MyType, Integer> countMap;
// Getters and setters
public List<MyType> getTypes() {
    return types;
}
public void setTypes(List<MyType> types) {
    this.types = types;
}
public Map<MyType, Integer> getCountMap() {
    return countMap;
}
public void setCountMap(Map<MyType, Integer> countMap) {
    this.countMap = countMap;
}
@Override
public Object deserializeKey(String key, DeserializationContext ctxt) {
    // Assuming you have a method to fetch MyType based on id
    // For example, querying an existing list or map of MyType instances by id
    MyType myType = getMyTypeById(key); // You need to implement this method
    return myType;
}
private MyType getMyTypeById(String id) {
    // Here, you would query your data source to find the correct MyType by its ID.
    // This could involve looking up the ID in a cache or a list that holds your MyType instances.
    // For the sake of the example, let's assume it's simple:
    MyType myType = new MyType();
    myType.setId(id); // You may need to populate other fields as well
    return myType;
}
public MyDataModule() {
    // Register the custom key deserializer for Map<MyType, Integer>
    addKeyDeserializer(MyType.class, new MyTypeKeyDeserializer());
}
public static void main(String[] args) {
    try {
        ObjectMapper mapper = new ObjectMapper();
        // Register the custom module with the ObjectMapper
        mapper.registerModule(new MyDataModule());
        String json = "{\n" +
                "\"types\": [\n" +
                "  {\"id\": \"123\", \"longText\": \"very long text 1 ...\"},\n" +
                "  {\"id\": \"456\", \"longText\": \"very long text 2 ...\"}\n" +
                "],\n" +
                "\"countMap\": {\n" +
                "  \"123\": 100,\n" +
                "  \"456\": 200\n" +
                "}\n" +
                "}";
        // Deserialize the JSON into MyData object
        MyData myData = mapper.readValue(json, MyData.class);
        // Print out the deserialized MyData object
        System.out.println(myData.getCountMap()); // Should correctly map MyType keys
    } catch (Exception e) {
        e.printStackTrace();
    }
}