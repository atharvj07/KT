@RestController
public class RoutingController implements ErrorController, WebMvcConfigurer {

@Controller
public class RoutingController implements ErrorController, WebMvcConfigurer {

registry.addViewController(MAIN_APPLICATION_PATH)
    .setViewName("forward:/login.html");

registry.addViewController(MAIN_APPLICATION_PATH)
    .setViewName("login"); // Assumes login.html is in `src/main/resources/templates/`

@PostMapping("/validateLogin")
public String validateLogin(@ModelAttribute Users userInfo, Model model) {
    // For debugging
    System.out.println("Username: " + userInfo.getUsername());
    System.out.println("Password: " + userInfo.getPassword());

    // Simulate authentication
    if ("admin".equals(userInfo.getUsername()) && "password".equals(userInfo.getPassword())) {
        return "redirect:/dashboard"; // Redirect to dashboard after successful login
    }

    model.addAttribute("error", "Invalid credentials");
    return "login"; // Return login page with error message
}

public class Users {
    private String username;
    private String password;

    // Getters and Setters
}

@GetMapping("/")
public String showLoginForm(Model model) {
    model.addAttribute("userInfo", new Users());
    return "login";
}

<form th:action="@{/validateLogin}" method="post" th:object="${userInfo}">

.setViewName("forward:/login.html");
.setViewName("login"); // Assumes login.html is in `src/main/resources/templates/`
// For debugging
System.out.println("Username: " + userInfo.getUsername());
System.out.println("Password: " + userInfo.getPassword());
// Simulate authentication
if ("admin".equals(userInfo.getUsername()) && "password".equals(userInfo.getPassword())) {
    return "redirect:/dashboard"; // Redirect to dashboard after successful login
}
model.addAttribute("error", "Invalid credentials");
return "login"; // Return login page with error message
private String username;
private String password;
// Getters and Setters
model.addAttribute("userInfo", new Users());
return "login";