app -> depends on [A, B]
    B -> depends on [A]
    
   @Bean
   public ConnectionFactory connectionFactory() {
       return new ActiveMQConnectionFactory("tcp://localhost:61616");
   }
   
```
app -> depends on [A, B]
B -> depends on [A]
```
   return new ActiveMQConnectionFactory("tcp://localhost:61616");