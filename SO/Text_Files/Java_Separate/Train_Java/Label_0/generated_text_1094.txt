kafkaContainer.waitingFor(Wait.forListeningPort());

// Before sending the message, explicitly create the topic (if auto-creation is not enabled)
AdminClient adminClient = AdminClient.create(producerProps);
NewTopic newTopic = new NewTopic("tappedtechnologies.emails.recipients", 1, (short) 1);
adminClient.createTopics(Collections.singletonList(newTopic));

producerProps.put("bootstrap.servers", kafkaContainer.getBootstrapServers());

CountDownLatch latch = new CountDownLatch(1);

// In the consumer, after saving the user:
latch.countDown();

// In the test:
latch.await(10, TimeUnit.SECONDS);