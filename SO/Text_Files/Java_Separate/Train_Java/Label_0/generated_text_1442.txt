public class HomeViewModel extends ViewModel {
    private final MutableLiveData<Integer> progress = new MutableLiveData<>();

    public LiveData<Integer> getProgress() {
        return progress;
    }

    public void startTask() {
        new Thread(() -> {
            for (int i = 0; i <= 30; i++) {
                int finalI = i;
                progress.postValue(finalI);
                SystemClock.sleep(500);
            }
        }).start();
    }
}

public class HomeFragment extends Fragment {
    private HomeViewModel homeViewModel;
    private ListAdapter adapter;
    private ArrayList<String> items;

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
        return inflater.inflate(R.layout.fragment_home, container, false);
    }

    @Override
    public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {
        super.onViewCreated(view, savedInstanceState);
        
        RecyclerView mRecyclerView = view.findViewById(R.id.recyclerview);
        Activity activity = requireActivity();
        homeViewModel = new ViewModelProvider(this).get(HomeViewModel.class);

        items = new ArrayList<>(Arrays.asList("Item1", "Item2", "Item3"));
        adapter = new ListAdapter(activity, items, homeViewModel);
        mRecyclerView.setAdapter(adapter);
        mRecyclerView.setLayoutManager(new LinearLayoutManager(activity));

        homeViewModel.getProgress().observe(getViewLifecycleOwner(), value -> {
            if (adapter != null) {
                adapter.updateItem(0, "Progress: " + value);
            }
        });
    }
}

public class ListAdapter extends RecyclerView.Adapter<ListAdapter.ViewHolder> {
    private final ArrayList<String> items;
    private final LayoutInflater mInflater;
    private final HomeViewModel viewModel;

    public ListAdapter(Context context, ArrayList<String> items, HomeViewModel viewModel) {
        mInflater = LayoutInflater.from(context);
        this.items = items;
        this.viewModel = viewModel;
    }

    @NonNull
    @Override
    public ListAdapter.ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        View mItemView = mInflater.inflate(R.layout.item, parent, false);
        return new ViewHolder(mItemView, this);
    }

    @Override
    public void onBindViewHolder(@NonNull ListAdapter.ViewHolder holder, int position) {
        holder.name.setText(items.get(position));
    }

    @Override
    public int getItemCount() {
        return items.size();
    }

    public void updateItem(int position, String newValue) {
        if (position < items.size()) {
            items.set(position, newValue);
            notifyItemChanged(position);
        }
    }

    public class ViewHolder extends RecyclerView.ViewHolder {
        final TextView name;

        public ViewHolder(@NonNull View itemView, ListAdapter mAdapter) {
            super(itemView);
            this.name = itemView.findViewById(R.id.file_name);
            itemView.setOnClickListener(v -> viewModel.startTask());
        }
    }
}

private final MutableLiveData<Integer> progress = new MutableLiveData<>();
public LiveData<Integer> getProgress() {
    return progress;
}
public void startTask() {
    new Thread(() -> {
        for (int i = 0; i <= 30; i++) {
            int finalI = i;
            progress.postValue(finalI);
            SystemClock.sleep(500);
        }
    }).start();
}
private HomeViewModel homeViewModel;
private ListAdapter adapter;
private ArrayList<String> items;
@Override
public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
    return inflater.inflate(R.layout.fragment_home, container, false);
}
@Override
public void onViewCreated(@NonNull View view, @Nullable Bundle savedInstanceState) {
    super.onViewCreated(view, savedInstanceState);
    
    RecyclerView mRecyclerView = view.findViewById(R.id.recyclerview);
    Activity activity = requireActivity();
    homeViewModel = new ViewModelProvider(this).get(HomeViewModel.class);
    items = new ArrayList<>(Arrays.asList("Item1", "Item2", "Item3"));
    adapter = new ListAdapter(activity, items, homeViewModel);
    mRecyclerView.setAdapter(adapter);
    mRecyclerView.setLayoutManager(new LinearLayoutManager(activity));
    homeViewModel.getProgress().observe(getViewLifecycleOwner(), value -> {
        if (adapter != null) {
            adapter.updateItem(0, "Progress: " + value);
        }
    });
}
private final ArrayList<String> items;
private final LayoutInflater mInflater;
private final HomeViewModel viewModel;
public ListAdapter(Context context, ArrayList<String> items, HomeViewModel viewModel) {
    mInflater = LayoutInflater.from(context);
    this.items = items;
    this.viewModel = viewModel;
}
@NonNull
@Override
public ListAdapter.ViewHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
    View mItemView = mInflater.inflate(R.layout.item, parent, false);
    return new ViewHolder(mItemView, this);
}
@Override
public void onBindViewHolder(@NonNull ListAdapter.ViewHolder holder, int position) {
    holder.name.setText(items.get(position));
}
@Override
public int getItemCount() {
    return items.size();
}
public void updateItem(int position, String newValue) {
    if (position < items.size()) {
        items.set(position, newValue);
        notifyItemChanged(position);
    }
}
public class ViewHolder extends RecyclerView.ViewHolder {
    final TextView name;
    public ViewHolder(@NonNull View itemView, ListAdapter mAdapter) {
        super(itemView);
        this.name = itemView.findViewById(R.id.file_name);
        itemView.setOnClickListener(v -> viewModel.startTask());
    }
}