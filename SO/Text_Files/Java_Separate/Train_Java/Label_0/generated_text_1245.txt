@ParameterizedTest
@MethodSource("getFlags")
void shouldTestIfFlagWorks(String someFlag) {
    // Given
    Mockito.doReturn(true).when(someService).execute(someFlag);

    // When
    Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));

    // Then
    Assertions.assertThat(thrown).hasMessage("Oh no, exception!");

    // Reset invocation tracking
    Mockito.clearInvocations(someService);
}

@ParameterizedTest
@MethodSource("getFlags")
void shouldTestIfFlagWorks(String someFlag) {
    // Given
    if (!Objects.equals(someFlag, "flag1")) {
        Mockito.doReturn(true).when(someService).execute(someFlag);
    }

    // When
    Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));

    // Then
    Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
}

@ParameterizedTest
@MethodSource("getFlags")
void shouldTestIfFlagWorks(String someFlag) {
    // Given
    if (!Objects.equals(someFlag, "flag1")) {
        Mockito.lenient().doReturn(true).when(someService).execute(someFlag);
    }

    // When
    Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));

    // Then
    Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
}

@ExtendWith(MockitoExtension.class)
@MockitoSettings(strictness = Strictness.LENIENT)
class MyTest {
    // Your tests
}

@ParameterizedTest
@MethodSource("getFlagsThatNeedMocking")
void shouldTestIfFlagWorksWithMocking(String someFlag) {
    Mockito.doReturn(true).when(someService).execute(someFlag);

    Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));

    Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
}

@ParameterizedTest
@MethodSource("getFlagsThatDoNotNeedMocking")
void shouldTestIfFlagWorksWithoutMocking(String someFlag) {
    Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));

    Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
}

private static Stream<Arguments> getFlagsThatNeedMocking() {
    return Stream.of(
        Arguments.of("flag2")
    );
}

private static Stream<Arguments> getFlagsThatDoNotNeedMocking() {
    return Stream.of(
        Arguments.of("flag1")
    );
}

// Given
Mockito.doReturn(true).when(someService).execute(someFlag);
// When
Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));
// Then
Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
// Reset invocation tracking
Mockito.clearInvocations(someService);
// Given
if (!Objects.equals(someFlag, "flag1")) {
    Mockito.doReturn(true).when(someService).execute(someFlag);
}
// When
Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));
// Then
Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
// Given
if (!Objects.equals(someFlag, "flag1")) {
    Mockito.lenient().doReturn(true).when(someService).execute(someFlag);
}
// When
Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));
// Then
Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
// Your tests
Mockito.doReturn(true).when(someService).execute(someFlag);
Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));
Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
Throwable thrown = Assertions.catchThrowable(() -> serviceUnderTest.process(someFlag));
Assertions.assertThat(thrown).hasMessage("Oh no, exception!");
return Stream.of(
    Arguments.of("flag2")
);
return Stream.of(
    Arguments.of("flag1")
);