-- schema-h2.sql
   SET CASE_INSENSITIVE_IDENTIFIERS TRUE;
   create table if not exists person (
       id int auto_increment primary key,
       name varchar(50) not null,
       age int not null
   );
   
   @MybatisPlusTest
   @Sql(scripts = "classpath:schema-h2.sql")
   public class PersonMapperTest {

       @Autowired
       private PersonMapper personMapper;

       @Test
       public void testSelectCertainColumns() {
           List<Map<String, Object>> rows = personMapper.selectCertainColumns();
           // Now rows.get(0).get("name") should work without case issues
           assertNotNull(rows.get(0).get("name"));
       }
   }
   
   @TestConfiguration
   public class H2TestConfig {

       @Bean
       public DataSource dataSource() {
           return DataSourceBuilder.create()
                   .url("jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;CASE_INSENSITIVE_IDENTIFIERS=TRUE")
                   .driverClassName("org.h2.Driver")
                   .username("sa")
                   .password("")
                   .build();
       }
   }

   @MybatisPlusTest
   @Import(H2TestConfig.class)  // Import custom H2 configuration
   public class PersonMapperTest {

       @Autowired
       private PersonMapper personMapper;

       @Test
       public void testSelectCertainColumns() {
           List<Map<String, Object>> rows = personMapper.selectCertainColumns();
           assertNotNull(rows.get(0).get("name"));
       }
   }
   
   @Repository
   public interface PersonMapper extends BaseMapper<Person> {
       @Select("SELECT NAME, AGE FROM PERSON")
       public List<Map<String, Object>> selectCertainColumns();
   }
   
   @Repository
   public interface PersonMapper extends BaseMapper<Person> {
       @Select("SELECT NAME, AGE FROM PERSON")
       @Results({
           @Result(property = "name", column = "NAME"),
           @Result(property = "age", column = "AGE")
       })
       public List<Map<String, Object>> selectCertainColumns();
   }
   
   id int auto_increment primary key,
   name varchar(50) not null,
   age int not null
   @Autowired
   private PersonMapper personMapper;
   @Test
   public void testSelectCertainColumns() {
       List<Map<String, Object>> rows = personMapper.selectCertainColumns();
       // Now rows.get(0).get("name") should work without case issues
       assertNotNull(rows.get(0).get("name"));
   }
   @Bean
   public DataSource dataSource() {
       return DataSourceBuilder.create()
               .url("jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;CASE_INSENSITIVE_IDENTIFIERS=TRUE")
               .driverClassName("org.h2.Driver")
               .username("sa")
               .password("")
               .build();
   }
   @Autowired
   private PersonMapper personMapper;
   @Test
   public void testSelectCertainColumns() {
       List<Map<String, Object>> rows = personMapper.selectCertainColumns();
       assertNotNull(rows.get(0).get("name"));
   }
   @Select("SELECT NAME, AGE FROM PERSON")
   public List<Map<String, Object>> selectCertainColumns();
   @Select("SELECT NAME, AGE FROM PERSON")
   @Results({
       @Result(property = "name", column = "NAME"),
       @Result(property = "age", column = "AGE")
   })
   public List<Map<String, Object>> selectCertainColumns();