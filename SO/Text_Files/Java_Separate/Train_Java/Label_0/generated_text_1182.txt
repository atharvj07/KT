@Operation(
       summary = "Get Song CSV",
       responses = {
           @ApiResponse(
               content = @Content(
                   mediaType = "text/csv",
                   schema = @Schema(implementation = Song.class)
               )
           )
       }
   )
   @GetMapping(value = "/songs", produces = "text/csv")
   public ResponseEntity<List<Song>> getSongsCsv() {
       // Your logic here
   }
   
   @Override
   public boolean canWrite(Type type, Class<?> clazz, MediaType mediaType) {
       return super.canWrite(clazz, mediaType) && clazz == Song.class && mediaType != null && mediaType.isCompatibleWith(new MediaType("text", "csv"));
   }
   
   @Operation(
       summary = "Get Songs in CSV Format",
       responses = {
           @ApiResponse(
               content = @Content(
                   mediaType = "text/csv",
                   schema = @Schema(implementation = Song.class, example = "song_id,song_name\n1,Test Song")
               )
           )
       }
   )
   @GetMapping(value = "/songs", produces = "text/csv")
   public ResponseEntity<List<Song>> getSongsCsv() {
       // Your logic to return CSV response
   }
   
   summary = "Get Song CSV",
   responses = {
       @ApiResponse(
           content = @Content(
               mediaType = "text/csv",
               schema = @Schema(implementation = Song.class)
           )
       )
   }
   // Your logic here
   return super.canWrite(clazz, mediaType) && clazz == Song.class && mediaType != null && mediaType.isCompatibleWith(new MediaType("text", "csv"));
   summary = "Get Songs in CSV Format",
   responses = {
       @ApiResponse(
           content = @Content(
               mediaType = "text/csv",
               schema = @Schema(implementation = Song.class, example = "song_id,song_name\n1,Test Song")
           )
       )
   }
   // Your logic to return CSV response