import java.util.random.RandomGenerator;
import java.util.concurrent.ThreadLocalRandom;

public class ThreadSafeRandomGenerator {

    private static final ThreadLocal<RandomGenerator> threadLocalRandom = 
        ThreadLocal.withInitial(ThreadLocalRandom::current);

    public static RandomGenerator current() {
        return threadLocalRandom.get();
    }
}

import java.util.random.RandomGenerator;
import java.util.SplittableRandom;
import java.util.concurrent.ForkJoinPool;

public class SplittableRandomGenerator {

    private static final SplittableRandom mainRandom = new SplittableRandom();

    public static RandomGenerator current() {
        return new RandomGenerator() {
            private final SplittableRandom random = mainRandom.split(); // Split when needed

            @Override
            public int nextInt() {
                return random.nextInt();
            }

            @Override
            public long nextLong() {
                return random.nextLong();
            }

            // Implement other methods similarly
        };
    }

    public static void main(String[] args) {
        ForkJoinPool forkJoinPool = new ForkJoinPool();
        forkJoinPool.submit(() -> {
            RandomGenerator generator = current();
            System.out.println(generator.nextInt());
        }).join();
    }
}

private static final ThreadLocal<RandomGenerator> threadLocalRandom = 
    ThreadLocal.withInitial(ThreadLocalRandom::current);
public static RandomGenerator current() {
    return threadLocalRandom.get();
}
private static final SplittableRandom mainRandom = new SplittableRandom();
public static RandomGenerator current() {
    return new RandomGenerator() {
        private final SplittableRandom random = mainRandom.split(); // Split when needed
        @Override
        public int nextInt() {
            return random.nextInt();
        }
        @Override
        public long nextLong() {
            return random.nextLong();
        }
        // Implement other methods similarly
    };
}
public static void main(String[] args) {
    ForkJoinPool forkJoinPool = new ForkJoinPool();
    forkJoinPool.submit(() -> {
        RandomGenerator generator = current();
        System.out.println(generator.nextInt());
    }).join();
}