Users
      ??? user1
      ?   ??? Products
      ?       ??? product1
      ?       ??? product2
      ??? user2
      ?   ??? Products
      ?       ??? product1
      ?       ??? product2
    
private void loadAllProducts() {
    productList = new ArrayList<>();

    DatabaseReference ref = FirebaseDatabase.getInstance().getReference("Users");

    // This will fetch all users and their associated products
    ref.addValueEventListener(new ValueEventListener() {
        @Override
        public void onDataChange(@NonNull DataSnapshot dataSnapshot) {
            productList.clear();

            // Iterate over all users
            for (DataSnapshot userSnapshot : dataSnapshot.getChildren()) {
                // Fetch the "Products" child for each user
                DataSnapshot productsSnapshot = userSnapshot.child("Products");

                // Iterate over all products for this user
                for (DataSnapshot productSnapshot : productsSnapshot.getChildren()) {
                    ModelProduct modelProduct = productSnapshot.getValue(ModelProduct.class);
                    if (modelProduct != null) {
                        productList.add(modelProduct); // Add product to the list
                    }
                }
            }

            // Set up your adapter
            adapterProductBuyer = new AdapterProductBuyer(ShopDetailsActivity.this, productList);
            productRv.setAdapter(adapterProductBuyer);
        }

        @Override
        public void onCancelled(@NonNull DatabaseError databaseError) {
            // Handle error here if needed
        }
    });
}

  {
    "rules": {
      "Users": {
        ".read": "auth != null",  // Ensure users are authenticated
        ".write": "auth != null"
      }
    }
  }
  
```
Users
  ??? user1
  ?   ??? Products
  ?       ??? product1
  ?       ??? product2
  ??? user2
  ?   ??? Products
  ?       ??? product1
  ?       ??? product2
```
productList = new ArrayList<>();
DatabaseReference ref = FirebaseDatabase.getInstance().getReference("Users");
// This will fetch all users and their associated products
ref.addValueEventListener(new ValueEventListener() {
    @Override
    public void onDataChange(@NonNull DataSnapshot dataSnapshot) {
        productList.clear();
        // Iterate over all users
        for (DataSnapshot userSnapshot : dataSnapshot.getChildren()) {
            // Fetch the "Products" child for each user
            DataSnapshot productsSnapshot = userSnapshot.child("Products");
            // Iterate over all products for this user
            for (DataSnapshot productSnapshot : productsSnapshot.getChildren()) {
                ModelProduct modelProduct = productSnapshot.getValue(ModelProduct.class);
                if (modelProduct != null) {
                    productList.add(modelProduct); // Add product to the list
                }
            }
        }
        // Set up your adapter
        adapterProductBuyer = new AdapterProductBuyer(ShopDetailsActivity.this, productList);
        productRv.setAdapter(adapterProductBuyer);
    }
    @Override
    public void onCancelled(@NonNull DatabaseError databaseError) {
        // Handle error here if needed
    }
});
"rules": {
  "Users": {
    ".read": "auth != null",  // Ensure users are authenticated
    ".write": "auth != null"
  }
}