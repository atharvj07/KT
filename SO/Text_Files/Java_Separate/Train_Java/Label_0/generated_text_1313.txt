@Entity
@Table(name = "product")
public class Product {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)  // Auto-generated by the DB
    private Long id;

    private String name;

    // Optionally, embed other parts of the key
    @Embeddable
    public static class ProductId implements Serializable {
        private String name;

        // Getters and setters
    }
}

@Entity
@Table(name = "product")
@EntityListeners(ProductListener.class)
public class Product {
    @EmbeddedId
    private ProductId id;

    private String name;

    @Embeddable
    public static class ProductId implements Serializable {
        private Long id;
        
        // Getters and setters
    }
}

public class ProductListener {
    @PostPersist
    public void assignAutoGeneratedId(Product product) {
        // After entity is persisted, set the auto-generated id
        Long generatedId = product.getId().getId();  // Hibernate would auto-generate this in the DB
        product.getId().setId(generatedId);
    }
}

@Entity
@Table(name = "product")
public class Product {
    @EmbeddedId
    private ProductId id;

    private String name;

    @Embeddable
    public static class ProductId implements Serializable {
        @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "product_seq")
        @SequenceGenerator(name = "product_seq", sequenceName = "product_seq", allocationSize = 1)
        private Long id;
    }
}

@Entity
@Table(name = "product")
@IdClass(ProductId.class)
public class Product {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Id
    private String name;

    // Other fields, getters, setters, etc.
}

public class ProductId implements Serializable {
    private Long id;
    private String name;

    // Default constructor, equals(), hashCode(), etc.
}

@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)  // Auto-generated by the DB
private Long id;
private String name;
// Optionally, embed other parts of the key
@Embeddable
public static class ProductId implements Serializable {
    private String name;
    // Getters and setters
}
@EmbeddedId
private ProductId id;
private String name;
@Embeddable
public static class ProductId implements Serializable {
    private Long id;
    
    // Getters and setters
}
@PostPersist
public void assignAutoGeneratedId(Product product) {
    // After entity is persisted, set the auto-generated id
    Long generatedId = product.getId().getId();  // Hibernate would auto-generate this in the DB
    product.getId().setId(generatedId);
}
@EmbeddedId
private ProductId id;
private String name;
@Embeddable
public static class ProductId implements Serializable {
    @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "product_seq")
    @SequenceGenerator(name = "product_seq", sequenceName = "product_seq", allocationSize = 1)
    private Long id;
}
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
@Id
private String name;
// Other fields, getters, setters, etc.
private Long id;
private String name;
// Default constructor, equals(), hashCode(), etc.