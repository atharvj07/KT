@Configuration
   public class JacksonConfig {
   
       @Bean
       public ObjectMapper objectMapper() {
           ObjectMapper objectMapper = new ObjectMapper();
           objectMapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);
           return objectMapper;
       }
   }
   
   @GetMapping
   public ResponseEntity<?> getData(@RequestBody InputParametersDTO inputParametersDTO) {
       float lat = inputParametersDTO.getLatitude(); // 52.52
       String endDate = inputParametersDTO.getEndDate(); // Should not be null
       // DO OTHER STUFF
   }
   
   @GetMapping
   public ResponseEntity<?> getData(
           @RequestParam float latitude,
           @RequestParam float longitude,
           @RequestParam String startDate,
           @RequestParam String endDate) {
       // handle parameters
   }
   
   {
       "latitude": 52.52,
       "longitude": 13.41,
       "start_date": "2023-05-08",
       "end_date": "2023-05-22"
   }
   
   @GetMapping
   public ResponseEntity<?> getData(@RequestBody InputParametersDTO inputParametersDTO) {
       System.out.println("Received input: " + inputParametersDTO);
       // Other logic
   }
   
import com.fasterxml.jackson.annotation.JsonProperty;
import lombok.*;

@AllArgsConstructor
@NoArgsConstructor
@Getter
@Setter
@Builder
public class InputParametersDTO {

    private float latitude;
    private float longitude;

    @JsonProperty("start_date")
    private String startDate;

    @JsonProperty("end_date")
    private String endDate;
}

@RestController
@RequestMapping("/api/v1/demo")
public class OpenMeteoController {

    @PostMapping
    @ResponseBody
    public ResponseEntity<?> getData(@RequestBody InputParametersDTO inputParametersDTO) {
        float lat = inputParametersDTO.getLatitude();
        String endDate = inputParametersDTO.getEndDate();
        // Process other logic
        return ResponseEntity.ok().body("Data processed successfully.");
    }
}

   @Bean
   public ObjectMapper objectMapper() {
       ObjectMapper objectMapper = new ObjectMapper();
       objectMapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);
       return objectMapper;
   }
   float lat = inputParametersDTO.getLatitude(); // 52.52
   String endDate = inputParametersDTO.getEndDate(); // Should not be null
   // DO OTHER STUFF
       @RequestParam float latitude,
       @RequestParam float longitude,
       @RequestParam String startDate,
       @RequestParam String endDate) {
   // handle parameters
   "latitude": 52.52,
   "longitude": 13.41,
   "start_date": "2023-05-08",
   "end_date": "2023-05-22"
   System.out.println("Received input: " + inputParametersDTO);
   // Other logic
private float latitude;
private float longitude;
@JsonProperty("start_date")
private String startDate;
@JsonProperty("end_date")
private String endDate;
@PostMapping
@ResponseBody
public ResponseEntity<?> getData(@RequestBody InputParametersDTO inputParametersDTO) {
    float lat = inputParametersDTO.getLatitude();
    String endDate = inputParametersDTO.getEndDate();
    // Process other logic
    return ResponseEntity.ok().body("Data processed successfully.");
}