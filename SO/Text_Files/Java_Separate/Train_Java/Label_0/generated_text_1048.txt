import java.util.Optional;

public class CommandExecutor {

    public static void main(String[] args) {
        int cr = execCmds();
        System.out.println("Final return code: " + cr);
    }

    private static int execCmds() {
        return checkAndExecute("my command")
                .orElseGet(() -> checkAndExecute("my next command")
                .orElse(0)); // Continue executing commands if previous ones succeed
    }

    private static Optional<Integer> checkAndExecute(String command) {
        int result = executeCmd(command);
        return (result != 0) ? Optional.of(result) : Optional.empty();
    }

    private static int executeCmd(String command) {
        // Simulating command execution (Replace this with actual command logic)
        System.out.println("Executing: " + command);
        return Math.random() > 0.5 ? 1 : 0; // Simulating success (0) or failure (1)
    }
}

import java.util.List;
import java.util.Optional;

public class CommandExecutor {

    public static void main(String[] args) {
        int cr = execCmds(List.of("my command", "my next command", "another command"));
        System.out.println("Final return code: " + cr);
    }

    private static int execCmds(List<String> commands) {
        for (String cmd : commands) {
            Optional<Integer> result = checkAndExecute(cmd);
            if (result.isPresent()) {
                return result.get(); // Stop execution and return the failure code
            }
        }
        return 0; // All commands succeeded
    }

    private static Optional<Integer> checkAndExecute(String command) {
        int result = executeCmd(command);
        return (result != 0) ? Optional.of(result) : Optional.empty();
    }

    private static int executeCmd(String command) {
        // Simulating command execution (Replace with actual logic)
        System.out.println("Executing: " + command);
        return Math.random() > 0.5 ? 1 : 0; // Simulating success (0) or failure (1)
    }
}

public static void main(String[] args) {
    int cr = execCmds();
    System.out.println("Final return code: " + cr);
}
private static int execCmds() {
    return checkAndExecute("my command")
            .orElseGet(() -> checkAndExecute("my next command")
            .orElse(0)); // Continue executing commands if previous ones succeed
}
private static Optional<Integer> checkAndExecute(String command) {
    int result = executeCmd(command);
    return (result != 0) ? Optional.of(result) : Optional.empty();
}
private static int executeCmd(String command) {
    // Simulating command execution (Replace this with actual command logic)
    System.out.println("Executing: " + command);
    return Math.random() > 0.5 ? 1 : 0; // Simulating success (0) or failure (1)
}
public static void main(String[] args) {
    int cr = execCmds(List.of("my command", "my next command", "another command"));
    System.out.println("Final return code: " + cr);
}
private static int execCmds(List<String> commands) {
    for (String cmd : commands) {
        Optional<Integer> result = checkAndExecute(cmd);
        if (result.isPresent()) {
            return result.get(); // Stop execution and return the failure code
        }
    }
    return 0; // All commands succeeded
}
private static Optional<Integer> checkAndExecute(String command) {
    int result = executeCmd(command);
    return (result != 0) ? Optional.of(result) : Optional.empty();
}
private static int executeCmd(String command) {
    // Simulating command execution (Replace with actual logic)
    System.out.println("Executing: " + command);
    return Math.random() > 0.5 ? 1 : 0; // Simulating success (0) or failure (1)
}