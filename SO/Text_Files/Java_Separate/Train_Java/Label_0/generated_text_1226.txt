plantsSelectionFragmentAdapter.removeItem(selectedPlantItem.first);

this.btnDeletePlant.setOnClickListener(v -> {
    if (selectedPlantsItems.size() > 0) {
        List<Task<Void>> deleteTasks = new ArrayList<>();

        for (Pair<Integer, PlantRecord> selectedPlantItem : selectedPlantsItems) {
            DatabaseReference removingPlantRecordRef =
                    FirebaseDatabase
                            .getInstance()
                            .getReference()
                            .child(PlantRecord.TABLE_ID)
                            .child(CurrentLoggedUser.getUserId())
                            .child(selectedPlantItem.second.id);

            Log.i(TAG, "selectedPlantItem.second.id:" + selectedPlantItem.second.id);

            // Add deletion task to a list
            deleteTasks.add(removingPlantRecordRef.removeValue());
        }

        // Wait for all deletions to complete
        Tasks.whenAll(deleteTasks)
                .addOnCompleteListener(task -> {
                    if (task.isSuccessful()) {
                        Log.i(TAG, "All deletions completed.");
                        for (Pair<Integer, PlantRecord> selectedPlantItem : selectedPlantsItems) {
                            plantsSelectionFragmentAdapter.removeItem(selectedPlantItem.first);
                        }
                    } else {
                        Log.e(TAG, "Error deleting items.");
                    }
                });
    }
});

Collections.sort(selectedPlantsItems, (a, b) -> Integer.compare(b.first, a.first)); // Sort indices in descending order
for (Pair<Integer, PlantRecord> selectedPlantItem : selectedPlantsItems) {
    plantsSelectionFragmentAdapter.removeItem(selectedPlantItem.first);
}

this.plantsDataSource.removeIf(pair -> selectedPlantsItems.contains(pair));
notifyDataSetChanged();

if (selectedPlantsItems.size() > 0) {
    List<Task<Void>> deleteTasks = new ArrayList<>();
    for (Pair<Integer, PlantRecord> selectedPlantItem : selectedPlantsItems) {
        DatabaseReference removingPlantRecordRef =
                FirebaseDatabase
                        .getInstance()
                        .getReference()
                        .child(PlantRecord.TABLE_ID)
                        .child(CurrentLoggedUser.getUserId())
                        .child(selectedPlantItem.second.id);
        Log.i(TAG, "selectedPlantItem.second.id:" + selectedPlantItem.second.id);
        // Add deletion task to a list
        deleteTasks.add(removingPlantRecordRef.removeValue());
    }
    // Wait for all deletions to complete
    Tasks.whenAll(deleteTasks)
            .addOnCompleteListener(task -> {
                if (task.isSuccessful()) {
                    Log.i(TAG, "All deletions completed.");
                    for (Pair<Integer, PlantRecord> selectedPlantItem : selectedPlantsItems) {
                        plantsSelectionFragmentAdapter.removeItem(selectedPlantItem.first);
                    }
                } else {
                    Log.e(TAG, "Error deleting items.");
                }
            });
}
plantsSelectionFragmentAdapter.removeItem(selectedPlantItem.first);