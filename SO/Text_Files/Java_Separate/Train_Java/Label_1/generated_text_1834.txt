List&lt;Integer&gt; list = Arrays.asList(1, 2, 3, 4, 5);
Optional&lt;Integer&gt; result = list
                           .stream()
                           .filter(num -&gt; num &lt; 4)
                           .findFirst();              // .findAny();